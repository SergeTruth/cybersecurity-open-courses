window.COURSE_MODULE = {
  "title": "Telemetry and Data Sources",
  "graphicAlt": "Conceptual visual of multiple telemetry sources feeding a structured hunt workspace with noted data quality gaps.",
  "narration": "Threat hunting depends on available, trustworthy telemetry. A hunter can only reason from evidence that exists, is accessible, and can be interpreted correctly. The best hypothesis will fail if the required data was never collected, is retained for too short a period, or lacks the fields needed to identify users, hosts, commands, or destinations. This is why telemetry review is part of the hunt, not a separate administrative task.\n\nCommon data sources include endpoint process events, authentication logs, DNS logs, proxy and firewall logs, EDR telemetry, cloud control-plane logs, identity-provider logs, and email security logs. Endpoint process events help answer questions about parent-child process chains, command lines, file execution, and persistence behavior. Authentication logs help answer questions about account use, source locations, device changes, and failed or successful access. DNS and proxy logs help answer questions about domains and outbound destinations. Cloud control-plane logs show administrative actions such as role changes, key creation, storage access, or unusual API calls.\n\nData quality matters as much as data presence. Missing fields can make a useful query impossible. Clock drift can make a timeline look wrong. Inconsistent hostnames can split one asset into several identities. NAT can cause many systems to share one source IP. Short log retention can hide the start of an activity chain. Noisy event volume can bury relevant events under routine software behavior. A hunt should account for these issues instead of pretending the data is perfect.\n\nTrust also has layers. An endpoint agent may be offline. A cloud log may be delayed. A firewall log may show connection metadata without application detail. An identity log may show a successful sign-in but not what the user did afterward. Hunters should understand what each source can prove and what it cannot prove. A proxy log may show that a system connected to a domain, but it may not prove which process made the request unless endpoint data is correlated.\n\nTelemetry gaps are valid hunt findings. If the hunt cannot answer a question because DNS logs are missing from a network segment or cloud administrative logs are not retained long enough, document that gap. That outcome can drive logging improvements, retention changes, parsing fixes, or new detection coverage.\n\nA beginner threat hunter should build a habit of asking three telemetry questions before querying: what source should show this behavior, what fields do I need, and what limitations could weaken my conclusion? That habit keeps the investigation grounded in evidence and prevents overconfidence.\n\nTelemetry review should also include access and privacy expectations. Hunters should use approved data sources, follow retention and handling rules, and avoid collecting more information than the hunt requires. Good hunting respects both security needs and responsible data handling. The point is to answer the defensive question with enough evidence, not to expose unrelated user or business activity.",
  "narrationPoints": [
    "Threat hunting depends on trustworthy telemetry.",
    "Common sources include endpoint, identity, DNS, proxy, firewall, cloud, and email logs.",
    "Data quality issues can change hunt conclusions.",
    "Retention, timestamps, hostnames, and NAT affect interpretation.",
    "Telemetry gaps should be documented as hunt outcomes."
  ]
};
