window.COURSE_MODULE = {
  "title": "Protecting Cloud Data",
  "graphicAlt": "Conceptual visual of cloud data protected by access control, encryption, keys, backups, and retention.",
  "narration": "Cloud data protection starts with knowing what data exists, where it lives, who owns it, who can access it, and how long it should be kept. Data may be stored in object storage, databases, file shares, queues, logs, snapshots, backups, analytics platforms, or temporary processing locations. If the team does not know where sensitive data lives, it cannot reliably protect access, encryption, retention, or deletion.\n\nClassification gives data protection decisions a foundation. Public documentation, internal operational data, customer records, credentials, regulated data, and security logs do not all need the same handling. Classification helps teams decide which storage should be private by default, which identities may access it, which logs need protection, which backups require encryption, and which data must be deleted after a defined period.\n\nEncryption is important, but it is not a complete control by itself. Encryption in transit protects data moving between clients, services, and workloads. Encryption at rest protects stored data when implemented correctly. But access control and key management matter just as much. If too many identities can read the object, query the database, or use the key, encryption will not fix the authorization problem. Keys should have owners, rotation processes, usage logging, and separation from the data they protect when possible.\n\nObject storage deserves special attention because it is widely used and easy to integrate. A storage bucket or container may hold application files, exports, backups, reports, logs, or customer data. Its access policy, public exposure setting, object-level permissions, lifecycle rules, and logging should be reviewed together. Private by default is the safer starting point. Public access should be a deliberate design choice with clear content boundaries and monitoring.\n\nBackups are part of data security, not only availability. They should be protected from casual modification or deletion, encrypted where appropriate, and tested through restore exercises. A backup that nobody can restore is not a recovery control. Retention should be intentional. Keeping sensitive data forever increases risk, while deleting data too early can break recovery, compliance, or investigation needs.\n\nA practical data review asks: what data is here, who owns it, which identities can read or change it, how is it encrypted, who controls the keys, where are backups, and when should this data be deleted? Safe cloud data protection comes from combining classification, least privilege, encryption, key management, backup testing, retention, and clear ownership.",
  "narrationPoints": [
    "Data protection starts with knowing where data lives.",
    "Access control matters as much as encryption.",
    "Keys and secrets need careful management.",
    "Backups must be protected and tested.",
    "Sensitive data needs retention and deletion rules."
  ]
};
