window.COURSE_MODULE = {
  "title": "Workloads, Images, and Runtime Risk",
  "graphicAlt": "Conceptual visual of cloud workloads including virtual machines, containers, functions, images, and runtime controls.",
  "narration": "Cloud workloads are the compute pieces that run applications and jobs. They may be virtual machines, containers, serverless functions, managed compute services, batch jobs, or application platform components. The exact runtime changes from service to service, but the security questions are consistent: what code runs, what image or template produced it, what identity does it use, what secrets can it reach, what network paths are open, and how is runtime activity monitored?\n\nImages and templates shape runtime security before the workload ever starts. A virtual machine image can include outdated packages, unnecessary services, default accounts, or weak configuration. A container image can include unused tools, vulnerable libraries, embedded secrets, or excessive permissions. A serverless package can include dependencies that nobody reviews. Secure workload design starts with maintained base images, minimal packages, approved templates, and repeatable build pipelines.\n\nPatching and vulnerability management still matter in the cloud. Managed services may reduce some operating system work, but applications, dependencies, images, and runtime settings still need review. Teams should know how images are built, how often they are updated, how vulnerabilities are prioritized, and how fixes are rolled out. Immutable infrastructure can help because workloads are replaced from known images instead of manually repaired in place. That benefit only appears if the images and automation are maintained.\n\nRuntime permissions are just as important as software versions. A workload should have the identity it needs and no more. A function that reads one queue does not need broad administrator rights. A container that writes application logs does not need access to every secret. Secrets should be injected through approved mechanisms, rotated when needed, and kept out of images, environment dumps, and build logs. Workload identity should be reviewed the same way human access is reviewed.\n\nRuntime monitoring closes the loop. Teams need visibility into process behavior, deployment changes, failed starts, unusual network connections, unexpected privilege changes, and suspicious access to data or secrets. The goal is defensive awareness, not noise. Alerts should focus on signals that prompt useful review or remediation.\n\nAutomation can improve workload security by making builds consistent, enforcing baselines, and reducing manual drift. It can also spread mistakes quickly if templates are stale or pipelines are overprivileged. A healthy workload routine maintains base images, reviews dependencies, limits runtime identity, protects secrets, monitors behavior, and redeploys safely when risk changes.",
  "narrationPoints": [
    "Cloud workloads include VMs, containers, and functions.",
    "Images and templates shape runtime security.",
    "Patching and vulnerability management still matter.",
    "Workloads need limited identity and secrets access.",
    "Automation improves security only when maintained."
  ]
};
