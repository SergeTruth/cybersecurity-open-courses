window.COURSE_MODULE = {
  "title": "Live Systems and toram Style Boots",
  "graphicAlt": "Conceptual visual of a live Linux image copied into RAM before starting the desktop session.",
  "narration": "Live Linux systems are a common place to encounter RAM-backed booting. A live system often starts from a compressed read-only filesystem image on USB media, optical media, or a network source. During boot, the live environment presents a working Linux session without installing to the internal disk. Because the base image is read-only, the system usually adds a writable layer so the session can create files, write temporary state, install packages temporarily, or change settings for that boot.\n\nSome live distributions support a toram-style option. The exact name and behavior vary, so treat toram as a concept, not a universal command. In this style, the boot process copies the live image or required media content into RAM. After the copy, reads can come from memory instead of the original boot device. This may let the physical media be removed, reduce media wear, improve read-heavy performance after startup, or make recovery work more convenient.\n\nThe costs are real. Copying the image into RAM can make initial boot slower. It consumes memory before applications even start. A large desktop live image may not fit comfortably on low-memory systems. If the writable layer is also in RAM, package installs, browser caches, logs, and user activity can increase memory use quickly. If persistence is not configured, changes disappear at shutdown.\n\nDistribution support matters. One live distribution may support a simple boot option. Another may require a different parameter or may not support the workflow. Secure boot, graphics drivers, encrypted volumes, Wi-Fi firmware, vendor laptops, and unusual storage controllers can all complicate the experience. Do not assume one tested boot line applies everywhere.\n\nThe safe path is to test with a noncritical machine or virtual machine first. Confirm that the system actually copied what you expected into RAM. Confirm whether the boot media can be removed. Check available memory after boot and under workload. Decide whether anything needs to persist, such as downloaded tools, logs, or user files. A live-to-RAM boot can be excellent for recovery and lab use, but only when the memory and persistence behavior match the task.",
  "narrationPoints": [
    "Live systems often use compressed read-only images.",
    "A writable layer makes the session usable.",
    "Some live systems can copy media into RAM.",
    "This can reduce dependence on the boot device.",
    "RAM requirements and distro support vary."
  ]
};
