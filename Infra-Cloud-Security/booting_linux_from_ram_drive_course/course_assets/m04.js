window.COURSE_MODULE = {
  "title": "Common RAM-Boot Patterns",
  "graphicAlt": "Conceptual visual comparing live-to-RAM, overlay, selected volatile paths, and hybrid persistence patterns.",
  "narration": "Several RAM-backed Linux patterns appear in real systems. The first is live media copied into RAM. A live distribution often starts from a compressed read-only image. With the right distribution support, that image may be copied into memory during boot. After loading, the system can run with fewer reads from the original boot media. This is useful for portable environments, recovery sessions, and machines where the boot device should be removed or used less after startup.\n\nA second pattern uses a read-only base image with a writable RAM-backed overlay. The lower layer contains the base operating system. The upper layer records changes. A merged view looks writable to the running system. If the upper layer is tmpfs, changes vanish at reboot. This is common in live systems and can also be useful in kiosks, labs, and systems that should reset cleanly.\n\nA third pattern makes only selected paths volatile. For example, temporary caches, selected logs, browser caches, package download caches, or scratch directories may live in tmpfs while the rest of the installed system remains persistent. This is less dramatic than a full RAM root, but often easier to support and recover.\n\nA fourth pattern is a full volatile root. This is more aggressive. The whole working root may reset every boot, with persistence only through deliberately mounted paths or save mechanisms. It can suit training machines, clean-room workflows, and controlled appliances, but it requires careful planning.\n\nA fifth pattern is hybrid persistence. The base may be read-only, the running changes may be temporary, but /home, selected configuration, machine identity, logs, or explicit snapshots may persist. Hybrid systems usually match real operational needs better than pure volatility.\n\nThe right pattern depends on the goal. Reducing disk writes, creating a disposable session, improving read-heavy boot media behavior, protecting a base image, and supporting recovery all lead to different designs. Start with the use case, not with a command copied from another distribution.",
  "narrationPoints": [
    "Live-to-RAM boots copy a base image.",
    "Overlay designs combine read-only and writable layers.",
    "Some systems make only selected paths volatile.",
    "Hybrid designs keep chosen data persistent.",
    "The right pattern depends on the use case."
  ]
};
