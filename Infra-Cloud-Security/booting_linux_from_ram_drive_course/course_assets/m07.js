window.COURSE_MODULE = {
  "title": "Planning RAM, Persistence, and Updates",
  "graphicAlt": "Conceptual visual of RAM budget, persistent data, logs, updates, and application workload planning.",
  "narration": "Planning starts with the workload and the RAM budget. Count more than the base image. Include the kernel, initramfs, root image, writable overlay, file cache, desktop environment, graphics stack, browsers, containers, development tools, package operations, and the applications the system actually runs. A design that looks fine immediately after boot may fail when a browser opens many tabs or when a package update writes thousands of files.\n\nNext, decide what must persist. User data is the obvious category, but it is not the only one. Logs may be needed for troubleshooting or audit. Configuration may need to survive across boots. Package databases and updates may need a maintenance path. SSH host keys, machine identity, VPN profiles, certificates, browser profiles, application caches, containers, virtual machine images, and local databases all need explicit decisions. If a value should survive, decide where and how. If it should not survive, document that too.\n\nUpdates are often the hardest part. If the base system is read-only and changes land in a RAM overlay, ordinary package updates may disappear at reboot. That may be acceptable for a disposable live session, but not for a managed workstation. Possible strategies include rebuilding the base image, snapshotting a known-good state, maintaining a persistent writable layer, or applying updates in a maintenance boot. Each strategy has tradeoffs in complexity, rollback, and testing.\n\nLogs deserve special attention. RAM-only logs are useful for a clean reset, but poor for diagnosing a failure that happened before reboot. Persistent local logs can consume storage and reveal activity. Remote logs can preserve evidence without writing heavily to the local system. Choose intentionally.\n\nMeasure under realistic conditions. Boot the system, run the expected workload, simulate updates, fill caches, restart services, and watch memory pressure. Test shutdown and reboot. Confirm what persists and what resets. Document RAM assumptions, persistent mounts, update steps, and rollback steps. RAM-backed systems reward planning and punish guesses.",
  "narrationPoints": [
    "Plan RAM around the real workload.",
    "Decide what must persist before building.",
    "Logs, updates, and user data need decisions.",
    "Base images need an update strategy.",
    "Test memory pressure before production use."
  ]
};
