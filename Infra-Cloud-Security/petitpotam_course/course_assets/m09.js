window.COURSE_MODULE = {
  "title": "Course Summary and Defensive Roadmap",
  "narration": "PetitPotam is best understood as a defensive case study in chain thinking. The original research highlighted how an EFSRPC coercion path could force Windows authentication, but the severe outcomes came from combining coercion with relayable NTLM and an exposed target such as an insecure AD CS enrollment endpoint. The chain is not magic and not automatic. It requires reachable RPC, outbound authentication, a relay position, NTLM acceptance, missing signing or binding, target authorization, certificate issuance, and certificate authentication behavior that makes the certificate useful.\n\nThe first roadmap item is to understand the authentication path. Inventory domain controllers, AD CS servers, enrollment endpoints, sensitive servers, and network routes. Identify where NTLM is still used, where Kerberos should work but does not, where servers can authenticate outbound, and where services accept integrated Windows authentication. The second item is to identify coercion surfaces and relayable protocols without abusing them. Review RPC and SMB exposure, named-pipe accessibility, outbound SMB and WebDAV controls, and Defender for Identity coverage.\n\nThe third item is to secure relay destinations. For AD CS, remove unnecessary Certificate Authority Web Enrollment and Certificate Enrollment Web Service endpoints. For endpoints that remain, require HTTPS and Extended Protection for Authentication where supported. Review certificate templates, enrollment permissions, authentication-capable enhanced key usages, manager approval, authorized signatures, CA permissions, and template publication. Treat ESC8 as one specific web-enrollment risk, and also review ESC11 and other certificate posture issues that could support separate certificate abuse paths.\n\nThe fourth item is to reduce NTLM and enforce binding or signing. Use NTLM auditing to understand dependencies, remediate avoidable fallback, restrict exceptions, and move toward staged enforcement. Require SMB signing where it is not already required by platform defaults. Use SMB client NTLM blocking and outbound SMB controls where supported and tested. Evaluate LDAP signing and channel binding. Restrict RPC reachability to management paths. Do not claim that HTTPS alone, strong certificate mapping alone, disabling one service, or one patch is a complete defense.\n\nThe fifth item is monitoring. Use Microsoft Defender XDR and Defender for Identity where available, including the Suspicious network connection over Encrypting File System Remote Protocol alert, Suspicious Domain Controller certificate request labeled ESC8, and certificate posture assessments for insecure AD CS endpoints and RPC certificate enrollment packet privacy. Correlate those signals with IIS logs, CA request and issuance records, Kerberos certificate-authentication events, NTLM operational logs, endpoint telemetry, firewall records, and network flows.\n\nThe sixth item is incident readiness. Prepare playbooks that can preserve evidence, identify whether coercion happened, determine whether relay succeeded, find suspicious certificate requests, assess certificate use, revoke or contain suspicious certificates, harden affected endpoints, and validate remediation safely. Certificate response has special timing and trust-chain considerations, so PKI administrators, identity engineers, security operations, and incident responders should practice the handoff before a real alert.\n\nFinally, keep controls alive. Windows releases, AD CS configuration, Defender for Identity assessments, SMB defaults, NTLM deprecation work, proxy designs, and application dependencies change over time. Reassess after certificate-service changes, server upgrades, PKI redesign, new legacy integrations, and network segmentation updates. The resilient formula is: secure the coercion surface, secure the authentication protocol, secure the relay destination, secure certificate issuance, monitor identity use, and validate continually. That engineering loop is the real defense. The course takeaway is disciplined identity engineering, not memorization of a single research name.",
  "narrationPoints": [
    "PetitPotam defense is chain defense, not a single patch decision.",
    "Inventory authentication paths, coercion surfaces, relay targets, and certificate issuance.",
    "Secure AD CS enrollment, templates, CA permissions, and certificate mapping.",
    "Reduce NTLM while enforcing signing, channel binding, service binding, and segmentation.",
    "Monitor EFSRPC, NTLM, IIS, AD CS, Kerberos, and network evidence together."
  ]
};
