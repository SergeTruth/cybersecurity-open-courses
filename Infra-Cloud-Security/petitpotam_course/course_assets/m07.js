window.COURSE_MODULE = {
  "title": "Hardening AD CS, NTLM, SMB, RPC, and Certificate Authentication",
  "narration": "Hardening should break the chain in multiple places. Start with AD CS because the certificate path can convert relay into durable authentication material. Remove Certificate Authority Web Enrollment and Certificate Enrollment Web Service endpoints if they are not required. Where they are required, require HTTPS and configure Extended Protection for Authentication correctly, with the required setting where compatible. If proxies or TLS offload are involved, test carefully because channel binding behavior can depend on how the TLS channel reaches IIS.\n\nThen harden certificate issuance. Treat AD CS and certificate authorities as tier-zero assets. Reduce published templates. Remove unnecessary authentication-capable enhanced key usages. Avoid broad enrollment permissions on sensitive templates. Do not allow untrusted users to supply arbitrary subject or subject alternative name values for authentication templates. Use manager approval or authorized signatures where business impact warrants it. Review CA access-control lists, template ownership, enrollment-agent templates, and certificate manager permissions. The goal is to reduce what any relayed identity could successfully request.\n\nOn fully updated domain controllers, strong certificate mapping is now enforced. Microsoft moved domain controllers to Enforcement mode in February 2025, and the September 9, 2025 security update ended support for returning patched systems to Compatibility mode. Strong certificate mapping reduces certificate impersonation risk, but it does not replace Extended Protection for Authentication or HTTPS protections on enrollment endpoints. Use mapping enforcement and endpoint hardening together.\n\nNTLM reduction is another layer. Audit incoming and outgoing NTLM, fix avoidable dependencies, prefer Kerberos where service principal names and application support allow it, restrict NTLM on sensitive services, and document narrow exceptions. Compatibility testing is essential because legacy applications may fail when NTLM is blocked. That is not a reason to leave NTLM unrestricted forever. It is a reason to use staged enforcement, monitoring, and exception management.\n\nSMB and network hardening reduce forced-authentication opportunity and relay usefulness. SMB-signing defaults vary by edition and role. Windows 11 version 24H2 Enterprise, Pro, and Education require inbound and outbound SMB signing by default, while Windows 11 Home does not. Windows Server 2025 requires outbound SMB signing by default, but inbound signing may still require explicit policy. Administrators should verify effective client and server policy rather than relying only on the operating-system version. SMB traffic should be segmented so servers do not authenticate to arbitrary destinations. Outbound SMB from domain controllers and tier-zero servers should be limited to known required paths. Microsoft also documents SMB client NTLM blocking for remote outbound connections in newer Windows versions. Use these capabilities where supported and tested.\n\nDirectory and RPC protections matter too. LDAP signing and LDAP channel binding should be evaluated and enforced according to current Microsoft guidance and application compatibility. RPC reachability to domain controllers and certificate authorities should be restricted to trusted management and service networks. Named-pipe access should not be assumed safe simply because it is inside the network. Host firewalls, network firewalls, privileged access workstations, tiering, and management-plane segmentation all reduce the chance that a low-trust system can trigger or relay privileged authentication.\n\nPatching remains necessary. Supported Windows Server releases should receive current cumulative updates, AD CS servers and domain controllers should be maintained, and Defender for Identity sensors should be deployed where telemetry and posture assessments are required. But patching should be framed honestly. It can remove known vulnerabilities and improve defaults, yet it does not replace securing NTLM, signing, binding, certificate templates, endpoint exposure, and network paths. PetitPotam taught that a chain can survive if defenders patch one link but leave another equivalent link open.\n\nA layered mitigation plan should be written as control ownership. PKI owners secure AD CS endpoints and templates. Identity engineers reduce NTLM and enforce Kerberos readiness. Windows administrators configure signing, EPA, and supported hardening policies. Network teams restrict RPC and SMB paths. Detection engineers monitor EFSRPC, NTLM, IIS, certificate issuance, and PKINIT. Incident responders define certificate-focused playbooks. When every team knows which link it owns, the chain becomes much harder to assemble. Ownership also makes future audits repeatable when server versions or business exceptions change and during incident response too.",
  "narrationPoints": [
    "Remove unnecessary AD CS web enrollment; harden required endpoints with HTTPS and EPA.",
    "Template, CA, and enrollment permissions determine certificate consequence.",
    "Strong certificate mapping complements, but does not replace, endpoint relay protection.",
    "NTLM restriction should follow audit, dependency remediation, and staged enforcement.",
    "SMB signing, outbound SMB control, RPC segmentation, and LDAP binding reduce relay paths."
  ]
};
