window.COURSE_MODULE = {
  "title": "PetitPotam in the Windows Identity Threat Model",
  "narration": "PetitPotam, pronounced roughly petty po tam, appeared publicly in 2021 as research and a proof of concept by security researcher topotam. The attention was not because a new password-stealing primitive had appeared out of nowhere. The attention came from the way a forced-authentication technique could be chained with long-standing NTLM relay weaknesses and exposed Active Directory Certificate Services, or AD CS. When the coerced account is a domain controller computer account and the relay target can issue an authentication-capable certificate, the consequence can become severe.\n\nMicrosoft's KB5005413 characterizes PetitPotam as a classic NTLM relay attack, and that wording is important. PetitPotam is not every NTLM relay technique. It is not the same thing as every forced-authentication technique. It is the name associated with the original EFSRPC coercion research and tooling. The coercion mechanism causes a Windows host to attempt outbound authentication. The relay destination is a separate service that accepts the relayed authentication. Domain compromise is not created by the RPC request alone; it depends on the rest of the chain being present.\n\nThe core chain is: access to a reachable RPC interface, forced authentication from a Windows account or computer, an attacker-controlled authentication destination, NTLM authentication that can be relayed, a relay target lacking appropriate signing or binding protection, target acceptance of the relayed identity, and an authorized result from that target service. In the AD CS path, the forced authentication may be relayed to a certificate-enrollment endpoint, resulting in certificate issuance under the relayed identity. That certificate can then create certificate-based impersonation risk if directory authentication accepts it.\n\nSeveral terms need careful separation. Active Directory is the directory service that stores domain identity, policy, and computer information. A domain controller authenticates users and computers and hosts critical directory services. A computer account is the domain identity of a Windows machine, and machine authentication can carry important privileges in specific workflows. NTLM is a challenge-response authentication protocol used for compatibility in many Windows environments. Remote Procedure Call, or RPC, lets software invoke operations on remote systems. EFSRPC is the Encrypting File System Remote Protocol, a legitimate RPC interface for encrypted-file maintenance.\n\nAD CS provides certificate authorities and enrollment services for Windows environments. A certificate authority issues certificates. Certificate enrollment is the process of requesting and receiving a certificate. An authentication certificate is a certificate that can be accepted by authentication systems for a user or computer identity. Certificate Authority Web Enrollment—the Certsrv interface—and Certificate Enrollment Web Service are possible enrollment surfaces. ESC8 is a common label for insecure AD CS web-enrollment endpoints that accept NTLM without the right relay protections.\n\nThe security boundaries span more than one protocol. There is a boundary between the RPC caller and the Windows server, between the Windows server and the authentication destination, between that destination and the relay target, between the relay target and the certificate authority, between certificate issuance and directory authentication, and between the certificate-authenticated identity and privileged directory operations. MITRE ATT&CK maps forced authentication to T1187, but the ATT&CK technique is broader than PetitPotam.\n\nA credential hash is not necessarily disclosed as a reusable password, and relay can succeed without cracking a password. An NTLM challenge-response is still security-sensitive because it can sometimes be relayed to a service that accepts it. The relayed identity has the permissions of the coerced account at the target service. If the coerced identity is a domain controller computer account and the target is an insecure AD CS enrollment endpoint, the authorized result may be a certificate with high-impact authentication value.\n\nThe defensive lesson is to think in chains. Remove unnecessary authentication, bind authentication to its intended service or channel, require signing where applicable, restrict network paths, secure certificate enrollment, and monitor identity and certificate activity. Breaking one required link can prevent the full result, but durable defense usually breaks several links so a single configuration drift does not reopen the path.",
  "narrationPoints": [
    "PetitPotam is associated with EFSRPC coercion research, not every relay technique.",
    "Forced authentication, NTLM relay, certificate issuance, and certificate use are separate stages.",
    "AD CS web enrollment exposure can turn a relayed identity into an authentication certificate.",
    "The relayed identity keeps only the authority it has at the target service.",
    "Breaking any required chain link can prevent the full attack path."
  ]
};
