window.COURSE_MODULE = {
  "title": "Assessing PetitPotam and NTLM Relay Exposure",
  "narration": "Exposure assessment should be configuration-led and non-destructive. The objective is to determine whether the environment contains the required links for a PetitPotam-style chain, not to recreate the chain against production domain controllers. Start with asset inventory: domain controllers, AD CS servers, IIS servers hosting certificate enrollment, Certificate Authority Web Enrollment, Certificate Enrollment Web Service, network device paths, tier-zero servers, and systems that can initiate outbound SMB or WebDAV. Then identify where NTLM is still accepted and where signing or binding is required.\n\nThe AD CS inventory should answer which certificate authorities exist, which servers host enrollment endpoints, which endpoints are still needed, which are exposed through load balancers or reverse proxies, and whether Windows Authentication includes NTLM. For IIS-backed enrollment services, verify that HTTP access is disabled and Require SSL is enabled. Do not treat an ordinary HTTP-to-HTTPS redirect as equivalent to removing the HTTP enrollment surface. Also verify that Extended Protection for Authentication is enabled and required where compatible, and determine whether proxy or TLS-termination designs affect channel binding. A proxy path can complicate EPA planning, so version and architecture details matter.\n\nCertificate-template review should focus on consequence. Which templates are published by each CA? Which templates include client-authentication, smart-card-logon, PKINIT client-authentication, any-purpose, or no enhanced key usage? Which templates can be enrolled by broad groups such as Domain Computers, Domain Users, Authenticated Users, or Everyone? Which templates allow subject information supplied in the request? Which require manager approval or authorized signatures? Which templates are actually needed? Removing unnecessary publication is often safer than trying to explain every theoretical path.\n\nNTLM assessment should start in audit mode. Windows includes policy settings for auditing incoming NTLM traffic, outgoing NTLM traffic, and NTLM authentication in a domain. Those events can reveal servers, clients, applications, and service accounts that still depend on NTLM. The audit phase should distinguish unavoidable legacy dependencies from misconfigurations such as missing service principal names, old client libraries, appliances that cannot use Kerberos, or applications configured to negotiate NTLM unnecessarily. Exceptions should be narrow, documented, and periodically reviewed.\n\nNetwork assessment should include inbound and outbound paths. Inbound RPC and SMB reachability to domain controllers and sensitive servers should be restricted to management and service paths that are genuinely required. Outbound SMB from servers, especially tier-zero assets, should not be allowed to arbitrary destinations. Microsoft guidance for SMB traffic segmentation emphasizes controlling lateral, inbound, and outbound SMB paths carefully because Windows clients and servers do need SMB for legitimate domain and file-service operations. The point is to allow required paths and block uncontrolled ones.\n\nService-protection assessment should cover SMB signing, LDAP signing, LDAP channel binding, IIS EPA, and AD CS RPC enrollment packet privacy where applicable. Microsoft Defender for Identity certificate posture assessments include the ESC11 recommendation when the RPC certificate enrollment interface does not enforce packet privacy and the ESC8 recommendation for insecure certificate enrollment IIS endpoints. These assessments do not replace manual review, but they provide a useful view into high-impact certificate infrastructure drift.\n\nA safe validation plan uses layered evidence. Confirm endpoint existence through inventory. Confirm IIS and authentication settings from configuration. Confirm template and CA policy from AD CS administrative data. Confirm NTLM usage through audit logs. Confirm network paths through firewall and segmentation policy. Confirm telemetry coverage from domain controller, AD CS, IIS, endpoint, and network sensors. If a team performs active testing, it should happen under explicit authorization, in a lab or controlled maintenance window, with detection teams aware, and without producing uncontrolled relayed certificates.\n\nThe assessment output should not be a single vulnerable or not vulnerable label. It should identify which links are present, which controls are already breaking the chain, which exceptions remain, which mitigations require compatibility testing, and which owners must sign off. That makes remediation tractable: remove unneeded enrollment endpoints, require EPA and HTTPS, reduce NTLM, enforce signing and binding, restrict network paths, harden templates, and monitor certificate issuance. It also gives leaders a defensible priority list and timing constraints instead of a vague request to fix relay.",
  "narrationPoints": [
    "Exposure assessment should identify chain links without recreating the attack chain.",
    "Inventory AD CS endpoints, IIS authentication, templates, CA policy, and NTLM usage.",
    "Review both inbound RPC/SMB reachability and outbound SMB paths from sensitive servers.",
    "Use audit mode before broad NTLM restriction.",
    "Record which controls break which link in the chain."
  ]
};
