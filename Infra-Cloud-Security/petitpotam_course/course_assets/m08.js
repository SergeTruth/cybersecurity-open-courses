window.COURSE_MODULE = {
  "title": "Incident Response and Remediation Validation",
  "narration": "Response to suspected PetitPotam-style activity should start by stabilizing identity infrastructure and preserving evidence. Do not begin by running coercion tests to see whether the alert is real. Capture the alert details, source and target addresses, involved accounts, timestamps, domain controller, AD CS server, IIS endpoint, certificate request identifiers, and any certificate thumbprints. Preserve relevant domain controller, AD CS, IIS, endpoint, firewall, and network logs before retention windows or log rotation remove the sequence.\n\nTriage should determine which chain links were observed. Was there a suspicious EFSRPC connection? Did the targeted Windows host attempt outbound authentication? Was NTLM accepted by an enrollment endpoint or another service? Was a certificate request submitted? Was a certificate issued, denied, or pending? Which identity was named? Which template was used? Does the certificate include authentication-capable usage? Was it later used for Kerberos PKINIT or another certificate-based authentication path? The answer determines severity.\n\nContainment depends on evidence and business impact. Potential steps include temporarily disabling unnecessary certificate-enrollment endpoints, moving an endpoint behind tighter network controls, switching a vulnerable IIS endpoint to required EPA where tested, disabling risky templates, suspending broad enrollment permissions, increasing monitoring, blocking suspicious outbound SMB destinations, isolating an involved host, and revoking or placing suspicious certificates on hold. If a domain controller certificate was illicitly issued, treat the event as high impact and involve Active Directory, PKI, incident response, and legal or governance stakeholders as appropriate.\n\nCertificate response has timing complications. Revocation is important, but it may not be instantaneous across all relying parties because revocation checking, cache behavior, certificate revocation list publication, online responder availability, and application behavior vary. Responders should record the certificate serial number, issuer, subject, thumbprint, template, requester, issuance time, revocation time, and evidence of use. Rotate machine-account secrets when evidence indicates that the password or its derived credentials may have been compromised. Treat maliciously issued certificates separately: identify and revoke them, publish updated revocation information, and correct the enrollment or mapping weakness that allowed their issuance or use. Teams should also review privileged group changes, inspect directory replication indicators, and search for later persistence created with the certificate-authenticated identity.\n\nRemediation should address root cause, not only the observed alert. If the alert showed EFSRPC activity but no certificate issuance, the environment may still have relay exposure. If a certificate was issued, the team must fix both the enrollment endpoint and the certificate-template or CA policy that made issuance useful. If NTLM relay succeeded against a non-AD CS service, the mitigation may require signing or binding for that service. If outbound SMB to untrusted destinations made coercion observable, segmentation and egress controls should be reviewed.\n\nValidation after remediation should be safe and evidence-driven. Confirm that unnecessary endpoints are removed. Confirm that remaining AD CS web enrollment endpoints require HTTPS and EPA as designed. Confirm that certificate templates no longer permit risky issuance. Confirm that NTLM audit volume decreases or is restricted by documented exception. Confirm that SMB signing, outbound SMB control, LDAP signing, channel binding, and RPC segmentation are applied where planned. Confirm that Defender for Identity posture assessments change state after their normal update timing.\n\nUse controlled testing only when necessary and authorized. A lab environment can demonstrate behavior without endangering production. In production, prefer configuration inspection, log review, synthetic benign access to enrollment pages, policy-read checks, and telemetry simulation that does not produce coerced authentication from domain controllers or issue uncontrolled certificates. Document what was validated, what was not validated, and what residual risk remains because of legacy dependencies, unsupported servers, proxy design, or business exceptions.\n\nClose the incident by updating the threat model, hardening standard, monitoring logic, certificate issuance review process, and change-control requirements. PetitPotam-style response is a reminder that identity incidents often cross team boundaries. The final report should explain the chain in defensive terms: which coercion surface was touched, which authentication path was possible, which relay target was protected or unprotected, whether certificate issuance occurred, whether certificate authentication occurred, what controls were changed, and how the organization will detect drift. That record prevents the same exposure from returning during the next PKI or server refresh with named owners.",
  "narrationPoints": [
    "Incident response should preserve evidence before testing or changing systems.",
    "Triage must determine whether coercion, relay, certificate issuance, and certificate use occurred.",
    "Revocation matters but may not be instantly effective everywhere.",
    "Remediation should fix the exposed chain links, not only the alert symptom.",
    "Validation should avoid recreating a destructive production relay chain."
  ]
};
