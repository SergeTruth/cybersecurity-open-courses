window.COURSE_MODULE = {
  "title": "Active Directory Certificate Services and the ESC8 Path",
  "narration": "Active Directory Certificate Services provides certificate authorities, templates, enrollment policy, and enrollment interfaces for Windows environments. AD CS is often tier-zero infrastructure because certificates can become authentication material. A certificate authority decides whether to issue a certificate, which template applies, which identity is named, which enhanced key usages are present, and whether the certificate can be used for authentication. In a relay chain, the dangerous step is not merely reaching AD CS. It is causing a certificate-enrollment endpoint to accept a relayed identity and issue a useful certificate.\n\nThe widely discussed AD CS path uses EFSRPC coercion to cause NTLM authentication by a Windows host, relays that authentication to an insecure certificate-enrollment endpoint, requests a certificate under the relayed identity, and then relies on certificate-based authentication accepting that certificate. ESC8 is the common label for insecure AD CS web-enrollment endpoints, especially Certificate Authority Web Enrollment and Certificate Enrollment Web Service when they allow NTLM without HTTPS or without Extended Protection for Authentication. ESC8 is one AD CS exposure class; it is not every AD CS weakness.\n\nCertificate Authority Web Enrollment and Certificate Enrollment Web Service are IIS-backed HTTP or HTTPS enrollment surfaces. Microsoft guidance for AD CS relay mitigation recommends enabling Extended Protection for Authentication and disabling HTTP on AD CS servers, with the required EPA setting being the more secure option. The reason is subtle but important. HTTPS protects the outer transport, but without EPA the Windows authentication inside the connection may still lack sufficient evidence that it belongs to that channel or service. EPA adds channel or service binding that helps stop relay acceptance.\n\nCertificate issuance and certificate use are separate controls. A certificate may be issued but not useful for authentication, or it may be issued with extended key usages that make it useful for client authentication, smart card logon, or PKINIT. Templates, enrollment permissions, manager approval, authorized signatures, subject and subject alternative name rules, application policies, and CA policy all affect whether an issued certificate has dangerous value. A relayed domain controller identity is especially sensitive because computer certificates can participate in domain authentication flows.\n\nOn fully updated domain controllers, strong certificate mapping is now enforced. Microsoft moved domain controllers to Enforcement mode in February 2025, and the September 9, 2025 security update ended support for returning patched systems to Compatibility mode. Strong certificate mapping reduces certificate impersonation risk, but it does not replace Extended Protection for Authentication or HTTPS protections on enrollment endpoints. If an insecure endpoint issues a certificate to the relayed identity, endpoint protection and mapping enforcement still solve different problems.\n\nDefenders should review certificate templates and CA permissions alongside IIS settings. Templates that allow broad enrollment, authentication-capable enhanced key usages, supply-in-request subject fields, weak manager approval, or risky application policies may increase the consequence of a relay or separate AD CS abuse path. CA access-control lists, template ownership, and enrollment-agent configuration matter as well. Microsoft Defender for Identity certificate posture assessments now surface several AD CS configuration issues, including insecure web enrollment endpoints labeled ESC8 and RPC enrollment packet-privacy concerns labeled ESC11.\n\nThe practical AD CS question is: can a relayed identity reach an enrollment endpoint, authenticate with NTLM, request a certificate that the CA will issue, and then use that certificate for authentication? If the answer is no at any link, the full chain breaks. If the answer is uncertain, defenders should not guess. They should inventory endpoints, review IIS authentication and EPA settings, validate template issuance rules, inspect CA audit events, and test changes in a controlled environment.\n\nAD CS hardening is not only a PetitPotam project. It is identity infrastructure defense. Certificate authorities, templates, enrollment endpoints, and certificate authentication policies determine who can obtain authentication material and how the directory trusts it. Treating AD CS as tier-zero, limiting enrollment surfaces, requiring binding protections, and monitoring certificate issuance makes the environment more resilient against PetitPotam-style chains and against other certificate-focused attacks.",
  "narrationPoints": [
    "ESC8 is the insecure AD CS web-enrollment endpoint path, not every AD CS weakness.",
    "EPA and HTTPS protect enrollment endpoints from NTLM relay acceptance.",
    "Certificate issuance and certificate-based authentication are separate control points.",
    "Enforced strong certificate mapping does not replace securing enrollment endpoints.",
    "Template and CA policy determine whether an issued certificate is useful for authentication."
  ]
};
