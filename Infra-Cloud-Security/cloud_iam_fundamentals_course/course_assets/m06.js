window.COURSE_MODULE = {
  "title": "Workload and Automation Identity",
  "graphicAlt": "Conceptual visual of workload identities for CI/CD, containers, virtual machines, serverless functions, service accounts, and deployment automation.",
  "narration": "Cloud IAM is not only about employees signing into a console. Workloads also need identity. CI/CD pipelines deploy infrastructure. Containers call cloud APIs. Virtual machines retrieve configuration. Serverless functions process events. Kubernetes workloads read secrets or publish messages. Applications use service accounts, managed identities, workload identities, tokens, or deployment roles to perform tasks without a human clicking a button.\n\nNon-human identities often create the largest blind spots. A shared service account may be used by several applications, so no one knows which system caused an action. A deployment role may be overpowered because it was created to make automation easy. A leaked access key may remain active because no owner is tracking it. A workload in development may accidentally have access to production resources. These problems are not exotic; they are normal engineering drift.\n\nSafer patterns include workload identity federation, managed identities, short-lived credentials, narrowly scoped deployment roles, environment separation, secret rotation, and clear ownership. A build pipeline should have the permissions needed to deploy its application, not broad rights across the account. A serverless function should read the specific data it needs, not every storage resource. A Kubernetes workload should not share one powerful identity with unrelated workloads.\n\nAuditability is essential. Automation should leave a clear trail of which principal acted, which pipeline or workload used it, what changed, and who owns the system. When a production change happens, responders should not have to guess whether it came from a user, a deployment tool, a managed identity, or a compromised key. Workload identity hardening is how cloud teams make automation fast without making it opaque.\n\nEnvironment separation is a practical guardrail. A development pipeline should not normally deploy to production unless that is its explicit job. A test service account should not read production secrets. A serverless function in one application should not write to another team's storage. These boundaries make mistakes easier to contain and ownership easier to understand.\n\nWorkload identity design should be part of application architecture. When a new service is created, teams should ask which cloud APIs it needs, which resources it can touch, how credentials are delivered, who owns the identity, how logs identify its actions, and how access will be removed later. Answering those questions early is far cheaper than cleaning up an overpowered service account after production launch.\n\nAutomation should have the same review discipline as human access. If a pipeline is allowed to create networks, assign roles, or deploy privileged functions, reviewers should understand why. The fact that a principal is non-human does not make it lower risk. In many cloud environments, workload identities are among the most powerful identities in the account.",
  "narrationPoints": [
    "Cloud IAM includes workload identities.",
    "Shared service accounts obscure ownership.",
    "Deployment roles should be narrowly scoped.",
    "Short-lived credentials reduce secret leakage risk.",
    "Auditability matters for automation."
  ]
};
