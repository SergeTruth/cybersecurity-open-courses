window.COURSE_MODULE = {
  "title": "Privilege Escalation and Risky Permission Paths",
  "graphicAlt": "Conceptual visual of risky IAM permission paths such as policy attachment, key creation, trust changes, role passing, impersonation, pipeline edits, and logging changes.",
  "narration": "Some cloud permissions are risky because they allow a principal to create, modify, or impersonate other identities. The danger is not always obvious from the role name. A principal may not be labeled administrator, but if it can attach policies, create access keys, update trust relationships, pass roles, impersonate service accounts, or modify deployment automation, it may be able to reach more privilege than the visible assignment suggests.\n\nDefensive IAM review should look for permission paths. Can this identity grant itself more access? Can it change a role that another workload trusts? Can it edit a function, startup script, container image, or pipeline that runs with stronger permissions? Can it create credentials for another identity? Can it disable logging or security controls? These questions are design and review questions, not instructions for abuse. They help teams find paths that need guardrails.\n\nDeployment systems deserve special care. A CI/CD pipeline may be able to update infrastructure, deploy functions, modify containers, or change cloud configuration. If that pipeline has broad permissions, then anyone who can change the pipeline definition, build artifact, or deployment script may indirectly influence privileged cloud actions. Secure design separates duties, scopes deployment roles, reviews pipeline access, and monitors sensitive changes.\n\nThe practical goal is to reduce surprise. IAM review should not stop at role names like reader, developer, operator, or deployer. It should examine sensitive actions, trust relationships, impersonation, role passing, policy changes, credential creation, and logging changes. When risky paths are necessary, they should be documented, approved, monitored, and constrained so that legitimate administration does not become uncontrolled privilege growth.\n\nRisky permission paths are often found at the edges between teams. A developer may not be a cloud administrator, but may edit a pipeline that uses an administrator-like deployment role. A support identity may not own policies, but may be able to create keys for a powerful service account. A function editor may not own data directly, but may change code that runs with data access. These indirect paths deserve the same seriousness as direct grants.\n\nDefensive controls can include permission boundaries, approval workflows, restricted role passing, separate deployment roles, policy review, protected branches, mandatory code review, and alerts on sensitive IAM changes. The exact control depends on the platform and workflow. The important habit is to ask how privilege can move, not only where privilege appears to sit today.\n\nLogging and alerting help make risky paths manageable. A permission may be necessary for a deployment platform, but sensitive use should still be visible. Alerts on policy attachment, trust changes, key creation, role passing, impersonation, and security logging changes give defenders a chance to spot unexpected behavior before it becomes a larger incident.",
  "narrationPoints": [
    "Some permissions create indirect privilege.",
    "Role passing and impersonation require review.",
    "Policy and trust changes are sensitive.",
    "Deployment pipelines can become access paths.",
    "IAM review must trace permission paths."
  ]
};
