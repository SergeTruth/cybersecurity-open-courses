window.COURSE_MODULE = {
  "title": "Federation, MFA, and Temporary Access",
  "graphicAlt": "Conceptual visual of federated sign-in, MFA, role assumption, conditional access, temporary credentials, and just-in-time access.",
  "narration": "Many organizations federate cloud access through an identity provider. Instead of managing separate cloud passwords for every user, employees sign in through single sign-on using corporate identity controls. Groups, conditional access, MFA, device posture, and role assignment can then determine which cloud roles a person may assume. This centralizes lifecycle management: when someone changes roles or leaves the organization, access can be adjusted from the identity provider.\n\nMulti-factor authentication is especially important for sensitive cloud access. A password alone should not be enough to reach powerful administrative roles. MFA does not prove that every action is safe, but it reduces the chance that a stolen password becomes immediate control-plane access. Conditional access policies can add more context, such as trusted devices, location, risk signals, or stronger authentication for higher-risk actions.\n\nAuthentication and authorization are different. Authentication answers who the user or workload is. Authorization answers what that principal can do. A user may authenticate successfully through SSO and MFA but still be unauthorized to delete a database, modify a firewall rule, or read a secret. Cloud IAM security depends on both: strong sign-in controls and carefully designed permissions.\n\nTemporary access reduces standing privilege. Role assumption, just-in-time access, privileged access workflows, and short-lived credentials can let authorized users perform tasks without keeping powerful permissions active all the time. Long-lived access keys and standing administrator grants create operational risk because they can be copied, forgotten, or misused. Where long-lived credentials still exist, they should be owned, rotated, monitored, and reduced over time.\n\nJust-in-time access is especially useful for powerful roles. Instead of leaving administrator access active all day, an engineer can request time-limited elevation for a specific task. The approval, duration, reason, and resulting actions can be logged. This creates a better record for audit and incident response while reducing the window in which a compromised session can cause damage.\n\nLong-lived keys should be treated as exceptions, not the default. If a key is needed, it should have an owner, purpose, scope, rotation plan, storage expectation, and monitoring. Many environments still contain old keys created for scripts, vendors, or migrations. Finding and reducing those keys is often one of the highest-value IAM cleanup projects.\n\nFederation also improves offboarding and emergency response. If a user leaves the company or a credential is suspected of compromise, central identity controls can remove or restrict cloud access quickly. Local-only cloud users and unmanaged keys are harder to find and harder to disable. That is why mature programs reduce standalone accounts and monitor exceptions closely.",
  "narrationPoints": [
    "Federation centralizes human access.",
    "MFA protects sign-in for sensitive access.",
    "Authentication and authorization are different.",
    "Temporary credentials reduce standing risk.",
    "Long-lived keys need strict control."
  ]
};
