window.COURSE_MODULE = {
  "title": "Principals, Identities, and Accounts",
  "graphicAlt": "Conceptual visual of cloud principals including users, groups, roles, service accounts, managed identities, applications, tokens, and temporary credentials.",
  "narration": "A principal is an identity that can request access to a cloud resource or API. Common building blocks include users, groups, roles, service accounts, managed identities, workload identities, applications, API keys, access tokens, and temporary credentials. Different providers use different names. AWS roles, Azure managed identities, and Google Cloud service accounts are not identical, but they all represent a basic idea: something is being authenticated and then authorized to perform actions.\n\nHuman identities and non-human identities need different controls. A person may sign in through a browser or command-line tool using a federated identity provider, multi-factor authentication, and role assumption. A workload may run inside a virtual machine, container, serverless function, Kubernetes pod, CI/CD pipeline, or deployment automation. It may need to read a secret, write logs, publish an artifact, deploy infrastructure, or call another cloud service. The security design should reflect the identity type and job.\n\nEvery identity should have a purpose, owner, lifecycle, and permission boundary. Purpose explains why it exists. Ownership explains who reviews it, approves changes, and responds when something looks wrong. Lifecycle explains when it should be created, rotated, disabled, or deleted. A permission boundary or equivalent guardrail limits what the identity can do, even if a policy or role assignment changes later. These details prevent stale identities from becoming quiet risk.\n\nCredentials also differ. Long-lived access keys and static secrets are operationally risky because they can be copied, forgotten, or leaked. Temporary credentials, managed identity mechanisms, workload identity federation, and short-lived tokens reduce that risk when implemented correctly. The goal is not merely to create identities. The goal is to make each identity understandable, owned, scoped, auditable, and removable when its purpose ends.\n\nHuman identities usually follow people and job roles. They need onboarding, role changes, offboarding, MFA, and periodic review. Non-human identities follow systems and automation. They need owners, rotation or short-lived credentials, clear deployment context, and removal when the workload retires. Treating both categories the same leads to mistakes: humans get permanent machine keys, or workloads inherit human-style administrator privileges.\n\nNaming and metadata help. An identity name, tag, label, description, or owner field should tell a reviewer what the identity is for. If a service account called temp-admin has existed for two years and no one knows what it runs, that is not only untidy; it is a risk. Identity inventory is the foundation for access review, incident response, and cleanup.\n\nReviews should ask whether an identity still matches reality. Does the group still represent the team? Does the role still match the application? Is the API key still needed? Is the token issuer still trusted? IAM drift often comes from identities that were correct when created but were never revisited after projects, teams, and workloads changed.",
  "narrationPoints": [
    "Principals are identities that can request access.",
    "Human and workload identities need different controls.",
    "Every identity needs purpose and ownership.",
    "Temporary credentials reduce long-lived secret risk.",
    "Lifecycle management prevents stale access."
  ]
};
