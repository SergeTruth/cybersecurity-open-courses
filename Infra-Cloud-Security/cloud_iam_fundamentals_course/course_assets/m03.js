window.COURSE_MODULE = {
  "title": "Permissions, Policies, and Effective Access",
  "graphicAlt": "Conceptual visual of policies, permissions, conditions, inheritance, resource policies, explicit denies, and effective access evaluation.",
  "narration": "Permissions describe allowed actions on resources. A permission might allow reading an object, starting a virtual machine, listing secrets, changing a firewall rule, creating a key, or updating a policy. Policies are the rules that grant, deny, or condition those permissions. They may be attached to an identity, a resource, a group, a role, an organization, a folder, a subscription, a project, or another scope depending on the provider.\n\nCloud environments use several access models. Role-based access control, or RBAC, grants roles that bundle permissions for a job or scope. Attribute-based access control, or ABAC, uses attributes such as tags, labels, departments, environments, resource names, or identity properties to make access decisions. Identity-based policies travel with the principal. Resource-based policies define who can access a resource. Conditions can limit when, where, or how access is used. Explicit deny rules can override grants.\n\nEffective access is the access a principal actually has after all relevant rules are evaluated. It can be different from what one visible role assignment suggests. A user may inherit access from a group, receive additional access through a role assumption path, be blocked by an explicit deny, or gain access through a resource policy. A workload may look limited in one project but have a token that can impersonate a service account elsewhere. This is why IAM review must look beyond the first role name.\n\nPractical IAM analysis asks several questions. What principal is requesting access? What action is requested? Which resource is affected? What policies apply from identity, resource, and inherited scopes? Are there conditions? Is there an explicit deny? Is access temporary or standing? Does another path allow impersonation or delegation? Understanding effective access is essential for least privilege, troubleshooting, incident response, and governance.\n\nPolicy evaluation can also cross boundaries. A project-level grant, subscription-level role, organization policy, bucket policy, key policy, or service control may all affect the final answer. Troubleshooting access often means tracing the request through several layers. Governance review works the same way. A visible assignment may look safe until inherited permissions or a resource policy adds a wider path.\n\nThe practical outcome is humility. Do not assume a principal has only the permissions you remember assigning. Use provider tools, access analysis, logs, and policy review to understand actual access. Effective access is what matters during an incident, an audit, or a design review. It is also what matters when you decide whether a role is truly least privilege.\n\nAccess design should also account for failure modes. If a condition depends on a tag, who can change that tag? If access depends on group membership, who manages the group? If a resource policy grants access to another account or tenant, who reviews that relationship? Effective access includes both the grant and the governance around the grant.",
  "narrationPoints": [
    "Permissions allow actions on resources.",
    "Policies grant, deny, or condition access.",
    "RBAC and ABAC model access differently.",
    "Inherited and resource-based policies affect outcomes.",
    "Effective access may differ from one visible role."
  ]
};
