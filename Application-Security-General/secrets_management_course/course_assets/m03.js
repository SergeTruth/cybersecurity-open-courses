window.COURSE_MODULE = {
  "title": "How Secrets Leak",
  "graphicAlt": "Conceptual visual of safe defensive leak paths such as source control, logs, tickets, build output, and container images.",
  "narration": "Secrets often leak through ordinary work, not through dramatic events. A developer may hardcode a token temporarily during local testing and forget to remove it before committing. An engineer may paste a connection string into a ticket while asking for help. A pipeline may print an environment variable during debugging. A container image may preserve a build argument. A support bundle may include configuration that was never meant to leave the host.\n\nCommon leak paths include source code, commit history, issue trackers, chat channels, log files, crash reports, test reports, build output, screenshots, local configuration files, unmanaged scripts, container layers, mobile application packages, and environment dumps. None of these channels is inherently malicious. Most exist to help teams build and operate software. That is exactly why secret handling has to account for them. People naturally use the tools that help them solve problems under pressure.\n\nSource control deserves special attention because history is persistent. Removing a secret from the latest version of a file does not necessarily remove it from previous commits, forks, clones, pull requests, or cached build output. The safe response to a committed secret is usually to revoke or rotate it, investigate where it went, and prevent recurrence. Editing history may be useful in some cases, but it is not a substitute for treating the exposed value as compromised.\n\nLogs are another frequent path. Applications should not print passwords, tokens, authorization headers, private keys, connection strings, or raw environment dumps. Debug logging is especially risky because it is often enabled when teams are under pressure and may be copied into tickets or retained by centralized systems. Masking can help, but the better design is to avoid sending secret material to logs in the first place.\n\nBuild and deployment systems create their own risks. CI/CD variables, test fixtures, container build arguments, package artifacts, release bundles, and deployment manifests can all preserve values longer than expected. A secret used only during build time should not be baked into the final image. A token used by one protected branch should not be available to every fork or every pull request. A pipeline that masks values in the console may still pass them to tools that write files.\n\nThe right response is systemic. Blaming one person for one paste does not make the next paste less likely. Teams need safer defaults, pre-commit checks, repository and pipeline scanning, protected variables, review habits, clear examples, and response playbooks. The best programs assume that humans will take shortcuts under pressure and then design guardrails that make the safe path the easiest path.",
  "narrationPoints": [
    "Secrets often leak through normal work channels.",
    "Source code, logs, tickets, and chat are common risks.",
    "Build artifacts and images can preserve secrets.",
    "Debugging shortcuts can create long-term exposure.",
    "Safer defaults reduce reliance on memory."
  ]
};
