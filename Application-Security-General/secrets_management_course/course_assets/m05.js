window.COURSE_MODULE = {
  "title": "Access Control and Least Privilege",
  "graphicAlt": "Conceptual visual of least-privilege access for humans, services, workloads, and emergency access.",
  "narration": "Least privilege means people, services, workloads, and automation receive only the secret access they need to perform an approved job. A developer who supports one service does not automatically need every production database credential. A build job that deploys one application does not need administrator access to every cloud account. A monitoring tool may need read-only telemetry access, not broad write access. The principle is simple, but it takes discipline to maintain.\n\nHuman access and machine access are different, but both need control. Human access should be tied to roles, teams, approvals, and periodic review. Machine access should be tied to service accounts, workload identities, deployment environments, and explicit ownership. A service account should not be a nameless shared identity that survives forever because everyone is afraid to touch it. It should have an owner, a purpose, a scope, and a review schedule.\n\nBroad access to a secret manager can be as risky as broad access to the systems those secrets unlock. If an identity can read every production secret, that identity effectively has access to many production systems. Vault administrator roles, break-glass accounts, synchronization jobs, and automation tokens need careful review. Administrative convenience can quietly become a single point of broad compromise.\n\nShort-lived credentials reduce long-term exposure. Instead of distributing permanent keys, teams can use managed identities, workload identity federation, just-in-time access, temporary tokens, or certificates with defined lifetimes. These patterns do not remove the need for access control, but they reduce the value of an old copied credential and make recovery easier when exposure is suspected.\n\nBreak-glass access is sometimes necessary, but it should be rare, auditable, and practiced. Emergency access should have clear conditions, approval or notification expectations, monitoring, and after-action review. A break-glass path that is never tested may fail during an incident. A break-glass path that is too easy to use may become routine access by another name.\n\nAccess control is not a one-time setup. Teams change, services are replaced, pipelines evolve, and privileges accumulate. Regular access review is how a secrets program stays honest. Ask whether each identity still needs each secret, whether the scope is still correct, whether a shorter-lived or managed identity option exists, and whether audit logs show expected use. Least privilege is a maintenance habit, not a checkbox.",
  "narrationPoints": [
    "Secret access should follow least privilege.",
    "Humans and workloads both need access control.",
    "Service accounts require ownership and review.",
    "Short-lived credentials reduce long-term exposure.",
    "Broad vault access can create broad risk."
  ]
};
