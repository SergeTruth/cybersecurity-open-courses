window.COURSE_MODULE = {
  "title": "What Counts as a Secret",
  "graphicAlt": "Conceptual visual of common secret types including tokens, private keys, certificates, passwords, and service credentials.",
  "narration": "A secret is any value that grants access, proves identity, protects data, signs a message, or allows one system to trust another. The obvious examples are passwords, API keys, OAuth tokens, private keys, SSH keys, TLS private keys, database credentials, cloud access keys, webhook signing secrets, encryption keys, session signing keys, and service account credentials. These values need special handling because possession can be enough to perform an action.\n\nSome secrets are easy to recognize because they look random or are labeled as credentials. Others are hidden inside a larger value. A database connection string may include a username, password, hostname, database name, and driver settings all in one line. A storage URL may include a signed token. A configuration object may include both harmless settings and a value that authorizes access. Treating the whole object as ordinary configuration can expose the sensitive part.\n\nContext matters. A hostname by itself may not be secret. A public certificate may not be secret. A username may not be secret in one environment. But the combination of a host, user, password, database name, and environment can become sensitive. A token that looks like a harmless random string may allow production deployment. A private key may be useful only with the matching account, but if that account controls signing or decryption, the impact can be serious.\n\nClassification helps teams choose the right controls. One useful approach is to classify by scope, environment, privilege, lifetime, and impact. Scope asks what the secret can access. Environment asks whether it belongs to development, test, staging, production, or a shared platform. Privilege asks whether the secret can read, write, administer, deploy, sign, decrypt, or manage other secrets. Lifetime asks whether it is long-lived, short-lived, rotating, or one-time. Impact asks what would happen if it were exposed.\n\nNot every secret needs the same handling, but every secret needs an owner and a plan. A low-privilege development token may require different response steps than a production signing key. A short-lived workload identity token may be safer than a permanent shared key. A certificate private key may require coordination with infrastructure, application owners, and external partners. These differences are why inventory and classification are practical tools, not paperwork.\n\nWhen in doubt, treat access-bearing values conservatively. Use clearly fake examples in training, such as EXAMPLE_API_KEY or REDACTED_SECRET, and avoid putting realistic-looking tokens into documents, tests, screenshots, or sample repositories. The habit you want is simple: if a value could let a person or system cross a trust boundary, handle it as a secret until proven otherwise.",
  "narrationPoints": [
    "Secrets include passwords, keys, tokens, and certificates.",
    "Connection strings can contain hidden credentials.",
    "Some values are sensitive because of context.",
    "Scope and privilege determine impact.",
    "Classification guides protection and rotation."
  ]
};
