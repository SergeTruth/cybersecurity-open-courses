window.COURSE_MODULE = {
  "title": "Delivering Secrets to Applications",
  "graphicAlt": "Conceptual visual of safe secret delivery to applications, containers, cloud workloads, and CI/CD pipelines.",
  "narration": "Applications need secrets at the moment they connect, sign, decrypt, or authenticate. The delivery pattern should make the secret available to the authorized workload without spreading it into places that are hard to control. Common patterns include fetching a secret at startup, retrieving it at runtime through a secret manager, mounting it into a container through an orchestrator, using a platform-managed identity, or receiving a short-lived token from a trusted broker.\n\nRuntime access can reduce hardcoding. Instead of storing a database password in source code, the application can read it from an approved secret manager using a workload identity. Instead of placing a cloud key in a deployment manifest, the workload can use managed identity or federation. These approaches keep static secrets out of code and make access policy easier to manage, but they still require careful configuration, monitoring, and error handling.\n\nEnvironment variables are common because they are easy for platforms and pipelines to inject. They can be acceptable in some environments, but they are not magic. Depending on the platform, environment values may appear in process inspection, crash dumps, debug pages, support bundles, logs, metrics, or administrative consoles. If a team uses environment variables, it should understand where those values can travel and should avoid dumping the environment during troubleshooting.\n\nCI/CD pipelines need special care. Pipeline variables should be scoped to the minimum project, branch, environment, and job that needs them. Logs should be masked, but masking is not enough if a tool writes the value to a file or artifact. Protected branches, approvals, environment gates, and short-lived deployment credentials can reduce risk. Pull requests from untrusted contexts should not receive production deployment secrets.\n\nContainers and images introduce another delivery boundary. A secret used during build should not be preserved in the final image layer. A runtime secret should not be copied into the application directory or included in a diagnostic bundle. Mounted secrets should have tight file permissions, and applications should read them intentionally rather than recursively printing configuration for convenience.\n\nApplications themselves must be designed not to leak secrets. Do not log authorization headers, raw tokens, private keys, passwords, or full connection strings. Do not return secrets in error responses. Do not cache secrets longer than needed. Do not include secrets in telemetry attributes or exception messages. Good delivery gets the secret to the workload; good application behavior keeps it from leaving again.",
  "narrationPoints": [
    "Applications need safe secret delivery patterns.",
    "Runtime access can reduce hardcoded secrets.",
    "Environment variables require careful handling.",
    "CI/CD secrets need scope, masking, and approvals.",
    "Applications should never log secrets."
  ]
};
