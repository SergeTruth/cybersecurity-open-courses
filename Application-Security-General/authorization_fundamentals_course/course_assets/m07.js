window.COURSE_MODULE = {
  "title": "Policy Enforcement and Architecture",
  "graphicAlt": "Blank placeholder image for a lesson on authorization policy enforcement architecture.",
  "narration": "Authorization architecture defines where decisions are made and where they are enforced. A policy decision point, at a high level, evaluates policy and context. A policy enforcement point applies the result by allowing or denying the operation. In practice, these may be centralized services, application-local checks, middleware, guard clauses, framework annotations, database controls, or combinations of those patterns.\n\nCentralized authorization can improve consistency and governance, but it must be integrated carefully with application context. Application-local checks can be close to the code and easier to understand, but they may become inconsistent across teams. Reusable authorization middleware and guard clauses can help make safe behavior easier to apply. The architecture should make correct checks routine rather than relying on every developer to remember every edge case.\n\nData filtering and database-layer controls at a high level can support authorization by limiting which records queries can return. However, data filtering should not be confused with complete policy design. The system still needs to know which actions are allowed, how tenant boundaries are enforced, and how administrative exceptions work. Caching authorization decisions can improve performance, but stale permissions can create risk if changes are not reflected quickly enough.\n\nFail-closed behavior and error handling are important. If the authorization system cannot evaluate a decision, the safer default is usually to deny rather than allow. Error messages should not reveal sensitive resources or policy internals to unauthorized callers. A strong architecture makes safe behavior consistent, observable, and hard to bypass accidentally as the application evolves.",
  "narrationPoints": [
    "Authorization architecture defines where decisions are made and where they are enforced.",
    "Centralized authorization can improve consistency and governance, but it must be integrated carefully with application context.",
    "Data filtering and database-layer controls at a high level can support authorization by limiting which records queries can return.",
    "Fail-closed behavior and error handling are important."
  ]
};
