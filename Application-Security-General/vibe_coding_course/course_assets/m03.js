window.COURSE_MODULE = {
  "title": "Give the AI Useful Context",
  "graphicAlt": "A coding assistant receiving selected repository files, tests, interfaces, error logs, dependency versions, and project rules through a focused context funnel.",
  "narration": "An AI coding assistant does not automatically know the history, architecture, or conventions of a project. A short feature request may be enough for a toy example, but real repositories have existing interfaces, naming patterns, data models, tests, build commands, and tradeoffs. If the assistant cannot see the facts that govern the change, it will fill gaps with guesses.\n\nUseful context includes the files directly involved in the change, nearby tests, interface definitions, schemas, dependency versions, error messages, and the runtime environment. It also includes behavior that must remain unchanged. If you are adding a feature to an existing settings page, the assistant should see the current component, the service it calls, and the tests that describe current behavior. Without that grounding, the answer may be locally reasonable but inconsistent with the rest of the application.\n\nMore context is not always better context. Pasting an entire repository without direction can bury the important details under unrelated material. Good context is selected because it affects the decision at hand. A current type definition, migration file, test suite, or project README is usually more authoritative than an old comment or a confident guess made by the model.\n\nContext can also contain untrusted instructions. A log file, issue description, web page, or dependency document may include text that looks like a command to the assistant. Treat that material as project data unless you intentionally decide otherwise. The model should use it to understand the problem, not automatically follow every embedded instruction it encounters.\n\nSupplying context is an iterative skill. Start with the goal and the most relevant files. If the assistant identifies a dependency, or a failing test reveals an incorrect assumption, add the next piece of evidence. The aim is not to overwhelm the model. The aim is to give it enough reliable project truth to make a small, coherent change that fits the codebase.",
  "narrationPoints": [
    "Models do not automatically know the full project.",
    "Relevant files, tests, interfaces, and errors improve accuracy.",
    "Authoritative project sources matter more than stale comments or guesses.",
    "Untrusted embedded instructions should be treated as data.",
    "Context can be supplied incrementally as evidence appears."
  ]
};
