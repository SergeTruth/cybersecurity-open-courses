window.COURSE_MODULE = {
  "title": "Deployment, Testing, and Maintenance",
  "graphicAlt": "Blank placeholder image for Deployment, Testing, and Maintenance.",
  "narration": "Security headers can be applied in several places: application code, web servers, reverse proxies, CDNs, load balancers, service meshes, and cloud platform configuration. The right location depends on ownership and consistency. Some headers are best controlled by the application because they vary by route or page. Others can be standardized at the edge. Teams should know where each header is set and which layer wins when configurations overlap.\n\nEnvironment-specific configuration matters. Development, staging, and production may use different domains, script sources, API endpoints, CDNs, identity providers, and partner integrations. A policy that is perfect in production may break staging if staging uses different hostnames. A staging policy that allows everything may hide production problems. Mature teams define environment differences deliberately and avoid letting temporary exceptions become permanent.\n\nTesting should combine tools and real workflows. Header scanners can identify missing or weak headers, but they cannot always know whether a policy matches application behavior. Browser developer tools can show blocked resources, mixed content warnings, CSP reports, and frame restrictions. Automated checks in CI/CD can verify expected headers on key routes. Manual validation should cover authentication, embedded pages, uploads, downloads, and third-party integrations.\n\nAvoid cargo-cult copy-paste header sets. A header list from a blog may be a useful starting point, but every directive should have a reason. Document exceptions, owners, and review dates. Revisit headers when architecture changes, when a new frontend framework is introduced, when a CDN changes, when partners are added, or when a page starts using browser capabilities it did not previously need. Headers are configuration, but they are also security design.",
  "narrationPoints": [
    "Security headers can be applied in several places: application code, web servers, reverse proxies, CDNs, load balancers, service meshes, and c...",
    "Environment-specific configuration matters.",
    "Testing should combine tools and real workflows.",
    "Avoid cargo-cult copy-paste header sets."
  ]
};
