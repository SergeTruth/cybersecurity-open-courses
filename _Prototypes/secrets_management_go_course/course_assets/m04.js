window.COURSE_MODULE = {
  "title": "Keeping Secrets Out of Source, Logs, and Errors",
  "graphicAlt": "Conceptual visual of redaction controls protecting secrets from source files, logs, errors, and serialized objects.",
  "narration": "Many secret leaks in Go services are not caused by a secret store failure. They are caused by ordinary developer convenience. A fmt.Printf call added during debugging prints a full config struct. A structured log records an Authorization header. A wrapped error includes a database DSN. A panic dumps a request object. A test fixture contains a realistic token. A golden file preserves output that included credentials. A JSON response accidentally serializes an internal field. These are small local decisions that become durable exposure paths.\n\nThe rule is not simply do not log the password. Modern services carry secrets in shapes that do not look like a variable named password. A DSN can include username and password. An HTTP Authorization header can contain a bearer token. A webhook signature secret may be part of a verifier configuration. A private key may be loaded as PEM bytes. A cloud credential may be a structured object. Logging the entire object because it is convenient can expose the secret even when no one typed log password.\n\nRedaction should be a design pattern, not a heroic last-minute filter. Secret-bearing types can implement String carefully so fmt does not reveal sensitive fields. Logging helpers can accept safe summaries instead of raw config. Structs intended for JSON should avoid exporting secret fields, or should use explicit redacted views. Errors should identify what failed, such as database connection setup failed, without embedding the credential that was used. When context is needed, include nonsecret identifiers such as environment, service name, host alias, or provider key name, not the value itself.\n\nTests deserve the same discipline. Use fake values that cannot unlock real systems. Keep .env files out of source control. Avoid copying real production headers into fixtures. If a test needs to prove redaction works, assert that the output contains REDACTED and does not contain the original value. This turns leak prevention into a behavior the codebase checks.\n\nIn Go, you cannot rely on every future caller to remember which fields are dangerous. Build safer defaults into the types and helpers they use. If printing a config object is unsafe, make the printed representation safe. If logs need request metadata, provide a function that extracts only approved fields. Good redaction does not make secrets harmless, but it reduces the number of accidental paths where a routine debug action becomes a credential incident.\n\nReviewers should look for indirect logging too. A helper may not log Password directly, but it may log a request, config, URL, or error that contains the password inside another field. The safest pattern is to design safe views for diagnostics. Give the logger only the fields that are approved for disclosure. When the unsafe value never reaches the logging call, redaction does not depend on memory or luck.",
  "narrationPoints": [
    "Leak paths include debugging prints, logs, errors, tests, and JSON output.",
    "Full config objects can accidentally expose DSNs and tokens.",
    "Redaction should be built into secret-bearing types.",
    "Errors should describe failure without including secret values.",
    "Tests and fixtures need the same leak discipline as production code."
  ]
};
