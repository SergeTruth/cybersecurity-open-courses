window.COURSE_MODULE = {
  "title": "What Counts as a Secret in a Go Application",
  "graphicAlt": "Conceptual visual of a Go service separating ordinary configuration, identifiers, and sensitive secrets.",
  "narration": "A secret in a Go application is any value that gives access, proves identity, protects integrity, or unlocks private material. Common examples include API keys, database passwords, OAuth client secrets, signing keys, TLS private keys, service account tokens, webhook signing secrets, and cloud credentials. If disclosure of the value would let another person or process call an internal service, connect to a database, impersonate a workload, decrypt protected data, or forge trusted messages, treat it as a secret.\n\nNot every configuration value is a secret. A port number, feature flag name, public client identifier, service URL, or region setting may be configuration without being sensitive by itself. Some identifiers are safe to share, while others become sensitive when combined with a secret. For example, a database host name may not be a secret, but the username and password used to connect to it usually are. A cloud account ID may be an identifier, while the access key material is a secret. The engineering habit is to classify values by consequence, not by variable name.\n\nGo services often touch secrets early. A server may read configuration, construct dependencies, open a database pool, create HTTP clients, configure TLS, initialize background workers, or connect to message queues before it starts accepting traffic. This makes startup a critical control point. Missing secrets should be detected clearly. Misplaced secrets should not be printed. Insecure defaults should not quietly replace required production values.\n\nSecrets management is more than hiding strings. A secret has a lifecycle. Someone or something creates it. A system stores it. A workload receives it. Code uses it. Operators rotate it. Auditors review access. Incident responders revoke it when needed. Eventually it is deleted. If a team only thinks about where to put the value today, it will miss the questions that matter tomorrow: who owns it, what can it access, how long should it live, how will it be rotated, and what happens if it is exposed.\n\nIn Go, clean secrets management usually shows up as boring code and clear boundaries. The application loads required values through a small configuration layer. It validates them without logging them. It passes credentials only to the components that need them. It redacts secret-bearing structs. It uses context timeouts when calling external providers. It avoids printing full request headers, DSNs, or raw configuration objects. The goal is not cleverness. The goal is a service that can start, run, fail, rotate, and be investigated without leaking the values that protect the system.\n\nA practical classification habit is to ask three questions during design review. What can this value access? Who or what is allowed to use it? What would we need to rotate if it appeared in a log, ticket, or repository? Those questions keep the discussion grounded. They also help Go teams avoid treating every string as equally sensitive while still protecting the values that can change system trust.",
  "narrationPoints": [
    "Secrets include credentials, keys, tokens, and private material.",
    "Configuration and identifiers are not always secrets.",
    "Go services often load secrets during startup wiring.",
    "Secrets need lifecycle engineering, not string hiding.",
    "Every secret should have a clear purpose and boundary."
  ]
};
