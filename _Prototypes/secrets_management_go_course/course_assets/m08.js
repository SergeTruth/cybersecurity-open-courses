window.COURSE_MODULE = {
  "title": "Testing, CI/CD, and Local Development",
  "graphicAlt": "Conceptual visual of local development, tests, CI/CD pipelines, and secret scanning controls around a Go service.",
  "narration": "Secrets management is tested as much in the development workflow as in production. Local development often uses .env-like files, shell variables, or local configuration files. That can be fine for developer convenience, but those files must stay out of source control, use development-only values, and avoid sharing production credentials. A local secret should not unlock production simply because the code path is the same.\n\nGo tests are a good place to enforce boundaries. Instead of calling a real managed secret store in ordinary unit tests, define a small provider interface and use a fake implementation. The fake returns known nonsecret values or controlled errors. This makes tests faster, more reliable, and safer. It also lets you test fail-closed behavior: missing secret, empty value, provider timeout, revoked value, and refresh failure. Integration tests can exercise a real provider when needed, but they should run in a controlled environment with dedicated test secrets and clear cleanup.\n\nAvoid realistic secrets in fixtures. A token that looks real can trigger scanners and confuse reviewers. Worse, a copied production header in a fixture may actually work. Use obvious nonworking values such as test-only-token or example.invalid when a placeholder is enough. If a test proves redaction, assert that sensitive input is absent from output. If a config loader fails, assert that the error names the missing key without including the secret value.\n\nCI and CD systems need special care. Pipeline variables, logs, artifacts, cache directories, test reports, and container build output may all expose values. Treat pipeline output as sensitive. Disable shell tracing around secret-handling steps. Avoid commands that print entire environments. Do not bake secrets into Docker images or compiled binaries. If a build needs access to a secret, prefer a scoped CI secret or workload identity that is available only to the required job and environment.\n\nSecret scanning is a useful control, not a perfect guarantee. Scanners can catch common key formats, high-entropy strings, and known credential patterns before they merge. They should run in pre-commit hooks, pull requests, or CI gates where possible. But scanning does not replace code review, redaction, or rotation planning. If a real secret is committed, treat it as exposed and rotate it.\n\nFinally, keep environments separate. Development, test, staging, and production should not share credentials. A staging bug should not expose production access. A developer laptop should not need production secrets for normal work. The safer workflow is boring: fake providers in unit tests, dedicated test credentials for integration tests, scoped CI secrets, scanning gates, and production access only where the service truly needs it.\n\nLocal development should also teach the production habit. If developers routinely run the service with explicit nonworking values, fake providers, and ignored local files, the safe path becomes normal. If the easiest path requires copying production secrets, the workflow is training people into risk. Good tooling makes the secure path the fast path for everyday Go work.",
  "narrationPoints": [
    "Local secrets should stay out of git and production paths.",
    "Tests should use fake providers and nonworking values.",
    "CI pipeline output and artifacts can expose secrets.",
    "Secret scanning helps block risky commits and builds.",
    "Dev, test, staging, and production secrets should stay separated."
  ]
};
