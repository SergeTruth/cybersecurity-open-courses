window.COURSE_MODULE = {
  "title": "Rotation, Caching, and Failure Modes",
  "graphicAlt": "Conceptual visual of credential rotation, cache refresh, stale secret detection, and resilient service behavior.",
  "narration": "Secrets change. Sometimes they change on a schedule. Sometimes they change because a person leaves, a token expires, a provider rotates a key, or an incident requires revocation. A Go service that assumes credentials never change will eventually turn a routine rotation into an outage. Rotation needs to be part of the design, not a surprising production exercise.\n\nStatic secrets, such as a long-lived database password or webhook signing secret, are simple to understand but risky when they live too long. They need an owner, rotation interval, and replacement procedure. Dynamic or short-lived credentials reduce exposure time because they expire automatically, but they require refresh logic. A service using dynamic database credentials, for example, must understand when connections need to be recreated and how to handle credentials that expire while work is in flight.\n\nCaching is a tradeoff. A local cache reduces latency and avoids calling the provider for every request. It can also reduce provider load and make the service more resilient during brief provider interruptions. But cached secrets can become stale. They can delay revocation. They can cause multiple service instances to use different versions during a rollout. The cache needs a time-to-live, refresh behavior, and error policy that matches the risk of the secret and the needs of the service.\n\nRefresh logic should avoid two extremes. One extreme is panic on any provider error, which can take down healthy traffic during a short provider outage. The other extreme is use an old secret forever, which defeats rotation and revocation. A better design is explicit: use a valid cached value until its allowed lifetime ends, refresh in the background when possible, fail closed when no valid value exists, and report metrics when refresh fails. The exact policy depends on the system, but the policy should be written down and tested.\n\nCommon failure modes include an expired provider token, a revoked credential, a provider outage, a partial rollout where only some instances receive the new value, a rotated database password while old connections remain open, and clients that verify signatures with only one active key. Many production systems need overlap windows, where old and new secrets both work briefly. Others need immediate revocation, where overlap is unacceptable.\n\nFor Go engineers, the rotation question is practical: how does this dependency learn about a new secret, how does it replace old state, how does it behave when refresh fails, and how will operators see what happened? If those answers are clear before an incident, rotation becomes maintenance instead of drama.\n\nRotation should be observable. Operators need to know when the last refresh succeeded, which version or generation is active when that information is safe to expose, and whether failures are increasing. Metrics and logs should describe refresh health without printing the secret. This lets teams detect stale credentials, provider outages, or partial rollout problems before customers experience a hard failure.",
  "narrationPoints": [
    "Static secrets need planned rotation and revocation.",
    "Dynamic credentials reduce lifetime but require refresh logic.",
    "Caches trade latency for staleness and revocation delay.",
    "Refresh logic should handle provider failures and partial rollout.",
    "Rotation plans need tests, metrics, and rollback thinking."
  ]
};
