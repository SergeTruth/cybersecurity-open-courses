window.COURSE_MODULE = {
  "title": "The Secret Lifecycle",
  "graphicAlt": "Conceptual visual of a secret moving through creation, storage, access, rotation, revocation, audit, and deletion.",
  "narration": "The secret lifecycle starts before application code reads a value. A secret is created for a purpose, stored in a location, granted to a workload or person, used by code, rotated on a schedule or after an event, audited for access, revoked when trust changes, and deleted when it is no longer needed. Each phase is a chance to reduce risk or accidentally expand it.\n\nHardcoding a secret is dangerous even when the repository is private. Source control is optimized for memory. Git history, forks, pull request diffs, code review tools, local clones, backups, and build systems can preserve a value long after the line is removed. Once a secret enters source control, the right response is usually rotation, not just deletion. Removing the visible string may clean the current file, but it does not make old commits, build logs, or developer machines forget.\n\nSecret exposure also happens outside code. A Go service can leak a database password through a formatted error, a panic, a test fixture, a golden file, or a structured log field. A CI job can print environment variables during debugging. A Dockerfile can copy a secret into a build layer. A crash dump can contain process memory. A binary or container image can carry embedded configuration if the build process substituted values at compile time. These paths matter because attackers and incident responders both look beyond the current source file.\n\nOwnership makes lifecycle management possible. Every secret should have a purpose, an owner, an environment, a permission boundary, and a rotation path. Purpose answers why the secret exists. Owner answers who can approve changes and respond to exposure. Environment separates development, test, staging, and production. Permission boundary defines what the value can do. Rotation path explains how to replace it without guessing during an outage.\n\nFor Go engineers, lifecycle thinking changes design. Instead of passing a raw string through many packages, you define where the value enters the program and which dependency receives it. Instead of letting missing values fall back to insecure defaults, you fail closed at startup. Instead of logging full configuration for convenience, you implement redaction. Instead of assuming a database password is permanent, you design for rotation and connection refresh. The lifecycle is not paperwork around the code. It is part of the system behavior that keeps a service recoverable when credentials change or leak.\n\nA useful lifecycle review also asks where the secret might be copied automatically. Build caches, deployment manifests, container registries, crash reporting tools, and observability pipelines may all receive data from the application path. The safest teams assume that a secret can travel unless the system is designed to stop it. That assumption leads to better rotation procedures, cleaner access reviews, and fewer surprises during incident response.",
  "narrationPoints": [
    "A secret has creation, storage, access, use, and deletion phases.",
    "Private repositories still preserve secret exposure in history.",
    "Logs, build artifacts, Docker layers, and CI output can leak values.",
    "Every secret needs an owner, purpose, environment, and rotation path.",
    "Lifecycle thinking supports revocation and incident response."
  ]
};
