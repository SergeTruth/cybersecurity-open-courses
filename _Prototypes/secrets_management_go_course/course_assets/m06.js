window.COURSE_MODULE = {
  "title": "Using Secrets Safely at Runtime",
  "graphicAlt": "Conceptual visual of a Go service passing secrets only to required runtime consumers with least privilege boundaries.",
  "narration": "After a Go service loads a secret, the next question is where that value goes. Runtime consumers include database connection pools, HTTP clients that set Authorization headers, TLS clients that use private keys, webhook verifiers, token signers, message queue clients, and cloud SDKs. A common mistake is to pass an entire config object everywhere because it is easy. That expands the number of packages, tests, logs, and developers that can accidentally touch the secret.\n\nPrefer narrow construction. The database package receives the database DSN or credential object because it needs it. The webhook verifier receives the webhook secret because it validates signatures. The HTTP client constructor receives a token source or credential provider because it attaches authorization. Other packages receive nonsecret settings or already constructed dependencies. This style makes the permission boundary visible in code.\n\nRuntime memory deserves honest language. Go strings are convenient, immutable, and widely used by libraries. They are not ideal when you need guaranteed memory wiping, because copying, garbage collection, compiler behavior, and library internals make strong zeroization promises difficult. For most web services, practical controls usually matter more: use least privilege, prefer short-lived credentials, isolate processes, avoid unnecessary copies, avoid debug dumps, and keep secrets out of broad global variables.\n\nDatabase DSNs are a good example. A DSN may include host, username, password, database name, and connection options. It is tempting to log the DSN when connection setup fails. Do not. Log the safe parts: database alias, host if approved, environment, and error class. Pass the DSN only to the driver or connection layer. If your driver supports structured credential options or token sources, consider those patterns because they can make rotation and redaction easier.\n\nHTTP Authorization headers need similar treatment. Build them as close as possible to the outbound request path. Do not store full headers in shared request logs. If you must record request metadata, record that authorization was present, not the credential. TLS private keys and signing keys should be loaded through controlled paths and handed only to the TLS configuration or signing component that needs them.\n\nThe runtime goal is controlled usefulness. A secret exists so the service can do required work. It should not become general application state. Keep it scoped, pass it deliberately, avoid exposing it through diagnostics, and assume that someday it will need to change while the service keeps operating.\n\nBe especially careful with helper functions that seem harmless. A function that builds an HTTP client, formats a DSN, or constructs a signer may create extra copies of the secret. That is sometimes unavoidable, but it should be intentional. Keep constructors narrow, avoid returning secret-bearing structs to broad callers, and document which component owns refresh or replacement when the credential changes.",
  "narrationPoints": [
    "Runtime consumers include databases, HTTP clients, TLS keys, and signers.",
    "Pass secrets only to components that need them.",
    "Go strings are convenient but do not guarantee memory wiping.",
    "Short-lived credentials and least privilege reduce exposure impact.",
    "Avoid unnecessary copies, debug exposure, and broad global state."
  ]
};
