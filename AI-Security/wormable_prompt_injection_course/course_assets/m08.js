window.COURSE_MODULE = {
  "title": "Course Summary and Secure Design Checklist",
  "narration": "Wormable prompt injection is best understood as a systems problem. The model matters, but the model is only one part of the chain. The application retrieves content, formats context, chooses tools, stores memory, writes outputs, connects accounts, and decides whether actions require approval. Propagation appears when those pieces allow untrusted instructions to move from one surface to another with increasing authority.\n\nThe first design habit is to classify content by trust. User intent, system instructions, retrieved documents, tool outputs, memory entries, model drafts, and executable actions are different things. Keep them visibly separate in prompts, logs, schemas, and authorization logic. Do not let untrusted content rewrite the rules of the workflow.\n\nThe second habit is least privilege. Agents should use narrow identities, narrow connectors, and narrow action scopes. Read access should be separated from write access. Drafting should be separated from sending. Search should be separated from update. High-risk actions should have human approval or deterministic policy gates.\n\nThe third habit is propagation control. Limit where model-generated content can be stored, forwarded, indexed, or synchronized. Preserve source provenance. Filter retrieval. Monitor unusual fan-out. Rate-limit outbound activity. Add canary and audit signals that make suspicious copying visible before it becomes a broader incident.\n\nThe final habit is honest assurance. No current defense fully eliminates prompt injection when models process untrusted content. A mature architecture assumes that failures are possible and designs for containment. The secure checklist is simple to state and demanding to implement: separate instruction from data, constrain authority, verify actions, preserve provenance, limit persistence, monitor propagation, and rehearse response. If a team can explain those controls for each AI workflow, it is much closer to managing wormability as an engineering risk rather than a vague fear.",
  "narrationPoints": [
    "Wormable prompt injection is a systems and workflow risk.",
    "Classify content by trust and do not let data rewrite workflow rules.",
    "Least privilege and approval gates limit agent blast radius.",
    "A mature design assumes failures are possible and builds containment."
  ]
};
