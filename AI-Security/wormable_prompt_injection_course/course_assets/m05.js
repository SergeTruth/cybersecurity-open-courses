window.COURSE_MODULE = {
  "title": "Agent Permissions, Connectors, and Blast Radius",
  "narration": "Wormable prompt injection becomes more serious when the model is part of an agent. An agent may plan steps, call tools, browse, send messages, edit records, update files, or trigger workflows. The model is no longer only generating text for a human to read. It may be influencing operations across connected systems.\n\nThe core design mistake is excessive agency. If the agent has broad credentials, broad tool access, write permissions, and unattended execution, prompt injection can become an authorization problem. The model may not understand the security boundary, but the application still gave it a way to cross that boundary. OWASP emphasizes least privilege and human approval for high-risk actions because the surrounding system must compensate for model uncertainty.\n\nA connector should be treated like an API surface, not like a harmless convenience feature. Email, calendar, storage, source control, ticketing, CRM, cloud, chat, and browser connectors all carry different consequences. Reading a page is not the same as sending a message. Drafting a recommendation is not the same as submitting a transaction. Permission scopes should reflect those differences.\n\nBlast radius can be reduced with separate identities, narrow tokens, allowlisted tools, action-specific policies, rate limits, destination controls, and approval gates. A low-risk summarization task should not run with the same authority as a workflow that can message customers or change production configuration. When possible, split read, draft, and execute privileges into different steps. Review connector permissions by use case, not by product category, because the same connector may be low risk in one workflow and high risk in another.\n\nThe safest agent designs assume the model can be influenced by untrusted context. They place meaningful controls around what the model can observe, decide, and do. That is how a prompt injection becomes a contained error instead of a cross-system propagation event. The goal is not to remove useful automation; it is to make automation proportional to the trustworthiness of its inputs and the consequence of its actions.",
  "narrationPoints": [
    "Agentic systems raise the impact because models can influence actions.",
    "Excessive agency turns prompt injection into an authorization and workflow problem.",
    "Connectors should be scoped like API surfaces.",
    "Separate identities, narrow tokens, and approval gates reduce blast radius."
  ]
};
