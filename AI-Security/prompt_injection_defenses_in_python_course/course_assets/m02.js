window.COURSE_MODULE = {
  "title": "AI Application Architecture and Trust Boundaries",
  "graphicAlt": "Blank placeholder image for a lesson on AI application architecture and trust boundaries.",
  "narration": "A defensible AI application architecture names the major sources of instruction, data, identity, and action. Users provide messages and goals. System and developer instructions define trusted behavior. Retrieved documents, uploaded files, web pages, emails, tickets, tool responses, model outputs, logs, and memory provide information with different levels of authority. If those sources are mixed together without labels or controls, the model may treat low-trust content as if it were application policy.\n\nTrust boundaries exist wherever data, instructions, identity, credentials, or control moves between components with different authority. A user's message crosses one boundary. A retrieved document crosses another. A tool response crosses another. Model output crossing into a database update, email, ticket, or API call crosses a high-impact boundary. These boundaries are where Python code should validate context, enforce permissions, constrain tools, and decide whether human review is needed.\n\nAuthority levels matter. Trusted configuration can define system rules, allowed tools, data sources, user roles, and approval requirements. Untrusted content can provide evidence, facts, user intent, or business context, but it should not be allowed to grant itself permissions or rewrite the rules. A document can say what the document says. It should not be able to decide who may read other documents, call a tool, change a workflow, or bypass review.\n\nThe practical design goal is to make security decisions reviewable. A reviewer should be able to find where prompts are assembled, where retrieved content is labeled, where authorization checks happen, where tools are invoked, where outputs are validated, and where audit events are written. When those decisions are hidden inside free-form model text, they are difficult to test. When they are enforced in application structure, they become normal engineering controls.",
  "narrationPoints": [
    "A defensible AI application architecture names the major sources of instruction, data, identity, and action.",
    "Users provide messages and goals.",
    "System and developer instructions define trusted behavior.",
    "Retrieved documents, uploaded files, web pages, emails, tickets, tool responses, model outputs, logs, and memory provide information with different levels of authority.",
    "If those sources are mixed together without labels or controls, the model may treat low-trust content as if it were application policy.",
    "Trust boundaries exist wherever data, instructions, identity, credentials, or control moves between components with different authority."
  ]
};
