window.COURSE_MODULE = {
  "title": "Direct and Indirect Injection Risk Patterns",
  "graphicAlt": "Blank placeholder image for a lesson on direct and indirect prompt injection risk patterns.",
  "narration": "Direct injection risk appears when a user enters instructions that conflict with the application's intended rules. In a well-designed system, the user can ask for help, ask questions, and provide goals, but the user's message should not be able to change the permission model, expand tool access, retrieve unauthorized data, or disable review steps. The application should treat the user request as intent that must be evaluated within policy.\n\nIndirect injection risk appears when untrusted instructions are embedded in content the system later reads. Documents, web pages, emails, tickets, code comments, repository files, PDFs, logs, and knowledge-base articles can all contain text that looks like directions to the model. The system may retrieve that content because it seems relevant to the user's task. The defense is to label it as content from a source, not as a new instruction layer.\n\nSome content is unreliable even when it is not intentionally hostile. It may be stale, incomplete, misleading, copied from another context, written for a different audience, or inconsistent with other sources. Conflicting instructions and cross-document disagreements should not be resolved by letting the most forceful text win. The application should preserve provenance, show evidence, communicate uncertainty, and use authoritative sources where the decision matters.\n\nIngestion pipelines, retrieval systems, and tool outputs all need trust review because they decide what content reaches the model. A pipeline that indexes every document without access metadata can create exposure later. A retrieval system that mixes tenants or departments can leak context. A tool output that is treated as trusted instruction can redirect behavior. Defensive design assumes some content is adversarial or unreliable and limits what that content can influence.",
  "narrationPoints": [
    "Direct injection risk appears when a user enters instructions that conflict with the application's intended rules.",
    "In a well-designed system, the user can ask for help, ask questions, and provide goals, but the user's message should not be able to change the permission model...",
    "The application should treat the user request as intent that must be evaluated within policy.",
    "Indirect injection risk appears when untrusted instructions are embedded in content the system later reads.",
    "Documents, web pages, emails, tickets, code comments, repository files, PDFs, logs, and knowledge-base articles can all contain text that looks like directions to the...",
    "The system may retrieve that content because it seems relevant to the user's task."
  ]
};
