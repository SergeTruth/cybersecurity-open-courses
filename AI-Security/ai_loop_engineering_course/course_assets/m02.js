window.COURSE_MODULE = {
  "title": "Define the Goal, Evidence, and Stop Conditions",
  "graphicAlt": "A task-contract document connecting a goal to permitted scope, operational constraints, acceptance criteria, required evidence, failure states, approval requirements, escalation paths, and resource limits.",
  "narration": "A loop cannot decide that it has succeeded unless success has been defined in observable terms. If the goal is only \"make this better,\" the loop has no stable reference point. It may rewrite working output, chase cosmetic improvements, spend budget on irrelevant actions, or declare completion based on a fluent summary rather than evidence.\n\nThe task contract is the shared agreement that guides the loop. It should identify the intended outcome, permitted scope, available inputs, expected outputs, operational constraints, acceptance criteria, required evidence, failure conditions, escalation conditions, and resource limits. The contract does not need to be a long legal document. It needs to be precise enough that planning, tool use, evaluation, and termination all point to the same objective. It also gives reviewers a compact way to decide whether the loop stayed inside its assignment.\n\nIn the coding example, a vague goal is \"improve this code.\" A stronger contract is \"make the three identified tests pass without changing the public interface or adding dependencies.\" That statement provides a target, a boundary, and an evaluation path. The loop may inspect selected files, edit within the repository, and run the approved test command. It may not deploy, retrieve secrets, or modify unrelated directories.\n\nCompletion also needs evidence. A model statement that the task is complete is a claim, not proof. Useful evidence might be a passing test, valid schema, successful build, matching checksum, policy approval, accepted diff, or explicit human acceptance. When evidence is unavailable, the loop should report uncertainty rather than convert confidence into completion. This distinction prevents fluent status messages from replacing engineering verification.\n\nA strong loop has more than a success state. It may stop because the task succeeded, required information is missing, current access is insufficient, a policy blocked the next action, the budget was exhausted, progress stopped, or human review is required. \"Continue until done\" is unsafe because \"done\" has not been made measurable. Clear stop conditions keep the system from confusing motion with progress and give every later component a contract it can enforce.",
  "narrationPoints": [
    "Observable success criteria make loop completion testable.",
    "A task contract defines scope, constraints, evidence, limits, and escalation.",
    "Completion claims require external evidence.",
    "Success is only one possible terminal state.",
    "Missing information and policy blocks should create controlled stops.",
    "A measurable definition of done prevents uncontrolled retries."
  ]
};
