window.COURSE_MODULE = {
  "title": "Engineer Tools and Action Boundaries",
  "graphicAlt": "An AI model proposing actions through a guarded tool gateway with schemas, argument validation, permission checks, bounded tools, audit logging, and separated read, write, publish, and deploy capabilities.",
  "narration": "Tools turn model output into effects. A model response by itself may only propose, summarize, or explain. A tool can read files, modify code, query a database, send a message, or change infrastructure. That difference is why tool design belongs at the center of loop engineering. The moment a tool executes, an abstract action intent becomes operational behavior.\n\nTreat tool design as an application programming interface problem. Each tool should have a narrow purpose, clear name, structured input schema, validated arguments, defined preconditions, defined outputs, bounded side effects, meaningful error responses, and auditable execution records. A vague tool such as \"run anything\" gives the model a large action surface. A tool such as \"run approved repository tests\" is easier to reason about and safer to authorize. The interface should make the safe action easy and the unsafe action impossible.\n\nLeast-capability design gives the loop only the tools required for the task. Separate read operations from write operations. Separate draft generation from publication. Separate a deployment recommendation from the authority to deploy. A loop that can inspect a repository and propose a patch does not automatically need access to secrets, production systems, unrelated directories, or arbitrary outbound communication.\n\nTool output is an observation, not automatically trusted truth. Results may be incomplete, stale, malformed, or adversarial. A file, log, email, web page, or test output may contain text that attempts to redirect the model. The loop should normalize observations, validate their structure, preserve provenance, and treat embedded instructions as data unless they come from an authorized instruction source.\n\nIdempotency also matters. An idempotent action can be repeated without creating duplicate records or applying the same effect twice. When an operation is not idempotent, the loop should record a unique operation identifier and check whether the action already occurred before retrying it. In the coding loop, bounded tools might list repository files, read selected files, apply a patch inside the repository, and run an approved test command. Reliability comes from ensuring only valid, bounded actions can execute.",
  "narrationPoints": [
    "Tools convert model proposals into real effects.",
    "Tool interfaces should be narrow, structured, validated, and auditable.",
    "General-purpose tools increase capability and risk surface.",
    "Read, write, publish, and deploy permissions should be separated.",
    "Tool outputs are observations that require validation and provenance.",
    "Idempotent design reduces damage from retries."
  ]
};
