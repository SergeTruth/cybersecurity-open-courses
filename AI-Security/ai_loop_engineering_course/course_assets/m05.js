window.COURSE_MODULE = {
  "title": "Evaluate Results and Repair Failures",
  "graphicAlt": "An evaluator-optimizer loop in which generated work passes through deterministic tests, policy checks, a scored rubric, and human review before a focused repair is attempted.",
  "narration": "A loop needs a way to decide whether an action improved the task state. Without evaluation, the system may produce activity without progress. It might rewrite the same file several times, generate longer explanations, or run tools that do not move the task closer to a terminal condition. Evaluation is the feedback signal that separates useful iteration from noise.\n\nDifferent tasks need different evaluators. Deterministic checks include compiler results, schema validation, test suites, policy decisions, reference comparisons, reconciled totals, and programmatic assertions. Human review may be necessary when the judgment involves risk, product intent, or business meaning. Model-based grading can help assess qualities such as clarity or relevance, but it needs a rubric, examples, calibration, and periodic human review. A score without a tested rubric is just another generated claim.\n\nPrefer deterministic evidence when it is available. A passing test, valid configuration, or policy engine result usually gives a clearer signal than asking the same model whether its own output looks correct. The component that generated an artifact should not be treated as the only authority certifying it. Separation may come from deterministic checks, different context, an independent evaluator role, or a human reviewer.\n\nRepair should respond to specific failure evidence. Capture the failed criterion, preserve the relevant observation, form a concrete repair hypothesis, make the smallest justified change, rerun the relevant evaluation, and compare the new result with the previous state. If the same failure repeats without new evidence, the loop should stop, narrow the task, or escalate rather than regenerate randomly. That stop protects good partial work and prevents uncertainty from being hidden behind more output.\n\nIn the coding example, a failing test is not just a red mark. It is an observation. The next iteration should inspect the related code, propose a specific cause, apply a narrow patch, and rerun the targeted test before running the full suite. Randomly rewriting the entire feature can destroy working behavior and make cause and effect impossible to trace. The quality of evaluation determines whether the loop learns, stalls, or becomes falsely confident.",
  "narrationPoints": [
    "Activity is not the same as progress.",
    "Deterministic checks provide strong evidence when available.",
    "Model-based grading requires rubrics, examples, and calibration.",
    "The generator should not be the only evaluator of its output.",
    "Repairs should respond to specific failure evidence.",
    "Repeated failure without new evidence should trigger a stop or escalation."
  ]
};
