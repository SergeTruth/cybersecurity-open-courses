window.COURSE_MODULE = {
  "title": "Secure the Loop with Guardrails and Approval Gates",
  "graphicAlt": "A secure AI loop inside layered controls for sandboxing, least privilege, prompt-injection resistance, secret isolation, policy enforcement, human approval, audit logging, and emergency shutdown.",
  "narration": "Security controls must be part of the execution path, not only advisory text inside a prompt. A prompt can tell a model to be careful, but executable policy determines whether a tool call is allowed, which data can be reached, whether a credential is exposed, and when a human must approve the next step. Reliable security is enforced by the harness.\n\nDefense in depth for AI loops includes least-privilege identities, sandboxed execution, tool allowlists, structured input validation, output validation, data classification, secret isolation, network restrictions, policy enforcement, human approval gates, audit logging, and emergency termination. These controls overlap by design. If one layer fails or a model proposes an unsafe action, another layer should still restrict the effect. Monitoring helps detect problems, but it should not be the only protection against preventable damage.\n\nPrompt injection is a control-boundary problem. Retrieved documents, source files, web pages, logs, emails, and tool responses may contain text that attempts to redirect the model. That text should be treated as untrusted task data unless the system explicitly recognizes it as authorized instruction. Stable system policy and the task contract should outrank content encountered during execution.\n\nTool outputs must not grant new permissions or rewrite approval policy. A document cannot authorize the loop to expand its network access. A test log cannot permit credential retrieval. A source file comment cannot override the task contract. Instruction authority should be represented in the system design, not inferred from whichever text appears most recently in context.\n\nApproval gates should provide useful context to reviewers. Before production deployment, destructive data changes, credential use, external communications, financial transactions, access-control changes, expanded tool boundaries, unfamiliar downloaded code, or consequential publication, the reviewer needs the proposed action, expected effect, supporting evidence, affected resources, reversibility, and consequences of rejection. In the coding example, the loop can prepare a deployment recommendation, but it cannot obtain production credentials or deploy. A loop must never be able to grant itself greater authority by producing persuasive text.",
  "narrationPoints": [
    "Security controls belong in executable policy, not only prompts.",
    "Defense in depth limits the effect of unsafe proposals.",
    "Untrusted content must not become authoritative instruction.",
    "Tool output cannot grant new permissions.",
    "High-impact actions require contextual human approval.",
    "Credentials should stay outside model-visible context where possible.",
    "A loop must not authorize its own privilege expansion."
  ]
};
