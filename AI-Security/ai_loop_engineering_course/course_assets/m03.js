window.COURSE_MODULE = {
  "title": "Design the Loop State and Control Flow",
  "graphicAlt": "A state-machine diagram showing queued, running, waiting for approval, blocked, completed, failed, and budget-exhausted states around a controlled AI execution loop.",
  "narration": "A dependable loop needs explicit state. A growing conversation transcript is not enough. Transcripts are useful records, but they are not structured control data. They can bury the current objective, mix stale assumptions with fresh evidence, and make recovery difficult after interruption. State is the application-maintained record that tells the loop where it is and what has happened. It is the part operators can query without rereading every generated sentence.\n\nUseful state fields include a task identifier, current status, current objective or subtask, completed actions, pending actions, evidence collected, files or records changed, errors encountered, retry counts, budget consumed, approval status, last meaningful progress, and terminal reason. These fields do not all belong in every prompt. They belong in the harness so the application can reason about progress and safety.\n\nState is different from context. State is the structured record the application maintains. Context is the selected information supplied to the model for a particular iteration. A good loop selects the minimum useful context from state, source artifacts, recent observations, and stable policy. Copying the entire state and history into every call is expensive and can make important facts harder to find. Selective context also keeps obsolete attempts from competing with current evidence.\n\nThe control flow should be explicit. Load the task contract and current state. Construct focused context. Request one proposed next action. Validate that proposal against schema and policy. Execute one approved action. Normalize and record the observation. Evaluate progress. Update state. Then continue, pause, escalate, or terminate. Each iteration should produce a defined state transition, not just another paragraph of output.\n\nDurable state supports resumability and governance. If a coding loop pauses after editing two files and running one failing test, it should resume from a checkpoint without repeating every earlier action or assuming external conditions stayed fixed. Engineers should be able to see the test command, failing tests, edited files, current hypothesis, previous repair attempt, remaining budget, and stop reason without reconstructing the run from free-form text.",
  "narrationPoints": [
    "Explicit state is different from conversation history.",
    "State records status, evidence, actions, errors, approvals, and budgets.",
    "Context is selected from state and artifacts for one iteration.",
    "Each iteration should produce a defined state transition.",
    "Durable checkpoints support interruption and resumption.",
    "Structured state makes loop behavior explainable and auditable."
  ]
};
