window.COURSE_MODULE = {
  "title": "Decomposition and Iterative Prompting",
  "graphicAlt": "Multi-stage prompt workflow with intermediate review, revision, and final output.",
  "narration": "Complex tasks are often easier when divided into smaller stages. A document workflow might first extract facts, then organize them by theme, then draft a final summary. A software workflow might identify requirements, propose test cases, and then draft implementation notes. A research assistance workflow might gather source-supported claims, compare them, and then produce a brief with citations. Decomposition turns one large ambiguous request into smaller reviewable steps.\n\nSmaller tasks are easier to specify. Intermediate outputs can be inspected before they are used. Errors may be easier to locate. Different steps may use different prompts, tools, retrieval sources, or models. For example, an extraction step may require strict labeled fields, while a drafting step may require readable prose. Separating those steps can improve reliability and make failures easier to diagnose.\n\nMulti-step workflows have tradeoffs. More model calls can increase cost and latency. Errors can propagate between stages if an early extraction mistake becomes input to later drafting. Intermediate data may include sensitive content that needs protection. Workflow state, retries, caching, logging, and approvals become application concerns. Decomposition helps when it improves control, not merely because it adds steps.\n\nIterative prompting should also be controlled. Test the prompt. Inspect failures. Revise one important factor. Retest. Record what changed. Randomly adding instructions after every disappointing output can make the prompt longer and less reliable. A disciplined iteration cycle treats prompts as versioned components: define the expected behavior, test representative cases, change deliberately, and compare results.\n\nA practical iteration log can be simple. Record the prompt version, the reason for the change, the test cases affected, and the observed improvement or regression. If a summarization prompt starts omitting caveats, add a requirement for uncertainty handling and rerun the same cases. If a classification prompt confuses two labels, improve the label definitions or add examples. The goal is evidence-driven improvement, not prompt folklore.",
  "narrationPoints": [
    "Complex tasks can be divided into smaller stages.",
    "Intermediate outputs support review and debugging.",
    "Multi-step workflows add cost and complexity.",
    "Errors can propagate between stages.",
    "Prompt changes should be tested and documented."
  ]
};
