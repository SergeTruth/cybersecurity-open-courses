window.COURSE_MODULE = {
  "title": "Output Formats and Examples",
  "graphicAlt": "Comparison of unstructured output, structured output, zero-shot prompting, and few-shot examples.",
  "narration": "Output requirements make responses easier to review, reuse, or process. Useful formats include headings, bullet lists, tables, labeled fields, checklists, templates, or JSON-like structures. A support workflow may need fields for issue type, severity, customer impact, and suggested next action. A writing workflow may need a title, summary, assumptions, and revision notes. Structure helps humans scan results and helps software route or validate them.\n\nThe format should fit the downstream use. If a human will review the answer, clarity may matter more than strict syntax. If software will parse the result, the format should be simple and the application should still validate it. A model can produce malformed or unexpected output even when instructed carefully. Deterministic validation belongs in the application, not only in the prompt.\n\nExamples shape behavior. Zero-shot prompting gives instructions without examples. One-shot prompting gives one example. Few-shot prompting gives several representative examples. Examples can demonstrate labels, style, level of detail, transformations, edge-case handling, or formatting. For classification, examples can clarify the boundary between categories. For data extraction, examples can show how to handle missing fields.\n\nExamples also carry risk. Poor examples can introduce bias, accidental constraints, or contradictory patterns. If every example is short, the model may produce short answers even when a longer answer is needed. If examples ignore edge cases, the prompt may perform poorly on real inputs. Examples should represent ordinary cases and difficult cases when reliability matters. Treat examples as training signals inside the prompt, and test their effects rather than assuming they help.\n\nFor machine-consumed output, keep the schema modest. A huge nested structure may look precise but be harder for the model to produce reliably. Prefer clear field names, simple value rules, and explicit handling for unknown information. If the downstream system needs strict JSON, the application should parse and validate the output, then handle errors. Prompt wording can reduce format failures, but it should not be the only control.",
  "narrationPoints": [
    "Output formats make results easier to review.",
    "Machine-consumed output still requires validation.",
    "Zero-shot prompting uses no examples.",
    "Few-shot prompting demonstrates desired behavior.",
    "Examples can improve consistency or introduce bias."
  ]
};
