window.COURSE_MODULE = {
  "title": "Logging, Diagnostics, and Exposure Prevention",
  "graphicAlt": "Bullet summary graphic for Logging, Diagnostics, and Exposure Prevention.",
  "narration": "Secrets commonly leak through places that were intended to help troubleshooting: logs, exceptions, stack traces, telemetry, traces, health checks, crash dumps, screenshots, support bundles, debug pages, and diagnostic endpoints. These channels often have broader access and longer retention than the original secret store.\n\nRedaction and filtering reduce accidental exposure. Applications should avoid logging request headers, connection strings, tokens, keys, certificates, provider-specific credentials, and full configuration objects. Structured logging can capture safe categories, source names, validation outcomes, and correlation identifiers without printing secret values.\n\nFull configuration dumps are risky because sensitive values and ordinary settings sit side by side. Even if a dump is meant for authorized operators, it can be copied into tickets, chat, email, or support packages. Diagnostics should be designed to answer operational questions without revealing access material.\n\nDevelopment and production diagnostics should differ. Developers may need more detail locally, using development-only values. Production should provide safe user messages, protected operator logs, and correlation IDs. A development diagnostic feature should not become a production endpoint by accident.\n\nSupport and troubleshooting workflows deserve review. Screen sharing, screenshots, exported logs, support bundles, and incident notes can all capture sensitive values. Teams should define what may be collected, how it is redacted, where it is stored, who can access it, and when it is deleted.\n\nExposure prevention is not only about code. It is about habits, tooling, documentation, and operational guardrails that keep secrets from appearing in places where they cannot be controlled.",
  "narrationPoints": [
    "Secrets commonly leak through places that were intended to help troubleshooting: logs, exceptions, stack traces, telemetry, traces, health checks, crash dumps, screenshots, support bundles, debug pages, and diagnostic endpoints.",
    "Applications should avoid logging request headers, connection strings, tokens, keys, certificates, provider-specific credentials, and full configuration objects.",
    "Full configuration dumps are risky because sensitive values and ordinary settings sit side by side.",
    "Production should provide safe user messages, protected operator logs, and correlation IDs.",
    "Teams should define what may be collected, how it is redacted, where it is stored, who can access it, and when it is deleted."
  ]
};
