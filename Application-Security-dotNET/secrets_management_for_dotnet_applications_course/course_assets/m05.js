window.COURSE_MODULE = {
  "title": "Connection Strings, Certificates, Keys, and Tokens",
  "graphicAlt": "Bullet summary graphic for Connection Strings, Certificates, Keys, and Tokens.",
  "narration": "Connection strings should be treated as sensitive configuration. Even when a string does not visibly contain a password, it can reveal internal database names, provider details, routing assumptions, managed identity use, or service topology. It may also enable access through platform identity or integrated authentication.\n\nDatabase credentials and service accounts should be scoped to the application's real needs. A runtime application usually should not share the same access as migrations, administrators, reporting jobs, or emergency maintenance. Separate identities make ownership and least privilege easier to review.\n\nCertificates and private keys represent trust and identity. A certificate reference such as ExampleCertificateName may identify the material used to authenticate a service or establish trust. The private key behind that reference must be protected, access-controlled, rotated according to policy, and kept out of logs and exports.\n\nSigning keys and encryption keys are high-impact secrets. Signing material can determine whether data or tokens are trusted. Encryption material can protect confidentiality or unlock protected data. These secrets require ownership, rotation plans, backup or recovery decisions, and careful separation from unrelated environments.\n\nAPI keys, service tokens, and webhook secrets often act as bearer-style access material. Whoever can present the value may be treated as authorized by another system. That makes accidental display, logging, browser exposure, support exports, or reuse across unrelated environments especially risky.\n\nDifferent secret types require different handling. Authentication secrets, encryption material, signing material, integration credentials, and storage access each have different rotation, backup, scope, and monitoring needs. Avoid custom cryptographic handling and prefer platform-supported mechanisms and reviewed libraries.",
  "narrationPoints": [
    "Connection strings should be treated as sensitive configuration.",
    "Database credentials and service accounts should be scoped to the application's real needs.",
    "The private key behind that reference must be protected, access-controlled, rotated according to policy, and kept out of logs and exports.",
    "API keys, service tokens, and webhook secrets often act as bearer-style access material.",
    "Avoid custom cryptographic handling and prefer platform-supported mechanisms and reviewed libraries."
  ]
};
