window.COURSE_MODULE = {
  "title": "CI/CD, Containers, and Deployment Secrets",
  "graphicAlt": "Bullet summary graphic for CI/CD, Containers, and Deployment Secrets.",
  "narration": "Build and release pipelines often handle powerful secrets. CI/CD variables, service connections, deployment credentials, signing material, package publishing credentials, storage access, and cloud identities can all influence what ships and where it runs. Pipeline configuration is part of the application's security boundary.\n\nSeparate build-time and runtime secrets where possible. A build may need access to package feeds or signing systems, while the running application may need database or storage access. Keeping those responsibilities separate reduces the chance that an artifact, log, or test report carries runtime secrets forward.\n\nContainer images, image layers, build arguments, deployment manifests, generated files, and release bundles should not contain secret values. Once a secret is baked into an artifact, it can be copied through registries, caches, backups, and developer machines. Prefer runtime injection through approved secret mechanisms or workload identity patterns.\n\nPipeline variables and service connections need access control and review. A pipeline identity should have only the rights required to build, test, deploy, or sign the application. Production deployment credentials should not be available to every branch, every contributor, or every test job by default.\n\nContainer and orchestrator secrets are useful, but they still require lifecycle control. Teams should understand who can read them, how they are mounted or exposed, how rotation works, what appears in logs, and whether application code or sidecars cache values longer than expected.\n\nProduction secret changes should require approval and communication. A rotated ExampleApiKey or replaced ExampleCertificateName may require coordinated deployment, staged rollout, monitoring, and rollback planning. Secret changes are operational changes, not just configuration edits.",
  "narrationPoints": [
    "CI/CD variables, service connections, deployment credentials, signing material, package publishing credentials, storage access, and cloud identities can all influence what ships and where it runs.",
    "Keeping those responsibilities separate reduces the chance that an artifact, log, or test report carries runtime secrets forward.",
    "Prefer runtime injection through approved secret mechanisms or workload identity patterns.",
    "Container and orchestrator secrets are useful, but they still require lifecycle control.",
    "A rotated ExampleApiKey or replaced ExampleCertificateName may require coordinated deployment, staged rollout, monitoring, and rollback planning."
  ]
};
