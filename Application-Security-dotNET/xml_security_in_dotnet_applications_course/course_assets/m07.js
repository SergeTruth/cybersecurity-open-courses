window.COURSE_MODULE = {
  "title": "XSLT and Transformation Safety",
  "graphicAlt": "Bullet summary graphic for XSLT and Transformation Safety.",
  "narration": "XSLT is not just formatting. A stylesheet describes transformation logic that can select data, combine values, produce new documents, and sometimes interact with extension behavior depending on configuration. For security review, treat stylesheets as code-like input because they influence what the application reads and what it emits.\n\nDo not accept and execute untrusted stylesheets in ordinary application workflows. If a transformation is needed, keep the stylesheet in an application-controlled location, version it, review it, and deploy it deliberately. A partner-supplied or user-supplied stylesheet should be treated as a special design requirement, not as a default convenience feature.\n\nReview scripting, extension objects, external document access, imports, includes, parameters, and resolver behavior. These features can be useful in controlled environments, but they expand what the transformation can do. Keep them disabled unless there is a documented and reviewed reason to enable them.\n\nTransformation output needs its own review. A workflow may transform XML into HTML, JavaScript-bearing pages, CSV files, office documents, reports, emails, or downstream XML. Each target format has different output encoding, formula handling, content-type, data exposure, and viewer behavior considerations.\n\nErrors and diagnostics should be safe. Transformation failures can reveal local file paths, internal resource names, sensitive XML values, or configuration details. Production behavior should give users a clear failure outcome without dumping internals, while logs should retain safe operational context for troubleshooting.\n\nA safe XSLT baseline is straightforward: use trusted stylesheets, restrict optional features, keep resource access controlled, review output format risks, and document any exception. That turns transformations from a hidden behavior into an explicit part of the application design.",
  "narrationPoints": [
    "For security review, treat stylesheets as code-like input because they influence what the application reads and what it emits.",
    "If a transformation is needed, keep the stylesheet in an application-controlled location, version it, review it, and deploy it deliberately.",
    "Keep them disabled unless there is a documented and reviewed reason to enable them.",
    "Transformation failures can reveal local file paths, internal resource names, sensitive XML values, or configuration details.",
    "A safe XSLT baseline is straightforward: use trusted stylesheets, restrict optional features, keep resource access controlled, review output format risks, and document any exception."
  ]
};
