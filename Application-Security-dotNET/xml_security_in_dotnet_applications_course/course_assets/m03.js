window.COURSE_MODULE = {
  "title": "DTDs, External Entities, and External Resources",
  "graphicAlt": "Bullet summary graphic for DTDs, External Entities, and External Resources.",
  "narration": "DTDs and external resource resolution are among the most important XML features to review. They exist for legitimate reasons in some XML ecosystems, but most business data exchange workflows do not need them. When a workflow only expects structured business data, leaving extra document features enabled increases behavior that the application did not ask for and may not monitor.\n\nExternal entity and resolver behavior can cause an XML processor to attempt work outside the document itself. That can mean looking for resources in locations influenced by document content, configuration, or schema declarations. For untrusted XML, the safer default is usually to prohibit DTD processing and prevent broad resolver access unless the application has a documented requirement.\n\nThe key design question is who controls references. If the XML producer can influence where the parser looks for additional resources, the application has expanded its trust boundary. A safer design keeps schemas, DTDs, and related resources in trusted application-controlled locations, or disables those features entirely when they are not needed.\n\nIf a business process truly requires DTDs or external resources, treat that requirement as an exception that needs ownership. Document why it is required, which resources are permitted, how those resources are retrieved, what network or file access is allowed, and how changes are reviewed. A narrow, documented exception is easier to operate than an open-ended parser behavior.\n\nReview XmlResolver behavior wherever XML is parsed, validated, transformed, or signed. Resolver configuration should not grant broad file or network access by default. It should be clear whether the resolver is null, restricted, or intentionally pointed at controlled locations. The same review applies to schemas and transformations that may have their own resource-loading behavior.\n\nFailure handling matters as much as parser configuration. If a document references something that is not allowed, the application should fail predictably, log safe context, and avoid exposing local paths, internal addresses, credentials, secrets, or full parser diagnostics to users. Defensive XML handling includes both prevention and safe failure.",
  "narrationPoints": [
    "DTDs and external resource resolution are among the most important XML features to review.",
    "External entity and resolver behavior can cause an XML processor to attempt work outside the document itself.",
    "A safer design keeps schemas, DTDs, and related resources in trusted application-controlled locations, or disables those features entirely when they are not needed.",
    "If a business process truly requires DTDs or external resources, treat that requirement as an exception that needs ownership.",
    "If a document references something that is not allowed, the application should fail predictably, log safe context, and avoid exposing local paths, internal addresses, credentials, secrets, or full parser diagnostics to users."
  ]
};
