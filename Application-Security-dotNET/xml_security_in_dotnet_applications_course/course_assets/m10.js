window.COURSE_MODULE = {
  "title": "Course Summary: A Secure XML Processing Baseline",
  "graphicAlt": "Bullet summary graphic for Course Summary: A Secure XML Processing Baseline.",
  "narration": "A secure XML processing baseline for .NET applications starts by treating external XML as untrusted unless the application has a verified reason to do otherwise. The source of a document, the transport channel, or the partner name does not remove the need for controlled parsing, validation, resource limits, and business review.\n\nConfigure XML parsers explicitly. Decide which parser fits the workflow, define DTD behavior, control resolver behavior, set size and entity-related limits where appropriate, and avoid scattered parser construction that depends on uncertain defaults. Make exceptions rare, documented, and easy to review.\n\nUse trusted schemas and controlled validation contracts. Keep schemas versioned, reviewed, and deployed from known sources. Do not let an untrusted document decide where the application retrieves validation material. Remember that schema validation improves structure checks, but it does not replace authorization or business rules.\n\nKeep query and transformation behavior constrained. Avoid building XPath expressions directly from user input. Map user choices to predefined selectors where possible. Treat XSLT stylesheets as code-like input, use trusted stylesheets, and restrict external resource access and optional transformation features unless they are clearly required.\n\nFinally, handle trust and operations deliberately. Verify XML signatures only as part of a clear business decision. Redact logs and errors. Monitor parse failures, oversized documents, validation drift, transformation failures, and signature failures. Review production XML workflows as complete systems, from upload and parsing to downstream business action.",
  "narrationPoints": [
    "The source of a document, the transport channel, or the partner name does not remove the need for controlled parsing, validation, resource limits, and business review.",
    "Decide which parser fits the workflow, define DTD behavior, control resolver behavior, set size and entity-related limits where appropriate, and avoid scattered parser construction that depends on uncertain defaults.",
    "Remember that schema validation improves structure checks, but it does not replace authorization or business rules.",
    "Treat XSLT stylesheets as code-like input, use trusted stylesheets, and restrict external resource access and optional transformation features unless they are clearly required.",
    "Monitor parse failures, oversized documents, validation drift, transformation failures, and signature failures."
  ]
};
