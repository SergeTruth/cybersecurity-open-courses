window.COURSE_MODULE = {
  "title": "XML Denial of Service and Resource Limits",
  "graphicAlt": "Bullet summary graphic for XML Denial of Service and Resource Limits.",
  "narration": "XML processing can be resource intensive. The application may spend CPU parsing structure, memory holding nodes, network access resolving resources, disk space storing uploads, and time validating or transforming documents. Reliability problems can appear even when the document is not accepted as valid business data.\n\nLarge documents, deeply nested structures, expensive validation, and memory-heavy loading can all strain services. That is why size expectations should be part of the XML contract. The service should know the maximum request body size, maximum upload size, maximum document character count, timeout behavior, and what happens when a document exceeds those limits.\n\nIn .NET, parser settings such as maximum document characters and maximum characters from entities should be reviewed where relevant to the parsing path. The exact settings depend on the API and workflow, but the principle is consistent: the application should not accept unbounded work from an XML input source.\n\nControls should exist at more than one layer. Web servers, reverse proxies, gateways, upload handlers, queues, storage policies, parsers, validation code, and background workers may each need limits. Cancellation and timeout handling are also important so the application can stop work that no longer has a valid caller or useful business outcome.\n\nStreaming can help because the application can process incrementally and reject early, but streaming is not a substitute for a limit strategy. If the workflow eventually builds an in-memory object graph or performs validation, those later stages need controls too. The whole XML path should be reviewed from arrival to storage to parsing to downstream use.\n\nOperational monitoring completes the baseline. Track parsing failures, oversized documents, validation failures, timeouts, unusual XML volume, and repeated failures by workflow or partner. These signals help teams detect misconfiguration, broken integrations, and resource pressure before they become production incidents.",
  "narrationPoints": [
    "The service should know the maximum request body size, maximum upload size, maximum document character count, timeout behavior, and what happens when a document exceeds those limits.",
    "In .NET, parser settings such as maximum document characters and maximum characters from entities should be reviewed where relevant to the parsing path.",
    "Controls should exist at more than one layer.",
    "The whole XML path should be reviewed from arrival to storage to parsing to downstream use.",
    "Track parsing failures, oversized documents, validation failures, timeouts, unusual XML volume, and repeated failures by workflow or partner."
  ]
};
