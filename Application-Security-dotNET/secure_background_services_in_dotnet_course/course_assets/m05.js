window.COURSE_MODULE = {
  "title": "Authorization, Ownership, and Tenant Boundaries",
  "graphicAlt": "Bullet summary graphic for Authorization, Ownership, and Tenant Boundaries.",
  "narration": "Background work still needs access control. A worker may not have an interactive user at the moment it runs, but it often acts on behalf of a user, a tenant, an administrator, a scheduled policy, or a service process. The application should know which category applies because it affects authorization, audit records, error handling, and accountability.\n\nAuthentication and authorization are different in worker contexts. A service identity proves which workload is calling a database, queue, storage account, or API. Authorization decides whether this specific operation should occur on this specific object for this specific requester, tenant, or policy. A valid ExampleServiceIdentity should not automatically mean every job is allowed.\n\nObject-level authorization matters for records, files, reports, exports, jobs, and administrative actions. A worker may be allowed to generate reports, but not every tenant's report. It may be allowed to process imports, but only for jobs that are approved and still owned by the expected tenant. Access checks should follow the object and operation, not only the worker role.\n\nTenant boundaries deserve explicit checks. A tenant identifier in a message is a claim, not proof. The worker should verify ownership against authoritative data and preserve tenant context throughout processing, storage, logging, and external calls. Cross-tenant operations, support actions, migrations, and repairs should receive stronger review because mistakes can affect many users.\n\nUser-initiated jobs and system-initiated jobs need different audit context. A user-initiated ExampleImportJob should preserve the requester, approver if needed, tenant, job ID, and reason. A system-initiated cleanup job should preserve the service identity, policy, schedule, and affected scope. Audit trails should explain why the worker acted, not only that it acted.\n\nPrivileged workers should not perform actions that the requester was not allowed to trigger. This is the defensive version of a confused-deputy problem: a trusted worker can unintentionally lend its broader access to a lower-privilege request. Administrative, support, cleanup, migration, and repair jobs should be reviewed as high-impact paths with clear approval, scope, and logging.",
  "narrationPoints": [
    "The application should know which category applies because it affects authorization, audit records, error handling, and accountability.",
    "Authorization decides whether this specific operation should occur on this specific object for this specific requester, tenant, or policy.",
    "Access checks should follow the object and operation, not only the worker role.",
    "The worker should verify ownership against authoritative data and preserve tenant context throughout processing, storage, logging, and external calls.",
    "A system-initiated cleanup job should preserve the service identity, policy, schedule, and affected scope."
  ]
};
