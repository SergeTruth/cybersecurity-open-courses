window.COURSE_MODULE = {
  "title": "Data Handling, Files, and External Integrations",
  "graphicAlt": "Bullet summary graphic for Data Handling, Files, and External Integrations.",
  "narration": "Background services are common data movement points. They import files, export reports, synchronize records, transform data, generate documents, call external APIs, update storage, and move work between queues and databases. Because they often connect multiple systems, they can accidentally become places where normal validation, authorization, privacy, and retention rules are forgotten.\n\nFiles, serialized data, CSV rows, documents, and third-party responses should be treated as untrusted input. A worker may receive an ExampleImportJob from a trusted scheduling system, but the file it processes may still contain unexpected rows, missing fields, unsupported versions, or records that do not belong to the expected tenant. Parsing success is not proof that the data is safe to use.\n\nSafe data handling includes database updates, imports, exports, report generation, file processing, and synchronization. The worker should validate inputs, preserve tenant and ownership context, apply object-level authorization where appropriate, use controlled storage locations, and keep server-generated identifiers separate from user-facing names. Long-running jobs should not create shortcuts around the normal business rules.\n\nProtect sensitive data during processing, storage, export, and transmission. Temporary files, generated reports, intermediate artifacts, failed-job snapshots, and support bundles can contain personal data, business-sensitive data, or internal identifiers. They need retention limits, access control, secure transfer where appropriate, and deletion or archival rules that match the data's sensitivity.\n\nService-to-service calls and third-party integrations should use least privilege, clear timeouts, safe retry behavior, and scoped credentials. A background service should not use a broad credential for convenience when a narrower identity or API permission would work. External calls also need failure handling because partial success across databases, queues, storage, and APIs is a normal background-processing challenge.\n\nThe practical rule is simple: background services do not get to skip validation, authorization, or privacy rules because they run outside the web request. They may need different mechanisms, but the same security intent applies. If a user could not do something directly, the worker should not do it for that user without an approved workflow and audit context.",
  "narrationPoints": [
    "Because they often connect multiple systems, they can accidentally become places where normal validation, authorization, privacy, and retention rules are forgotten.",
    "Files, serialized data, CSV rows, documents, and third-party responses should be treated as untrusted input.",
    "The worker should validate inputs, preserve tenant and ownership context, apply object-level authorization where appropriate, use controlled storage locations, and keep server-generated identifiers separate from user-facing names.",
    "A background service should not use a broad credential for convenience when a narrower identity or API permission would work.",
    "If a user could not do something directly, the worker should not do it for that user without an approved workflow and audit context."
  ]
};
