window.COURSE_MODULE = {
  "title": "Course Summary: Secure Background Service Workflow",
  "graphicAlt": "Bullet summary graphic for Course Summary: Secure Background Service Workflow.",
  "narration": "Secure background services in .NET are built through a repeatable workflow. Start by defining the worker boundary: what starts it, what input it consumes, which systems it touches, which identity it uses, which team owns it, and how it stops. A worker that cannot be explained clearly is difficult to secure and difficult to operate.\n\nValidate queued work and serialized inputs before processing. Check schema, size, version, required fields, ownership, tenant context, object references, authorization context, and business meaning. Treat internal queues, databases, files, and imported records as inputs that still need verification for the current operation.\n\nUse least privilege for runtime identities and secret access. Keep worker permissions separate from deployment, migration, administration, and support permissions. Validate required configuration at startup without exposing secret values, and plan credential ownership, rotation, and revocation before production.\n\nDesign processing for duplicates, retries, failures, cancellation, and resource limits. Use idempotency where repeated processing is possible, define retry limits, route poison messages for review, respect cancellation tokens, set timeouts, and bound concurrency, batch sizes, prefetching, and memory use. The worker should fail safely and observably.\n\nFinally, monitor and maintain the service over time. Use safe structured logs, useful health metrics, audit trails for high-impact work, alerting based on meaningful patterns, and runbooks for response. Background service security is not a one-time code review. It is an ongoing practice of keeping automated work predictable, limited, observable, and accountable.",
  "narrationPoints": [
    "Start by defining the worker boundary: what starts it, what input it consumes, which systems it touches, which identity it uses, which team owns it, and how it stops.",
    "Treat internal queues, databases, files, and imported records as inputs that still need verification for the current operation.",
    "Validate required configuration at startup without exposing secret values, and plan credential ownership, rotation, and revocation before production.",
    "Use idempotency where repeated processing is possible, define retry limits, route poison messages for review, respect cancellation tokens, set timeouts, and bound concurrency, batch sizes, prefetching, and memory use.",
    "Background service security is not a one-time code review."
  ]
};
