window.COURSE_MODULE = {
  "title": "Cancellation, Timeouts, and Resource Protection",
  "graphicAlt": "Bullet summary graphic for Cancellation, Timeouts, and Resource Protection.",
  "narration": "Cancellation tokens are cooperative signals that let a .NET worker stop gracefully. They are especially important for services that may be processing files, queue messages, database updates, or external calls when the host shuts down. A worker that ignores cancellation may delay deployment, hold locks, abandon partial work, or make shutdown behavior unpredictable.\n\nGraceful shutdown should match the unit of work. Some operations can stop quickly and leave the job for later retry. Others need to finish a small critical section, record state, release a lease, close a file, or update a message visibility timeout. The worker should have a clear policy so operators know what happens during deployments, restarts, failures, and scaling events.\n\nTimeouts protect slow operations. Database queries, HTTP calls, storage operations, file processing, and long-running jobs should have reasonable limits based on the business process. Without timeouts, one dependency problem can tie up worker threads, connections, leases, and queue capacity. A timeout is not just an operations setting; it is a resource protection control.\n\nConcurrency limits, rate limits, batch sizes, queue prefetching, and bounded work protect shared capacity. More parallelism is not always better. A worker that consumes too many messages at once can overwhelm databases, downstream APIs, storage accounts, or memory. Bounded processing lets teams reason about worst-case behavior and protects other services that share the same resources.\n\nAvoid unbounded loops, unbounded memory use, uncontrolled parallelism, and runaway retries. These patterns turn ordinary failures into broader incidents. Backpressure and load management help the worker slow down, reject work, delay work, or route failures when downstream systems cannot keep up.\n\nLong-running behavior should be observable and controllable. Operators should be able to see queue depth, active jobs, running duration, retry counts, cancellation events, resource use, and stuck work. They should also know how to pause, drain, restart, or scale workers safely when production conditions change.",
  "narrationPoints": [
    "The worker should have a clear policy so operators know what happens during deployments, restarts, failures, and scaling events.",
    "A timeout is not just an operations setting; it is a resource protection control.",
    "Concurrency limits, rate limits, batch sizes, queue prefetching, and bounded work protect shared capacity.",
    "Backpressure and load management help the worker slow down, reject work, delay work, or route failures when downstream systems cannot keep up.",
    "Operators should be able to see queue depth, active jobs, running duration, retry counts, cancellation events, resource use, and stuck work."
  ]
};
