window.COURSE_MODULE = {
  "title": "Idempotency, Retries, and Failure Safety",
  "graphicAlt": "Bullet summary graphic for Idempotency, Retries, and Failure Safety.",
  "narration": "Background services often encounter duplicate messages, retries, delayed processing, partial failures, and interrupted operations. A queue may deliver the same work more than once. A database update may succeed while a notification call fails. A worker may stop after writing a file but before marking the job complete. Secure and reliable design assumes these conditions happen.\n\nIdempotency means designing an operation so repeated processing does not cause unsafe duplicate effects. An ExampleJobId, idempotency key, state machine, unique constraint, or processed-record marker can help the worker determine whether the intended change already happened. The goal is not only to avoid duplicate work. It is to avoid duplicate state transitions, duplicate exports, duplicate notifications, and confusing audit records.\n\nRetry policies need limits and review. Retrying a temporary dependency failure may be reasonable. Retrying a permanently invalid message wastes resources and hides the real issue. Retrying a non-idempotent action can create repeated effects if the worker cannot tell what already succeeded. Use explicit ExampleRetryPolicy settings for delay, maximum attempts, retryable conditions, and failure routing.\n\nNon-idempotent actions require special care. Payments, state transitions, notifications, exports, report publication, and external API calls may have side effects outside the worker's transaction. Before retrying, the worker needs a way to determine whether the action already completed or whether it is safe to attempt again. When that cannot be proven, manual review may be safer than automatic repetition.\n\nPoison messages and dead-letter queues support safe handling. A message that repeatedly fails validation or processing should not loop forever and starve the rest of the queue. A dead-letter or review workflow should preserve enough safe context for troubleshooting: ExampleJobId, message type, failure category, timestamp, retry count, and correlation ID, while avoiding sensitive content where it is not needed.\n\nTransaction boundaries, compensation, and recovery planning should be designed before incidents. Job state transitions should be recorded clearly: received, validated, authorized, processing, completed, failed, dead-lettered, canceled, or manually reviewed. Those states help operators understand what happened and help auditors distinguish safe retries from repeated unintended effects.",
  "narrationPoints": [
    "Background services often encounter duplicate messages, retries, delayed processing, partial failures, and interrupted operations.",
    "Idempotency means designing an operation so repeated processing does not cause unsafe duplicate effects.",
    "Use explicit ExampleRetryPolicy settings for delay, maximum attempts, retryable conditions, and failure routing.",
    "Before retrying, the worker needs a way to determine whether the action already completed or whether it is safe to attempt again.",
    "A dead-letter or review workflow should preserve enough safe context for troubleshooting: ExampleJobId, message type, failure category, timestamp, retry count, and correlation ID, while avoiding sensitive content where it is not needed."
  ]
};
