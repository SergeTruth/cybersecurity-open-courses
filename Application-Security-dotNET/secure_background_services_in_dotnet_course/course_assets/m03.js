window.COURSE_MODULE = {
  "title": "Configuration, Secrets, and Runtime Identity",
  "graphicAlt": "Bullet summary graphic for Configuration, Secrets, and Runtime Identity.",
  "narration": "Background services often need environment-specific configuration. A development worker may read from a local queue and write to a test database. A staging worker may process synthetic data. A production worker may touch real customers, real tenants, real reports, and real integrations. Those environments should not blur together. Configuration should make the worker's target systems, feature flags, intervals, limits, and identities explicit.\n\nConnection strings, API keys, service tokens, certificates, signing keys, storage credentials, and queue credentials are sensitive. They should not be hardcoded into source code, committed in configuration files, printed during startup, or copied into logs. Use appropriate configuration systems, secret stores, managed identities, workload identities, or least-privilege service accounts where the platform supports them.\n\nRuntime identity should match the worker's real job. A report generator should not need deployment permissions. A queue consumer should not need broad administrative access. A cleanup job should not share the same credential as a migration tool unless that access is clearly justified. Separating runtime permissions from deployment, migration, reporting, and administration permissions limits the impact of mistakes.\n\nWorkers deserve permission review because they may operate with broad access and little human oversight. A privileged ExampleServiceIdentity can repeatedly process jobs, update records, move files, or call third-party services. Least privilege should cover databases, queues, storage, APIs, secrets, logs, and management operations. Access that is convenient during development may be excessive in production.\n\nStartup validation should confirm that required settings exist and are coherent without exposing secret values. A worker can safely report that a required queue name is missing, that a configured batch size is outside the approved range, or that a credential reference cannot be resolved. It should not print secret material, connection strings, tokens, or private key content as part of diagnostic output.\n\nCredential ownership should be planned. Know who can rotate the worker identity, who approves access changes, how revocation happens, which alerts indicate authentication failures, and what systems depend on the credential. A background service should not become a long-lived mystery identity that no team wants to touch.",
  "narrationPoints": [
    "Configuration should make the worker's target systems, feature flags, intervals, limits, and identities explicit.",
    "Use appropriate configuration systems, secret stores, managed identities, workload identities, or least-privilege service accounts where the platform supports them.",
    "Separating runtime permissions from deployment, migration, reporting, and administration permissions limits the impact of mistakes.",
    "Startup validation should confirm that required settings exist and are coherent without exposing secret values.",
    "Know who can rotate the worker identity, who approves access changes, how revocation happens, which alerts indicate authentication failures, and what systems depend on the credential."
  ]
};
