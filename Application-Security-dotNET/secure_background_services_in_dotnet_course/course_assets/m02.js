window.COURSE_MODULE = {
  "title": "Hosted Services, Worker Services, and Execution Boundaries",
  "graphicAlt": "Bullet summary graphic for Hosted Services, Worker Services, and Execution Boundaries.",
  "narration": ".NET hosted services, Worker Services, IHostedService, and BackgroundService provide a common way to run work outside the direct request path. At a conceptual level, they give the application a startup phase, a running phase, and a stop phase. Those phases matter for security because they determine when configuration is loaded, when dependencies are created, when work begins, and how gracefully the service stops.\n\nStartup should validate required configuration and fail clearly when the worker cannot run safely. Execution should be predictable: a queue consumer should process known message types, a timer should run on an understood interval, and a polling loop should have clear limits. Shutdown should respect cancellation and avoid leaving files, records, messages, or external operations in ambiguous states when the host stops.\n\nDependency injection lifetimes deserve special attention in long-running services. A singleton worker that accidentally holds onto scoped services can create stale state, concurrency problems, or resource leaks. A worker that creates scopes intentionally for each unit of work is easier to reason about. The lifecycle of database contexts, HTTP clients, repositories, validators, and transaction objects should match the unit of work being processed.\n\nScheduling, timers, polling loops, queue consumers, and recurring jobs all need ownership. A timer that overlaps with itself, a loop that runs without delay, or a consumer that prefetches more work than it can safely process can create operational pressure. These are not only performance concerns. They affect reliability, auditing, and the ability to recover when something goes wrong.\n\nSeparating orchestration from business logic can improve reviewability. The worker can own scheduling, message receipt, cancellation, retries, and logging. A separate service can own validation, authorization checks, and business decisions. That division makes it easier to test the business behavior without reproducing the whole hosted-service runtime and easier to review the worker mechanics without reading all domain code.\n\nAvoid hidden global state and unclear execution ownership. Workers should document what starts them, what input they consume, which identity they use, what data they touch, how concurrency is controlled, and who responds when alerts fire. Before production deployment, reviewers should be able to explain the execution boundary in plain language.",
  "narrationPoints": [
    ".NET hosted services, Worker Services, IHostedService, and BackgroundService provide a common way to run work outside the direct request path.",
    "Startup should validate required configuration and fail clearly when the worker cannot run safely.",
    "A singleton worker that accidentally holds onto scoped services can create stale state, concurrency problems, or resource leaks.",
    "A separate service can own validation, authorization checks, and business decisions.",
    "Before production deployment, reviewers should be able to explain the execution boundary in plain language."
  ]
};
