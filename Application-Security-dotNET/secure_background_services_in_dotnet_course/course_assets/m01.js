window.COURSE_MODULE = {
  "title": "Background Services as Security-Critical Workloads",
  "graphicAlt": "Bullet summary graphic for Background Services as Security-Critical Workloads.",
  "narration": "Background services are automated workloads that run without direct user interaction. In .NET systems they may appear as Worker Services, hosted services, queue consumers, scheduled tasks, import jobs, cleanup jobs, report generators, email senders, notification services, synchronization workers, and maintenance processes. Because they are not always visible in the web request path, they are easy to underestimate.\n\nMany workers have access to sensitive systems. A single ExampleWorkerName may read from queues, update databases, write files, call APIs, generate reports, move data to storage, send notifications, and use secrets or service identities. That makes background processing part of the application's security boundary, not a side feature. The fact that the work happens later or off-screen does not make it lower risk.\n\nWeb request security and background processing security are related, but they are not identical. A web request usually has an immediate user, a request context, a response, and a short execution window. A background job may run minutes later, retry several times, process batched data, operate under a service identity, or continue after the original user session is gone. That means authorization, tenant context, logging, and failure behavior need deliberate design.\n\nHidden automation creates risk when ownership is unclear. If no team owns the worker contract, retry behavior, permissions, data retention, alerting, or operational runbook, failures can become hard to investigate. A job might silently skip records, repeat a state change, retain temporary files, or process stale data long before anyone notices.\n\nSecure background service work is normal engineering work. It begins in design, where teams define boundaries, inputs, permissions, data handling, and failure modes. It continues in implementation, deployment, monitoring, incident response, and maintenance. The practical goal is to make workers predictable, reviewable, observable, and limited to the access they actually need.",
  "narrationPoints": [
    "Background services are automated workloads that run without direct user interaction.",
    "The fact that the work happens later or off-screen does not make it lower risk.",
    "A web request usually has an immediate user, a request context, a response, and a short execution window.",
    "A job might silently skip records, repeat a state change, retain temporary files, or process stale data long before anyone notices.",
    "Secure background service work is normal engineering work."
  ]
};
