window.COURSE_MODULE = {
  "title": "Authorization and Access Control",
  "graphicAlt": "Bullet summary graphic for Authorization and Access Control.",
  "narration": "Authorization answers the question, what is this identity allowed to do. In ASP.NET Core, authorization can be role-based, claim-based, policy-based, resource-based, or a combination. The right model depends on the business rule. A good access-control design should be easy to explain and easy to locate during review.\n\nRoles can be useful when they map to real responsibilities, such as an administrator, support operator, approver, or tenant manager. They become too coarse when one role is expected to capture every record-level, tenant-level, workflow-level, and action-level decision. Broad roles are easy to apply but hard to reason about when the application grows.\n\nClaims describe attributes about the identity, such as an organization, tenant relationship, assurance level, or permission marker. Claims are useful inputs to decisions, but they should be interpreted carefully. A claim usually tells the application something about the caller; it does not automatically prove access to every resource referenced by a request.\n\nPolicies let teams define reusable authorization requirements. They can combine roles, claims, authentication scheme expectations, custom checks, and other requirements into a named rule. Policies are helpful because endpoint declarations become more readable and because reviewers can inspect the policy implementation instead of chasing repeated logic across controllers and handlers.\n\nResource-based authorization is important when access depends on the specific object. A user may be allowed to view reports, but not every report. A tenant administrator may approve one tenant's request, but not another tenant's request. Resource checks may require loading the resource, verifying ownership, checking workflow state, and then making an authorization decision close to the operation.\n\nAccess rules must be enforced on the server. Hiding a button, disabling a menu item, or omitting a link can improve the user experience, but it is not the security control. MVC actions, Razor Pages, controllers, APIs, Minimal APIs, background/admin surfaces, and operational endpoints should have server-side protection that matches the sensitivity of the action.",
  "narrationPoints": [
    "A good access-control design should be easy to explain and easy to locate during review.",
    "Roles can be useful when they map to real responsibilities, such as an administrator, support operator, approver, or tenant manager.",
    "Claims are useful inputs to decisions, but they should be interpreted carefully.",
    "A tenant administrator may approve one tenant's request, but not another tenant's request.",
    "Hiding a button, disabling a menu item, or omitting a link can improve the user experience, but it is not the security control."
  ]
};
