window.COURSE_MODULE = {
  "title": "Configuration, Secrets, and Data Protection",
  "graphicAlt": "Bullet summary graphic for Configuration, Secrets, and Data Protection.",
  "narration": "Configuration is part of the security boundary in ASP.NET Core. Appsettings files, environment variables, user secrets, command-line settings, deployment variables, cloud configuration, and external secret stores can affect identity, cookies, logging, Data Protection, database access, feature flags, CORS, diagnostics, and endpoint behavior. A configuration mistake can change the security posture without a code change.\n\nDevelopment secret storage is not the same as production-grade secret protection. User secrets can keep local development values out of source files, but they are not a production secret store. Production secrets need managed storage, access control, rotation, auditability, operational ownership, and deployment practices that keep secret values away from repositories, logs, tickets, and build artifacts.\n\nSecrets should not be committed to source control. Repositories are copied, cached, mirrored, reviewed, backed up, and sometimes shared broadly. A secret committed once can remain in history even after it is removed from the current file. Configuration review should look for connection strings, API keys, tokens, certificates, signing material, and private endpoints before release.\n\nASP.NET Core Data Protection provides services used to protect cookies, tokens, and application data. Conceptually, it uses managed keys so protected data can later be read by the application when appropriate. Teams do not need to invent their own protection mechanism for framework features, but they do need to understand where keys are stored and how they behave across deployments.\n\nKey persistence matters in multi-instance deployments. If each instance has incompatible keys, one instance may not read protected cookies, tokens, or data produced by another. Key rotation also needs planning so old protected data can remain readable for the required period while new data uses current keys. The right key storage depends on the hosting environment, but it should be explicit and reviewed.\n\nConfiguration and key handling should be tested in production-like environments. Local behavior can hide problems because one developer machine is not the same as a scaled deployment. Review how settings are loaded, how missing settings fail, how secrets are referenced without being printed, and how Data Protection keys are shared, rotated, and protected.",
  "narrationPoints": [
    "Development secret storage is not the same as production-grade secret protection.",
    "Configuration review should look for connection strings, API keys, tokens, certificates, signing material, and private endpoints before release.",
    "ASP.NET Core Data Protection provides services used to protect cookies, tokens, and application data.",
    "The right key storage depends on the hosting environment, but it should be explicit and reviewed.",
    "Configuration and key handling should be tested in production-like environments."
  ]
};
