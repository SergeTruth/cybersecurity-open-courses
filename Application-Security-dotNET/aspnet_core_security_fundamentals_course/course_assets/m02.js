window.COURSE_MODULE = {
  "title": "The Request Pipeline and Security Middleware",
  "graphicAlt": "Bullet summary graphic for The Request Pipeline and Security Middleware.",
  "narration": "The ASP.NET Core request pipeline is a security control surface. Every request flows through middleware before it reaches an endpoint, and each component can change what happens next. Routing, HTTPS redirection, exception handling, authentication, authorization, endpoint mapping, static files, CORS, antiforgery behavior, and custom middleware should be understood as part of one flow rather than isolated settings.\n\nMiddleware order matters because some decisions depend on earlier context. Routing must identify endpoint metadata before endpoint authorization can use it. Authentication must establish the user before authorization evaluates access rules. Exception handling must produce controlled responses without exposing internals. Endpoint mapping must not create unexpected paths around the protections the team expects.\n\nDevelopment behavior should stay in development. Detailed error pages, verbose diagnostics, local identity assumptions, permissive origins, relaxed certificate behavior, or test-only endpoints can help during local work, but they should not become production behavior. Production should use controlled errors, secure transport expectations, explicit endpoint protection, and logging that helps operators without revealing sensitive details to callers.\n\nSecure transport assumptions also belong in the pipeline review. Applications commonly sit behind reverse proxies, load balancers, ingress controllers, or cloud services. The application should be configured so it understands forwarded headers, HTTPS expectations, and redirect behavior in the actual deployment environment. Otherwise a setting that worked locally may not match how production traffic arrives.\n\nEndpoint protection should be visible. MVC controllers, Razor Pages, Minimal APIs, endpoint groups, health checks, static assets, and administrative surfaces should have intentional access rules. Reviewers should be able to tell which endpoints are public, which require authentication, which require policies, and which are limited to operational systems.\n\nThe practical review question is, what happens to a request from arrival to endpoint execution and response. If the team cannot describe that path, security behavior may depend on accident. A coordinated request pipeline makes access decisions, error behavior, transport assumptions, and routing easier to test and maintain.",
  "narrationPoints": [
    "Every request flows through middleware before it reaches an endpoint, and each component can change what happens next.",
    "Routing must identify endpoint metadata before endpoint authorization can use it.",
    "Production should use controlled errors, secure transport expectations, explicit endpoint protection, and logging that helps operators without revealing sensitive details to callers.",
    "MVC controllers, Razor Pages, Minimal APIs, endpoint groups, health checks, static assets, and administrative surfaces should have intentional access rules.",
    "The practical review question is, what happens to a request from arrival to endpoint execution and response."
  ]
};
