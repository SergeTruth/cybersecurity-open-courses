window.COURSE_MODULE = {
  "title": "Authentication Fundamentals",
  "graphicAlt": "Bullet summary graphic for Authentication Fundamentals.",
  "narration": "Authentication answers the question, who is making this request. In ASP.NET Core, authentication usually happens through configured schemes. A browser application may use cookies. An API may use bearer tokens. An enterprise application may rely on an external provider. A single application can support more than one scheme, but the team should understand which endpoints use which scheme and why.\n\nCookie authentication is common for server-rendered browser applications because the browser can send the authentication cookie automatically with requests to the application. Bearer tokens are common for APIs because clients can present a token with each request. External providers can centralize identity, federation, multifactor enrollment, account lifecycle, or organizational policies. Each model has different operational expectations.\n\nASP.NET Core Identity is a framework option for account management. It can support users, password workflows, claims, roles, sign-in, token generation, account confirmation, and related flows. It is useful when the application owns accounts directly, but teams still need to configure it carefully, review lifecycle paths, and decide how it fits with organizational identity practices.\n\nAuthentication is not authorization. A signed-in user is identified, but identification does not answer what that user may access. The application still needs access rules for endpoints, records, tenant data, administrative workflows, exports, and state-changing operations. Treating login as blanket permission is one of the most common design mistakes.\n\nEngineering mistakes often come from trusting state too early. A page that shows a login form does not protect every endpoint. A local development identity does not represent production identity. A username in a request value is not proof of identity. A remembered session may not match current risk expectations. Authentication state should come from the configured authentication system, not from informal request data.\n\nLogout and session behavior also need production review. Teams should understand cookie lifetime, sliding expiration, revocation expectations, sign-out behavior, external provider logout where relevant, and what happens when roles or claims change during an active session. The user's identity lifecycle continues after the first successful sign-in.",
  "narrationPoints": [
    "In ASP.NET Core, authentication usually happens through configured schemes.",
    "Bearer tokens are common for APIs because clients can present a token with each request.",
    "ASP.NET Core Identity is a framework option for account management.",
    "Authentication state should come from the configured authentication system, not from informal request data.",
    "Teams should understand cookie lifetime, sliding expiration, revocation expectations, sign-out behavior, external provider logout where relevant, and what happens when roles or claims change during an active session."
  ]
};
