window.COURSE_MODULE = {
  "title": "Cookies, Sessions, CSRF, and CORS",
  "graphicAlt": "Bullet summary graphic for Cookies, Sessions, CSRF, and CORS.",
  "narration": "Cookies are security-sensitive state carriers. In ASP.NET Core they may support authentication, antiforgery protection, correlation flows, preferences, or application session behavior. A cookie can influence who the application thinks the user is, which workflow is in progress, or whether a browser request is accepted. Cookie configuration therefore deserves review, not copy-and-paste treatment.\n\nSecure, HttpOnly, SameSite, expiration, sliding expiration, and cookie scope all matter. Secure helps ensure cookies are sent over protected transport. HttpOnly reduces exposure to browser scripts. SameSite influences cross-site sending behavior. Expiration and sliding expiration affect session lifetime. Scope controls which host or path receives the cookie. Each setting should match the application's deployment and user expectations.\n\nCSRF is best understood in safe terms: a browser can send existing credentials with a cross-site state-changing request. Antiforgery protections help the application distinguish an intentional user action in the application from a cross-site request that arrives with existing browser credentials. State-changing browser workflows should be reviewed for antiforgery behavior, method choice, and cookie settings.\n\nCORS is different. It is a browser-enforced cross-origin access control mechanism. It controls which origins can read certain responses from browser-based cross-origin requests. It is not authentication, and it does not prove user identity or service identity. A permissive CORS policy can increase exposure when combined with sensitive responses or credentialed browser requests.\n\nAvoid broad origins and credentials without a clear need. A narrowly scoped CORS policy is easier to reason about than a policy that trusts many origins for convenience. Teams should document which front-end origins are approved, whether credentials are required, which methods and headers are allowed, and how this changes across environments.\n\nSession state should be minimized. Storing large or sensitive values in session can create privacy, scaling, troubleshooting, and lifecycle problems. Prefer storing only what is necessary and keeping authoritative decisions on the server side. If session data influences access, workflow state, or sensitive behavior, it should be treated as security-relevant state.",
  "narrationPoints": [
    "In ASP.NET Core they may support authentication, antiforgery protection, correlation flows, preferences, or application session behavior.",
    "Each setting should match the application's deployment and user expectations.",
    "State-changing browser workflows should be reviewed for antiforgery behavior, method choice, and cookie settings.",
    "A narrowly scoped CORS policy is easier to reason about than a policy that trusts many origins for convenience.",
    "If session data influences access, workflow state, or sensitive behavior, it should be treated as security-relevant state."
  ]
};
