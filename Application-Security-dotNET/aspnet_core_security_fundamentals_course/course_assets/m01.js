window.COURSE_MODULE = {
  "title": "Security in the ASP.NET Core Application Lifecycle",
  "graphicAlt": "Bullet summary graphic for Security in the ASP.NET Core Application Lifecycle.",
  "narration": "Security in ASP.NET Core is not one switch, one middleware call, or one final release checklist. It is a set of engineering decisions made throughout the application lifecycle. Architecture defines trust boundaries and data flows. Coding choices define how requests are validated and authorized. Configuration controls identity providers, secrets, cookies, diagnostics, and environments. Deployment and operations determine whether the application remains secure when real users, real data, and real failures are involved.\n\nASP.NET Core gives teams a strong foundation, but the framework does not know the application's business rules. It cannot decide which endpoints require authentication, which roles are too broad, which tenant owns a resource, which fields should appear in a response, or which events should generate an alert. Framework defaults help reduce common mistakes, but they work best when paired with deliberate design and review.\n\nThe request pipeline, identity model, authorization policies, Data Protection configuration, error handling, logging, and deployment settings all interact. A secure application depends on those pieces working together. A strong authentication setup does not help if endpoints are left open. Careful validation does not help if sensitive details are logged. Secure cookies do not help if session behavior and logout expectations are not reviewed.\n\nThreat modeling does not need to be ceremonial to be useful. Teams can ask simple questions during design and review: who is calling this endpoint, what data crosses the boundary, what is trusted, what must be checked on the server, what can fail, what should be logged, and what sensitive data must not be exposed. These questions turn security from an afterthought into normal engineering practice.\n\nThis course focuses on defensive fundamentals for professional application teams. The goal is to build a practical baseline: explicit authentication, server-side authorization, safe state handling, input validation, output encoding, careful configuration, protected secrets, useful logging, and production behavior that can be operated safely.",
  "narrationPoints": [
    "Security in ASP.NET Core is not one switch, one middleware call, or one final release checklist.",
    "Framework defaults help reduce common mistakes, but they work best when paired with deliberate design and review.",
    "ASP.NET Core gives teams a strong foundation, but the framework does not know the application's business rules.",
    "The request pipeline, identity model, authorization policies, Data Protection configuration, error handling, logging, and deployment settings all interact.",
    "Teams can ask simple questions during design and review: who is calling this endpoint, what data crosses the boundary, what is trusted, what must be checked on the server, what can fail, what should be logged, and what sensitive data must not be exposed."
  ]
};
