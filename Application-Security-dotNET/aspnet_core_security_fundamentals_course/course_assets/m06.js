window.COURSE_MODULE = {
  "title": "Input Handling, Output Encoding, and XSS Reduction",
  "graphicAlt": "Bullet summary graphic for Input Handling, Output Encoding, and XSS Reduction.",
  "narration": "ASP.NET Core applications should treat input as untrusted even when it comes from authenticated users. Input includes route values, query strings, form fields, JSON bodies, headers, cookies, files, claims, database records, queue messages, configuration, and responses from other services. Authentication may identify the caller, but it does not make every value correct, authorized, or safe for the intended operation.\n\nModel validation should reflect expected shape, type, size, and meaning. A field may need a maximum length, allowed format, numeric range, required status, supported value set, or relationship to another field. Server-side validation is the enforcement point. Client-side validation improves usability and reduces accidental mistakes, but it can be changed, skipped, or out of date.\n\nOutput encoding is the partner of input validation. Razor's default encoding behavior helps reduce XSS risk when teams keep the defaults and let the framework encode values for the destination context. Problems appear when code renders untrusted content as raw HTML, mixes data into script or attribute contexts without the right helper, or assumes that previously stored content is now safe.\n\nJSON and API output also need shaping. Responses should include fields the caller needs, not the entire domain object. Error responses should be consistent and useful without exposing stack traces, internal paths, tokens, connection strings, personal data, or implementation details. Response contracts are part of the security boundary.\n\nFile upload handling deserves careful limits. Review allowed types, size limits, storage location, generated server-side names, authorization, scanning or review processes where appropriate, and downstream processing. The original filename should be treated as display data, not as a trusted storage instruction.\n\nThe practical pattern is to validate on input, authorize before action, encode on output, and keep error details controlled. These decisions should be visible in code review and covered by tests for ordinary invalid input, boundary values, missing fields, and unexpected states.",
  "narrationPoints": [
    "ASP.NET Core applications should treat input as untrusted even when it comes from authenticated users.",
    "Output encoding is the partner of input validation.",
    "Error responses should be consistent and useful without exposing stack traces, internal paths, tokens, connection strings, personal data, or implementation details.",
    "The original filename should be treated as display data, not as a trusted storage instruction.",
    "The practical pattern is to validate on input, authorize before action, encode on output, and keep error details controlled."
  ]
};
