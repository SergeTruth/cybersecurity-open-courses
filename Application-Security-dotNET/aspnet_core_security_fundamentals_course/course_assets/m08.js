window.COURSE_MODULE = {
  "title": "Secure API and Minimal API Practices",
  "graphicAlt": "Bullet summary graphic for Secure API and Minimal API Practices.",
  "narration": "APIs need explicit authentication and authorization rules. Whether the application uses controllers, Minimal APIs, endpoint groups, or a mix, reviewers should be able to see which endpoints are public, which require an authenticated caller, which require a policy, and which are intended only for internal or operational use. Security rules should not be hidden in assumptions about routes or network location.\n\nMinimal APIs and endpoint groups can be organized so rules are visible. Grouping related endpoints, applying shared authorization metadata, and keeping high-risk operations separate from public endpoints can make review easier. Controllers and Razor Pages benefit from the same discipline: consistent attributes, clear policy names, and documented exceptions.\n\nDTOs help prevent over-posting and accidental binding of sensitive fields. A request type should represent the fields the caller is allowed to supply for that operation. It should not expose internal flags, tenant ownership, approval status, pricing decisions, administrative fields, or server-controlled properties simply because those fields exist on a domain object.\n\nResponses should be shaped intentionally. Return the data the caller needs, use consistent error handling, avoid internal exception detail, and choose status codes that reflect the outcome without revealing unnecessary internals. A safe API contract is stable, minimal, and understandable to both consumers and reviewers.\n\nRate-limit thinking and abuse resistance belong in API design at a high level. Teams should consider expensive operations, repeated failures, large requests, dependency pressure, and operational limits. The exact implementation may vary, but the application should not assume every client behaves politely or every dependency has unlimited capacity.\n\nService-to-service trust should be handled carefully. Network location alone is not identity. A request from an internal network, private subnet, service mesh, or known host still needs a clear identity and authorization model appropriate for the operation. Internal APIs often handle sensitive data and deserve the same clarity as public APIs.",
  "narrationPoints": [
    "Whether the application uses controllers, Minimal APIs, endpoint groups, or a mix, reviewers should be able to see which endpoints are public, which require an authenticated caller, which require a policy, and which are intended only for internal or operational use.",
    "Minimal APIs and endpoint groups can be organized so rules are visible.",
    "A request type should represent the fields the caller is allowed to supply for that operation.",
    "A safe API contract is stable, minimal, and understandable to both consumers and reviewers.",
    "Rate-limit thinking and abuse resistance belong in API design at a high level."
  ]
};
