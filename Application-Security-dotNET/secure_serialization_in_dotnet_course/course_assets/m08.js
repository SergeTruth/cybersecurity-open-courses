window.COURSE_MODULE = {
  "title": "Versioning, Compatibility, and Message Contracts",
  "graphicAlt": "Bullet summary graphic for Versioning, Compatibility, and Message Contracts.",
  "narration": "Serialization contracts often live longer than a single release. API clients may update at different times. Queue messages may be delayed or retried. Stored serialized data may be read months later. Export files may be imported by external tools. Event messages may be consumed by teams that deploy on a different schedule. Secure design assumes old and new data may coexist.\n\nVersioning should cover API payloads, queue messages, event messages, imports, exports, and stored serialized data. A version does not need to be complicated, but producers and consumers should know which fields are required, which are optional, which are deprecated, and how unknown data should be handled. Ownership should be clear so teams know who can approve a contract change.\n\nBackward and forward compatibility require discipline. Adding an optional field may be safe if old consumers ignore it and new consumers handle absence. Renaming or removing a field can be disruptive if stored data or external clients still depend on it. Changing defaults can alter behavior even when the serialized shape looks similar. Compatibility needs tests, not hope.\n\nAvoid unsafe shortcuts when older data no longer matches newer object models. Broad acceptance, loose conversions, or hidden fallback behavior can mask a migration problem and produce ambiguous state. A safer approach is to identify affected data, define migration rules, preserve enough context for diagnostics, and reject or quarantine data that cannot be interpreted safely.\n\nMigration strategies for stored serialized data should be planned at a high level before deployment. Teams may migrate eagerly, migrate on read, maintain a compatibility reader for a limited period, or support multiple versions explicitly. Each approach has operational tradeoffs. What matters is that the path is documented, tested, monitored, and owned.\n\nContract tests help prevent surprises. Before deployment, test old producer to new consumer, new producer to old consumer where required, missing fields, unknown fields, deprecated fields, and incompatible versions. The goal is to make compatibility behavior predictable and visible before real production data exposes the gap.",
  "narrationPoints": [
    "Secure design assumes old and new data may coexist.",
    "Versioning should cover API payloads, queue messages, event messages, imports, exports, and stored serialized data.",
    "Avoid unsafe shortcuts when older data no longer matches newer object models.",
    "What matters is that the path is documented, tested, monitored, and owned.",
    "Before deployment, test old producer to new consumer, new producer to old consumer where required, missing fields, unknown fields, deprecated fields, and incompatible versions."
  ]
};
