window.COURSE_MODULE = {
  "title": "Deserialization Risks and Object Construction",
  "graphicAlt": "Bullet summary graphic for Deserialization Risks and Object Construction.",
  "narration": "Deserialization is object construction from data that may have crossed a trust boundary. The application receives text, bytes, or a structured document, and the serializer produces a .NET object. That object may now pass through services, validators, repositories, authorization checks, or business workflows. If the object can be created in an invalid or contradictory state, the problem moves from parsing into application logic.\n\nConstructors, setters, required members, nullable fields, default values, and initialization patterns all affect safety. A DTO with nullable properties may be acceptable for reading partial data, but business code should not assume those values are present until validation proves it. A default value may make a missing field look intentional. A setter may allow state that the application would never create through its normal domain methods.\n\nAvoid designs where deserialized objects can quietly represent impossible business states. An ExampleOrderMessage should not be considered ready for processing merely because it was parsed. It should be checked for required identifiers, ownership context, supported status values, expected version, reasonable sizes, and any business conditions needed by the workflow. The result of validation should be explicit.\n\nSeparate parsing, validation, authorization, and business logic. Parsing answers, can the data be read into the expected shape. Validation answers, does the data meet the contract and business rules. Authorization answers, is this actor allowed to perform this action on this resource. Business logic answers, what should the application do now. Combining those concerns makes it harder to review failures and easier to trust data too early.\n\nHandle malformed, oversized, unexpected, incomplete, or incompatible serialized input safely. Defensive behavior might include rejecting the input, recording a safe diagnostic event, returning a limited error, moving a message to a review process, or preserving a failure state for operations. The exact behavior depends on the workflow, but it should not leak sensitive diagnostic detail to untrusted parties.\n\nDo not assume queues, caches, files, databases, or internal tools make serialized data safe. Those locations may contain old versions, imported data, corrupted records, administrative mistakes, replayed messages, or values produced by code that has since changed. Validate when the data is used, not only when it first arrived.",
  "narrationPoints": [
    "If the object can be created in an invalid or contradictory state, the problem moves from parsing into application logic.",
    "Avoid designs where deserialized objects can quietly represent impossible business states.",
    "Combining those concerns makes it harder to review failures and easier to trust data too early.",
    "Defensive behavior might include rejecting the input, recording a safe diagnostic event, returning a limited error, moving a message to a review process, or preserving a failure state for operations.",
    "Validate when the data is used, not only when it first arrived."
  ]
};
