window.COURSE_MODULE = {
  "title": "Sensitive Data, Privacy, and Serialization Output",
  "graphicAlt": "Bullet summary graphic for Sensitive Data, Privacy, and Serialization Output.",
  "narration": "Serialization is a common accidental data exposure path. A property that is harmless inside a server-side domain object may be inappropriate in an API response, log entry, telemetry event, cache export, support bundle, client-side storage value, or downloaded file. The risk often appears when teams serialize a convenient object without stopping to ask who will see the output and how long it will live.\n\nSensitive fields include secrets, tokens, connection strings, private keys, certificates, personal data, financial data, internal identifiers, authorization state, tenant boundaries, administrative notes, support comments, workflow flags, and operational metadata. Not every field has the same sensitivity, but every serialized output should be reviewed for audience and purpose. If the consumer does not need a field, the safer default is not to serialize it.\n\nUse explicit DTOs, projection, ignore rules, and data minimization where appropriate. Projection is especially useful because it lets code choose the output shape deliberately rather than serializing the entire object graph. Ignore rules can help, but they should not become the only protection when the same object is used in many contexts. Different boundaries often deserve different contract types.\n\nDebug serializers, object dumps, exception handling, and diagnostics deserve attention. A line added to understand an issue may capture a full object graph and send it into logs, telemetry, test artifacts, or support exports. That data can spread far beyond the original code path. Production diagnostics should be useful without becoming an uncontrolled copy of application data.\n\nProtect serialized data at rest and in transit when it contains sensitive information. That may involve transport protection, encryption at rest, access control, retention limits, redaction, secure transfer, and review before external sharing. The correct controls depend on the data and audience, but the serialized copy should not be treated as less important than the original source.",
  "narrationPoints": [
    "A property that is harmless inside a server-side domain object may be inappropriate in an API response, log entry, telemetry event, cache export, support bundle, client-side storage value, or downloaded file.",
    "Not every field has the same sensitivity, but every serialized output should be reviewed for audience and purpose.",
    "Use explicit DTOs, projection, ignore rules, and data minimization where appropriate.",
    "A line added to understand an issue may capture a full object graph and send it into logs, telemetry, test artifacts, or support exports.",
    "Protect serialized data at rest and in transit when it contains sensitive information."
  ]
};
