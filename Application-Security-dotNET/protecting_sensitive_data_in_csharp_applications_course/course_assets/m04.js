window.COURSE_MODULE = {
  "title": "Encryption, Data Protection, and Key Management",
  "graphicAlt": "Bullet summary graphic for Encryption, Data Protection, and Key Management.",
  "narration": "Encryption and data protection controls should be selected based on the security property needed and the workflow. Data in transit, recoverable stored data, framework payloads, passwords, and application secrets all have different protection needs. One control should not be stretched to solve every problem.\n\nUse TLS for data in transit rather than trying to build custom transport protection. Use ASP.NET Core Data Protection for appropriate protected framework and application payloads such as cookies, antiforgery tokens, reset tokens, and short-lived protected values. Use maintained cryptography APIs and platform services instead of custom algorithms or copied cryptographic snippets.\n\nPasswords are a separate case. Passwords should use password hashing, not reversible encryption. A password hashing design verifies the submitted password without storing a value that is meant to be recovered later. That is a different goal from protecting a value that the application must decrypt or unprotect.\n\nField-level encryption, database encryption, disk encryption, and application-level protection solve different problems. Database or disk encryption may protect storage media or backups. Field-level encryption may limit exposure of selected values. Application-level protection may control who can recover a value and when. Each choice affects search, indexing, rotation, backup, recovery, and operations.\n\nKey management matters. Keys need secure generation, storage, access control, rotation, revocation, backup, and compromise response. The long-term strength of encryption depends not only on the algorithm, but on who can access the keys, how keys are monitored, and how the organization responds when key material must change.",
  "narrationPoints": [
    "Encryption and data protection controls should be selected based on the security property needed and the workflow.",
    "Use ASP.NET Core Data Protection for appropriate protected framework and application payloads such as cookies, antiforgery tokens, reset tokens, and short-lived protected values.",
    "A password hashing design verifies the submitted password without storing a value that is meant to be recovered later.",
    "Field-level encryption, database encryption, disk encryption, and application-level protection solve different problems.",
    "The long-term strength of encryption depends not only on the algorithm, but on who can access the keys, how keys are monitored, and how the organization responds when key material must change."
  ]
};
