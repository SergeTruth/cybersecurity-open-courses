window.COURSE_MODULE = {
  "title": "Secrets, Configuration, and Connection Data",
  "graphicAlt": "Bullet summary graphic for Secrets, Configuration, and Connection Data.",
  "narration": "Secrets and connection data are sensitive even when they do not look like user data. A connection string, API key, signing key, certificate, private key, token, storage key, or internal endpoint can grant access or reveal how the system is built. That makes configuration part of the sensitive data surface.\n\nSecrets should not be hardcoded in C# source, committed to source control, shipped in client assets, placed in public configuration, or copied into documentation and screenshots. A value that starts as a local convenience can become difficult to remove once it appears in history, build output, tickets, or shared notes.\n\nDevelopment secret mechanisms should not be treated as production secret storage. Local user secrets, developer environment variables, and workstation-specific configuration are useful for development, but production secrets need governed runtime configuration, platform secret stores, managed identities, or equivalent controlled mechanisms.\n\nConfiguration should be separated by environment. Development, staging, and production should not casually share secrets, database targets, storage accounts, certificates, or tenant-specific settings. Environment separation helps prevent accidental access to production data from weaker environments.\n\nLogs, CI output, deployment artifacts, crash reports, support tickets, and screenshots should be reviewed for secret exposure. The safest operational culture assumes secrets can leak through tooling unless redaction, access control, and review are designed in advance.",
  "narrationPoints": [
    "Secrets and connection data are sensitive even when they do not look like user data.",
    "Secrets should not be hardcoded in C# source, committed to source control, shipped in client assets, placed in public configuration, or copied into documentation and screenshots.",
    "Local user secrets, developer environment variables, and workstation-specific configuration are useful for development, but production secrets need governed runtime configuration, platform secret stores, managed identities, or equivalent controlled mechanisms.",
    "Development, staging, and production should not casually share secrets, database targets, storage accounts, certificates, or tenant-specific settings.",
    "Logs, CI output, deployment artifacts, crash reports, support tickets, and screenshots should be reviewed for secret exposure."
  ]
};
