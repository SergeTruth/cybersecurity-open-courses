window.COURSE_MODULE = {
  "title": "Logging, Telemetry, Exceptions, and Redaction",
  "graphicAlt": "Bullet summary graphic for Logging, Telemetry, Exceptions, and Redaction.",
  "narration": "Diagnostics are one of the most common places sensitive data leaks. Structured logging, HTTP logging, request and response bodies, exception messages, stack traces, telemetry properties, metrics labels, distributed traces, crash dumps, screenshots, and support tickets can all carry values that were never meant for broad operational access.\n\nDo not log passwords, tokens, API keys, connection strings, authorization headers, cookies, reset links, raw payment data, raw personal data, or full sensitive payloads. Even when logs are access-controlled, they are usually copied, indexed, retained, exported, searched, and viewed by more systems and people than the primary application database.\n\nRedaction should be designed before production, not improvised after sensitive data appears in logs. Decide which fields are suppressed, masked, tokenized, or replaced with safe references. Review framework logging, middleware, HTTP clients, exception handling, and third-party telemetry integrations because they may capture data outside normal application logging code.\n\nTroubleshooting value can be preserved with safe identifiers, correlation IDs, coarse categories, safe endpoint names, event types, and redacted fields. Operators usually need to know what failed, where it failed, and how to correlate the event. They rarely need raw sensitive payloads.\n\nError responses should be useful without exposing stack traces, internal paths, database details, secret names, tenant details, or raw sensitive values. Log retention, access control, export behavior, and support procedures should match the sensitivity of the data that might appear in diagnostics.",
  "narrationPoints": [
    "Structured logging, HTTP logging, request and response bodies, exception messages, stack traces, telemetry properties, metrics labels, distributed traces, crash dumps, screenshots, and support tickets can all carry values that were never meant for broad operational access.",
    "Do not log passwords, tokens, API keys, connection strings, authorization headers, cookies, reset links, raw payment data, raw personal data, or full sensitive payloads.",
    "Review framework logging, middleware, HTTP clients, exception handling, and third-party telemetry integrations because they may capture data outside normal application logging code.",
    "Troubleshooting value can be preserved with safe identifiers, correlation IDs, coarse categories, safe endpoint names, event types, and redacted fields.",
    "Error responses should be useful without exposing stack traces, internal paths, database details, secret names, tenant details, or raw sensitive values."
  ]
};
