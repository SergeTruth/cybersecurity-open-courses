window.COURSE_MODULE = {
  "title": "Kestrel Request Limits and Server-Side Controls",
  "graphicAlt": "Bullet summary graphic for Kestrel Request Limits and Server-Side Controls.",
  "narration": "Request controls are broader than rate limiting. ASP.NET Core applications also depend on server-side controls that limit request size, connection behavior, header processing, body transfer behavior, and protocol-specific resource use. These controls help protect the application before a request reaches expensive business logic.\n\nKestrel request body size limits help control how large a request body the server will accept. This matters for APIs, file uploads, import endpoints, and form submissions. A body limit should match the endpoint's business need. Unlimited sizes or extremely large defaults can increase memory, disk, CPU, and downstream capacity pressure.\n\nConnection limits protect server capacity by limiting how many active connections the server handles. Header timeouts help limit slow request setup. Minimum request body data rates and response data rates provide expectations for progress during transfers. These controls help reduce stalled or excessively slow requests from tying up resources indefinitely.\n\nHTTP/2-specific limits also matter conceptually. Header size, stream behavior, and flow-control-related limits can affect how much work the server accepts before application logic runs. Teams do not need to memorize every setting to make good decisions, but they should know that protocol behavior can influence resource consumption.\n\nLimits should align with expected application use cases. A public JSON API, internal service endpoint, file upload flow, report download, health check, and admin interface may have different needs. The right size or timeout is not universal. It should reflect what the endpoint is meant to do and what the backend can support.\n\nHosting behind IIS, reverse proxies, gateways, ingress controllers, CDNs, or load balancers can change where limits are enforced. A request may be rejected before Kestrel sees it, or Kestrel may accept work that an upstream layer allowed. Production review should map which layer owns each limit and how the layers align.",
  "narrationPoints": [
    "ASP.NET Core applications also depend on server-side controls that limit request size, connection behavior, header processing, body transfer behavior, and protocol-specific resource use.",
    "Kestrel request body size limits help control how large a request body the server will accept.",
    "Connection limits protect server capacity by limiting how many active connections the server handles.",
    "A public JSON API, internal service endpoint, file upload flow, report download, health check, and admin interface may have different needs.",
    "A request may be rejected before Kestrel sees it, or Kestrel may accept work that an upstream layer allowed."
  ]
};
