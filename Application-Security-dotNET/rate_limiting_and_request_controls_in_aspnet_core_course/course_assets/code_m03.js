window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples turn 'Choosing Limiter Algorithms' into concrete defensive .NET review patterns.",
  "codeExamples": [
    {
      "title": "Create a coherent limiter algorithm policy",
      "language": "csharp",
      "blurb": "The factory admits only closed limiter algorithms, validates permit, window, queue, segment, and replenishment values against application maxima, and enforces the fields each algorithm actually uses rather than accepting a contradictory options bag.",
      "code": "public enum LimiterAlgorithm { FixedWindow, SlidingWindow, TokenBucket }\n\npublic sealed class RequestLimiterPolicy\n{\n    private RequestLimiterPolicy(\n        LimiterAlgorithm algorithm,\n        int permitLimit,\n        TimeSpan window,\n        int queueLimit,\n        int segmentsPerWindow,\n        int tokensPerPeriod)\n    {\n        Algorithm = algorithm;\n        PermitLimit = permitLimit;\n        Window = window;\n        QueueLimit = queueLimit;\n        SegmentsPerWindow = segmentsPerWindow;\n        TokensPerPeriod = tokensPerPeriod;\n    }\n\n    public LimiterAlgorithm Algorithm { get; }\n    public int PermitLimit { get; }\n    public TimeSpan Window { get; }\n    public int QueueLimit { get; }\n    public int SegmentsPerWindow { get; }\n    public int TokensPerPeriod { get; }\n\n    public static RequestLimiterPolicy Create(\n        LimiterAlgorithm algorithm,\n        int permitLimit,\n        TimeSpan window,\n        int queueLimit,\n        int segmentsPerWindow = 0,\n        int tokensPerPeriod = 0)\n    {\n        if (!Enum.IsDefined(algorithm) || permitLimit is < 1 or > 10_000 ||\n            window < TimeSpan.FromMilliseconds(100) || window > TimeSpan.FromMinutes(10) ||\n            queueLimit is < 0 or > 100 ||\n            algorithm == LimiterAlgorithm.FixedWindow && (segmentsPerWindow != 0 || tokensPerPeriod != 0) ||\n            algorithm == LimiterAlgorithm.SlidingWindow &&\n                (segmentsPerWindow is < 2 or > 20 || tokensPerPeriod != 0) ||\n            algorithm == LimiterAlgorithm.TokenBucket &&\n                (segmentsPerWindow != 0 || tokensPerPeriod < 1 || tokensPerPeriod > permitLimit))\n        {\n            throw new ArgumentException(\"Limiter policy rejected.\");\n        }\n        return new RequestLimiterPolicy(\n            algorithm, permitLimit, window, queueLimit, segmentsPerWindow, tokensPerPeriod);\n    }\n}\n"
    },
    {
      "title": "Assign request cost from an application-owned endpoint catalog",
      "language": "csharp",
      "blurb": "A closed operation enum determines cost, a per-request budget rejects invalid future operations and insufficient capacity before subtraction, and no caller can claim that an expensive export or upload has the cost of a lightweight read.",
      "code": "public enum RateLimitedOperation { ReadOrder, SearchOrders, UploadDocument, ExportOrders }\n\npublic sealed class RequestCostBudget\n{\n    private readonly object _sync = new();\n    private int _remaining;\n\n    public RequestCostBudget(int capacity)\n    {\n        if (capacity is < 1 or > 10_000) throw new ArgumentOutOfRangeException(nameof(capacity));\n        _remaining = capacity;\n    }\n\n    public int Remaining\n    {\n        get\n        {\n            lock (_sync) return _remaining;\n        }\n    }\n\n    public bool TryConsume(RateLimitedOperation operation)\n    {\n        if (!Enum.IsDefined(operation)) throw new ArgumentOutOfRangeException(nameof(operation));\n        var cost = operation switch\n        {\n            RateLimitedOperation.ReadOrder => 1,\n            RateLimitedOperation.SearchOrders => 5,\n            RateLimitedOperation.UploadDocument => 20,\n            RateLimitedOperation.ExportOrders => 100,\n            _ => throw new ArgumentOutOfRangeException(nameof(operation))\n        };\n        lock (_sync)\n        {\n            if (_remaining < cost) return false;\n            _remaining -= cost;\n            return true;\n        }\n    }\n}\n"
    }
  ]
};
