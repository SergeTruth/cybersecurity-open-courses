window.COURSE_MODULE = {
  "title": "Rejection Behavior, Queues, and Client Communication",
  "graphicAlt": "Bullet summary graphic for Rejection Behavior, Queues, and Client Communication.",
  "narration": "When a request exceeds a limit, the application should respond predictably. The response should communicate that the request was limited without exposing sensitive implementation detail. Clients should be able to distinguish a limit response from an ambiguous application failure, and operators should be able to see which policy triggered.\n\nRetry guidance can be useful when it is clear and minimal. A client may need to know whether waiting before another request is reasonable. That guidance should be consistent with the policy and user experience. It should not reveal internal capacity details or create a noisy contract that the team cannot maintain.\n\nQueueing can smooth short bursts, but queued requests still consume resources. A queue may hold memory, threads, connection slots, request context, or downstream capacity. If a queue is unbounded or poorly configured, it can turn a short traffic spike into long latency and confusing client behavior.\n\nQueue limits should be bounded and intentional. Teams should decide whether queueing is appropriate for a given endpoint, how long users can reasonably wait, and what should happen when the queue is full. For some endpoints, quick rejection is more honest than making users wait for work that is unlikely to complete soon.\n\nLogging should capture limit events without exposing secrets or sensitive request content. Useful fields can include policy name, endpoint, route group, safe partition category, status, queue state, correlation ID, and timing. Avoid logging tokens, credentials, request bodies, uploaded file contents, or private identifiers that are not needed for operations.\n\nGood rejection behavior is part of user experience and operations. It protects the system, tells clients what happened, and gives operators enough signal to tune policies without guessing.",
  "narrationPoints": [
    "Clients should be able to distinguish a limit response from an ambiguous application failure, and operators should be able to see which policy triggered.",
    "A client may need to know whether waiting before another request is reasonable.",
    "Teams should decide whether queueing is appropriate for a given endpoint, how long users can reasonably wait, and what should happen when the queue is full.",
    "Logging should capture limit events without exposing secrets or sensitive request content.",
    "Good rejection behavior is part of user experience and operations."
  ]
};
