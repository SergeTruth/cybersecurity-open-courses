window.COURSE_MODULE = {
  "title": "Choosing Limiter Algorithms",
  "graphicAlt": "Bullet summary graphic for Choosing Limiter Algorithms.",
  "narration": "Limiter algorithms shape both protection and user experience. A fixed window limiter counts requests during a fixed period and resets on a schedule. It is straightforward to reason about, but traffic near a window boundary can feel uneven because one period ends and another begins.\n\nA sliding window limiter smooths behavior by dividing time into smaller segments and accounting for recent activity across the window. This can reduce the sharp boundary effect of fixed windows. It is often a better fit when teams want a time-based request cap that feels more consistent to users over time.\n\nA token bucket limiter allows controlled bursts. Tokens replenish over time, and requests consume tokens. This model can be useful when an application should tolerate short bursts while still enforcing an average rate. It should be configured around realistic endpoint cost and backend capacity, not around an arbitrary desire to permit bursts.\n\nA concurrency limiter is different from a time-based request limiter. Instead of counting requests over a period, it caps simultaneous active work. That can be useful for expensive operations that tie up CPU, database connections, storage calls, or downstream services while they run. A concurrency limit may protect a backend even when the request count is not high.\n\nQueue limits and queue ordering affect user experience and resource use. Queueing can smooth short bursts, but queued requests still consume memory, wait time, and operational capacity. A queue that is too large can make failures slow and confusing. A queue that is too small can reject short bursts that the system could safely absorb. The queue should be intentional and bounded.\n\nAlgorithm choice should be based on user experience, resource cost, burst tolerance, and backend capacity. A cheap read endpoint may need a different model from a report export. Authentication, upload, search, and tenant-wide operations may deserve stricter or more specialized policies. The right limiter is the one that matches the work being protected.",
  "narrationPoints": [
    "A fixed window limiter counts requests during a fixed period and resets on a schedule.",
    "A sliding window limiter smooths behavior by dividing time into smaller segments and accounting for recent activity across the window.",
    "A concurrency limit may protect a backend even when the request count is not high.",
    "Queue limits and queue ordering affect user experience and resource use.",
    "A cheap read endpoint may need a different model from a report export."
  ]
};
