window.COURSE_MODULE = {
  "title": "Partitions, Identity, Tenants, and Fairness",
  "graphicAlt": "Bullet summary graphic for Partitions, Identity, Tenants, and Fairness.",
  "narration": "Partitioning lets different users, tenants, clients, routes, or other groups have separate counters. Without partitioning, one busy client can consume a shared limit and affect unrelated users. With partitioning, a policy can be fairer because it tracks request flow against a meaningful identity or workload category.\n\nPossible partition keys include authenticated user, tenant ID, API key, client application, route group, host, or a network-derived signal. Each key has tradeoffs. A user key may work well for signed-in workflows. A tenant key may protect shared backend capacity in multi-tenant systems. A route key may separate expensive endpoints from cheap endpoints. An API key may represent a client application or integration.\n\nPartition keys should be stable, meaningful, privacy-aware, and hard to spoof. If the key changes frequently, the limit may not represent real usage. If the key is too broad, unrelated callers may interfere with each other. If the key contains sensitive data, logs and metrics may expose information unnecessarily. If the key is easy to forge, the policy may not protect the intended group.\n\nA single global counter can punish unrelated users. For example, one tenant's high-volume reporting activity should not necessarily consume all capacity for every other tenant. Fairness is especially important when backend resources are shared, such as databases, queues, search indexes, storage accounts, or third-party services.\n\nUnauthenticated requests require special care because identity may be weak or absent. Public endpoints may rely on route, host, client application, network-derived signals, or other context, but those keys should be treated as lower confidence than a strong authenticated identity. The policy should reflect that uncertainty and avoid storing unnecessary personal or sensitive data.\n\nThe practical design question is, who or what should be protected from whom. Good partitioning makes that answer visible. It turns rate limiting from one global throttle into a fairness and resource-protection policy that matches the application's real boundaries.",
  "narrationPoints": [
    "With partitioning, a policy can be fairer because it tracks request flow against a meaningful identity or workload category.",
    "Possible partition keys include authenticated user, tenant ID, API key, client application, route group, host, or a network-derived signal.",
    "If the key is easy to forge, the policy may not protect the intended group.",
    "Unauthenticated requests require special care because identity may be weak or absent.",
    "The practical design question is, who or what should be protected from whom."
  ]
};
