window.COURSE_MODULE = {
  "title": "Uploads, Request Bodies, and Decompression Risk",
  "graphicAlt": "Bullet summary graphic for Uploads, Request Bodies, and Decompression Risk.",
  "narration": "Upload paths and large request bodies need explicit limits. Body size limits may be global, endpoint-specific, or host-specific. A public profile image upload, an internal import endpoint, a document submission workflow, and a machine-to-machine data feed should not automatically share the same request body assumptions.\n\nLarge bodies can consume memory, disk, CPU, and backend capacity. Buffering, temporary storage, scanning, parsing, transformation, and downstream processing all add cost. Even when the server eventually rejects a request, the path to that rejection can still use resources. Controls should be placed early enough to protect the system.\n\nRequest decompression deserves defensive review. Compressed bodies may be smaller on the wire than after decompression. The application should still constrain decompressed size through endpoint or server limits. The risk is not only storage size; it is also CPU time, memory pressure, parser behavior, and the downstream systems that receive the expanded content.\n\nDecompression-bomb risk should be handled conceptually as a resource-consumption concern. The defensive response is to define size limits, timeouts, safe decompression behavior, endpoint scope, monitoring, and failure handling. The course does not need construction details to teach the engineering lesson: expanded content still needs boundaries.\n\nFile upload controls should include size, type, storage location, generated server-side names, scanning or review workflow where appropriate, retention rules, authorization, and business justification. Uploaded files should be processed by a documented workflow, not by a generic endpoint that accepts anything for convenience.\n\nThe practical baseline is to make large-request behavior explicit. Know which endpoints accept large bodies, why they need them, where data is buffered, which limits apply, how failures are reported, and which logs or metrics tell operators when limits are reached.",
  "narrationPoints": [
    "Upload paths and large request bodies need explicit limits.",
    "Even when the server eventually rejects a request, the path to that rejection can still use resources.",
    "The application should still constrain decompressed size through endpoint or server limits.",
    "The defensive response is to define size limits, timeouts, safe decompression behavior, endpoint scope, monitoring, and failure handling.",
    "Uploaded files should be processed by a documented workflow, not by a generic endpoint that accepts anything for convenience."
  ]
};
