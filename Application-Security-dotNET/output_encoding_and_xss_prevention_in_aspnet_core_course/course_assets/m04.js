window.COURSE_MODULE = {
  "title": "Context-Aware Encoding Rules",
  "graphicAlt": "Bullet summary graphic for Context-Aware Encoding Rules.",
  "narration": "Encoding must match the destination context. The browser interprets text differently depending on where it appears. HTML body text, attribute values, URL components, script data, style data, JSON, and client-side templates are not the same destination. A single generic encoding step cannot safely cover every place a value might be rendered.\n\nHTML body text and HTML attributes are related but still different. Framework-supported Razor and tag helper patterns usually handle common cases well when developers provide data as data rather than as assembled markup. Problems often appear when code concatenates strings into attributes, creates markup in helpers, or moves values between contexts without reconsidering the destination.\n\nJavaScript, CSS, URL, and JSON-related contexts need extra caution. The safest design is often to avoid placing untrusted data directly into risky contexts. For example, prefer passing structured data through safe framework mechanisms, using data attributes carefully, or letting a client framework bind text safely rather than mixing server strings into executable client-side areas.\n\nURL handling should distinguish between a full URL, a path, a query value, and a display label. CSS-related rendering should be kept narrow and controlled. JSON should be generated by serializers rather than hand-built strings. These distinctions matter because the browser does not interpret every destination as plain text.\n\nPrefer framework-supported helpers and encoders over custom string manipulation. Custom encoding code tends to miss context differences, edge cases, future changes, or team conventions. Approved platform patterns are easier to review and easier to keep consistent across modules, views, APIs, and client code.\n\nReview transitions where server data becomes client-side data. A value may start as a safe server-side string, become JSON, move into a client state store, and then appear in the DOM. Each transition should preserve the separation between data and markup. Context-aware encoding is a design discipline, not only a last-minute function call.",
  "narrationPoints": [
    "HTML body text and HTML attributes are related but still different.",
    "The safest design is often to avoid placing untrusted data directly into risky contexts.",
    "URL handling should distinguish between a full URL, a path, a query value, and a display label.",
    "Approved platform patterns are easier to review and easier to keep consistent across modules, views, APIs, and client code.",
    "Context-aware encoding is a design discipline, not only a last-minute function call."
  ]
};
