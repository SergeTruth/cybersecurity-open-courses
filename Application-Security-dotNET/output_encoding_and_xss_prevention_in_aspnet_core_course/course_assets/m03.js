window.COURSE_MODULE = {
  "title": "Razor Encoding Defaults and Unsafe Escape Hatches",
  "graphicAlt": "Bullet summary graphic for Razor Encoding Defaults and Unsafe Escape Hatches.",
  "narration": "Razor's default behavior is one of the most important safety features in ASP.NET Core views. When normal string values are rendered in standard Razor output, the framework encodes them for HTML display. That means a display name, comment, status message, or product label is treated as text rather than browser structure. This default is valuable because it protects ordinary display code without requiring every developer to remember a manual step.\n\nFramework defaults are strongest when teams stay inside the intended rendering model. Standard Razor expressions, tag helpers, view models, and framework-supported rendering patterns give the runtime enough information to handle values appropriately for common cases. Problems usually appear when code steps outside that model without a clear reason or review path.\n\nEscape hatches need discipline. Raw output helpers, manually assembled markup, broad helper methods that return already-rendered content, and code that combines untrusted strings with browser structure can bypass normal protections. Some applications have legitimate reasons to render trusted markup, but those cases should be narrow, documented, and tied to a reviewed source of trust.\n\nShared layouts, partial views, view components, tag helpers, and reusable display helpers can either preserve safety or weaken it across the whole application. A small unsafe shortcut inside a shared component can affect many pages. Conversely, a well-designed helper can make safe display easier and reduce repeated decisions across teams.\n\nTrusted markup should be rare and controlled. If the application accepts or stores content that is intended to contain markup, the team should know where that content comes from, what controls are applied, who can create or approve it, and where it is displayed. Treat intentional markup rendering as a special capability, not as a casual formatting convenience.\n\nA strong review habit is to search for places that bypass default encoding or construct markup outside normal Razor patterns. Those locations should have a clear explanation, tests or review notes where appropriate, and an owner who understands the lifecycle of the content being rendered.",
  "narrationPoints": [
    "When normal string values are rendered in standard Razor output, the framework encodes them for HTML display.",
    "Framework defaults are strongest when teams stay inside the intended rendering model.",
    "Some applications have legitimate reasons to render trusted markup, but those cases should be narrow, documented, and tied to a reviewed source of trust.",
    "Conversely, a well-designed helper can make safe display easier and reduce repeated decisions across teams.",
    "A strong review habit is to search for places that bypass default encoding or construct markup outside normal Razor patterns."
  ]
};
