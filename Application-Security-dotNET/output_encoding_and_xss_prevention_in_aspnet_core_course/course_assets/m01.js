window.COURSE_MODULE = {
  "title": "XSS Prevention Starts with Output Context",
  "graphicAlt": "Bullet summary graphic for XSS Prevention Starts with Output Context.",
  "narration": "Cross-site scripting prevention starts with output context. A value is not risky only because it came from a user, and it is not safe only because it came from a database. The key question is where that value will be rendered and how the browser will interpret it. Display names, comments, product descriptions, support messages, CMS fields, and imported text can all be harmless as plain text and risky when they are placed into a browser-interpreted context without the right handling.\n\nASP.NET Core developers should think in rendering destinations. Plain HTML body text, HTML attributes, URLs, script data, style data, JSON delivered to a client, and client-side templating all have different rules. A value that is safe to show as visible text may not be safe to place into another context without a framework-supported pattern for that context.\n\nRazor and ASP.NET Core provide helpful defaults. Normal string output in Razor is encoded for HTML display, which is a strong baseline when teams stay inside the intended rendering model. The risk increases when code bypasses those defaults, builds markup manually, mixes data with structure, or pushes server values into client-side code without a clear context-aware pattern.\n\nXSS prevention is not a single magic function. It is a repeatable engineering habit: identify untrusted or unreviewed data, understand where it will appear, choose the safest rendering model, apply context-appropriate encoding, and review any place that intentionally renders markup. This keeps prevention tied to the actual browser behavior rather than to vague trust labels.\n\nThe practical goal is to make safe rendering ordinary. Teams should know which helpers are approved, which escape hatches require review, which components can render rich text, and where server data becomes browser content. When those decisions are explicit, output encoding becomes part of normal development instead of a late security patch.",
  "narrationPoints": [
    "A value is not risky only because it came from a user, and it is not safe only because it came from a database.",
    "ASP.NET Core developers should think in rendering destinations.",
    "Normal string output in Razor is encoded for HTML display, which is a strong baseline when teams stay inside the intended rendering model.",
    "XSS prevention is not a single magic function.",
    "When those decisions are explicit, output encoding becomes part of normal development instead of a late security patch."
  ]
};
