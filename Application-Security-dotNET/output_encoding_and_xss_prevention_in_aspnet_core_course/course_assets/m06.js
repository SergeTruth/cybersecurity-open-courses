window.COURSE_MODULE = {
  "title": "APIs, JavaScript Clients, and DOM-Safe Rendering",
  "graphicAlt": "Bullet summary graphic for APIs, JavaScript Clients, and DOM-Safe Rendering.",
  "narration": "API output may be safe as data and still become unsafe when a browser client renders it incorrectly. An ASP.NET Core API can return a display name, comment, product description, or support message as structured JSON. The risk appears when a browser client treats that string as markup or inserts it into a context that interprets it as browser structure rather than data.\n\nKeep data and markup responsibilities clear. The server should return well-structured data with clear field meanings. The client should render that data using safe DOM APIs, safe framework binding patterns, and components that keep text separate from markup. When the server sends pre-rendered fragments or the client turns strings into markup, the boundary becomes harder to review.\n\nAvoid client-side shortcuts that turn untrusted strings into rendered markup. This includes convenience helpers, wrapper components, legacy utilities, or template patterns that treat content as already trusted. If a feature truly needs rich content, use a reviewed rich text path rather than scattering one-off rendering decisions across the frontend.\n\nServer-rendered pages, single-page applications, and hybrid applications have different rendering models. A value may be rendered once by Razor, later hydrated into client state, then displayed again by JavaScript. Each display point needs a safe pattern. It is not enough to say the server encoded it once if the client later changes the context.\n\nReview templating boundaries where API values become visible browser content. Look at table components, notification systems, markdown display, comment views, profile fields, search results, error messages, and CMS blocks. These are common places where data is meant to be visible and where teams may accidentally mix data with markup.\n\nAPI and frontend teams should share rendering assumptions. API documentation should identify which fields are plain text, which fields are identifiers, which fields are URLs, which fields may contain approved rich content, and which fields are never intended to be rendered directly. Clear contracts reduce guesswork in client code.",
  "narrationPoints": [
    "API output may be safe as data and still become unsafe when a browser client renders it incorrectly.",
    "The client should render that data using safe DOM APIs, safe framework binding patterns, and components that keep text separate from markup.",
    "Avoid client-side shortcuts that turn untrusted strings into rendered markup.",
    "Server-rendered pages, single-page applications, and hybrid applications have different rendering models.",
    "API documentation should identify which fields are plain text, which fields are identifiers, which fields are URLs, which fields may contain approved rich content, and which fields are never intended to be rendered directly."
  ]
};
