window.COURSE_MODULE = {
  "title": "Safe Handling of Rich Text and User-Provided HTML",
  "graphicAlt": "Bullet summary graphic for Safe Handling of Rich Text and User-Provided HTML.",
  "narration": "Plain text is usually safer and simpler than accepting markup. Many product requirements can be satisfied with plain text, markdown rendered through a reviewed path, limited formatting controls, or predefined presentation components. Before accepting user-provided HTML or broad rich text, ask whether the business need truly requires it.\n\nRich text changes the security model. A CMS field, support article, product description, user profile, marketing block, or partner-provided content may need formatting, but it also creates a path where content can be interpreted as browser structure. That path needs specialized controls, clear ownership, and review by people who understand both the product requirement and the rendering model.\n\nSanitization is not the same as encoding. Encoding prepares data for a specific output context. Sanitization transforms content by allowing or removing specific markup, attributes, and structures according to a policy. A sanitizer should be treated as an allow-list control with well-understood behavior, not as a vague cleanup step that makes arbitrary content safe forever.\n\nStored HTML should not receive broad trust automatically. Content can come from editors, imports, migrations, third-party systems, administrator tools, or older versions of the application. If stored markup is rendered, the team should know whether it has been sanitized, when that happened, which policy was used, and whether the same policy still matches the current display context.\n\nKeep preview, edit, display, export, and moderation workflows consistent. A rich text editor preview that renders differently from the final page can hide problems from reviewers. An export path that renders stored content differently from the web page can create a second display model. Consistency makes review and testing easier.\n\nAvoid custom sanitizers unless the team has the expertise and maintenance plan to keep them safe. Sanitization rules age as browsers, libraries, and product features change. If rich content is required, use mature libraries or platform services according to policy, document approved patterns, and limit who can create or approve rich content when the risk is higher.",
  "narrationPoints": [
    "Before accepting user-provided HTML or broad rich text, ask whether the business need truly requires it.",
    "A CMS field, support article, product description, user profile, marketing block, or partner-provided content may need formatting, but it also creates a path where content can be interpreted as browser structure.",
    "If stored markup is rendered, the team should know whether it has been sanitized, when that happened, which policy was used, and whether the same policy still matches the current display context.",
    "A rich text editor preview that renders differently from the final page can hide problems from reviewers.",
    "If rich content is required, use mature libraries or platform services according to policy, document approved patterns, and limit who can create or approve rich content when the risk is higher."
  ]
};
