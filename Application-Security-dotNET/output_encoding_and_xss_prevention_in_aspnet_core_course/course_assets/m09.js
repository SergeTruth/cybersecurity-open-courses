window.COURSE_MODULE = {
  "title": "Review, Testing, Dependencies, and Legacy Code",
  "graphicAlt": "Bullet summary graphic for Review, Testing, Dependencies, and Legacy Code.",
  "narration": "Code review for XSS prevention should focus on rendering decisions. Raw output, manually assembled markup, broad display helpers, custom encoders, rich text rendering, markdown rendering, CMS display, and client-side DOM shortcuts deserve special attention. Reviewers do not need offensive payload examples to ask the right question: what context is this value rendered into, and which approved pattern protects it?\n\nShared components multiply risk and protection. Layouts, partial views, view components, tag helpers, reusable table components, notification components, form helpers, and display templates may affect many pages. A safe shared helper can reduce repeated mistakes. An unsafe shared helper can spread risky rendering across the application.\n\nDependencies are part of the rendering model. UI packages, rich text editors, markdown renderers, sanitizers, template engines, client-side frameworks, and server-side helpers should be reviewed for maintenance, configuration, and approved use. A dependency that handles content should have an owner, an update path, and a clear understanding of which safety guarantees it does and does not provide.\n\nLegacy code often contains older rendering assumptions. It may have hand-built markup, helper methods that predate current framework conventions, broad trusted-content flags, or views that combine server data with client code in hard-to-review ways. Migration should move those areas toward Razor defaults, context-aware helpers, operation-specific view models, and documented rich text paths.\n\nTesting can support prevention without becoming payload-heavy training. Tests can verify that ordinary display names, comments, descriptions, and support messages are rendered as text in expected contexts. They can also cover rich text policies, component behavior, and regression cases from previous defects using safe fixtures and assertions about rendered structure rather than exploit demonstrations.\n\nLogging and diagnostics need review too. Applications sometimes preserve raw user content in logs, error pages, debug panels, or support exports and later render that content in internal tools. Logs should provide useful context without becoming a place where untrusted content is reinterpreted by a browser. Defensive review includes both customer-facing pages and operational views.",
  "narrationPoints": [
    "Code review for XSS prevention should focus on rendering decisions.",
    "A dependency that handles content should have an owner, an update path, and a clear understanding of which safety guarantees it does and does not provide.",
    "Migration should move those areas toward Razor defaults, context-aware helpers, operation-specific view models, and documented rich text paths.",
    "Tests can verify that ordinary display names, comments, descriptions, and support messages are rendered as text in expected contexts.",
    "Logs should provide useful context without becoming a place where untrusted content is reinterpreted by a browser."
  ]
};
