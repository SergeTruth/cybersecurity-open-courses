window.COURSE_MODULE = {
  "title": "Key Management, Secret Storage, and Rotation",
  "graphicAlt": "Bullet summary graphic for Key Management, Secret Storage, and Rotation.",
  "narration": "Key management is often the hardest part of practical cryptography. Choosing a maintained algorithm is only the beginning. The system also needs to generate keys safely, store them safely, grant access narrowly, rotate them deliberately, back them up where appropriate, recover from failures, and retire them when no longer needed.\n\nKeys need lifecycle management: generation, storage, use, access control, rotation, revocation, backup, recovery, destruction, and compromise response. Each stage needs ownership. A key with no owner or rotation path becomes an operational liability even if the algorithm is strong.\n\nDevelopment, staging, and production keys should be separated. Environment separation reduces blast radius, prevents test behavior from affecting production, and allows rotation or containment without unnecessary disruption. Shared keys across unrelated environments create avoidable coupling.\n\nKeys and secrets should not be stored in source control, logs, public configuration, build output, screenshots, support tickets, or client-side assets. These locations are copied and retained through normal engineering and support workflows. Secret storage should be intentional and access-controlled.\n\nRotation should be planned so dependent systems can continue operating during transition. Some systems need overlap between old and new keys. Others need versioned keys, key identifiers, dual-read behavior, or phased deployment. The rotation plan should be designed before rotation is urgent.\n\nCloud key stores or HSM-backed services may reduce direct key exposure, but they still require permissions and lifecycle governance. Access policies, managed identity, audit logs, network configuration, backup, and emergency procedures still matter.\n\nKey compromise response should include containment, rotation, revocation, monitoring, communication, and follow-up review. The response plan should identify which data, tokens, services, and dependent systems may be affected by a key change.",
  "narrationPoints": [
    "A key with no owner or rotation path becomes an operational liability even if the algorithm is strong.",
    "Keys and secrets should not be stored in source control, logs, public configuration, build output, screenshots, support tickets, or client-side assets.",
    "The rotation plan should be designed before rotation is urgent.",
    "Cloud key stores or HSM-backed services may reduce direct key exposure, but they still require permissions and lifecycle governance.",
    "Key compromise response should include containment, rotation, revocation, monitoring, communication, and follow-up review."
  ]
};
