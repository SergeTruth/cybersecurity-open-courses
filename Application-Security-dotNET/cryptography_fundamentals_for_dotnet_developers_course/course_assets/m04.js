window.COURSE_MODULE = {
  "title": "Symmetric Encryption and Authenticated Encryption",
  "graphicAlt": "Bullet summary graphic for Symmetric Encryption and Authenticated Encryption.",
  "narration": "Symmetric encryption uses the same secret key to protect and recover data. It is commonly used when an application needs confidentiality for stored data, message payloads, or protected values. The security of the result depends on the algorithm, mode, key, nonce or IV handling, metadata handling, and operational lifecycle.\n\nConfidentiality alone is usually not enough. If a protected value can be modified without detection, the application may recover data that no longer represents what was originally protected. Authenticated encryption protects confidentiality and also detects unauthorized modification, which makes it a better fit for many application data protection scenarios.\n\nNonces and IVs must be generated, used, and stored according to the requirements of the chosen algorithm and mode. Some designs require uniqueness. Some require unpredictability. Some require both. Reusing a nonce with certain algorithms can undermine the intended protection, so algorithm-specific rules must be followed exactly.\n\nDevelopers should prefer maintained authenticated-encryption APIs or framework services when they fit the use case. If ASP.NET Core Data Protection fits the protected-payload scenario, it is often a better choice than hand-writing encryption code. If direct encryption is required, the team should document the chosen API, key source, nonce handling, and storage format.\n\nAvoid hand-combining encryption and MAC logic unless implementing a reviewed protocol. The order of operations, covered data, key separation, error behavior, and compatibility details matter. A construction that looks reasonable can still miss important requirements.\n\nA safe design record should explain what is encrypted, why confidentiality is needed, how integrity is checked, where the key lives, how nonces or IVs are handled, and how the data can be rotated or retired later.",
  "narrationPoints": [
    "Symmetric encryption uses the same secret key to protect and recover data.",
    "Nonces and IVs must be generated, used, and stored according to the requirements of the chosen algorithm and mode.",
    "If direct encryption is required, the team should document the chosen API, key source, nonce handling, and storage format.",
    "Avoid hand-combining encryption and MAC logic unless implementing a reviewed protocol.",
    "A safe design record should explain what is encrypted, why confidentiality is needed, how integrity is checked, where the key lives, how nonces or IVs are handled, and how the data can be rotated or retired later."
  ]
};
