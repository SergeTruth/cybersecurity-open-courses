window.COURSE_MODULE = {
  "title": "Asymmetric Cryptography, Signatures, and Certificates",
  "graphicAlt": "Bullet summary graphic for Asymmetric Cryptography, Signatures, and Certificates.",
  "narration": "Asymmetric cryptography uses a public and private key pair. The public key can be shared. The private key must be protected. Many .NET systems encounter asymmetric cryptography through TLS certificates, signed tokens, client certificates, document signatures, package signing, or service identity.\n\nDigital signatures prove that data was signed by a holder of the private key and help detect modification. Verification uses the corresponding public key and the trust model around it. This is different from encryption. Encryption protects confidentiality. Signing supports integrity and proof of origin within the rules of the trust model.\n\nCertificates bind public keys to identities through a trust model. A certificate does not help if the application does not validate it correctly. Validation commonly includes trust chain, expiration, hostname or identity expectations, usage constraints, and revocation behavior where appropriate.\n\nDisabling certificate validation or accepting any certificate removes important security guarantees. It may make a development connection easier, but it breaks the identity and integrity expectations that certificate validation is meant to provide. Production behavior should use explicit trust rules, not permissive shortcuts.\n\nPrivate key protection is an operational concern. Keys may live in certificate stores, managed key services, hardware-backed systems, files, containers, or deployment platforms. The team should know who can access private keys, how they are rotated, how expiration is handled, and what happens when a certificate must be replaced.\n\nThe practical review is to ask which identity is being established, which party holds the private key, how the public key is trusted, and whether encryption, signing, or certificate validation is actually the right mechanism for the task.",
  "narrationPoints": [
    "Asymmetric cryptography uses a public and private key pair.",
    "Verification uses the corresponding public key and the trust model around it.",
    "A certificate does not help if the application does not validate it correctly.",
    "Disabling certificate validation or accepting any certificate removes important security guarantees.",
    "The practical review is to ask which identity is being established, which party holds the private key, how the public key is trusted, and whether encryption, signing, or certificate validation is actually the right mechanism for the task."
  ]
};
