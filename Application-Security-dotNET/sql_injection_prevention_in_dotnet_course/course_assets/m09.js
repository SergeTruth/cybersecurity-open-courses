window.COURSE_MODULE = {
  "title": "Review, Testing, Dependencies, and Legacy Remediation",
  "graphicAlt": "Bullet summary graphic for Review, Testing, Dependencies, and Legacy Remediation.",
  "narration": "Code review for SQL injection prevention should focus on query construction patterns. String-built SQL, interpolation in command text, broad raw SQL helpers, custom query builders, reporting code, search features, export tools, and administrative utilities are all worth careful attention. Reviewers do not need payload demonstrations to ask the right question: is variable data separated from database command structure?\n\nMicro-ORMs and third-party data-access libraries can be safe when used correctly, but each has its own conventions. Teams should understand how a library binds parameters, how it handles dynamic query options, where raw command text appears, and what patterns are approved locally. Convenience wrappers should not hide unsafe construction from review.\n\nDependency review matters for database drivers, ORM packages, query-builder libraries, reporting components, and administrative tooling. These dependencies influence how commands are built, how values are bound, how errors surface, and how updates are applied. A data-access dependency should have an owner and a maintenance plan.\n\nLegacy remediation should be prioritized. Start by inventorying risky query paths. Prioritize operations touching sensitive data, broad reports, administrative tools, multi-tenant boundaries, high-volume workflows, and externally reachable endpoints. Replace unsafe construction with parameterized patterns, narrow query shape, and add regression coverage around the expected behavior.\n\nSafe testing can be defensive without becoming payload-heavy instruction. Tests can verify that common values such as customer IDs, dates, statuses, search terms, and report filters are passed through approved parameter patterns and cannot alter the intended query shape. Static analysis, code review checklists, and CI checks can also help catch regressions.\n\nDocument approved patterns so future changes do not recreate the same risk. A short guide that explains which APIs to use, how to handle dynamic filters, where raw SQL is allowed, and what review is required can save time and improve consistency across teams.",
  "narrationPoints": [
    "Code review for SQL injection prevention should focus on query construction patterns.",
    "Micro-ORMs and third-party data-access libraries can be safe when used correctly, but each has its own conventions.",
    "Dependency review matters for database drivers, ORM packages, query-builder libraries, reporting components, and administrative tooling.",
    "Static analysis, code review checklists, and CI checks can also help catch regressions.",
    "A short guide that explains which APIs to use, how to handle dynamic filters, where raw SQL is allowed, and what review is required can save time and improve consistency across teams."
  ]
};
