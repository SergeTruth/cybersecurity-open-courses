window.COURSE_MODULE = {
  "title": "Input Validation and Query Shape Control",
  "graphicAlt": "Bullet summary graphic for Input Validation and Query Shape Control.",
  "narration": "Input validation is a supporting control for SQL injection prevention, not a replacement for parameterized queries. Validation answers whether a value is acceptable for the business purpose: does the customer ID have the expected shape, is the date range reasonable, is the status one the workflow supports, and is the requested report option allowed.\n\nAllow-listing is especially useful for values that influence query shape. Sort fields, filter names, report columns, export options, page sizes, enum-like values, and mode switches should come from approved sets. The application can map those approved names to internal query expressions without letting the caller define database structure directly.\n\nClient input should not control SQL structure. A caller may provide a search term, a date range, a status, or a supported sort choice, but the application should decide how those values are represented in the query. The line between value and structure should be obvious in code review.\n\nPaging, result limits, bounded date ranges, and controlled filters support both security and reliability. They reduce accidental broad queries, avoid unnecessary exposure, and protect shared database capacity. A query can be injection-resistant and still be operationally risky if it returns too much data or performs expensive work without limits.\n\nNormalization and canonicalization help ensure comparisons happen on consistent data. Before validating identifiers, names, states, or tenant keys, the application may need to handle casing, whitespace, culture-specific formatting, or alternate representations. The goal is to make validation and authorization decisions on the representation the application actually trusts.\n\nQuery builders should be designed so only approved query shapes can be produced. A safe builder does not expose arbitrary fragments as a convenience. It accepts well-defined options, validates them, maps them to internal expressions, and preserves parameterization for variable values. That makes flexibility reviewable instead of open-ended.",
  "narrationPoints": [
    "Input validation is a supporting control for SQL injection prevention, not a replacement for parameterized queries.",
    "The application can map those approved names to internal query expressions without letting the caller define database structure directly.",
    "Paging, result limits, bounded date ranges, and controlled filters support both security and reliability.",
    "Before validating identifiers, names, states, or tenant keys, the application may need to handle casing, whitespace, culture-specific formatting, or alternate representations.",
    "Query builders should be designed so only approved query shapes can be produced."
  ]
};
