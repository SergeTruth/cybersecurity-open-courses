window.COURSE_MODULE = {
  "title": "Least Privilege, Secrets, and Database Configuration",
  "graphicAlt": "Bullet summary graphic for Least Privilege, Secrets, and Database Configuration.",
  "narration": "Least privilege reduces blast radius. The database account used by a running application should have only the permissions the application needs for its normal responsibilities. Broad permissions may be convenient during development, but they increase the consequences of defects, misconfiguration, and unexpected behavior.\n\nDifferent tasks may require different accounts or permissions. Runtime access, migrations, reporting, maintenance, administration, and emergency operations do not necessarily need the same database rights. Separating those responsibilities can make releases safer and make operational review clearer.\n\nConnection strings, database credentials, certificates, and service identities are sensitive assets. They should be protected through secret-management mechanisms, managed identities, deployment systems, or other approved controls. They should not be hardcoded into source code, committed to repositories, pasted into tickets, printed in logs, or captured in screenshots.\n\nEnvironment separation matters. Development, test, staging, and production should not casually share credentials, data access permissions, or diagnostic settings. A development shortcut should not become a production permission model. Review database configuration as part of release and change management.\n\nDatabase permissions should be visible enough to review. Teams should know which application identity can access which database objects, which migrations can change schema, which reports can read broad data, and which administrative workflows require elevated access. Hidden permissions are hard to manage safely.\n\nLeast privilege does not replace parameterization, but it complements it. If a query path behaves unexpectedly, narrower permissions can reduce what that path can affect. Strong prevention and bounded impact work best together.",
  "narrationPoints": [
    "The database account used by a running application should have only the permissions the application needs for its normal responsibilities.",
    "Separating those responsibilities can make releases safer and make operational review clearer.",
    "Connection strings, database credentials, certificates, and service identities are sensitive assets.",
    "Review database configuration as part of release and change management.",
    "Teams should know which application identity can access which database objects, which migrations can change schema, which reports can read broad data, and which administrative workflows require elevated access."
  ]
};
