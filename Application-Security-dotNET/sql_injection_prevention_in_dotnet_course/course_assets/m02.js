window.COURSE_MODULE = {
  "title": "Data Flow, Trust Boundaries, and Query Construction",
  "graphicAlt": "Bullet summary graphic for Data Flow, Trust Boundaries, and Query Construction.",
  "narration": "Query-influencing data can enter a .NET application through route values, query strings, forms, JSON bodies, headers, files, queues, imports, logs, configuration, databases, and third-party services. Some of that data is obviously user-provided. Some appears later as stored records, partner data, operational metadata, or internal workflow values. All of it deserves review when it influences a database query.\n\nStored or internal data should not automatically be trusted. A search term saved yesterday, a report filter imported from a file, a status value received from a partner, or a configuration value supplied by deployment can still affect query behavior later. Storage changes where the data lives; it does not prove the data is safe to mix into command text.\n\nTrust boundaries exist between web clients, APIs, backend services, background jobs, data stores, administrators, reporting tools, and maintenance utilities. Query construction is one of those boundaries because it translates application intent into database instructions. That boundary should be narrow, explicit, and protected by approved data-access patterns.\n\nDynamic search, filtering, sorting, reporting, export, and administrative tools often deserve more review than simple lookup operations. They tend to accept flexible options and may touch sensitive or broad data sets. Flexibility is not automatically unsafe, but the application should control which query shapes are possible rather than letting callers define structure freely.\n\nA practical prevention technique is mapping data flow from entry point to query boundary. Identify where values enter, where they are normalized or validated, which model carries them, which service constructs the query, and which library sends it to the database. This map helps reviewers distinguish parameter values from query structure decisions.\n\nData-flow mapping also helps teams find neglected paths. Background imports, scheduled reports, support dashboards, and legacy admin screens may not receive the same attention as public controllers, but they can still influence important database operations. Prevention is stronger when the review follows data rather than only following web routes.",
  "narrationPoints": [
    "Query-influencing data can enter a .NET application through route values, query strings, forms, JSON bodies, headers, files, queues, imports, logs, configuration, databases, and third-party services.",
    "Storage changes where the data lives; it does not prove the data is safe to mix into command text.",
    "Query construction is one of those boundaries because it translates application intent into database instructions.",
    "A practical prevention technique is mapping data flow from entry point to query boundary.",
    "Prevention is stronger when the review follows data rather than only following web routes."
  ]
};
