window.COURSE_MODULE = {
  "title": "Error Handling, Logging, and Detection Signals",
  "graphicAlt": "Bullet summary graphic for Error Handling, Logging, and Detection Signals.",
  "narration": "Database errors shown to users should be safe and limited. They should not expose SQL text, table names, column names, connection strings, stack traces, internal paths, tokens, or sensitive record details. A user usually needs to know whether input should be corrected, a request can be retried, or support should be contacted. They do not need internal database diagnostics.\n\nAuthorized operators need useful detail, but logs should still avoid sensitive values. Structured logging can include operation names, sanitized validation outcomes, dependency names, timing, categories, correlation identifiers, and safe context. It should avoid raw secrets, full connection strings, sensitive record contents, and unnecessary query text that would create disclosure risk.\n\nCorrelation IDs or request identifiers help investigation. A suspicious validation failure, authorization denial, repeated database error, or administrative data operation may involve an API request, service-layer code, a database call, and a background job. Correlation lets operators connect events without placing sensitive data into every log entry.\n\nSecurity-relevant signals include validation failures around query options, authorization failures on data operations, repeated database errors, unusual query patterns, export activity, reporting access, and administrative data changes. These signals can help teams detect problems, tune controls, and identify areas that need remediation.\n\nDetection supports prevention; it does not replace safe query construction. Logs can reveal that something needs attention, but they cannot make an unsafe query safe. The right design prevents variable data from becoming command structure, limits database permissions, returns safe errors, and preserves enough operational context to investigate responsibly.",
  "narrationPoints": [
    "A user usually needs to know whether input should be corrected, a request can be retried, or support should be contacted.",
    "Structured logging can include operation names, sanitized validation outcomes, dependency names, timing, categories, correlation identifiers, and safe context.",
    "Correlation lets operators connect events without placing sensitive data into every log entry.",
    "Security-relevant signals include validation failures around query options, authorization failures on data operations, repeated database errors, unusual query patterns, export activity, reporting access, and administrative data changes.",
    "The right design prevents variable data from becoming command structure, limits database permissions, returns safe errors, and preserves enough operational context to investigate responsibly."
  ]
};
