window.COURSE_MODULE = {
  "title": "Stored Procedures, Dynamic SQL, and Database Boundaries",
  "graphicAlt": "Bullet summary graphic for Stored Procedures, Dynamic SQL, and Database Boundaries.",
  "narration": "Stored procedures can be useful database boundaries. They can centralize complex operations, reduce direct table access, support permission separation, and give database teams a clear place to review important behavior. But a stored procedure is not automatically safe simply because it lives in the database.\n\nDynamic SQL inside stored procedures still needs safe construction. If a procedure accepts values that influence database command structure, the same defensive questions apply: which parts are approved structure, which parts are parameter values, and how does the procedure prevent callers from controlling more than intended. Parameterization remains important inside database workflows.\n\nStored procedures also need careful permission design. Application accounts should not have broad database permissions by default. In some architectures, procedures can expose only the operations the application needs while limiting direct access to underlying objects. The right model depends on the application, but broad convenience permissions should be reviewed before release.\n\nReporting, search, export, maintenance, and administrative procedures often combine flexible input with broad data access. They may run outside normal request flows, serve privileged users, or support operational needs under time pressure. These procedures deserve clear ownership, parameter review, logging expectations, and least-privilege design.\n\nApplication and database teams share prevention responsibility. Application developers understand caller context, DTOs, validation, authorization, and business workflow. Database specialists understand permissions, procedure behavior, query plans, and data boundaries. Safe design improves when both sides review how application values become database operations.\n\nA good release process treats database objects as part of the application security surface. Procedure changes, permission changes, reporting changes, and migration scripts should be reviewed with the same seriousness as controller or service-layer code that constructs queries directly.",
  "narrationPoints": [
    "But a stored procedure is not automatically safe simply because it lives in the database.",
    "Dynamic SQL inside stored procedures still needs safe construction.",
    "Application accounts should not have broad database permissions by default.",
    "Safe design improves when both sides review how application values become database operations.",
    "A good release process treats database objects as part of the application security surface."
  ]
};
