window.COURSE_MODULE = {
  "title": "Parameterized Queries and Safe Command Patterns",
  "graphicAlt": "Bullet summary graphic for Parameterized Queries and Safe Command Patterns.",
  "narration": "Parameterized queries are the central defensive pattern for values supplied to SQL commands. The command describes the operation the application intends to perform. Parameters carry the values used by that operation, such as customer IDs, tenant IDs, date ranges, statuses, search terms, or paging values. Keeping those roles separate prevents values from becoming database instruction text.\n\nADO.NET and database providers support command and parameter APIs for this separation. The exact classes and providers can vary, but the design principle remains the same: command text is defined by the application, and variable values are bound through parameters. Reviewers should be able to see that separation without having to reason about manual string construction.\n\nType-safe handling matters. Values such as identifiers, dates, numeric ranges, booleans, statuses, and enum-like options should be represented as appropriate .NET types as early as practical. Stronger types reduce ambiguous conversions, make validation easier, and help data-access code express intent more clearly.\n\nEscaping, blacklisting, and character replacement are not reliable substitutes for parameterization. They depend on assumptions about database syntax, encoding, provider behavior, future query changes, and every possible place a value might be used. A safe pattern should not require every developer to predict how a database parser might interpret a manually altered string.\n\nString concatenation and interpolation become risky when variable data is used to build SQL command text. Even when a value looks harmless, the code pattern is difficult to audit and may be copied into less controlled situations. Teams should prefer approved query APIs and helper functions that make unsafe construction unusual.\n\nConsistency is part of the defense. If one repository uses safe parameterized commands, another uses a micro-ORM, and a third builds SQL fragments through custom helpers, reviewers need clear local rules for each pattern. Approved examples, code review checklists, and shared data-access utilities help make safe query construction the default behavior.",
  "narrationPoints": [
    "Parameters carry the values used by that operation, such as customer IDs, tenant IDs, date ranges, statuses, search terms, or paging values.",
    "ADO.NET and database providers support command and parameter APIs for this separation.",
    "A safe pattern should not require every developer to predict how a database parser might interpret a manually altered string.",
    "String concatenation and interpolation become risky when variable data is used to build SQL command text.",
    "If one repository uses safe parameterized commands, another uses a micro-ORM, and a third builds SQL fragments through custom helpers, reviewers need clear local rules for each pattern."
  ]
};
