window.COURSE_MODULE = {
  "title": "DbContext, Configuration, and Connection Security",
  "graphicAlt": "Bullet summary graphic for DbContext, Configuration, and Connection Security.",
  "narration": "DbContext lifetime should match the application pattern. In many ASP.NET Core applications, a scoped DbContext per request is a normal starting point because it keeps change tracking and unit-of-work behavior bounded. Background jobs, batch operations, and long-running processes may need different patterns, but those patterns should be intentional rather than accidental reuse of a request-oriented design.\n\nConfiguration should differ across development, test, staging, and production. Development may need local databases, detailed diagnostics, and disposable data. Production needs protected credentials, stable provider settings, controlled logging, safe retry behavior, and permission boundaries that reflect real operational risk. Environment-specific settings should be explicit and reviewable.\n\nConnection strings, database credentials, certificates, managed identities, and service principals are sensitive assets. They should be managed through approved secret-management and deployment mechanisms, not hardcoded in source, committed to ordinary configuration files, printed in logs, captured in screenshots, or exposed in build output. A leaked connection string can become direct access to sensitive data.\n\nDatabase provider configuration and EF Core service registration deserve review. Retry settings, command timeouts, detailed errors, sensitive data logging, lazy loading behavior, connection pooling, migrations configuration, and provider-specific options all influence security or operations. Defaults are useful, but teams should know which settings are active in production and why.\n\nDebug-oriented database settings should stay out of production. Detailed exception output, sensitive value logging, broad local permissions, automatic destructive behavior, and sample connection strings may be helpful during development, but they are inappropriate for real users and production data. Release review should include EF Core configuration, not only controller and service code.\n\nDifferent activities may need different database permissions. Runtime access, migrations, reporting, maintenance, and administration do not necessarily require the same rights. Separating those responsibilities where appropriate makes it easier to reduce blast radius and understand who or what can change data structures, read broad data, or perform privileged operations.",
  "narrationPoints": [
    "In many ASP.NET Core applications, a scoped DbContext per request is a normal starting point because it keeps change tracking and unit-of-work behavior bounded.",
    "Configuration should differ across development, test, staging, and production.",
    "Connection strings, database credentials, certificates, managed identities, and service principals are sensitive assets.",
    "Database provider configuration and EF Core service registration deserve review.",
    "Release review should include EF Core configuration, not only controller and service code."
  ]
};
