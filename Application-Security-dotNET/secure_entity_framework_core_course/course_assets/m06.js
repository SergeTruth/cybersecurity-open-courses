window.COURSE_MODULE = {
  "title": "Entity Design, Mapping, and Update Boundaries",
  "graphicAlt": "Bullet summary graphic for Entity Design, Mapping, and Update Boundaries.",
  "narration": "Entity design is part of defensive application architecture. Persistence entities describe how the application stores and relates data. API input models, view models, and command models describe what a caller or workflow is allowed to provide. Keeping those models separate where it improves safety helps prevent external input from shaping more of the entity than intended.\n\nOver-posting and unintended field updates are common design risks. A request to update a customer display name should not also allow changes to account ownership, tenant ID, pricing tier, approval state, role state, or audit metadata. Operation-specific models and explicit mapping make it clearer which properties can be changed by each workflow.\n\nNavigation properties and relationship changes need ownership review. Updating a relationship can be as sensitive as updating a scalar property. A caller should not be able to attach records to another tenant, move ownership, alter membership, or change a workflow relationship simply because an entity graph can represent that change.\n\nTracking behavior matters. Tracked queries are useful when the application intends to modify entities, but they also make updates easier to perform accidentally if broad object graphs are attached or mapped carelessly. No-tracking queries can reduce overhead and clarify read-only paths. The choice should match intent rather than habit.\n\nConcurrency tokens and conflict handling are practical defensive tools. They help the application notice when data changed between read and write, so updates do not silently overwrite important state. Conflict handling should be designed around the business workflow, user experience, and audit expectations.\n\nProtect server-owned fields deliberately. Ownership, tenant identifiers, approval state, role assignments, pricing, status transitions, audit fields, and security-relevant flags should be changed only through reviewed workflows. EF Core mapping is powerful, so the application must be explicit about which parts of the entity are caller-controlled and which are not.",
  "narrationPoints": [
    "API input models, view models, and command models describe what a caller or workflow is allowed to provide.",
    "A request to update a customer display name should not also allow changes to account ownership, tenant ID, pricing tier, approval state, role state, or audit metadata.",
    "A caller should not be able to attach records to another tenant, move ownership, alter membership, or change a workflow relationship simply because an entity graph can represent that change.",
    "Conflict handling should be designed around the business workflow, user experience, and audit expectations.",
    "EF Core mapping is powerful, so the application must be explicit about which parts of the entity are caller-controlled and which are not."
  ]
};
