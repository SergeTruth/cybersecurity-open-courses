window.COURSE_MODULE = {
  "title": "Sensitive Data, Logging, and Auditing",
  "graphicAlt": "Bullet summary graphic for Sensitive Data, Logging, and Auditing.",
  "narration": "Sensitive data should be identified before deciding how it is stored, queried, logged, or exposed. Personal data, financial records, health information, customer secrets, internal operational data, tokens, credentials, tenant identifiers, and administrative changes may all require different controls. EF Core is often close to that data, so its logging and query behavior deserve attention.\n\nAvoid sensitive values in EF logs, SQL command logs, exception messages, telemetry, screenshots, and diagnostics. Detailed logging may be useful during local development, but production logs should not become a second copy of sensitive records or secrets. Teams should know which EF Core logging options are active in each environment.\n\nAudit events should be meaningful. Authentication-related data changes, authorization failures, administrative updates, tenant boundary events, sensitive record access, role changes, ownership changes, and unusual data export activity may all deserve audit attention. Audit records should capture enough context to investigate without storing unnecessary sensitive values.\n\nCorrelation IDs or request identifiers help connect activity across layers. A single user action may pass through an API, service layer, EF Core query, database call, and background job. Correlation lets operators investigate without placing raw sensitive data into every log entry.\n\nLog access and retention are part of the security model. Logs can reveal operational details, user behavior, identifiers, and sensitive workflows even when they avoid raw secrets. Access should be limited, retention should be intentional, and exports or support bundles should be reviewed before sharing.\n\nDetection supports prevention, but it does not replace secure data access design. Logs can reveal unusual patterns, but the application still needs safe queries, authorization-aware data access, controlled updates, and least privilege. Good observability helps teams learn and respond without making the data layer more exposed.",
  "narrationPoints": [
    "EF Core is often close to that data, so its logging and query behavior deserve attention.",
    "Teams should know which EF Core logging options are active in each environment.",
    "Authentication-related data changes, authorization failures, administrative updates, tenant boundary events, sensitive record access, role changes, ownership changes, and unusual data export activity may all deserve audit attention.",
    "A single user action may pass through an API, service layer, EF Core query, database call, and background job.",
    "Logs can reveal unusual patterns, but the application still needs safe queries, authorization-aware data access, controlled updates, and least privilege."
  ]
};
