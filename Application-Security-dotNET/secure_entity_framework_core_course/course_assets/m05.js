window.COURSE_MODULE = {
  "title": "Authorization-Aware Data Access and Multi-Tenancy",
  "graphicAlt": "Bullet summary graphic for Authorization-Aware Data Access and Multi-Tenancy.",
  "narration": "Authentication tells the application who or what is calling. Authorization decides whether that identity may access a specific record, tenant, report, file, job, or administrative action. EF Core queries should be designed with authorization in mind, not treated as simple data retrieval followed by a later hope that the response is appropriate.\n\nObject-level authorization is critical in data access. A user may be allowed to open the orders feature, but not every order. A support user may view one tenant's records, but not another tenant's sensitive data. A background worker may update processing state, but not ownership or approval status. The query should include the relevant access rule whenever practical.\n\nTenant boundaries require careful enforcement. Tenant identifiers supplied by clients should not be blindly trusted. The application should derive tenant context from trusted identity, session, routing, or service context as appropriate, validate it, and apply it consistently. Mistakes in multi-tenant filtering can expose data even when individual query syntax is safe.\n\nGlobal query filters can be useful tools for tenant isolation or soft-delete behavior, but they still need review and testing. Teams should understand where filters apply, where they can be intentionally bypassed, how administrative paths work, and how background jobs establish tenant or scope context. A global filter is a tool, not a substitute for design review.\n\nService-layer authorization and repository or query patterns should preserve access rules. If one path uses an authorization-aware query and another path retrieves the same entity directly, the application may create a bypass around its normal controls. High-risk support and administrative workflows should be especially explicit and auditable.\n\nLeast privilege applies to users, service accounts, background workers, administrators, partner integrations, and database accounts. Broad permissions can make development easier, but they increase the impact of mistakes. The data access layer should make expected authority visible rather than hiding it behind generic fetch and save methods.",
  "narrationPoints": [
    "A support user may view one tenant's records, but not another tenant's sensitive data.",
    "The application should derive tenant context from trusted identity, session, routing, or service context as appropriate, validate it, and apply it consistently.",
    "Global query filters can be useful tools for tenant isolation or soft-delete behavior, but they still need review and testing.",
    "Service-layer authorization and repository or query patterns should preserve access rules.",
    "Least privilege applies to users, service accounts, background workers, administrators, partner integrations, and database accounts."
  ]
};
