window.COURSE_MODULE = {
  "title": "Query Safety with LINQ and Parameters",
  "graphicAlt": "Bullet summary graphic for Query Safety with LINQ and Parameters.",
  "narration": "For normal application queries, LINQ and provider-supported parameterized patterns are the safer default. EF Core can translate expressions and bound values into database operations while keeping variable values conceptually separate from the database command structure. This reduces the risk created by manually assembling query text from request values.\n\nSafe APIs are strongest when query structure remains under developer control. A caller may provide a customer ID, a tenant ID, an order status, a date range, or a supported filter option. The application should decide how those values are represented in the query. The caller should not provide arbitrary field names, expressions, sort fragments, or query fragments.\n\nValidation still matters. IDs, statuses, ranges, enum-like values, page sizes, and filter choices should be checked for the current business purpose before they influence behavior. A parameterized value can still be inappropriate. For example, a valid status string may not be valid for the current workflow, and a valid tenant identifier may not belong to the current user.\n\nProjection and data shaping are security and privacy controls. Returning full entities from a query can expose fields the caller does not need, such as audit values, internal flags, relationship details, or operational metadata. Use query shapes and response models that return only the records and fields the caller is allowed to see.\n\nPerformance-driven query changes deserve security review. Optimizing a slow path may introduce broader includes, raw SQL, caching, denormalized projections, or different filtering behavior. These changes can be appropriate, but they should preserve authorization, tenant boundaries, data minimization, and safe parameter handling.\n\nConsistency makes query safety maintainable. Teams should document how to express common filters, sorts, projections, paging, and date ranges. When safe patterns are familiar and easy to find, developers are less likely to invent one-off query construction under schedule pressure.",
  "narrationPoints": [
    "EF Core can translate expressions and bound values into database operations while keeping variable values conceptually separate from the database command structure.",
    "The caller should not provide arbitrary field names, expressions, sort fragments, or query fragments.",
    "Use query shapes and response models that return only the records and fields the caller is allowed to see.",
    "Optimizing a slow path may introduce broader includes, raw SQL, caching, denormalized projections, or different filtering behavior.",
    "Teams should document how to express common filters, sorts, projections, paging, and date ranges."
  ]
};
