window.COURSE_MODULE = {
  "title": "Performance, Resource Limits, and Operational Resilience",
  "graphicAlt": "Bullet summary graphic for Performance, Resource Limits, and Operational Resilience.",
  "narration": "Performance and security are connected in data-heavy applications. A query that reads too broadly, loads too many related entities, exports too much data, or runs without limits can create availability and privacy problems even when it is parameterized. Bounded data access is part of secure design.\n\nUse pagination, filtered queries, reasonable date ranges, and bounded result sets. These controls reduce accidental full-table reads, limit exposure, and protect shared database capacity. They also make application behavior more predictable for users, operators, and downstream systems.\n\nBe careful with includes, lazy loading, and relationship traversal. Loading related data can be necessary, but it can also create unexpected data access, performance spikes, or accidental exposure. Developers should understand what each query loads and whether the caller is authorized to receive that data.\n\nUnbounded exports, reports, and administrative views deserve review. These workflows may be legitimate, but they often touch broad data and run under privileged contexts. They should have clear filters, resource limits, authorization checks, audit records, and operational expectations.\n\nTimeouts, cancellation tokens, retries, and circuit-breaking behavior should be designed deliberately. Retrying database operations without understanding failure mode can increase load. Ignoring cancellation can waste resources after a caller disconnects. Timeouts and cancellation help the application fail predictably rather than slowly consuming shared capacity.\n\nBackground jobs and batch operations should follow the same data access rules as request paths. They need tenant context, authorization assumptions, resource limits, logging, and safe failure handling. Operational resilience means failures are observable, recoverable, and safe for data integrity and availability.",
  "narrationPoints": [
    "Bounded data access is part of secure design.",
    "Use pagination, filtered queries, reasonable date ranges, and bounded result sets.",
    "Developers should understand what each query loads and whether the caller is authorized to receive that data.",
    "Retrying database operations without understanding failure mode can increase load.",
    "Operational resilience means failures are observable, recoverable, and safe for data integrity and availability."
  ]
};
