window.COURSE_MODULE = {
  "title": "EF Core Security Foundations",
  "graphicAlt": "Bullet summary graphic for EF Core Security Foundations.",
  "narration": "Entity Framework Core is a powerful data access layer, but it is not a complete security boundary by itself. It can help developers express queries, map entities, track changes, and use safer provider-supported query patterns. It does not automatically decide who may read a record, which tenant owns it, which fields can be changed, or whether a workflow should be allowed.\n\nEF Core reduces some query-construction risk when developers stay inside supported LINQ and parameterized patterns. That is valuable, but secure data access still requires authorization, input validation, data shaping, least privilege, safe logging, careful migrations, dependency maintenance, and review of high-risk paths. The framework is a tool inside a larger design.\n\nTrust boundaries appear throughout the EF Core lifecycle. Controllers, APIs, background jobs, domain services, repositories, DbContext instances, database providers, administrators, reporting tools, and migration processes each make different assumptions. When data or identity crosses those boundaries, the application should be clear about what is trusted, what is validated, and what rule is being enforced.\n\nA safe framework default is not the same thing as a safe application design. A query may be parameterized and still return too much data. A tracked entity may be valid and still allow an unintended field change. A migration may be technically correct and still introduce a risky permission or destructive operation. Security review has to look at intent, not only syntax.\n\nSecure EF Core usage is normal engineering. Teams should design data access that is understandable, reviewable, testable, and operable. The goal is not to avoid EF Core features. The goal is to use them deliberately so database behavior supports the same access rules, privacy expectations, and operational safeguards as the rest of the application.",
  "narrationPoints": [
    "Entity Framework Core is a powerful data access layer, but it is not a complete security boundary by itself.",
    "EF Core reduces some query-construction risk when developers stay inside supported LINQ and parameterized patterns.",
    "When data or identity crosses those boundaries, the application should be clear about what is trusted, what is validated, and what rule is being enforced.",
    "A safe framework default is not the same thing as a safe application design.",
    "Teams should design data access that is understandable, reviewable, testable, and operable."
  ]
};
