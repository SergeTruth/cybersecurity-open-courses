window.COURSE_MODULE = {
  "title": "Secrets, Connection Strings, and Key Material",
  "graphicAlt": "Bullet summary graphic for Secrets, Connection Strings, and Key Material.",
  "narration": "Secrets are high-risk configuration values. They include database credentials, API keys, certificates, private keys, signing keys, encryption keys, service tokens, storage credentials, webhook secrets, and connection strings. Even harmless placeholder names such as ExampleApiKey or ExampleConnectionString should remind teams to keep real values out of ordinary code paths.\n\nSecret values should not be stored in source code, appsettings files, build logs, crash dumps, screenshots, support bundles, or ordinary telemetry. These locations are copied, searched, retained, and shared in ways that make accidental disclosure hard to contain. Secret handling should assume that convenience copies will eventually escape their original context.\n\nUse supported secret-management mechanisms, managed identities, deployment secrets, platform secret stores, or external secret providers where appropriate. The best choice depends on the hosting model, but the principle is consistent: the application should receive only the access it needs, through a channel designed for sensitive values.\n\nSeparate secret references from secret values. A configuration value may safely name ExampleCertificateName or ExampleStorageAccount when it is only a reference, while the actual key material remains in a protected store. This distinction helps teams document configuration without publishing the secret itself.\n\nSecret rotation, ownership, access control, and emergency revocation should be planned before they are needed. Teams should know who owns each secret, which applications use it, which identities can read it, how it rotates, how it can be revoked, and how to recover if a value is exposed.\n\nTreat connection strings as sensitive even when they do not visibly contain a password. They can reveal internal names, provider details, account relationships, or trust assumptions. In many environments they also enable access through managed identity or integrated authentication, so they still deserve protection and review.",
  "narrationPoints": [
    "Secret values should not be stored in source code, appsettings files, build logs, crash dumps, screenshots, support bundles, or ordinary telemetry.",
    "Use supported secret-management mechanisms, managed identities, deployment secrets, platform secret stores, or external secret providers where appropriate.",
    "A configuration value may safely name ExampleCertificateName or ExampleStorageAccount when it is only a reference, while the actual key material remains in a protected store.",
    "Secret rotation, ownership, access control, and emergency revocation should be planned before they are needed.",
    "Treat connection strings as sensitive even when they do not visibly contain a password."
  ]
};
