window.COURSE_MODULE = {
  "title": "Logging, Diagnostics, and Configuration Exposure",
  "graphicAlt": "Bullet summary graphic for Logging, Diagnostics, and Configuration Exposure.",
  "narration": "Configuration values should not appear in user-facing errors, logs, telemetry, traces, health checks, crash reports, screenshots, or support bundles when those values are sensitive. This includes connection strings, tokens, headers, keys, certificate material, provider-specific secrets, internal paths, and sometimes database names or account identifiers.\n\nDiagnostics should separate user visibility from operator visibility. Users need safe error messages and support references. Authorized operators may need more detail, but that detail should be protected, redacted, retained appropriately, and limited to people with a real operational need.\n\nStructured logging and redaction can help. Logs can capture setting categories, configuration source names, validation outcomes, correlation IDs, and safe state transitions without printing secret values. Avoid logging entire configuration objects because safe and sensitive values often sit next to each other.\n\nHealth checks and status pages are useful, but they can expose too much. A health endpoint may reveal provider names, dependency status, internal routes, storage names, or environment details. Decide what is safe for public, internal, and operator-only audiences, and configure each endpoint accordingly.\n\nDevelopment and production diagnostics should differ. Detailed local errors can help developers. Production diagnostics should prioritize safe user messages, protected logs, monitoring signals, and correlation identifiers. Copying development diagnostic behavior into production creates avoidable exposure.\n\nCorrelation IDs support investigation without leaking secrets. They let support teams and operators connect user reports to protected logs while keeping sensitive configuration values out of user-visible channels. Good diagnostics are useful because they are bounded and safe.",
  "narrationPoints": [
    "Configuration values should not appear in user-facing errors, logs, telemetry, traces, health checks, crash reports, screenshots, or support bundles when those values are sensitive.",
    "Users need safe error messages and support references.",
    "Avoid logging entire configuration objects because safe and sensitive values often sit next to each other.",
    "A health endpoint may reveal provider names, dependency status, internal routes, storage names, or environment details.",
    "Production diagnostics should prioritize safe user messages, protected logs, monitoring signals, and correlation identifiers."
  ]
};
