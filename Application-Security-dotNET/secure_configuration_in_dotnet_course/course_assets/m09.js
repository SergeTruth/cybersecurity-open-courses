window.COURSE_MODULE = {
  "title": "Cloud, Container, CI/CD, and Operational Configuration",
  "graphicAlt": "Bullet summary graphic for Cloud, Container, CI/CD, and Operational Configuration.",
  "narration": "Modern .NET applications often receive configuration from containers, orchestrators, cloud platforms, serverless settings, managed services, external configuration stores, and CI/CD systems. These layers are powerful, but they also expand the places where security-sensitive behavior can change.\n\nEnvironment variables are useful, but they are not automatically secret-safe. They may be visible through platform tools, process inspection, logs, crash dumps, deployment history, or support exports depending on the environment. Use them intentionally and prefer platform secret mechanisms or managed identities when they better match the risk.\n\nCloud secret stores, managed identities, workload identities, and platform configuration services can reduce direct secret handling when configured carefully. The goal is not merely to move values somewhere else. The goal is to control access, audit use, support rotation, and keep runtime identities narrowly scoped.\n\nCI/CD systems carry deployment credentials, service connections, signing material, storage credentials, package tokens, and environment variables. Those values should be protected with least privilege, approval workflows, branch protections, environment separation, and access review. A pipeline identity should not have broader rights than the deployment requires.\n\nConfiguration review belongs in pull requests, release approvals, infrastructure changes, and incident response. A code change can be safe but paired with risky configuration. An infrastructure update can change runtime behavior without touching application code. Review should include both intended values and where they will be applied.\n\nDetect drift between intended and effective settings. Backups, rollback plans, rotation history, change history, and monitoring help teams understand how configuration changed over time. Secure configuration is maintained continuously, not only at initial deployment.",
  "narrationPoints": [
    "Use them intentionally and prefer platform secret mechanisms or managed identities when they better match the risk.",
    "Cloud secret stores, managed identities, workload identities, and platform configuration services can reduce direct secret handling when configured carefully.",
    "CI/CD systems carry deployment credentials, service connections, signing material, storage credentials, package tokens, and environment variables.",
    "Configuration review belongs in pull requests, release approvals, infrastructure changes, and incident response.",
    "Backups, rollback plans, rotation history, change history, and monitoring help teams understand how configuration changed over time."
  ]
};
