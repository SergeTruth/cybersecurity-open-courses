window.COURSE_MODULE = {
  "title": "Secure Defaults, Feature Flags, and Risky Toggles",
  "graphicAlt": "Bullet summary graphic for Secure Defaults, Feature Flags, and Risky Toggles.",
  "narration": "Some configuration values directly change security behavior. Debug mode, detailed errors, permissive CORS, relaxed authentication, disabled authorization checks, cookie settings, TLS assumptions, broad logging, health detail, test integrations, and experimental features can all change the effective risk of the application.\n\nFeature flags are operational controls, but they still require security review. A flag can change who sees a feature, which integration is used, whether a workflow is enforced, or how much data is logged. Treat security-relevant flags as controlled configuration, not as harmless product switches.\n\nRisky toggles need ownership, expiration, and review. If a temporary setting is enabled to respond to an incident or unblock testing, the team should know who approved it, why it exists, when it expires, and what must happen before it becomes permanent. Otherwise emergency settings quietly become long-term posture.\n\nSafe defaults are especially important for new environments and new deployments. A freshly created staging slot, container environment, or cloud app should not begin life with permissive settings simply because no one has configured it yet. Defaults should fail closed or choose conservative behavior whenever practical.\n\nAvoid configuration that silently weakens security under failure conditions. If an identity provider is unavailable, an authorization service cannot be reached, or a feature flag provider times out, the application should not automatically enable broad access. Failure behavior should be explicit and reviewed.\n\nDocument approved values for security-sensitive settings. Developers and operators should know which values are allowed, which are development-only, which require production approval, and which should trigger investigation. Clear allowed states reduce accidental drift.",
  "narrationPoints": [
    "Debug mode, detailed errors, permissive CORS, relaxed authentication, disabled authorization checks, cookie settings, TLS assumptions, broad logging, health detail, test integrations, and experimental features can all change the effective risk of the application.",
    "Feature flags are operational controls, but they still require security review.",
    "A freshly created staging slot, container environment, or cloud app should not begin life with permissive settings simply because no one has configured it yet.",
    "Avoid configuration that silently weakens security under failure conditions.",
    "Developers and operators should know which values are allowed, which are development-only, which require production approval, and which should trigger investigation."
  ]
};
