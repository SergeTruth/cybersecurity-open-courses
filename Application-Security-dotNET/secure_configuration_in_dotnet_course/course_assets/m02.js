window.COURSE_MODULE = {
  "title": "Configuration Sources and Precedence",
  "graphicAlt": "Bullet summary graphic for Configuration Sources and Precedence.",
  "narration": ".NET applications can read configuration from appsettings files, environment-specific files, environment variables, command-line arguments, developer user secrets, external providers, cloud configuration stores, container settings, and platform services. That flexibility is useful, but it means a setting may not come from the file a reviewer first opens.\n\nSource order matters because later sources can override earlier values. A value in an environment variable may replace a value from an appsettings file. A deployment setting may override a default. A command-line argument may change runtime behavior. For security-sensitive settings, precedence can decide which policy is actually active.\n\nPrecedence matters for authentication, authorization, CORS, logging, Data Protection, connection strings, diagnostic behavior, cookie settings, and feature flags. A safe value in source control is not enough if a later source can replace it with a weaker value during deployment or operations.\n\nTeams should review effective configuration, not only individual files. The important question is what the running application sees after all providers are applied. That may require startup validation, deployment review, documented provider order, and operational visibility into approved settings without exposing secret values.\n\nLocal developer convenience should not become a production assumption. A local ExampleApiKey placeholder, verbose logging setting, relaxed endpoint, or mock service URL may help development, but it should be clearly separated from production behavior and should fail safely if accidentally promoted.\n\nDocumentation helps developers and operators share the same mental model. Important settings should have a clear owner, expected source, allowed values, security impact, and review path. Hidden assumptions about where a value came from are a common cause of configuration drift.",
  "narrationPoints": [
    ".NET applications can read configuration from appsettings files, environment-specific files, environment variables, command-line arguments, developer user secrets, external providers, cloud configuration stores, container settings, and platform services.",
    "Precedence matters for authentication, authorization, CORS, logging, Data Protection, connection strings, diagnostic behavior, cookie settings, and feature flags.",
    "Teams should review effective configuration, not only individual files.",
    "A local ExampleApiKey placeholder, verbose logging setting, relaxed endpoint, or mock service URL may help development, but it should be clearly separated from production behavior and should fail safely if accidentally promoted.",
    "Important settings should have a clear owner, expected source, allowed values, security impact, and review path."
  ]
};
