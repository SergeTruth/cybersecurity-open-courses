window.COURSE_MODULE = {
  "title": "Hosting, Middleware, and Runtime Settings",
  "graphicAlt": "Bullet summary graphic for Hosting, Middleware, and Runtime Settings.",
  "narration": "Hosting configuration is part of application security. A .NET application may run locally, behind IIS, on Kestrel, in containers, behind a reverse proxy, in a cloud app service, or in a serverless platform. Each hosting model has settings that affect public URLs, transport behavior, headers, request handling, and diagnostics.\n\nHTTPS expectations, reverse proxy awareness, forwarded headers, and public URL assumptions should be reviewed defensively. If the application misunderstands the original scheme, host, or client context, it may generate incorrect redirects, apply cookie settings incorrectly, log misleading information, or make access decisions from the wrong inputs.\n\nMiddleware configuration influences authentication, authorization, routing, sessions, cookies, request limits, error handling, CORS, static files, and health endpoints. The same code can behave differently when middleware options or order change. Startup and hosting configuration should be reviewed as security-relevant code.\n\nRequest size limits, upload limits, timeout settings, and resource-protection values help keep the application reliable. They prevent accidental or excessive requests from consuming memory, disk, threads, or downstream service capacity. These settings should match endpoint purpose and environment capacity.\n\nRuntime differences matter. A setting that is safe under local Kestrel may behave differently behind a proxy or in a container. A static file path, diagnostic route, or health endpoint may be reachable in one environment and hidden in another. Release review should verify the deployed runtime behavior, not just local assumptions.\n\nDevelopment middleware, debug endpoints, test routes, and diagnostic pages should not be exposed in production. If production operators need diagnostics, provide controlled, authorized, and logged mechanisms rather than publishing development tools to real users.",
  "narrationPoints": [
    "Each hosting model has settings that affect public URLs, transport behavior, headers, request handling, and diagnostics.",
    "HTTPS expectations, reverse proxy awareness, forwarded headers, and public URL assumptions should be reviewed defensively.",
    "Middleware configuration influences authentication, authorization, routing, sessions, cookies, request limits, error handling, CORS, static files, and health endpoints.",
    "Request size limits, upload limits, timeout settings, and resource-protection values help keep the application reliable.",
    "Development middleware, debug endpoints, test routes, and diagnostic pages should not be exposed in production."
  ]
};
