window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples turn 'Registration, Login, and Verification Flows' into concrete defensive .NET review patterns.",
  "codeExamples": [
    {
      "title": "Perform password verification work for unknown accounts",
      "language": "csharp",
      "blurb": "The login verifier validates bounded inputs, uses one immutable dummy record for missing accounts, performs the same password-hash API call, and returns only a generic accepted or rejected result.",
      "code": "using Microsoft.AspNetCore.Identity;\n\npublic sealed record LoginAccount(\n    string Id,\n    string PasswordHash,\n    bool Disabled);\n\npublic interface ILoginAccountStore\n{\n    Task<LoginAccount?> FindAsync(\n        string login,\n        CancellationToken cancellationToken);\n}\n\npublic sealed class LoginVerifier\n{\n    private readonly ILoginAccountStore _store;\n    private readonly IPasswordHasher<LoginAccount> _hasher;\n    private readonly LoginAccount _dummy;\n\n    public LoginVerifier(\n        ILoginAccountStore store,\n        IPasswordHasher<LoginAccount> hasher)\n    {\n        _store = store ?? throw new ArgumentNullException(nameof(store));\n        _hasher = hasher ?? throw new ArgumentNullException(nameof(hasher));\n        var seed = new LoginAccount(\"dummy-account\", \"\", true);\n        _dummy = seed with\n        {\n            PasswordHash = _hasher.HashPassword(seed, \"dummy-password-value\")\n        };\n    }\n\n    public async Task<bool> VerifyAsync(\n        string login,\n        string password,\n        CancellationToken cancellationToken)\n    {\n        if (!IsLogin(login))\n            throw new ArgumentException(\"Login identifier rejected.\", nameof(login));\n        ArgumentNullException.ThrowIfNull(password);\n        if (password.Length is 0 or > 1024)\n            throw new ArgumentException(\"Password length rejected.\", nameof(password));\n\n        var found = await _store.FindAsync(login, cancellationToken).ConfigureAwait(false);\n        var usableFound = found is not null &&\n            string.Equals(found.Id, login, StringComparison.Ordinal) &&\n            !string.IsNullOrWhiteSpace(found.PasswordHash) &&\n            found.PasswordHash.Length <= 2048;\n        var account = usableFound ? found! : _dummy;\n        var result = _hasher.VerifyHashedPassword(\n            account,\n            account.PasswordHash,\n            password);\n        return usableFound && !account.Disabled &&\n               result != PasswordVerificationResult.Failed;\n    }\n\n    private static bool IsLogin(string value) =>\n        !string.IsNullOrEmpty(value) &&\n        value.Length is >= 3 and <= 128 &&\n        string.Equals(value, value.Trim(), StringComparison.Ordinal) &&\n        !value.Any(char.IsControl);\n}\n"
    },
    {
      "title": "Validate registration inputs without retaining a password DTO",
      "language": "csharp",
      "blurb": "The validator requires a bounded canonical email identity, evaluates the password without trimming or normalization in caller-owned span storage, and returns no object that retains the submitted password.",
      "code": "using System.Net.Mail;\n\npublic static class RegistrationInputPolicy\n{\n    public static bool IsAcceptable(\n        string email,\n        ReadOnlySpan<char> password)\n    {\n        if (string.IsNullOrWhiteSpace(email) ||\n            email.Length > 254 ||\n            !string.Equals(email, email.Trim(), StringComparison.Ordinal) ||\n            email.Any(char.IsControl) ||\n            !MailAddress.TryCreate(email, out var address) ||\n            !string.Equals(address.Address, email, StringComparison.OrdinalIgnoreCase))\n        {\n            return false;\n        }\n\n        if (password.Length is < 12 or > 1024) return false;\n        foreach (var character in password)\n            if (char.IsControl(character)) return false;\n        return true;\n    }\n}\n"
    }
  ]
};
