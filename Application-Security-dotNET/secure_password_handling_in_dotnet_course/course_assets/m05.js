window.COURSE_MODULE = {
  "title": "Password Reset, Change, and Account Recovery",
  "graphicAlt": "Bullet summary graphic for Password Reset, Change, and Account Recovery.",
  "narration": "Password reset is a high-risk account recovery workflow because it can transfer control of an account when handled poorly. A reset flow is not just a convenience feature. It is an alternative path into the account, and it should be designed with the same care as primary authentication.\n\nReset tokens should be time-limited, purpose-specific, protected, and one-time use where possible. A token created for password reset should not be reusable for other account actions, should expire within a defined window, and should become invalid after successful use. The application should also handle repeated requests predictably so users receive help without creating unnecessary operational noise.\n\nReset links and tokens need logging and analytics review. Tokens can appear in URLs, emails, browser history, support screenshots, analytics tools, reverse proxy logs, and error reports. Those systems should redact or avoid sensitive values. A reset token should never become a long-lived artifact in observability or support tooling.\n\nPassword change by an authenticated user and password reset through recovery are different workflows. A signed-in user changing a known password may require current password verification and session review. A recovery flow may require additional notification, token validation, and stronger audit evidence. Treating the two paths separately makes policy decisions easier to test and explain.\n\nAfter a password change or reset, review existing sessions, remembered devices, recovery codes, MFA state, and user notifications. Some applications invalidate existing sessions. Others apply risk-based handling. User notification can help detect unwanted recovery activity, and support-assisted recovery should have clear identity verification and audit expectations.",
  "narrationPoints": [
    "Password reset is a high-risk account recovery workflow because it can transfer control of an account when handled poorly.",
    "A token created for password reset should not be reusable for other account actions, should expire within a defined window, and should become invalid after successful use.",
    "A reset token should never become a long-lived artifact in observability or support tooling.",
    "Password change by an authenticated user and password reset through recovery are different workflows.",
    "After a password change or reset, review existing sessions, remembered devices, recovery codes, MFA state, and user notifications."
  ]
};
