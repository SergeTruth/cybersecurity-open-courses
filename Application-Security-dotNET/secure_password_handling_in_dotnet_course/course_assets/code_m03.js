window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples turn 'Password Hashing in .NET and ASP.NET Core Identity' into concrete defensive .NET review patterns.",
  "codeExamples": [
    {
      "title": "Hash and verify passwords through ASP.NET Core Identity",
      "language": "csharp",
      "blurb": "The wrapper validates bounded account and password inputs, delegates versioned format and work-factor decisions to IPasswordHasher, and preserves the rehash-needed result.",
      "code": "using Microsoft.AspNetCore.Identity;\n\npublic sealed record PasswordUser(string Id);\n\npublic readonly record struct PasswordVerification(\n    bool Accepted,\n    bool RehashRequired);\n\npublic sealed class IdentityPasswordService\n{\n    private readonly IPasswordHasher<PasswordUser> _hasher;\n\n    public IdentityPasswordService(IPasswordHasher<PasswordUser> hasher) =>\n        _hasher = hasher ?? throw new ArgumentNullException(nameof(hasher));\n\n    public string Hash(PasswordUser user, string password)\n    {\n        Validate(user, password);\n        return _hasher.HashPassword(user, password);\n    }\n\n    public PasswordVerification Verify(\n        PasswordUser user,\n        string encodedHash,\n        string password)\n    {\n        Validate(user, password);\n        if (string.IsNullOrWhiteSpace(encodedHash) || encodedHash.Length > 2048)\n            return new(false, false);\n\n        return _hasher.VerifyHashedPassword(user, encodedHash, password) switch\n        {\n            PasswordVerificationResult.Success => new(true, false),\n            PasswordVerificationResult.SuccessRehashNeeded => new(true, true),\n            _ => new(false, false)\n        };\n    }\n\n    private static void Validate(PasswordUser user, string password)\n    {\n        ArgumentNullException.ThrowIfNull(user);\n        ArgumentNullException.ThrowIfNull(password);\n        if (!Identifier.IsValid(user.Id))\n            throw new ArgumentException(\"User identifier rejected.\", nameof(user));\n        if (password.Length is 0 or > 1024)\n            throw new ArgumentException(\"Password length rejected.\", nameof(password));\n    }\n\n    private static class Identifier\n    {\n        public static bool IsValid(string value) =>\n            !string.IsNullOrEmpty(value) &&\n            value.Length <= 64 &&\n            value.All(character =>\n                character is >= 'A' and <= 'Z' or\n                             >= 'a' and <= 'z' or\n                             >= '0' and <= '9' or '-' or '_');\n    }\n}\n"
    },
    {
      "title": "Apply a bounded password policy without trimming or normalizing secrets",
      "language": "csharp",
      "blurb": "The policy evaluates the submitted sequence as-is, permits Unicode, enforces application-owned length limits, rejects control characters, and requires multiple character categories.",
      "code": "public static class RegistrationPasswordPolicy\n{\n    public static bool IsAcceptable(ReadOnlySpan<char> password)\n    {\n        if (password.Length is < 12 or > 1024) return false;\n\n        var lower = false;\n        var upper = false;\n        var digit = false;\n        var other = false;\n        foreach (var character in password)\n        {\n            if (char.IsControl(character)) return false;\n            lower |= char.IsLower(character);\n            upper |= char.IsUpper(character);\n            digit |= char.IsDigit(character);\n            other |= !char.IsLetterOrDigit(character) && !char.IsWhiteSpace(character);\n        }\n\n        var categories = (lower ? 1 : 0) + (upper ? 1 : 0) +\n                         (digit ? 1 : 0) + (other ? 1 : 0);\n        return categories >= 3;\n    }\n}\n"
    }
  ]
};
