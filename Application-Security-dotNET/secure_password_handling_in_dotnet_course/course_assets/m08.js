window.COURSE_MODULE = {
  "title": "Protecting Passwords from Operational Exposure",
  "graphicAlt": "Bullet summary graphic for Protecting Passwords from Operational Exposure.",
  "narration": "Correct password hashing does not prevent operational exposure. Passwords and password-equivalent secrets can leak before hashing, during reset, through diagnostics, in support workflows, or from systems around the identity application. Those paths deserve review even when the database stores only password hashes.\n\nNever log plaintext passwords, password reset tokens, recovery codes, MFA secrets, raw request bodies from login forms, or credential-bearing headers. Review telemetry, exception reports, traces, support screenshots, analytics, crash dumps, request replay tools, browser storage, and test artifacts. If a tool captures requests or errors, decide how sensitive fields are suppressed before production use.\n\nDebug and development behavior should not silently become production behavior. Verbose error pages, request dumps, local logging habits, seeded accounts, and test shortcuts can become real exposure paths when copied into deployment scripts or shared environments. Production should have explicit redaction, least privilege, and monitoring expectations.\n\nSecrets used by the identity system also matter. Connection strings, signing keys, token-protection keys, email provider credentials, and data-protection keys should be stored outside source control and managed through approved secret storage. Access to identity databases, backups, logs, and admin tools should be limited and audited.\n\nTest data should not include real user passwords or realistic reused secrets. Support procedures should avoid asking users to disclose passwords. The safest password handling culture is one where teams do not need to see, copy, or store password material to operate the application.",
  "narrationPoints": [
    "Passwords and password-equivalent secrets can leak before hashing, during reset, through diagnostics, in support workflows, or from systems around the identity application.",
    "Never log plaintext passwords, password reset tokens, recovery codes, MFA secrets, raw request bodies from login forms, or credential-bearing headers.",
    "Verbose error pages, request dumps, local logging habits, seeded accounts, and test shortcuts can become real exposure paths when copied into deployment scripts or shared environments.",
    "Access to identity databases, backups, logs, and admin tools should be limited and audited.",
    "Test data should not include real user passwords or realistic reused secrets."
  ]
};
