window.COURSE_MODULE = {
  "title": "Registration, Login, and Verification Flows",
  "graphicAlt": "Bullet summary graphic for Registration, Login, and Verification Flows.",
  "narration": "Registration and login flows should use protected transport and deliberate server-side handling. The password entry screen is only the first boundary. The application should ensure that credentials are submitted to the intended endpoint, over HTTPS, and into a flow that avoids accidental logging, diagnostic capture, or unnecessary exposure in client-side behavior.\n\nIdentifiers such as email addresses and usernames should be normalized and validated consistently. The application should decide how casing, whitespace, international characters, aliases, and uniqueness are handled. That decision affects account lookup, duplicate prevention, audit logs, support workflows, and user experience. It should not leak more account state than needed.\n\nLogin failure messages should be predictable. A user needs a clear result, but the application should avoid unnecessary disclosure about whether a specific account exists, whether the password was wrong, whether the email is unconfirmed, or whether an account is disabled. Account status, email confirmation, disabled accounts, and locked accounts should be handled deliberately in the application design.\n\nPassword verification should use the configured password hasher, not direct string comparison and not a separate ad hoc verification routine. The sign-in path should establish the intended authentication session after successful verification without retaining password material in memory longer than necessary, copying it into logs, or passing it to unrelated components.\n\nFailed sign-ins should be rate-limited, logged safely, and reviewed for abuse patterns. The logs should support monitoring and incident response without storing passwords or excessive personal data. Successful and failed authentication flows should both be observable enough for operations while preserving the confidentiality of password material.",
  "narrationPoints": [
    "Registration and login flows should use protected transport and deliberate server-side handling.",
    "Identifiers such as email addresses and usernames should be normalized and validated consistently.",
    "Account status, email confirmation, disabled accounts, and locked accounts should be handled deliberately in the application design.",
    "Password verification should use the configured password hasher, not direct string comparison and not a separate ad hoc verification routine.",
    "The logs should support monitoring and incident response without storing passwords or excessive personal data."
  ]
};
