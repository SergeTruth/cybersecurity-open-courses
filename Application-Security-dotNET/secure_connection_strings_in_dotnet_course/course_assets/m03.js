window.COURSE_MODULE = {
  "title": "Development Secrets and Local Workflows",
  "graphicAlt": "Bullet summary graphic for Development Secrets and Local Workflows.",
  "narration": "Development workflows need convenience without normalizing unsafe secret handling. Developers need to run the app locally, test database behavior, and debug configuration. The safe pattern is to support local productivity while keeping sensitive values out of source code, shared samples, and long-lived support artifacts.\n\nASP.NET Core Secret Manager is for development only. It helps keep local secrets out of project files, but it should not be treated as encrypted production secret storage. It is a developer convenience for local workflows, not a production vault or lifecycle management system.\n\nEnvironment variables can also keep local secrets out of source code, but local machines still need protection. Workstations can have terminal history, shell profiles, local logs, copied command output, screenshots, and support chats. Local values should be scoped to local or development resources whenever possible.\n\nLocal database accounts should have limited privileges and should not reuse production credentials. A developer should not need production database access to run ordinary local tooling. When production access is necessary for a narrow operational task, it should follow a separate approved process.\n\nappsettings.Development.json should not contain shared sensitive values that could be committed, copied into examples, or reused across teams. If a file must show the shape of a connection setting, use placeholders and documentation that points developers to the correct local secret mechanism.\n\n.gitignore, sample files, onboarding docs, support runbooks, and local setup scripts should be reviewed as part of the workflow. Many secret exposures happen outside the main application code path, through the supporting material that helps people run the app.",
  "narrationPoints": [
    "The safe pattern is to support local productivity while keeping sensitive values out of source code, shared samples, and long-lived support artifacts.",
    "ASP.NET Core Secret Manager is for development only.",
    "Environment variables can also keep local secrets out of source code, but local machines still need protection.",
    "Local database accounts should have limited privileges and should not reuse production credentials.",
    ".gitignore, sample files, onboarding docs, support runbooks, and local setup scripts should be reviewed as part of the workflow."
  ]
};
