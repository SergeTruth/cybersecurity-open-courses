window.COURSE_MODULE = {
  "title": "EF Core, Migrations, and Runtime Usage",
  "graphicAlt": "Bullet summary graphic for EF Core, Migrations, and Runtime Usage.",
  "narration": "EF Core applications often use connection strings at design time, migration time, seeding time, and runtime. These contexts are related, but they do not always need the same identity or privilege level. Treating them as one connection string can make development easier but production review weaker.\n\nDesign-time tooling should not require production credentials on developer machines. Developers need safe ways to scaffold, test, and run migrations locally without gaining unnecessary access to production databases. Local development should use local or development resources whenever possible.\n\nMigration credentials may need more privilege than runtime application credentials, especially when schema changes are involved. That privilege should be handled deliberately. A deployment pipeline might run migrations with a controlled identity, while the application runtime identity remains narrower.\n\nAutomated deployment pipelines should protect migration connection strings and avoid printing them. Migration output, logs, command arguments, build summaries, environment dumps, and failure reports should be reviewed for secret exposure. Pipeline permissions should be limited to the environments they deploy.\n\nRuntime configuration should be explicit enough that the app connects to the intended environment. Test, staging, and production targets should be hard to confuse through naming, deployment approvals, platform configuration, and safety checks. A misdirected migration can be more damaging than a failed deployment.\n\nSeed data and database initialization should not embed secrets or sensitive connection details. Initialization logic should also be reviewed for environment assumptions, logging behavior, and whether it is appropriate to run automatically in production.",
  "narrationPoints": [
    "EF Core applications often use connection strings at design time, migration time, seeding time, and runtime.",
    "A deployment pipeline might run migrations with a controlled identity, while the application runtime identity remains narrower.",
    "Automated deployment pipelines should protect migration connection strings and avoid printing them.",
    "Test, staging, and production targets should be hard to confuse through naming, deployment approvals, platform configuration, and safety checks.",
    "Seed data and database initialization should not embed secrets or sensitive connection details."
  ]
};
