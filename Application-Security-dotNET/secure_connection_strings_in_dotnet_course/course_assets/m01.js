window.COURSE_MODULE = {
  "title": "Connection Strings as Sensitive Configuration",
  "graphicAlt": "Bullet summary graphic for Connection Strings as Sensitive Configuration.",
  "narration": "Connection strings are sensitive configuration, not harmless text. They often describe where an application connects, how it authenticates, which database or service it uses, and which provider options shape runtime behavior. Even when a connection string does not contain a password, it may still reveal useful internal architecture and operational context.\n\nA connection string may include database hosts, ports, service endpoints, database names, usernames, passwords, tokens, tenant context, integrated authentication settings, encryption settings, timeout settings, pooling behavior, or application labels. That mix of identity, location, and behavior makes the value worth protecting.\n\nA leaked connection string can create data, cost, availability, compliance, and operational risk. The direct impact depends on the permissions behind it, the network path, the authentication method, and the systems that trust it. A read-only reporting connection is different from a migration connection, but both still deserve lifecycle ownership.\n\nSecurity depends on how the connection string is created, stored, transmitted, logged, used, rotated, and revoked. Putting a value into configuration does not make it safe. The application, deployment platform, database, logging system, support process, and incident process all influence the real level of protection.\n\nConnection strings should be inventoried and owned like other secrets. Teams should know what each sensitive connection string is for, who owns it, which environment it belongs to, what privileges it grants, where it is stored, how it is rotated, and what response path applies if it is exposed.",
  "narrationPoints": [
    "Even when a connection string does not contain a password, it may still reveal useful internal architecture and operational context.",
    "A connection string may include database hosts, ports, service endpoints, database names, usernames, passwords, tokens, tenant context, integrated authentication settings, encryption settings, timeout settings, pooling behavior, or application labels.",
    "A read-only reporting connection is different from a migration connection, but both still deserve lifecycle ownership.",
    "The application, deployment platform, database, logging system, support process, and incident process all influence the real level of protection.",
    "Teams should know what each sensitive connection string is for, who owns it, which environment it belongs to, what privileges it grants, where it is stored, how it is rotated, and what response path applies if it is exposed."
  ]
};
