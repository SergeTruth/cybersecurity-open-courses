window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples turn 'Passwordless and Managed Identity Connections' into concrete defensive .NET review patterns.",
  "codeExamples": [
    {
      "title": "Build a passwordless managed-identity connection string",
      "language": "csharp",
      "blurb": "The factory fixes the cloud database endpoint, database, encryption policy, and managed-identity authentication mode, validates one optional canonical client identifier, and uses a structural builder with no password or persistent credential field.",
      "code": "using System.Data.Common;\n\npublic static class ManagedIdentityConnectionString\n{\n    public static string Build(string? managedIdentityClientId)\n    {\n        if (managedIdentityClientId is not null && !IsCanonicalClientId(managedIdentityClientId))\n            throw new ArgumentException(\"Managed identity client identifier rejected.\", nameof(managedIdentityClientId));\n\n        var builder = new DbConnectionStringBuilder\n        {\n            [\"Server\"] = \"tcp:orders.database.windows.net,1433\",\n            [\"Database\"] = \"orders\",\n            [\"Authentication\"] = \"Active Directory Managed Identity\",\n            [\"Encrypt\"] = true,\n            [\"Trust Server Certificate\"] = false,\n            [\"Persist Security Info\"] = false,\n            [\"Connect Timeout\"] = 5\n        };\n        if (managedIdentityClientId is not null)\n            builder[\"User ID\"] = managedIdentityClientId;\n        return builder.ConnectionString;\n    }\n\n    private static bool IsCanonicalClientId(string value) =>\n        Guid.TryParseExact(value, \"D\", out var clientId) &&\n        string.Equals(value, clientId.ToString(\"D\"), StringComparison.Ordinal);\n}\n"
    },
    {
      "title": "Validate a short-lived managed-identity database token",
      "language": "csharp",
      "blurb": "The lease factory binds the response to the fixed database audience, requires canonical bounded token characters and an expiry within ten minutes, copies the token into private storage, and clears its character buffer when disposed.",
      "code": "public sealed class ManagedIdentityTokenLease : IDisposable\n{\n    private readonly object _sync = new();\n    private char[]? _token;\n\n    private ManagedIdentityTokenLease(char[] token, DateTimeOffset expiresUtc)\n    {\n        _token = token;\n        ExpiresUtc = expiresUtc;\n    }\n\n    public DateTimeOffset ExpiresUtc { get; }\n\n    public static ManagedIdentityTokenLease Create(\n        string audience,\n        ReadOnlySpan<char> token,\n        DateTimeOffset expiresUtc,\n        TimeProvider clock)\n    {\n        ArgumentNullException.ThrowIfNull(clock);\n        var now = clock.GetUtcNow();\n        if (audience != \"https://database.windows.net/.default\" ||\n            token.Length is < 43 or > 4_096 || token.Contains('\\0') ||\n            !IsToken(token) ||\n            expiresUtc <= now || expiresUtc - now > TimeSpan.FromMinutes(10))\n        {\n            throw new ArgumentException(\"Managed identity token rejected.\");\n        }\n        return new ManagedIdentityTokenLease(token.ToArray(), expiresUtc);\n    }\n\n    private static bool IsToken(ReadOnlySpan<char> token)\n    {\n        foreach (var character in token)\n        {\n            if (!(char.IsAsciiLetterOrDigit(character) || character is '-' or '_' or '.'))\n                return false;\n        }\n        return true;\n    }\n\n    public TResult Use<TResult>(ConnectionSecretReader<TResult> reader)\n    {\n        ArgumentNullException.ThrowIfNull(reader);\n        char[] copy;\n        lock (_sync)\n        {\n            var token = _token ?? throw new ObjectDisposedException(nameof(ManagedIdentityTokenLease));\n            copy = token.ToArray();\n        }\n        try\n        {\n            return reader(copy);\n        }\n        finally\n        {\n            copy.AsSpan().Clear();\n        }\n    }\n\n    public void Dispose()\n    {\n        lock (_sync)\n        {\n            if (_token is not { } token) return;\n            token.AsSpan().Clear();\n            _token = null;\n        }\n    }\n}\n\npublic delegate TResult ConnectionSecretReader<TResult>(ReadOnlySpan<char> value);\n"
    }
  ]
};
