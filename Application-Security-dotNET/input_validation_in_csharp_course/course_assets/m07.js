window.COURSE_MODULE = {
  "title": "Files, Paths, and Structured Input",
  "graphicAlt": "Blank placeholder image for Files, Paths, and Structured Input.",
  "narration": "File names and paths should always be treated as untrusted input. A user-supplied filename may include unexpected characters, reserved names, long values, misleading extensions, or path traversal sequences. Path traversal is the risk that input influences file access outside the intended directory. Defensive code should generate safe storage names, resolve paths against an approved base directory, and verify that the final path remains within that directory before reading or writing.\n\nUpload validation should include size limits, allowed content categories, and careful handling of declared metadata. A content type header and a file extension are useful hints, but neither proves what the file contains. Depending on the application, additional content inspection, scanning, or processing isolation may be needed. Even when content is not immediately processed, metadata should be validated before it reaches logs, UI, search indexes, or workflow systems.\n\nStructured input brings its own concerns. JSON schema concepts can help define expected shapes, reject extra fields, and constrain nested objects or arrays. XML parsing should be configured defensively at a high level, especially around external entity processing and resource consumption. CSV files should be treated as structured but messy input: rows may be missing columns, fields may be quoted strangely, and spreadsheet-oriented content may need special care before export or display.\n\nUnsafe deserialization assumptions are a recurring theme. Deserializing data into an object does not prove it is safe, authorized, or meaningful. The resulting object should still be validated, especially if it controls file access, commands, permissions, pricing, workflow state, or persistence. The safer pattern is to parse into a limited DTO, validate the DTO, then map intentionally into domain objects or commands that express only allowed behavior.",
  "narrationPoints": [
    "File names and paths should always be treated as untrusted input.",
    "A user-supplied filename may include unexpected characters, reserved names, long values, misleading extensions, or path traversal sequences.",
    "Path traversal is the risk that input influences file access outside the intended directory.",
    "Defensive code should generate safe storage names, resolve paths against an approved base directory, and verify that the final path remains within that directory.",
    "Upload validation should include size limits, allowed content categories, and careful handling of declared metadata.",
    "A content type header and a file extension are useful hints, but neither proves what the file contains."
  ]
};
