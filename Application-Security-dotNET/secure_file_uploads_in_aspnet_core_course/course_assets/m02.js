window.COURSE_MODULE = {
  "title": "Upload Entry Points, Limits, and Request Handling",
  "graphicAlt": "Bullet summary graphic for Upload Entry Points, Limits, and Request Handling.",
  "narration": "Upload entry points are not limited to a single browser form. Files can arrive through ASP.NET Core MVC pages, minimal APIs, Web APIs, multipart requests, administrative tools, scheduled imports, partner integrations, and support workflows. A secure design identifies every entry point and applies consistent rules where the same business process is involved.\n\nLimits should be deliberate. Request size limits, form limits, multipart limits, file count limits, endpoint-specific limits, and business limits help protect the application from accidental overload and unclear behavior. A profile photo, invoice attachment, bulk import, and administrative report upload may each need different limits because they serve different purposes.\n\nStreaming versus buffering is a design decision. Buffering may be simpler for small files, but it can put pressure on memory, disk, or temporary storage. Streaming can support larger files and early processing, but it requires careful validation, cancellation, error handling, and storage design. Choose based on expected size, performance needs, validation requirements, and operational capacity.\n\nInterrupted uploads should fail gracefully. Timeouts and cancellation tokens help the application stop work when a client disconnects or a request exceeds policy. Upload code should avoid continuing expensive processing after the caller is gone and should clean up partial temporary files when the workflow cannot safely continue.\n\nEarly rejection is useful when a request is clearly unsupported or oversized. The application should reject files that do not match the endpoint purpose, exceed limits, or violate basic business rules before doing expensive work. That said, early rejection is only the first layer; accepted files still need storage isolation, validation, scanning or review where appropriate, and authorization checks.\n\nAvoid unbounded reads and memory-heavy processing of untrusted upload data. A safe upload handler controls how much it reads, where data is buffered, how temporary files are managed, and what happens when validation or processing fails. Those controls should apply consistently across public endpoints, internal workflows, and administrative paths.",
  "narrationPoints": [
    "A secure design identifies every entry point and applies consistent rules where the same business process is involved.",
    "Request size limits, form limits, multipart limits, file count limits, endpoint-specific limits, and business limits help protect the application from accidental overload and unclear behavior.",
    "Streaming can support larger files and early processing, but it requires careful validation, cancellation, error handling, and storage design.",
    "Upload code should avoid continuing expensive processing after the caller is gone and should clean up partial temporary files when the workflow cannot safely continue.",
    "Early rejection is useful when a request is clearly unsupported or oversized."
  ]
};
