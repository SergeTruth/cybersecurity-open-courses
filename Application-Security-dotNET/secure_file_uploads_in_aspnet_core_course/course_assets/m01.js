window.COURSE_MODULE = {
  "title": "Why File Uploads Are Security-Sensitive",
  "graphicAlt": "Bullet summary graphic for Why File Uploads Are Security-Sensitive.",
  "narration": "File uploads are security-sensitive because they cross a major trust boundary. A user, partner, integration, administrator, or background import sends content that the application did not create and cannot fully understand at the moment it arrives. Even when the user is authenticated, the uploaded file is still external input.\n\nAccepting a file is different from trusting a file. An ASP.NET Core endpoint may receive a profile photo, support attachment, invoice, CSV import, report, or product image. That only means the request reached the application and met the first set of handling rules. It does not mean the file is appropriate for preview, processing, public access, long-term storage, or download by another user.\n\nUpload workflows affect many parts of the system. They touch request handling, temporary storage, permanent storage, background processing, scanning, preview generation, imports, search indexing, downloads, administrator tools, support workflows, retention, backups, and monitoring. A weak assumption in one step can create problems in another.\n\nASP.NET Core gives developers useful request-handling features, model binding, form limits, streaming options, configuration hooks, and middleware patterns. Those features help, but application design still controls what file categories are allowed, where files are stored, which identities can access them, how they are processed, and when they are deleted.\n\nSecure upload handling connects prevention, detection, privacy, reliability, and maintainability. Prevention limits what the application accepts and where it can go. Detection records events and state changes. Privacy controls what metadata and content are exposed. Reliability prevents large or slow uploads from harming the service. Maintainability keeps the workflow understandable enough to review as requirements change.",
  "narrationPoints": [
    "File uploads are security-sensitive because they cross a major trust boundary.",
    "An ASP.NET Core endpoint may receive a profile photo, support attachment, invoice, CSV import, report, or product image.",
    "A weak assumption in one step can create problems in another.",
    "ASP.NET Core gives developers useful request-handling features, model binding, form limits, streaming options, configuration hooks, and middleware patterns.",
    "Prevention limits what the application accepts and where it can go."
  ]
};
