window.COURSE_MODULE = {
  "title": "File Type Validation and Content Inspection",
  "graphicAlt": "Bullet summary graphic for File Type Validation and Content Inspection.",
  "narration": "File type validation should use multiple signals. File extension, MIME type, content type header, file signature, metadata, and parser results each provide different information. None of them alone proves that a file is safe or appropriate for a business workflow.\n\nAllow-list permitted file categories based on business need. If a support case only needs document attachments, do not accept unrelated categories. If a profile feature only needs images, keep the allowed categories narrow. Business purpose should drive what the endpoint accepts, not what the platform can technically receive.\n\nBrowser-provided labels and client metadata are useful hints, not guarantees. A content type header, display name, or extension can help route a validation decision, but the application should not treat those labels as authoritative. Server-side validation should decide whether the file matches what the workflow expects.\n\nValidate more than category. Size, structure, expected format, required fields, image dimensions, document properties, processing requirements, and import constraints may all matter. A file can be the right general type and still be too large, malformed, incomplete, or unsuitable for the operation.\n\nValidation outcomes should be consistent across upload, preview, processing, and download. A file that is rejected for normal processing should not later become previewable through a separate path. A file allowed for private storage should not become public through a different handler. Consistency prevents state confusion.\n\nHandle metadata carefully. Metadata can contain names, locations, titles, device information, comments, or other details that may be sensitive or misleading. Decide what metadata to retain, strip, display, index, or log based on the file category, user expectation, and privacy requirements.",
  "narrationPoints": [
    "File extension, MIME type, content type header, file signature, metadata, and parser results each provide different information.",
    "Business purpose should drive what the endpoint accepts, not what the platform can technically receive.",
    "Server-side validation should decide whether the file matches what the workflow expects.",
    "A file can be the right general type and still be too large, malformed, incomplete, or unsuitable for the operation.",
    "Validation outcomes should be consistent across upload, preview, processing, and download."
  ]
};
