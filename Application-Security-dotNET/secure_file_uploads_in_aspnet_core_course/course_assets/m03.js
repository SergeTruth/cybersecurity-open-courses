window.COURSE_MODULE = {
  "title": "File Names, Paths, and Storage Isolation",
  "graphicAlt": "Bullet summary graphic for File Names, Paths, and Storage Isolation.",
  "narration": "Client-supplied file names should be treated as display metadata, not as storage decisions. A file name may be useful to show a user which invoice, report, or image they selected, but it should not decide the server path, object key, final storage location, access policy, or processing behavior.\n\nGenerate safe internal names or identifiers for storage. The application can preserve a sanitized display name separately when the business needs it, while using a server-generated identifier for actual storage. This separates user-facing labels from operational storage details and reduces accidental reliance on client-provided values.\n\nDo not use client-supplied paths. Upload workflows should avoid accepting paths, folder decisions, object prefixes, or storage locations from the caller unless those values are mapped through a tightly controlled business rule. The server should decide where temporary, quarantined, private, public, and processed files live.\n\nUploaded files should be stored outside executable application roots. A file accepted from a user should not become part of the application code path, static content root, or executable deployment area merely because it was uploaded. Storage design should prevent uploaded content from being treated as application behavior.\n\nStorage areas should reflect file state and audience. Temporary files, quarantined files, private user files, public published files, processed derivatives, thumbnails, and rejected uploads may need separate locations, policies, cleanup schedules, and permissions. Clear state separation makes the workflow easier to review and operate.\n\nApply least privilege to storage identities. The web application, background scanner, processing worker, support tool, and administrator account may not need the same permissions. Limit read, write, delete, publish, and listing permissions according to each component's job, and review those permissions during release and infrastructure changes.",
  "narrationPoints": [
    "Client-supplied file names should be treated as display metadata, not as storage decisions.",
    "The server should decide where temporary, quarantined, private, public, and processed files live.",
    "A file accepted from a user should not become part of the application code path, static content root, or executable deployment area merely because it was uploaded.",
    "Storage areas should reflect file state and audience.",
    "Limit read, write, delete, publish, and listing permissions according to each component's job, and review those permissions during release and infrastructure changes."
  ]
};
