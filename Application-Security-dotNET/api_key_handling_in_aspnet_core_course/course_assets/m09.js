window.COURSE_MODULE = {
  "title": "Client-Side and Third-Party Integration Mistakes",
  "graphicAlt": "Bullet summary graphic for Client-Side and Third-Party Integration Mistakes.",
  "narration": "Third-party API keys should not be embedded in browser-delivered JavaScript, public frontend configuration, mobile apps, or downloadable assets when the key grants server-side privileges. If the provider key allows billed usage, data access, account changes, or backend operations, the browser should not receive it.\n\nBackend mediation can keep provider keys on the server and expose only the specific application behavior that clients need. Instead of giving a browser direct access to a provider credential, the ASP.NET Core backend can authenticate the user or client, authorize the requested action, call the provider, and return a bounded response.\n\nPublic or publishable keys still need careful review. Some providers intentionally expose keys that identify a project or environment while relying on restrictions and server-side policy for protection. Teams should still understand what the key reveals, where it is allowed to run, which quotas apply, and how misuse would be detected.\n\nSample code, screenshots, support logs, documentation, CI output, crash reports, generated clients, and onboarding guides can expose keys accidentally. A course, runbook, or support article should use placeholders and safe identifiers, not real secrets copied from production systems.\n\nPer-environment keys reduce blast radius. Development, staging, test, and production should use separate credentials and separate provider settings when possible. That separation makes rotation, monitoring, and incident response more targeted.\n\nThird-party provider permissions, billing limits, referrer restrictions, IP allowlists, service restrictions, usage quotas, and alert settings should be reviewed where available. Provider-side controls do not replace application security, but they help limit consequences when a client or integration behaves unexpectedly.",
  "narrationPoints": [
    "Third-party API keys should not be embedded in browser-delivered JavaScript, public frontend configuration, mobile apps, or downloadable assets when the key grants server-side privileges.",
    "Instead of giving a browser direct access to a provider credential, the ASP.NET Core backend can authenticate the user or client, authorize the requested action, call the provider, and return a bounded response.",
    "A course, runbook, or support article should use placeholders and safe identifiers, not real secrets copied from production systems.",
    "Development, staging, test, and production should use separate credentials and separate provider settings when possible.",
    "Provider-side controls do not replace application security, but they help limit consequences when a client or integration behaves unexpectedly."
  ]
};
