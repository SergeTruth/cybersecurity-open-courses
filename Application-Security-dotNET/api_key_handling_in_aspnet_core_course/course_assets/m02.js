window.COURSE_MODULE = {
  "title": "Inbound vs. Outbound API Key Handling",
  "graphicAlt": "Bullet summary graphic for Inbound vs. Outbound API Key Handling.",
  "narration": "Inbound API keys are keys that clients present to your ASP.NET Core application. They may identify a partner system, service account, automation job, tenant integration, or internal workload. Inbound handling focuses on validation, caller identity, authorization, rate limits, monitoring, revocation, and clear failure behavior.\n\nOutbound API keys are keys your application uses to call third-party APIs or internal services. Outbound handling focuses on secret storage, least privilege, environment separation, rotation, request construction, provider restrictions, and avoiding accidental exposure through logs, configuration, or client-delivered assets.\n\nThe two directions have different ownership questions. For inbound keys, your application owns the validation rules and service policy. For outbound keys, your application is a client of another system and must protect provider credentials according to the provider contract and your organization's production controls.\n\nDo not mix development, staging, and production keys. Environment separation reduces consequences when a non-production system is misconfigured, copied, or shared too broadly. It also helps operators rotate or disable a key without disrupting unrelated environments.\n\nAvoid reusing one shared key across unrelated systems or tenants when separate identity and accountability are needed. Shared keys make it harder to understand which client performed an action, which owner should be contacted, which scope should apply, and which integration should be disabled during response.\n\nInventory is a security control. Every key should have an owner, purpose, environment, scope, creation date, expected rotation path, and revocation process. Without inventory, key handling becomes guesswork during operations and incident response.",
  "narrationPoints": [
    "Inbound API keys are keys that clients present to your ASP.NET Core application.",
    "Outbound handling focuses on secret storage, least privilege, environment separation, rotation, request construction, provider restrictions, and avoiding accidental exposure through logs, configuration, or client-delivered assets.",
    "For inbound keys, your application owns the validation rules and service policy.",
    "Avoid reusing one shared key across unrelated systems or tenants when separate identity and accountability are needed.",
    "Without inventory, key handling becomes guesswork during operations and incident response."
  ]
};
