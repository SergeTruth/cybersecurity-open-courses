window.COURSE_MODULE = {
  "title": "Scopes, Tenants, Authorization, and Least Privilege",
  "graphicAlt": "Bullet summary graphic for Scopes, Tenants, Authorization, and Least Privilege.",
  "narration": "A valid API key should not automatically mean full access. Authentication establishes that the request presented a known credential. Authorization decides what that credential is allowed to do. Keeping those decisions separate is essential for ASP.NET Core APIs that support multiple clients, tenants, environments, or operation types.\n\nAPI keys should be scoped to a purpose, environment, tenant, client, or allowed operation. A reporting integration may need read access to a narrow set of endpoints. A deployment automation key may need a specific administrative function. A partner key may need one tenant boundary. Broad keys should be rare, approved, and monitored.\n\nServer-side authorization must enforce endpoint, method, resource, and tenant boundaries. A client-supplied tenant identifier should be checked against trusted key metadata or server-side data. The caller should not be able to choose a tenant or privilege boundary merely by sending a value in the request.\n\nKey metadata can help decide allowed operations, rate limits, data boundaries, expiration, owner, and review path. That metadata should be managed as part of the key lifecycle and used consistently by authorization logic, monitoring, and support tooling.\n\nAdministrative, export, payment, configuration, destructive, and high-cost operations need explicit authorization review. These operations can have broad business impact, create cost, or change production state. They should not be covered only by a generic valid-key check.\n\nAPI keys may identify an application or integration rather than an individual human. That affects accountability and audit design. Logs and records should make it clear which key, owner, client, tenant, or integration performed an action and what human approval path applies where required.",
  "narrationPoints": [
    "Keeping those decisions separate is essential for ASP.NET Core APIs that support multiple clients, tenants, environments, or operation types.",
    "API keys should be scoped to a purpose, environment, tenant, client, or allowed operation.",
    "The caller should not be able to choose a tenant or privilege boundary merely by sending a value in the request.",
    "Key metadata can help decide allowed operations, rate limits, data boundaries, expiration, owner, and review path.",
    "Logs and records should make it clear which key, owner, client, tenant, or integration performed an action and what human approval path applies where required."
  ]
};
