window.COURSE_MODULE = {
  "title": "Request Placement, Transport, and Exposure Paths",
  "graphicAlt": "Bullet summary graphic for Request Placement, Transport, and Exposure Paths.",
  "narration": "API keys should be transmitted only over HTTPS in production. Transport security helps protect credential-bearing requests in transit and helps clients communicate with the intended service. If a request contains a key, the route between client and service should be treated as sensitive.\n\nDedicated request headers are usually easier to handle safely than query-string keys. Headers can be targeted by authentication handlers, gateway rules, and redaction policies. Query strings are part of URLs, and URLs are copied, cached, logged, bookmarked, screenshotted, and sent through many tools in normal operations.\n\nQuery-string keys create unnecessary exposure paths. URLs may appear in browser history, server logs, reverse proxy logs, referrer headers, analytics tools, support tickets, copied links, crash reports, sample requests, and screenshots. Even when the application itself is careful, surrounding tooling may preserve the full URL.\n\nKeys should not be placed in frontend JavaScript, public HTML, mobile app assets, downloadable configuration, or public repositories when they grant server-side privileges. Anything delivered to users or packaged into public assets should be assumed visible to those users.\n\nDiagnostics, traces, crash dumps, screenshots, and sample requests should be treated as possible exposure paths. A key can leave the intended trust boundary through ordinary troubleshooting artifacts. That is why redaction and support workflows need review before production.\n\nDo not log full request headers, URLs, query strings, or bodies without redaction review. Logs should help operators understand behavior while avoiding full credentials and sensitive payloads. The safest log design records identifiers, outcomes, and correlation context rather than raw credential material.",
  "narrationPoints": [
    "If a request contains a key, the route between client and service should be treated as sensitive.",
    "Query strings are part of URLs, and URLs are copied, cached, logged, bookmarked, screenshotted, and sent through many tools in normal operations.",
    "Keys should not be placed in frontend JavaScript, public HTML, mobile app assets, downloadable configuration, or public repositories when they grant server-side privileges.",
    "Diagnostics, traces, crash dumps, screenshots, and sample requests should be treated as possible exposure paths.",
    "Do not log full request headers, URLs, query strings, or bodies without redaction review."
  ]
};
