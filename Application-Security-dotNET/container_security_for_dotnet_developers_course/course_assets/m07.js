window.COURSE_MODULE = {
  "title": "Kubernetes and Orchestrator Security Controls",
  "graphicAlt": "Bullet summary graphic for Kubernetes and Orchestrator Security Controls.",
  "narration": "Many production .NET containers run under an orchestrator such as Kubernetes, Azure Container Apps, Docker Swarm, or another platform. Even when platform teams own the cluster, developers should understand the controls that shape application behavior. The app and the platform settings need to agree.\n\nSecurity context settings can define user identity, privilege behavior, filesystem settings, and capability handling. Where applicable, settings such as runAsNonRoot, runAsUser, allowPrivilegeEscalation, capabilities, privileged mode, and readOnlyRootFilesystem should be reviewed against what the application actually requires.\n\nResource requests and limits protect platform stability and application predictability. Requests help the scheduler place work. Limits help prevent one workload from consuming capacity that other workloads need. .NET applications should be tested under realistic memory and CPU constraints so failures are understandable rather than surprising.\n\nService accounts, RBAC, namespaces, network policies, secrets, config maps, admission controls, and image pull policies all affect runtime security. A workload should receive only the platform permissions and network access it needs. Configuration and secret objects should have clear ownership, review, and rotation expectations.\n\nProbes need careful design. Liveness and readiness probes should help the platform route traffic and recover unhealthy instances, but they should not hide dependency failures, trigger unnecessary restart loops, or expose detailed internals. Applications should behave predictably under restricted pod settings and should not require broad host access by default.",
  "narrationPoints": [
    "Many production .NET containers run under an orchestrator such as Kubernetes, Azure Container Apps, Docker Swarm, or another platform.",
    "Security context settings can define user identity, privilege behavior, filesystem settings, and capability handling.",
    ".NET applications should be tested under realistic memory and CPU constraints so failures are understandable rather than surprising.",
    "Configuration and secret objects should have clear ownership, review, and rotation expectations.",
    "Liveness and readiness probes should help the platform route traffic and recover unhealthy instances, but they should not hide dependency failures, trigger unnecessary restart loops, or expose detailed internals."
  ]
};
