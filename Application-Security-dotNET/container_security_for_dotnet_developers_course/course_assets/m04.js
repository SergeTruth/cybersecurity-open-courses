window.COURSE_MODULE = {
  "title": "Running as Non-Root and Limiting Runtime Privilege",
  "graphicAlt": "Bullet summary graphic for Running as Non-Root and Limiting Runtime Privilege.",
  "narration": "Containers should run with the least privilege needed for the application to operate. Running as root inside the container can make mistakes more consequential, especially when writable paths, mounted volumes, broad capabilities, or host integrations are involved. A non-root runtime user is a practical default for many .NET services.\n\nMoving to non-root is not only a single Dockerfile line. File ownership, working directories, temporary directories, upload locations, log behavior, certificate access, and bind mounts all need review. The application should know where it is allowed to write, and those locations should be intentionally created and permissioned.\n\nPrivileged containers and broad Linux capabilities should be rare, documented, and reviewed. Many .NET web applications do not need extra capabilities, host namespace access, device mounts, or broad filesystem access. Reducing runtime privilege helps make production behavior more predictable and easier to operate.\n\nRead-only root filesystems can reduce accidental writes and make unexpected filesystem changes more visible. They also require planning. An application may still need writable locations for temporary files, uploads, caches, logs, diagnostics, or framework features. Those paths should be explicit and separate from the application binaries.\n\nCapabilities, seccomp, AppArmor, SELinux, user namespaces, and runtime security options may be owned by a platform team, but developers should understand their application impact. Application code should not assume it can install tools at runtime, write anywhere in the container, change filesystem state casually, or depend on permissions that production policy does not grant.",
  "narrationPoints": [
    "Containers should run with the least privilege needed for the application to operate.",
    "The application should know where it is allowed to write, and those locations should be intentionally created and permissioned.",
    "Privileged containers and broad Linux capabilities should be rare, documented, and reviewed.",
    "An application may still need writable locations for temporary files, uploads, caches, logs, diagnostics, or framework features.",
    "Application code should not assume it can install tools at runtime, write anywhere in the container, change filesystem state casually, or depend on permissions that production policy does not grant."
  ]
};
