window.COURSE_MODULE = {
  "title": "Dockerfile and Build Pipeline Hardening",
  "graphicAlt": "Bullet summary graphic for Dockerfile and Build Pipeline Hardening.",
  "narration": "A Dockerfile is part of the application security surface because it decides what enters the image and what remains available at runtime. It also affects repeatability, build provenance, package restore behavior, user identity, working directory, file permissions, environment variables, and operational expectations.\n\nMulti-stage builds help separate build activity from production runtime. The build stage can use an SDK image to restore packages, compile code, run allowed build steps, and publish output. The runtime stage can then copy only the published application and the runtime dependencies needed to operate. That separation reduces unnecessary content in the final image.\n\nThe build context deserves the same care as source control. A .dockerignore file should exclude secrets, local configuration, test data, build output, temporary files, user-specific files, and anything else the image build does not need. Sending less context to the builder reduces accidental exposure and makes builds easier to audit.\n\nBuild-time secrets need special handling. Ordinary build arguments and build-time environment variables can be recorded in image history, layers, logs, or cache metadata. If a build truly needs access to a private feed or signing material, use platform-supported build-secret mechanisms and review what appears in logs and artifacts.\n\nThe final image should not contain package caches, test output, source code, local development files, package feed credentials, signing keys, or extra tools that are not part of production operation. CI logs should avoid tokens, connection strings, private feed credentials, and signing material. The build pipeline should produce a runtime image that is small enough to inspect and predictable enough to rebuild.",
  "narrationPoints": [
    "A Dockerfile is part of the application security surface because it decides what enters the image and what remains available at runtime.",
    "The build stage can use an SDK image to restore packages, compile code, run allowed build steps, and publish output.",
    "A .dockerignore file should exclude secrets, local configuration, test data, build output, temporary files, user-specific files, and anything else the image build does not need.",
    "Ordinary build arguments and build-time environment variables can be recorded in image history, layers, logs, or cache metadata.",
    "The final image should not contain package caches, test output, source code, local development files, package feed credentials, signing keys, or extra tools that are not part of production operation."
  ]
};
