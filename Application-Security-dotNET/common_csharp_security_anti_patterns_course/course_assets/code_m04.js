window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples turn 'Missing Authorization and Client-Side Trust' into concrete defensive .NET review patterns.",
  "codeExamples": [
    {
      "title": "Authorize both tenant membership and document ownership",
      "language": "csharp",
      "blurb": "The decision requires an authenticated principal, one canonical subject and tenant claim, an exact scope token, and a repository-supplied resource whose tenant and owner match those immutable claim snapshots.",
      "code": "using System.Security.Claims;\n\npublic sealed record StoredDocument(string Id, string TenantId, string OwnerId);\n\npublic static class DocumentAccess\n{\n    public static bool CanRead(ClaimsPrincipal principal, StoredDocument document)\n    {\n        ArgumentNullException.ThrowIfNull(principal);\n        ArgumentNullException.ThrowIfNull(document);\n        if (principal.Identity?.IsAuthenticated != true ||\n            !IsIdentifier(document.Id) ||\n            !IsIdentifier(document.TenantId) ||\n            !IsIdentifier(document.OwnerId))\n        {\n            return false;\n        }\n\n        var subjects = principal.FindAll(\"sub\").Select(claim => claim.Value).Take(2).ToArray();\n        var tenants = principal.FindAll(\"tenant_id\").Select(claim => claim.Value).Take(2).ToArray();\n        var scopes = principal.FindAll(\"scope\").Select(claim => claim.Value).Take(9).ToArray();\n        if (subjects.Length != 1 || tenants.Length != 1 ||\n            !IsIdentifier(subjects[0]) || !IsIdentifier(tenants[0]) ||\n            scopes.Length > 8 || scopes.Any(scope => scope.Length > 1024))\n        {\n            return false;\n        }\n\n        var canRead = scopes\n            .SelectMany(scope => scope.Split(' ', StringSplitOptions.RemoveEmptyEntries))\n            .Any(scope => string.Equals(scope, \"document.read\", StringComparison.Ordinal));\n        return canRead &&\n               string.Equals(tenants[0], document.TenantId, StringComparison.Ordinal) &&\n               string.Equals(subjects[0], document.OwnerId, StringComparison.Ordinal);\n    }\n\n    private static bool IsIdentifier(string value) =>\n        !string.IsNullOrEmpty(value) && value.Length <= 64 &&\n        value.All(character => char.IsAsciiLetterOrDigit(character) || character is '-' or '_');\n}\n"
    },
    {
      "title": "Recompute purchase authority from a trusted catalog",
      "language": "csharp",
      "blurb": "The quote boundary treats product, quantity, and claimed total as untrusted, retrieves the authoritative unit price from an application-owned catalog, uses checked decimal arithmetic, and rejects a mismatched client total.",
      "code": "public sealed record ClientPurchaseRequest(\n    string ProductId,\n    int Quantity,\n    decimal ClaimedTotal);\n\npublic interface IProductCatalog\n{\n    bool TryGetUnitPrice(string productId, out decimal unitPrice);\n}\n\npublic sealed class ApprovedPurchaseQuote\n{\n    private ApprovedPurchaseQuote(string productId, int quantity, decimal total)\n    {\n        ProductId = productId;\n        Quantity = quantity;\n        Total = total;\n    }\n\n    public string ProductId { get; }\n    public int Quantity { get; }\n    public decimal Total { get; }\n\n    public static ApprovedPurchaseQuote Create(\n        ClientPurchaseRequest request,\n        IProductCatalog catalog)\n    {\n        ArgumentNullException.ThrowIfNull(request);\n        ArgumentNullException.ThrowIfNull(catalog);\n        if (!IsIdentifier(request.ProductId) || request.Quantity is < 1 or > 100)\n            throw new ArgumentException(\"Purchase request rejected.\", nameof(request));\n        if (!catalog.TryGetUnitPrice(request.ProductId, out var unitPrice) ||\n            unitPrice is < 0.01m or > 1_000_000m)\n        {\n            throw new InvalidOperationException(\"Catalog price rejected.\");\n        }\n\n        decimal total;\n        try\n        {\n            total = checked(unitPrice * request.Quantity);\n        }\n        catch (OverflowException exception)\n        {\n            throw new InvalidOperationException(\"Purchase total overflowed.\", exception);\n        }\n        if (request.ClaimedTotal != total)\n            throw new UnauthorizedAccessException(\"Client total did not match the catalog.\");\n        return new ApprovedPurchaseQuote(request.ProductId, request.Quantity, total);\n    }\n\n    private static bool IsIdentifier(string value) =>\n        !string.IsNullOrEmpty(value) && value.Length <= 48 &&\n        value.All(character => char.IsAsciiLetterOrDigit(character) || character is '-' or '_');\n}\n\npublic static class PurchaseAuthorization\n{\n    public static ApprovedPurchaseQuote CreateQuote(\n        ClientPurchaseRequest request,\n        IProductCatalog catalog) =>\n        ApprovedPurchaseQuote.Create(request, catalog);\n}\n"
    }
  ]
};
