window.COURSE_MODULE = {
  "title": "Missing Authorization and Client-Side Trust",
  "graphicAlt": "Bullet summary graphic for Missing Authorization and Client-Side Trust.",
  "narration": "Authentication identifies a caller. Authorization decides what that caller may do. A common anti-pattern is treating those as the same decision. A logged-in user is not automatically allowed to view every record, change every setting, export every report, or perform every action reachable from the application.\n\nUI hiding is not server-side authorization. Hidden buttons, disabled fields, route names, JavaScript checks, obscured links, and frontend state can improve user experience, but they do not enforce access at the point where data is read or state is changed. The server must make the decision using trusted information.\n\nClient-supplied user IDs, account IDs, tenant IDs, object IDs, roles, scopes, and flags are not authority by themselves. They are input that must be checked against authenticated identity, server-side state, resource ownership, tenant membership, subscription status, record status, and business policy.\n\nResource-based authorization is often required when access depends on the object being touched. The question is not only whether the caller has a role. It may be whether the caller owns this record, belongs to this tenant, is assigned to this case, has approval for this transaction, or is allowed to act while the workflow is in its current state.\n\nAdministrative, export, financial, destructive, configuration, support, and impersonation workflows need explicit policy review. These paths are often less frequently used than ordinary user workflows, which makes accidental gaps easier to miss during manual testing.\n\nA safer habit is to put authorization near the server-side action and make it reviewable. Controllers, handlers, services, policies, resource checks, and data filters should show where the decision happens and what information it uses. Reviewers should not have to trust that the UI prevented an unsafe request.",
  "narrationPoints": [
    "A logged-in user is not automatically allowed to view every record, change every setting, export every report, or perform every action reachable from the application.",
    "The server must make the decision using trusted information.",
    "Resource-based authorization is often required when access depends on the object being touched.",
    "A safer habit is to put authorization near the server-side action and make it reviewable.",
    "Reviewers should not have to trust that the UI prevented an unsafe request."
  ]
};
