window.COURSE_MODULE = {
  "title": "Unsafe SQL, Query Construction, and Data Access",
  "graphicAlt": "Bullet summary graphic for Unsafe SQL, Query Construction, and Data Access.",
  "narration": "Building SQL or query fragments through string concatenation is a classic data access anti-pattern. Parameterized queries and maintained ORM patterns reduce many value-handling risks, but they do not remove the need to review the full data access path.\n\nParameterization protects values. It does not automatically make every dynamic table name, sort field, filter expression, projection, stored procedure call, raw SQL block, or query builder option safe. When user or request input influences query shape, the application should map that input to known choices instead of treating it as free-form query structure.\n\nORMs help, but they do not automatically enforce tenant boundaries, resource ownership, authorization rules, or safe projections. A LINQ query can still return too much data. A repository method can still skip a policy check. A convenient Include can still pull sensitive related records into a response model.\n\nHigh-risk workflows deserve extra review: export, reporting, administration, search, bulk update, migration, reconciliation, and dynamic filtering. These features often need flexible data access, which makes ownership, filtering, projection, and authorization decisions more important, not less.\n\nDatabase identities should follow least privilege. An application account that only needs to read and update a bounded set of tables should not have broad administrative permissions. Separation between runtime access, migration access, reporting access, and operational maintenance helps limit mistakes.\n\nErrors and logs should not expose SQL text with sensitive values, connection strings, internal schema details, or provider configuration. Useful diagnostics can identify the operation, failure category, correlation ID, and safe resource identifiers without recording details that should remain internal.",
  "narrationPoints": [
    "Building SQL or query fragments through string concatenation is a classic data access anti-pattern.",
    "A convenient Include can still pull sensitive related records into a response model.",
    "High-risk workflows deserve extra review: export, reporting, administration, search, bulk update, migration, reconciliation, and dynamic filtering.",
    "Separation between runtime access, migration access, reporting access, and operational maintenance helps limit mistakes.",
    "Errors and logs should not expose SQL text with sensitive values, connection strings, internal schema details, or provider configuration."
  ]
};
