window.COURSE_MODULE = {
  "title": "Logging, Error Handling, and Operational Exposure",
  "graphicAlt": "Bullet summary graphic for Logging, Error Handling, and Operational Exposure.",
  "narration": "Logs and diagnostics often expose what source code avoided exposing. A team may carefully protect a token in memory and then print it during troubleshooting. A controller may avoid returning sensitive fields while telemetry captures them in a structured property. Operational output is part of the data handling surface.\n\nSensitive data can leak through structured logs, HTTP logs, exception messages, stack traces, telemetry, traces, metrics labels, crash dumps, health checks, debug pages, support tickets, screenshots, and CI output. These systems often have different retention, access control, search, export, and sharing behavior than the application database.\n\nDo not log passwords, tokens, API keys, connection strings, authorization headers, cookies, reset links, raw personal data, raw financial data, or full sensitive payloads. When sensitive details are needed for a controlled investigation, use an approved evidence process rather than routine application logging.\n\nError responses should be useful but limited. They should not reveal internal paths, schema details, stack traces, secret names, tenant identifiers, provider configuration, or raw exception internals. The user-facing response and the operator diagnostic record should serve different audiences.\n\nRedaction should be designed and tested before production. It should cover structured properties, message templates, exception details, HTTP headers, request bodies, response bodies, background job payloads, and dependency logs where relevant. A redaction policy that exists only as a code comment will not protect the system consistently.\n\nUseful logging preserves safe context: correlation ID, operation name, failure category, safe resource identifier, environment, dependency category, timing, and outcome. Production diagnostics should be access-controlled, environment-scoped, retained appropriately, and periodically reviewed for sensitive data exposure.",
  "narrationPoints": [
    "A team may carefully protect a token in memory and then print it during troubleshooting.",
    "Sensitive data can leak through structured logs, HTTP logs, exception messages, stack traces, telemetry, traces, metrics labels, crash dumps, health checks, debug pages, support tickets, screenshots, and CI output.",
    "Do not log passwords, tokens, API keys, connection strings, authorization headers, cookies, reset links, raw personal data, raw financial data, or full sensitive payloads.",
    "The user-facing response and the operator diagnostic record should serve different audiences.",
    "Useful logging preserves safe context: correlation ID, operation name, failure category, safe resource identifier, environment, dependency category, timing, and outcome."
  ]
};
