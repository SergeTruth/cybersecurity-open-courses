window.COURSE_MODULE = {
  "title": "Hardcoded Secrets and Configuration Drift",
  "graphicAlt": "Bullet summary graphic for Hardcoded Secrets and Configuration Drift.",
  "narration": "Hardcoded secrets are one of the most common and avoidable C# security anti-patterns. Secrets include passwords, connection strings, API keys, tokens, certificates, private keys, signing keys, storage keys, and provider credentials. They are not ordinary configuration values because exposure can grant access or undermine trust.\n\nSecrets should not appear in source code, committed appsettings files, sample projects, test fixtures, screenshots, documentation, logs, CI output, deployment artifacts, support tickets, or copied troubleshooting notes. Those locations are replicated, indexed, retained, and shared in ways that are difficult to control after the fact.\n\nDevelopment-only convenience must not become production secret storage. A local secret file, a temporary environment variable, or a shared test credential may be acceptable inside a controlled development workflow, but that does not make it a production pattern. Production secrets need access control, inventory, ownership, rotation, and operational review.\n\nEnvironment separation matters. Development, test, staging, production, and customer-specific deployments should not casually share credentials or security-sensitive configuration. Shared secrets increase blast radius, make testing less trustworthy, and make rotation harder when one environment needs a change.\n\nConfiguration drift occurs when production behavior differs from reviewed expectations because settings were copied, overridden, forgotten, or changed outside the normal review path. A secure code path can behave unsafely if the deployed configuration disables a control, points to the wrong service, or uses a broader credential than intended.\n\nSafer habits include controlled secret stores, managed identity where appropriate, environment-aware configuration, documented ownership, rotation schedules, redaction rules, startup validation, and periodic review. The key habit is treating configuration as part of the security design rather than a pile of values that only matters at deployment time.",
  "narrationPoints": [
    "Hardcoded secrets are one of the most common and avoidable C# security anti-patterns.",
    "Production secrets need access control, inventory, ownership, rotation, and operational review.",
    "Shared secrets increase blast radius, make testing less trustworthy, and make rotation harder when one environment needs a change.",
    "Configuration drift occurs when production behavior differs from reviewed expectations because settings were copied, overridden, forgotten, or changed outside the normal review path.",
    "The key habit is treating configuration as part of the security design rather than a pile of values that only matters at deployment time."
  ]
};
