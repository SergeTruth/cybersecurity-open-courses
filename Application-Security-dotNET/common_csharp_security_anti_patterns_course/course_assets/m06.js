window.COURSE_MODULE = {
  "title": "Insecure Deserialization and Dangerous Type Handling",
  "graphicAlt": "Bullet summary graphic for Insecure Deserialization and Dangerous Type Handling.",
  "narration": "Deserialization should treat input as data, not as permission to choose application behavior. A parser or serializer may turn bytes into objects, but the application still owns the trust decision. Input from users, queues, files, partners, caches, or external services should be mapped into expected contracts and validated for the operation.\n\nA dangerous anti-pattern is letting untrusted input select concrete .NET types, handler classes, formatter types, command names, plugin names, or reflection targets. That design gives data too much influence over code paths. The safer approach is to map input to known, allowed message types and handlers under application control.\n\nLegacy or dangerous serialization mechanisms for untrusted data should be replaced or isolated behind a reviewed compatibility boundary. Binary formats, type name handling, polymorphic deserialization, reflection-based activation, and plugin loading all require explicit trust boundaries and documented ownership.\n\nPrefer narrow DTOs, fixed contracts, allowlisted message types, schema validation where appropriate, and maintained serializers configured for the intended model. A narrow contract makes it easier to see what data is accepted, which fields are required, and which values are ignored or rejected.\n\nLoaded assemblies and plugins are code, not harmless configuration. If a system can load executable components, the design needs signing, source control, release governance, access control, operational monitoring, and rollback planning. Treat plugin loading as a deployment and trust decision.\n\nFailure messages should be safe. Deserialization errors should not expose internal type names, assembly names, file paths, secrets, or raw sensitive payloads to broad audiences. Logs should capture safe context such as message type category, workflow, correlation ID, and failure reason without disclosing unnecessary internals.",
  "narrationPoints": [
    "A dangerous anti-pattern is letting untrusted input select concrete .NET types, handler classes, formatter types, command names, plugin names, or reflection targets.",
    "Legacy or dangerous serialization mechanisms for untrusted data should be replaced or isolated behind a reviewed compatibility boundary.",
    "A narrow contract makes it easier to see what data is accepted, which fields are required, and which values are ignored or rejected.",
    "Treat plugin loading as a deployment and trust decision.",
    "Deserialization errors should not expose internal type names, assembly names, file paths, secrets, or raw sensitive payloads to broad audiences."
  ]
};
