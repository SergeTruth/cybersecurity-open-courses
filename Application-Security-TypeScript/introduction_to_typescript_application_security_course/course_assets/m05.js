window.COURSE_MODULE = {
  "title": "Authentication, Authorization, and Session-Aware Code",
  "graphicAlt": "Draft visual summary for Authentication, Authorization, and Session-Aware Code",
  "narration": "Authentication and authorization are related, but they answer different questions. Authentication asks who the user or caller is. Authorization asks what that caller is allowed to do. A TypeScript type can model a user, role, claim, tenant, or request context, but the type alone does not prove that an action is allowed.\n\nSensitive checks should happen in trusted server-side code. API handlers, service functions, middleware, and domain services should verify that the current caller can perform the requested operation. This includes route-level checks, object-level permission checks, tenant boundaries, ownership rules, and capability requirements. It is not enough for the frontend to hide a button or mark a route as protected.\n\nAuthorization logic should be consistent and reviewable. Scattered checks make it harder to know whether every sensitive path is protected. Many applications benefit from shared helpers, policy functions, middleware, or service-layer methods that make permission decisions explicit. The right structure depends on the framework, but the security goal is the same: avoid surprising gaps.\n\nSession-aware code should avoid assumptions that are too broad. A logged-in user may not be allowed to access every object. An administrator in one tenant may not have authority in another. A background job may have different permissions than an interactive request. Deny-by-default behavior, least privilege, and tests for access-control edge cases help keep those assumptions honest.\n\nTypeScript can make these checks easier to review by modeling request contexts, authenticated principals, roles, capabilities, and resource owners. Those models are helpful only when they are tied to real enforcement. Sensitive operations should make the required permission visible at the call site or service boundary, so reviewers can see both the action and the rule protecting it before data changes.",
  "narrationPoints": [
    "Authentication and authorization are related, but they answer different questions.",
    "Sensitive checks should happen in trusted server-side code.",
    "Authorization logic should be consistent and reviewable.",
    "Session-aware code should avoid assumptions that are too broad.",
    "TypeScript can make these checks easier to review by modeling request contexts, authenticated principals, roles, capabilities, and resource owners."
  ]
};
