window.COURSE_MODULE = {
  "title": "Browser and Frontend Security",
  "graphicAlt": "Draft visual summary for Browser and Frontend Security",
  "narration": "Frontend TypeScript can make browser code more organized, but it does not automatically make rendering safe. Framework escaping, safe templating, and predictable component patterns are important defenses. Developers should be careful with APIs that insert raw HTML, manually manipulate the DOM, or build URLs from untrusted values. If a framework provides safe rendering conventions, use them intentionally.\n\nBrowser storage also needs care. Values placed in local storage, session storage, IndexedDB, or other browser-accessible locations can be read by browser code. Sensitive data should not be stored there casually. Cookies, tokens, session state, and framework authentication helpers need to be understood at a high level so teams know which values are accessible to browser code and which are protected by server-side controls.\n\nClient-side checks can improve usability, but they are not final enforcement. Route guards, hidden buttons, disabled controls, and client-side role checks can guide a user through the interface. They should not be the only thing protecting backend data or sensitive operations. The server must enforce authorization because server-side code is the trusted place to make those decisions.\n\nOther browser security settings matter too. Content security policy, CSRF protections, secure cookie options, same-site behavior, and framework defaults should be reviewed in the context of the application. The practical habit is to let TypeScript clarify frontend state while relying on secure rendering, framework-aware configuration, and trusted server enforcement for security decisions.\n\nFrontend code should also avoid placing sensitive assumptions in scattered components. A shared helper for safe URL creation, a small wrapper for trusted rendering patterns, or documented storage rules can reduce inconsistency. The safer pattern is to make the secure path easy for ordinary feature work, not something every component has to rediscover during each change.",
  "narrationPoints": [
    "Frontend TypeScript can make browser code more organized, but it does not automatically make rendering safe.",
    "Browser storage also needs care.",
    "Client-side checks can improve usability, but they are not final enforcement.",
    "Other browser security settings matter too.",
    "Frontend code should also avoid placing sensitive assumptions in scattered components."
  ]
};
