window.COURSE_MODULE = {
  "title": "Validating Web and API Input",
  "graphicAlt": "Blank placeholder image for Validating Web and API Input.",
  "narration": "Web and API input should be validated at the point where the application first interprets it. For REST endpoints, that often means validating the request body, route parameters, query string, headers, and cookies before calling business services. For GraphQL, variables and custom scalar values need the same kind of attention, even though the framework provides some structural checks. For forms, validation should account for required fields, length limits, formats, and the difference between an empty value and a missing value.\n\nFile-related input deserves special care. The file content may be handled elsewhere, but metadata such as filename, declared MIME type, size, extension, and user-supplied description can still affect storage paths, logging, authorization, and downstream workflows. Authentication-related inputs are also sensitive. Tokens, session identifiers, redirect targets, user IDs, tenant IDs, and role-related claims should be checked against expected formats and verified against trusted server-side state before they influence access decisions.\n\nMiddleware is useful for broad, repeated validation such as JSON body size, content type, authentication header shape, and common parameter parsing. Endpoint-level validation is still needed because each endpoint has its own contract. A profile update, a billing change, and an administrative action may all accept JSON, but they should not accept the same fields or the same combinations of values. Validation should be specific enough to prevent accidental over-posting and intentional field injection.\n\nError responses should help legitimate clients correct their requests without giving away unnecessary internal detail. It is reasonable to say that a field is missing, a value is invalid, or a payload does not match the expected schema. It is usually not reasonable to return stack traces, raw database errors, secret configuration values, or detailed internal object models. Good API validation makes bad input predictable: reject early, explain safely, log responsibly, and keep business logic focused on already-validated data.",
  "narrationPoints": [
    "Web and API input should be validated at the point where the application first interprets it.",
    "For REST endpoints, that often means validating the request body, route parameters, query string, headers, and cookies before calling business services.",
    "For GraphQL, variables and custom scalar values need the same kind of attention, even though the framework provides some structural checks.",
    "For forms, validation should account for required fields, length limits, formats, and the difference between an empty value and a missing value.",
    "File-related input deserves special care.",
    "The file content may be handled elsewhere, but metadata such as filename, declared MIME type, size, extension, and user-supplied description can still affect storage."
  ]
};
