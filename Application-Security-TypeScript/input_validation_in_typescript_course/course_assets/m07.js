window.COURSE_MODULE = {
  "title": "Common TypeScript Validation Failures",
  "graphicAlt": "Blank placeholder image for Common TypeScript Validation Failures.",
  "narration": "The most common failure is trusting TypeScript types for runtime data. A request body annotated as AdminUpdateRequest is still whatever the client sent until runtime validation proves otherwise. Unsafe any and broad type assertions make this worse by removing compiler pressure at the exact places where discipline is needed most. During review, any use of any, unknown-to-specific casts, or large assertions near an input boundary should raise a simple question: where was this data actually checked.\n\nMass assignment is another frequent problem. It occurs when user-controlled fields are copied into an object that has properties the user should not control. In TypeScript, this can look deceptively clean because object spread and partial update types are convenient. Validation should reject unexpected fields, map allowed fields intentionally, and keep server-controlled properties such as role, ownerId, tenantId, approval status, and audit fields out of user-controlled assignment paths.\n\nPrototype pollution, path traversal, command injection risks, and unsafe deserialization patterns all become more likely when validation is vague. At a high level, prototype pollution involves unsafe object handling that can influence inherited behavior. Path traversal involves treating user input as part of a filesystem path without strict constraints and safe resolution. Command injection risk appears when unchecked strings influence shell commands or process execution. Defensive validation does not make dangerous APIs safe by magic, but it narrows the inputs and supports safer design choices.\n\nFrontend and backend validation gaps are also common. A React form may prevent invalid input in normal use, but an attacker or tester can send requests directly to the backend. A mobile client may lag behind the server contract. A partner integration may omit fields after a deployment. The backend should validate as the authority. The frontend should validate for usability. Treating those as separate responsibilities prevents a friendly user experience from being mistaken for a security control.",
  "narrationPoints": [
    "The most common failure is trusting TypeScript types for runtime data.",
    "A request body annotated as AdminUpdateRequest is still whatever the client sent until runtime validation proves otherwise.",
    "Unsafe any and broad type assertions make this worse by removing compiler pressure at the exact places where discipline is needed most.",
    "During review, any use of any, unknown-to-specific casts, or large assertions near an input boundary should raise a simple question: where was this data.",
    "Mass assignment is another frequent problem.",
    "It occurs when user-controlled fields are copied into an object that has properties the user should not control."
  ]
};
