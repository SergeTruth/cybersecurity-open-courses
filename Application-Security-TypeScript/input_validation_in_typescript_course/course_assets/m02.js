window.COURSE_MODULE = {
  "title": "TypeScript Trust Boundaries",
  "graphicAlt": "Blank placeholder image for TypeScript Trust Boundaries.",
  "narration": "A trust boundary is any point where data moves from a less trusted context into code that wants to make decisions. In a TypeScript web service, the obvious boundary is the API request body, but it is only one part of the picture. Query parameters, route parameters, headers, and cookies all arrive as external input. They may be missing, duplicated, incorrectly encoded, too long, or intentionally shaped to trigger edge cases in routing, authorization, logging, or database access.\n\nBrowser-side TypeScript has its own boundaries. Values from localStorage and sessionStorage should not be treated as trusted just because the application wrote them earlier. A user can edit them, an extension can change them, and an older version of the application may have stored a shape the current code no longer expects. WebSocket messages and postMessage events also deserve careful checks because they often carry loosely structured data between contexts that can change independently.\n\nServer-side code has boundaries that are easy to forget during review. Environment variables are strings, even when the application expects numbers, booleans, URLs, feature flags, or comma-separated lists. JSON files and configuration files can be stale or malformed. Third-party SDK responses can change after an upstream API update. Even internal queues and event streams should be validated at the consumer, because producer bugs and schema drift eventually happen in real systems.\n\nThe practical habit is to identify every place where unknown data enters typed code and put a narrow validation layer there. That does not mean scattering checks randomly throughout the application. It means recognizing boundaries, naming them in code, and converting inputs into well-defined internal objects as early as the design reasonably allows. The result is clearer code: downstream functions can depend on validated shapes instead of repeating defensive checks or silently accepting unsafe assumptions.",
  "narrationPoints": [
    "A trust boundary is any point where data moves from a less trusted context into code that wants to make decisions.",
    "In a TypeScript web service, the obvious boundary is the API request body, but it is only one part of the picture.",
    "Query parameters, route parameters, headers, and cookies all arrive as external input.",
    "They may be missing, duplicated, incorrectly encoded, too long, or intentionally shaped to trigger edge cases in routing, authorization, logging, or database access.",
    "Browser-side TypeScript has its own boundaries.",
    "Values from localStorage and sessionStorage should not be treated as trusted just because the application wrote them earlier."
  ]
};
