window.COURSE_MODULE = {
  "title": "Why Input Validation Matters in TypeScript",
  "graphicAlt": "Blank placeholder image for Why Input Validation Matters in TypeScript.",
  "narration": "TypeScript gives developers a much better way to describe application data, but it does not make outside data safe by itself. A request body, a cookie, a message from a browser tab, or a record loaded from storage can still contain values that do not match the type you expected. The compiler can help you reason about your own code, but it cannot force a remote client or a third-party service to send honest, well-formed data.\n\nThe key detail is that TypeScript types are erased when the code is compiled to JavaScript. Interfaces, type aliases, and annotations guide the compiler and the editor, but they are not runtime checks. When the application is actually running, a value that was declared as a User, Order, or Settings object is just JavaScript data. If that value came from outside the trusted codebase, the application needs explicit runtime validation before relying on it.\n\nExternal input shows up in more places than teams usually list during design. Browsers submit forms. APIs send JSON. Files are uploaded. Queues deliver messages. Environment variables configure services. Local storage, session storage, and cookies can be changed by users or extensions. Partner SDKs may return unexpected shapes when versions drift. Each of those sources is a boundary where the program should move carefully from untrusted runtime values into typed application logic.\n\nA healthy TypeScript validation strategy treats validation as a bridge. On one side of the bridge, data is unknown because it came from a source the application does not control. On the other side, the program has domain objects that are safe enough to use for business decisions. Validation, parsing, normalization, and clear error handling are what make that bridge trustworthy. Without that step, TypeScript can create a false sense of confidence around data the runtime never actually checked.",
  "narrationPoints": [
    "TypeScript gives developers a much better way to describe application data, but it does not make outside data safe by itself.",
    "A request body, a cookie, a message from a browser tab, or a record loaded from storage can still contain values that do not.",
    "The compiler can help you reason about your own code, but it cannot force a remote client or a third-party service to send honest.",
    "The key detail is that TypeScript types are erased when the code is compiled to JavaScript.",
    "Interfaces, type aliases, and annotations guide the compiler and the editor, but they are not runtime checks.",
    "When the application is actually running, a value that was declared as a User, Order, or Settings object is just JavaScript data."
  ]
};
