window.COURSE_MODULE = {
  "title": "Checking Reads and Writes Before Access",
  "graphicAlt": "Bullet summary graphic for Checking Reads and Writes Before Access.",
  "narration": "The core rule of bounds-safe C is direct: check before access. Validate an index before using it to read or write. Validate a range before copying, appending, parsing, formatting, or advancing a cursor. The validation should happen before pointer arithmetic or array access, not after the program has already stepped outside the valid region.\n\nA single index check is not enough when an operation touches a range. The code should validate both the start offset and the requested length. A start offset may be in range while the range from start through start plus length exceeds the buffer. Addition used for range checks should also be reviewed so the calculation itself does not overflow.\n\nAvoid performing pointer arithmetic before confirming the range is valid. Creating a pointer far outside the object and then checking it later can make the code difficult to reason about and may already violate C's object boundary rules. Prefer checking offsets and lengths as integer values first, then deriving the pointer once the range is known to be valid.\n\nRepeated bounds logic is a good candidate for small helper functions. A helper that checks whether a range is within a buffer, appends only when capacity allows, or validates an index before returning a pointer can reduce copy-paste mistakes. The helper's contract should be precise: accepted units, allowed ranges, output state, and failure result.\n\nFailure behavior should be explicit and safe. If the requested read or write is out of range, the function should return an error, reject the input, leave the output unchanged, or reset the output to a known safe state according to its contract. A partially updated buffer with ambiguous count can create a second defect after the original bounds failure.\n\nGood range checks are also readable. The next reviewer should be able to see what object is protected, which size belongs to that object, which operation is being authorized, and what happens when the operation does not fit. That clarity is as important as the check itself.",
  "narrationPoints": [
    "The validation should happen before pointer arithmetic or array access, not after the program has already stepped outside the valid region.",
    "The code should validate both the start offset and the requested length.",
    "Prefer checking offsets and lengths as integer values first, then deriving the pointer once the range is known to be valid.",
    "A helper that checks whether a range is within a buffer, appends only when capacity allows.",
    "If the requested read or write is out of range, the function should return an error, reject the input, leave the output unchanged."
  ]
};
