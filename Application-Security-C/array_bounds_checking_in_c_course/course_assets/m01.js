window.COURSE_MODULE = {
  "title": "Why Array Bounds Checking Matters",
  "graphicAlt": "Bullet summary graphic for Why Array Bounds Checking Matters.",
  "narration": "Array bounds checking matters in C because the language gives developers direct control over memory while relying on code to stay inside valid object boundaries. That control is one reason C remains useful for systems programming, embedded work, performance-sensitive components, and low-level libraries. It also means an array access that looks ordinary can become unsafe if the index, length, offset, or capacity was misunderstood.\n\nC does not automatically check every array access at runtime. An expression such as an indexed read or write depends on the programmer's reasoning about the valid range. If that reasoning is wrong, the program may read from or write to memory outside the intended array. The result may vary by build, platform, data shape, and surrounding code.\n\nOut-of-bounds reads can produce unreliable results, expose unintended data to later logic, or make decisions based on bytes that are not part of the intended object. Even when the program does not crash, it may be using data that the developer never meant to consider. That is enough to break validation, parsing, comparison, logging, or control decisions.\n\nOut-of-bounds writes are often more damaging because they can corrupt nearby state, change a length field, disturb a pointer, break a data structure, or crash the process. The safe way to discuss this in engineering terms is not to dwell on misuse. It is to recognize that a single unchecked write can make the program's future behavior untrustworthy.\n\nMost bounds problems begin as ordinary engineering mistakes: a wrong loop condition, a count confused with capacity, an unchecked input length, an offset added before validation, or an assumption that a terminator exists. Defensive C treats indexes, lengths, offsets, and capacities as first-class design concerns. The rest of this course builds habits for making those concerns visible and reviewable.",
  "narrationPoints": [
    "Array bounds checking matters in C because the language gives developers direct control over memory.",
    "C does not automatically check every array access at runtime.",
    "Out-of-bounds reads can produce unreliable results, expose unintended data to later logic.",
    "Out-of-bounds writes are often more damaging because they can corrupt nearby state, change a length field, disturb a pointer.",
    "Most bounds problems begin as ordinary engineering mistakes: a wrong loop condition, a count confused with capacity."
  ]
};
