window.COURSE_MODULE = {
  "title": "How C Represents Arrays, Pointers, and Memory",
  "graphicAlt": "Bullet summary graphic for How C Represents Arrays, Pointers, and Memory.",
  "narration": "A C array is a contiguous sequence of elements of the same type. If an array has ten int elements, valid element indexes run from zero through nine. The array object has a fixed extent, and the code must keep reads and writes inside that extent. This simple rule becomes harder to preserve when arrays are passed through functions, converted to pointers, or paired with separately stored length values.\n\nPointer arithmetic is scaled by element size. Advancing an int pointer by one moves to the next int, not to the next byte. Advancing a structure pointer by one moves by the size of the structure. This scaling is useful, but it also means reviewers must know which type the pointer has and what object it is supposed to traverse.\n\nArray names often decay to pointers when passed to functions. A function parameter written to look like an array parameter is usually treated as a pointer parameter. Once that decay occurs, the callee generally receives an address, not a complete array object with its original length attached. The original capacity must be passed separately or represented through a surrounding structure.\n\nA pointer by itself does not carry reliable bounds information. It may point to the first element of an array, the middle of an array, a single object, allocated storage, a string literal, or storage owned by another component. Without a valid length or contract, a callee cannot safely infer how many elements are available.\n\nThe sizeof operator is a frequent source of review mistakes. Applied to an actual array in the same scope, sizeof can report the size of the whole array object. Applied to a pointer parameter, it reports the size of the pointer itself, not the pointed-to region. Code should not rely on a callee recovering an original array length with sizeof after array-to-pointer decay.\n\nDefensive APIs make this behavior explicit. If a function reads a buffer, pass the number of valid elements or bytes available to read. If a function writes, pass the destination capacity. If it appends, pass both current count and capacity or use a structured buffer type. The pointer is only one part of the contract.",
  "narrationPoints": [
    "The array object has a fixed extent, and the code must keep reads and writes inside that extent.",
    "A function parameter written to look like an array parameter is usually treated as a pointer parameter.",
    "A pointer by itself does not carry reliable bounds information.",
    "Code should not rely on a callee recovering an original array length with sizeof after array-to-pointer decay.",
    "If it appends, pass both current count and capacity or use a structured buffer type."
  ]
};
