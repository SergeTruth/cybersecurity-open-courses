window.COURSE_MODULE = {
  "title": "Common Bounds Mistakes",
  "graphicAlt": "Bullet summary graphic for Common Bounds Mistakes.",
  "narration": "Common bounds mistakes are usually not mysterious. They are small gaps between what the code assumes and what the data actually guarantees. A loop runs one step too far. A size belongs to one buffer but is used with another. A negative value is accepted as an index. A length from a file or message is trusted before the buffer it describes has been validated.\n\nOff-by-one loop conditions are a classic example. For a zero-based array with count elements, valid indexes are less than count. A loop condition that allows i equal to count steps one position beyond the final element. Reviewers should pay close attention to code that uses less-than-or-equal comparisons around array indexes, especially when the variable name suggests a count rather than a final valid index.\n\nNegative indexes arise when signed values are not validated before use. A value may come from parsing, arithmetic, a search result, a sentinel, or a conversion. If negative values are not meaningful for the operation, reject them before converting or indexing. Converting a negative signed value to an unsigned type can produce a very large value rather than a helpful error.\n\nUnsigned wraparound can appear when subtracting sizes or computing remaining length. If code subtracts a larger value from a smaller unsigned value, the result wraps within the unsigned range. That wrapped result can look like a large available length unless the code first checks that the subtraction is valid.\n\nUsing one buffer's size to access another buffer is another everyday mistake. Similar names, parallel arrays, copied code, and reused helper functions can hide the mismatch. The size value should describe the exact object being accessed, and interfaces should make that relationship hard to confuse.\n\nSentinel assumptions also need review. A character array is not necessarily a valid string, and a parsed region may not contain a terminator. Code that walks until a sentinel should first know that the sentinel exists within the valid bounds or use a length-limited operation. Bounds safety depends on facts, not on the hope that data arrives in a friendly shape.",
  "narrationPoints": [
    "A length from a file or message is trusted before the buffer it describes has been validated.",
    "Reviewers should pay close attention to code that uses less-than-or-equal comparisons around array indexes.",
    "Negative indexes arise when signed values are not validated before use.",
    "The size value should describe the exact object being accessed, and interfaces should make that relationship hard to confuse.",
    "Bounds safety depends on facts, not on the hope that data arrives in a friendly shape."
  ]
};
