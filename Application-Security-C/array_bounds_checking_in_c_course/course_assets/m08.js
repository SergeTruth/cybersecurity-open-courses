window.COURSE_MODULE = {
  "title": "Defensive Refactoring Patterns",
  "graphicAlt": "Bullet summary graphic for Defensive Refactoring Patterns.",
  "narration": "Defensive refactoring is often the practical path to better bounds checking. Many C codebases contain useful legacy code with repeated indexes, magic numbers, hand-written copies, local assumptions, and APIs that predate current review standards. The goal is to improve safety without pretending the whole codebase can be rewritten at once.\n\nReplace magic numbers with named constants, derived sizes, or values tied to the actual object being accessed. A literal number may be correct today, but it is hard to audit when the array changes. A named limit or derived count communicates intent and reduces the chance that one caller updates a buffer without updating the matching check.\n\nCentralize repeated bounds checks where patterns repeat. If several functions validate an offset and length in the same way, a reviewed helper can reduce divergence. If several functions append to the same dynamic buffer type, a single append helper can maintain count, capacity, and terminator behavior consistently.\n\nSmall helper functions can make checked indexing, appending, copying, and reserving easier to review. The helper should not hide complexity behind a vague name. It should document units, accepted ranges, output behavior, and failure handling. A good helper makes the calling code shorter and the safety contract more obvious.\n\nStructured buffer metadata can keep pointer, count, and capacity together. That does not make code automatically safe, but it makes invalid states harder to represent and easier to detect. A struct can also carry unit conventions, ownership rules, and helper functions that operate on the buffer consistently.\n\nRefactor incrementally when working with legacy code. Start at boundaries where input enters, where buffers are copied, where strings are formed, or where repeated review findings appear. Preserve behavior with tests, then make the bounds contract clearer. Small, visible improvements compound into a codebase where safe access is easier to maintain.",
  "narrationPoints": [
    "Defensive refactoring is often the practical path to better bounds checking.",
    "A named limit or derived count communicates intent and reduces the chance that one caller updates a buffer without updating the matching check.",
    "If several functions validate an offset and length in the same way, a reviewed helper can reduce divergence.",
    "Structured buffer metadata can keep pointer, count, and capacity together.",
    "Start at boundaries where input enters, where buffers are copied, where strings are formed, or where repeated review findings appear."
  ]
};
