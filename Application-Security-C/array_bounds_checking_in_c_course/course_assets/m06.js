window.COURSE_MODULE = {
  "title": "Strings, Terminators, and Length-Limited Operations",
  "graphicAlt": "Bullet summary graphic for Strings, Terminators, and Length-Limited Operations.",
  "narration": "C strings are arrays of characters terminated by a null character. That convention is powerful and simple, but it also means that string operations depend on a terminator being present within the valid bounds. A character array that holds bytes is not automatically a valid C string.\n\nA buffer filled from a file, socket, device, parser, or fixed-width record may contain character data without a terminator. It may also contain embedded null bytes. Treating that region as a C string without checking its bounds can cause a function to read past the intended data while searching for the terminator.\n\nThe strlen function assumes a terminator can be found. It does not know the capacity of the underlying buffer unless the data itself contains a null character before the boundary. If the data may be unterminated, use length-aware logic that receives the valid buffer length and stops within that range.\n\nCopying into fixed-size buffers requires space for the terminator when the destination is meant to become a C string. A destination with capacity N can hold at most N minus one ordinary characters plus the null terminator. Off-by-one mistakes around this rule are common because the content length and storage capacity are close but not identical.\n\nTruncation can be safer than writing past the destination, but it must be handled deliberately. A truncated name, path, identifier, token, or message may no longer mean the same thing. The function should report truncation or make it clear that shortened output is an acceptable result for that specific use.\n\nLength-limited operations are useful only when their parameters are correct and their results are checked. Prefer APIs and helper functions that keep destination capacity, source length, resulting length, and terminator behavior visible. String safety is buffer safety plus terminator rules, not a separate magic category.",
  "narrationPoints": [
    "Treating that region as a C string without checking its bounds can cause a function to read past the intended data.",
    "If the data may be unterminated, use length-aware logic that receives the valid buffer length and stops within that range.",
    "Off-by-one mistakes around this rule are common because the content length and storage capacity are close but not identical.",
    "Truncation can be safer than writing past the destination, but it must be handled deliberately.",
    "Prefer APIs and helper functions that keep destination capacity, source length, resulting length, and terminator behavior visible."
  ]
};
