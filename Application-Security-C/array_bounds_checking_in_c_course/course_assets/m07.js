window.COURSE_MODULE = {
  "title": "Dynamic Buffers, Reallocation, and Capacity Growth",
  "graphicAlt": "Bullet summary graphic for Dynamic Buffers, Reallocation, and Capacity Growth.",
  "narration": "Dynamically allocated buffers do not remove the need for bounds checking. They can grow, but only when the program explicitly tracks count, capacity, and ownership. A heap buffer with unclear capacity is no safer than a local array with unclear capacity. The difference is lifetime and resizing behavior, not automatic bounds knowledge.\n\nCapacity growth should avoid integer overflow. Doubling a capacity, adding a record size, reserving space for a terminator, or growing by a requested length all involve arithmetic that can exceed the range of the size type. Check the arithmetic before allocating so the new capacity actually represents the intended storage.\n\nThe realloc function should be handled carefully because failure leaves the original allocation unchanged. Assigning the result directly back to the only pointer can lose the ability to free or continue using the original buffer when the resize fails. A common defensive pattern is to assign the result to a temporary pointer, check it, and then update the buffer state only on success.\n\nAppending requires checking available space before writing. If capacity is insufficient, the code should grow the buffer safely or reject the append. After success, update the count consistently. After failure, preserve the old count, old capacity, and old contents according to the buffer contract.\n\nInitialization and cleanup should preserve valid program state. A dynamic buffer object should start with known pointer, count, and capacity values. Cleanup should free owned storage and reset metadata if the object may be reused. Error paths should not leave a buffer with a pointer from one allocation and a capacity from another.\n\nOwnership and lifetime should be clear. The code should state who allocates, who grows, who frees, and whether callers may keep pointers into the buffer across growth operations. Reallocation can move storage, so retained interior pointers may become stale after a successful grow. A bounds-safe design treats resize as both a capacity event and a pointer-lifetime event.",
  "narrationPoints": [
    "The difference is lifetime and resizing behavior, not automatic bounds knowledge.",
    "The realloc function should be handled carefully because failure leaves the original allocation unchanged.",
    "After failure, preserve the old count, old capacity, and old contents according to the buffer contract.",
    "Error paths should not leave a buffer with a pointer from one allocation and a capacity from another.",
    "A bounds-safe design treats resize as both a capacity event and a pointer-lifetime event."
  ]
};
