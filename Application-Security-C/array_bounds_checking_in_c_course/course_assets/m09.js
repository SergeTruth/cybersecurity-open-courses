window.COURSE_MODULE = {
  "title": "Tooling, Testing, and Review Practices",
  "graphicAlt": "Bullet summary graphic for Tooling, Testing, and Review Practices.",
  "narration": "Tooling helps find bounds defects earlier, but it does not replace clear contracts. Compiler warnings, static analysis, sanitizers, assertions, and boundary-focused tests all see different parts of the problem. The strongest practice is to combine them with APIs that carry size information and code review that traces how that size is used.\n\nCompiler warnings can catch suspicious patterns such as signed and unsigned comparisons, array bounds concerns, truncation, ignored return values, and questionable conversions. Warnings should be treated as review items, especially when a warning appears near indexing, copying, formatting, or parser advancement.\n\nStatic analysis can identify risky flows and suspicious indexes across functions. It can follow input-derived lengths, offsets, pointer parameters, and capacity values through code paths that are tedious to inspect manually. Static analysis is most useful when the codebase has consistent naming and helper patterns that make intent visible.\n\nRuntime sanitizers can expose out-of-bounds reads and writes during testing. They are especially valuable when tests cover empty buffers, single-element buffers, maximum accepted sizes, one-past-boundary values, malformed records, missing terminators, and resize failures. A sanitizer finding should become both a fix and a regression test.\n\nAssertions can document invariants during development. They can express assumptions such as index less than count, count no greater than capacity, terminator space available, or offset plus length within range. Assertions are not a substitute for runtime validation of external input, but they make internal assumptions visible and easier to test.\n\nFuzzing and boundary-focused tests can exercise unusual sizes and inputs at a safe defensive level. The goal is to find crashes, unexpected behavior, and missing rejection paths before release. Code review should then connect those findings back to API contracts, helper functions, and documented bounds rules.",
  "narrationPoints": [
    "The strongest practice is to combine them with APIs that carry size information and code review that traces how that size is used.",
    "Compiler warnings can catch suspicious patterns such as signed and unsigned comparisons, array bounds concerns, truncation.",
    "Static analysis is most useful when the codebase has consistent naming and helper patterns that make intent visible.",
    "A sanitizer finding should become both a fix and a regression test.",
    "Code review should then connect those findings back to API contracts, helper functions, and documented bounds rules."
  ]
};
