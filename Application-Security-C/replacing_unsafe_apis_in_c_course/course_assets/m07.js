window.COURSE_MODULE = {
  "title": "Designing Safe Wrappers and Helper Functions",
  "graphicAlt": "Bullet summary graphic for Designing Safe Wrappers and Helper Functions.",
  "narration": "Small project-specific wrappers can make unsafe API migration more maintainable. When many call sites need the same buffer checks, truncation policy, path validation, or return-value handling, a wrapper can centralize the rule and make future review easier.\n\nA good wrapper makes the contract more visible. It should expose destination capacity, current used length where relevant, ownership expectations, truncation behavior, and failure reporting. The caller should be able to tell whether the operation completed, whether data was truncated, and who owns any returned allocation.\n\nA wrapper that hides errors can be worse than the original call. If it silently truncates data, swallows conversion failures, or returns a partial result without a clear status, it creates a false sense of safety. The purpose of a wrapper is not to make errors disappear. It is to make errors consistent and hard to ignore.\n\nStructured buffer types can help keep pointer, capacity, used length, and ownership together. They reduce the chance that a function receives a pointer from one buffer and the size from another. They also make repeated operations easier to reason about because the same fields travel together.\n\nHelpers should stay simple enough for reviewers to understand. A large reusable framework can become another opaque layer, especially in legacy C code. For most migrations, small focused helpers are better: one for bounded formatting, one for validated numeric conversion, one for safe file creation, or one for buffer append behavior.\n\nThe wrapper design should include tests. Normal input, boundary input, truncation, allocation failure where practical, invalid arguments, and cleanup paths should be covered. A wrapper becomes trustworthy when its contract is documented, tested, and used consistently.",
  "narrationPoints": [
    "When many call sites need the same buffer checks, truncation policy, path validation, or return-value handling.",
    "The caller should be able to tell whether the operation completed, whether data was truncated, and who owns any returned allocation.",
    "If it silently truncates data, swallows conversion failures, or returns a partial result without a clear status.",
    "Structured buffer types can help keep pointer, capacity, used length, and ownership together.",
    "Normal input, boundary input, truncation, allocation failure where practical, invalid arguments."
  ]
};
