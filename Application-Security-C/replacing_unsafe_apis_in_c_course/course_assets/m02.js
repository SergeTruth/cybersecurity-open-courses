window.COURSE_MODULE = {
  "title": "Recognizing Risky API Patterns",
  "graphicAlt": "Bullet summary graphic for Recognizing Risky API Patterns.",
  "narration": "A useful review skill is learning to recognize risky API patterns before focusing on any single function name. Missing size arguments are one of the clearest warning signs when a function writes into caller-provided memory. If the callee cannot know the destination capacity, safety depends entirely on assumptions outside the call.\n\nFixed-size buffers deserve the same scrutiny. A fixed buffer can be perfectly acceptable when the maximum input size is enforced before the copy or conversion occurs. It becomes risky when the maximum is only implied by a comment, guessed from a protocol, or assumed because current test data is small.\n\nUnchecked return values are another recurring problem. Many APIs communicate truncation, failed conversion, short reads, incomplete writes, allocation failure, or invalid input through their return value. If the caller ignores that signal, the next line of code may operate on incomplete or invalid data while appearing to succeed.\n\nImplicit null-termination assumptions can turn bounded data into unsafe string handling. A buffer read from a file, socket, environment entry, or binary record may have a known byte count without being a valid C string. Passing that buffer to string-only routines before establishing termination and length can create unreliable behavior.\n\nOwnership and lifetime rules also belong in the contract. Who owns the pointer? Who frees it? How long is the returned value valid? Can it point to internal storage? Can the caller modify it? Ambiguity here can lead to leaks, stale pointers, double cleanup, or use of data after its valid lifetime.\n\nCode review should identify the contract around each pointer, buffer, length, and returned value. The safest call sites make capacity, used length, ownership, and error behavior visible. When those details are hidden, the first task is to reveal the contract before choosing a replacement API.",
  "narrationPoints": [
    "A useful review skill is learning to recognize risky API patterns before focusing on any single function name.",
    "A fixed buffer can be perfectly acceptable when the maximum input size is enforced before the copy or conversion occurs.",
    "Many APIs communicate truncation, failed conversion, short reads, incomplete writes, allocation failure.",
    "A buffer read from a file, socket, environment entry, or binary record may have a known byte count without being a valid C string.",
    "Code review should identify the contract around each pointer, buffer, length, and returned value."
  ]
};
