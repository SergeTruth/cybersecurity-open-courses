window.COURSE_MODULE = {
  "title": "File, Path, Environment, and Process APIs",
  "graphicAlt": "Bullet summary graphic for File, Path, Environment, and Process APIs.",
  "narration": "Unsafe API replacement is not limited to strings and memory. File, path, environment, and process APIs also create boundaries that need clear contracts. A call that opens a file, reads an environment variable, creates a temporary file, or launches another process can carry security and reliability assumptions that are easy to miss.\n\nFile and path APIs become risky when user-controlled strings influence filesystem access without constraints. A safer design defines trusted directories, allowed file names, expected file types, permissions, and whether symbolic links or relative paths are acceptable. Replacing one file API with another does not help if the path policy remains unclear.\n\nTemporary file creation should avoid predictable names and broad permissions. The code should use platform-supported mechanisms that create the file safely rather than building a name and opening it later. The directory, permissions, cleanup behavior, and error handling should all be part of the review.\n\nEnvironment variables should be treated as untrusted process input unless the deployment boundary provides a stronger guarantee. They may be missing, malformed, inherited unexpectedly, or set differently across service managers, shells, containers, and test environments. Code should validate environment-derived values before using them for paths, sizes, modes, or credentials.\n\nProcess launch APIs require careful command construction and environment control. Ambiguous shell-style construction, uncontrolled inherited environment state, and broad file descriptor inheritance can create behavior the caller did not intend. Safer process launch code passes arguments as structured values where supported and gives the child only what it needs.\n\nPlatform-specific safer APIs should be used deliberately and documented when portability matters. A cross-platform project may need a small abstraction, but that abstraction should make access intent, path rules, permissions, and failure behavior explicit rather than hiding important differences.",
  "narrationPoints": [
    "File, path, environment, and process APIs also create boundaries that need clear contracts.",
    "File and path APIs become risky when user-controlled strings influence filesystem access without constraints.",
    "The directory, permissions, cleanup behavior, and error handling should all be part of the review.",
    "Environment variables should be treated as untrusted process input unless the deployment boundary provides a stronger guarantee.",
    "Process launch APIs require careful command construction and environment control."
  ]
};
