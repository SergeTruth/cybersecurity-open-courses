window.COURSE_MODULE = {
  "title": "Refactoring Legacy Code Without Breaking Behavior",
  "graphicAlt": "Bullet summary graphic for Refactoring Legacy Code Without Breaking Behavior.",
  "narration": "Legacy migration should start with an inventory. Code search, compiler warnings, static analysis, and manual review can identify unsafe or discouraged APIs. The next step is classification. A risky function in dead test code is different from the same function in a network-facing parser, installer, update workflow, or privileged service.\n\nEach call site needs context before it changes. What input can reach it? What buffer is being written? What size limit is assumed? What happens today when input is too long or malformed? Which callers depend on that behavior? Without those answers, a replacement can accidentally preserve the weak assumption or reject input that another component still expects.\n\nMechanical replacement is tempting because it feels measurable. The function name changes, the warning disappears, and the diff looks tidy. But a new API called with the wrong size, an unchecked truncation result, or a mismatched ownership rule can leave the program no safer than before. Migration quality is in the contract, not only the name.\n\nRefactoring should update tests for normal input, boundary input, truncation, malformed values, allocation failure where practical, partial I/O, and legacy compatibility. If a new path rejects data that old code accepted, tests should show that the rejection is intentional and understood.\n\nBehavior changes should be documented. Rejecting ambiguous input may be the right secure choice, but operators and dependent components need to know. Documentation also helps future reviewers distinguish intended policy from accidental regression.\n\nIncremental migration is often safer than one large unreviewed rewrite. A team can migrate high-risk call sites first, introduce small helpers, add tests, measure warnings, and enforce new patterns over time. Steady visible progress is usually more reliable than a single sweeping change that nobody can review carefully.",
  "narrationPoints": [
    "Code search, compiler warnings, static analysis, and manual review can identify unsafe or discouraged APIs.",
    "But a new API called with the wrong size, an unchecked truncation result.",
    "Refactoring should update tests for normal input, boundary input, truncation, malformed values, allocation failure where practical.",
    "Rejecting ambiguous input may be the right secure choice, but operators and dependent components need to know.",
    "Steady visible progress is usually more reliable than a single sweeping change that nobody can review carefully."
  ]
};
