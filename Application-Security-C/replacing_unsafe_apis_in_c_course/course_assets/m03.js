window.COURSE_MODULE = {
  "title": "Safer String Copying and Concatenation",
  "graphicAlt": "Bullet summary graphic for Safer String Copying and Concatenation.",
  "narration": "String copying and concatenation are common migration targets because many older APIs copy until they find a terminator. That pattern is fragile when input length is not controlled by the program. A caller may believe that a source is short because it came from a familiar place, but the API itself does not enforce that belief.\n\nSafer string handling begins with explicit destination capacity. The code should know how many bytes the destination can hold, how many bytes are already used, and how much room remains for a terminator when a C string is required. Capacity is not the same as current string length, and neither should be guessed from unrelated constants.\n\nBounded alternatives are helpful, but they do not remove the need for policy. Truncation may be acceptable for a display label and unacceptable for a filesystem path, identifier, command argument, or security-relevant value. The caller should check whether truncation occurred and either reject it, report it, or handle it according to a documented rule.\n\nSome safer-looking functions have surprising behavior. A function may pad the destination, fail to terminate in an edge case, differ across platforms, or be unavailable in a portability target. Migration should follow project standards and platform documentation rather than assuming that any bounded variant is automatically right.\n\nConcatenation requires special care because the destination already contains data. The code must account for current length, remaining capacity, and terminator space. Repeated concatenation into a fixed buffer should be reviewed for both correctness and maintainability because size reasoning becomes harder with each step.\n\nBefore using string-only operations, reviewers should confirm that the data is actually a C string. Data read from a bounded source may need explicit termination, validation, or conversion. A strong review habit is to ask: where did this string become terminated, and how do we know the destination still has capacity?",
  "narrationPoints": [
    "Capacity is not the same as current string length, and neither should be guessed from unrelated constants.",
    "The caller should check whether truncation occurred and either reject it, report it.",
    "A function may pad the destination, fail to terminate in an edge case, differ across platforms.",
    "The code must account for current length, remaining capacity, and terminator space.",
    "A strong review habit is to ask: where did this string become terminated, and how do we know the destination still has capacity."
  ]
};
