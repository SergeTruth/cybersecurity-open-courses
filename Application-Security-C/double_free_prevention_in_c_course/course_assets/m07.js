window.COURSE_MODULE = {
  "title": "Compound Objects and Nested Cleanup",
  "graphicAlt": "Bullet summary graphic for Compound Objects and Nested Cleanup.",
  "narration": "Compound objects make double-free prevention more challenging because ownership can be nested. A structure may own a name buffer, a dynamic array, a set of child objects, and a handle managed by another component. Each member needs a cleanup rule, and those rules need to be consistent with the object's construction path.\n\nAn ownership map is a simple way to reason about nested cleanup. For each field, identify whether the object owns it, borrows it, points into another object, or treats it as optional. That map can live in documentation, comments, naming conventions, or the destroy function's structure, but reviewers need to be able to reconstruct it.\n\nArrays of pointers need per-element ownership rules. Does the array own each pointed-to object? Are some elements borrowed? Are null elements allowed? If insertion fails halfway through, which elements should be released? Without clear answers, cleanup can either leak members or release the same member twice.\n\nPartially constructed compound objects need selective teardown. If the first three fields are initialized and the fourth fails, cleanup should release the first three according to their ownership rules and leave untouched fields alone. Initialization order and cleanup order should be predictable.\n\nDestroy functions should free only memory owned by the object being destroyed. They should not free borrowed data, shared external storage, or pointers owned by containers elsewhere unless the ownership map says that is their job. Hidden cleanup side effects make double-free review much harder.\n\nFor linked, tree-like, or nested structures, keep the explanation high level and contract-based: who owns children, who unlinks nodes, who destroys stored values, and whether traversal cleanup can revisit the same object. Predictable ownership order is more important than clever teardown logic.",
  "narrationPoints": [
    "Compound objects make double-free prevention more challenging because ownership can be nested.",
    "For each field, identify whether the object owns it, borrows it, points into another object, or treats it as optional.",
    "Initialization order and cleanup order should be predictable.",
    "Destroy functions should free only memory owned by the object being destroyed.",
    "For linked, tree-like, or nested structures, keep the explanation high level and contract-based: who owns children, who unlinks nodes."
  ]
};
