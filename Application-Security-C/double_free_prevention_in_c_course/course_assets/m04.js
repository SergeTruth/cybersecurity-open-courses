window.COURSE_MODULE = {
  "title": "Cleanup Path Design",
  "graphicAlt": "Bullet summary graphic for Cleanup Path Design.",
  "narration": "Double-free defects often come from cleanup paths that evolved faster than the ownership model. A function may start with one allocation and one return. Later it gains more allocations, validation checks, early exits, and helper calls. If every branch grows its own cleanup logic, the same allocation can become eligible for release in more than one place.\n\nCentralized cleanup can reduce that risk when a function owns several resources. A single cleanup section can release only what was acquired, in a predictable order, and return one status. This is not a rule that every function must have one exit. It is a design option that makes release behavior easier to review when branching becomes complex.\n\nCleanup should free only resources that were initialized and owned by the current code. Flags, null-initialized fields, construction state, and ownership transfer markers can all help cleanup logic decide what is safe to release. The important part is that the decision is based on tracked state, not on duplicated guesses.\n\nAvoid freeing the same member in several branches unless the state transition makes it impossible for another branch to free it again. Duplicated release logic should trigger a review question: can these paths both run for the same object? If the answer is hard to prove, the structure needs simplification.\n\nError handling should leave predictable state for the caller. An output pointer should be unchanged, null, or clearly owned according to the contract. A partially constructed object should not escape with ambiguous cleanup responsibility. A transferred object should not also be cleaned up by the old owner.\n\nBefore adding a new return or failure case, review the cleanup flow. Identify what has been allocated, what has been transferred, what remains borrowed, and what must be released exactly once. Cleanup paths deserve the same engineering attention as successful execution.",
  "narrationPoints": [
    "Double-free defects often come from cleanup paths that evolved faster than the ownership model.",
    "Cleanup should free only resources that were initialized and owned by the current code.",
    "Duplicated release logic should trigger a review question: can these paths both run for the same object.",
    "An output pointer should be unchanged, null, or clearly owned according to the contract.",
    "Before adding a new return or failure case, review the cleanup flow."
  ]
};
