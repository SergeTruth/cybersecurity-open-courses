window.COURSE_MODULE = {
  "title": "Integers, Endianness, and Overflow",
  "graphicAlt": "Bullet summary graphic for Integers, Endianness, and Overflow.",
  "narration": "Binary formats usually define numeric fields with exact widths and byte order. A field may be a 16-bit unsigned little-endian value, a 32-bit big-endian length, or a signed value with a specific interpretation. The parser should decode those fields explicitly from bytes rather than relying on whatever layout happens to exist on the host running the code.\n\nEndianness is part of the contract. If the format says a value is little-endian, the parser assembles it as little-endian even on a big-endian host. If the format says big-endian, the parser assembles it as big-endian even on a little-endian host. Explicit decoding avoids accidental portability defects and makes the code's assumptions visible.\n\nSignedness needs the same care. A value read as unsigned may later be converted to signed, compared against signed limits, or used in an expression where integer promotion changes the result. Those conversions can change meaning. Defensive code chooses types intentionally, checks ranges before conversion, and avoids using negative or wrapped values as sizes, indexes, or offsets.\n\nOverflow checks are essential whenever parsed numbers drive allocation, indexing, copying, or pointer movement. Multiplying a count by an element size is a classic example. The individual count may be within range, and the element size may be reasonable, but the product may not fit the type used for allocation. The calculation should be checked before it becomes a size passed to memory management or a loop bound.\n\nThe parser should also reject values that cannot be represented safely in the types it uses internally. If the format allows a 64-bit length but the implementation can only handle sizes that fit in size_t on the current platform, that limit must be enforced deliberately. Clear implementation limits are better than accidental wraparound.",
  "narrationPoints": [
    "Binary formats usually define numeric fields with exact widths and byte order.",
    "Explicit decoding avoids accidental portability defects and makes the code's assumptions visible.",
    "A value read as unsigned may later be converted to signed, compared against signed limits.",
    "Overflow checks are essential whenever parsed numbers drive allocation, indexing, copying, or pointer movement.",
    "The parser should also reject values that cannot be represented safely in the types it uses internally."
  ]
};
