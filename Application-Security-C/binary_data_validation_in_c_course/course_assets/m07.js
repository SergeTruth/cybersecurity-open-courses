window.COURSE_MODULE = {
  "title": "Format Consistency and Cross-Field Validation",
  "graphicAlt": "Bullet summary graphic for Format Consistency and Cross-Field Validation.",
  "narration": "A binary input can pass individual field checks and still be invalid as a whole. A length may be within the maximum, an offset may be inside the file, and a count may be nonzero, but the combination may not make sense. Cross-field validation checks the relationships that give the format coherence.\n\nMagic values and version fields are basic consistency checks. They help confirm that the parser is handling the intended format and that the rest of the rules are appropriate. If the magic value is wrong or the version is unsupported, continuing as if the input were familiar creates unnecessary risk.\n\nCounts should match what can actually fit in the input. If a table claims to contain fifty records, the parser should verify that fifty records of the expected minimum size fit in the declared region. If records have variable-length fields, each record needs its own validation. The count is a claim, not a guarantee.\n\nOffsets should point inside the expected data region. A pointer to a string table, payload, or metadata section should not silently point into a header or into unrelated data unless the format explicitly allows that. Overlap rules should be documented and checked. Some formats permit shared regions or references; others do not. The parser should not invent that policy while running.\n\nReserved fields and unsupported flags are another form of consistency. If a flag changes the meaning of later data and the parser does not support it, the parser should reject the input or follow a documented compatibility rule. Ignoring unknown semantics can make later validation misleading.\n\nChecksums and integrity fields can support validation, but they do not replace bounds checks, type checks, or consistency checks. A checksum may show that data was not accidentally changed, but it does not prove the data is safe to parse. Treat integrity verification as one layer in a broader validation process.",
  "narrationPoints": [
    "A binary input can pass individual field checks and still be invalid as a whole.",
    "Magic values and version fields are basic consistency checks.",
    "If a table claims to contain fifty records, the parser should verify that fifty records of the expected minimum size fit in the declared region.",
    "The parser should not invent that policy while running.",
    "Checksums and integrity fields can support validation, but they do not replace bounds checks, type checks, or consistency checks."
  ]
};
