window.COURSE_MODULE = {
  "title": "Binary Data as an Explicit Contract",
  "graphicAlt": "Bullet summary graphic for Binary Data as an Explicit Contract.",
  "narration": "A binary format is a contract. The producer agrees to write bytes in a certain order, with certain meanings, and the consumer agrees to interpret those bytes only when the contract is satisfied. This applies whether the format is a public standard, an internal file layout, a network message, a memory snapshot, or a small structure stored on disk.\n\nThat contract may include a magic value that identifies the format, a header that describes the rest of the data, a version field, flags, lengths, offsets, record counts, checksums, ordering rules, and limits on what combinations are valid. These are not decorative fields. They are the parser's first opportunity to decide whether it is looking at the expected thing and whether it can safely continue.\n\nGood validation happens before deeper parsing. The parser should identify the format, confirm the version is supported, reject or deliberately ignore unsupported flags according to policy, and check that required fields are present. Reserved fields deserve special attention. Sometimes the safest choice is to require them to be zero so future meanings do not get misread by older code. In other cases, compatibility requires ignoring them, but that should be an explicit design choice.\n\nFormat documentation matters because it keeps the parser from becoming a collection of folklore. The documentation should define required fields, optional fields, valid ranges, byte order, alignment expectations, size limits, how records are ordered, what counts mean, and what failure behavior is expected. When the documentation is thin, developers tend to infer rules from examples, and examples rarely cover malformed or borderline input.\n\nThe code should make its format assumptions easy to review. Named constants, small validation helpers, clear error paths, and staged parsing make it easier for another engineer to trace why a value is considered safe. A binary parser is not only a decoder; it is the executable expression of the format contract. If that contract is hidden in casts, pointer arithmetic, and unchecked assumptions, review becomes guesswork.",
  "narrationPoints": [
    "The producer agrees to write bytes in a certain order, with certain meanings.",
    "The parser should identify the format, confirm the version is supported.",
    "Format documentation matters because it keeps the parser from becoming a collection of folklore.",
    "If that contract is hidden in casts, pointer arithmetic, and unchecked assumptions, review becomes guesswork.",
    "A binary parser is not only a decoder; it is the executable expression of the format contract."
  ]
};
