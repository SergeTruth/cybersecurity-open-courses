window.COURSE_MODULE = {
  "title": "Reading Bytes Safely",
  "graphicAlt": "Bullet summary graphic for Reading Bytes Safely.",
  "narration": "Safe binary validation starts before individual fields are decoded. The parser needs a reliable view of how many bytes are actually available and how much capacity exists in the destination buffer. File size, message length, and transport metadata are useful inputs, but they should not be treated as proof that a future read will deliver exactly that amount of data.\n\nMany C APIs can return fewer bytes than requested. A partial read may be normal at end of file, may reflect an interrupted operation, or may indicate that data arrived in smaller pieces than expected. The important rule is simple: do not use bytes that have not actually been read. Check the return value, update the number of available bytes, and treat missing data as a validation failure rather than a surprising internal condition.\n\nField readers should verify that enough bytes remain before reading. If a header requires four bytes for a magic value and two bytes for a version, the parser checks those limits before touching the memory. The same pattern applies to arrays, records, optional sections, and nested structures. A helper such as a cursor with a current position and remaining length can make these checks consistent and easier to audit.\n\nBuffer capacity and data length are different facts. Capacity is how much storage was allocated. Data length is how much valid input was received. Mixing them leads to code that reads uninitialized bytes, parses stale data, or believes a buffer is complete because it is large enough. Keep those values separate in names and in logic.\n\nEnd-of-input should be ordinary. A truncated file or short message is not an exceptional mystery; it is one of the most common invalid inputs a parser will see. Handling it cleanly means returning a clear validation failure, avoiding partial object construction, and leaving caller state predictable.\n\nFinally, binary data is not text unless the format says it is text and defines the rules. A byte sequence may contain zeros, invalid encodings, embedded separators, or no terminator at all. If a field is later treated as a C string, the parser must verify length, encoding expectations if relevant, and termination or create a safe terminated copy deliberately.",
  "narrationPoints": [
    "Safe binary validation starts before individual fields are decoded.",
    "A helper such as a cursor with a current position and remaining length can make these checks consistent and easier to audit.",
    "Mixing them leads to code that reads uninitialized bytes, parses stale data, or believes a buffer is complete because it is large enough.",
    "Handling it cleanly means returning a clear validation failure, avoiding partial object construction.",
    "If a field is later treated as a C string, the parser must verify length, encoding expectations if relevant."
  ]
};
