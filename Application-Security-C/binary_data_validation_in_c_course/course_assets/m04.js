window.COURSE_MODULE = {
  "title": "Length Fields, Offsets, and Range Checks",
  "graphicAlt": "Bullet summary graphic for Length Fields, Offsets, and Range Checks.",
  "narration": "Lengths, offsets, and counts are some of the most important fields in binary validation because they influence memory access. They tell the parser where data begins, how much data exists, how many records to expect, or how large an allocation might be. Because they come from input, they are not facts until the parser proves they make sense for the available data.\n\nA common mistake is to check only the beginning of a range. An offset may point inside the buffer, but the data that begins there may extend beyond the end. Defensive code checks both the start and the end of the range before reading, copying, allocating, or advancing a cursor. In practice, that means validating that the offset is within bounds, the length is within policy, and the expression that combines them cannot wrap around.\n\nNested data requires nested validation. A top-level table may have a valid range, but each record inside that table may contain its own length or offset fields. Those inner values need to be checked against the region they belong to, not only against the whole file. This keeps one valid outer structure from becoming a pass for invalid inner access.\n\nRange checks should happen before pointer arithmetic or memory access. In C, computing an invalid pointer may already move the code outside the assumptions the compiler and platform expect. Using indexes and explicit integer checks before deriving pointers is easier to reason about and less fragile under optimization.\n\nDeclared sizes should also be cross-checked against actual availability. If a header claims there are ten records, the parser should verify that ten records can fit. If an offset points to a payload, the parser should verify that the payload exists and belongs to the expected section. Validation is strongest when every size-related claim is connected back to bytes that are actually present.",
  "narrationPoints": [
    "Lengths, offsets, and counts are some of the most important fields in binary validation because they influence memory access.",
    "Validating that the offset is within bounds, the length is within policy, and the expression that combines them cannot wrap around.",
    "A top-level table may have a valid range, but each record inside that table may contain its own length or offset fields.",
    "Range checks should happen before pointer arithmetic or memory access.",
    "If an offset points to a payload, the parser should verify that the payload exists and belongs to the expected section."
  ]
};
