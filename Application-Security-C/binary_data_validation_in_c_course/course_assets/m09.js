window.COURSE_MODULE = {
  "title": "Testing, Fuzzing, and Review Practices",
  "graphicAlt": "Bullet summary graphic for Testing, Fuzzing, and Review Practices.",
  "narration": "Binary parsers deserve tests that focus on failure paths, not only happy paths. Useful tests include empty input, one-byte input, truncated headers, truncated payloads, oversized fields, inconsistent counts, unsupported versions, unexpected flags, boundary values, maximum allowed sizes, and values just beyond the limit. These tests make validation rules executable.\n\nCorpus tests are especially helpful for formats that evolve. A corpus can include known valid samples and known invalid samples, with expected outcomes. When the parser changes, the corpus helps preserve intentional behavior and catches regressions in edge cases that are easy to forget.\n\nFuzzing can support robustness testing by exercising unusual input combinations at scale. At a high level, fuzzing feeds many generated or mutated inputs into a parser and observes whether the parser crashes, hangs, leaks, or violates expected checks. It is not a replacement for design review, but it is a valuable way to discover assumptions that ordinary unit tests missed.\n\nRuntime sanitizers add another layer during testing. Address, undefined behavior, leak, and memory sanitizers can expose out-of-bounds access, use of uninitialized data, integer issues, and cleanup mistakes while the test suite and fuzzing harnesses run. Findings should be treated as signals that validation, arithmetic, or ownership needs attention.\n\nStatic analysis can identify unchecked lengths, risky casts, suspicious pointer arithmetic, ignored return values, and calculations that may overflow. The best results come when analysis is combined with clear code structure. Tools are more useful when parser intent is explicit.\n\nCode review should trace every length, offset, count, allocation, and pointer access back to a validation rule. Reviewers should ask: where did this value come from, what range was allowed, could the calculation overflow, does the full range fit, what happens when the input is truncated, and how is partial state cleaned up. That discipline turns review from style feedback into real parser assurance.",
  "narrationPoints": [
    "Useful tests include empty input, one-byte input, truncated headers, truncated payloads, oversized fields.",
    "At a high level, fuzzing feeds many generated or mutated inputs into a parser and observes whether the parser crashes, hangs, leaks.",
    "Findings should be treated as signals that validation, arithmetic, or ownership needs attention.",
    "Static analysis can identify unchecked lengths, risky casts, suspicious pointer arithmetic, ignored return values.",
    "Code review should trace every length, offset, count, allocation, and pointer access back to a validation rule."
  ]
};
