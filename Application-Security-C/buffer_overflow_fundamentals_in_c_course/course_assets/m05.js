window.COURSE_MODULE = {
  "title": "String and Memory API Review",
  "graphicAlt": "Bullet summary graphic for String and Memory API Review.",
  "narration": "String and memory APIs must be reviewed in terms of destination capacity, source length, expected result, and terminator behavior. The name of the function is not enough to prove safety. The caller must pass the right arguments and handle the result correctly.\n\nFunctions that include a size parameter are not automatically safe. A size parameter might mean destination capacity, maximum bytes to copy, maximum characters to read, or some other limit depending on the API. Passing the source length when the API expects destination capacity can still produce unsafe behavior.\n\nReview calls that copy strings, concatenate strings, format output, copy memory, parse records, or build protocol messages. For each call, ask what destination is being written, how large it is, how much source data is available, what output is expected, and how the function reports truncation or failure.\n\nFormat strings should be controlled by the program, not by untrusted input. Formatting functions interpret the format string as instructions, so untrusted data should be passed as data under a fixed format. This is a general defensive habit for output routines, logs, and diagnostics.\n\nOutput buffers should have explicit capacity and clear truncation behavior. If the result is too long, the code should know whether it failed, shortened the result, left a valid terminator, or produced an output that must not be used. Silent truncation can create confusing behavior and downstream errors.\n\nAvoid storing or logging raw memory when it may include uninitialized, stale, or sensitive data. Diagnostic output should be deliberate and bounded. Application-specific wrappers or helper functions can help when they make contracts clearer and are reviewed, but they should not hide uncertainty behind a friendly name.",
  "narrationPoints": [
    "String and memory APIs must be reviewed in terms of destination capacity, source length, expected result.",
    "A size parameter might mean destination capacity, maximum bytes to copy, maximum characters to read, or some other limit depending on the API.",
    "Review calls that copy strings, concatenate strings, format output, copy memory, parse records.",
    "Formatting functions interpret the format string as instructions, so untrusted data should be passed as data under a fixed format.",
    "Avoid storing or logging raw memory when it may include uninitialized, stale."
  ]
};
