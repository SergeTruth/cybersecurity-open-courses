window.COURSE_MODULE = {
  "title": "Root Causes: Size, Length, and Terminators",
  "graphicAlt": "Bullet summary graphic for Root Causes: Size, Length, and Terminators.",
  "narration": "Many buffer overflows come from confusing capacity with current length. Capacity is the total space available. Length is how much meaningful data is currently present. A buffer with capacity for one hundred bytes may currently contain ten bytes, and a function that appends data needs to consider both values.\n\nOff-by-one errors often happen when code forgets space for a terminating null character. A C string needs storage for the content and storage for the terminator. If a buffer has room for sixteen bytes total, it cannot safely hold a sixteen-character C string plus a terminator.\n\nC strings require correct null termination, and not every byte sequence is a valid C string. Data from a network record, file block, device buffer, or binary protocol may contain embedded null bytes or may not include a terminator at all. Treating arbitrary bytes as a C string can make later operations read beyond intended data.\n\nUnchecked input can exceed assumptions. Values from files, sockets, environment variables, command-line arguments, devices, or other processes may be longer than expected, may use a different encoding, or may omit expected delimiters. Upstream validation is helpful only when the current code can rely on it and that reliance is documented.\n\nTruncation should be handled as a meaningful result, not silently ignored. Sometimes truncation is acceptable if the code records it and preserves a valid output. Sometimes it must be treated as an error. The dangerous habit is pretending a shortened value is the same as the complete value.\n\nBuffer safety requires clear contracts about input size, output capacity, resulting length, terminator behavior, and failure behavior. Those contracts should be visible at function boundaries, not hidden in comments far away from the copy or formatting operation.",
  "narrationPoints": [
    "A buffer with capacity for one hundred bytes may currently contain ten bytes.",
    "Off-by-one errors often happen when code forgets space for a terminating null character.",
    "Data from a network record, file block, device buffer, or binary protocol may contain embedded null bytes or may not include a terminator at all.",
    "Upstream validation is helpful only when the current code can rely on it and that reliance is documented.",
    "Sometimes truncation is acceptable if the code records it and preserves a valid output."
  ]
};
