window.COURSE_MODULE = {
  "title": "Strings, Terminators, and Copying Risks",
  "graphicAlt": "Bullet summary graphic for Strings, Terminators, and Copying Risks.",
  "narration": "C strings rely on a null terminator. That convention means a string operation often walks memory until it finds a zero byte. If the terminator is missing inside the valid buffer, the operation may continue beyond the intended data. A character buffer is therefore not automatically a valid C string.\n\nData from a file, socket, device, fixed-width record, or parser may contain characters without a terminator. It may also contain embedded null bytes. Code should be clear about whether it is handling a bounded byte region, a character array, or a properly terminated C string. Treating all three as the same thing creates bounds risk.\n\nThe strlen function assumes a terminator can be found. It does not receive capacity information unless the calling code provides a separate bound through another mechanism. If the data may be unterminated, use length-aware logic that stops within the known valid region.\n\nCopies into fixed-size buffers must account for available space and the terminator. A destination with capacity N can hold at most N minus one ordinary characters when the result is a C string. Copying exactly N characters and then adding a terminator requires N plus one bytes of storage.\n\nTruncation can be safer than writing past the destination, but it must be handled deliberately. A truncated file name, identifier, message, or path may no longer mean the same thing. The function should report truncation or document that shortened output is acceptable for the specific operation.\n\nPrefer length-aware operations and explicit termination where appropriate. The important habit is not trusting a friendly function name. Review destination capacity, source length, resulting length, terminator behavior, and failure reporting. String safety is bounded-buffer safety with an extra terminator rule.",
  "narrationPoints": [
    "Code should be clear about whether it is handling a bounded byte region, a character array, or a properly terminated C string.",
    "If the data may be unterminated, use length-aware logic that stops within the known valid region.",
    "Copying exactly N characters and then adding a terminator requires N plus one bytes of storage.",
    "Truncation can be safer than writing past the destination, but it must be handled deliberately.",
    "Review destination capacity, source length, resulting length, terminator behavior, and failure reporting."
  ]
};
