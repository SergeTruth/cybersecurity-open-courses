window.COURSE_MODULE = {
  "title": "Heap Management Mistakes",
  "graphicAlt": "Bullet summary graphic for Heap Management Mistakes.",
  "narration": "Heap memory requires clear ownership and cleanup responsibility. A dynamically allocated object should have an owner, a documented lifetime, and a predictable release path. Without those rules, the codebase can accumulate stale pointers, leaks, duplicated cleanup, and confusing error behavior.\n\nA use-after-free occurs when code accesses memory after it has been released. The pointer value may still be stored in a variable, structure, callback context, or cache, but the allocation no longer belongs to that code. Defensive cleanup often sets pointers to null after release when reuse is possible, but the deeper fix is clear ownership and avoiding stale aliases.\n\nA double-free occurs when code attempts to release the same allocation more than once. This can happen when two components believe they own the same pointer, when an error path falls through into shared cleanup, or when ownership transfer is not documented. Cleanup should be centralized where practical and state should make repeated release less likely.\n\nMemory leaks happen when ownership is lost and cleanup becomes impossible. Leaks may not corrupt memory immediately, but they can degrade reliability over time, exhaust resources, and obscure which component is responsible for an object. Error paths are especially important because allocations often happen before all later operations are known to succeed.\n\nThe realloc function needs careful handling. If it fails, the original allocation remains valid and still needs to be managed. Assigning the result directly over the only pointer can lose the original allocation on failure. A safer pattern uses a temporary pointer, checks the result, and updates the buffer state only after success.\n\nCleanup paths should leave program state predictable and valid. On success, ownership should be clear. On failure, partially initialized objects should be either cleaned up or returned in a documented state. The next caller or cleanup routine should not have to guess which pointers are live and which have already been released.",
  "narrationPoints": [
    "Heap memory requires clear ownership and cleanup responsibility.",
    "Defensive cleanup often sets pointers to null after release when reuse is possible.",
    "Cleanup should be centralized where practical and state should make repeated release less likely.",
    "Error paths are especially important because allocations often happen before all later operations are known to succeed.",
    "A safer pattern uses a temporary pointer, checks the result, and updates the buffer state only after success."
  ]
};
