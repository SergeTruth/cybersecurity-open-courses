window.COURSE_MODULE = {
  "title": "Integer Sizes, Allocation Math, and Type Assumptions",
  "graphicAlt": "Bullet summary graphic for Integer Sizes, Allocation Math, and Type Assumptions.",
  "narration": "Integer sizes and allocation math are part of memory safety. Buffers are finite, and the values that describe them are finite too. When code calculates a size, an offset, a count, or a capacity, that arithmetic must match the object that will be accessed later.\n\nAllocation math must distinguish bytes from elements. An element count multiplied by the size of each element produces a byte count for allocation. A byte count is not the same as an index count, a string length, or a structure count. Naming and helper functions should make the unit visible.\n\nMultiplication for allocation sizes can overflow. Addition can also overflow when code adds a header, terminator, alignment padding, or trailing data. If the arithmetic wraps or truncates, the allocation may be smaller than later code expects. Check the arithmetic before calling allocation functions or copying data.\n\nSigned and unsigned comparisons can produce surprising results when values are converted. A negative index or length should be rejected before it is converted to an unsigned size. A wrapped size should not be trusted as a large valid capacity. Defensive code validates both the type range and the business range.\n\nStructure layout, padding, and alignment should not be guessed casually. The compiler controls layout according to the platform, ABI, and type rules. Code that serializes, copies, or overlays structures should be careful about what bytes are meaningful, what padding may exist, and whether the external representation truly matches the in-memory object.\n\nCasts should not be used to hide type, size, or ownership problems. A cast can request a conversion, but it does not prove the value fits, the pointer is aligned, the lifetime is valid, or the destination owns the storage. When a cast appears near memory access, reviewers should ask what contract makes the access safe.",
  "narrationPoints": [
    "When code calculates a size, an offset, a count, or a capacity, that arithmetic must match the object that will be accessed later.",
    "A byte count is not the same as an index count, a string length, or a structure count.",
    "Defensive code validates both the type range and the business range.",
    "The compiler controls layout according to the platform, ABI, and type rules.",
    "Casts should not be used to hide type, size, or ownership problems."
  ]
};
