window.COURSE_MODULE = {
  "title": "Defensive Design Patterns",
  "graphicAlt": "Bullet summary graphic for Defensive Design Patterns.",
  "narration": "Defensive design patterns make memory assumptions visible. C does not prevent every unsafe state, so good APIs and data structures should make safe use easier than unsafe use. The strongest patterns keep related facts together: pointer, count, capacity, ownership, lifetime, and valid range.\n\nKeep pointers and sizes together in function arguments or structured types. A function that receives only a raw pointer must rely on hidden knowledge or external assumptions. A function that receives pointer plus count, pointer plus capacity, or a buffer structure can make its bounds contract explicit.\n\nDistinguish count, capacity, and byte size. Count describes meaningful elements currently present. Capacity describes available storage. Byte size describes memory extent. A string also has terminator rules. Mixing these ideas in one loosely named variable makes memory review harder than it needs to be.\n\nDefine ownership transfer rules clearly. If a function takes ownership of a pointer, the caller should stop using it. If a function borrows a pointer, it should not store it beyond the promised lifetime. If a function returns allocated storage, the caller should know which cleanup routine applies. Ownership uncertainty is a common root of heap mistakes.\n\nCentralize cleanup where practical. A single cleanup path can reduce duplicate frees and missed releases, especially in functions with several failure points. The cleanup path should still know which resources were acquired, so initialization and state tracking matter.\n\nUse helper functions for repeated bounds and append logic. A reviewed helper can validate ranges, grow dynamic buffers safely, maintain terminators, and update count and capacity consistently. Helpers should have precise contracts so they improve clarity rather than hiding risk behind a friendly name.",
  "narrationPoints": [
    "The strongest patterns keep related facts together: pointer, count, capacity, ownership, lifetime.",
    "A function that receives pointer plus count, pointer plus capacity, or a buffer structure can make its bounds contract explicit.",
    "Mixing these ideas in one loosely named variable makes memory review harder than it needs to be.",
    "If a function takes ownership of a pointer, the caller should stop using it.",
    "A single cleanup path can reduce duplicate frees and missed releases, especially in functions with several failure points."
  ]
};
