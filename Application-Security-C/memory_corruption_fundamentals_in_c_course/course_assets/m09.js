window.COURSE_MODULE = {
  "title": "Detection, Tooling, and Review",
  "graphicAlt": "Bullet summary graphic for Detection, Tooling, and Review.",
  "narration": "Detection and tooling help teams find memory defects earlier, but they do not replace clear design. Compiler warnings, static analysis, runtime sanitizers, assertions, boundary-focused tests, and code review all catch different kinds of mistakes. The goal is to create overlapping support for the memory contracts the code is supposed to follow.\n\nCompiler warnings can identify suspicious code patterns: signed and unsigned mismatches, truncation, ignored results, questionable casts, uninitialized values, and possible array bounds concerns. Warning changes near memory access, allocation, copying, or cleanup deserve review rather than quick suppression.\n\nStatic analysis can identify risky flows, unchecked values, inconsistent assumptions, escaping pointers, lost ownership, and paths where cleanup may be incomplete. Static tools are especially useful when pointer and size values move through several functions before they are used.\n\nRuntime sanitizers can detect memory errors during testing. They can expose out-of-bounds access, use after release, use after scope, leaks, and other issues depending on tool support and configuration. They are most useful when tests exercise boundary values, error paths, resize behavior, and unusual inputs.\n\nAssertions can document invariants during development. They can state that count is no greater than capacity, a pointer is non-null when count is nonzero, an index is in range, or ownership has moved to a cleanup path. Assertions are not a replacement for validating external input, but they make internal expectations visible.\n\nFuzzing can support robustness testing at a high level by exercising unexpected sizes and shapes. Boundary tests should cover zero sizes, maximum sizes, empty strings, missing terminators, allocation failure paths, and cleanup after partial initialization. Tools support engineering discipline, but review and design discipline remain necessary.",
  "narrationPoints": [
    "Detection and tooling help teams find memory defects earlier, but they do not replace clear design.",
    "Warning changes near memory access, allocation, copying, or cleanup deserve review rather than quick suppression.",
    "Static tools are especially useful when pointer and size values move through several functions before they are used.",
    "Assertions are not a replacement for validating external input, but they make internal expectations visible.",
    "Boundary tests should cover zero sizes, maximum sizes, empty strings, missing terminators, allocation failure paths."
  ]
};
