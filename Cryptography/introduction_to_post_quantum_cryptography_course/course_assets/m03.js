window.COURSE_MODULE = {
  "title": "Key Encapsulation, Signatures, and Cryptographic Roles",
  "graphicAlt": "Instructional illustration for Key Encapsulation, Signatures, and Cryptographic Roles.",
  "narration": "Before planning a migration, teams need to understand what cryptography is doing in each place. Key establishment is about helping two parties arrive at shared secret material over an untrusted network. A key encapsulation mechanism, or KEM, is one way to support that role. Conceptually, one party creates protected key material that another party can recover with the right private information, allowing both sides to derive a shared secret for later symmetric encryption.\n\nDigital signatures play a different role. A signature lets a verifier check that data was signed by the holder of a private key and that the signed data has not been modified. That supports authentication, integrity, software update trust, code signing, certificate authorities, secure boot chains, identity tokens, document signing, and device enrollment. Signatures do not automatically provide confidentiality. They answer questions about origin and integrity, not secrecy.\n\nThese roles appear in many systems that practitioners use every day. TLS uses key establishment and authentication to protect web sessions and APIs. VPNs and SSH use public-key mechanisms for remote access. Software updates and packages rely on signatures. Certificates bind keys to identities. Secure email, device authentication, identity platforms, and firmware signing all depend on cryptographic roles that may have to change at different speeds.\n\nReplacing one algorithm is therefore rarely a simple find-and-replace operation. A KEM change can affect protocol negotiation, message size, performance, and library support. A signature change can affect certificate formats, chain sizes, hardware security module behavior, storage, logging, and verification code. The lesson is practical: identify the role first, then choose the migration path. A team that treats all cryptography as one category will miss dependencies and create avoidable surprises.",
  "narrationPoints": [
    "Before planning a migration, teams need to understand what cryptography is doing in each place.",
    "Digital signatures play a different role.",
    "These roles appear in many systems that practitioners use every day.",
    "Replacing one algorithm is therefore rarely a simple find-and-replace operation."
  ]
};
