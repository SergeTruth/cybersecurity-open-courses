window.COURSE_MODULE = {
  "title": "A03: Injection",
  "graphicAlt": "Graphic for A03: Injection",
  "narration": "SQL injection remains a common example because the attack changes the meaning of a database query. The same pattern can appear anywhere user input is mixed into an interpreter without strong boundaries. Command injection is especially dangerous because it can let an attacker execute operating system commands through application features such as file conversion, diagnostics, import jobs, or administrative utilities.\n\nLDAP, NoSQL, template, expression language, and other injection types follow the same core failure: data crosses a trust boundary and becomes executable or structural input. Output encoding and input validation are complementary controls. Validation rejects input that does not belong, while encoding and parameterization preserve the boundary between data and instructions.\n\nUse parameterized queries and safe APIs instead of string-built commands. Validate input against expected formats and ranges. Encode output for the correct context and handle errors without leaking internals.\n\nSelected NIST mappings for this topic include SI-10 - Information Input Validation; SI-11 - Error Handling; SI-7 - Software, Firmware, and Information Integrity.",
  "narrationPoints": [
    "SQL injection remains a common example because the attack changes the meaning of a database query. The same pattern can appear anywhere user input is mixed into an interpreter without strong boundaries. Command injection is especially dangerous because it can let an attacker execute operating system commands through application features such as file conversion, diagnostics, import jobs, or administrative utilities.",
    "LDAP, NoSQL, template, expression language, and other injection types follow the same core failure: data crosses a trust boundary and becomes executable or structural input. Output encoding and input validation are complementary controls. Validation rejects input that does not belong, while encoding and parameterization preserve the boundary between data and instructions.",
    "Use parameterized queries and safe APIs instead of string-built commands. Validate input against expected formats and ranges. Encode output for the correct context and handle errors without leaking internals.",
    "Selected NIST mappings for this topic include SI-10 - Information Input Validation; SI-11 - Error Handling; SI-7 - Software, Firmware, and Information Integrity."
  ]
};
