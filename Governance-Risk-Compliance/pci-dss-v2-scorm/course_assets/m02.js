window.COURSE_MODULE = {
  "title": "A01: Broken Access Control",
  "graphicAlt": "Graphic for A01: Broken Access Control",
  "narration": "Authorization failures occur when an application does not reliably verify whether the authenticated user is allowed to perform a specific action or access a specific payment record. Login alone is not enough; every sensitive request needs server-side authorization. Privilege escalation can give a lower-privileged user administrative capabilities or access to records belonging to another account.\n\nIn payment environments this can expose transaction history, refunds, customer data, or administrative functions. Access control bypasses often appear in APIs, hidden routes, disabled buttons, direct URL access, or client-controlled role values. If the server trusts client-side decisions, attackers can often call the underlying function directly.\n\nInsecure direct object references happen when identifiers such as order IDs, customer IDs, invoice IDs, or payment record references are exposed without checking ownership or role-based permission. Apply least privilege to users, services, and administrators. Use role-based access control with explicit permission checks.\n\nEnforce authorization on the server for every sensitive object and action. Selected PCI DSS mappings for this topic include Req. 7.1 - Restrict access to system components and cardholder data; Req. 7.2 - Role-based access control; Req. 8.2 - User authentication management.",
  "narrationPoints": [
    "Authorization failures occur when an application does not reliably verify whether the authenticated user is allowed to perform a specific action or access a specific payment record. Login alone is not enough; every sensitive request needs server-side authorization. Privilege escalation can give a lower-privileged user administrative capabilities or access to records belonging to another account.",
    "In payment environments this can expose transaction history, refunds, customer data, or administrative functions. Access control bypasses often appear in APIs, hidden routes, disabled buttons, direct URL access, or client-controlled role values. If the server trusts client-side decisions, attackers can often call the underlying function directly.",
    "Insecure direct object references happen when identifiers such as order IDs, customer IDs, invoice IDs, or payment record references are exposed without checking ownership or role-based permission. Apply least privilege to users, services, and administrators. Use role-based access control with explicit permission checks.",
    "Enforce authorization on the server for every sensitive object and action. Selected PCI DSS mappings for this topic include Req. 7.1 - Restrict access to system components and cardholder data; Req. 7.2 - Role-based access control; Req. 8.2 - User authentication management."
  ]
};
