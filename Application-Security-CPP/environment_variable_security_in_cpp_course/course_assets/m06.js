window.COURSE_MODULE = {
  "title": "Privilege Boundaries and Environment Sanitization",
  "graphicAlt": "Privilege-boundary diagram showing a broad parent environment reduced to PATH, LANG, and an approved region before a higher-privilege child process starts.",
  "narration": "Inherited environment variables become more important when a program runs with elevated privileges, acts as a service, launches helper processes, processes sensitive data, or crosses user boundaries. In those situations, trusting ambient environment state can make policy depend on whoever controlled the launch context.\n\nSome variables influence behavior indirectly through libraries, loaders, search paths, localization, temporary directories, diagnostics, or runtime frameworks. The application code may never read those variables directly, yet its dependencies or tools may still respond to them. Security-sensitive processes need an explicit environment policy rather than broad inheritance by habit.\n\nSanitization means deciding which variables are approved and what to do with everything else. A program might ignore unapproved values, clear them before sensitive work, or construct a minimal environment for child processes. The exact approach depends on platform and application role, but the decision should be deliberate and testable.\n\nEnvironment policy should be reviewed with the same care as file permissions or network access. Document allowed variables, trusted sources, default behavior, and sensitive contexts where inheritance is restricted. The goal is least privilege for configuration influence: only the values that are expected and validated should affect security-sensitive behavior.\n\nSanitization should be implemented close to the boundary where privilege changes or sensitive work begins. Waiting until later in the call path can leave libraries, diagnostics, or helper code exposed to ambient values during initialization. A small allowlist is easier to explain, test, and audit than a large denylist.",
  "narrationPoints": [
    "Inherited environment variables become more important when a program runs with elevated privileges, acts as a service, launches helper processes, processes sensitive data, or crosses user boundaries.",
    "Some variables influence behavior indirectly through libraries, loaders, search paths, localization, temporary directories, diagnostics, or runtime frameworks.",
    "The exact approach depends on platform and application role, but the decision should be deliberate and testable.",
    "A program might ignore unapproved values, clear them before sensitive work, or construct a minimal environment for child processes.",
    "Environment policy should be reviewed with the same care as file permissions or network access.",
    "Sanitization should be implemented close to the boundary where privilege changes or sensitive work begins."
  ]
};
