window.COURSE_MODULE = {
  "title": "Reading Environment Variables Safely in C++",
  "graphicAlt": "Flow diagram from getenv through absence handling, owned string copying, syntax checks, and a typed deployment name, with rejection branches leaving configuration unchanged.",
  "narration": "C++ programs often use std::getenv or platform APIs to retrieve environment values. The defensive issue is not merely how to call the API. The larger question is where environment access belongs in the design. Scattered calls across many modules make it difficult to validate values consistently or understand which settings affect behavior.\n\nA safer pattern is to read environment variables at startup or configuration load time. Copy values into owned strings or a typed configuration structure, validate them, and then pass the validated configuration object to the rest of the application. This separates untrusted environment input from trusted runtime state.\n\nMissing variables should be handled deliberately. Some settings can use secure defaults. Some should produce a clear configuration error. Some may be required for startup. The code should not silently treat missing, empty, malformed, and intentionally disabled values as the same condition unless that behavior is part of the policy.\n\nDo not retain raw pointers returned by environment APIs as long-term application state. Copy the value you intend to use, validate it, and avoid repeated interpretation. Centralized loading improves review because the team can inspect one environment contract instead of hunting for hidden reads throughout the codebase.\n\nCentralization also makes tests easier to write. A configuration loader can be exercised with a controlled map of names and values, including absent and malformed cases, without relying on the process-wide environment in every unit test. That keeps security policy visible and reduces surprises from unrelated variables on a developer machine.",
  "narrationPoints": [
    "C++ programs often use std::getenv or platform APIs to retrieve environment values.",
    "Copy values into owned strings or a typed configuration structure, validate them, and then pass the validated configuration object to the rest of the application.",
    "The code should not silently treat missing, empty, malformed, and intentionally disabled values as the same condition unless that behavior is part of the policy.",
    "Centralized loading improves review because the team can inspect one environment contract instead of hunting for hidden reads throughout the codebase.",
    "Copy the value you intend to use, validate it, and avoid repeated interpretation.",
    "A configuration loader can be exercised with a controlled map of names and values, including absent and malformed cases, without relying on the process-wide environment in every unit test."
  ]
};
