window.COURSE_MODULE = {
  "title": "Secrets and Sensitive Environment Values",
  "graphicAlt": "Configuration view with public region and mode fields flowing to diagnostics while password and nested provider credentials stop at a closed allowlist barrier.",
  "narration": "Environment variables are often used for secrets because deployment systems, containers, and CI platforms can inject them conveniently. That convenience has risk. Credentials, tokens, API keys, database passwords, signing material, and connection strings should be treated as sensitive even when they arrive through a familiar deployment workflow.\n\nDepending on platform and configuration, environment values may be visible through process inspection tools, service configuration, crash reports, diagnostic dumps, shell history, CI output, or inherited child process environments. The details vary, but the engineering lesson is stable: avoid assuming that environment storage is private by default.\n\nSecrets should not be printed, dumped, or included in exception messages. Logs should identify that a sensitive setting is missing, malformed, expired, or inaccessible without including the value itself. Redaction should happen before data reaches broad log sinks. Debug tooling should also be reviewed so it does not accidentally expose full process configuration.\n\nPrefer managed secret delivery mechanisms where available. When environment variables are used for secrets, restrict access to the process, minimize copying, support rotation and revocation, and avoid passing secret values to child processes unless explicitly required. Secret handling is not only about storage; it is about every place the value can travel.\n\nOperationally, teams should rehearse what happens when an environment-delivered secret changes. The application should reload or restart cleanly, errors should not reveal the old or new value, and dependent child processes should not keep receiving stale credentials longer than policy allows.",
  "narrationPoints": [
    "Credentials, tokens, API keys, database passwords, signing material, and connection strings should be treated as sensitive even when they arrive through a familiar deployment workflow.",
    "Environment variables are often used for secrets because deployment systems, containers, and CI platforms can inject them conveniently.",
    "Depending on platform and configuration, environment values may be visible through process inspection tools, service configuration, crash reports, diagnostic dumps, shell history, CI output, or inherited child process environments.",
    "Logs should identify that a sensitive setting is missing, malformed, expired, or inaccessible without including the value itself.",
    "When environment variables are used for secrets, restrict access to the process, minimize copying, support rotation and revocation, and avoid passing secret values to child processes unless explicitly required.",
    "The application should reload or restart cleanly, errors should not reveal the old or new value, and dependent child processes should not keep receiving stale credentials longer than policy allows."
  ]
};
