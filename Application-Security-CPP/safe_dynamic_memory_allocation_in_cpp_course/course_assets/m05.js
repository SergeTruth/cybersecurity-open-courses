window.COURSE_MODULE = {
  "title": "Allocation Size, Initialization, and Construction Safety",
  "graphicAlt": "Construction pipeline showing external dimensions passing checked arithmetic and policy limits before allocation, followed by complete initialization and invariant establishment before the object becomes visible to callers.",
  "narration": "Allocation safety depends on more than calling the right allocation mechanism. Size values need to be trusted only after validation. Counts, lengths, dimensions, element sizes, and serialized fields should be checked before they influence allocation, indexing, copying, or construction.\n\nSize arithmetic deserves explicit review. Multiplication, addition, and unit conversion can exceed the representable range or produce a value that does not match the intended object layout. Check arithmetic before allocating, especially when external input controls counts or lengths.\n\nPrefer containers over manually allocated arrays when possible. A container keeps size, capacity, ownership, and cleanup behavior together. That is easier to review than a raw pointer plus a separate length value that must stay correct through every branch.\n\nInitialization discipline matters. Allocated storage should become a valid object before it is exposed to other code. Constructors, factories, and RAII wrappers should establish invariants before callbacks, output parameters, global state, or other observers can see the object.\n\nPartially constructed resources should not escape. If construction fails halfway through, cleanup should happen inside the owner that knows what has been initialized. Other code should not receive an object that requires guessing which parts are valid.\n\nDefine failure behavior when allocation or construction cannot safely complete. The policy might be an error return, an exception, a rejected request, a smaller operation, or a fail-closed path according to project standards. What matters is that incomplete construction does not create ambiguous ownership.",
  "narrationPoints": [
    "Counts, lengths, dimensions, element sizes, and serialized fields should be checked before they influence allocation, indexing, copying, or construction.",
    "Check arithmetic before allocating, especially when external input controls counts or lengths.",
    "A container keeps size, capacity, ownership, and cleanup behavior together.",
    "Constructors, factories, and RAII wrappers should establish invariants before callbacks, output parameters, global state, or other observers can see the object.",
    "Other code should not receive an object that requires guessing which parts are valid.",
    "The policy might be an error return, an exception, a rejected request, a smaller operation, or a fail-closed path according to project standards."
  ]
};
