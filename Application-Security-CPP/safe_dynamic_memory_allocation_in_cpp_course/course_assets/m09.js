window.COURSE_MODULE = {
  "title": "Testing, Sanitizers, and Review Practices",
  "graphicAlt": "Test matrix pairing empty, boundary, oversized, allocation-failure, move, destruction, and concurrency cases with AddressSanitizer, UndefinedBehaviorSanitizer, LeakSanitizer, ThreadSanitizer, and explicit invariant checks.",
  "narration": "Allocation defects often appear under edge conditions: error paths, partial construction, unusual input sizes, shutdown, retry logic, and concurrency. Tests that cover only successful creation and normal cleanup miss many of the lifecycle paths where ownership mistakes appear.\n\nSanitizers can reveal memory and lifetime defects during development and CI. AddressSanitizer, LeakSanitizer, UndefinedBehaviorSanitizer, debug library modes, and related tooling can make hidden defects visible while tests exercise the relevant code. They do not replace design, but they give valuable evidence.\n\nTests should cover cleanup, ownership transfer, allocation failure behavior where practical, boundary sizes, repeated create-destroy cycles, shutdown, and partial construction. A good test suite verifies that resources are released and that failed construction does not expose ambiguous objects.\n\nStatic analysis and compiler warnings can flag ownership, allocation, and cleanup patterns worth review. These tools are most useful when the team triages findings, documents intentional exceptions, and turns real issues into fixes rather than background noise.\n\nCI should make memory-safety checks repeatable. Tool findings should become tracked engineering defects with owners, fixes, and regression tests where appropriate. Allocation safety improves when testing and review are part of normal delivery rather than a separate cleanup effort.\n\nReview practices should connect tool findings back to design. If a leak appears in a failure path, ask which owner should have released the resource. If a boundary test fails, ask whether the size rule belongs in a parser, factory, allocator wrapper, or higher-level policy.",
  "narrationPoints": [
    "Allocation defects often appear under edge conditions: error paths, partial construction, unusual input sizes, shutdown, retry logic, and concurrency.",
    "Sanitizers can reveal memory and lifetime defects during development and CI.",
    "Tests should cover cleanup, ownership transfer, allocation failure behavior where practical, boundary sizes, repeated create-destroy cycles, shutdown, and partial construction.",
    "Static analysis and compiler warnings can flag ownership, allocation, and cleanup patterns worth review.",
    "Allocation safety improves when testing and review are part of normal delivery rather than a separate cleanup effort.",
    "If a boundary test fails, ask whether the size rule belongs in a parser, factory, allocator wrapper, or higher-level policy."
  ]
};
