window.COURSE_MODULE = {
  "title": "Refactoring Legacy Lifetime Bugs",
  "graphicAlt": "Legacy-refactor comparison replacing raw allocation plus escaping view with validation-first immutable shared storage.",
  "narration": "Many teams inherit C++ code with raw allocation, manual cleanup, pointer caches, unclear ownership, and object graphs that are difficult to reason about. The safest improvement path is usually incremental. Start by understanding the existing lifecycle before changing it.\n\nMap creators, owners, observers, and destroyers. Identify which component creates each object, which component is responsible for keeping it alive, which code merely observes it, and where destruction happens. This map often reveals places where ownership is implied by convention rather than expressed in code.\n\nReplace ownership paths before observer paths. If a raw pointer actually owns an object, move that ownership into RAII or a smart pointer type first. Once ownership is clear, non-owning observers can be shortened, replaced with references for call-only access, or converted to weak observation or stable handles where needed.\n\nAdd RAII wrappers around manual cleanup when direct modernization is too large for one change. A wrapper can centralize release behavior, prevent duplicate cleanup paths, and give tests a more predictable lifecycle boundary. This is often a practical bridge between legacy code and safer design.\n\nRegression tests are important before behavior-sensitive changes. Lifetime defects can depend on destruction order, mutation timing, callback scheduling, and move operations. Tests should cover the lifecycles that the refactor is meant to preserve, then add cases that verify safer failure behavior.\n\nDocument assumptions that cannot be removed immediately. Prioritize externally reachable, asynchronous, high-churn, and ownership-heavy paths first. Each refactor should leave the codebase easier to reason about than it was before, even if some legacy conventions remain temporarily.",
  "narrationPoints": [
    "Many teams inherit C++ code with raw allocation, manual cleanup, pointer caches, unclear ownership, and object graphs that are difficult to reason about.",
    "Map creators, owners, observers, and destroyers.",
    "If a raw pointer actually owns an object, move that ownership into RAII or a smart pointer type first.",
    "A wrapper can centralize release behavior, prevent duplicate cleanup paths, and give tests a more predictable lifecycle boundary.",
    "Tests should cover the lifecycles that the refactor is meant to preserve, then add cases that verify safer failure behavior.",
    "Each refactor should leave the codebase easier to reason about than it was before, even if some legacy conventions remain temporarily."
  ]
};
