window.COURSE_MODULE = {
  "title": "Why Concurrent Data Structures Matter",
  "graphicAlt": "Several threads converge on one container, with unsynchronized operations breaking invariants and a documented synchronization boundary serializing permitted access.",
  "narration": "Concurrent data structures appear anywhere multiple threads, tasks, or asynchronous workers share access to state. They may look like ordinary containers, queues, maps, caches, registries, pools, or tables, but the operating conditions are different. In single-threaded code, a caller can often assume that a container stays unchanged while an operation is running. Under concurrency, another thread may be reading, inserting, erasing, shutting down, or replacing related state at the same time.\n\nThat change matters because a data structure is more than memory that holds values. It also has invariants: relationships that must remain true for the structure to be correct. A queue may require its size counter to match the stored items. A cache may require its index, eviction list, and lifetime policy to agree. A registry may require that a name, handle, and object remain linked until all users are done. If different threads update those pieces without a shared rule, the program can drift into states that no caller expected.\n\nDefensive C++ engineering treats these structures as explicit boundaries. The design should say who owns the state, which operations are safe concurrently, which operations require exclusive access, and when objects may be destroyed. This is not only a performance topic. Broken synchronization can cause reliability failures, data corruption, incorrect authorization decisions, resource exhaustion, and hard-to-repeat crashes during shutdown. The goal of this course is to build a practical roadmap: protect invariants, manage lifetimes, prefer clear APIs, and verify the design with diagnostics, tests, and review.",
  "narrationPoints": [
    "Concurrent data structures appear anywhere multiple threads, tasks, or asynchronous workers share access to state.",
    "Under concurrency, another thread may be reading, inserting, erasing, shutting down, or replacing related state at the same time.",
    "A cache may require its index, eviction list, and lifetime policy to agree.",
    "If different threads update those pieces without a shared rule, the program can drift into states that no caller expected.",
    "The design should say who owns the state, which operations are safe concurrently, which operations require exclusive access, and when objects may be destroyed.",
    "The goal of this course is to build a practical roadmap: protect invariants, manage lifetimes, prefer clear APIs, and verify the design with diagnostics, tests, and review."
  ]
};
