window.COURSE_MODULE = {
  "title": "Maps, Caches, and Reader-Writer Access",
  "graphicAlt": "A shared-mutex cache permits parallel readers, exclusive updates, size-bounded eviction, and owned value copies after the lock is released.",
  "narration": "Maps, caches, registries, and lookup tables are deceptively difficult under concurrency because they combine several responsibilities. A single operation may look like a read, but the structure may also refresh timestamps, promote an item in an eviction list, insert a missing value, or remove stale entries. The first design question is whether reads are truly read-only and what lifetime guarantee a successful lookup provides. Returning a direct reference to an internal object can be unsafe if another thread may erase or replace that object immediately after the lock is released.\n\nReader-writer locks can help read-heavy workloads, but they do not remove the need for clear invariants. Shared access is only safe while operations are actually read-only with respect to every protected field. If a lookup updates metrics, cache ordering, or lazy initialization state, it may require exclusive access or a different design. Immutable snapshots are often simpler: a reader receives an owned value, a copy of relevant data, or a shared immutable view that remains valid independent of later mutation.\n\nCaches add resource governance to the problem. Size limits, eviction rules, stale-entry handling, and refresh behavior should be explicit. Without those rules, a cache can become an availability risk by growing without bound, serving data beyond its intended lifetime, or destroying entries while other threads still depend on them. Defensive APIs avoid surprising callers. They document whether returned values are copies, shared handles, scoped access objects, or temporary observations, and they test replacement and eviction paths as carefully as successful lookup paths.",
  "narrationPoints": [
    "Maps, caches, registries, and lookup tables are deceptively difficult under concurrency because they combine several responsibilities.",
    "The first design question is whether reads are truly read-only and what lifetime guarantee a successful lookup provides.",
    "Reader-writer locks can help read-heavy workloads, but they do not remove the need for clear invariants.",
    "Shared access is only safe while operations are actually read-only with respect to every protected field.",
    "Immutable snapshots are often simpler: a reader receives an owned value, a copy of relevant data, or a shared immutable view that remains valid independent of later mutation.",
    "Without those rules, a cache can become an availability risk by growing without bound, serving data beyond its intended lifetime, or destroying entries while other threads still depend on them."
  ]
};
