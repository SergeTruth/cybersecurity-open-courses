window.COURSE_MODULE = {
  "title": "Configuration, Environment Variables, and Deployment Injection",
  "graphicAlt": "Secret-lifecycle diagram for Configuration, Environment Variables, and Deployment Injection, connecting an opaque provider reference to least-privilege retrieval, bounded in-memory use, protected storage, rotation, revocation, safe telemetry, and incident recovery.",
  "narration": "Secret storage and secret delivery are connected. A secret may be protected in a vault but still exposed when it is passed to an application through an unsafe channel. C++ applications often receive configuration from files, environment variables, command-line arguments, service managers, container platforms, CI systems, deployment pipelines, or runtime injection mechanisms. Each path has different visibility, inheritance, and logging behavior.\n\nCommand-line secrets can be visible to process inspection tools, service metadata, shell history, or diagnostic collection. Environment variables are convenient, but they may be inherited by child processes, copied into crash reports, or surfaced by debugging and support tooling. Configuration files require strict permissions, controlled paths, integrity checks, and careful backup behavior. CI and deployment systems can provide safer injection when configured correctly, but they can also leak values through console output, build artifacts, or overly broad project permissions.\n\nApplications should document which delivery mechanisms are approved for each environment and reject unsafe alternatives in production where possible. The delivery path should match the secret type and platform. Local development may use a different controlled mechanism than production, but it should not train developers to paste real credentials into source files or command lines. Clear startup diagnostics can report that a required secret is missing or inaccessible without printing the value itself. Deployment reviews should also confirm that secrets are not copied into images, templates, or generated configuration that outlives the intended release. That combination of approved delivery, redaction, and explicit failure reduces accidental exposure.",
  "narrationPoints": [
    "C++ applications often receive configuration from files, environment variables, command-line arguments, service managers, container platforms, CI systems, deployment pipelines, or runtime injection mechanisms.",
    "Configuration files require strict permissions, controlled paths, integrity checks, and careful backup behavior.",
    "The delivery path should match the secret type and platform.",
    "Deployment reviews should also confirm that secrets are not copied into images, templates, or generated configuration that outlives the intended release.",
    "Applications should document which delivery mechanisms are approved for each environment and reject unsafe alternatives in production where possible.",
    "Clear startup diagnostics can report that a required secret is missing or inaccessible without printing the value itself."
  ]
};
