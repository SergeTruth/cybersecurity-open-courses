window.COURSE_MODULE = {
  "title": "Course Summary: A Secret Storage Roadmap",
  "graphicAlt": "Secret-lifecycle diagram for Course Summary: A Secret Storage Roadmap, connecting an opaque provider reference to least-privilege retrieval, bounded in-memory use, protected storage, rotation, revocation, safe telemetry, and incident recovery.",
  "narration": "A practical secret storage roadmap begins with identification and classification. Decide what is a secret, what it grants, who owns it, which environment it belongs to, how long it should live, and which component truly needs access. Avoid hardcoded and embedded values in source, binaries, documentation, images, and test fixtures. If a secret is committed or packaged, treat removal and rotation as part of the response, not as separate optional cleanup.\n\nUse approved storage and delivery mechanisms. Prefer platform secret stores, managed vaults, orchestrator features, hardware-backed storage, or vetted libraries over custom schemes. Document how secrets reach the application, and reject unsafe delivery paths in production where possible. Keep runtime exposure short and intentional. Minimize copies, avoid broad ownership, do not log secret values, and use secure-memory facilities when the assurance requirement calls for them.\n\nFor file-based secrets, control the path, permissions, key management, backups, and cleanup behavior. Apply least privilege so applications receive only the secrets they need. Make rotation and revocation tested routines. Keep audit records useful without revealing values. Review logs, tests, crash reports, support bundles, build artifacts, dependencies, and deployment settings. When a new service, library, or pipeline step is added, ask how it receives secrets and where those values can be preserved. Secure secret storage is a lifecycle practice. It succeeds when teams make safe handling visible, repeatable, and governed across development and operations.",
  "narrationPoints": [
    "If a secret is committed or packaged, treat removal and rotation as part of the response, not as separate optional cleanup.",
    "Decide what is a secret, what it grants, who owns it, which environment it belongs to, how long it should live, and which component truly needs access.",
    "Avoid hardcoded and embedded values in source, binaries, documentation, images, and test fixtures.",
    "Minimize copies, avoid broad ownership, do not log secret values, and use secure-memory facilities when the assurance requirement calls for them.",
    "Prefer platform secret stores, managed vaults, orchestrator features, hardware-backed storage, or vetted libraries over custom schemes.",
    "Review logs, tests, crash reports, support bundles, build artifacts, dependencies, and deployment settings."
  ]
};
