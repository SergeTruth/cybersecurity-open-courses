window.COURSE_MODULE = {
  "title": "Logging, Error Handling, and Testing",
  "graphicAlt": "Client-service connection diagram for Logging, Error Handling, and Testing, showing exposure policy, handle ownership, partial I/O, exact protocol framing, TLS peer authentication, resource budgets, shutdown, and payload-free operational events.",
  "narration": "Network failures are normal, so they should be tested directly. A secure socket component should have coverage for disconnects, partial reads, partial writes, malformed messages, oversized messages, timeouts, authentication failures, TLS validation failures, shutdown, and retry behavior. These tests do not need to model every possible network condition, but they should prove that common failure paths preserve safe state.\n\nLogging is part of the safety story. Operators need enough information to understand whether a connection failed because of policy, invalid input, timeout, resource pressure, or a dependency problem. At the same time, logs should not print secrets, tokens, credentials, private payloads, session keys, or unnecessary internal details. A helpful log explains the event category and context without turning sensitive network data into stored data.\n\nError responses need similar balance. Legitimate callers should receive clear enough responses to correct valid mistakes, but the service should avoid excessive implementation detail. Internal exception messages, stack traces, parser internals, or raw payload excerpts usually belong in protected diagnostics, not in protocol responses. Consistent error mapping also makes clients easier to write and monitor.\n\nCode review should treat socket code as security-sensitive engineering. Reviewers should check protocol boundaries, resource limits, TLS assumptions, bind behavior, cleanup behavior, and whether failure paths release resources. Release review should confirm that tests cover the intended platform and deployment model. The result is code that is easier to operate and safer to change.",
  "narrationPoints": [
    "A secure socket component should have coverage for disconnects, partial reads, partial writes, malformed messages, oversized messages, timeouts, authentication failures, TLS validation failures, shutdown, and retry behavior.",
    "At the same time, logs should not print secrets, tokens, credentials, private payloads, session keys, or unnecessary internal details.",
    "Internal exception messages, stack traces, parser internals, or raw payload excerpts usually belong in protected diagnostics, not in protocol responses.",
    "Reviewers should check protocol boundaries, resource limits, TLS assumptions, bind behavior, cleanup behavior, and whether failure paths release resources.",
    "Release review should confirm that tests cover the intended platform and deployment model.",
    "Code review should treat socket code as security-sensitive engineering."
  ]
};
