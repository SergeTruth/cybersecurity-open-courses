window.COURSE_MODULE = {
  "title": "Network Trust Boundaries and Protocol Design",
  "graphicAlt": "Client-service connection diagram for Network Trust Boundaries and Protocol Design, showing exposure policy, handle ownership, partial I/O, exact protocol framing, TLS peer authentication, resource budgets, shutdown, and payload-free operational events.",
  "narration": "Network input should not become trusted just because it arrives on an expected port or appears to come from an expected component. A secure design treats the protocol boundary as a formal contract. The contract should define how messages are framed, which commands are allowed, which field types and encodings are valid, how versions are negotiated, what authentication is expected, and how errors are reported.\n\nTCP is a byte stream, not an application message API. One receive operation may return part of a message, multiple messages, or no complete message at all. The application or protocol library must know how to identify a complete frame before it parses fields or makes decisions. Datagram-oriented designs such as UDP have different assumptions: each datagram has boundaries, but delivery, ordering, and size behavior require separate design choices.\n\nAmbiguous parsing is a security and reliability risk. If two components interpret the same bytes differently, the system can make inconsistent decisions about identity, authorization, size, or state. The safest protocols define length limits, allowed encodings, ordering rules, version behavior, and rejection behavior in one place. That definition should be testable, not just implied by whichever parser was written first.\n\nInvalid, incomplete, oversized, unexpected, or out-of-order data should fail safely and consistently. A defensive protocol parser releases temporary resources, avoids partial state commits, and returns clear errors without leaking unnecessary internal detail. The goal is predictable behavior under imperfect network conditions, because imperfect conditions are the normal case for socket programs.",
  "narrationPoints": [
    "Network input should not become trusted just because it arrives on an expected port or appears to come from an expected component.",
    "A secure design treats the protocol boundary as a formal contract.",
    "The contract should define how messages are framed, which commands are allowed, which field types and encodings are valid, how versions are negotiated, what authentication is expected, and how errors are reported.",
    "The application or protocol library must know how to identify a complete frame before it parses fields or makes decisions.",
    "The safest protocols define length limits, allowed encodings, ordering rules, version behavior, and rejection behavior in one place.",
    "A defensive protocol parser releases temporary resources, avoids partial state commits, and returns clear errors without leaking unnecessary internal detail."
  ]
};
