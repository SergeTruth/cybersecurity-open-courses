window.COURSE_MODULE = {
  "title": "TLS, Certificate Validation, and Secure Channels",
  "graphicAlt": "Client-service connection diagram for TLS, Certificate Validation, and Secure Channels, showing exposure policy, handle ownership, partial I/O, exact protocol framing, TLS peer authentication, resource budgets, shutdown, and payload-free operational events.",
  "narration": "Raw sockets do not provide confidentiality, integrity, or peer authentication by themselves. If data crosses an untrusted network or carries sensitive information, a secure-channel mechanism such as TLS is usually required. The defensive goal is not to design custom cryptography. The goal is to use approved libraries, safe defaults, and clear policy so the channel provides the protection the application expects.\n\nCertificate validation matters. A client should verify that the certificate chain is trusted, that the certificate is valid for the current time and policy, and that the hostname matches the service identity it intended to reach. Disabling certificate checks for convenience is a serious production risk because it can turn an encrypted connection into a connection with no reliable peer authentication.\n\nServer-side policy matters as well. Supported protocol versions, certificate configuration, private key handling, client authentication requirements, and library update practices should be owned and reviewed. These settings should follow organizational policy and current platform guidance rather than being copied from old examples. Development exceptions should be visibly separated from production configuration.\n\nTLS errors should fail safely. A certificate failure, handshake failure, policy mismatch, or hostname mismatch should not silently downgrade the connection or continue as if the channel were secure. Diagnostics should be useful for operators without printing secrets or private key material. Secure channels are part of the protocol contract, so tests and release reviews should verify that validation behavior is still active.",
  "narrationPoints": [
    "If data crosses an untrusted network or carries sensitive information, a secure-channel mechanism such as TLS is usually required.",
    "A client should verify that the certificate chain is trusted, that the certificate is valid for the current time and policy, and that the hostname matches the service identity it intended to reach.",
    "Disabling certificate checks for convenience is a serious production risk because it can turn an encrypted connection into a connection with no reliable peer authentication.",
    "Supported protocol versions, certificate configuration, private key handling, client authentication requirements, and library update practices should be owned and reviewed.",
    "Secure channels are part of the protocol contract, so tests and release reviews should verify that validation behavior is still active.",
    "A certificate failure, handshake failure, policy mismatch, or hostname mismatch should not silently downgrade the connection or continue as if the channel were secure."
  ]
};
