window.COURSE_MODULE = {
  "title": "Addressing, Binding, and Exposure Control",
  "graphicAlt": "Client-service connection diagram for Addressing, Binding, and Exposure Control, showing exposure policy, handle ownership, partial I/O, exact protocol framing, TLS peer authentication, resource budgets, shutdown, and payload-free operational events.",
  "narration": "A service's bind address determines who can reach it. Binding to loopback is very different from binding to every interface. IPv4, IPv6, wildcard addresses, public interfaces, local-only services, container networking, and service discovery can all change the actual exposure of a listener. Defensive socket programming starts by making that exposure an explicit deployment decision.\n\nFor local control planes, development-only services, or process-to-process helpers, loopback may be the correct default. For services that must accept remote clients, the bind address, port, firewall expectation, and authentication model should be documented together. A listener should not become externally reachable just because a default address was convenient during development or because the deployment environment changed around it.\n\nClients also need careful address handling. DNS and address resolution can fail, return multiple answers, return IPv4 or IPv6 addresses, or change over time. A client should handle these outcomes safely and avoid assuming that a name or resolved address automatically has the intended trust level. Connection policy, certificate hostname checks, and service identity decisions should be based on defined rules rather than casual string comparisons.\n\nPorts, listeners, and service-discovery behavior belong in operational documentation and release review. The safest design exposes only the required interface, limits unnecessary listeners, and provides clear configuration for operators. Exposure control is not a substitute for authentication or encryption, but it reduces accidental reachability and makes the security model easier to reason about.",
  "narrationPoints": [
    "Defensive socket programming starts by making that exposure an explicit deployment decision.",
    "For services that must accept remote clients, the bind address, port, firewall expectation, and authentication model should be documented together.",
    "Connection policy, certificate hostname checks, and service identity decisions should be based on defined rules rather than casual string comparisons.",
    "A client should handle these outcomes safely and avoid assuming that a name or resolved address automatically has the intended trust level.",
    "The safest design exposes only the required interface, limits unnecessary listeners, and provides clear configuration for operators.",
    "Ports, listeners, and service-discovery behavior belong in operational documentation and release review."
  ]
};
