window.COURSE_MODULE = {
  "title": "Why Socket Security Matters",
  "graphicAlt": "Client-service connection diagram for Why Socket Security Matters, showing exposure policy, handle ownership, partial I/O, exact protocol framing, TLS peer authentication, resource budgets, shutdown, and payload-free operational events.",
  "narration": "Sockets are one of the lowest-level ways C++ applications communicate across a network or between local processes. They appear in servers, clients, agents, embedded devices, desktop applications, service integrations, telemetry collectors, and internal tools. Because they sit close to the operating system and close to raw input, small mistakes can have wide effects on reliability, availability, and confidentiality.\n\nA socket is a trust boundary. The application may receive data from another machine, another process, another user, or an unreliable network path. That data might be incomplete, delayed, malformed, duplicated, unexpectedly large, or simply inconsistent with the protocol the developer had in mind. Secure socket programming begins by assuming that received bytes are untrusted until the protocol has framed, parsed, and validated them.\n\nUnsafe socket code can corrupt application state, leak resources, hang threads, expose sensitive data, accept malformed input, or weaken authentication and encryption decisions. The problem is rarely one dramatic bug. More often, it is a chain of ordinary engineering shortcuts: unclear protocol assumptions, manual cleanup scattered through error paths, missing timeouts, casual logging, or a connection lifecycle that was not designed for failure.\n\nThe defensive mindset is practical. Define the protocol contract, choose exposure deliberately, represent socket ownership clearly, handle partial I/O, set limits, validate inputs, use approved secure-channel libraries, and make failure behavior predictable. Socket code deserves security review because it often becomes part of the application boundary, even when the service is intended only for internal use.",
  "narrationPoints": [
    "Because they sit close to the operating system and close to raw input, small mistakes can have wide effects on reliability, availability, and confidentiality.",
    "Secure socket programming begins by assuming that received bytes are untrusted until the protocol has framed, parsed, and validated them.",
    "The application may receive data from another machine, another process, another user, or an unreliable network path.",
    "Unsafe socket code can corrupt application state, leak resources, hang threads, expose sensitive data, accept malformed input, or weaken authentication and encryption decisions.",
    "Define the protocol contract, choose exposure deliberately, represent socket ownership clearly, handle partial I/O, set limits, validate inputs, use approved secure-channel libraries, and make failure behavior predictable.",
    "Socket code deserves security review because it often becomes part of the application boundary, even when the service is intended only for internal use."
  ]
};
