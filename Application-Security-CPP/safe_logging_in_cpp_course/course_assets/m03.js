window.COURSE_MODULE = {
  "title": "Log Levels, Events, and Context",
  "graphicAlt": "Structured event layout separating severity, stable event name, timestamp, request correlation, component, outcome, and bounded context, with guidance against using free-form severity or secrets as labels.",
  "narration": "Log levels should communicate operational meaning. Debug events help developers inspect detailed behavior. Info events record normal, meaningful state changes. Warnings identify unusual conditions that may need attention. Error events represent failed operations, and critical-style events mark urgent system impact.\n\nThe level should not be chosen only by convenience or habit. If every event is an error, operators lose signal. If important failures are buried at a verbose level, responders may miss them. A level policy helps teams interpret logs consistently.\n\nEvents also need stable names and component context. A clear event name, component, operation, and outcome make logs easier to query and review. Stable fields are better than slightly different free-text messages scattered across the codebase.\n\nSafe correlation is valuable. Request IDs, correlation IDs, operation IDs, tenant-safe identifiers, or non-sensitive object IDs can connect related events without revealing raw sensitive data. The identifier should be designed for investigation, not copied blindly from untrusted input.\n\nAvoid vague messages such as operation failed with no context. Also avoid noisy repetition that floods storage and hides important events. Repeated failures may need rate limiting, aggregation, or a summary event with counters.\n\nThe goal is useful context without over-collection. A maintainer should be able to understand what happened and where to investigate next, while sensitive data, raw inputs, and unnecessary internals remain out of the record.\n\nContext should also be stable across releases. If field names, event names, or status values change casually, dashboards and incident playbooks lose reliability. Treat the event schema as part of the operational interface.\n\nWhen a message needs more detail, prefer adding a reviewed structured field over stuffing more prose into the text. That keeps the event searchable and makes future privacy review much easier.",
  "narrationPoints": [
    "Error events represent failed operations, and critical-style events mark urgent system impact.",
    "If important failures are buried at a verbose level, responders may miss them.",
    "A clear event name, component, operation, and outcome make logs easier to query and review.",
    "The identifier should be designed for investigation, not copied blindly from untrusted input.",
    "Repeated failures may need rate limiting, aggregation, or a summary event with counters.",
    "A maintainer should be able to understand what happened and where to investigate next, while sensitive data, raw inputs, and unnecessary internals remain out of the record."
  ]
};
