window.COURSE_MODULE = {
  "title": "Error Handling, Exceptions, and Diagnostic Boundaries",
  "graphicAlt": "Diagnostic boundary diagram translating internal exceptions and system errors into stable public events, retaining detailed evidence only in restricted sinks and avoiding secrets, raw payloads, and misleading success records.",
  "narration": "Failure paths are where logging is most useful and most risky. Exceptions, error codes, failed parsing, rejected authentication, unexpected states, and cleanup failures all create pressure to record more detail. Safe logging keeps that detail controlled.\n\nSeparate user-facing errors from internal diagnostics. A user-facing message may say that an operation failed or input was invalid. Internal logs may include component names, safe identifiers, operation context, and error categories that help maintainers investigate.\n\nEven internal logs need limits. Do not include raw secrets, full request bodies, sensitive records, or broad object dumps just because an exception was caught. Exception messages from libraries should be reviewed before they are logged in production contexts.\n\nLogging should not destabilize cleanup paths. Destructors and stack unwinding paths should avoid operations that can throw or block unpredictably. If cleanup itself can fail, the project should define how that condition is reported without hiding the original failure.\n\nPreserve diagnostic usefulness. Record what operation failed, where it happened, a safe correlation value, and enough classification to support triage. Avoid replacing a meaningful original error with a vague logging failure or secondary exception.\n\nThe boundary question is: who should see this detail? Maintainers may need structured diagnostic context. Users and external callers usually need a safe summary. Good logging design serves both audiences without mixing them.\n\nException-heavy code should also avoid logging the same failure at every layer. Repeated logging can create noise and duplicate sensitive context. Decide which boundary records the diagnostic event and which layers add structured context or simply propagate the error.\n\nWhen cleanup fails, record the cleanup issue without losing the primary error. A secondary diagnostic should help maintainers understand the full sequence while preserving the original cause and keeping secrets out of the event.",
  "narrationPoints": [
    "Exceptions, error codes, failed parsing, rejected authentication, unexpected states, and cleanup failures all create pressure to record more detail.",
    "Internal logs may include component names, safe identifiers, operation context, and error categories that help maintainers investigate.",
    "Do not include raw secrets, full request bodies, sensitive records, or broad object dumps just because an exception was caught.",
    "Destructors and stack unwinding paths should avoid operations that can throw or block unpredictably.",
    "Avoid replacing a meaningful original error with a vague logging failure or secondary exception.",
    "Decide which boundary records the diagnostic event and which layers add structured context or simply propagate the error."
  ]
};
