window.COURSE_MODULE = {
  "title": "What Not to Log",
  "graphicAlt": "Redaction map placing passwords, tokens, private keys, session identifiers, personal data, and sensitive payloads outside the log record while preserving safe event identifiers and operational context.",
  "narration": "Safe logging starts with data minimization. A log should contain the information needed for a defined operational purpose, not every value that happens to be in scope. The easiest sensitive log to manage is the one never written.\n\nSecrets do not belong in logs. Passwords, API keys, tokens, session identifiers, private keys, authorization headers, signing material, recovery codes, and sensitive configuration values should be excluded or represented through carefully designed safe markers.\n\nPersonal and regulated data require the same discipline. Names, addresses, account details, device identifiers, location information, health or financial data, and other regulated fields should be minimized according to policy. Even when one field looks harmless, logs can aggregate context across many events.\n\nAvoid logging full objects, raw request bodies, large records, memory dumps, or broad exception details without review. These shortcuts can turn diagnostic storage into a sensitive data store that is copied, indexed, retained, and viewed by more people than the original system.\n\nRedaction should happen before data reaches the log sink. Once sensitive data is written to a local file, queue, agent, collector, or search system, removal becomes much harder. Treat redaction as part of event construction, not a later cleanup wish.\n\nInternal paths and implementation details should also be limited. Maintainers need enough context to investigate, but logs should not reveal unnecessary local directory layouts, internal topology, or configuration details to audiences that do not need them.\n\nA useful review question is: if this log line were copied into a ticket, retained for a year, or viewed by another operations team, would the data still be appropriate? If not, redesign the event before shipping it.\n\nFor C++ code, be especially cautious with helper functions that stringify entire objects. A convenience operator or serializer may include fields the caller did not intend to expose. Safe logging should choose fields deliberately instead of relying on broad object formatting.",
  "narrationPoints": [
    "A log should contain the information needed for a defined operational purpose, not every value that happens to be in scope.",
    "Avoid logging full objects, raw request bodies, large records, memory dumps, or broad exception details without review.",
    "Treat redaction as part of event construction, not a later cleanup wish.",
    "Maintainers need enough context to investigate, but logs should not reveal unnecessary local directory layouts, internal topology, or configuration details to audiences that do not need them.",
    "A useful review question is: if this log line were copied into a ticket, retained for a year, or viewed by another operations team, would the data still be appropriate?",
    "Safe logging should choose fields deliberately instead of relying on broad object formatting."
  ]
};
