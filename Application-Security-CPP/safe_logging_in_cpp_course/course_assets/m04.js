window.COURSE_MODULE = {
  "title": "Structured Logging and Safe Message Construction",
  "graphicAlt": "Record-construction pipeline showing typed fields entering a bounded serializer, correct JSON escaping, fixed event names, redaction before formatting, and rejection when size or schema limits are exceeded.",
  "narration": "Structured logging records events as stable fields rather than only free-form text. A C++ service might record an event name, component, operation, outcome, duration, safe identifier, and bounded error category. Downstream tools can query those fields reliably.\n\nConsistent schemas make logs more useful. If one component writes request_id, another writes correlation, and another hides the same value in a message string, investigations become slower and automation becomes brittle. Shared field names and event conventions reduce ambiguity.\n\nMessage construction should be safe by design. Prefer fixed templates with values passed as separate fields. This keeps trusted labels separate from untrusted or variable data and reduces the chance that formatting rules change the meaning of the event.\n\nDo not treat untrusted text as a format string. In C and C++ style formatting APIs, the format string controls interpretation. Keep format templates constant and reviewed, and pass variable values through typed formatting or structured fields.\n\nAvoid ad hoc concatenation that mixes trusted labels with raw values. Concatenated strings are harder to parse, harder to redact, and easier to make inconsistent. Structured fields make review and downstream processing more predictable.\n\nStructured logging does not automatically make every event safe. Field selection, redaction, bounds, encoding, and access control still matter. The benefit is that the event's shape is easier to inspect and govern.\n\nA well-constructed event should be readable by humans, processable by tools, and reviewable by maintainers. That combination supports debugging, monitoring, compliance, and privacy goals without turning logs into uncontrolled text dumps.\n\nType-safe formatting is part of that discipline. Prefer APIs that separate templates from values and make formatting failures visible during development. Avoid designs where a runtime string can accidentally control how other values are interpreted.\n\nStructured fields also help reduce unnecessary duplication. Instead of repeating component, outcome, duration, and identifier text inside every message, store them once as fields and let the message summarize the event clearly.",
  "narrationPoints": [
    "A C++ service might record an event name, component, operation, outcome, duration, safe identifier, and bounded error category.",
    "Keep format templates constant and reviewed, and pass variable values through typed formatting or structured fields.",
    "Structured fields make review and downstream processing more predictable.",
    "Field selection, redaction, bounds, encoding, and access control still matter.",
    "A well-constructed event should be readable by humans, processable by tools, and reviewable by maintainers.",
    "Avoid designs where a runtime string can accidentally control how other values are interpreted."
  ]
};
