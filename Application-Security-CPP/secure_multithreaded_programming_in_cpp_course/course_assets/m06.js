window.COURSE_MODULE = {
  "title": "Thread Lifetime, Ownership, and Cancellation",
  "graphicAlt": "Concurrent execution diagram for Thread Lifetime, Ownership, and Cancellation, showing worker lanes around protected shared state, scoped synchronization, cancellation and shutdown paths, plus a deterministic test checkpoint for the shared invariant.",
  "narration": "Thread lifetime is a core part of secure concurrent design. A thread should have a clear owner, a clear unit of work, and a clear shutdown path. If the code cannot explain who is responsible for stopping and joining a thread, cleanup risk is already present.\n\nA std::thread must be joined or otherwise handled according to its contract. Joining gives the owning code a synchronization point and helps ensure that referenced objects are still alive while the thread uses them. std::jthread, when available, improves this pattern by joining in its destructor and supporting cooperative stop requests.\n\nDetached threads are review-sensitive. Detaching removes the easy join point and makes lifetime, shutdown, error reporting, and resource cleanup harder to reason about. It may be appropriate in narrow designs, but it should not be the default escape from ownership complexity.\n\nCaptured this is a common source of lifetime risk. A background task that stores a pointer or reference to an object must not run after that object is destroyed unless ownership has been transferred or lifetime is intentionally extended.\n\nCancellation should be cooperative and explicit. Threads should check stop flags, stop tokens, queue shutdown states, or other documented signals at appropriate points. Forced assumptions about when a thread will stop make cleanup brittle.\n\nThread pools and background workers need the same discipline. Work items should not outlive the data they reference, and shutdown should stop accepting new work, notify waiting workers, drain or cancel pending work according to policy, and join worker threads.\n\nReview lifetime by following references, captures, buffers, callbacks, and ownership transfers across the thread boundary. Secure cleanup depends on knowing which objects remain valid until each thread is done with them.",
  "narrationPoints": [
    "Thread lifetime is a core part of secure concurrent design.",
    "Joining gives the owning code a synchronization point and helps ensure that referenced objects are still alive while the thread uses them.",
    "Detaching removes the easy join point and makes lifetime, shutdown, error reporting, and resource cleanup harder to reason about.",
    "A background task that stores a pointer or reference to an object must not run after that object is destroyed unless ownership has been transferred or lifetime is intentionally extended.",
    "Work items should not outlive the data they reference, and shutdown should stop accepting new work, notify waiting workers, drain or cancel pending work according to policy, and join worker threads.",
    "Review lifetime by following references, captures, buffers, callbacks, and ownership transfers across the thread boundary."
  ]
};
