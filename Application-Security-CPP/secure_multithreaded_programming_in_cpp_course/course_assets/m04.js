window.COURSE_MODULE = {
  "title": "Deadlocks, Lock Ordering, and Blocking Risk",
  "graphicAlt": "Concurrent execution diagram for Deadlocks, Lock Ordering, and Blocking Risk, showing worker lanes around protected shared state, scoped synchronization, cancellation and shutdown paths, plus a deterministic test checkpoint for the shared invariant.",
  "narration": "Deadlock is an availability and reliability risk. It can occur when threads wait on each other in a cycle, often because different code paths acquire the same locks in different orders. Once that cycle forms, progress may stop even though every individual lock operation looks reasonable in isolation.\n\nLock ordering is a design convention that prevents many nested-lock problems. If a component sometimes needs two or more locks, define the order in which they must be acquired and review code against that order. std::scoped_lock can help acquire multiple mutexes safely, but the larger design still needs a clear ownership model.\n\nBlocking work inside critical sections increases risk. Network calls, file I/O, callbacks into other components, long computations, waits on other subsystems, and logging that can block all extend the time other threads are unable to proceed.\n\nKeep critical sections small and deliberate. Copy the data needed for longer work, release the lock, then perform the slower operation when possible. This reduces contention and makes it easier to reason about which invariants are protected.\n\nTimeouts can be useful for operational behavior, but they are not a substitute for sound design. A timeout may help a thread recover or report a problem, but it does not explain why lock acquisition became risky or whether shared state is still consistent.\n\nShutdown paths deserve special attention. If one thread is trying to stop workers while another waits on a lock held by a worker that is waiting for shutdown, the service can stall at exactly the moment reliability matters most.\n\nReview deadlock risk by tracing lock order, blocking calls, callbacks, and shutdown interactions. The goal is not only to avoid permanent waits, but to keep the system responsive under contention and failure.",
  "narrationPoints": [
    "Once that cycle forms, progress may stop even though every individual lock operation looks reasonable in isolation.",
    "If a component sometimes needs two or more locks, define the order in which they must be acquired and review code against that order.",
    "Network calls, file I/O, callbacks into other components, long computations, waits on other subsystems, and logging that can block all extend the time other threads are unable to proceed.",
    "Copy the data needed for longer work, release the lock, then perform the slower operation when possible.",
    "A timeout may help a thread recover or report a problem, but it does not explain why lock acquisition became risky or whether shared state is still consistent.",
    "Review deadlock risk by tracing lock order, blocking calls, callbacks, and shutdown interactions."
  ]
};
