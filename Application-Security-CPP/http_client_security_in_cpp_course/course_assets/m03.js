window.COURSE_MODULE = {
  "title": "TLS, Certificates, and Endpoint Identity",
  "graphicAlt": "TLS connection diagram binding the intended api.example.com origin to hostname verification, peer certificate validation, TLS 1.3 minimum, and a reviewed trust store.",
  "narration": "HTTPS security depends on both encryption and endpoint authentication. A C++ HTTP client should validate the certificate chain, verify that the certificate matches the intended hostname, and use a trust store that is appropriate for the environment. Encryption without the correct endpoint identity check does not provide the trust decision most applications expect.\n\nValidation failures should stop the operation by default. Expired certificates, untrusted issuers, hostname mismatches, incomplete chains, and unsupported trust configuration are signals that the client cannot confidently identify the endpoint. The safe response is predictable failure, clear diagnostics, and an operational path for fixing the configuration or certificate.\n\nTrust-store selection should be intentional. Some applications rely on the operating system trust store. Others use a private PKI, a managed certificate bundle, or mutual TLS for service-to-service identity. Each choice needs ownership, update policy, environment separation, and tests that confirm the expected behavior. Development certificates can be useful during local work, but development convenience must not become production policy.\n\nClient certificates and private keys are authentication material. They should be stored and loaded through approved mechanisms, protected with appropriate filesystem or secret-store controls, rotated when required, and kept out of logs, traces, and support artifacts. The same care applies to passphrases or configuration values used to access them.\n\nThe practical habit is simple: keep TLS validation on, document the trust model, test both successful and failed validation paths, and make endpoint identity part of the client review checklist. Teams should also record who owns certificates, trust bundles, renewal automation, and emergency replacement procedures. When the trust model is written down, operations teams can fix certificate problems without asking developers to weaken the client, and reviewers can confirm that local test shortcuts are not present in production configuration or shared release settings anywhere by mistake.",
  "narrationPoints": [
    "Encryption without the correct endpoint identity check does not provide the trust decision most applications expect.",
    "The safe response is predictable failure, clear diagnostics, and an operational path for fixing the configuration or certificate.",
    "Others use a private PKI, a managed certificate bundle, or mutual TLS for service-to-service identity.",
    "The same care applies to passphrases or configuration values used to access them.",
    "The practical habit is simple: keep TLS validation on, document the trust model, test both successful and failed validation paths, and make endpoint identity part of the client review checklist.",
    "Teams should also record who owns certificates, trust bundles, renewal automation, and emergency replacement procedures."
  ]
};
