window.COURSE_MODULE = {
  "title": "Authentication, Tokens, Cookies, and Secrets",
  "graphicAlt": "Credential flow showing a short-lived token retrieved only after origin approval, attached to one request, omitted from diagnostics, and never forwarded to another destination.",
  "narration": "HTTP clients often carry authentication material: API keys, bearer tokens, authorization headers, cookies, client certificates, refresh tokens, service credentials, or signed request metadata. These values connect the local application to remote authority, so they should be handled as secrets rather than ordinary configuration strings.\n\nCredentials should come from approved configuration and secret-management mechanisms. Hardcoded keys, debug defaults, copied sample tokens, and shared service credentials make review and rotation harder. A secure design identifies where the credential is stored, who can read it, how it is deployed, how it is rotated, and which environment or service it belongs to.\n\nScope matters. Tokens and service identities should grant only the operations the client needs. Development, staging, and production credentials should be separated. Credentials for unrelated services should not be reused just because it is convenient. Expiration and rotation behavior should be planned so the application handles renewal or replacement without exposing values or failing unpredictably.\n\nCookies require the same care in non-browser clients. A client that stores or forwards cookies should have a clear policy for which domains, paths, lifetimes, and services are involved. Accidental credential forwarding can turn a simple redirect or shared client object into a security problem.\n\nLogging is a common place for mistakes. Authorization headers, cookies, bearer tokens, private keys, signed request material, and sensitive response data should not appear in logs, exceptions, crash reports, traces, or support bundles. Useful diagnostics can record safe categories, endpoint labels, status classes, timing, and correlation IDs without exposing the credential itself. The goal is to make support possible while keeping secret values out of durable records that many people and systems may later access during normal production operations safely.",
  "narrationPoints": [
    "HTTP clients often carry authentication material: API keys, bearer tokens, authorization headers, cookies, client certificates, refresh tokens, service credentials, or signed request metadata.",
    "Hardcoded keys, debug defaults, copied sample tokens, and shared service credentials make review and rotation harder.",
    "A secure design identifies where the credential is stored, who can read it, how it is deployed, how it is rotated, and which environment or service it belongs to.",
    "Tokens and service identities should grant only the operations the client needs.",
    "A client that stores or forwards cookies should have a clear policy for which domains, paths, lifetimes, and services are involved.",
    "Useful diagnostics can record safe categories, endpoint labels, status classes, timing, and correlation IDs without exposing the credential itself."
  ]
};
