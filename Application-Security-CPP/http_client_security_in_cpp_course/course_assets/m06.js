window.COURSE_MODULE = {
  "title": "Redirects, Proxies, DNS, and Destination Control",
  "graphicAlt": "Redirect sequence showing every Location target passing exact scheme, host, credential, port, and hop-limit checks while ambient proxies remain disabled.",
  "narration": "Destination control is central to HTTP client security. The URL in code or configuration is not always the final network destination. Redirects can change the scheme, host, port, or path. Proxy settings can route traffic through another component. DNS resolution can map a name to different addresses over time. Each layer should be considered in the client policy.\n\nRedirect behavior should be explicit. Some clients should not follow redirects at all. Others may allow a small number of redirects within the same host or approved service boundary. Credential forwarding across redirects needs careful review because an authorization header, cookie, client certificate context, or sensitive request body may no longer be appropriate after the destination changes.\n\nProxy configuration is also security-relevant. A proxy may be required for enterprise egress, inspection, or network policy. It may also be supplied through environment settings or deployment configuration. The application should define which proxy sources it trusts, how proxy credentials are protected, and whether sensitive service traffic may use a proxy.\n\nApplications that accept user-controlled URLs need a clear destination model. Some features should only call approved services. Some should accept only preconfigured endpoint names rather than arbitrary URLs. Where a destination allowlist is used, it should consider scheme, hostname, port, and any service-specific path requirements.\n\nThe key habit is bounded behavior. Redirect counts, allowed schemes, credential forwarding, proxy use, and destination selection should be visible in code and tests. That makes the client easier to operate and safer to change. It also prevents configuration drift from becoming invisible policy, because reviewers can compare the intended destination model with what the running application will actually allow in each environment before release review.",
  "narrationPoints": [
    "The URL in code or configuration is not always the final network destination.",
    "Credential forwarding across redirects needs careful review because an authorization header, cookie, client certificate context, or sensitive request body may no longer be appropriate after the destination changes.",
    "Others may allow a small number of redirects within the same host or approved service boundary.",
    "A proxy may be required for enterprise egress, inspection, or network policy.",
    "Where a destination allowlist is used, it should consider scheme, hostname, port, and any service-specific path requirements.",
    "Redirect counts, allowed schemes, credential forwarding, proxy use, and destination selection should be visible in code and tests."
  ]
};
