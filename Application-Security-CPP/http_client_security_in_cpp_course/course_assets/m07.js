window.COURSE_MODULE = {
  "title": "Response Handling, Parsing, and Resource Limits",
  "graphicAlt": "Streaming response diagram with chunks accumulating under a byte ceiling, followed by media-type verification, strict decoding, and a closed order-summary schema.",
  "narration": "HTTP responses are input. A successful connection or a 200 status code does not automatically make the response safe, complete, correctly typed, or semantically trusted. The client should validate status codes, content type, expected schema, character encoding, and application-level meaning before using a response to update internal state.\n\nResource limits belong in response handling. A response body can be larger than expected, arrive slowly, use compression, or contain nested structured data. Clients should set maximum response sizes where practical, prefer streaming for large downloads, constrain decompression behavior, and avoid buffering entire bodies blindly. Allocation decisions should happen after the client has enough information to bound the work.\n\nParsing should be structured and defensive. Use maintained parsers for JSON, XML, headers, archives, or other formats instead of ad hoc text slicing. Validate required fields, numeric ranges, enum values, version fields, and cross-field relationships before converting remote data into trusted application objects. Parser failures should leave the application in a known state.\n\nFile downloads need special policy. The client should define allowed content types, destination paths, overwrite behavior, temporary file handling, size limits, integrity checks where required by policy, and cleanup for partial files. A failed or cancelled download should not leave confusing artifacts that later code treats as complete.\n\nThe safe mental model is consistent: responses come from outside the local trust boundary. Read them with limits, parse them with maintained components, validate their meaning, and only then use them in trusted application flows. Include failure cases in tests, especially oversized bodies, unexpected content types, invalid encodings, partial downloads, and remote data that is well-formed but outside the application contract or current service version expectations and limits clearly.",
  "narrationPoints": [
    "The client should validate status codes, content type, expected schema, character encoding, and application-level meaning before using a response to update internal state.",
    "Allocation decisions should happen after the client has enough information to bound the work.",
    "Validate required fields, numeric ranges, enum values, version fields, and cross-field relationships before converting remote data into trusted application objects.",
    "The client should define allowed content types, destination paths, overwrite behavior, temporary file handling, size limits, integrity checks where required by policy, and cleanup for partial files.",
    "Read them with limits, parse them with maintained components, validate their meaning, and only then use them in trusted application flows.",
    "The safe mental model is consistent: responses come from outside the local trust boundary."
  ]
};
