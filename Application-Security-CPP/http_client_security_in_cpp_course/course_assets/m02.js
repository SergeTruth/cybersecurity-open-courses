window.COURSE_MODULE = {
  "title": "Choosing and Configuring HTTP Libraries",
  "graphicAlt": "Adapter diagram showing one maintained HTTP library receiving centrally reviewed origin, TLS, proxy, redirect, timeout, and response-limit settings used by every call site.",
  "narration": "Choosing an HTTP library is an engineering and governance decision. A maintained library or approved platform API gives the team documented behavior, security updates, TLS support, timeout controls, proxy handling, redirect policy, streaming options, and known platform compatibility. That is a stronger foundation than ad hoc socket code or custom protocol parsing.\n\nThe selection process should include security-relevant questions. Does the library perform certificate-chain validation and hostname verification through supported mechanisms? Can callers control redirects, proxy use, request limits, and response streaming? Are timeouts available for connect, read, and total operation deadlines? Is the library actively maintained, and does the team know how updates are reviewed and deployed?\n\nConfiguration visibility matters as much as the library choice. A wrapper can help standardize timeouts, logging, allowed destinations, retry behavior, and error handling across a codebase. That wrapper should make security-sensitive behavior easier to review, not hide it behind vague defaults. Reviewers should still be able to see whether validation is enabled, which trust store is used, and how redirects or proxies are treated.\n\nLibrary ownership should also be explicit. Someone needs to track versions, supported platforms, deprecations, release notes, and compatibility with the application build. Network code often becomes long-lived infrastructure, so update policy and test coverage are part of secure design.\n\nA mature client stack keeps teams focused on application policy instead of reimplementing protocol details. Use the library for protocol structure, transport support, and parsing primitives, then configure it deliberately for the application’s trust, reliability, and observability needs. The review should also cover how the library behaves on each supported platform, because certificate stores, proxy settings, and networking defaults may vary between desktop, server, container, and embedded deployments.",
  "narrationPoints": [
    "A maintained library or approved platform API gives the team documented behavior, security updates, TLS support, timeout controls, proxy handling, redirect policy, streaming options, and known platform compatibility.",
    "Is the library actively maintained, and does the team know how updates are reviewed and deployed.",
    "Reviewers should still be able to see whether validation is enabled, which trust store is used, and how redirects or proxies are treated.",
    "Network code often becomes long-lived infrastructure, so update policy and test coverage are part of secure design.",
    "Use the library for protocol structure, transport support, and parsing primitives, then configure it deliberately for the application’s trust, reliability, and observability needs.",
    "The review should also cover how the library behaves on each supported platform, because certificate stores, proxy settings, and networking defaults may vary between desktop, server, container, and embedded deployments."
  ]
};
