window.COURSE_MODULE = {
  "title": "Parsing Before Trusting",
  "graphicAlt": "Exact-frame diagram showing version and length fields consuming the complete message, with separate rejection paths for truncation, unknown versions, embedded nulls, and trailing bytes.",
  "narration": "Parsing and validation should be deliberate steps. Parsing turns raw text or bytes into a structured value. Validation decides whether that value is acceptable for the program's contract. In C++, those steps are often close together, but it is useful to keep the responsibilities distinct.\n\nEvery parse result should report success or failure in a way the caller must handle. A clear result object, std::optional, expected-style return type, status code, or exception policy can all work when used consistently. The important point is that failure is visible and cannot be accidentally ignored.\n\nPartial parses deserve special attention. A conversion that reads a leading number and leaves trailing characters can make the program accept something other than the intended value. A parser should report how much input was consumed or otherwise reject extra content that does not belong to the contract.\n\nAvoid silent fallback behavior. Defaulting a failed parse to zero, an empty string, the first enum value, or a permissive mode can hide invalid input and move uncertainty deeper into the program. Defaults should be intentional and limited to cases where the contract says absence is allowed.\n\nKeep raw input separate from parsed and trusted values. Use names and types that make the distinction obvious. For example, raw_count_text and validated_count communicate different states. That discipline helps reviewers find places where a raw value might be used too early.\n\nFinally, make parse failures testable. Unit tests should cover invalid text, trailing content, missing fields, wrong delimiters, duplicate values, and boundary cases. A validation system is stronger when failure cases are ordinary, documented behavior rather than rare surprises.\n\nParsing code should also avoid mixing diagnostics with acceptance decisions. A clear error can explain what failed, while the caller still receives an unmistakable rejected result. That separation keeps recovery paths predictable.",
  "narrationPoints": [
    "A clear result object, std::optional, expected-style return type, status code, or exception policy can all work when used consistently.",
    "A parser should report how much input was consumed or otherwise reject extra content that does not belong to the contract.",
    "Defaulting a failed parse to zero, an empty string, the first enum value, or a permissive mode can hide invalid input and move uncertainty deeper into the program.",
    "Keep raw input separate from parsed and trusted values.",
    "A validation system is stronger when failure cases are ordinary, documented behavior rather than rare surprises.",
    "A clear error can explain what failed, while the caller still receives an unmistakable rejected result."
  ]
};
