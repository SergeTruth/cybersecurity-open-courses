window.COURSE_MODULE = {
  "title": "Numeric Input and Size Calculations",
  "graphicAlt": "Numeric pipeline showing full from_chars consumption, count bounds, overflow-safe multiplication, and an allocation size produced only after every precondition succeeds.",
  "narration": "Numeric input is a common source of subtle validation defects. A number can parse successfully and still be unsafe or meaningless for the intended use. Counts, offsets, indexes, lengths, capacities, timeouts, and allocation sizes all need domain checks before they influence program behavior.\n\nSigned and unsigned boundaries should be reviewed carefully. A negative value may become a very large value if it is converted without checking. A large unsigned value may not fit in a signed target type. A value that fits in one platform type may not fit in another. Validation should happen before narrowing conversions.\n\nExternally influenced sizes should be bounded before allocation, loop bounds, copy operations, indexing, slicing, and buffer growth. A count is not ready for use simply because it was represented as an integer. It must be within the range the program can process safely and reasonably.\n\nDerived calculations need overflow-aware thinking. If the program multiplies a record count by an item size, adds a header length, or converts units, the derived value may exceed the target type even when each input value looks acceptable by itself. Validate both inputs and results.\n\nIndexes and offsets should be checked against the actual container or record being used. An index that is valid for one array length may be invalid after filtering, decoding, or truncation. Keep the validated value close to the object it applies to.\n\nThe review question is simple: what will this number control? If it controls memory, iteration, selection, timing, or resource use, validate the range, unit, type, conversion, and derived calculations before trusting it.\n\nUse numeric parsing APIs that let the program distinguish successful conversion, out-of-range values, and leftover input. Then preserve the checked type when possible instead of repeatedly converting the same raw value in different locations.",
  "narrationPoints": [
    "A number can parse successfully and still be unsafe or meaningless for the intended use.",
    "Externally influenced sizes should be bounded before allocation, loop bounds, copy operations, indexing, slicing, and buffer growth.",
    "If the program multiplies a record count by an item size, adds a header length, or converts units, the derived value may exceed the target type even when each input value looks acceptable by itself.",
    "An index that is valid for one array length may be invalid after filtering, decoding, or truncation.",
    "If it controls memory, iteration, selection, timing, or resource use, validate the range, unit, type, conversion, and derived calculations before trusting it.",
    "Use numeric parsing APIs that let the program distinguish successful conversion, out-of-range values, and leftover input."
  ]
};
