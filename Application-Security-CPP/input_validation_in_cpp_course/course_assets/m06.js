window.COURSE_MODULE = {
  "title": "Structured Inputs: Files, Config, and Data Formats",
  "graphicAlt": "Structured-input tree showing required fields, duplicate detection, unknown-field rejection, exact boolean conversion, bounded numeric parsing, and a closed UploadPolicy result.",
  "narration": "Structured inputs include configuration files, command-line option sets, environment-derived settings, file metadata, serialized records, JSON-like documents, binary records, and API messages. These inputs often parse into a convenient object, but parsing alone does not mean the object satisfies the program's contract.\n\nValidate the outer structure before deeper use. Check required fields, field types, unexpected fields, duplicate entries, version fields, and record sizes. If the format allows arrays or maps, validate maximum item counts before iterating through everything or storing the result.\n\nNesting depth and recursive structures need limits. A deeply nested document may be valid according to a general parser but unreasonable for your program. Decide how much nesting, expansion, and indirection is acceptable for the feature being implemented.\n\nCross-field consistency is part of validation. A declared count should match the number of records actually present. A version should match the fields being used. A mode may require one field and disallow another. A size field should agree with the decoded data it describes.\n\nUse robust libraries where appropriate, especially for established formats. A parser library can handle syntax and low-level format rules, while your code validates the domain contract after parsing. That second step is where business rules, resource limits, and internal type conversion belong.\n\nConvert accepted external structures into narrow internal domain objects. Do not let the rest of the program depend on a loosely typed external document. A clean internal representation keeps validation centralized and makes later code easier to reason about.\n\nWhen formats evolve, validation should evolve deliberately. Version fields, feature flags, and migration rules should be checked before older and newer records are mixed in ways the program was not designed to handle.",
  "narrationPoints": [
    "Structured inputs include configuration files, command-line option sets, environment-derived settings, file metadata, serialized records, JSON-like documents, binary records, and API messages.",
    "If the format allows arrays or maps, validate maximum item counts before iterating through everything or storing the result.",
    "A size field should agree with the decoded data it describes.",
    "A parser library can handle syntax and low-level format rules, while your code validates the domain contract after parsing.",
    "A clean internal representation keeps validation centralized and makes later code easier to reason about.",
    "Version fields, feature flags, and migration rules should be checked before older and newer records are mixed in ways the program was not designed to handle."
  ]
};
