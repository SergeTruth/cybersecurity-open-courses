window.COURSE_MODULE = {
  "title": "Text, Encoding, and Canonicalization",
  "graphicAlt": "Text-processing diagram showing UTF-8 validation followed by explicitly limited ASCII-edge trimming, with full Unicode normalization assigned to a reviewed Unicode library.",
  "narration": "Text validation is more complex than checking whether a string is nonempty. The program has to know what encoding it expects, how invalid byte sequences are handled, which characters are allowed, what length means, and whether comparisons are case-sensitive or locale-sensitive.\n\nDifferent components can interpret the same text differently. One layer may preserve whitespace, another may trim it, and another may fold case. One component may treat two representations as equivalent while another treats them as different. Validation should happen on the form the program will actually use.\n\nCanonicalization means converting input into a consistent representation before comparison or policy decisions. That can include trimming where appropriate, normalizing case for identifiers that are case-insensitive, applying a documented Unicode or locale policy, or resolving a path-like value through a defensive, approved API.\n\nWhitespace rules should be explicit. Leading spaces, trailing spaces, tabs, line endings, empty strings, and visually similar values can create confusion if each component makes its own decision. Decide whether whitespace is meaningful, normalized, or rejected for each field.\n\nPath-like values should remain boundary-controlled. Treat them as structured inputs with expected base locations, allowed forms, and canonical comparison rules. Keep this defensive: the goal is to ensure the program works with the intended canonical value, not a differently interpreted string.\n\nAfter text is validated and canonicalized, store the accepted representation separately from the raw string. Reviewers should be able to identify which string is raw input, which one is canonical, and which one is safe for the program's specific use.\n\nBe especially careful when values pass between libraries, operating system APIs, databases, and user interfaces. Each layer may have its own comparison and normalization rules, so centralizing the accepted text policy reduces disagreement.",
  "narrationPoints": [
    "Text validation is more complex than checking whether a string is nonempty.",
    "One component may treat two representations as equivalent while another treats them as different.",
    "Canonicalization means converting input into a consistent representation before comparison or policy decisions.",
    "Treat them as structured inputs with expected base locations, allowed forms, and canonical comparison rules.",
    "Reviewers should be able to identify which string is raw input, which one is canonical, and which one is safe for the program's specific use.",
    "Be especially careful when values pass between libraries, operating system APIs, databases, and user interfaces."
  ]
};
