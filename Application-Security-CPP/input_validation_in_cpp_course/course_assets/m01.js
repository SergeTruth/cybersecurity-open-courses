window.COURSE_MODULE = {
  "title": "Input Validation as a Trust Boundary",
  "graphicAlt": "Trust-boundary diagram showing external bytes converted through decoding, structural parsing, range checks, authorization, and resource limits before a typed value enters application state.",
  "narration": "Input validation is the point where external data crosses into trusted program logic. In C++, that boundary deserves special attention because raw bytes, strings, counts, indexes, configuration values, and records can quickly influence memory layout, object state, resource use, and control flow.\n\nExternal input does not come only from forms or command-line prompts. It can arrive through files, command-line arguments, environment variables, network messages, configuration, plugins, API responses, databases, build metadata, and other components. A value may look internal simply because another service or library handed it to your code, but it still needs a contract before it becomes trusted state.\n\nValidation is not just a formatting step. It is a boundary-control decision. The program decides whether the data has the expected shape, type, range, encoding, size, and relationship to other values. If the answer is no, the program should fail predictably before the value reaches deeper logic.\n\nA reliable design keeps raw input separate from trusted internal values. Raw text or bytes can be stored in clearly named variables and converted into narrow domain types only after parsing and validation succeed. That separation helps reviewers see where trust changes.\n\nValidation also supports memory safety and resource safety. Counts should be bounded before allocation. Indexes should be checked before use. Text should be interpreted consistently. Structured records should be validated before conversion. When validation is explicit, the rest of the program can operate on clearer assumptions.\n\nFor review, ask where input first enters, what contract it must satisfy, and where the trusted representation begins. Strong C++ validation is a design habit, not a last-minute filter.",
  "narrationPoints": [
    "In C++, that boundary deserves special attention because raw bytes, strings, counts, indexes, configuration values, and records can quickly influence memory layout, object state, resource use, and control flow.",
    "A value may look internal simply because another service or library handed it to your code, but it still needs a contract before it becomes trusted state.",
    "The program decides whether the data has the expected shape, type, range, encoding, size, and relationship to other values.",
    "Raw text or bytes can be stored in clearly named variables and converted into narrow domain types only after parsing and validation succeed.",
    "Validation also supports memory safety and resource safety.",
    "Ask where input first enters, what contract it must satisfy, and where the trusted representation begins."
  ]
};
