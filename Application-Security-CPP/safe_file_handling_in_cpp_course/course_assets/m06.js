window.COURSE_MODULE = {
  "title": "Temporary Files and Race-Resistant Patterns",
  "graphicAlt": "Temporary-file lifecycle showing private directory selection, unpredictable exclusive creation, restrictive permissions, immediate unlinking or tracked cleanup, bounded use, and no reopening through attacker-controlled names.",
  "narration": "Temporary files are useful for staging output, processing intermediate data, or exchanging information with another component. They also create reliability and security risk when their lifecycle is vague. A temporary file can accumulate after failure, be readable by the wrong principal, collide with another process, or be mistaken for trusted output later.\n\nAvoid predictable names in shared writable locations unless a platform API provides safe creation semantics. The fragile pattern is to check whether a name exists and then open it later. Between those two operations, filesystem state can change. Another process may create, remove, replace, or link a target. This time-of-check/time-of-use issue is best handled by using APIs or workflows that create a new file atomically rather than relying on a separate precheck.\n\nTemporary files should still live under policy. The application should know which directory is allowed, which permissions are appropriate, and whether the file may contain sensitive data. If temporary contents are sensitive, cleanup and access control matter from creation through deletion. If temporary contents are part of a replacement workflow, the relationship between the temporary file and final destination should be documented.\n\nRAII and scoped lifecycle management help keep cleanup reliable. A wrapper can close handles and remove temporary files on error paths, while still allowing intentional preservation for diagnostics when policy permits. The important review question is simple: what happens to the temporary file during success, failure, exception, cancellation, and shutdown?",
  "narrationPoints": [
    "A temporary file can accumulate after failure, be readable by the wrong principal, collide with another process, or be mistaken for trusted output later.",
    "Temporary files are useful for staging output, processing intermediate data, or exchanging information with another component.",
    "Avoid predictable names in shared writable locations unless a platform API provides safe creation semantics.",
    "If temporary contents are part of a replacement workflow, the relationship between the temporary file and final destination should be documented.",
    "The application should know which directory is allowed, which permissions are appropriate, and whether the file may contain sensitive data.",
    "The important review question is simple: what happens to the temporary file during success, failure, exception, cancellation, and shutdown."
  ]
};
