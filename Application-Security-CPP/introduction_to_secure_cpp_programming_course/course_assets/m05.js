window.COURSE_MODULE = {
  "title": "Input Validation and Parsing",
  "graphicAlt": "Message parser diagram showing kind and length fields, exact payload consumption, supported versions, and a rejection branch for every extra or missing byte.",
  "narration": "External input should be treated as untrusted until the program validates it. Input can arrive through files, sockets, command-line arguments, environment variables, IPC, configuration, serialized data, plugin interfaces, or user interfaces. Some of it may be malformed by accident. Some of it may reflect old versions, partial writes, or incompatible producers.\n\nGood parsers validate structure before relying on it. That includes lengths, counts, offsets, versions, flags, encodings, and state transitions. If a message says it contains ten records, the parser should confirm that ten records actually fit. If an offset points into a buffer, the parser should confirm that the pointed-to range exists before using it.\n\nNumeric conversion deserves its own care. Code should detect invalid input, range errors, overflow, underflow, and leftover characters. A partial parse can be dangerous when the program treats it as a clean value. Strong parsing returns both the converted value and a clear status about whether the entire input met expectations.\n\nValidation should occur before allocation, copying, indexing, dispatch, or state-changing operations. For example, a length from input should not be used to allocate a huge buffer before checking whether the length is within policy. A type field should not dispatch to a handler before the surrounding structure has been validated.\n\nFailure behavior should be explicit, safe, and easy to test. A parser can reject a record, return an error object, close a connection, skip an optional field, or enter a recovery path, but it should not continue with stale data or guessed defaults. Logs should explain what category of validation failed without exposing sensitive content.\n\nSecure parsing is repeatable engineering. Define the accepted format, validate before trust, represent failures clearly, and test boundary cases. Reviewers should ask which inputs are trusted, which are merely parsed, and which are allowed to change program state.",
  "narrationPoints": [
    "External input should be treated as untrusted until the program validates it.",
    "If an offset points into a buffer, the parser should confirm that the pointed-to range exists before using it.",
    "Code should detect invalid input, range errors, overflow, underflow, and leftover characters.",
    "Validation should occur before allocation, copying, indexing, dispatch, or state-changing operations.",
    "A parser can reject a record, return an error object, close a connection, skip an optional field, or enter a recovery path, but it should not continue with stale data or guessed defaults.",
    "Define the accepted format, validate before trust, represent failures clearly, and test boundary cases."
  ]
};
