window.COURSE_MODULE = {
  "title": "Modern C++ Safety Foundations",
  "graphicAlt": "Foundation diagram linking explicit ownership, least privilege, narrow interfaces, safe defaults, dependency governance, and observable failure behavior across the development lifecycle.",
  "narration": "Modern C++ gives developers safer building blocks than many older C-style patterns. Raw pointers, raw arrays, manual allocation, and manual cleanup still exist, and sometimes they are necessary at low-level boundaries. But most application logic can often use standard containers, RAII types, clear ownership, and typed interfaces instead of open-ended memory manipulation.\n\nRAII, or Resource Acquisition Is Initialization, ties resource management to object lifetime. When an object is constructed, it acquires the resource it owns. When the object leaves scope, its destructor releases that resource. This pattern reduces the chance that an early return, exception, or error path forgets cleanup.\n\nStandard containers such as vector, array, string, map, and unordered_map can manage allocation and track size more safely than handwritten buffer logic. They do not make every operation automatically safe, but they move many basic memory-management details into well-tested library code and make size information available to the program.\n\nLanguage features can also express intent. const tells readers and the compiler when a value should not be modified. References can express required object presence better than nullable raw pointers in many APIs. Strong types can prevent mixing unrelated identifiers or units. Constructors and destructors can enforce initialization and cleanup rules.\n\nAvoiding unnecessary raw memory handling reduces the number of places where pointer, size, and lifetime mistakes can occur. A secure codebase does not need to prove every developer can reason about every buffer by inspection. It should choose abstractions that make the safe path routine and the risky path visible.\n\nModern C++ features improve safety only when developers understand their contracts and use them consistently. A string_view is not an owning string. A moved-from object still has a valid but limited state. A container reference can become invalid after reallocation. Secure C++ depends on both using better tools and knowing where their boundaries are.",
  "narrationPoints": [
    "Raw pointers, raw arrays, manual allocation, and manual cleanup still exist, and sometimes they are necessary at low-level boundaries.",
    "RAII, or Resource Acquisition Is Initialization, ties resource management to object lifetime.",
    "Standard containers such as vector, array, string, map, and unordered_map can manage allocation and track size more safely than handwritten buffer logic.",
    "References can express required object presence better than nullable raw pointers in many APIs.",
    "Avoiding unnecessary raw memory handling reduces the number of places where pointer, size, and lifetime mistakes can occur.",
    "Modern C++ features improve safety only when developers understand their contracts and use them consistently."
  ]
};
