window.COURSE_MODULE = {
  "title": "Ownership, Lifetime, and RAII",
  "graphicAlt": "Ownership diagram showing size validation before allocation and an OwnedBytes value carrying storage through its lifetime without an escaped span or dangling observer.",
  "narration": "Ownership answers the question of who is responsible for a resource. That resource might be memory, a file handle, a socket, a lock, a cryptographic context, or a temporary object. Lifetime answers when that object or resource is valid to use. Many C++ security and reliability issues are really ownership and lifetime issues in disguise.\n\nRAII helps by tying cleanup to object lifetime. A file wrapper closes the file in its destructor. A lock guard releases the lock when the guard leaves scope. A container releases its allocation when the container is destroyed. This pattern makes cleanup happen during normal returns, early returns, and exceptions without duplicating cleanup code across every path.\n\nSmart pointers can express ownership more clearly than raw pointers when dynamic ownership is needed. A unique_ptr communicates single ownership. A shared_ptr communicates shared ownership, though it should not be used just to avoid thinking about design. A weak_ptr can break cycles or observe shared ownership without extending lifetime.\n\nMove semantics allow ownership to transfer from one object to another. That is powerful, but reviewers should know what state remains after a move. Moved-from objects are still valid for operations allowed by their type, but the program should not assume they retain their old value unless the type explicitly promises it.\n\nRaw pointers and references still have a place as non-owning views or interoperability boundaries. The important rule is that non-owning access must not outlive the owned object. Passing a reference into a callback, storing a pointer in a long-lived object, or launching asynchronous work with borrowed data all deserve careful lifetime review.\n\nRAII and smart pointers reduce risk, but they do not remove the need for design clarity. Secure C++ code should make ownership visible in types, keep borrowing short and obvious, and treat dangling references, stale pointers, unclear shared ownership, and cleanup during failure as review priorities.",
  "narrationPoints": [
    "Many C++ security and reliability issues are really ownership and lifetime issues in disguise.",
    "A container releases its allocation when the container is destroyed.",
    "A shared_ptr communicates shared ownership, though it should not be used just to avoid thinking about design.",
    "Moved-from objects are still valid for operations allowed by their type, but the program should not assume they retain their old value unless the type explicitly promises it.",
    "Passing a reference into a callback, storing a pointer in a long-lived object, or launching asynchronous work with borrowed data all deserve careful lifetime review.",
    "RAII and smart pointers reduce risk, but they do not remove the need for design clarity."
  ]
};
