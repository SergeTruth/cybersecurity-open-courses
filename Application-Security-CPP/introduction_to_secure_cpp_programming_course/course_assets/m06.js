window.COURSE_MODULE = {
  "title": "Error Handling and Safe Failure",
  "graphicAlt": "Typed-error diagram separating malformed quantity text from outside-policy values while accepted input produces a bounded domain object and no partial state.",
  "narration": "C++ programs use different error-handling styles. Some rely on exceptions. Some use return values, status objects, expected-like types, error codes, callbacks, or combinations of these patterns. The security question is not which style sounds best in isolation. The question is whether failure leaves the program in a predictable and valid state.\n\nRAII can simplify cleanup during exceptions and early returns. If a lock, file, temporary allocation, or handle is owned by an object with a destructor, cleanup does not depend on every error path remembering a manual release call. That makes secure cleanup easier to review and less vulnerable to future edits.\n\nPartial initialization should be visible. An object that failed to initialize should not pretend to be fully usable. Constructors, factory functions, status-returning initialization, and invariant checks should make it clear when an object is ready and when it is not. A half-ready object can cause later code to make decisions based on missing or stale state.\n\nError handling should avoid continuing with stale data, partial output, invalid pointers, or untrusted defaults. A failed parse should not leave old values active unless that behavior is deliberate and documented. A failed write should not be logged as success. A failed security check should not silently choose a permissive fallback.\n\nDiagnostics should provide useful context without exposing secrets or misleading operators. A good log can identify the operation, component, status category, and recovery path. It should not dump credentials, sensitive data, or internal state that does not belong in operational records.\n\nSafe failure is a design property. Decide what each component guarantees after failure, test those paths, and keep cleanup automatic where practical. Review should trace not only the successful path but also the path where input is bad, allocation fails, a dependency is unavailable, or concurrent shutdown begins.",
  "narrationPoints": [
    "The question is whether failure leaves the program in a predictable and valid state.",
    "If a lock, file, temporary allocation, or handle is owned by an object with a destructor, cleanup does not depend on every error path remembering a manual release call.",
    "Constructors, factory functions, status-returning initialization, and invariant checks should make it clear when an object is ready and when it is not.",
    "Error handling should avoid continuing with stale data, partial output, invalid pointers, or untrusted defaults.",
    "Diagnostics should provide useful context without exposing secrets or misleading operators.",
    "Review should trace not only the successful path but also the path where input is bad, allocation fails, a dependency is unavailable, or concurrent shutdown begins."
  ]
};
