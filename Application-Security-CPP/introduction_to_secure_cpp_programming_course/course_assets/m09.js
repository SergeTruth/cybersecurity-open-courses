window.COURSE_MODULE = {
  "title": "Tooling, Testing, and Code Review",
  "graphicAlt": "Assurance diagram layering compiler warnings, static analysis, sanitizers, explicit negative tests, fuzzing, dependency review, and deployment checks over the same security boundary.",
  "narration": "Tooling makes secure C++ more repeatable. Compiler warnings can identify suspicious conversions, deprecated APIs, unreachable logic, missing virtual destructors, risky control flow, and other patterns worth review. A warning is not the same as a defect, but ignored warnings become noise that hides real problems.\n\nSanitizers can expose memory errors, undefined behavior, thread races, and leaks during testing. They are especially useful for code that handles buffers, parsers, object lifetimes, and concurrency. Sanitizer findings should lead to understanding the underlying contract, not just patching the immediate stack trace.\n\nStatic analysis can help identify unchecked input, ownership confusion, unsafe APIs, inconsistent assumptions, and error paths that leave objects in questionable states. It does not replace code review, but it gives reviewers a map of areas that deserve attention.\n\nTests should include boundary inputs, malformed data, failure paths, large inputs, empty inputs, concurrency edge cases, and dependency error behavior. Secure tests are not only happy-path examples. They show that the program rejects invalid data, cleans up resources, and does not continue after an unsafe state.\n\nFuzzing can support robustness testing at a high level when parsers, protocols, file formats, or serialized data are present. The goal is to find unexpected crashes, hangs, assertion failures, and invalid states during controlled testing so teams can harden validation and error handling before release.\n\nSecure code review should trace trust boundaries, ownership, lifetime, validation, error handling, dependency use, concurrency, and logging behavior. Reviewers should ask where data comes from, who owns each object, what happens on failure, which assumptions are enforced, and whether diagnostics reveal too much.",
  "narrationPoints": [
    "Compiler warnings can identify suspicious conversions, deprecated APIs, unreachable logic, missing virtual destructors, risky control flow, and other patterns worth review.",
    "Sanitizer findings should lead to understanding the underlying contract, not just patching the immediate stack trace.",
    "Static analysis can help identify unchecked input, ownership confusion, unsafe APIs, inconsistent assumptions, and error paths that leave objects in questionable states.",
    "Tests should include boundary inputs, malformed data, failure paths, large inputs, empty inputs, concurrency edge cases, and dependency error behavior.",
    "Fuzzing can support robustness testing at a high level when parsers, protocols, file formats, or serialized data are present.",
    "Secure code review should trace trust boundaries, ownership, lifetime, validation, error handling, dependency use, concurrency, and logging behavior."
  ]
};
