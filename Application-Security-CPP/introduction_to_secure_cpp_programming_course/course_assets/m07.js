window.COURSE_MODULE = {
  "title": "Concurrency and Shared State",
  "graphicAlt": "Concurrency diagram showing debit and snapshot operations acquiring the same account mutex and returning values rather than references into unlocked shared state.",
  "narration": "Concurrency adds risk because multiple threads or asynchronous tasks can observe and modify state at the same time. A design that is clear in single-threaded code may become fragile when callbacks, futures, thread pools, signal paths, or background workers enter the picture.\n\nData races in C++ can create undefined behavior. That means the program result is not merely hard to predict; the language rules no longer provide the normal guarantees. For secure engineering, this matters because a data race near validation state, authorization state, object lifetime, or resource cleanup can undermine assumptions that other code depends on.\n\nShared ownership should be minimized and documented where it is unavoidable. Immutable data, message passing, clear task boundaries, and ownership transfer can reduce the amount of state that needs synchronization. When mutable state is shared, the code should define who can access it, under what lock or atomic rule, and for how long.\n\nSynchronization choices should match the data being protected. Locks can protect complex invariants but can also introduce deadlocks if ordering is unclear. Atomics can be appropriate for narrow state but are not a universal replacement for locks. Queues and task systems can isolate ownership but require clear shutdown and backpressure behavior.\n\nObject lifetime becomes especially important when work crosses threads, callbacks, futures, or asynchronous APIs. A callback that captures a reference may run after the referenced object is gone. A background task may outlive the object that launched it. Shutdown paths should define cancellation, joining, ownership release, and error reporting.\n\nReview should trace shared data, synchronization, ownership transfer, shutdown behavior, and error paths. The goal is not to make every component lock-heavy. The goal is to make shared state intentional, bounded, and testable.",
  "narrationPoints": [
    "Concurrency adds risk because multiple threads or asynchronous tasks can observe and modify state at the same time.",
    "For secure engineering, this matters because a data race near validation state, authorization state, object lifetime, or resource cleanup can undermine assumptions that other code depends on.",
    "When mutable state is shared, the code should define who can access it, under what lock or atomic rule, and for how long.",
    "Queues and task systems can isolate ownership but require clear shutdown and backpressure behavior.",
    "A callback that captures a reference may run after the referenced object is gone.",
    "Review should trace shared data, synchronization, ownership transfer, shutdown behavior, and error paths."
  ]
};
