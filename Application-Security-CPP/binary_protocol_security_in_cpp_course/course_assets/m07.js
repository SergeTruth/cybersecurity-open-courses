window.COURSE_MODULE = {
  "title": "Resource Limits and Malformed Input Handling",
  "graphicAlt": "Nested limits cap frame bytes, item count, recursion depth, and cumulative work before the parser allocates or loops over attacker-controlled values.",
  "narration": "Binary protocol code needs resource limits because malformed or unexpected input can otherwise consume memory, CPU, file handles, queue space, or session state. Limits should cover maximum message size, maximum field length, maximum element count, maximum nesting depth, decompression output where relevant, recursion, allocation budgets, per-peer rate, and total time spent on parsing.\n\nMalformed input should lead to predictable failure. The parser should distinguish incomplete messages, invalid messages, unsupported versions, unauthenticated messages, and resource-exceeded messages when that distinction helps operations or policy. It should not continue reading beyond the current frame, spin in a loop, grow buffers without a cap, or leave the session in a confusing partial state.\n\nPreserving invariants is the heart of safe failure. If a message is rejected, previously trusted state should remain valid, partially created objects should be released, and callbacks or downstream actions should not run as if parsing succeeded. For streaming protocols, the implementation should also define whether the connection can resynchronize or must be closed after specific error categories.\n\nLogging should help operations without storing sensitive raw data unnecessarily. Counters and categories such as invalid length, unsupported version, bad state, authentication failure, and resource limit reached are often more useful than dumping the original bytes. When samples are needed for controlled debugging, access, retention, and redaction should be deliberate.\n\nA parser that fails safely is easier to operate. It rejects bad input, releases resources, records safe diagnostics, and keeps the rest of the application in a known state. These behaviors should be verified under load as well as in unit tests, because resource pressure often reveals cleanup and timeout assumptions that ordinary valid-message tests miss in practice and staging.",
  "narrationPoints": [
    "Binary protocol code needs resource limits because malformed or unexpected input can otherwise consume memory, CPU, file handles, queue space, or session state.",
    "The parser should distinguish incomplete messages, invalid messages, unsupported versions, unauthenticated messages, and resource-exceeded messages when that distinction helps operations or policy.",
    "If a message is rejected, previously trusted state should remain valid, partially created objects should be released, and callbacks or downstream actions should not run as if parsing succeeded.",
    "For streaming protocols, the implementation should also define whether the connection can resynchronize or must be closed after specific error categories.",
    "Counters and categories such as invalid length, unsupported version, bad state, authentication failure, and resource limit reached are often more useful than dumping the original bytes.",
    "A parser that fails safely is easier to operate."
  ]
};
