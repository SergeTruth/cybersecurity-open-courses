window.COURSE_MODULE = {
  "title": "Authentication, Integrity, and Replay Controls",
  "graphicAlt": "An authenticated frame binds header and payload to a verification tag, then a replay window rejects duplicate or stale sequence numbers.",
  "narration": "Parsing and trusting are different decisions. A parser may successfully extract a header, version, length, and body, but that does not prove who sent the message, whether it was changed in transit, whether it belongs to the current session, or whether it is safe to process in the current state.\n\nWhen binary messages cross trust boundaries or affect sensitive state, the design may need transport security, message authentication, signatures, sequence numbers, timestamps, or freshness checks according to system requirements. The right control depends on the environment and threat model, but the implementation should use approved cryptographic libraries, TLS where appropriate, or platform mechanisms rather than custom cryptographic logic.\n\nIntegrity checks should happen before sensitive use when policy requires them. That does not always mean before every byte is parsed, because the parser may need a bounded envelope to locate the integrity data. It does mean that the application should not act on fields, allocate large resources based on trusted meaning, or change sensitive state until required authenticity and integrity conditions are satisfied.\n\nRepeated valid messages can still be unsafe if the protocol does not define freshness, idempotency, or sequence behavior. A payment instruction, state transition, or command may need a sequence number, nonce, timestamp window, or server-side record showing it has already been handled. Those rules should be part of the protocol contract, not a side note.\n\nA defensive design keeps authentication, integrity, and replay controls visible in review. The parser extracts structure, the validation layer checks policy, and the application acts only after the message is both well-formed and trusted for the current context.",
  "narrationPoints": [
    "A parser may successfully extract a header, version, length, and body, but that does not prove who sent the message, whether it was changed in transit, whether it belongs to the current session, or whether it is safe to process in the current state.",
    "When binary messages cross trust boundaries or affect sensitive state, the design may need transport security, message authentication, signatures, sequence numbers, timestamps, or freshness checks according to system requirements.",
    "Integrity checks should happen before sensitive use when policy requires them.",
    "Repeated valid messages can still be unsafe if the protocol does not define freshness, idempotency, or sequence behavior.",
    "A defensive design keeps authentication, integrity, and replay controls visible in review.",
    "The parser extracts structure, the validation layer checks policy, and the application acts only after the message is both well-formed and trusted for the current context."
  ]
};
