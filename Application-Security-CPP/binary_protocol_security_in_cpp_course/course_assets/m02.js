window.COURSE_MODULE = {
  "title": "Framing, Lengths, and Message Boundaries",
  "graphicAlt": "A length-prefixed frame shows a four-byte header, exact payload boundary, rejected truncation, and rejected trailing bytes for a one-frame API.",
  "narration": "Framing is the first parsing decision. Before a parser can interpret fields, it needs to know where a message starts, where it ends, and how much data is required to continue. Some protocols use fixed-size headers. Others use length-prefixed records, streaming records, nested sections, or a mixture of fixed and variable data.\n\nA safe parser does not assume that one file read, socket read, or device read contains exactly one complete message. Input may arrive in pieces, multiple records may be batched together, and a partial header may need to wait for more bytes. Framing logic should distinguish incomplete data from invalid data. Incomplete data means the parser needs more bytes within policy. Invalid data means the parser should reject the record and preserve a safe state.\n\nLength fields are untrusted until checked. A length value should be validated before allocation, buffering, copying, decompression, or nested parsing. The parser should compare the declared length against bytes actually available, maximum record size, maximum message size, and any state-specific policy. It should also check derived values, such as header length plus body length, before using them.\n\nGood framing code tracks three questions: how many bytes are available, how many bytes are required, and whether continuing would exceed a limit. That makes boundary handling boring in the best way. The parser either waits for more data, rejects the message, or proceeds with a clearly bounded frame. This also improves observability because failures can be classified as truncated input, invalid length, unsupported record type, or resource limit reached instead of one vague parse error during troubleshooting and support escalation. That precision helps teams fix the right layer quickly and safely now.",
  "narrationPoints": [
    "Before a parser can interpret fields, it needs to know where a message starts, where it ends, and how much data is required to continue.",
    "A safe parser does not assume that one file read, socket read, or device read contains exactly one complete message.",
    "Framing logic should distinguish incomplete data from invalid data.",
    "Invalid data means the parser should reject the record and preserve a safe state.",
    "The parser should compare the declared length against bytes actually available, maximum record size, maximum message size, and any state-specific policy.",
    "The parser either waits for more data, rejects the message, or proceeds with a clearly bounded frame."
  ]
};
