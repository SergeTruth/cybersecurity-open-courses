window.COURSE_MODULE = {
  "title": "Fuzzing, Testing, Observability, and Review",
  "graphicAlt": "A fuzzing harness feeds arbitrary bytes to the parser while invariant tests, corpus cases, structured logging, and rate-limited rejection metrics collect evidence.",
  "narration": "Binary protocol safety depends on continuous validation work. Unit tests should cover valid messages, minimum and maximum sizes, boundary values, optional fields, missing fields, truncated messages, oversized fields, invalid versions, invalid state transitions, authentication failures, and resource-limit decisions. These tests make the protocol contract executable.\n\nRegression tests are especially valuable for parser fixes. When a bug is found, the team should add a test that captures the expected safe behavior so the same assumption does not return later. Boundary cases should include lengths around limits, offsets near frame edges, counts of zero and one, maximum supported counts, and nested data at allowed depth.\n\nFuzzing has a defensive role: it exercises parser robustness and memory safety under varied inputs. It should be integrated as a way to find crashes, sanitizer findings, hangs, unexpected allocations, and inconsistent error handling. AddressSanitizer, UndefinedBehaviorSanitizer, compiler warnings, static analysis, and coverage-guided testing can all strengthen confidence when they fit the build system.\n\nObservability completes the loop. Production systems should expose safe counters for rejected messages, parse error categories, version mismatches, resource-limit rejections, failed authentication or integrity checks, and unusual rates. These signals help teams spot compatibility problems, client rollouts, corrupted data sources, or operational pressure.\n\nReview questions should be concrete. Where are lengths checked? Are offsets bounded? What state is required? Which versions are accepted? When is a message trusted? What resources are capped? The best reviews connect code structure, tests, and operational signals. When those three sources agree, teams can change protocol code with more confidence and detect regressions earlier, before unexpected input reaches production environments. Review should include parser code, caller expectations, parsed objects, error handling, and operational behavior during maintenance.",
  "narrationPoints": [
    "Binary protocol safety depends on continuous validation work.",
    "Regression tests are especially valuable for parser fixes.",
    "Boundary cases should include lengths around limits, offsets near frame edges, counts of zero and one, maximum supported counts, and nested data at allowed depth.",
    "AddressSanitizer, UndefinedBehaviorSanitizer, compiler warnings, static analysis, and coverage-guided testing can all strengthen confidence when they fit the build system.",
    "The best reviews connect code structure, tests, and operational signals.",
    "Review should include parser code, caller expectations, parsed objects, error handling, and operational behavior during maintenance."
  ]
};
