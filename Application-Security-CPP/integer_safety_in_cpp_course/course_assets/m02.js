window.COURSE_MODULE = {
  "title": "Integer Types, Ranges, and Representation",
  "graphicAlt": "Number-line comparison showing signed and unsigned ranges, fixed-width protocol fields, size_t extents, and the application domain range that must fit each chosen type.",
  "narration": "Integer safety starts with understanding what a type can represent. An integer type is not just a small box for a number. It carries a range, a signedness rule, conversion behavior, and sometimes portability assumptions. When a value crosses an API boundary, those details can determine whether the result is meaningful.\n\nSigned and unsigned types serve different purposes. Signed types can represent values below zero, which may be appropriate for deltas, offsets, or states where negative values have meaning. Unsigned types represent nonnegative ranges, which can be useful for sizes and bit-oriented values. Problems appear when the code treats signedness as cosmetic.\n\nFixed-width types such as std::uint32_t or std::int64_t are helpful when a format, protocol, file, or cross-platform boundary requires a specific representation. They make storage expectations clearer. They should still be paired with domain rules because the largest representable value is often much larger than the largest acceptable value.\n\nOther integer types have platform-dependent widths. Plain int, long, and related aliases can vary across compilers, operating systems, and build targets. Code that assumes one width may behave differently when moved to another environment. Portability-sensitive code should name representation and range expectations explicitly.\n\nsize_t is the standard type for object sizes and many container sizes. It is useful and necessary, but it can create review challenges when mixed casually with signed values. A negative signed value converted to size_t no longer looks negative. Code should avoid casual mixing and should check range before crossing that boundary.\n\nThe main design habit is to treat range limits as requirements. Ask what the value means, what unit it uses, what minimum and maximum are valid for the domain, and what should happen when the value is outside that range. The chosen type should support that contract instead of hiding it.",
  "narrationPoints": [
    "When a value crosses an API boundary, those details can determine whether the result is meaningful.",
    "Unsigned types represent nonnegative ranges, which can be useful for sizes and bit-oriented values.",
    "Fixed-width types such as std::uint32_t or std::int64_t are helpful when a format, protocol, file, or cross-platform boundary requires a specific representation.",
    "Portability-sensitive code should name representation and range expectations explicitly.",
    "Code should avoid casual mixing and should check range before crossing that boundary.",
    "Ask what the value means, what unit it uses, what minimum and maximum are valid for the domain, and what should happen when the value is outside that range."
  ]
};
