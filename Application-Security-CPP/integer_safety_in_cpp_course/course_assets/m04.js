window.COURSE_MODULE = {
  "title": "Overflow, Underflow, and Arithmetic Preconditions",
  "graphicAlt": "Arithmetic guard diagram placing maximum-minus-left and maximum-divided-by-left checks before addition and multiplication, with overflow returning an explicit empty result.",
  "narration": "Safe arithmetic often means checking whether an operation is valid before performing it. Addition, subtraction, multiplication, division, shifts, and unit conversions can all have preconditions. If the result cannot be represented or does not make sense for the domain, the operation should not quietly continue.\n\nSigned overflow must not be treated as acceptable program behavior. It can invalidate assumptions that the optimizer, reviewer, and maintainer rely on. A defensive design checks maximum and minimum boundaries before performing an operation that could exceed them, especially when the result controls allocation, indexing, or limits.\n\nUnsigned arithmetic has defined wraparound behavior, but that does not automatically make it safe for application logic. A counter that wraps, a subtraction that moves from zero to a large value, or a size calculation that rolls over can still produce a wrong decision. Defined arithmetic is not the same as domain-valid arithmetic.\n\nSubtraction and multiplication deserve special care. Subtraction can underflow when a value is already at or below a boundary. Multiplication is common in size calculations, such as element count times element size, and it can exceed the type range before any allocation or copy happens. Division also has edge cases, including zero divisors and minimum signed values.\n\nFunctions should define what happens when a result cannot be represented safely. The policy might be an error return, an optional result, an exception, or rejecting the input earlier. What matters is consistency. Silent truncation, wraparound, or partial progress should not be the accidental behavior.\n\nTesting should include near-limit arithmetic. Values around zero, one, maximums, minimums, expected domain caps, and unit conversion boundaries are more revealing than ordinary middle-of-range values. Good tests verify both successful calculations and safe failure paths.",
  "narrationPoints": [
    "Safe arithmetic often means checking whether an operation is valid before performing it.",
    "A defensive design checks maximum and minimum boundaries before performing an operation that could exceed them, especially when the result controls allocation, indexing, or limits.",
    "Unsigned arithmetic has defined wraparound behavior, but that does not automatically make it safe for application logic.",
    "Subtraction can underflow when a value is already at or below a boundary.",
    "The policy might be an error return, an optional result, an exception, or rejecting the input earlier.",
    "Good tests verify both successful calculations and safe failure paths."
  ]
};
