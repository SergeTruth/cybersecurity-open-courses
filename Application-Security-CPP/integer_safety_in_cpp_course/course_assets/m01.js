window.COURSE_MODULE = {
  "title": "Why Integer Safety Matters",
  "graphicAlt": "Security-impact diagram showing an integer conversion affecting indexes, allocation sizes, quotas, protocol lengths, and authorization state rather than remaining an isolated arithmetic error.",
  "narration": "Integer safety matters because numeric values sit inside nearly every important C++ decision. They describe buffer sizes, container indexes, loop limits, offsets, counts, identifiers, permissions, protocol fields, timestamps, retry limits, and resource caps. Because integers look ordinary, teams can underestimate how much control flow and memory behavior depends on them.\n\nA small numeric mistake can change the meaning of a program. A conversion can turn a negative value into a very large count. A multiplication can produce a result that does not represent the intended size. A subtraction can move past zero. A comparison between signed and unsigned values can make a guard look correct while it checks the wrong condition.\n\nThese defects are rarely about one isolated operator. They usually come from unclear assumptions about domain, range, unit, and failure behavior. One function treats a value as a byte count, another as an element count, and a third as a signed offset. The code still compiles, but the contract has become hard to reason about.\n\nPrevention starts by making numeric intent visible. Choose types deliberately. Validate external values before relying on them. Check arithmetic preconditions before calculating sizes or indexes. Convert only after confirming that the destination type can represent the value. Make failure behavior explicit instead of letting invalid values drift through the system.\n\nTools help, but tools work best inside a disciplined engineering process. Compiler diagnostics, static analysis, sanitizers, boundary tests, and code review can all reveal integer defects earlier. They are strongest when the team already treats numeric values as part of secure design, not as incidental implementation detail.",
  "narrationPoints": [
    "Integer safety matters because numeric values sit inside nearly every important C++ decision.",
    "Because integers look ordinary, teams can underestimate how much control flow and memory behavior depends on them.",
    "A comparison between signed and unsigned values can make a guard look correct while it checks the wrong condition.",
    "One function treats a value as a byte count, another as an element count, and a third as a signed offset.",
    "Make failure behavior explicit instead of letting invalid values drift through the system.",
    "Compiler diagnostics, static analysis, sanitizers, boundary tests, and code review can all reveal integer defects earlier."
  ]
};
