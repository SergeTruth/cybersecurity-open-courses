window.COURSE_MODULE = {
  "title": "RAII and Smart Pointer Patterns",
  "graphicAlt": "RAII wrappers pair acquisition with deterministic destruction, and smart-pointer choices are matched to unique, shared, or observed lifetime contracts.",
  "narration": "RAII, Resource Acquisition Is Initialization, is the preferred C++ model for reliable cleanup. The resource is tied to an object's lifetime, and the destructor releases it. That means cleanup happens during normal control flow, early returns, exceptions, and partial failure paths without requiring every branch to remember a manual release step.\n\nSmart pointers are RAII tools when they express real ownership. Use std::unique_ptr when one owner controls a dynamically allocated object or resource. It makes ownership transfer explicit through moves and avoids raw owning pointers. Use std::shared_ptr only when shared lifetime is intentional. Shared ownership can be appropriate, but it also makes destruction timing less obvious.\n\nstd::weak_ptr is useful when code needs to observe an object managed by shared ownership without keeping it alive. It helps avoid reference cycles and supports callback or cache designs where the observer should handle the target disappearing. It does not replace ownership design; it depends on a clear shared ownership model around it.\n\nRAII is broader than memory. File descriptors, sockets, locks, handles, mapped regions, and custom library resources can all be wrapped so cleanup happens in a destructor. That consistency matters because lifetime bugs often appear on error paths. When cleanup is automatic, reviewers can focus on ownership relationships instead of chasing every exit branch for a missing release or double release.\n\nSmart pointers help prevent use-after-free only when used honestly. Passing raw pointers from get() to long-lived storage, overusing shared ownership, hiding release operations, or creating cycles can reintroduce lifetime risk. The review habit is to ask whether each pointer type matches the real relationship: owns, shares, observes, or borrows. When the type tells the truth, stale access is easier to find and prevent.",
  "narrationPoints": [
    "RAII, Resource Acquisition Is Initialization, is the preferred C++ model for reliable cleanup.",
    "Smart pointers are RAII tools when they express real ownership.",
    "Shared ownership can be appropriate, but it also makes destruction timing less obvious.",
    "File descriptors, sockets, locks, handles, mapped regions, and custom library resources can all be wrapped so cleanup happens in a destructor.",
    "Passing raw pointers from get() to long-lived storage, overusing shared ownership, hiding release operations, or creating cycles can reintroduce lifetime risk.",
    "When the type tells the truth, stale access is easier to find and prevent."
  ]
};
