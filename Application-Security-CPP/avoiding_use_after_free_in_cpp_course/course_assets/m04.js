window.COURSE_MODULE = {
  "title": "Containers, Iterators, and Invalidation",
  "graphicAlt": "A vector reallocation moves elements to new storage and leaves previously returned iterators, pointers, and references targeting the old allocation.",
  "narration": "Use-after-free-like defects are not limited to heap deletion. Containers can invalidate iterators, references, pointers, and views into their storage. After invalidation, the old access may still look like a normal value, but it no longer refers to a valid element or buffer. That makes container mutation a common lifetime review point.\n\nReallocation is a familiar example. Growing a std::vector or std::string may move elements to new storage, invalidating pointers and references into the old buffer. Erase operations can invalidate iterators and references to erased elements, and sometimes to elements after the erased position depending on the container. Resizing can destroy elements, change buffers, or alter assumptions about valid ranges.\n\nViews require the same care. A std::span or std::string_view does not own the data it describes. If the source container, string, or array is destroyed or modified in a way that invalidates storage, the view can become stale. A view that is stored in an object or returned from a function should trigger a lifetime review.\n\nDifferent containers provide different stability guarantees, so avoid carrying assumptions from one type to another. A node-based container may keep some references stable across operations that would invalidate a vector reference, but erased elements are still gone. A deque, map, vector, string, and unordered container each have their own invalidation rules. The secure habit is to check the rule at the mutation point.\n\nPrevention starts by keeping access and mutation close together. Avoid long-lived references into mutable containers. Prefer indexes or stable identifiers when appropriate, and revalidate after operations that may invalidate access. Document invalidation assumptions when an API returns views or iterators. In code review, ask what happens to every outstanding pointer, reference, iterator, or view after each container mutation.",
  "narrationPoints": [
    "Use-after-free-like defects are not limited to heap deletion.",
    "Growing a std::vector or std::string may move elements to new storage, invalidating pointers and references into the old buffer.",
    "A std::span or std::string_view does not own the data it describes.",
    "A node-based container may keep some references stable across operations that would invalidate a vector reference, but erased elements are still gone.",
    "Prevention starts by keeping access and mutation close together.",
    "In code review, ask what happens to every outstanding pointer, reference, iterator, or view after each container mutation."
  ]
};
