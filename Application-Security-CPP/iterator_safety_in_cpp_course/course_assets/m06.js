window.COURSE_MODULE = {
  "title": "Lifetimes, Views, and Borrowed Ranges",
  "graphicAlt": "Lifetime diagram contrasting a dangling string_view into a temporary with an owned token prefix and a borrowed-range constraint applied only to stable sources.",
  "narration": "An iterator is only meaningful while the range it refers to remains valid. If the container is destroyed, the string is temporary, the vector is moved from, or the view refers to data that has gone out of scope, the iterator no longer has a safe object behind it. Lifetime is therefore part of iterator safety, not a separate topic.\n\nModern C++ makes non-owning access common. std::span , std::string_view , filtered views, transformed views, and many lazy ranges do not necessarily own the data they expose. They are useful because they avoid unnecessary copying and make range pipelines expressive. They are also lifetime-sensitive because the referenced data must outlive the view and any iterator derived from it.\n\nReturning iterators or views from functions requires careful design. If the function returns a view into a local object, the caller receives a handle to something that no longer exists. If an object stores an iterator as a member, every mutation or replacement of the underlying container may affect that stored state. These patterns deserve explicit review.\n\nBorrowed ranges help express when iterators can safely outlive a range object, but they are not a reason to stop reasoning about ownership. The code still needs to communicate which object owns the data, which object observes it, and which operations can change validity.\n\nWhen lifetime is uncertain, choose a clearer design. Owned data, stable identifiers, indexes checked against the current container, or a different ownership boundary may be safer than carrying an iterator across time and hoping the range still exists.",
  "narrationPoints": [
    "If the container is destroyed, the string is temporary, the vector is moved from, or the view refers to data that has gone out of scope, the iterator no longer has a safe object behind it.",
    "Modern C++ makes non-owning access common. std::span , std::string_view , filtered views, transformed views.",
    "If an object stores an iterator as a member, every mutation or replacement of the underlying container may affect that stored state.",
    "Borrowed ranges help express when iterators can safely outlive a range object, but they are not a reason to stop reasoning about ownership.",
    "The code still needs to communicate which object owns the data, which object observes it, and which operations can change validity.",
    "Owned data, stable identifiers, indexes checked against the current container, or a different ownership boundary may be safer than carrying an iterator across time and hoping the range still exists."
  ]
};
