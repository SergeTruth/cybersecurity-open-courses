window.COURSE_MODULE = {
  "title": "Bounds-Aware Access and Size Discipline",
  "graphicAlt": "C++ container safety diagram for Bounds-Aware Access and Size Discipline, linking ownership and lifetime to bounds, invalidation, algorithm contracts, bounded growth, synchronization, and exception-safe commit behavior.",
  "narration": "Standard containers reduce many manual memory mistakes, but they do not make every access bounds-checked. A container-backed design still needs clear size discipline, especially when indexes, counts, offsets, or lengths come from outside the trusted part of the program.\n\nsize() tells you how many elements are present. capacity() tells you how much storage may be available without reallocating. Capacity is not initialized data. Code that treats capacity as available elements is making an unsafe assumption.\n\noperator[] can be appropriate when the surrounding logic has already proven bounds. The problem is not the operator itself; the problem is hiding an assumption that no reviewer can verify. When uncertainty remains, checked access such as at() or an explicit guard can make the contract clearer.\n\nExternally influenced sizes should be validated before allocation, indexing, copying, slicing, or transformation. A parsed number may fit in a type but still be too large for a container, too expensive to process, or inconsistent with another field.\n\nSigned and unsigned comparisons deserve review. Container sizes are commonly represented with unsigned types, while parsed inputs and loop indexes may be signed. Conversions should happen only after checking that values are nonnegative and within the intended range.\n\nIterator ranges are another form of bounds. A half-open range should come from the same container or a clearly related view, and the end iterator should really be reachable from the begin iterator. Mismatched ranges can make an algorithm's contract impossible to satisfy.\n\nThe defensive habit is to state the precondition before access. If code indexes, copies, reserves, or loops based on a value, reviewers should see where that value was checked and what limit the program is willing to support.\n\nSize discipline is also a resource-control practice. Reserving or resizing a container based on input can allocate large amounts of memory before any element is processed. Validate both the raw size and any derived size calculation before asking a container to grow.",
  "narrationPoints": [
    "A container-backed design still needs clear size discipline, especially when indexes, counts, offsets, or lengths come from outside the trusted part of the program.",
    "Code that treats capacity as available elements is making an unsafe assumption.",
    "When uncertainty remains, checked access such as at() or an explicit guard can make the contract clearer.",
    "Externally influenced sizes should be validated before allocation, indexing, copying, slicing, or transformation.",
    "A half-open range should come from the same container or a clearly related view, and the end iterator should really be reachable from the begin iterator.",
    "Validate both the raw size and any derived size calculation before asking a container to grow."
  ]
};
