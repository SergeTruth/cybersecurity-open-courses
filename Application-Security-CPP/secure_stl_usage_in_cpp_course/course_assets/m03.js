window.COURSE_MODULE = {
  "title": "Iterators, References, and Invalidation",
  "graphicAlt": "C++ container safety diagram for Iterators, References, and Invalidation, linking ownership and lifetime to bounds, invalidation, algorithm contracts, bounded growth, synchronization, and exception-safe commit behavior.",
  "narration": "Iterator, reference, and pointer invalidation is one of the most important standard-library safety topics. Containers make ownership easier, but access into a container is still governed by each container's mutation rules.\n\nOperations such as insert, erase, resize, reserve, clear, and reallocation can invalidate iterators, references, pointers, and non-owning views. The exact rules vary by container and operation, so reviewers should check the specific contract rather than relying on memory.\n\nA vector reallocation can make existing pointers and references stale. Erasing from a sequence can invalidate the erased element and sometimes elements after it. Clearing a container invalidates access to its elements. Strings can have similar issues when their internal buffer changes.\n\nLong-lived access is review-sensitive. Cached iterators, references stored in member variables, pointers into vectors or strings, and views returned to callers all require a clear lifetime story. If the source container can mutate, the cached access may stop being safe.\n\nErase loops deserve care. Code should use the iterator returned by erase when that is the appropriate pattern, or rebuild the range in a way that makes invalidation obvious. Hidden assumptions in loop updates are a common source of maintenance defects.\n\nThe safe habit is to discard or refresh access after mutation. When code mutates a container, ask which existing iterators, references, pointers, spans, or string_views are still valid. If the answer is uncertain, shorten the lifetime of that access.\n\nTests can help catch invalidation mistakes, especially with debug iterators or sanitizers where available. But the design should make invalidation assumptions visible enough that reviewers can reason about them before runtime.\n\nAPIs should avoid returning long-lived access into containers unless the lifetime contract is simple. Returning an index, key, copied value, or short-lived view may be safer than returning a reference that callers might store across later mutations.",
  "narrationPoints": [
    "Iterator, reference, and pointer invalidation is one of the most important standard-library safety topics.",
    "The exact rules vary by container and operation, so reviewers should check the specific contract rather than relying on memory.",
    "Erasing from a sequence can invalidate the erased element and sometimes elements after it.",
    "Code should use the iterator returned by erase when that is the appropriate pattern, or rebuild the range in a way that makes invalidation obvious.",
    "But the design should make invalidation assumptions visible enough that reviewers can reason about them before runtime.",
    "APIs should avoid returning long-lived access into containers unless the lifetime contract is simple."
  ]
};
