window.COURSE_MODULE = {
  "title": "Arrays, Pointers, and Lost Size Information",
  "graphicAlt": "An array decays into a pointer while its size, ownership, and lifetime labels fall away, illustrating information lost at a legacy function boundary.",
  "narration": "A pointer is not the same thing as a bounded array. A pointer may identify an element or the beginning of a range, but it does not inherently tell the callee how many elements are valid, where the range starts, where it ends, who owns the storage, or how long the object will remain alive.\n\nRaw arrays often lose useful information when they cross a function boundary. In many expressions, an array decays to a pointer, and the extent that was visible at the declaration site is no longer present in the parameter type. The receiving function may know an address, but not the valid range that address belongs to.\n\nPointer arithmetic can be legitimate in low-level C++ code, but it raises review difficulty. A calculation may be correct only if several earlier assumptions are true: the pointer belongs to the right object, the offset was checked, the resulting position is within the same range, and the lifetime has not ended. Those assumptions should not be invisible.\n\nBounds and lifetime must be reasoned about together. A range that was valid yesterday is not valid after the container reallocated, the owner was destroyed, or a view outlived the data it observes. A safe interface should make ownership and borrowing relationships clear enough that callers can preserve them.\n\nDefensive code keeps range information attached to the data for as long as practical. Containers, spans, iterators with clear begin and end rules, and small domain objects can all carry more meaning than a pointer alone. The goal is to reduce places where size context is discarded and later reconstructed by hand.\n\nDuring review, look for function signatures that accept pointer-only parameters, pointer plus loosely named integer parameters, or offsets that are not tied to a visible range. Those are signs that the code may be depending on an unwritten contract rather than a checked, testable one.",
  "narrationPoints": [
    "A pointer is not the same thing as a bounded array.",
    "In many expressions, an array decays to a pointer, and the extent that was visible at the declaration site is no longer present in the parameter type.",
    "Pointer arithmetic can be legitimate in low-level C++ code, but it raises review difficulty.",
    "A range that was valid yesterday is not valid after the container reallocated, the owner was destroyed, or a view outlived the data it observes.",
    "Defensive code keeps range information attached to the data for as long as practical.",
    "During review, look for function signatures that accept pointer-only parameters, pointer plus loosely named integer parameters, or offsets that are not tied to a visible range."
  ]
};
