window.COURSE_MODULE = {
  "title": "Prefer Containers and Bounded Views",
  "graphicAlt": "A comparison panel shows std::array carrying fixed extent, std::vector carrying dynamic size, and std::span carrying a non-owning bounded view.",
  "narration": "Modern C++ gives teams better choices than manual arrays for many designs. Containers and views do not make every mistake impossible, but they keep size, ownership, and valid range closer to the data. That makes ordinary code easier to write and review.\n\nUse std::array when fixed-size storage is part of the design. The number of elements is visible in the type, and the object owns its storage. This is useful for small fixed tables, embedded records, and compile-time-sized buffers where changing the extent should be a deliberate design change.\n\nUse std::vector for dynamic contiguous storage. It manages allocation and deallocation through RAII, carries size and capacity, and gives APIs a clear way to accept or return owned sequences. Code still needs range checks when indexing, but it no longer has to manually track the allocation lifetime.\n\nUse std::span for bounded non-owning views when an API needs to read or modify existing contiguous storage without taking ownership. A span keeps pointer and length together, which is usually more reviewable than separate parameters. The owner still has to outlive the view, so lifetime remains part of the contract.\n\nPrefer range-aware operations and standard algorithms when they express the intent. If the code can iterate over a range directly, transform a container, or use begin and end consistently, it often avoids unnecessary manual index calculations. The fewer manual bounds calculations a function contains, the fewer opportunities there are to get them wrong.\n\nThe larger design point is that the API shape should communicate ownership and size. A caller should be able to see whether a function owns data, borrows data, changes data, requires a specific extent, or operates on a flexible range. That visibility makes bounds safety part of the interface rather than an afterthought.",
  "narrationPoints": [
    "Modern C++ gives teams better choices than manual arrays for many designs.",
    "Use std::array when fixed-size storage is part of the design.",
    "Use std::span for bounded non-owning views when an API needs to read or modify existing contiguous storage without taking ownership.",
    "The owner still has to outlive the view, so lifetime remains part of the contract.",
    "The fewer manual bounds calculations a function contains, the fewer opportunities there are to get them wrong.",
    "A caller should be able to see whether a function owns data, borrows data, changes data, requires a specific extent, or operates on a flexible range."
  ]
};
