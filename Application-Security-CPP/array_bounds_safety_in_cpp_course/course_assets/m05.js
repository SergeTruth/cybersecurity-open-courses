window.COURSE_MODULE = {
  "title": "Safe Slicing, Subranges, and Offsets",
  "graphicAlt": "A subrange diagram validates offset against total size, then validates length against the remaining elements before constructing the view.",
  "narration": "Bounds safety becomes harder when code computes a start position and a length separately. A start index can be valid by itself while the requested range is not. For example, a position may point to an existing element, but the requested window may extend beyond the available data.\n\nValidate start and length as one range decision. The code should check the start, the length, the calculated end, and the remaining capacity or available input together. Validating only one part of the pair creates a false sense of safety and leaves the combined range unresolved.\n\nOffset plus length calculations need arithmetic care. The addition can exceed the representable range, or the result can be larger than the available data. Safer validation often checks remaining space first, such as whether the requested length is less than or equal to total size minus start, after confirming that start itself is in range.\n\nSubrange contracts should be explicit and testable. A function that accepts an offset and count should define the unit, valid bounds, behavior for empty ranges, behavior for end-of-range positions, and what happens when the requested range is invalid. That contract should be reflected in tests, not only in comments.\n\nPrefer bounded view abstractions when possible. A span, string view, iterator pair with clear ownership rules, or domain-specific range object can represent a slice more clearly than a raw pointer and count. The view should be created only after validating that the requested window is valid.\n\nBoundary tests are especially important for slicing code. Test empty ranges, start at zero, start at the final valid position, zero-length slices, full-length slices, near-end slices, and values just beyond each boundary. These cases reveal whether the validation is checking the whole range or only pieces of it.",
  "narrationPoints": [
    "Bounds safety becomes harder when code computes a start position and a length separately.",
    "For example, a position may point to an existing element, but the requested window may extend beyond the available data.",
    "Validating only one part of the pair creates a false sense of safety and leaves the combined range unresolved.",
    "Safer validation often checks remaining space first, such as whether the requested length is less than or equal to total size minus start, after confirming that start itself is in range.",
    "The view should be created only after validating that the requested window is valid.",
    "Test empty ranges, start at zero, start at the final valid position, zero-length slices, full-length slices, near-end slices, and values just beyond each boundary."
  ]
};
