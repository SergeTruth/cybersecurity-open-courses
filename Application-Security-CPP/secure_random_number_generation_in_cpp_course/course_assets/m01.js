window.COURSE_MODULE = {
  "title": "Why Random Number Security Matters",
  "graphicAlt": "Random-generation pipeline for Why Random Number Security Matters, leading from an approved operating-system entropy source through failure checks and unbiased mapping to a typed security artifact with documented size, purpose, and reuse rules.",
  "narration": "Random numbers are often quiet security boundaries. They may appear in session identifiers, password reset links, CSRF tokens, API tokens, cryptographic keys, nonces, initialization vectors, salts, challenges, correlation identifiers, and temporary names. In many systems, a random-looking value is what prevents one user from guessing another user's resource, reusing a sensitive workflow, or confusing one security event with another. When that value needs to be unpredictable, weak randomness can undercut otherwise careful design.\n\nNot every random value needs cryptographic strength. A randomized test input, game behavior, sampling decision, or load distribution choice may value repeatability, fairness, or statistical shape more than secrecy. Security-sensitive values have a different requirement: they must remain difficult to predict even when an observer understands the software design and may have seen previous outputs. That distinction should be made before choosing an API, not after code has spread through the project.\n\nDefensive C++ engineering treats random-number generation as part of design review. Teams should identify which values are security-sensitive, choose approved CSPRNG sources for those values, avoid predictable seeds, and make unsafe fallbacks unacceptable. The goal is not to turn every developer into a cryptographer. The goal is to create clear defaults, safe wrappers, reviewable call sites, and operational rules that keep security randomness from becoming a casual utility call with hidden consequences.",
  "narrationPoints": [
    "In many systems, a random-looking value is what prevents one user from guessing another user's resource, reusing a sensitive workflow, or confusing one security event with another.",
    "When that value needs to be unpredictable, weak randomness can undercut otherwise careful design.",
    "A randomized test input, game behavior, sampling decision, or load distribution choice may value repeatability, fairness, or statistical shape more than secrecy.",
    "Security-sensitive values have a different requirement: they must remain difficult to predict even when an observer understands the software design and may have seen previous outputs.",
    "Teams should identify which values are security-sensitive, choose approved CSPRNG sources for those values, avoid predictable seeds, and make unsafe fallbacks unacceptable.",
    "Defensive C++ engineering treats random-number generation as part of design review."
  ]
};
