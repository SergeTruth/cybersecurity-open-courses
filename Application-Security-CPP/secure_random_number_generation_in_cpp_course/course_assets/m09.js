window.COURSE_MODULE = {
  "title": "Review, Monitoring, and Operational Governance",
  "graphicAlt": "Random-generation pipeline for Review, Monitoring, and Operational Governance, leading from an approved operating-system entropy source through failure checks and unbiased mapping to a typed security artifact with documented size, purpose, and reuse rules.",
  "narration": "Weak RNG problems often enter through convenience. A developer adds a timestamp seed to make a warning disappear. A test hook remains enabled outside tests. A general-purpose PRNG is reused for a security token because it is already nearby. A generated secret is printed to logs during troubleshooting and the log path becomes permanent. These changes may look small in review, but they can change the security meaning of a value.\n\nCode review should flag custom RNGs, fixed seeds, time-based seeds, counters used as security randomness, modulo mapping for bounded values, unsafe fallback behavior, and random values printed to logs. Reviewers should also ask whether the call site has identified the value's purpose: token, key, nonce, IV, salt, challenge, or non-security random choice. Dependency review should confirm that cryptographic libraries and platform APIs are maintained, appropriate for the deployment environment, and configured according to organizational policy.\n\nOperational governance keeps these decisions from living only in individual memory. Document approved RNG sources, security-sensitive use cases, test-only sources, configuration rules, logging restrictions, and ownership for dependency updates. Monitoring should focus on safe signals, such as configuration drift, error rates from RNG wrappers, or unexpected use of disabled weak modes, without exposing generated secrets. When weak patterns are found, treat them as tracked engineering defects. Secure randomness is not a one-time utility selection; it is a lifecycle commitment.",
  "narrationPoints": [
    "A general-purpose PRNG is reused for a security token because it is already nearby.",
    "A generated secret is printed to logs during troubleshooting and the log path becomes permanent.",
    "Dependency review should confirm that cryptographic libraries and platform APIs are maintained, appropriate for the deployment environment, and configured according to organizational policy.",
    "Code review should flag custom RNGs, fixed seeds, time-based seeds, counters used as security randomness, modulo mapping for bounded values, unsafe fallback behavior, and random values printed to logs.",
    "Monitoring should focus on safe signals, such as configuration drift, error rates from RNG wrappers, or unexpected use of disabled weak modes, without exposing generated secrets.",
    "Document approved RNG sources, security-sensitive use cases, test-only sources, configuration rules, logging restrictions, and ownership for dependency updates."
  ]
};
