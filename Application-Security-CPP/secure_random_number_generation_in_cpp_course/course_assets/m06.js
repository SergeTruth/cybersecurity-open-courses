window.COURSE_MODULE = {
  "title": "Random Values for Tokens, Keys, Nonces, and Salts",
  "graphicAlt": "Random-generation pipeline for Random Values for Tokens, Keys, Nonces, and Salts, leading from an approved operating-system entropy source through failure checks and unbiased mapping to a typed security artifact with documented size, purpose, and reuse rules.",
  "narration": "Different security values have different requirements, so a secure RNG policy should not treat every random-looking value as the same thing. Tokens and session identifiers need enough unpredictability that they cannot be guessed in any practical way. They also need careful handling after generation: limited lifetime, appropriate storage, controlled logging, and clear invalidation behavior. A strong random token can still be mishandled if it is copied into logs or retained longer than the design expects.\n\nKeys require approved generation workflows, storage protections, access control, rotation plans, and destruction rules. Nonces and initialization vectors have requirements that depend on the cryptographic construction being used. Some require uniqueness, some require unpredictability, and some require both. Salts are usually not secrets, but they still need to be generated correctly for the scheme that uses them. Challenges, correlation identifiers, and temporary names may have still different requirements based on whether guessing them has security impact.\n\nThe safe lesson is to understand the use case before implementing the call site. Follow the requirements of the chosen cryptographic library, protocol, or organizational standard rather than inventing a local recipe. Document what the value is for, how much randomness is required, whether uniqueness matters, how it is encoded, how long it lives, and whether it can appear in telemetry. That documentation gives reviewers a concrete basis for judging whether the chosen random source and handling rules are appropriate.",
  "narrationPoints": [
    "Different security values have different requirements, so a secure RNG policy should not treat every random-looking value as the same thing.",
    "A strong random token can still be mishandled if it is copied into logs or retained longer than the design expects.",
    "Challenges, correlation identifiers, and temporary names may have still different requirements based on whether guessing them has security impact.",
    "Keys require approved generation workflows, storage protections, access control, rotation plans, and destruction rules.",
    "Salts are usually not secrets, but they still need to be generated correctly for the scheme that uses them.",
    "Follow the requirements of the chosen cryptographic library, protocol, or organizational standard rather than inventing a local recipe."
  ]
};
