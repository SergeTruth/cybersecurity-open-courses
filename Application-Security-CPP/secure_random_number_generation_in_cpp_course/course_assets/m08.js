window.COURSE_MODULE = {
  "title": "Testing Without Weakening Production Randomness",
  "graphicAlt": "Random-generation pipeline for Testing Without Weakening Production Randomness, leading from an approved operating-system entropy source through failure checks and unbiased mapping to a typed security artifact with documented size, purpose, and reuse rules.",
  "narration": "Secure randomness and repeatable tests pull in different directions. Tests often need deterministic behavior so failures can be reproduced. Production security values need unpredictability. A good design separates the random source interface from the code that consumes random values. The consuming code can be tested with a deterministic source in a controlled test context, while production wiring uses the approved CSPRNG source and rejects weak modes.\n\nThis separation should be intentional. Test hooks, fixed seeds, debug generators, and deterministic modes must not be reachable accidentally in production. Build configuration, dependency injection, feature flags, and environment-specific wiring should make the difference clear. If a test source is available, name it as test-only and restrict it to test binaries or explicitly controlled test configuration. Avoid designs where a missing production dependency silently causes a fallback to deterministic behavior.\n\nTests should verify both the consumer logic and the production boundary. Consumer tests can check formatting, length, rejection of invalid ranges, collision handling paths, and error propagation using deterministic inputs. Integration or configuration tests can verify that production code uses the approved source and that weak sources are not enabled in release settings. This gives the team reproducibility without weakening operational security. The design goal is not random behavior in every test; it is clear separation between deterministic test evidence and unpredictable production values.",
  "narrationPoints": [
    "A good design separates the random source interface from the code that consumes random values.",
    "Build configuration, dependency injection, feature flags, and environment-specific wiring should make the difference clear.",
    "Avoid designs where a missing production dependency silently causes a fallback to deterministic behavior.",
    "The design goal is not random behavior in every test; it is clear separation between deterministic test evidence and unpredictable production values.",
    "Consumer tests can check formatting, length, rejection of invalid ranges, collision handling paths, and error propagation using deterministic inputs.",
    "Integration or configuration tests can verify that production code uses the approved source and that weak sources are not enabled in release settings."
  ]
};
