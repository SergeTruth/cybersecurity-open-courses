window.COURSE_MODULE = {
  "title": "Course Summary: A Secure RNG Roadmap",
  "graphicAlt": "Random-generation pipeline for Course Summary: A Secure RNG Roadmap, leading from an approved operating-system entropy source through failure checks and unbiased mapping to a typed security artifact with documented size, purpose, and reuse rules.",
  "narration": "A secure RNG roadmap begins by identifying which values are security-sensitive. Tokens, keys, nonces, IVs, salts, challenges, and identifiers can have very different requirements, so name the use case before choosing the source. If the value must be unpredictable, use an operating-system CSPRNG or an approved cryptographic library. Do not substitute deterministic engines, fixed seeds, timestamps, counters, or hand-rolled entropy for security randomness.\n\nCentralize RNG access behind a small reviewed interface where practical. That interface should define the approved source, error handling, formatting helpers, bounded value behavior, and production restrictions. Keep deterministic sources available only for controlled tests, and design the consuming code so it can be tested without weakening production behavior. When output is mapped into a range or encoded as text, preserve the required entropy and avoid biased transformations.\n\nFinally, make secure randomness part of normal engineering governance. Avoid logging sensitive generated values. Review call sites, dependencies, platform behavior, configuration, and test hooks. Document what is approved and what is prohibited. Revisit the design when a product adds a new token, protocol, platform, dependency, or deployment mode. A useful team habit is to treat every new random value as a design decision until its purpose, source, format, lifetime, and review owner are clear. Secure random generation is not a last-minute utility call; it is a design responsibility that protects boundaries users rarely see but deeply depend on.",
  "narrationPoints": [
    "Tokens, keys, nonces, IVs, salts, challenges, and identifiers can have very different requirements, so name the use case before choosing the source.",
    "When output is mapped into a range or encoded as text, preserve the required entropy and avoid biased transformations.",
    "A useful team habit is to treat every new random value as a design decision until its purpose, source, format, lifetime, and review owner are clear.",
    "Secure random generation is not a last-minute utility call; it is a design responsibility that protects boundaries users rarely see but deeply depend on.",
    "Revisit the design when a product adds a new token, protocol, platform, dependency, or deployment mode.",
    "Review call sites, dependencies, platform behavior, configuration, and test hooks."
  ]
};
