window.COURSE_MODULE = {
  "title": "Use Libraries, Not Homemade Crypto",
  "graphicAlt": "Decision flow directing developers from a security requirement to an approved primitive, library API, parameter policy, and review.",
  "narration": "C++ teams should use well-reviewed cryptographic libraries, approved platform APIs, or organization-approved modules rather than implementing cryptographic primitives themselves.\n\nHomemade cryptography tends to fail in subtle places. Correct padding, randomness, authentication, key handling, compatibility, side-effect behavior, error reporting, and long-term maintenance are all difficult. A small-looking implementation can carry years of hidden assumptions.\n\nLibrary selection is an engineering decision. Review whether the library is maintained, documented, supported on your target platforms, and aligned with organizational policy. Look for safe defaults, clear APIs, supported algorithms, versioning guidance, and a realistic upgrade path.\n\nThe best crypto integration code is often small and boring. It calls approved APIs, checks return values, separates key management from business logic, rejects failed validation, and keeps configuration visible enough for review.\n\nError handling must be explicit. A failed encryption, failed verification, failed random generation, or failed certificate check should not be silently ignored or converted into a success path.\n\nWrapping a library can be useful when the wrapper narrows choices and enforces policy. It becomes risky when it hides errors, invents new protocols, or makes it hard to tell which primitive is being used.\n\nThe goal is not to make every developer a cryptographer. The goal is to make cryptographic use predictable, maintainable, policy-aligned, and easy to review.\n\nA practical wrapper might expose only approved operations, require callers to supply context, and centralize error mapping. It should not bury algorithm selection in configuration that reviewers never see.\n\nTeams should also plan for updates. Cryptographic guidance changes, libraries deprecate old modes, and platform requirements shift. Code that isolates approved use cleanly is easier to upgrade without rewriting business logic.\n\nKeep that path intentional.",
  "narrationPoints": [
    "C++ teams should use well-reviewed cryptographic libraries, approved platform APIs, or organization-approved modules rather than implementing cryptographic primitives themselves.",
    "A small-looking implementation can carry years of hidden assumptions.",
    "The best crypto integration code is often small and boring.",
    "Wrapping a library can be useful when the wrapper narrows choices and enforces policy.",
    "A practical wrapper might expose only approved operations, require callers to supply context, and centralize error mapping.",
    "Code that isolates approved use cleanly is easier to upgrade without rewriting business logic."
  ]
};
