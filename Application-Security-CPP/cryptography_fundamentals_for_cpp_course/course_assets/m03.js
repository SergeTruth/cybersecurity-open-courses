window.COURSE_MODULE = {
  "title": "Keys, Randomness, and Nonces",
  "graphicAlt": "Entropy flow showing an operating-system random source filling a key or nonce exactly, with failure routed to a closed error path.",
  "narration": "Cryptographic strength depends heavily on the values around the algorithm. Secret keys, public keys, salts, initialization values, IVs, nonces, and random values all have different purposes, and confusing them can weaken a design.\n\nKeys are sensitive assets. They need clear ownership, approved generation, controlled storage, rotation policy, access limits, and deletion or retirement behavior. A strong algorithm cannot compensate for exposed or poorly managed key material.\n\nCryptographic randomness should come from approved library or operating-system facilities. General-purpose pseudo-random functions are not appropriate for key generation, nonces that require unpredictability, or other security-sensitive values unless an approved cryptographic API is providing them.\n\nSalts, IVs, and nonces are not interchangeable. A salt supports uniqueness in password hashing or related derivation contexts. An IV or nonce may support a specific encryption mode. Some values must be unique, some must be unpredictable, and some can be public but still must follow strict rules.\n\nNonce reuse can be security-critical depending on the algorithm and mode. The application should not rely on informal memory of what was used before. It should have a clear generation and persistence strategy suitable for the library API.\n\nKey lifecycle decisions belong in the design. Define where keys come from, how they rotate, which components can read them, how old data remains accessible, and how failures are handled during transition.\n\nThe safe habit is to treat randomness and key material as first-class design elements, not incidental parameters passed into a crypto call at the last moment.\n\nReviewers should also look for accidental reuse across environments. Development, test, staging, and production should not share sensitive key material just because it makes deployment easier.\n\nClear ownership makes recovery safer when a key must be rotated, retired, or replaced after an operational incident.",
  "narrationPoints": [
    "Cryptographic strength depends heavily on the values around the algorithm.",
    "Cryptographic randomness should come from approved library or operating-system facilities.",
    "A salt supports uniqueness in password hashing or related derivation contexts.",
    "The application should not rely on informal memory of what was used before.",
    "Define where keys come from, how they rotate, which components can read them, how old data remains accessible, and how failures are handled during transition.",
    "Clear ownership makes recovery safer when a key must be rotated, retired, or replaced after an operational incident."
  ]
};
