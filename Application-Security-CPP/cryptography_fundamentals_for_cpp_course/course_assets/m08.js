window.COURSE_MODULE = {
  "title": "Crypto Misuse, Testing, and Code Review",
  "graphicAlt": "Crypto review matrix checking algorithm, key size, nonce size, tag size, failure behavior, and negative tampering tests.",
  "narration": "Crypto misuse usually comes from using a reasonable-looking tool for the wrong goal, using an approved library incorrectly, or ignoring a failure result that should stop processing.\n\nCommon review findings include encryption without authentication, nonce reuse, weak randomness, accepting unauthenticated data, custom trust decisions, logging keys, hardcoded secrets, stale libraries, and inconsistent error handling.\n\nTesting should verify application behavior, not reprove the mathematics. Use known-good library behavior, then test that the application passes the right inputs, checks return values, rejects invalid or corrupted data, and does not continue after verification fails.\n\nFailure paths need deliberate coverage. Corrupted ciphertext, wrong associated data, invalid signatures, expired certificates, missing keys, unavailable random sources, and configuration mistakes should all lead to predictable, safe outcomes.\n\nCode review should identify key ownership, nonce policy, algorithm or mode selection, trust anchors, library return-value handling, and the boundary where data becomes trusted enough to process.\n\nDependency maintenance is part of crypto safety. Track library versions, supported algorithms, deprecation guidance, platform updates, and organizational policy changes. A once-acceptable configuration may need revision later.\n\nCrypto code should be small enough that these checks are practical. If the logic is spread across many layers or hidden behind vague wrappers, important trust decisions become harder to review.\n\nReview should also include configuration and deployment. The code may call the right API, but a weak key source, disabled validation mode, stale dependency, or permissive runtime setting can still weaken the boundary.\n\nGood tests assert that failures stay failures. They should verify that the application does not quietly fall back to unprotected behavior when cryptographic operations fail.\n\nThat makes misuse visible during ordinary engineering work instead of during production recovery.",
  "narrationPoints": [
    "Crypto misuse usually comes from using a reasonable-looking tool for the wrong goal, using an approved library incorrectly, or ignoring a failure result that should stop processing.",
    "Use known-good library behavior, then test that the application passes the right inputs, checks return values, rejects invalid or corrupted data, and does not continue after verification fails.",
    "Dependency maintenance is part of crypto safety.",
    "Crypto code should be small enough that these checks are practical.",
    "The code may call the right API, but a weak key source, disabled validation mode, stale dependency, or permissive runtime setting can still weaken the boundary.",
    "That makes misuse visible during ordinary engineering work instead of during production recovery."
  ]
};
