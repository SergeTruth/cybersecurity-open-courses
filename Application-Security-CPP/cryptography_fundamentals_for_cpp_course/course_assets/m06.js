window.COURSE_MODULE = {
  "title": "Public-Key Crypto, Signatures, and Certificates",
  "graphicAlt": "Signature flow showing a validated certificate public key authenticating a message while certificate-chain and hostname checks remain separate.",
  "narration": "Public-key cryptography uses different roles for public and private keys. A public key can be distributed broadly, while the private key must remain protected. The security value depends on keeping that role separation intact.\n\nDigital signatures provide authenticity and integrity for signed data when verification is performed correctly and the verifying code trusts the right public key or certificate chain. A signature does not by itself provide confidentiality.\n\nTrust is the central engineering question. Where does the public key come from? Is it pinned, configured, distributed by an update system, or validated through a certificate chain? How does the application respond when verification fails?\n\nCertificates add policy. Validation may include chain building, trust store selection, hostname checks, expiration handling, revocation or policy checks where applicable, and clear behavior for development versus production environments.\n\nTLS should normally be handled by mature libraries or platform stacks. Custom protocol code is rarely the right place to make low-level trust decisions. Application code should configure the library correctly and avoid weakening validation for convenience.\n\nTemporary validation shortcuts have a habit of becoming permanent. If a test environment needs special trust configuration, make it explicit, isolated, and difficult to ship accidentally.\n\nA reviewable public-key design names the trust source, verification operation, accepted identities, failure behavior, and maintenance path for key or certificate changes.\n\nPrivate keys also need lifecycle control. Limit access, rotate according to policy, and make signing or certificate operations auditable enough that unusual use can be investigated.\n\nWhen C++ code integrates with TLS, prefer configuring the established stack over inventing custom identity checks in application logic.\n\nThis keeps trust policy centralized, documented, and easier to maintain across platforms.\n\nReview it regularly.",
  "narrationPoints": [
    "Public-key cryptography uses different roles for public and private keys.",
    "Digital signatures provide authenticity and integrity for signed data when verification is performed correctly and the verifying code trusts the right public key or certificate chain.",
    "How does the application respond when verification fails?",
    "Application code should configure the library correctly and avoid weakening validation for convenience.",
    "A reviewable public-key design names the trust source, verification operation, accepted identities, failure behavior, and maintenance path for key or certificate changes.",
    "This keeps trust policy centralized, documented, and easier to maintain across platforms."
  ]
};
