window.COURSE_MODULE = {
  "title": "Symmetric Encryption and Authenticated Encryption",
  "graphicAlt": "AEAD diagram showing key, unique nonce, plaintext, and associated data producing ciphertext plus tag, with tampering rejected before release.",
  "narration": "Symmetric encryption uses the same secret key for protecting data and later recovering it. It is common for stored records, local files, service messages, and other cases where both sides are allowed to share a secret.\n\nConfidentiality alone is not always enough. If encrypted data can be modified without detection, the application may process data it should not trust. Modern designs usually prefer authenticated encryption so confidentiality and integrity are handled together by a library-supported mode.\n\nAuthenticated encryption can also use associated data. Associated data is not encrypted, but it is bound to the authentication result. This can be useful for context such as version, tenant, purpose, record identifier, or protocol metadata that must match the protected content.\n\nThe application should reject unauthenticated data before use. Do not parse, deserialize, route, or act on protected content until the library reports successful authentication. Failure should be treated as a normal security-relevant outcome, not as an exceptional path that is rarely tested.\n\nUnique nonces or IVs may be required by the chosen mode. The application should follow the library documentation exactly and make nonce policy visible in code review.\n\nAvoid assembling encryption and authentication manually unless an approved design explicitly requires it. Combining primitives safely is subtle, and mature libraries already provide modes that reduce misuse.\n\nA reviewable symmetric-encryption design identifies the key source, nonce strategy, associated data, failure behavior, and the exact point where data becomes trusted enough for application use.\n\nIt should also identify what is intentionally not encrypted. Associated data, headers, and routing fields may remain visible, so the design should confirm that visibility is acceptable.\n\nOperationally, encryption code should be tested with valid data, modified data, wrong context, missing keys, and version transitions so failure handling is not improvised later.",
  "narrationPoints": [
    "Symmetric encryption uses the same secret key for protecting data and later recovering it.",
    "Modern designs usually prefer authenticated encryption so confidentiality and integrity are handled together by a library-supported mode.",
    "The application should reject unauthenticated data before use.",
    "Unique nonces or IVs may be required by the chosen mode.",
    "A reviewable symmetric-encryption design identifies the key source, nonce strategy, associated data, failure behavior, and the exact point where data becomes trusted enough for application use.",
    "Operationally, encryption code should be tested with valid data, modified data, wrong context, missing keys, and version transitions so failure handling is not improvised later."
  ]
};
