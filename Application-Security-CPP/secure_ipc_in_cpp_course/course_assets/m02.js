window.COURSE_MODULE = {
  "title": "Choosing IPC Mechanisms Safely",
  "graphicAlt": "Interprocess communication diagram for Choosing IPC Mechanisms Safely, with separate caller and service trust zones connected by an authenticated, length-bounded message channel and explicit authorization, cleanup, and failure controls.",
  "narration": "Choosing an IPC mechanism is a security and maintainability decision, not just a performance choice. Named pipes, local sockets, message queues, platform messaging APIs, RPC-style frameworks, shared memory, and helper processes each make different tradeoffs. The right answer depends on trust boundaries, identity support, permission controls, message shape, reliability, observability, platform support, and failure behavior.\n\nStart with the trust model. If the receiver needs to know who connected, choose a mechanism that can expose reliable peer identity or can be combined with a strong authorization layer. If callers need different permissions, the endpoint should support clear access policy instead of relying on broad global state. If messages are complex, the mechanism should make framing and validation straightforward.\n\nPerformance features need extra discipline. Shared memory can reduce copies, but it requires explicit bounds, synchronization, mapping size validation, lifetime rules, and cleanup. Streaming mechanisms are flexible, but they need framing, maximum message sizes, partial-read handling, and backpressure. Helper processes can improve containment, but they need startup, shutdown, restart, and health-check behavior.\n\nReviewability matters. An IPC API that makes identity, permissions, framing, limits, and errors visible is usually safer than one that hides those decisions behind convenience. The team should be able to explain why the mechanism was chosen, what it protects, what it does not protect, and how it behaves when the peer is slow, unauthorized, incompatible, or gone. Mechanism choice should also consider deployment and operations. Can the endpoint be configured safely on every supported platform? Can administrators reason about permissions? Can support staff distinguish startup failure from permission denial, version mismatch, or resource pressure? Those questions often decide whether an IPC design remains maintainable after release and upgrades.",
  "narrationPoints": [
    "The right answer depends on trust boundaries, identity support, permission controls, message shape, reliability, observability, platform support, and failure behavior.",
    "Choosing an IPC mechanism is a security and maintainability decision, not just a performance choice.",
    "If messages are complex, the mechanism should make framing and validation straightforward.",
    "Shared memory can reduce copies, but it requires explicit bounds, synchronization, mapping size validation, lifetime rules, and cleanup.",
    "Can support staff distinguish startup failure from permission denial, version mismatch, or resource pressure.",
    "An IPC API that makes identity, permissions, framing, limits, and errors visible is usually safer than one that hides those decisions behind convenience."
  ]
};
