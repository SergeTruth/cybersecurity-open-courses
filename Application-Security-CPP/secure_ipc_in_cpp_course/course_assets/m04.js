window.COURSE_MODULE = {
  "title": "Message Contracts and Validation",
  "graphicAlt": "Interprocess communication diagram for Message Contracts and Validation, with separate caller and service trust zones connected by an authenticated, length-bounded message channel and explicit authorization, cleanup, and failure controls.",
  "narration": "IPC messages are local API calls with a serialization boundary. A secure contract should define message types, required fields, optional fields, version identifiers, allowed values, encoding assumptions, size limits, and unsupported behavior. Without that contract, receivers tend to grow ambiguous defaults and hidden assumptions that become difficult to review.\n\nEvery message should be treated as untrusted until validated, even when it comes from another component on the same machine. The receiver should check the message type, version, required fields, numeric ranges, string lengths, identifiers, and state requirements before using the data. Unknown commands and unsupported versions should fail predictably, with safe diagnostics and no partial action.\n\nValidation should happen before sensitive operations. Do not allocate large buffers, open files, use elevated operations, mutate state, or pass data to downstream components until the message has satisfied the boundary contract. If a message contains nested structures, validate the outer frame and limits before parsing deeper content.\n\nAccepted messages should become narrow internal types. That conversion separates raw IPC input from trusted application requests and helps reviewers see exactly what was accepted. It also reduces the chance that a partially validated field or ambiguous default slips into business logic. Good message contracts make local communication ordinary to maintain because the allowed behavior is visible in code, tests, and documentation. Compatibility should be deliberate as well. If a new client sends a newer message, the receiver should know whether to reject it, ignore an optional field, or require an upgrade. Silent interpretation of unknown behavior makes IPC review fragile. Contract tests should include old clients, new clients, missing fields, extra fields, and unsupported commands in the wrong state during upgrades and rollbacks too safely.",
  "narrationPoints": [
    "A secure contract should define message types, required fields, optional fields, version identifiers, allowed values, encoding assumptions, size limits, and unsupported behavior.",
    "Without that contract, receivers tend to grow ambiguous defaults and hidden assumptions that become difficult to review.",
    "The receiver should check the message type, version, required fields, numeric ranges, string lengths, identifiers, and state requirements before using the data.",
    "Unknown commands and unsupported versions should fail predictably, with safe diagnostics and no partial action.",
    "If a message contains nested structures, validate the outer frame and limits before parsing deeper content.",
    "Good message contracts make local communication ordinary to maintain because the allowed behavior is visible in code, tests, and documentation."
  ]
};
