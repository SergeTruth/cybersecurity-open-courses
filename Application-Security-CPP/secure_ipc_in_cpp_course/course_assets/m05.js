window.COURSE_MODULE = {
  "title": "Serialization, Binary Data, and Resource Limits",
  "graphicAlt": "Interprocess communication diagram for Serialization, Binary Data, and Resource Limits, with separate caller and service trust zones connected by an authenticated, length-bounded message channel and explicit authorization, cleanup, and failure controls.",
  "narration": "IPC message bodies may be text, binary records, structured objects, or streamed data. Whatever the format, parsing code should be designed as security-critical C++ code. A valid frame only proves that bytes were received in some shape. It does not prove that the message is semantically valid, authorized, compatible, or affordable to process.\n\nLength fields and boundaries need strict checks. Validate declared sizes before reading, buffering, copying, decompressing, or allocating. Track how many bytes are available, how many are required, and whether continuing would exceed message, record, nesting, queue, or per-connection limits. A parser should distinguish incomplete data, which may need more bytes within policy, from invalid data, which should be rejected.\n\nAvoid treating raw bytes as application objects. Direct casts into C++ structures can depend on layout, padding, alignment, lifetime, and endianness assumptions that are not necessarily part of the IPC contract. Convert bytes into typed values deliberately, check remaining bytes before every read, and validate ranges before using values as counts, offsets, indexes, or allocation sizes.\n\nResource limits protect both availability and correctness. Bound message size, nesting depth, expansion behavior, queue growth, active requests, and memory per client. When parsing fails, preserve invariants, release partial resources, and avoid leaving the connection in a confusing state. Safe serialization is not just about decoding successfully; it is about failing predictably. Tests should cover truncated messages, oversized records, invalid encodings, unsupported versions, slow streams, and repeated parse failures so the receiver’s resource policy is exercised under realistic pressure. Monitoring should confirm that rejected data does not grow memory or queues over time during long-running sessions, retries, load tests, repeated restarts, reconnect storms, and client churn too often.",
  "narrationPoints": [
    "Whatever the format, parsing code should be designed as security-critical C++ code.",
    "A parser should distinguish incomplete data, which may need more bytes within policy, from invalid data, which should be rejected.",
    "Convert bytes into typed values deliberately, check remaining bytes before every read, and validate ranges before using values as counts, offsets, indexes, or allocation sizes.",
    "Bound message size, nesting depth, expansion behavior, queue growth, active requests, and memory per client.",
    "Safe serialization is not just about decoding successfully; it is about failing predictably.",
    "Monitoring should confirm that rejected data does not grow memory or queues over time during long-running sessions, retries, load tests, repeated restarts, reconnect storms, and client churn too often."
  ]
};
