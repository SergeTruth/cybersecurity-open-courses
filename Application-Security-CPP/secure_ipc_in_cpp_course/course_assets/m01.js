window.COURSE_MODULE = {
  "title": "IPC as a Local Trust Boundary",
  "graphicAlt": "Interprocess communication diagram for IPC as a Local Trust Boundary, with separate caller and service trust zones connected by an authenticated, length-bounded message channel and explicit authorization, cleanup, and failure controls.",
  "narration": "Inter-process communication is more than a convenient way to move bytes between local processes. In C++ systems, IPC often connects a desktop user interface to a service, a host process to a helper, a broker to a contained worker, or two services owned by different teams. That communication can cross important trust, privilege, session, and lifecycle boundaries.\n\nLocal does not automatically mean trusted. A caller may run as a different user, in a different session, with different permissions, inside a container, or at a different integrity level. It may also be outdated, misconfigured, interrupted, or sending messages the receiver did not expect. Treating every local peer as friendly makes the IPC endpoint harder to review and harder to operate safely.\n\nSecure IPC design starts by defining who may connect, what each caller is allowed to request, what message shapes are accepted, and how authority is attached to each operation. The server should validate messages before trusted use, check permissions close to the boundary, and avoid assuming that connection success means operation success.\n\nFailure behavior is part of the boundary. Peers can disconnect, send partial messages, time out, restart, or fail during a sensitive operation. A mature IPC design preserves invariants, releases resources, records safe diagnostics, and leaves the application in a known state when the peer does not behave as expected. Review should cover both normal message flow and the strange local conditions that appear during upgrades, restarts, and crashes.",
  "narrationPoints": [
    "In C++ systems, IPC often connects a desktop user interface to a service, a host process to a helper, a broker to a contained worker, or two services owned by different teams.",
    "Treating every local peer as friendly makes the IPC endpoint harder to review and harder to operate safely.",
    "Secure IPC design starts by defining who may connect, what each caller is allowed to request, what message shapes are accepted, and how authority is attached to each operation.",
    "The server should validate messages before trusted use, check permissions close to the boundary, and avoid assuming that connection success means operation success.",
    "A mature IPC design preserves invariants, releases resources, records safe diagnostics, and leaves the application in a known state when the peer does not behave as expected.",
    "Review should cover both normal message flow and the strange local conditions that appear during upgrades, restarts, and crashes."
  ]
};
