window.COURSE_MODULE = {
  "title": "Data Races, Shared State, and Atomic Objects",
  "graphicAlt": "A timeline contrasts overlapping non-atomic reads and writes with atomic operations that each complete indivisibly on one shared object.",
  "narration": "A data race happens when multiple threads access the same mutable object without proper synchronization and at least one access modifies it. In C++, that is not merely a surprising result; it can make the program's behavior undefined.\n\nPlain variables are not safe thread coordination mechanisms. A worker reading a plain boolean while another thread writes it, or several threads incrementing a plain counter, may appear to work during one test run and fail under different scheduling, optimization, or hardware behavior.\n\nstd::atomic provides operations with defined concurrent behavior for the atomic object. A load, store, exchange, fetch operation, or compare-and-exchange can be performed by multiple threads without creating a data race on that object.\n\nThe design question comes first: does the state need to be shared at all? Immutable data, thread confinement, ownership transfer, and message passing often reduce the need for shared mutation and make review simpler.\n\nAn atomic variable should not hide a larger unsynchronized invariant. If an atomic flag says a buffer is ready, the buffer's lifetime and publication rules still matter. If an atomic counter relates to a container, the container may still need separate protection.\n\nEach atomic should have a clear purpose. Naming, comments, and nearby code should explain whether it is a stop signal, a readiness marker, a metrics value, a generation count, or part of a state machine.\n\nThe safe habit is to reduce shared mutable state first, then make the remaining shared values explicit, narrow, and reviewable.\n\nThat habit prevents atomics from becoming a cosmetic fix for a design that still allows unrelated threads to mutate the same logical state without a shared contract.\n\nMake sharing intentional.\n\nKeep it visible.",
  "narrationPoints": [
    "A data race happens when multiple threads access the same mutable object without proper synchronization and at least one access modifies it.",
    "Plain variables are not safe thread coordination mechanisms.",
    "A load, store, exchange, fetch operation, or compare-and-exchange can be performed by multiple threads without creating a data race on that object.",
    "Immutable data, thread confinement, ownership transfer, and message passing often reduce the need for shared mutation and make review simpler.",
    "If an atomic counter relates to a container, the container may still need separate protection.",
    "The safe habit is to reduce shared mutable state first, then make the remaining shared values explicit, narrow, and reviewable."
  ]
};
