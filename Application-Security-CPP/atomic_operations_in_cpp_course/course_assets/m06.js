window.COURSE_MODULE = {
  "title": "Flags, Counters, and State Transitions",
  "graphicAlt": "A state machine permits Created-to-Running-to-Stopping-to-Stopped transitions and rejects caller-requested jumps that violate lifecycle policy.",
  "narration": "Many practical atomic uses involve flags, counters, and small state machines. These patterns can be safe and readable when each value has a defined purpose and the surrounding lifecycle is clear.\n\nStop flags are common, but the flag is only one part of shutdown. A worker must check the flag at appropriate points, stop accepting or processing work according to policy, release resources safely, and be joined or otherwise managed before referenced state disappears.\n\nReadiness flags imply publication rules. If one thread initializes data and then marks it ready, reviewers need to know how the data is published, which ordering is used, and how long the data remains valid for readers.\n\nMetrics counters may have different requirements. A counter used only for approximate telemetry may not need the same ordering as a flag used to coordinate object visibility. That distinction should be intentional rather than accidental.\n\nAtomic state machines should define valid states and allowed transitions. Prefer named states over magic integers, and keep the transition logic close to the atomic value. If many unrelated states accumulate, a dedicated synchronized object may be clearer.\n\nCompare-and-exchange can enforce state transitions, but retry behavior must be understandable. Reviewers should know what happens when another thread changes the value first and whether the operation should retry, return, or report a state mismatch.\n\nKeep atomic coordination narrow and testable. The best atomic state is small enough that a reviewer can list the readers, writers, transitions, ordering assumptions, and shutdown behavior without reconstructing the whole program.\n\nIf the list becomes long, split the state, name the protocol, or use a higher-level synchronization mechanism.\n\nSmall protocols are easier to test repeatedly.\n\nName them clearly.",
  "narrationPoints": [
    "Many practical atomic uses involve flags, counters, and small state machines.",
    "A worker must check the flag at appropriate points, stop accepting or processing work according to policy, release resources safely, and be joined or otherwise managed before referenced state disappears.",
    "A counter used only for approximate telemetry may not need the same ordering as a flag used to coordinate object visibility.",
    "If many unrelated states accumulate, a dedicated synchronized object may be clearer.",
    "Reviewers should know what happens when another thread changes the value first and whether the operation should retry, return, or report a state mismatch.",
    "If the list becomes long, split the state, name the protocol, or use a higher-level synchronization mechanism."
  ]
};
