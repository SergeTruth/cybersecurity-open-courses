window.COURSE_MODULE = {
  "title": "Why Endpoints Matter",
  "graphicAlt": "Endpoint laptop sending telemetry signals to a security operations workflow.",
  "narration": "An endpoint is a device that connects to an organization's environment. Laptops, desktops, servers, and virtual machines are all examples of endpoints. These systems matter because they are where people work, files are opened, credentials are used, and business applications run.\n\nAttackers often target endpoints because a compromised endpoint can become a foothold. From there, an attacker may steal data, install malware, capture credentials, move laterally, or reach other systems. Even when strong network and identity controls exist, endpoint activity can reveal what is actually happening on the device.\n\nEndpoint Detection and Response, or EDR, exists because organizations need visibility into these systems and a way to respond when suspicious activity appears. EDR is not a magic shield. It is a visibility and response layer that helps defenders understand endpoint behavior and act when something looks wrong.",
  "narrationPoints": [
    "An endpoint is a device that connects to an organization's environment. Laptops, desktops, servers, and virtual machines are all examples of endpoints. These systems matter because they are where people work, files are opened, credentials are used, and business applications run.",
    "Attackers often target endpoints because a compromised endpoint can become a foothold. From there, an attacker may steal data, install malware, capture credentials, move laterally, or reach other systems. Even when strong network and identity controls exist, endpoint activity can reveal what is actually happening on the device.",
    "Endpoint Detection and Response, or EDR, exists because organizations need visibility into these systems and a way to respond when suspicious activity appears. EDR is not a magic shield. It is a visibility and response layer that helps defenders understand endpoint behavior and act when something looks wrong.",
    "An endpoint is a device that connects to an organization's environment.",
    "Laptops, desktops, servers, and virtual machines are all examples of endpoints.",
    "These systems matter because they are where people work, files are opened, credentials are used, and business applications run.",
    "Attackers often target endpoints because a compromised endpoint can become a foothold."
  ]
};
