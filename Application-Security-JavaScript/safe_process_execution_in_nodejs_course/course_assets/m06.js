window.COURSE_MODULE = {
  "title": "Input, Output, Streams, and Data Handling",
  "graphicAlt": "Left-to-right security workflow for Input, Output, Streams, and Data Handling in Safe Process Execution in Node.js: untrusted input crosses a labeled boundary, a validating component enforces policy, and only a verified result reaches the protected resource.",
  "narration": "A child process is both a consumer and producer of data. Treat process input and process output as untrusted unless the source and tool behavior are fully controlled. Avoid sending secrets or raw sensitive data to external tools unless there is a clear need and a protection plan. If a scanner, converter, compiler, or CLI receives customer files or internal data, the workflow should define where the data goes, how it is protected, how long it remains, and who can see the results.\n\nOutput handling needs limits. Some process APIs buffer output in memory, while others allow streaming. For large or unpredictable output, use streaming where appropriate with backpressure, output size limits, timeouts, and error handling. Do not return raw stderr, stack traces, command lines, filesystem paths, or internal tool output directly to users. Tool output may include sensitive paths, environment details, error messages copied from input files, or operational information that belongs in protected logs rather than client responses.\n\nCapture enough diagnostic information for operations without leaking secrets, tokens, credentials, private paths, file contents, or unnecessary personal data. Generated files should be written only to intended storage locations with safe names and permissions. If process output is rendered in HTML, JSON, reports, logs, or tickets, encode or sanitize it for that context. The safe pattern is bounded input, bounded output, safe storage, sanitized client responses, and protected operational evidence.",
  "narrationPoints": [
      "Treat process input and process output as untrusted unless the source and tool behavior are fully controlled.",
      "Tool output may include sensitive paths, environment details, error messages copied from input files, or operational information that belongs in protected logs rather than client responses.",
      "The safe pattern is bounded input, bounded output, safe storage, sanitized client responses, and protected operational evidence.",
      "Avoid sending secrets or raw sensitive data to external tools unless there is a clear need and a protection plan.",
      "If a scanner, converter, compiler, or CLI receives customer files or internal data, the workflow should define where the data goes, how it is protected, how long it remains, and who can see the results.",
      "For large or unpredictable output, use streaming where appropriate with backpressure, output size limits, timeouts, and error handling."
  ]
};
