window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Code Review",
  "graphicAlt": "Two-path comparison for Testing, Logging, Monitoring, and Code Review in Safe Process Execution in Node.js: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "Safe process execution should be visible in engineering workflow. Code review should identify uses of child_process, shell wrappers, npm scripts, deployment scripts, build scripts, and libraries that execute external tools. Reviewers should check why a process is needed, which executable can run, which arguments are allowed, who can trigger it, what environment is provided, which working directory is used, how long it can run, what it can access, where output goes, and how cleanup happens.\n\nTests should cover allowed arguments, rejected arguments, missing tools, failed exits, spawn errors, timeouts, large output, unauthorized triggers, cleanup behavior, and environment assumptions. Static analysis and dependency review can help identify risky patterns, but they do not replace design review. Important tests should prove that unapproved commands cannot run, unexpected options are rejected, output is bounded, and failures do not leave sensitive temporary data or orphaned processes behind.\n\nLogs and monitoring should support investigation without creating new exposure. Record security-relevant events such as process started, completed, failed, timed out, rejected, or denied. Avoid raw command strings, secrets, tokens, credentials, private paths, file contents, and unnecessary personal data. Monitor unusual execution volume, repeated failures, long runtimes, high resource use, queue growth, and suspicious argument rejections. Incident response should include log review, process history review, file exposure review, permission review, and secret rotation when process execution is suspected.",
  "narrationPoints": [
      "Code review should identify uses of child_process, shell wrappers, npm scripts, deployment scripts, build scripts, and libraries that execute external tools.",
      "Static analysis and dependency review can help identify risky patterns, but they do not replace design review.",
      "Incident response should include log review, process history review, file exposure review, permission review, and secret rotation when process execution is suspected.",
      "Avoid raw command strings, secrets, tokens, credentials, private paths, file contents, and unnecessary personal data.",
      "Logs and monitoring should support investigation without creating new exposure.",
      "Reviewers should check why a process is needed, which executable can run, which arguments are allowed, who can trigger it, what environment is provided, which working directory is used, how long it can run, what it can access, where output goes, and how cleanup happens."
  ]
};
