window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Input, Output, Streams, and Data Handling through distinct, reviewable JavaScript security boundaries.",
  "codeExamples": [
    {
      "title": "Collect stdout and stderr under separate byte limits",
      "language": "javascript",
      "blurb": "The collector snapshots limits and lifecycle capabilities, copies retained Buffer chunks, removes every installed listener, and preserves registration, stream, size, termination, and teardown failures.",
      "code": "const MAXIMUM_STDOUT_BYTES = 1024 * 1024;\nconst MAXIMUM_STDERR_BYTES = 256 * 1024;\nconst FINAL_CHANNEL_CLOSE_MS = 1000;\n\nasync function rejectInvalidChannelChild(\n  child, onceMethod, killMethod, inspectionErrors = []\n) {\n  const contractError = new TypeError(\"child output lifecycle required\");\n  const cleanupErrors = [...inspectionErrors];\n  const once = typeof onceMethod === \"function\" ? onceMethod.bind(child) : null;\n  const kill = typeof killMethod === \"function\" ? killMethod.bind(child) : null;\n  let closeEvent;\n  if (once) {\n    try { once(\"error\", (error) => { cleanupErrors.push(error); }); }\n    catch (error) { cleanupErrors.push(error); }\n    let resolveClose;\n    const candidateClose = new Promise((resolve) => { resolveClose = resolve; });\n    try {\n      once(\"close\", resolveClose);\n      closeEvent = candidateClose;\n    } catch (error) { cleanupErrors.push(error); }\n  }\n  if (kill) {\n    try { kill(\"SIGKILL\"); }\n    catch (error) { cleanupErrors.push(error); }\n  } else {\n    cleanupErrors.push(new Error(\"invalid child has no termination capability\"));\n  }\n  if (closeEvent && kill) {\n    let timer;\n    const closed = await Promise.race([\n      closeEvent.then(() => true),\n      new Promise((resolve) => {\n        timer = setTimeout(() => resolve(false), FINAL_CHANNEL_CLOSE_MS);\n      })\n    ]);\n    clearTimeout(timer);\n    if (!closed) cleanupErrors.push(new Error(\"invalid child did not close after SIGKILL\"));\n  } else if (!closeEvent) {\n    cleanupErrors.push(new Error(\"invalid child has no close evidence\"));\n  }\n  throw new AggregateError(\n    [contractError, ...cleanupErrors], \"child output lifecycle validation failed\"\n  );\n}\n\nexport function collectBoundedChannels(child, limits) {\n  const stdoutLimit = limits?.stdout;\n  const stderrLimit = limits?.stderr;\n  if (!Number.isSafeInteger(stdoutLimit) || stdoutLimit < 1 ||\n      stdoutLimit > MAXIMUM_STDOUT_BYTES ||\n      !Number.isSafeInteger(stderrLimit) || stderrLimit < 1 ||\n      stderrLimit > MAXIMUM_STDERR_BYTES) {\n    throw new RangeError(\"process output limits invalid\");\n  }\n  let childOnceMethod;\n  let childKillMethod;\n  let childRemoveMethod;\n  let stdoutStream;\n  let stderrStream;\n  let stdoutOnceMethod;\n  let stdoutOnMethod;\n  let stdoutRemoveMethod;\n  let stderrOnceMethod;\n  let stderrOnMethod;\n  let stderrRemoveMethod;\n  const inspectionErrors = [];\n  try { childOnceMethod = child?.once; }\n  catch (error) { inspectionErrors.push(error); }\n  try { childKillMethod = child?.kill; }\n  catch (error) { inspectionErrors.push(error); }\n  try { childRemoveMethod = child?.removeListener; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stdoutStream = child?.stdout; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrStream = child?.stderr; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stdoutOnceMethod = stdoutStream?.once; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stdoutOnMethod = stdoutStream?.on; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stdoutRemoveMethod = stdoutStream?.removeListener; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrOnceMethod = stderrStream?.once; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrOnMethod = stderrStream?.on; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrRemoveMethod = stderrStream?.removeListener; }\n  catch (error) { inspectionErrors.push(error); }\n  if (inspectionErrors.length || typeof childOnceMethod !== \"function\" ||\n      typeof childKillMethod !== \"function\" || typeof childRemoveMethod !== \"function\" ||\n      typeof stdoutOnceMethod !== \"function\" || typeof stdoutOnMethod !== \"function\" ||\n      typeof stdoutRemoveMethod !== \"function\" ||\n      typeof stderrOnceMethod !== \"function\" || typeof stderrOnMethod !== \"function\" ||\n      typeof stderrRemoveMethod !== \"function\") {\n    return rejectInvalidChannelChild(\n      child, childOnceMethod, childKillMethod, inspectionErrors\n    );\n  }\n  const onceChild = childOnceMethod.bind(child);\n  const killChild = childKillMethod.bind(child);\n  const removeChildListener = childRemoveMethod.bind(child);\n  const onceStdout = stdoutOnceMethod.bind(stdoutStream);\n  const onStdout = stdoutOnMethod.bind(stdoutStream);\n  const removeStdoutListener = stdoutRemoveMethod.bind(stdoutStream);\n  const onceStderr = stderrOnceMethod.bind(stderrStream);\n  const onStderr = stderrOnMethod.bind(stderrStream);\n  const removeStderrListener = stderrRemoveMethod.bind(stderrStream);\n  return new Promise((resolve, reject) => {\n    const stdoutChunks = []; const stderrChunks = []; const cleanupErrors = [];\n    let outBytes = 0; let errBytes = 0; let failure; let closeTimer; let settled = false;\n    const onChildError = (error) => fail(error);\n    const onStdoutError = (error) => fail(error);\n    const onStderrError = (error) => fail(error);\n    const onStdoutData = (chunk) => {\n      if (failure) return;\n      if (!Buffer.isBuffer(chunk)) {\n        fail(new TypeError(\"stdout must emit Buffer chunks\"));\n        return;\n      }\n      if (chunk.length > stdoutLimit - outBytes) fail(new RangeError(\"stdout limit\"));\n      else { outBytes += chunk.length; stdoutChunks.push(Buffer.from(chunk)); }\n    };\n    const onStderrData = (chunk) => {\n      if (failure) return;\n      if (!Buffer.isBuffer(chunk)) {\n        fail(new TypeError(\"stderr must emit Buffer chunks\"));\n        return;\n      }\n      if (chunk.length > stderrLimit - errBytes) fail(new RangeError(\"stderr limit\"));\n      else { errBytes += chunk.length; stderrChunks.push(Buffer.from(chunk)); }\n    };\n    const cleanupListeners = () => {\n      for (const [remove, event, listener] of [\n        [removeChildListener, \"close\", onChildClose],\n        [removeChildListener, \"error\", onChildError],\n        [removeStdoutListener, \"error\", onStdoutError],\n        [removeStdoutListener, \"data\", onStdoutData],\n        [removeStderrListener, \"error\", onStderrError],\n        [removeStderrListener, \"data\", onStderrData]\n      ]) {\n        try { remove(event, listener); }\n        catch (error) { cleanupErrors.push(error); }\n      }\n    };\n    const finish = (action) => {\n      if (settled) return;\n      settled = true; clearTimeout(closeTimer); cleanupListeners(); action();\n    };\n    const fail = (error) => {\n      if (failure) return;\n      failure = error;\n      closeTimer = setTimeout(() => finish(() => reject(new AggregateError(\n        [failure, ...cleanupErrors, new Error(\"child did not close after SIGKILL\")],\n        \"process output limit and cleanup failed\"))), FINAL_CHANNEL_CLOSE_MS);\n      closeTimer.unref?.();\n      try { killChild(\"SIGKILL\"); } catch (killError) { cleanupErrors.push(killError); }\n    };\n    const onChildClose = (code, signal) => finish(() => {\n      if (failure && cleanupErrors.length) {\n        reject(new AggregateError([failure, ...cleanupErrors], \"process output and cleanup failed\"));\n      } else if (failure) reject(failure);\n      else if (cleanupErrors.length) {\n        reject(new AggregateError(cleanupErrors, \"process listener cleanup failed\"));\n      } else resolve({\n        code, signal,\n        stdout: Buffer.concat(stdoutChunks, outBytes),\n        stderr: Buffer.concat(stderrChunks, errBytes)\n      });\n    });\n    try {\n      onceChild(\"close\", onChildClose);\n      onceChild(\"error\", onChildError);\n      onceStdout(\"error\", onStdoutError);\n      onceStderr(\"error\", onStderrError);\n      onStdout(\"data\", onStdoutData);\n      onStderr(\"data\", onStderrData);\n    } catch (error) { fail(error); }\n  });\n}\n"
    },
    {
      "title": "Pipe child output with backpressure",
      "language": "javascript",
      "blurb": "The streaming helper snapshots child capabilities, bounds stderr, handles stream errors, removes every installed listener, and applies a final bounded close wait after forced termination.",
      "code": "import { pipeline } from \"node:stream/promises\";\n\nconst FINAL_STREAM_CLOSE_MS = 1000;\nconst MAXIMUM_OBSERVED_STDERR_BYTES = 1024 * 1024 * 1024;\nconst MAXIMUM_STDERR_PREVIEW_BYTES = 4096;\n\nasync function boundedClose(closeEvent, timeoutMs) {\n  let timer;\n  try {\n    return await Promise.race([\n      closeEvent.then((value) => ({ status: \"closed\", value })),\n      new Promise((resolve) => {\n        timer = setTimeout(() => resolve({ status: \"timeout\" }), timeoutMs);\n      })\n    ]);\n  } finally { clearTimeout(timer); }\n}\n\nasync function rejectInvalidStreamChild(\n  child, onceMethod, killMethod, inspectionErrors = []\n) {\n  const contractError = new TypeError(\"child stream lifecycle required\");\n  const cleanupErrors = [...inspectionErrors];\n  const once = typeof onceMethod === \"function\" ? onceMethod.bind(child) : null;\n  const kill = typeof killMethod === \"function\" ? killMethod.bind(child) : null;\n  let closeEvent;\n  if (once) {\n    try { once(\"error\", (error) => { cleanupErrors.push(error); }); }\n    catch (error) { cleanupErrors.push(error); }\n    let resolveClose;\n    const candidateClose = new Promise((resolve) => { resolveClose = resolve; });\n    try {\n      once(\"close\", resolveClose);\n      closeEvent = candidateClose;\n    } catch (error) { cleanupErrors.push(error); }\n  }\n  if (kill) {\n    try { kill(\"SIGKILL\"); }\n    catch (error) { cleanupErrors.push(error); }\n  } else {\n    cleanupErrors.push(new Error(\"invalid child has no termination capability\"));\n  }\n  if (closeEvent && kill) {\n    const result = await boundedClose(closeEvent, FINAL_STREAM_CLOSE_MS);\n    if (result.status === \"timeout\") {\n      cleanupErrors.push(new Error(\"invalid child did not close after SIGKILL\"));\n    }\n  } else if (!closeEvent) {\n    cleanupErrors.push(new Error(\"invalid child has no close evidence\"));\n  }\n  throw new AggregateError(\n    [contractError, ...cleanupErrors], \"child stream lifecycle validation failed\"\n  );\n}\n\nexport async function streamChildResult(child, destination) {\n  let childOnceMethod;\n  let childKillMethod;\n  let childRemoveMethod;\n  let stdoutStream;\n  let stderrStream;\n  let stderrOnMethod;\n  let stderrOnceMethod;\n  let stderrRemoveMethod;\n  const inspectionErrors = [];\n  try { childOnceMethod = child?.once; }\n  catch (error) { inspectionErrors.push(error); }\n  try { childKillMethod = child?.kill; }\n  catch (error) { inspectionErrors.push(error); }\n  try { childRemoveMethod = child?.removeListener; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stdoutStream = child?.stdout; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrStream = child?.stderr; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrOnMethod = stderrStream?.on; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrOnceMethod = stderrStream?.once; }\n  catch (error) { inspectionErrors.push(error); }\n  try { stderrRemoveMethod = stderrStream?.removeListener; }\n  catch (error) { inspectionErrors.push(error); }\n  if (inspectionErrors.length || typeof childOnceMethod !== \"function\" ||\n      typeof childKillMethod !== \"function\" || typeof childRemoveMethod !== \"function\" ||\n      !stdoutStream || typeof stderrOnMethod !== \"function\" ||\n      typeof stderrOnceMethod !== \"function\" ||\n      typeof stderrRemoveMethod !== \"function\") {\n    return rejectInvalidStreamChild(\n      child, childOnceMethod, childKillMethod, inspectionErrors\n    );\n  }\n  const onceChild = childOnceMethod.bind(child);\n  const killChild = childKillMethod.bind(child);\n  const removeChildListener = typeof childRemoveMethod === \"function\"\n    ? childRemoveMethod.bind(child) : null;\n  const onStderr = stderrOnMethod.bind(stderrStream);\n  const onceStderr = stderrOnceMethod.bind(stderrStream);\n  const removeStderrListener = stderrRemoveMethod.bind(stderrStream);\n  const stderr = [];\n  let retainedStderrBytes = 0;\n  let observedStderrBytes = 0;\n  let childError;\n  let didClose = false;\n  let rejectSideError;\n  const sideError = new Promise((_, reject) => { rejectSideError = reject; });\n  sideError.catch(() => {});\n  const onSideError = (error) => { childError ??= error; rejectSideError(error); };\n  const onStderrData = (chunk) => {\n    if (!Buffer.isBuffer(chunk)) {\n      onSideError(new TypeError(\"stderr must emit Buffer chunks\"));\n      return;\n    }\n    if (chunk.length > MAXIMUM_OBSERVED_STDERR_BYTES - observedStderrBytes) {\n      onSideError(new RangeError(\"stderr exceeds observed byte limit\"));\n      return;\n    }\n    observedStderrBytes += chunk.length;\n    if (retainedStderrBytes < MAXIMUM_STDERR_PREVIEW_BYTES) {\n      const count = Math.min(\n        chunk.length, MAXIMUM_STDERR_PREVIEW_BYTES - retainedStderrBytes\n      );\n      stderr.push(Buffer.from(chunk.subarray(0, count)));\n      retainedStderrBytes += count;\n    }\n  };\n  let resolveClosed;\n  const closed = new Promise((resolve) => { resolveClosed = resolve; });\n  const onClose = (code, signal) => {\n    didClose = true; resolveClosed({ code, signal });\n  };\n  let closeInstalled = false;\n  let childErrorInstalled = false;\n  let stderrErrorInstalled = false;\n  let stderrDataInstalled = false;\n  try {\n    onceChild(\"close\", onClose); closeInstalled = true;\n    onceChild(\"error\", onSideError); childErrorInstalled = true;\n    onceStderr(\"error\", onSideError); stderrErrorInstalled = true;\n    onStderr(\"data\", onStderrData); stderrDataInstalled = true;\n  } catch (error) {\n    const registrationErrors = [error];\n    for (const [installed, remove, event, listener] of [\n      [closeInstalled, removeChildListener, \"close\", onClose],\n      [childErrorInstalled, removeChildListener, \"error\", onSideError],\n      [stderrErrorInstalled, removeStderrListener, \"error\", onSideError],\n      [stderrDataInstalled, removeStderrListener, \"data\", onStderrData]\n    ]) {\n      if (!installed) continue;\n      try { remove(event, listener); }\n      catch (cleanupError) { registrationErrors.push(cleanupError); }\n    }\n    return rejectInvalidStreamChild(\n      child, childOnceMethod, childKillMethod, registrationErrors\n    );\n  }\n  let output; let primaryError;\n  const cleanupErrors = [];\n  try {\n    const [, result] = await Promise.race([\n      Promise.all([pipeline(stdoutStream, destination), closed]),\n      sideError\n    ]);\n    if (childError) throw childError;\n    const stderrPreview = Buffer.concat(\n      stderr, retainedStderrBytes\n    ).toString(\"utf8\");\n    if (result.code !== 0 || result.signal) {\n      throw new Error(\"child process failed\" + (stderrPreview ? \" with diagnostics\" : \"\"));\n    }\n    output = Object.freeze({ stderrPreview });\n  } catch (error) {\n    primaryError = error;\n    if (!didClose) {\n      try { killChild(\"SIGKILL\"); } catch (failure) { cleanupErrors.push(failure); }\n    }\n    const closeResult = await boundedClose(closed, FINAL_STREAM_CLOSE_MS);\n    if (closeResult.status === \"timeout\") {\n      cleanupErrors.push(new Error(\"child did not close after SIGKILL\"));\n    }\n    if (childError && childError !== error) cleanupErrors.push(childError);\n  }\n  for (const [remove, event, listener] of [\n    [removeChildListener, \"close\", onClose],\n    [removeChildListener, \"error\", onSideError],\n    [removeStderrListener, \"data\", onStderrData],\n    [removeStderrListener, \"error\", onSideError]\n  ]) {\n    try { remove(event, listener); }\n    catch (error) { cleanupErrors.push(error); }\n  }\n  if (primaryError && cleanupErrors.length) {\n    throw new AggregateError([primaryError, ...cleanupErrors], \"streaming and cleanup failed\");\n  }\n  if (primaryError) throw primaryError;\n  if (cleanupErrors.length) {\n    throw new AggregateError(cleanupErrors, \"stream listener cleanup failed\");\n  }\n  return output;\n}\n"
    }
  ]
};
