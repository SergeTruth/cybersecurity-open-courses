window.COURSE_MODULE = {
  "title": "Least Privilege, Isolation, Containers, and Operational Controls",
  "graphicAlt": "Trust-boundary map for Least Privilege, Isolation, Containers, and Operational Controls in Safe Process Execution in Node.js, with distinct caller, application, platform, and external-service zones connected only through labeled validation and authorization checkpoints.",
  "narration": "Process execution should assume failure is possible. Validation may be incomplete, a tool may have a vulnerability, a dependency may behave unexpectedly, or an input file may trigger expensive behavior. Least privilege and isolation limit the damage when something goes wrong. Run the Node.js process and child processes with only the privileges they need. Avoid executing child processes as root or with administrative privileges unless there is a documented exceptional requirement.\n\nHigh-risk processing can often be separated into isolated workers. A file conversion worker might run in a container with a read-only input mount, a narrow writable temporary directory, no production secrets, limited network access, and a restricted service account. Avoid mounting sensitive host paths, Docker sockets, secret directories, broad credentials, or unrelated storage into process-execution environments without strong justification. Public request handling and privileged administrative execution should be separated where practical.\n\nDefense in depth does not replace safe command construction and validation. A container boundary, read-only filesystem, limited mount, or service account reduces blast radius, but it does not make arbitrary command execution safe. Review which secrets, files, network destinations, and credentials are available to each execution context. Operational controls should make dangerous settings visible and make safer defaults easier for developers to use.",
  "narrationPoints": [
      "Least privilege and isolation limit the damage when something goes wrong.",
      "Avoid mounting sensitive host paths, Docker sockets, secret directories, broad credentials, or unrelated storage into process-execution environments without strong justification.",
      "Operational controls should make dangerous settings visible and make safer defaults easier for developers to use.",
      "Validation may be incomplete, a tool may have a vulnerability, a dependency may behave unexpectedly, or an input file may trigger expensive behavior.",
      "A file conversion worker might run in a container with a read-only input mount, a narrow writable temporary directory, no production secrets, limited network access, and a restricted service account.",
      "Run the Node.js process and child processes with only the privileges they need."
  ]
};
