window.COURSE_MODULE = {
  "title": "Environment, PATH, Working Directory, and Executable Selection",
  "graphicAlt": "Control-and-evidence diagram for Environment, PATH, Working Directory, and Executable Selection in Safe Process Execution in Node.js, linking each security decision to the protected asset, the enforcing component, the failure state, and the observable verification signal.",
  "narration": "Command safety is not only about arguments. A child process behaves differently depending on environment variables, PATH lookup, current working directory, locale, proxy settings, shell configuration, available files, and platform behavior. The environment inherited from a Node.js process may contain database credentials, cloud tokens, npm tokens, API keys, proxy settings, feature flags, and internal configuration. Do not pass the entire parent environment to a child process unless that decision is intentional and reviewed.\n\nProvide a minimal environment where practical. Prefer absolute paths or trusted executable locations for sensitive workflows instead of relying on arbitrary PATH lookup. Set the working directory intentionally and avoid using untrusted input to choose executables, scripts, environment keys, environment values, working directories, or output locations. A conversion tool should not receive production database credentials simply because the parent process had them. A deployment script should not run from a directory controlled by a request or upload.\n\nBuild-time execution and production execution are different risk contexts. CI runners may have package publishing tokens, deployment credentials, signing keys, or access to private registries. Production services may have database access and customer data. Containers, Linux, macOS, Windows, and serverless platforms may resolve executables and paths differently. Review the execution context in the environment where the command will actually run, not only on a developer workstation.",
  "narrationPoints": [
      "A child process behaves differently depending on environment variables, PATH lookup, current working directory, locale, proxy settings, shell configuration, available files, and platform behavior.",
      "Set the working directory intentionally and avoid using untrusted input to choose executables, scripts, environment keys, environment values, working directories, or output locations.",
      "CI runners may have package publishing tokens, deployment credentials, signing keys, or access to private registries.",
      "The environment inherited from a Node.js process may contain database credentials, cloud tokens, npm tokens, API keys, proxy settings, feature flags, and internal configuration.",
      "Prefer absolute paths or trusted executable locations for sensitive workflows instead of relying on arbitrary PATH lookup.",
      "A conversion tool should not receive production database credentials simply because the parent process had them."
  ]
};
