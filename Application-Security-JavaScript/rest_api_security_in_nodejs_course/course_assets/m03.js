window.COURSE_MODULE = {
  "title": "Authentication and Session Handling",
  "graphicAlt": "Security diagram for REST API Security in NodeJS, Authentication and Session Handling, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Authentication answers the question, who is making this request. NodeJS REST APIs may use session cookies, bearer tokens, OAuth flows, JWTs, API keys, service credentials, or combinations of these patterns. The important distinction is between receiving a credential and trusting it. Tokens and sessions need server-side validation appropriate to the design, including issuer, audience, expiration, signature, intended use, revocation expectations, and how the identity maps to local users or services.\n\nImplementation mistakes often happen in middleware and route wiring. One route may miss authentication middleware. A token parser may accept data without verifying the signature. A service may log bearer tokens during debugging. An API key may be treated as if it proves an end user's identity when it only identifies an application or integration. Authentication middleware should fail closed, behave consistently, and make the authenticated identity available to later authorization checks in a predictable way.\n\nAuthentication also depends on surrounding account and session behavior. Password handling, MFA, account recovery, session revocation, refresh tokens, token storage, cookie settings, and login abuse controls affect the API even when they live outside a route handler. A secure API design treats identity as a lifecycle. It asks how identities are established, how they are refreshed, how they are revoked, and how the API behaves when identity state changes while requests are still in flight.",
  "narrationPoints": [
    "Tokens and sessions need server-side validation appropriate to the design, including issuer, audience, expiration, signature, intended use, revocation expectations, and how the identity maps to local users or services.",
    "Authentication middleware should fail closed, behave consistently, and make the authenticated identity available to later authorization checks in a predictable way.",
    "Password handling, MFA, account recovery, session revocation, refresh tokens, token storage, cookie settings, and login abuse controls affect the API even when they live outside a route handler.",
    "NodeJS REST APIs may use session cookies, bearer tokens, OAuth flows, JWTs, API keys, service credentials, or combinations of these patterns.",
    "Authentication also depends on surrounding account and session behavior.",
    "A token parser may accept data without verifying the signature."
  ]
};
