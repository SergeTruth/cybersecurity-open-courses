window.COURSE_MODULE = {
  "title": "Dependencies, Secrets, Logging, and Monitoring",
  "graphicAlt": "Security diagram for REST API Security in NodeJS, Dependencies, Secrets, Logging, and Monitoring, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "The NodeJS ecosystem is part of the API attack surface. npm dependencies, transitive packages, middleware, JSON parsers, authentication libraries, validation packages, and build tools all influence risk. Lockfiles, dependency review, update discipline, and npm audit triage help, but scanning is not complete API security. Teams still need to understand which packages handle credentials, parsing, authorization, serialization, or network calls, because those packages sit close to sensitive behavior.\n\nSecrets should be handled as operational assets. API tokens, database credentials, signing keys, OAuth secrets, session secrets, and service credentials should come from secret managers, protected CI/CD variables, or platform identity features where possible. They should not be hardcoded, committed, printed, placed in images, leaked through logs, or exposed to client-visible bundles. Rotation and revocation should be feasible without rewriting the application.\n\nLogging and monitoring make API security real in production. Useful logs include request IDs, authenticated identity or service identity, endpoint, outcome, timing, authorization denials, validation failures, security-relevant actions, and selected business events. Logs should avoid tokens, passwords, session identifiers, sensitive payloads, and unnecessary personal data. Monitoring should detect unusual traffic, repeated failures, high error rates, authorization denials, suspicious use of sensitive flows, dependency failures, and changes in normal API behavior.",
  "narrationPoints": [
    "npm dependencies, transitive packages, middleware, JSON parsers, authentication libraries, validation packages, and build tools all influence risk.",
    "API tokens, database credentials, signing keys, OAuth secrets, session secrets, and service credentials should come from secret managers, protected CI/CD variables, or platform identity features where possible.",
    "Monitoring should detect unusual traffic, repeated failures, high error rates, authorization denials, suspicious use of sensitive flows, dependency failures, and changes in normal API behavior.",
    "Logging and monitoring make API security real in production.",
    "Useful logs include request IDs, authenticated identity or service identity, endpoint, outcome, timing, authorization denials, validation failures, security-relevant actions, and selected business events.",
    "Logs should avoid tokens, passwords, session identifiers, sensitive payloads, and unnecessary personal data."
  ]
};
