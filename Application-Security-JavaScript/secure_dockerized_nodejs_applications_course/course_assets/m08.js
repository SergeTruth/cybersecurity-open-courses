window.COURSE_MODULE = {
  "title": "Runtime Hardening, Resource Limits, and Observability",
  "graphicAlt": "Trust-boundary map for Runtime Hardening, Resource Limits, and Observability in Secure Dockerized Node.js Applications, with distinct caller, application, platform, and external-service zones connected only through labeled validation and authorization checkpoints.",
  "narration": "A secure image can still be run unsafely. Runtime configuration decides what the container can do after deployment. Hardening includes user identity, Linux capabilities, filesystem mode, process limits, memory limits, CPU limits, restart policy, and mounted paths. Drop unnecessary capabilities where possible, avoid privileged containers except for documented exceptional cases, and be cautious with broad host mounts or access to container runtime sockets. These settings should be visible during review, not hidden in deployment defaults.\n\nRead-only root filesystems and specific writable paths can reduce the impact of unexpected writes, but they require realistic application testing. Node.js applications may need temporary directories, upload locations, cache paths, or writable runtime state depending on design. Resource limits also need testing. Memory and CPU limits help prevent one container from exhausting shared resources, but limits that are too tight can cause instability. The goal is controlled behavior under normal load and predictable failure under stress.\n\nObservability supports both operations and security. Configure health checks, structured logging, metrics, and monitoring so teams can understand production behavior. Logs should be useful for investigation without exposing passwords, tokens, authorization headers, request bodies, session identifiers, or unnecessary personal data. Runtime policy should be reviewed along with application code because a container's effective security depends on how it is launched and observed, not only how it is built.",
  "narrationPoints": [
      "Hardening includes user identity, Linux capabilities, filesystem mode, process limits, memory limits, CPU limits, restart policy, and mounted paths.",
      "Memory and CPU limits help prevent one container from exhausting shared resources, but limits that are too tight can cause instability.",
      "Runtime policy should be reviewed along with application code because a container's effective security depends on how it is launched and observed, not only how it is built.",
      "Drop unnecessary capabilities where possible, avoid privileged containers except for documented exceptional cases, and be cautious with broad host mounts or access to container runtime sockets.",
      "Logs should be useful for investigation without exposing passwords, tokens, authorization headers, request bodies, session identifiers, or unnecessary personal data.",
      "Read-only root filesystems and specific writable paths can reduce the impact of unexpected writes, but they require realistic application testing."
  ]
};
