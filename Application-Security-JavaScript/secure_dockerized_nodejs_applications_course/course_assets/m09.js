window.COURSE_MODULE = {
  "title": "Image Scanning, SBOMs, Registries, and CI/CD Controls",
  "graphicAlt": "Two-path comparison for Image Scanning, SBOMs, Registries, and CI/CD Controls in Secure Dockerized Node.js Applications: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "A secure delivery pipeline should create a chain of evidence for each image. Teams should be able to answer what source produced the image, which dependencies are inside it, who or what built it, what tests and scans ran, who approved it, how it was tagged, where it is stored, and where it is deployed. Container scanning helps identify known vulnerabilities in operating system packages and application dependencies, but scanner results require triage rather than blind acceptance or dismissal.\n\nTriage should consider severity, exploitability, reachability, runtime exposure, available fixes, and whether the vulnerable component is present in the final runtime image. A build-only package may carry different risk than a library reachable by a public request path. SBOMs help describe what is inside an image and support vulnerability response when new advisories appear. They are evidence for response and governance, not proof that the application is secure.\n\nRegistries also need security controls. Enforce access control, audit logging, retention, immutability where appropriate, and clear tagging rules. CI/CD should build images from trusted source, protect secrets, run tests, scan images, produce or retain dependency evidence, tag images consistently, and deploy only approved artifacts. Signing, provenance, and attestations can improve confidence in where an image came from and how it was built, but application logic, configuration, secrets, and runtime policy still require review.",
  "narrationPoints": [
      "Container scanning helps identify known vulnerabilities in operating system packages and application dependencies, but scanner results require triage rather than blind acceptance or dismissal.",
      "A build-only package may carry different risk than a library reachable by a public request path.",
      "Signing, provenance, and attestations can improve confidence in where an image came from and how it was built, but application logic, configuration, secrets, and runtime policy still require review.",
      "CI/CD should build images from trusted source, protect secrets, run tests, scan images, produce or retain dependency evidence, tag images consistently, and deploy only approved artifacts.",
      "A secure delivery pipeline should create a chain of evidence for each image.",
      "Enforce access control, audit logging, retention, immutability where appropriate, and clear tagging rules."
  ]
};
