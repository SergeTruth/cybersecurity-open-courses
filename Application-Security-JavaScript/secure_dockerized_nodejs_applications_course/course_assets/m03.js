window.COURSE_MODULE = {
  "title": "Secure Dockerfiles and Base Image Selection",
  "graphicAlt": "Two-path comparison for Secure Dockerfiles and Base Image Selection in Secure Dockerized Node.js Applications: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "The base image is part of the application supply chain. A Node.js application with a few hundred lines of custom code may inherit many more files, packages, and configuration assumptions from its base image. Choose maintained images from trusted sources, keep them updated, and decide how updates are reviewed. Pinning versions can support repeatability, but pinning without an update process can preserve vulnerabilities. Floating tags can receive updates, but they may also create uncontrolled drift if builds are not tracked carefully.\n\nProduction images should contain only what the application needs to run. Multi-stage builds are a practical way to separate build tools from runtime contents. One stage can install dependencies, compile TypeScript, build frontend assets, or run tests. The final stage should copy only the required runtime files into a smaller image. That approach reduces the number of tools, compilers, package managers, shells, and temporary files available if the application or a dependency behaves unexpectedly.\n\n.dockerignore is easy to overlook, but it is a security control. It keeps source-control noise, local files, test artifacts, credentials, temporary output, logs, and other unnecessary files out of the build context. Dockerfile instructions should be understandable and reviewable, not a long chain of opaque shell commands. A reviewer should be able to tell where files come from, which packages are installed, when dependencies are resolved, which user runs the process, and what command starts the service.",
  "narrationPoints": [
      "A Node.js application with a few hundred lines of custom code may inherit many more files, packages, and configuration assumptions from its base image.",
      "That approach reduces the number of tools, compilers, package managers, shells, and temporary files available if the application or a dependency behaves unexpectedly.",
      "A reviewer should be able to tell where files come from, which packages are installed, when dependencies are resolved, which user runs the process, and what command starts the service.",
      "It keeps source-control noise, local files, test artifacts, credentials, temporary output, logs, and other unnecessary files out of the build context.",
      "The final stage should copy only the required runtime files into a smaller image.",
      "Production images should contain only what the application needs to run."
  ]
};
