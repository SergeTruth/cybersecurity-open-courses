window.COURSE_MODULE = {
  "title": "Users, Permissions, and Filesystem Safety",
  "graphicAlt": "Control-and-evidence diagram for Users, Permissions, and Filesystem Safety in Secure Dockerized Node.js Applications, linking each security decision to the protected asset, the enforcing component, the failure state, and the observable verification signal.",
  "narration": "Running as root inside a container is still a high-risk default. Container isolation may reduce some risk, but least privilege limits damage when application code, a dependency, or a runtime process behaves unexpectedly. Most production Node.js services need to read application files, bind to an allowed port, write temporary data in specific places, and send logs to standard output. They usually do not need broad root-level access to the filesystem or operating system capabilities.\n\nUse a non-root user for the Node.js process when the application does not require root. File ownership should support that choice so the application can read what it needs without write access everywhere. Separate build-time permissions from runtime permissions: a build stage may need to install packages or create artifacts, while the runtime stage should only run the application. Avoid carrying writable build directories, package caches, credentials, and temporary files into production images.\n\nFilesystem safety is about intent. Do not give the application write access to more of the container than it needs. Avoid storing mutable application state inside the container unless the design explicitly requires it. Where practical, use read-only root filesystems, controlled writable directories, and carefully scoped mounted volumes. These settings may require engineering work, such as defining a temporary directory or adjusting logging behavior, but they make unexpected writes easier to detect and harder to abuse.",
  "narrationPoints": [
      "Container isolation may reduce some risk, but least privilege limits damage when application code, a dependency, or a runtime process behaves unexpectedly.",
      "Separate build-time permissions from runtime permissions: a build stage may need to install packages or create artifacts, while the runtime stage should only run the application.",
      "Avoid storing mutable application state inside the container unless the design explicitly requires it.",
      "File ownership should support that choice so the application can read what it needs without write access everywhere.",
      "Most production Node.js services need to read application files, bind to an allowed port, write temporary data in specific places, and send logs to standard output.",
      "Avoid carrying writable build directories, package caches, credentials, and temporary files into production images."
  ]
};
