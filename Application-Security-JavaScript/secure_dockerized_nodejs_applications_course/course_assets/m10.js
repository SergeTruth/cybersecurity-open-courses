window.COURSE_MODULE = {
  "title": "Course Summary and Practical Checklist",
  "graphicAlt": "Lifecycle sequence for Course Summary and Practical Checklist in Secure Dockerized Node.js Applications, tracing creation, validation, use, failure handling, cleanup, and verification while highlighting the component responsible at every transition.",
  "narration": "Secure Dockerized Node.js applications start with treating the image as a production artifact and a supply chain object. Use maintained base images, keep them updated, and decide how updates are reviewed. Use multi-stage builds to separate build tools from runtime images. Keep Dockerfiles clear, keep build contexts clean with .dockerignore, and copy only what the final runtime needs. Use lockfiles and repeatable npm installs so the dependency tree is predictable and reviewable.\n\nAt runtime, aim for least privilege. Run the Node.js process as non-root when possible, limit writable filesystem paths, avoid unnecessary capabilities, avoid privileged containers, and define realistic memory and CPU expectations. Keep secrets out of Dockerfiles, image layers, repositories, logs, frontend bundles, and tickets. Deliver runtime secrets through approved mechanisms, protect build-time tokens, and remember that environment variables are only a delivery mechanism, not the whole secrets program.\n\nNetwork and operations controls complete the picture. Review published ports, reverse proxy settings, CORS, trusted proxy configuration, secure cookies, health checks, logging, and monitoring. Scan images, retain SBOM or dependency evidence where useful, triage findings responsibly, and protect registries with access control and auditability. A clean scan is helpful, but it does not prove the application, configuration, or runtime environment is secure.\n\nA practical first-week plan is simple: review Dockerfiles, check .dockerignore, confirm npm ci and lockfile use, verify non-root runtime, remove secrets from images and build logs, review published ports, add or tune image scanning, and document who owns base image updates. The long-term goal is a repeatable workflow where build design, dependency control, runtime permissions, secrets, CI/CD, registries, deployment policy, and observability work together.",
  "narrationPoints": [
      "Use multi-stage builds to separate build tools from runtime images.",
      "Run the Node.js process as non-root when possible, limit writable filesystem paths, avoid unnecessary capabilities, avoid privileged containers, and define realistic memory and CPU expectations.",
      "Review published ports, reverse proxy settings, CORS, trusted proxy configuration, secure cookies, health checks, logging, and monitoring.",
      "A practical first-week plan is simple: review Dockerfiles, check .dockerignore, confirm npm ci and lockfile use, verify non-root runtime, remove secrets from images and build logs, review published ports, add or tune image scanning, and document who owns base image updates.",
      "Deliver runtime secrets through approved mechanisms, protect build-time tokens, and remember that environment variables are only a delivery mechanism, not the whole secrets program.",
      "Scan images, retain SBOM or dependency evidence where useful, triage findings responsibly, and protect registries with access control and auditability."
  ]
};
