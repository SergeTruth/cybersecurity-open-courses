window.COURSE_MODULE = {
  "title": "Network Exposure, Ports, and Service Boundaries",
  "graphicAlt": "Layered architecture view of Network Exposure, Ports, and Service Boundaries for Secure Dockerized Node.js Applications, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "Container networking often changes between local Docker Compose and production. EXPOSE documents intended ports in the image, but it does not by itself publish those ports to the host. Real exposure is determined by published ports, reverse proxies, load balancers, service meshes, orchestrator services, ingress rules, and firewall or cloud network policy. A port that is convenient during development can become dangerous if it is published broadly in production.\n\nA Node.js service should listen on the intended interface and port for its deployment model. Public APIs, internal admin endpoints, metrics endpoints, debug ports, development servers, databases, caches, and queues should not all share the same exposure model. Network segmentation helps keep application containers, data stores, message queues, monitoring tools, and administrative interfaces in the right places. A database container should usually be reachable by the application network, not by the public internet.\n\nApplication settings must match the actual deployment path. TLS termination, forwarded headers, trusted proxy settings, CORS policy, secure cookies, and redirect behavior depend on whether traffic passes through a reverse proxy or load balancer. Health checks should reveal whether the service is healthy without leaking secrets, stack traces, dependency details, or internal configuration. Network security is a joint design between the container image, the application framework, and the platform running it.",
  "narrationPoints": [
      "Real exposure is determined by published ports, reverse proxies, load balancers, service meshes, orchestrator services, ingress rules, and firewall or cloud network policy.",
      "Network segmentation helps keep application containers, data stores, message queues, monitoring tools, and administrative interfaces in the right places.",
      "Health checks should reveal whether the service is healthy without leaking secrets, stack traces, dependency details, or internal configuration.",
      "A database container should usually be reachable by the application network, not by the public internet.",
      "Network security is a joint design between the container image, the application framework, and the platform running it.",
      "Public APIs, internal admin endpoints, metrics endpoints, debug ports, development servers, databases, caches, and queues should not all share the same exposure model."
  ]
};
