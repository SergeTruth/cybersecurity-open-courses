window.COURSE_MODULE = {
  "title": "Testing, Code Review, Logging, Monitoring, and CI/CD",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Testing, Code Review, Logging, Monitoring, and CI/CD, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "ORM security should be repeatable in daily engineering work. Code review should identify ORM calls, raw queries, dynamic filters, dynamic sorting, selected fields, relation includes, updates, nested writes, transactions, migrations, seed scripts, and admin tools. Reviewers should check validation, authorization, tenant scoping, field exposure, mutation allowlists, raw query safety, migration impact, database privileges, and error handling. The review should answer what model or table is accessed, what input shapes the query, what fields return, and what mutation is allowed.\n\nTests should cover unauthorized access, tenant boundaries, rejected filters, invalid sorts, sensitive field exclusion, unsafe update fields, raw query paths, destructive operations, relation traversal, and migration assumptions. CI/CD can run tests, linting, dependency checks, secret scanning, migration review, and deployment configuration checks where appropriate. Automated tools support the process, but they do not replace human review of business authorization, tenant isolation, and sensitive field exposure.\n\nLogs and monitoring should help defenders without leaking secrets. Capture useful security events such as denied access, validation failures, rejected filters, failed migrations, destructive operations, admin actions, suspicious query patterns, connection errors, unusual query volume, repeated authorization failures, migration failures, connection exhaustion, slow queries, unexpected exports, and administrative activity. Avoid logging connection strings, credentials, tokens, cookies, authorization headers, full sensitive records, raw secrets, SQL statements containing sensitive values, or unnecessary personal data. Incident response should include query-path review, affected model analysis, credential rotation, migration review, log review, backup review, and remediation validation.",
  "narrationPoints": [
    "Reviewers should check validation, authorization, tenant scoping, field exposure, mutation allowlists, raw query safety, migration impact, database privileges, and error handling.",
    "Tests should cover unauthorized access, tenant boundaries, rejected filters, invalid sorts, sensitive field exclusion, unsafe update fields, raw query paths, destructive operations, relation traversal, and migration assumptions.",
    "Incident response should include query-path review, affected model analysis, credential rotation, migration review, log review, backup review, and remediation validation.",
    "Avoid logging connection strings, credentials, tokens, cookies, authorization headers, full sensitive records, raw secrets, SQL statements containing sensitive values, or unnecessary personal data.",
    "Capture useful security events such as denied access, validation failures, rejected filters, failed migrations, destructive operations, admin actions, suspicious query patterns, connection errors, unusual query volume, repeated authorization failures, migration failures, connection exhaustion, slow queries, unexpected exports, and administrative activity.",
    "Logs and monitoring should help defenders without leaking secrets."
  ]
};
