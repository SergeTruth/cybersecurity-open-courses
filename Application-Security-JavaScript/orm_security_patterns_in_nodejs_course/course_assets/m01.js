window.COURSE_MODULE = {
  "title": "Why ORM Security Matters in Node.js",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Why ORM Security Matters in Node.js, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "ORMs and query builders help Node.js teams model data, perform queries, manage relationships, and reduce repetitive database code. They also sit directly between application behavior and sensitive data. A clean set of models can begin with simple CRUD operations, then grow into user accounts, tenant records, admin dashboards, exports, background jobs, reporting workflows, seed scripts, and production migrations. Each new workflow can read, change, expose, or destroy data if the access rules are not explicit.\n\nThe main security risks are practical: unsafe raw SQL, overbroad filters, mass assignment, missing authorization, tenant boundary failures, sensitive field exposure, excessive database privileges, exposed connection strings, unsafe migrations, verbose errors, and sensitive logging. Type safety and model definitions improve developer experience, but they do not prove that runtime input is safe or that a caller is authorized. An ORM can make unsafe access look tidy and consistent if the team treats the model layer as a substitute for security design.\n\nThe goal is to keep database access intentional, validated, authorized, least-privileged, observable, and recoverable. ORM security is not solved by installing Prisma, Sequelize, TypeORM, MikroORM, Mongoose, Knex-style query builders, or any custom repository layer. It comes from the patterns around them: runtime validation, controlled dynamic queries, safe mutations, explicit authorization, least-privilege accounts, secrets discipline, migration review, logging, monitoring, testing, and code review.",
  "narrationPoints": [
    "js teams model data, perform queries, manage relationships, and reduce repetitive database code.",
    "The main security risks are practical: unsafe raw SQL, overbroad filters, mass assignment, missing authorization, tenant boundary failures, sensitive field exposure, excessive database privileges, exposed connection strings, unsafe migrations, verbose errors, and sensitive logging.",
    "It comes from the patterns around them: runtime validation, controlled dynamic queries, safe mutations, explicit authorization, least-privilege accounts, secrets discipline, migration review, logging, monitoring, testing, and code review.",
    "ORM security is not solved by installing Prisma, Sequelize, TypeORM, MikroORM, Mongoose, Knex-style query builders, or any custom repository layer.",
    "An ORM can make unsafe access look tidy and consistent if the team treats the model layer as a substitute for security design.",
    "The goal is to keep database access intentional, validated, authorized, least-privileged, observable, and recoverable."
  ]
};
