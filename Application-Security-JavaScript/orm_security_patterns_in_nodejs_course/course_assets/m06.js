window.COURSE_MODULE = {
  "title": "Mass Assignment, Safe Mutations, and Transaction Integrity",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Mass Assignment, Safe Mutations, and Transaction Integrity, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Mutations carry high risk because they change data, ownership, authorization state, billing state, and audit history. Mass assignment is one of the most practical ORM risks. A profile update route may look harmless, but if it directly applies request fields to a model, the caller may be able to change fields the user interface never displayed. A create path, update path, upsert, replacement update, nested write, bulk update, or delete path should never blindly trust request-shaped objects.\n\nUse explicit allowlists for fields that callers may create or update. Sensitive fields such as role, permissions, tenantId, ownerId, passwordHash, tokenHash, emailVerified, accountStatus, billing state, audit fields, and security flags should not be client-controlled. Nested writes and relationship changes deserve review because they can create or modify related records. A caller who can update a project should not automatically be allowed to attach it to another organization, grant themselves a role, or create privileged related data.\n\nTransactions help maintain consistency, but they do not automatically enforce security rules. Authorization, scoping, validation, and audit evidence still have to be built into the transaction. Broad update or delete operations should be scoped, authorized, reviewed, tested, and logged. Destructive operations may need business confirmation logic and stronger evidence. The safe mutation pattern is explicit: authorize the action, validate the request, map allowed fields, scope the operation, preserve integrity, and record enough context to investigate later.",
  "narrationPoints": [
    "A create path, update path, upsert, replacement update, nested write, bulk update, or delete path should never blindly trust request-shaped objects.",
    "Sensitive fields such as role, permissions, tenantId, ownerId, passwordHash, tokenHash, emailVerified, accountStatus, billing state, audit fields, and security flags should not be client-controlled.",
    "The safe mutation pattern is explicit: authorize the action, validate the request, map allowed fields, scope the operation, preserve integrity, and record enough context to investigate later.",
    "Mutations carry high risk because they change data, ownership, authorization state, billing state, and audit history.",
    "Authorization, scoping, validation, and audit evidence still have to be built into the transaction.",
    "Mass assignment is one of the most practical ORM risks."
  ]
};
