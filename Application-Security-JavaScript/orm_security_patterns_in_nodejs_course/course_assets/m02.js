window.COURSE_MODULE = {
  "title": "Data-Access Architecture and Trust Boundaries",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Data-Access Architecture and Trust Boundaries, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "A secure ORM review starts by mapping where data access happens. Identify every place the ORM or query builder is used: route handlers, GraphQL resolvers, service layers, repositories, background workers, scheduled jobs, admin tools, seed scripts, migrations, reporting jobs, and tests. A repository query may look clean by itself, but the value it receives may originate from a public request, a webhook, a queue message, an uploaded file, a configuration value, an admin form, or a previously stored user-controlled record.\n\nExternal values remain security-relevant as they move through the system. Passing through validation, transformation, or a service layer does not automatically make a value trusted for every future use. Map the sources that shape filters, mutations, relation includes, selected fields, projections, raw queries, and export logic. Then identify sensitive models, tables, collections, and fields: users, sessions, roles, permissions, tenant IDs, billing data, audit logs, reset tokens, API keys, private documents, security settings, and operational metadata.\n\nThe useful review question is who can cause which data operation to happen. For each workflow, ask who supplied the value, what model or table it can affect, what authorization has already happened, what database privileges are being used, what fields will return, and how errors are handled. Security review should happen at the workflow level. The route, service, repository, ORM call, database account, response mapping, and deployment configuration all contribute to the final security outcome.",
  "narrationPoints": [
    "A repository query may look clean by itself, but the value it receives may originate from a public request, a webhook, a queue message, an uploaded file, a configuration value, an admin form, or a previously stored user-controlled record.",
    "Then identify sensitive models, tables, collections, and fields: users, sessions, roles, permissions, tenant IDs, billing data, audit logs, reset tokens, API keys, private documents, security settings, and operational metadata.",
    "The route, service, repository, ORM call, database account, response mapping, and deployment configuration all contribute to the final security outcome.",
    "A secure ORM review starts by mapping where data access happens.",
    "Security review should happen at the workflow level.",
    "For each workflow, ask who supplied the value, what model or table it can affect, what authorization has already happened, what database privileges are being used, what fields will return, and how errors are handled."
  ]
};
