window.COURSE_MODULE = {
  "title": "Models, Schemas, Relationships, and Sensitive Fields",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Models, Schemas, Relationships, and Sensitive Fields, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "ORM models and schemas describe how application code understands data. They define fields, relationships, constraints, defaults, indexes, generated query behavior, and sometimes migration behavior. That makes model design security-relevant. Ownership, tenant boundaries, permissions, and sensitive fields should be visible in the model layer rather than buried in assumptions. Fields such as userId, tenantId, ownerId, role, permissions, accountStatus, passwordHash, tokenHash, resetToken, emailVerified, isAdmin, deletedAt, createdBy, updatedBy, and audit fields deserve special attention.\n\nDatabase constraints, relations, required fields, unique indexes, foreign keys, and defaults can support integrity, but they do not replace authorization. A foreign key can show a relationship; it does not prove that the current caller may traverse it. A required tenantId can support tenant scoping; it does not decide which tenant the caller belongs to. A unique email constraint can protect consistency; it does not decide whether an email should be visible in a response.\n\nAvoid exposing internal model structures directly as public API shapes. Generated types can make large create and update objects convenient, but convenience can increase mass assignment risk. Sensitive fields should have explicit rules about who may read them, write them, filter on them, include them, populate them, or return them in responses. Model and schema changes should be reviewed as security-relevant whenever they affect identity, ownership, permissions, tenant boundaries, sensitive data, or audit history.",
  "narrationPoints": [
    "Ownership, tenant boundaries, permissions, and sensitive fields should be visible in the model layer rather than buried in assumptions.",
    "Database constraints, relations, required fields, unique indexes, foreign keys, and defaults can support integrity, but they do not replace authorization.",
    "Model and schema changes should be reviewed as security-relevant whenever they affect identity, ownership, permissions, tenant boundaries, sensitive data, or audit history.",
    "Fields such as userId, tenantId, ownerId, role, permissions, accountStatus, passwordHash, tokenHash, resetToken, emailVerified, isAdmin, deletedAt, createdBy, updatedBy, and audit fields deserve special attention.",
    "Sensitive fields should have explicit rules about who may read them, write them, filter on them, include them, populate them, or return them in responses.",
    "A unique email constraint can protect consistency; it does not decide whether an email should be visible in a response."
  ]
};
