window.COURSE_MODULE = {
  "title": "Course Summary and Practical Checklist",
  "graphicAlt": "Security diagram for ORM Security Patterns in Node.js, Course Summary and Practical Checklist, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Treat the ORM as a data-access boundary between application behavior and the database. Inventory usage across APIs, resolvers, workers, scripts, migrations, seed jobs, admin tools, reports, exports, and CI/CD. Map the inputs that reach each call, the models or tables involved, the authorization checks applied, the database account used, and the fields returned. That workflow-level view is what exposes risk hidden behind tidy repository methods or generated types.\n\nValidate runtime input before mapping it into query objects. Do not pass request objects directly into filters, data objects, update paths, relation includes, selected fields, projections, or raw queries. Prefer normal ORM model operations when they meet the requirement. Treat raw SQL, literal fragments, unsafe query APIs, and dynamic query structure as high-risk review points. Use explicit allowlists for updates, dynamic filters, sorting, relation includes, selected fields, report options, and pagination behavior.\n\nA practical first week plan is direct: search for direct request-object ORM calls, review raw queries, add allowlists for dynamic filters and sorts, verify tenant scoping, restrict update fields, protect database URLs, review migration workflows, reduce sensitive logging, and add tests for unauthorized access and unsafe update attempts. Then make those habits part of pull requests, CI/CD, logging, monitoring, dependency updates, and incident response. Secure ORM usage is not one library choice; it is a repeatable engineering discipline around data access.",
  "narrationPoints": [
    "Map the inputs that reach each call, the models or tables involved, the authorization checks applied, the database account used, and the fields returned.",
    "Validate runtime input before mapping it into query objects.",
    "A practical first week plan is direct: search for direct request-object ORM calls, review raw queries, add allowlists for dynamic filters and sorts, verify tenant scoping, restrict update fields, protect database URLs, review migration workflows, reduce sensitive logging, and add tests for unauthorized access and unsafe update attempts.",
    "Treat raw SQL, literal fragments, unsafe query APIs, and dynamic query structure as high-risk review points.",
    "Then make those habits part of pull requests, CI/CD, logging, monitoring, dependency updates, and incident response.",
    "Treat the ORM as a data-access boundary between application behavior and the database."
  ]
};
