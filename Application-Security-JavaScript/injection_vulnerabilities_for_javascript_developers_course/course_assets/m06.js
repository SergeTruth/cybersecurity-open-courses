window.COURSE_MODULE = {
  "title": "Template, Code, and Expression Injection",
  "graphicAlt": "Blank placeholder graphic for module 6",
  "narration": "Template, code, and expression injection risks appear when untrusted input can influence executable logic rather than plain content. Server-side template engines, expression evaluators, dynamic imports, plugin systems, and JavaScript evaluation features can all create sensitive interpretation points. The core question is whether the user controls data, or whether the user controls something the runtime treats as code or instructions.\n\nJavaScript offers powerful dynamic features, including eval-style behavior and constructors that can treat strings as executable code. These features are rarely appropriate for ordinary user input. Even when they seem convenient for rules, formulas, or customization, they can create a path where untrusted text changes program behavior. Safer designs use explicit configuration, constrained rule formats, or purpose-built parsers with limited capability.\n\nTemplate engines also require care. Rendering user content with a template is different from allowing users to define the template. If users can supply template syntax, expressions, or partials, the application must treat that as a high-risk design and apply strong constraints. Sandbox claims should be reviewed skeptically because sandboxes often have assumptions, escape conditions, or dependency limitations.\n\nDependencies matter because libraries may interpret strings in ways that are not obvious. A validation library, markdown renderer, expression engine, or template helper may have its own rules. Reviewing dependency documentation and configuration is part of injection prevention when the dependency processes user-controlled strings.\n\nThe safest pattern is separation. User content should remain content. Application logic should remain application-owned. If customization is required, expose a narrow, documented set of options rather than a general-purpose language. That design choice prevents untrusted users from turning configuration into code.",
  "narrationPoints": [
    "Template, code, and expression injection risks appear when untrusted input can influence executable logic rather than plain content.",
    "JavaScript offers powerful dynamic features, including eval-style behavior and constructors that can treat strings as executable code.",
    "Template engines also require care.",
    "Dependencies matter because libraries may interpret strings in ways that are not obvious.",
    "The safest pattern is separation."
  ]
};
