window.COURSE_MODULE = {
  "title": "What Injection Means in JavaScript Applications",
  "graphicAlt": "Blank placeholder graphic for module 1",
  "narration": "Injection happens when untrusted input changes the meaning of something the application sends to an interpreter or downstream system. That something might be a database query, command, document, template, script, URL, header, protocol message, or API request. The security failure is not simply that input was accepted. The failure is that data was allowed to become instructions or structure in a context where it should have remained data.\n\nJavaScript and Node.js applications encounter many interpreters. A backend may build SQL queries, NoSQL queries, shell commands, template output, JSON documents, URLs, HTTP headers, and messages to downstream APIs. A frontend may render HTML, set attributes, build URLs, update the DOM, parse JSON, and interact with browser security rules. Each context has its own rules for what characters, types, structures, and APIs mean.\n\nThis is why injection prevention is context-specific. Input validation checks whether data has the shape the application expects. Parameterization keeps values separate from query structure. Output encoding makes data safe for a specific rendering context. Escaping may be useful only when it matches the exact interpreter. Safe API design avoids string-built instructions when possible. These controls are related, but they are not interchangeable.\n\nA common mistake is trying to memorize dangerous inputs instead of understanding sensitive sinks. The safer mental model is to ask where untrusted data enters, where it flows, and where it is interpreted. If the data reaches a database, shell, template engine, browser, file path, URL, header, or external API, the developer must choose a control appropriate to that destination.\n\nThe goal is simple to state and demanding to implement: keep data as data. JavaScript developers prevent injection by designing application boundaries so untrusted input cannot change command meaning, query structure, document structure, or executable logic.",
  "narrationPoints": [
    "Injection happens when untrusted input changes the meaning of something the application sends to an interpreter or downstream system.",
    "JavaScript and Node.js applications encounter many interpreters.",
    "This is why injection prevention is context-specific.",
    "A common mistake is trying to memorize dangerous inputs instead of understanding sensitive sinks.",
    "The goal is simple to state and demanding to implement: keep data as data."
  ]
};
