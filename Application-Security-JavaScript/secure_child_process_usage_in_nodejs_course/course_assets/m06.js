window.COURSE_MODULE = {
  "title": "Environment, cwd, PATH, and Secret Exposure",
  "graphicAlt": "Two-path comparison for Environment, cwd, PATH, and Secret Exposure in Secure Child Process Usage in Node.js: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "The execution context can be as important as the command itself. A child process may inherit environment variables from the parent process unless the application controls what is passed. Those variables can contain database URLs, API keys, cloud credentials, OAuth secrets, signing keys, session secrets, proxy settings, feature flags, npm tokens, and deployment configuration. A conversion tool or scanner usually does not need production database credentials simply because the parent service has them.\n\nPass a minimal environment to child processes when practical. Set the working directory intentionally. Prefer trusted executable locations and avoid ambiguous PATH lookup for sensitive workflows. Avoid using untrusted input to choose cwd, executable paths, environment names, environment values, output paths, or temporary directories. A child process that runs from an unexpected working directory may read different files, create output in the wrong location, or execute a different helper than the reviewer expected.\n\nSeparate build-time and production runtime behavior. A CI job may have package publishing tokens, signing keys, cloud deployment credentials, or access to private registries. A production service may have customer data and database access. Both contexts can start child processes, but the secrets and privileges available to them should be different. Logs should never print inherited environment variables, full command lines containing secrets, or sensitive configuration.",
  "narrationPoints": [
      "Those variables can contain database URLs, API keys, cloud credentials, OAuth secrets, signing keys, session secrets, proxy settings, feature flags, npm tokens, and deployment configuration.",
      "Avoid using untrusted input to choose cwd, executable paths, environment names, environment values, output paths, or temporary directories.",
      "Logs should never print inherited environment variables, full command lines containing secrets, or sensitive configuration.",
      "A child process may inherit environment variables from the parent process unless the application controls what is passed.",
      "A child process that runs from an unexpected working directory may read different files, create output in the wrong location, or execute a different helper than the reviewer expected.",
      "A CI job may have package publishing tokens, signing keys, cloud deployment credentials, or access to private registries."
  ]
};
