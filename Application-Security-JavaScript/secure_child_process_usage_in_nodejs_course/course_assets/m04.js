window.COURSE_MODULE = {
  "title": "Shell Use, Command Injection, and Safer Alternatives",
  "graphicAlt": "Layered architecture view of Shell Use, Command Injection, and Safer Alternatives for Secure Child Process Usage in Node.js, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "Many risky shell calls exist because they were convenient, not because the shell was truly required. Shell features such as pipes, redirects, globbing, expansion, quoting, and command chaining add parsing complexity. When a feature only needs to resize an image, generate a report, call a scanner, or invoke a known tool, the team should first ask whether a Node.js library, platform API, service API, or direct executable call can meet the need without shell interpretation.\n\nDo not build shell command strings from user-controlled or externally influenced input. Treat values from users, databases, queues, uploaded filenames, webhooks, configuration, and admin forms as untrusted until validated for a specific purpose. The application should choose the command. The user should not. When execution is necessary, prefer a fixed executable path or trusted executable name with arguments passed as separate structured values.\n\nEscaping is fragile and platform-specific, so it should not be the primary defense. Quoting rules vary across shells and operating systems, and a small assumption change can reopen risk. If shell use is truly required, document why it is required, restrict who can trigger it, minimize inputs, isolate the execution environment, and review the design carefully. A healthy default preference is no external process first, no shell second, and direct structured execution when a child process is justified.",
  "narrationPoints": [
      "When a feature only needs to resize an image, generate a report, call a scanner, or invoke a known tool, the team should first ask whether a Node.js library, platform API, service API, or direct executable call can meet the need without shell interpretation.",
      "Treat values from users, databases, queues, uploaded filenames, webhooks, configuration, and admin forms as untrusted until validated for a specific purpose.",
      "If shell use is truly required, document why it is required, restrict who can trigger it, minimize inputs, isolate the execution environment, and review the design carefully.",
      "A healthy default preference is no external process first, no shell second, and direct structured execution when a child process is justified.",
      "Do not build shell command strings from user-controlled or externally influenced input.",
      "Many risky shell calls exist because they were convenient, not because the shell was truly required."
  ]
};
