window.COURSE_MODULE = {
  "title": "Errors, Logging, Secrets, and Safe Responses",
  "graphicAlt": "Control-and-evidence diagram for Errors, Logging, Secrets, and Safe Responses in SQL Injection Prevention in Node.js, linking each security decision to the protected asset, the enforcing component, the failure state, and the observable verification signal.",
  "narration": "Error handling is part of SQL injection prevention because diagnostic detail can expose how the application and database are built. Production responses should not reveal raw SQL, stack traces, table names, column names, connection strings, database hostnames, file paths, or internal implementation details. External callers need a safe error message and a useful correlation ID. Developers and operators need enough internal detail to investigate, but that detail belongs in protected logs, not in public API responses.\n\nLogs should be useful without becoming a second data leak. Avoid logging full SQL statements when they contain sensitive values. Avoid database passwords, connection strings, tokens, cookies, authorization headers, and full request payloads. Prefer request IDs, route names, authenticated user or service identity where appropriate, query category, error category, timing, validation result, and safe metadata. The goal is evidence for defenders without unnecessary exposure of secrets or regulated data.\n\nMonitoring can help identify suspicious query behavior and broken assumptions. Watch for repeated validation failures, repeated query errors, unusual report volume, unexpected authorization failures, and changes in query latency or result volume. Treat development and production diagnostics differently. Verbose errors can be useful during local development, but production should be deliberate, sanitized, and observable. Good logging makes incident response faster without giving an attacker a map of the database.",
  "narrationPoints": [
      "Production responses should not reveal raw SQL, stack traces, table names, column names, connection strings, database hostnames, file paths, or internal implementation details.",
      "Avoid database passwords, connection strings, tokens, cookies, authorization headers, and full request payloads.",
      "Watch for repeated validation failures, repeated query errors, unusual report volume, unexpected authorization failures, and changes in query latency or result volume.",
      "Developers and operators need enough internal detail to investigate, but that detail belongs in protected logs, not in public API responses.",
      "Prefer request IDs, route names, authenticated user or service identity where appropriate, query category, error category, timing, validation result, and safe metadata.",
      "External callers need a safe error message and a useful correlation ID."
  ]
};
