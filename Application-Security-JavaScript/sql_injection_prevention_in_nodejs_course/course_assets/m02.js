window.COURSE_MODULE = {
  "title": "Query Data Flow and Trust Boundaries",
  "graphicAlt": "Control-and-evidence diagram for Query Data Flow and Trust Boundaries in SQL Injection Prevention in Node.js, linking each security decision to the protected asset, the enforcing component, the failure state, and the observable verification signal.",
  "narration": "Finding SQL injection risk is really a data-flow exercise. Start by identifying every place the Node.js application sends SQL to a database, whether directly through a driver or indirectly through repositories, helpers, ORMs, query builders, migrations, reporting jobs, and admin tooling. Then trace the values that reach those calls. Route parameters, query strings, request bodies, headers, cookies, uploaded files, stored records, queue messages, webhooks, command-line arguments, and configuration can all influence queries.\n\nA value should not become trusted merely because it passed through another layer first. Data that arrived from a customer yesterday and was stored in a database can still be untrusted when it is used to build a report today. A message from an internal queue may still contain partner-controlled data. A support tool may be internal but still accepts human input. The review question is not only where the value is now, but who originally controlled it and what it is allowed to represent.\n\nPay special attention to authentication, authorization, account lookup, tenant filtering, billing, exports, reports, search, administrative actions, and deletion paths. These are query paths where a mistake has high impact. For each path, ask four questions: who supplied this value, does it become data or query structure, which database account runs the query, and what happens if the value is unexpected. That map gives reviewers a practical way to prioritize instead of searching blindly for one suspicious string.",
  "narrationPoints": [
      "Route parameters, query strings, request bodies, headers, cookies, uploaded files, stored records, queue messages, webhooks, command-line arguments, and configuration can all influence queries.",
      "A message from an internal queue may still contain partner-controlled data.",
      "For each path, ask four questions: who supplied this value, does it become data or query structure, which database account runs the query, and what happens if the value is unexpected.",
      "Pay special attention to authentication, authorization, account lookup, tenant filtering, billing, exports, reports, search, administrative actions, and deletion paths.",
      "Finding SQL injection risk is really a data-flow exercise.",
      "Start by identifying every place the Node.js application sends SQL to a database, whether directly through a driver or indirectly through repositories, helpers, ORMs, query builders, migrations, reporting jobs, and admin tooling."
  ]
};
