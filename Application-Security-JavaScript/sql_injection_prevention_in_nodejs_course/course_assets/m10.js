window.COURSE_MODULE = {
  "title": "Course Summary and Practical Checklist",
  "graphicAlt": "Layered architecture view of Course Summary and Practical Checklist for SQL Injection Prevention in Node.js, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "SQL injection prevention is the discipline of keeping query structure under application control and keeping untrusted values separate from that structure. Start by inventorying every place Node.js code sends SQL to a database, including direct driver calls, ORM raw queries, query builders, migrations, reports, exports, admin tools, and background jobs. Then trace the values that reach those queries and decide whether each value is data, a business choice, or attempted query structure.\n\nUse parameterized queries or prepared statements for ordinary values. Do not concatenate request values into SQL strings or insert them through template literals. Treat ORM raw query APIs and dynamic fragments as high-risk review points. Use allowlists for identifiers, sort directions, operators, report types, table choices, and filter modes. Validate input by business rules, but do not rely on validation or escaping as the only protection when parameter binding is available.\n\nA practical first week plan is straightforward: search for raw SQL and template-built queries, verify parameter binding, review ORM escape hatches, centralize dynamic sort and filter mappings, check runtime database privileges, reduce verbose production errors, and add tests around query boundaries. Then make those checks part of code review and CI/CD. The goal is not one heroic audit; it is a repeatable pattern that keeps database access safe as the application evolves.",
  "narrationPoints": [
      "SQL injection prevention is the discipline of keeping query structure under application control and keeping untrusted values separate from that structure.",
      "Validate input by business rules, but do not rely on validation or escaping as the only protection when parameter binding is available.",
      "A practical first week plan is straightforward: search for raw SQL and template-built queries, verify parameter binding, review ORM escape hatches, centralize dynamic sort and filter mappings, check runtime database privileges, reduce verbose production errors, and add tests around query boundaries.",
      "Treat ORM raw query APIs and dynamic fragments as high-risk review points.",
      "Start by inventorying every place Node.js code sends SQL to a database, including direct driver calls, ORM raw queries, query builders, migrations, reports, exports, admin tools, and background jobs.",
      "Then trace the values that reach those queries and decide whether each value is data, a business choice, or attempted query structure."
  ]
};
