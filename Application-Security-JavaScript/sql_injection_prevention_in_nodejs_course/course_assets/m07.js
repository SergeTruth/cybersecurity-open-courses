window.COURSE_MODULE = {
  "title": "Database Privileges, Stored Procedures, and Defense in Depth",
  "graphicAlt": "Lifecycle sequence for Database Privileges, Stored Procedures, and Defense in Depth in SQL Injection Prevention in Node.js, tracing creation, validation, use, failure handling, cleanup, and verification while highlighting the component responsible at every transition.",
  "narration": "Application code should prevent injection, but the database should still be designed to reduce impact if a query path fails. Runtime database accounts should use least privilege. Where practical, separate read, write, reporting, migration, and administrative privileges. Avoid giving a public-facing application account broad schema modification or administrative rights unless there is a clear and reviewed requirement. The fewer powers the account has, the smaller the blast radius of a mistake.\n\nDefense in depth can also include views, row-level controls, tenant-aware constraints, scoped accounts, database permissions, and carefully designed stored procedures. These controls can make unsafe outcomes harder, but they do not excuse unsafe query construction. A tenant filter in application code still needs review. A reporting account still needs appropriate scope. A migration credential should not be reused by the runtime API because it has more power than the application normally needs.\n\nStored procedures deserve nuance. They can centralize access and accept parameters in a controlled way, which can be helpful. They are not automatically safe if the procedure concatenates input into dynamic SQL internally. Review the procedure body, not only the Node.js call site. The same rule applies across layers: bind data as data, control structure deliberately, protect credentials, and ensure the database account is only powerful enough for the work it is supposed to perform.",
  "narrationPoints": [
      "Application code should prevent injection, but the database should still be designed to reduce impact if a query path fails.",
      "Defense in depth can also include views, row-level controls, tenant-aware constraints, scoped accounts, database permissions, and carefully designed stored procedures.",
      "The same rule applies across layers: bind data as data, control structure deliberately, protect credentials, and ensure the database account is only powerful enough for the work it is supposed to perform.",
      "A migration credential should not be reused by the runtime API because it has more power than the application normally needs.",
      "Where practical, separate read, write, reporting, migration, and administrative privileges.",
      "Avoid giving a public-facing application account broad schema modification or administrative rights unless there is a clear and reviewed requirement."
  ]
};
