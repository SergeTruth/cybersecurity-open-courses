window.COURSE_MODULE = {
  "title": "Why Path Traversal Prevention Matters",
  "graphicAlt": "Security diagram for Path Traversal Prevention in Node.js, Why Path Traversal Prevention Matters, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "A path traversal issue is a file-access control failure. It happens when input can cause an application to read, write, stream, download, delete, or otherwise operate on a file outside the directory or storage boundary the developer intended. The vulnerability is not using files. The vulnerability is letting untrusted input influence which file the application touches without a strong design boundary, an authorization decision, and a reliable check that the final target is allowed.\n\nNode.js applications often create file paths in ordinary features: report downloads, attachment downloads, static file serving, image previews, template selection, import and export jobs, log retrieval, backup access, upload handling, archive extraction, generated files, and object storage key construction. A route that begins as download a report by filename can become a pattern copied into admin tools, exports, previews, and background workers. Once that pattern spreads, the risk becomes harder to see and harder to fix consistently.\n\nThe consequences can be serious because file operations often sit close to sensitive data. Unsafe path handling can expose source code, configuration, secrets, logs, customer files, tenant data, internal reports, or files intended only for background processing. The goal of prevention is to make file access intentional, bounded, authorized, least-privileged, and testable. This course keeps the focus defensive: how to design file workflows so callers choose approved resources, not arbitrary paths.",
  "narrationPoints": [
    "A path traversal issue is a file-access control failure.",
    "js applications often create file paths in ordinary features: report downloads, attachment downloads, static file serving, image previews, template selection, import and export jobs, log retrieval, backup access, upload handling, archive extraction, generated files, and object storage key construction.",
    "Unsafe path handling can expose source code, configuration, secrets, logs, customer files, tenant data, internal reports, or files intended only for background processing.",
    "The vulnerability is letting untrusted input influence which file the application touches without a strong design boundary, an authorization decision, and a reliable check that the final target is allowed.",
    "The goal of prevention is to make file access intentional, bounded, authorized, least-privileged, and testable.",
    "This course keeps the focus defensive: how to design file workflows so callers choose approved resources, not arbitrary paths."
  ]
};
