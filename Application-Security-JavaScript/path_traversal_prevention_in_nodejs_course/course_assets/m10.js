window.COURSE_MODULE = {
  "title": "Course Summary and Practical Checklist",
  "graphicAlt": "Security diagram for Path Traversal Prevention in Node.js, Course Summary and Practical Checklist, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Path traversal prevention in Node.js starts with inventory. Find every feature that reads, writes, serves, lists, deletes, extracts, transforms, or generates files. Treat route parameters, query values, request bodies, uploaded filenames, archive entries, database values, queue messages, webhook payloads, and object keys as untrusted unless their origin and integrity are known. Avoid accepting raw user-controlled paths when an identifier, manifest lookup, or allowlisted business choice will work.\n\nWhen dynamic file selection is necessary, use fixed storage boundaries and verify that resolved candidates remain inside the intended base directory or storage scope. Keep public, private, temporary, generated, log, configuration, and secret storage separated. Use generated storage names and treat original filenames as display metadata. Enforce object-level authorization before private downloads or file operations, and remember that path safety and authorization solve different problems.\n\nAccount for implementation realities. Symlinks, race conditions, platform differences, container mounts, shared volumes, object storage behavior, and serverless temporary storage can affect path safety. Run with least privilege and narrow filesystem permissions so a missed check has less reach. Avoid broad writable directories, privileged containers, public web roots for untrusted files, and production write access to source or dependency directories. Safe response behavior and safe logging are part of the control set.\n\nA practical first-week plan is concrete: find all filesystem calls, review download routes, replace raw path input with identifiers where possible, add base-directory boundary tests, check static middleware configuration, review upload filename handling, remove sensitive path logging, and document ownership for file storage locations. Over time, make these checks part of pull request review, CI/CD, monitoring, and incident response so prevention is repeatable rather than dependent on memory.",
  "narrationPoints": [
    "Treat route parameters, query values, request bodies, uploaded filenames, archive entries, database values, queue messages, webhook payloads, and object keys as untrusted unless their origin and integrity are known.",
    "Enforce object-level authorization before private downloads or file operations, and remember that path safety and authorization solve different problems.",
    "Avoid broad writable directories, privileged containers, public web roots for untrusted files, and production write access to source or dependency directories.",
    "A practical first-week plan is concrete: find all filesystem calls, review download routes, replace raw path input with identifiers where possible, add base-directory boundary tests, check static middleware configuration, review upload filename handling, remove sensitive path logging, and document ownership for file storage locations.",
    "Over time, make these checks part of pull request review, CI/CD, monitoring, and incident response so prevention is repeatable rather than dependent on memory.",
    "When dynamic file selection is necessary, use fixed storage boundaries and verify that resolved candidates remain inside the intended base directory or storage scope."
  ]
};
