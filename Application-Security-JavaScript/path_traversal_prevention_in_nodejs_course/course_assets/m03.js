window.COURSE_MODULE = {
  "title": "Node.js Path APIs and Their Limits",
  "graphicAlt": "Security diagram for Path Traversal Prevention in Node.js, Node.js Path APIs and Their Limits, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Node.js provides useful path utilities for joining, resolving, normalizing, parsing, and comparing path strings. These APIs are important because they help create consistent path forms for the platform. The security mistake is over-trusting them. path.join, path.resolve, and path.normalize can help process a path, but they do not decide whether the caller is authorized, whether the requested file belongs to the right tenant, or whether the operation is safe for the business workflow.\n\nNormalization can make path strings easier to reason about, but prevention depends on boundary enforcement. The application should choose a known base directory or storage boundary, interpret the candidate path consistently, and verify that the final resolved or canonicalized target remains inside that boundary. That check should occur after relevant decoding and interpretation. URL-to-file conversion, percent-decoding, mixed separators, Unicode confusion, trailing separators, absolute paths, and relative segments can change how input is understood.\n\nPlatform behavior also matters. Windows and POSIX systems differ in drive letters, reserved names, separators, case sensitivity, and path interpretation. Container mounts, network filesystems, object storage abstractions, and serverless environments can introduce their own assumptions. The lesson is not to avoid Node.js path APIs. Use them as part of a defensive design that rejects unsafe input, resolves against known storage, checks the final boundary, and pairs path safety with authorization.",
  "narrationPoints": [
    "normalize can help process a path, but they do not decide whether the caller is authorized, whether the requested file belongs to the right tenant, or whether the operation is safe for the business workflow.",
    "The application should choose a known base directory or storage boundary, interpret the candidate path consistently, and verify that the final resolved or canonicalized target remains inside that boundary.",
    "The lesson is not to avoid Node.",
    "js provides useful path utilities for joining, resolving, normalizing, parsing, and comparing path strings.",
    "Use them as part of a defensive design that rejects unsafe input, resolves against known storage, checks the final boundary, and pairs path safety with authorization.",
    "Normalization can make path strings easier to reason about, but prevention depends on boundary enforcement."
  ]
};
