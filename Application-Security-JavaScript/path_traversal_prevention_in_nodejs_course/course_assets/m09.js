window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Code Review",
  "graphicAlt": "Security diagram for Path Traversal Prevention in Node.js, Testing, Logging, Monitoring, and Code Review, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Path traversal prevention is strongest when unsafe patterns are caught during design and code review, not only after a production alert. Reviewers should search for filesystem operations, path construction, download routes, static routes, upload handling, archive extraction, cleanup jobs, generated reports, and object storage key creation. For each path-like value, ask where it came from, what boundary it should stay inside, and which user or service is authorized for the resulting file.\n\nTests should verify that file access stays inside the intended boundary and that authorization is enforced for the specific object. Cover rejected path-like input, unauthorized access, tenant boundaries, generated names, temporary cleanup, error handling, and platform assumptions where relevant. Static analysis, dependency scanning, secret scanning, and focused security review can support prevention, but they do not replace design review. The most important evidence is that the workflow cannot select files outside its intended domain.\n\nLogging and monitoring should help defenders without leaking sensitive data. Record useful events such as rejected path requests, denied downloads, file access failures, cleanup failures, unusual download volume, and suspicious repeated attempts. Do not log file contents, secrets, tokens, authorization headers, cookies, private signed URLs, full sensitive paths, or unnecessary personal data. Incident response should include exposure review, access log review, secret rotation when needed, storage permission review, and cleanup of exposed links or artifacts.",
  "narrationPoints": [
    "Path traversal prevention is strongest when unsafe patterns are caught during design and code review, not only after a production alert.",
    "Static analysis, dependency scanning, secret scanning, and focused security review can support prevention, but they do not replace design review.",
    "Incident response should include exposure review, access log review, secret rotation when needed, storage permission review, and cleanup of exposed links or artifacts.",
    "Logging and monitoring should help defenders without leaking sensitive data.",
    "Tests should verify that file access stays inside the intended boundary and that authorization is enforced for the specific object.",
    "Do not log file contents, secrets, tokens, authorization headers, cookies, private signed URLs, full sensitive paths, or unnecessary personal data."
  ]
};
