window.COURSE_MODULE = {
  "title": "Why Math.random Is Not for Security",
  "graphicAlt": "Security diagram for Secure Random Numbers in Node.js, Why Math.random Is Not for Security, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Math.random is familiar, convenient, and widely used in JavaScript. That does not make it suitable for security. It is designed for ordinary pseudorandom behavior, not for values that must resist prediction by an attacker. It may be fine for UI variation, sampling, simulations, non-security games, demos, or randomized test data. The problem begins when that familiar convenience crosses into authentication, account recovery, authorization, or cryptography.\n\nA password reset token, session ID, API key, invite link, CSRF token, OAuth state value, authorization code, encryption key, nonce, salt, or IV has security meaning. If an attacker can predict or narrow the possible outputs, the application may fail even when every surrounding feature looks well designed. Attackers care about guessability and repeatability, not whether the output looks random in a browser console.\n\nCode review should flag Math.random anywhere near secrets, tokens, crypto, file access, account recovery, authorization decisions, or identifiers that protect access to private data. Reviewers should also watch for custom random generators, timestamp-based tokens, counter-based secrets, predictable seeds, short numeric codes without controls, and string slicing patterns that simply make an output look token-shaped.\n\nReplacing Math.random with a crypto API is necessary for secrets, but it is not the whole fix. The generated value still needs enough entropy, the right encoding, expiration, revocation where appropriate, safe comparison, protected storage, and log redaction. The practical rule is simple: use ordinary randomness for ordinary behavior, and use cryptographically secure randomness for security-sensitive values.",
  "narrationPoints": [
    "It may be fine for UI variation, sampling, simulations, non-security games, demos, or randomized test data.",
    "A password reset token, session ID, API key, invite link, CSRF token, OAuth state value, authorization code, encryption key, nonce, salt, or IV has security meaning.",
    "random anywhere near secrets, tokens, crypto, file access, account recovery, authorization decisions, or identifiers that protect access to private data.",
    "The generated value still needs enough entropy, the right encoding, expiration, revocation where appropriate, safe comparison, protected storage, and log redaction.",
    "Reviewers should also watch for custom random generators, timestamp-based tokens, counter-based secrets, predictable seeds, short numeric codes without controls, and string slicing patterns that simply make an output look token-shaped.",
    "random with a crypto API is necessary for secrets, but it is not the whole fix."
  ]
};
