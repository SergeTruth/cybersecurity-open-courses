window.COURSE_MODULE = {
  "title": "Node.js Randomness APIs",
  "graphicAlt": "Security diagram for Secure Random Numbers in Node.js, Node.js Randomness APIs, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Node.js gives developers several APIs for cryptographically strong random data. The most common server-side starting point is the node:crypto module. It exposes functions for random bytes, random integers, filling buffers, and generating random UUIDs. Modern Node.js runtimes also expose Web Crypto APIs that can be useful for standards-aligned or cross-platform patterns. The right choice depends on what kind of value the application needs.\n\ncrypto.randomBytes is a flexible tool for raw random bytes. It is commonly used for tokens, keys, salts, and other values that need strong unpredictability. Raw bytes are not automatically ready for every context. They usually need encoding before they become a URL parameter, cookie value, database string, header value, or displayed recovery code. Hex and base64url are common choices, but the encoding should be chosen deliberately for the path the value will travel.\n\ncrypto.randomInt is useful when the application needs a random integer inside a bounded range. It avoids common range-mapping mistakes that appear when developers take random bytes, use modulo, multiply floating point values, or slice strings until the result looks right. crypto.randomUUID is useful for UUID-shaped identifiers when that format fits the use case, especially for public or internal identifiers that need low collision risk.\n\nAn API being cryptographically strong does not complete the design by itself. A reset token still needs expiration and safe storage. An API key still needs ownership and revocation. A nonce still needs algorithm-specific non-reuse rules. A UUID still needs authorization checks if it identifies private data. The API creates a strong value; the application still has to handle that value safely.",
  "narrationPoints": [
    "js gives developers several APIs for cryptographically strong random data.",
    "It is commonly used for tokens, keys, salts, and other values that need strong unpredictability.",
    "It avoids common range-mapping mistakes that appear when developers take random bytes, use modulo, multiply floating point values, or slice strings until the result looks right.",
    "A reset token still needs expiration and safe storage.",
    "Hex and base64url are common choices, but the encoding should be chosen deliberately for the path the value will travel.",
    "They usually need encoding before they become a URL parameter, cookie value, database string, header value, or displayed recovery code."
  ]
};
