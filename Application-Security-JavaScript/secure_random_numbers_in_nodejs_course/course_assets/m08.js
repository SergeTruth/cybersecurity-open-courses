window.COURSE_MODULE = {
  "title": "UUIDs, Public IDs, and Secret Identifiers",
  "graphicAlt": "Security diagram for Secure Random Numbers in Node.js, UUIDs, Public IDs, and Secret Identifiers, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "UUIDs are useful in Node.js systems because they provide a convenient identifier format with very low collision probability when generated appropriately. They can work well for database identifiers, object identifiers, correlation IDs, distributed systems, and filenames where uniqueness matters. A random UUID can be a good public identifier, but it is not automatically the right answer for every secret or security boundary.\n\nPublic identifiers and bearer secrets should be designed differently. A public ID may appear in URLs, logs, customer support cases, exports, API responses, and analytics events. It may reveal relationships, volume, timing, customer activity, or cross-system correlation even when it is not secret. A bearer secret, by contrast, grants access or proves possession. It needs stronger handling, redaction, expiration, and revocation expectations.\n\nHard to guess is not the same thing as authorized. A private document URL with a UUID still needs a server-side access check. An invoice identifier, tenant object ID, image filename, case number, or download link should not become the only control protecting private data. Random identifiers may reduce casual enumeration, but they do not replace authentication, authorization, rate limiting, auditing, or access-control decisions close to the resource.\n\nIdentifier format choices also affect operations. Teams should consider routing, database indexing, customer support, privacy, log volume, incident response, and whether IDs move between systems. A useful review question is: if this identifier appears in a log, URL, ticket, or browser history, what does it reveal and what can someone do with it? That question separates public IDs from secrets and prevents UUIDs from being treated as magic security dust.",
  "narrationPoints": [
    "A random UUID can be a good public identifier, but it is not automatically the right answer for every secret or security boundary.",
    "Public identifiers and bearer secrets should be designed differently.",
    "Random identifiers may reduce casual enumeration, but they do not replace authentication, authorization, rate limiting, auditing, or access-control decisions close to the resource.",
    "That question separates public IDs from secrets and prevents UUIDs from being treated as magic security dust.",
    "Teams should consider routing, database indexing, customer support, privacy, log volume, incident response, and whether IDs move between systems.",
    "They can work well for database identifiers, object identifiers, correlation IDs, distributed systems, and filenames where uniqueness matters."
  ]
};
