window.COURSE_MODULE = {
  "title": "Tokens, Sessions, and One-Time Links",
  "graphicAlt": "Security diagram for Secure Random Numbers in Node.js, Tokens, Sessions, and One-Time Links, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Tokens are where random generation becomes application behavior. Session IDs, reset tokens, magic links, invite links, API keys, email verification tokens, OAuth state values, and CSRF tokens all need purpose-specific design. Some are short lived. Some are single use. Some identify a browser session. Some authorize an integration. Some only prevent request confusion. The generation step should match the purpose and the lifetime.\n\nA good token has enough entropy for its threat model. A token that expires in ten minutes and is protected by rate limiting may have different requirements than a long-lived API key. Encoding also matters. Values placed in URLs, cookies, headers, email links, or database columns need representations that survive those contexts safely. Base64url and hex are common, but the team should know why a format was chosen and whether it leaks through referrers or logs.\n\nSensitive tokens should be stored carefully. For many reset-token and verification-token workflows, storing a digest of the token can reduce exposure if the database is read. The application compares a submitted token against the stored verifier, expires it after use or after a short time window, and revokes it when account state changes. API keys and session identifiers need ownership, rotation, revocation, and monitoring rather than permanent silent acceptance.\n\nA random token does not prove authorization by itself. The application must still check account state, purpose, scope, tenant boundaries, session status, and whether the action is still allowed. The token also must not appear in logs, screenshots, support tickets, analytics events, client-visible errors, or full authorization headers. Token security is a full lifecycle: generate, encode, deliver, store, compare, expire, revoke, redact, monitor, and respond to exposure.",
  "narrationPoints": [
    "Session IDs, reset tokens, magic links, invite links, API keys, email verification tokens, OAuth state values, and CSRF tokens all need purpose-specific design.",
    "A token that expires in ten minutes and is protected by rate limiting may have different requirements than a long-lived API key.",
    "API keys and session identifiers need ownership, rotation, revocation, and monitoring rather than permanent silent acceptance.",
    "The token also must not appear in logs, screenshots, support tickets, analytics events, client-visible errors, or full authorization headers.",
    "For many reset-token and verification-token workflows, storing a digest of the token can reduce exposure if the database is read.",
    "Values placed in URLs, cookies, headers, email links, or database columns need representations that survive those contexts safely."
  ]
};
