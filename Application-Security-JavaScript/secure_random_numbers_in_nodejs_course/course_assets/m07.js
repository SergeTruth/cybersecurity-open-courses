window.COURSE_MODULE = {
  "title": "Salts, Nonces, IVs, and Algorithm Requirements",
  "graphicAlt": "Security diagram for Secure Random Numbers in Node.js, Salts, Nonces, IVs, and Algorithm Requirements, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Salts, nonces, and IVs are easy to confuse because they often look like random bytes near cryptographic code. They are not interchangeable. A salt, a password reset token, an AES-GCM nonce, an initialization vector, and a UUID may all be generated by a random API, but they serve different purposes and have different failure modes. The safest developer habit is to read and follow the requirements of the specific algorithm or library being used.\n\nA salt usually does not need to be secret. Its job is often to make each password verifier or derivation instance unique so identical inputs do not produce identical stored values. A salt must be generated with the right size and stored with the verifier or derived output so verification can happen later. Hiding the salt is not the primary control; uniqueness and correct pairing are the primary concerns.\n\nA nonce often means number used once, and for some algorithms reuse can be dangerous or catastrophic. An IV may need uniqueness, unpredictability, or both depending on the algorithm and mode. Some modes tolerate a predictable counter if it is never reused under a key; others require random IVs. Developers should not hardcode IVs, derive them casually from timestamps or filenames, or reuse them across contexts without a reviewed design.\n\nSalts, nonces, IVs, authentication tags, and ciphertext often need to be stored or transmitted together in a structured format. They are often not secret, but they must be correct and paired with the right data and key. Centralizing cryptographic code helps prevent one route handler from inventing a nonce strategy while another copies an old example. Random-looking values in crypto code deserve explicit purpose, size, storage, and reuse rules.",
  "narrationPoints": [
    "Salts, nonces, and IVs are easy to confuse because they often look like random bytes near cryptographic code.",
    "A salt must be generated with the right size and stored with the verifier or derived output so verification can happen later.",
    "Some modes tolerate a predictable counter if it is never reused under a key; others require random IVs.",
    "Salts, nonces, IVs, authentication tags, and ciphertext often need to be stored or transmitted together in a structured format.",
    "An IV may need uniqueness, unpredictability, or both depending on the algorithm and mode.",
    "Developers should not hardcode IVs, derive them casually from timestamps or filenames, or reuse them across contexts without a reviewed design."
  ]
};
