window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Code Review",
  "graphicAlt": "Trust-boundary map for Testing, Logging, Monitoring, and Code Review in Safe File Handling in Node.js, with distinct caller, application, platform, and external-service zones connected only through labeled validation and authorization checkpoints.",
  "narration": "Safe file handling should be visible in pull requests, tests, logs, dashboards, and incident runbooks. Code review should identify filesystem APIs, path construction, storage locations, permissions, file type decisions, temporary file use, cleanup behavior, download routes, and object-store access. Reviewers should be able to answer where files can be read from, where files can be written, who can retrieve them, how limits are enforced, and what happens when something fails.\n\nTests should cover allowed files, rejected paths, boundary enforcement, unauthorized access, large files, cleanup paths, failed operations, and concurrent behavior where relevant. Include cases where operations fail halfway through, where a user is authenticated but not authorized for a specific file, and where generated files expire or are deleted. CI/CD can also include dependency checks, secret scanning, relevant static analysis, and review gates for changes that affect storage, downloads, or permissions.\n\nLogs and monitoring should provide evidence without creating new exposure. Log useful events such as file created, read, served, rejected, deleted, expired, quarantined, or failed. Avoid logging full sensitive paths, secrets, tokens, authorization headers, cookies, private URLs, file contents, or unnecessary personal data. Monitor unusual download volume, repeated path rejections, storage growth, cleanup failures, processing failures, and authorization denials. Incident response should include suspicious access, leaked paths, storage misconfiguration, and cleanup failures.",
  "narrationPoints": [
      "Code review should identify filesystem APIs, path construction, storage locations, permissions, file type decisions, temporary file use, cleanup behavior, download routes, and object-store access.",
      "Tests should cover allowed files, rejected paths, boundary enforcement, unauthorized access, large files, cleanup paths, failed operations, and concurrent behavior where relevant.",
      "Avoid logging full sensitive paths, secrets, tokens, authorization headers, cookies, private URLs, file contents, or unnecessary personal data.",
      "Reviewers should be able to answer where files can be read from, where files can be written, who can retrieve them, how limits are enforced, and what happens when something fails.",
      "Logs and monitoring should provide evidence without creating new exposure.",
      "CI/CD can also include dependency checks, secret scanning, relevant static analysis, and review gates for changes that affect storage, downloads, or permissions."
  ]
};
