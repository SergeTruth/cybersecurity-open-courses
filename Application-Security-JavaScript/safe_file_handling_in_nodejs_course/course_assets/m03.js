window.COURSE_MODULE = {
  "title": "Paths, Normalization, and Traversal Boundaries",
  "graphicAlt": "Trust-boundary map for Paths, Normalization, and Traversal Boundaries in Safe File Handling in Node.js, with distinct caller, application, platform, and external-service zones connected only through labeled validation and authorization checkpoints.",
  "narration": "The core defensive idea is simple: the application should decide where files may live, and user input should not be allowed to move an operation outside that boundary. Treat user-controlled path segments as untrusted input. Avoid passing user-supplied paths directly to filesystem APIs. When possible, use application-controlled identifiers mapped to known files instead of accepting arbitrary paths from requests, forms, queue messages, or metadata fields.\n\nWhen a file must be selected dynamically, use a fixed base directory or storage boundary. Resolve or canonicalize the candidate path and confirm that it remains inside the intended base location before performing the operation. This boundary check should be paired with authorization and storage isolation. Normalization alone is not a complete security control. path.join, path.resolve, and path.normalize can help process paths, but they do not decide whether the requested operation should be allowed.\n\nPath handling also has platform details. Be careful with absolute paths, dot-dot segments, mixed separators, URL encoding, Unicode confusion, Windows drive behavior, reserved names, case sensitivity, and values that look harmless in one environment but resolve differently in another. Keep examples and tests high-level and defensive: verify that the resolved path stays inside the approved location, verify that the user is authorized for the file, and verify that storage layout prevents one bug from reaching unrelated data.",
  "narrationPoints": [
      "When possible, use application-controlled identifiers mapped to known files instead of accepting arbitrary paths from requests, forms, queue messages, or metadata fields.",
      "Normalization alone is not a complete security control. path.join, path.resolve, and path.normalize can help process paths, but they do not decide whether the requested operation should be allowed.",
      "Keep examples and tests high-level and defensive: verify that the resolved path stays inside the approved location, verify that the user is authorized for the file, and verify that storage layout prevents one bug from reaching unrelated data.",
      "Avoid passing user-supplied paths directly to filesystem APIs.",
      "The core defensive idea is simple: the application should decide where files may live, and user input should not be allowed to move an operation outside that boundary.",
      "Treat user-controlled path segments as untrusted input."
  ]
};
