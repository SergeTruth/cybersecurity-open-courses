window.COURSE_MODULE = {
  "title": "Permissions, Storage Isolation, and Secrets",
  "graphicAlt": "Control-and-evidence diagram for Permissions, Storage Isolation, and Secrets in Safe File Handling in Node.js, linking each security decision to the protected asset, the enforcing component, the failure state, and the observable verification signal.",
  "narration": "Storage layout is a security control. If uploaded files, logs, generated reports, public assets, configuration, and secrets all live near each other with broad permissions, a small file-handling bug can become a larger exposure. Separate read-only application code from writable runtime data. Keep secrets, configuration, logs, uploaded files, generated files, temporary files, cache entries, and public assets in clearly separated locations with permissions that match their purpose.\n\nThe Node.js process should run with the least privilege needed for its file operations. Production applications usually should not require write access to source code, dependency directories, or broad host paths. Be cautious with broad filesystem permissions, broad container mounts, shared volumes, and host directories that expose unrelated data. In Docker, serverless, virtual machine, and shared-host deployments, file permissions need to support the deployment model without giving the application more reach than it needs.\n\nSecrets deserve special attention. Do not store secrets in publicly served directories, uploaded file areas, logs, generated reports, temporary artifacts, or cached export files. Storage access should be reviewed alongside application authorization. A user may have permission to generate a report without having permission to read the raw files used to create it. A worker may need write access to one output directory without needing read access to secrets or tenant data outside that workflow.",
  "narrationPoints": [
      "Keep secrets, configuration, logs, uploaded files, generated files, temporary files, cache entries, and public assets in clearly separated locations with permissions that match their purpose.",
      "The Node.js process should run with the least privilege needed for its file operations.",
      "A worker may need write access to one output directory without needing read access to secrets or tenant data outside that workflow.",
      "If uploaded files, logs, generated reports, public assets, configuration, and secrets all live near each other with broad permissions, a small file-handling bug can become a larger exposure.",
      "In Docker, serverless, virtual machine, and shared-host deployments, file permissions need to support the deployment model without giving the application more reach than it needs.",
      "Production applications usually should not require write access to source code, dependency directories, or broad host paths."
  ]
};
