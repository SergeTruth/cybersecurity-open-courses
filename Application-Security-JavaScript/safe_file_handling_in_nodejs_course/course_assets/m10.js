window.COURSE_MODULE = {
  "title": "Course Summary and Practical Checklist",
  "graphicAlt": "Two-path comparison for Course Summary and Practical Checklist in Safe File Handling in Node.js: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "Safe file handling in Node.js begins by inventorying file flows and classifying file types by business purpose and sensitivity. Know which files are public, private, tenant-scoped, user-scoped, administrative, temporary, generated, or internal-only. Avoid passing user-controlled paths directly to filesystem APIs. Prefer application-controlled IDs and known storage mappings. When dynamic path selection is needed, use fixed storage boundaries and verify that resolved paths remain inside them.\n\nSeparate purposes that are often mixed together. Generate storage names and treat original filenames as untrusted metadata. Use allowlists and multiple validation signals for file type decisions where appropriate. Keep public assets, private files, temporary files, logs, configuration, and secrets in separate locations with permissions that match their purpose. Run the Node.js process with least privilege and avoid broad writable directories, broad host mounts, or production write access to source and dependency directories.\n\nBound resource use and retrieval. Use streaming, size limits, timeouts, quotas, concurrency controls, and cleanup jobs for large or long-running file operations. Enforce object-level authorization before private downloads, previews, exports, attachments, signed URLs, and metadata responses. Set response headers deliberately and avoid exposing internal paths or sensitive metadata through errors. Logging and monitoring should help defenders without leaking file contents or secrets.\n\nA practical first-week plan is concrete: find all filesystem calls, map storage directories, review path construction, check public and private storage separation, confirm download authorization, add size limits, review temporary cleanup, remove sensitive path logging, and add tests for traversal and authorization boundaries. The long-term goal is a repeatable workflow where design, path boundaries, validation, storage isolation, permissions, resource limits, safe responses, logging, monitoring, and incident response work together.",
  "narrationPoints": [
      "When dynamic path selection is needed, use fixed storage boundaries and verify that resolved paths remain inside them.",
      "Keep public assets, private files, temporary files, logs, configuration, and secrets in separate locations with permissions that match their purpose.",
      "Logging and monitoring should help defenders without leaking file contents or secrets.",
      "The long-term goal is a repeatable workflow where design, path boundaries, validation, storage isolation, permissions, resource limits, safe responses, logging, monitoring, and incident response work together.",
      "A practical first-week plan is concrete: find all filesystem calls, map storage directories, review path construction, check public and private storage separation, confirm download authorization, add size limits, review temporary cleanup, remove sensitive path logging, and add tests for traversal and authorization boundaries.",
      "Run the Node.js process with least privilege and avoid broad writable directories, broad host mounts, or production write access to source and dependency directories."
  ]
};
