window.COURSE_MODULE = {
  "title": "Event Architecture and Trust Boundaries",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Event Architecture and Trust Boundaries, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Before reviewing individual handlers, map the event-driven system. Identify producers, APIs, brokers, queues, topics, streams, consumers, workers, schedulers, serverless functions, retry paths, dead-letter queues, replay tools, and external webhook sources. Also identify what each event triggers. The point of the map is not decorative architecture. It is to show where data enters, where authority is applied, and where a failure can change business state.\n\nThe phrase 'it came from the queue' is not the same as 'it is trustworthy.' A queue is a transport. The payload may still contain user-controlled values, stale state from another service, malformed fields, or data that was valid for one tenant but not another. Managed brokers and internal services can move messages reliably, but they do not automatically convert arbitrary JSON into authorized business actions. Trust needs to be established at each boundary where a consumer acts on the event.\n\nDifferent event categories carry different assumptions. Internal service events, external partner events, user-triggered events, system-generated events, scheduled events, administrative events, and manual replay events should not all be treated the same. Each category has a source, identity, expected schema, expected frequency, allowed consumers, and failure mode. A scheduled cleanup job and a payment webhook may both be events, but the risk and review questions are very different.\n\nAccess to the event system also matters. Define who can publish, who can consume, who can inspect, who can replay, who can delete, and who can administer. A production event bus, a dead-letter queue, and a replay console can all contain sensitive data or trigger important operations. Security review should consider the full workflow, not only the handler function that happens to process the message today.",
  "narrationPoints": [
    "Identify producers, APIs, brokers, queues, topics, streams, consumers, workers, schedulers, serverless functions, retry paths, dead-letter queues, replay tools, and external webhook sources.",
    "Trust needs to be established at each boundary where a consumer acts on the event.",
    "Internal service events, external partner events, user-triggered events, system-generated events, scheduled events, administrative events, and manual replay events should not all be treated the same.",
    "Security review should consider the full workflow, not only the handler function that happens to process the message today.",
    "A production event bus, a dead-letter queue, and a replay console can all contain sensitive data or trigger important operations.",
    "A scheduled cleanup job and a payment webhook may both be events, but the risk and review questions are very different."
  ]
};
