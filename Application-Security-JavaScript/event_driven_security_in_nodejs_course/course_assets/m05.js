window.COURSE_MODULE = {
  "title": "Webhooks, External Events, and Source Verification",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Webhooks, External Events, and Source Verification, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "A webhook receiver is an API endpoint that can trigger business actions. Payment providers, identity systems, Git hosting platforms, SaaS integrations, and partner systems may all send events into a Node.js application. Those events are external input, even when they arrive from a trusted vendor. Treat them as untrusted until the source, integrity, timestamp, event type, account mapping, tenant mapping, and business state have been verified.\n\nSource verification should follow the provider's documented mechanism. That might mean HMAC signatures, certificate checks, bearer tokens, mutual TLS, IP allowlisting, or another authentication pattern. Many webhook signature schemes require validation over the exact raw request body. If middleware parses or reformats the payload before verification, the signature check may fail or verify the wrong representation. This is a common Node.js integration detail worth testing explicitly.\n\nReplay protection matters when a captured event can be sent again. Use timestamps, nonces, event IDs, delivery IDs, expiration windows, and provider-specific semantics where the design requires them. A secret URL alone is not enough for high-impact webhook actions. URLs leak into logs, browser history, tickets, analytics, and screenshots more often than teams expect. Treat webhook endpoints as public-facing security boundaries, not as hidden internal functions.\n\nExternal event handlers should fail safely. They should validate schema and business state before taking action, avoid exposing internal stack traces or private details in responses, and store enough metadata for audit, deduplication, and incident response. A payment event, identity event, or partner callback should leave evidence about what was received and what decision was made without turning logs into a warehouse of sensitive payloads.",
  "narrationPoints": [
    "Treat them as untrusted until the source, integrity, timestamp, event type, account mapping, tenant mapping, and business state have been verified.",
    "If middleware parses or reformats the payload before verification, the signature check may fail or verify the wrong representation.",
    "Use timestamps, nonces, event IDs, delivery IDs, expiration windows, and provider-specific semantics where the design requires them.",
    "They should validate schema and business state before taking action, avoid exposing internal stack traces or private details in responses, and store enough metadata for audit, deduplication, and incident response.",
    "Source verification should follow the provider's documented mechanism.",
    "Those events are external input, even when they arrive from a trusted vendor."
  ]
};
