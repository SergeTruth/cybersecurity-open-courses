window.COURSE_MODULE = {
  "title": "Idempotency, Replay Protection, and Duplicate Events",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Idempotency, Replay Protection, and Duplicate Events, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Many event-driven systems deliver messages at least once. That means duplicate processing is not an edge case. Retries, network failures, broker behavior, external webhook redelivery, manual replay, and consumer crashes can all cause the same event to be processed more than once. If the handler is not designed for that reality, reliability behavior can become a security and business-integrity problem.\n\nIdempotent handlers produce the same safe result when the same event is processed more than once. The exact pattern depends on the operation. A payment workflow might use an idempotency key and an operation record. A document workflow might check whether a preview already exists for the same object version. A provisioning workflow might use state transitions that cannot be applied twice. A notification workflow might tolerate duplicates in some contexts but not for account or security alerts.\n\nReplay protection should be scoped to the source and impact of the event. Consider timestamps, nonces, event IDs, delivery IDs, expiration windows, source identity, tenant, event type, and operation. Ordering should not be assumed unless the platform and design explicitly guarantee it. If events can arrive out of order, handlers need to check current state before applying changes. A stale event should not be able to roll back a newer security decision or overwrite a later workflow state.\n\nDeduplication needs correct scope. A globally unique event ID may be enough in one system, while another needs uniqueness by tenant, producer, event type, and operation. Duplicate handling is especially important for payments, account changes, emails, file deletion, provisioning, notifications, and workflow transitions. Design handlers as if retries, duplicates, delayed messages, and replay are normal operating conditions.",
  "narrationPoints": [
    "Retries, network failures, broker behavior, external webhook redelivery, manual replay, and consumer crashes can all cause the same event to be processed more than once.",
    "A payment workflow might use an idempotency key and an operation record.",
    "Replay protection should be scoped to the source and impact of the event.",
    "A globally unique event ID may be enough in one system, while another needs uniqueness by tenant, producer, event type, and operation.",
    "Consider timestamps, nonces, event IDs, delivery IDs, expiration windows, source identity, tenant, event type, and operation.",
    "If events can arrive out of order, handlers need to check current state before applying changes."
  ]
};
