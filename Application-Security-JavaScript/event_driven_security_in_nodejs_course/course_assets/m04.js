window.COURSE_MODULE = {
  "title": "Producer and Consumer Authorization",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Producer and Consumer Authorization, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Event-driven systems have at least two layers of authorization. Infrastructure authorization decides who can publish, consume, inspect, replay, delete, or administer events. Application authorization decides whether a specific event should cause a specific business action. Both layers matter. A valid queue message should not automatically be allowed to modify every tenant, account, document, invoice, or permission record just because a worker received it.\n\nProducers should be authorized to publish only the event types they are responsible for. Consumers should have only the permissions needed for their assigned workflow. Avoid shared high-privilege credentials across unrelated producers and consumers. When a worker processes one queue, it should not automatically have access to every queue, topic, database, storage bucket, secret, or administrative API. Separation of publish, consume, inspect, replay, delete, and administer permissions reduces the damage caused by mistakes and compromised components.\n\nConsumer permissions must match the resource and action the handler actually needs. A thumbnail worker might read one object path and write a derived preview. It should not be able to delete all files or update billing records. A notification worker might read event metadata and send a message. It should not inherit database administrator permissions. Least privilege is easier when functions and workers have clear purposes instead of one shared runtime identity for everything.\n\nBusiness authorization still has to happen in the handler or supporting service. Do not trust tenant IDs, user IDs, ownership claims, roles, scopes, callback URLs, or workflow states that arrive inside the event payload. Verify them against authoritative state before taking action. Least privilege reduces blast radius, but validation and safe handler logic decide whether the event should be acted on at all.",
  "narrationPoints": [
    "A valid queue message should not automatically be allowed to modify every tenant, account, document, invoice, or permission record just because a worker received it.",
    "When a worker processes one queue, it should not automatically have access to every queue, topic, database, storage bucket, secret, or administrative API.",
    "Consumer permissions must match the resource and action the handler actually needs.",
    "Least privilege reduces blast radius, but validation and safe handler logic decide whether the event should be acted on at all.",
    "Application authorization decides whether a specific event should cause a specific business action.",
    "Event-driven systems have at least two layers of authorization."
  ]
};
