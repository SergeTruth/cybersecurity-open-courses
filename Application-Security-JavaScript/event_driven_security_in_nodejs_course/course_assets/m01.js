window.COURSE_MODULE = {
  "title": "Why Event-Driven Security Matters",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Why Event-Driven Security Matters, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Event-driven Node.js systems are useful because they let work happen outside the original request. A user uploads a document, an API publishes an event, workers generate previews, another worker scans the file, a notification service sends a message, and an audit service records what happened. That workflow can be cleaner than one large request handler, but it also means several components can change state based on messages. Security decisions move from one controller into a distributed chain of producers, brokers, consumers, retries, and failure paths.\n\nEvents often trigger important business actions. They can start payments, update accounts, send emails, provision resources, synchronize records, export data, update audit history, or delete files. A handler may be small, but it can still have broad authority over databases, object storage, internal APIs, queues, and customer data. When event-driven design is treated as plumbing rather than application logic, teams can miss the fact that a message is now a command with consequences.\n\nThe common risks are practical: forged events, unvalidated payloads, overprivileged workers, replayed messages, duplicate processing, unsafe retries, exposed secrets, noisy logs, weak monitoring, and unclear ownership. A queue or event bus can provide reliable delivery, but it does not prove that the payload is safe, current, authorized, or intended. A retry setting can improve availability, but it can also repeat a payment, send duplicate notifications, or replay a destructive operation if the handler is not designed for duplicates.\n\nThe goal is to make the workflow explicit and reviewable. A reviewer should be able to identify event sources, message shape, producer authority, consumer authority, handler behavior, failure handling, replay paths, and operational evidence. Event-driven security is not solved by one queue, one signature header, one schema, one worker identity, or one retry policy. It requires secure design across the full event lifecycle.",
  "narrationPoints": [
    "A user uploads a document, an API publishes an event, workers generate previews, another worker scans the file, a notification service sends a message, and an audit service records what happened.",
    "When event-driven design is treated as plumbing rather than application logic, teams can miss the fact that a message is now a command with consequences.",
    "The common risks are practical: forged events, unvalidated payloads, overprivileged workers, replayed messages, duplicate processing, unsafe retries, exposed secrets, noisy logs, weak monitoring, and unclear ownership.",
    "Event-driven security is not solved by one queue, one signature header, one schema, one worker identity, or one retry policy.",
    "It requires secure design across the full event lifecycle.",
    "A reviewer should be able to identify event sources, message shape, producer authority, consumer authority, handler behavior, failure handling, replay paths, and operational evidence."
  ]
};
