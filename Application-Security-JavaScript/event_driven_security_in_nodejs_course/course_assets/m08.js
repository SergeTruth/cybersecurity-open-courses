window.COURSE_MODULE = {
  "title": "Secrets, Data Exposure, and Message Content",
  "graphicAlt": "Security diagram for Event-Driven Security in Node.js, Secrets, Data Exposure, and Message Content, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Events are designed to move between systems, which makes payload minimization important. A message should contain only the data consumers need to do their work. Avoid placing secrets, access tokens, refresh tokens, private keys, session IDs, authorization headers, cookies, full credentials, private URLs, or unnecessary personal data in event payloads. Once sensitive data enters an event, it may be copied into brokers, logs, tracing systems, dead-letter queues, replay tools, test fixtures, support workflows, and analytics pipelines.\n\nPrefer references or identifiers when consumers can fetch data through authorized service calls. That pattern is not always perfect, because it can add latency and dependency calls, but it often keeps sensitive data closer to the system that owns it. When payloads must include sensitive fields, make that explicit in the schema and define access control, encryption decisions, retention, redaction, and operational handling. Do not leave sensitive event content as an accidental side effect of convenience.\n\nQueue and broker administrators may be able to inspect message payloads depending on platform design. So might developers with debugging access, support staff using replay tools, or incident responders reviewing dead-letter queues. Those access paths may be legitimate, but they should be known and governed. If a message contains a full customer record or bearer token, every inspection path becomes part of the data exposure surface.\n\nWorker configuration and secrets should come from approved secret-management mechanisms, not from event payloads or committed files. Event schemas should mark sensitive fields and define redaction expectations for logs, traces, metrics, dashboards, and support tooling. Safe event design assumes that messages travel, fail, retry, and get inspected. Publish only what is needed and protect everything that remains.",
  "narrationPoints": [
    "Avoid placing secrets, access tokens, refresh tokens, private keys, session IDs, authorization headers, cookies, full credentials, private URLs, or unnecessary personal data in event payloads.",
    "When payloads must include sensitive fields, make that explicit in the schema and define access control, encryption decisions, retention, redaction, and operational handling.",
    "If a message contains a full customer record or bearer token, every inspection path becomes part of the data exposure surface.",
    "Worker configuration and secrets should come from approved secret-management mechanisms, not from event payloads or committed files.",
    "A message should contain only the data consumers need to do their work.",
    "Publish only what is needed and protect everything that remains."
  ]
};
