window.COURSE_MODULE = {
  "title": "What Counts as a Secret",
  "graphicAlt": "Lifecycle sequence for What Counts as a Secret in Secrets Management in Node.js, tracing creation, validation, use, failure handling, cleanup, and verification while highlighting the component responsible at every transition.",
  "narration": "Developers often underestimate what counts as a secret because many credentials look like ordinary configuration. Common Node.js examples include database passwords, API keys, OAuth client secrets, JWT signing keys, session secrets, webhook signing secrets, private keys, encryption keys, cloud credentials, service account tokens, SSH keys, npm tokens, and CI/CD deployment tokens. Names such as DATABASE_URL, JWT_SECRET, SESSION_SECRET, STRIPE_SECRET_KEY, GITHUB_TOKEN, NPM_TOKEN, WEBHOOK_SECRET, AWS_ACCESS_KEY_ID, OAUTH_CLIENT_SECRET, and PRIVATE_KEY should immediately trigger care.\n\nNot every sensitive value is technically a secret, but it may still need protection. Internal hostnames, tenant identifiers, customer identifiers, private package registry URLs, security configuration, feature flags, and deployment topology can help an attacker or expose business context. Public identifiers and secret values should be treated differently, but teams still need clear rules for both. Naming conventions help, but they are not enough if code, logs, and workflows treat everything as harmless text.\n\nA practical program keeps an inventory of important secrets. For each secret, record its purpose, owner, storage location, delivery path, environments, privileges, rotation expectations, and decommissioning path. Also consider where secrets might accidentally appear: source code, .env files, shell history, logs, build artifacts, Docker layers, crash dumps, monitoring tools, package metadata, copied documentation, and screenshots. You cannot protect what the team cannot recognize.",
  "narrationPoints": [
      "Names such as DATABASE_URL, JWT_SECRET, SESSION_SECRET, STRIPE_SECRET_KEY, GITHUB_TOKEN, NPM_TOKEN, WEBHOOK_SECRET, AWS_ACCESS_KEY_ID, OAUTH_CLIENT_SECRET, and PRIVATE_KEY should immediately trigger care.",
      "Not every sensitive value is technically a secret, but it may still need protection.",
      "Also consider where secrets might accidentally appear: source code, .env files, shell history, logs, build artifacts, Docker layers, crash dumps, monitoring tools, package metadata, copied documentation, and screenshots.",
      "Common Node.js examples include database passwords, API keys, OAuth client secrets, JWT signing keys, session secrets, webhook signing secrets, private keys, encryption keys, cloud credentials, service account tokens, SSH keys, npm tokens, and CI/CD deployment tokens.",
      "Public identifiers and secret values should be treated differently, but teams still need clear rules for both.",
      "For each secret, record its purpose, owner, storage location, delivery path, environments, privileges, rotation expectations, and decommissioning path."
  ]
};
