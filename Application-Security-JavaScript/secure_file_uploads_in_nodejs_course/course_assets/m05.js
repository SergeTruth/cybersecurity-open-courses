window.COURSE_MODULE = {
  "title": "Temporary Handling, Storage Isolation, and Path Safety",
  "graphicAlt": "Layered architecture view of Temporary Handling, Storage Isolation, and Path Safety for Secure File Uploads in Node.js, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "Storage is where upload mistakes become persistent. Temporary upload locations should be isolated, access-controlled, and cleaned up reliably after success, failure, timeout, or user cancellation. Permanent storage should avoid executable application directories, public web roots, and locations where the application server might interpret file content as code or trusted static assets. Store uploaded content outside the runtime path when possible, and keep metadata in a database where authorization and lifecycle rules can be applied.\n\nGenerated storage names make path handling safer and reviewable. Use server-side identifiers or object keys rather than user-provided filenames. Validate that final filesystem paths or object keys remain inside the intended storage boundary. Avoid using untrusted input to construct paths, URLs, object keys, shell commands, or downstream processing arguments. If object storage is used, review bucket policies, access controls, lifecycle rules, retention settings, and whether files are private by default.\n\nDownload design should be connected to storage design. Private files can be served through controlled download routes, short-lived signed URLs, or storage mechanisms that enforce authorization. Public files should be public only because the business workflow requires it, not because direct public access was easier to configure. Deletion, retention, cleanup of abandoned temporary files, ownership changes, and legal or business holds should be part of the design rather than afterthoughts.",
  "narrationPoints": [
      "Permanent storage should avoid executable application directories, public web roots, and locations where the application server might interpret file content as code or trusted static assets.",
      "Validate that final filesystem paths or object keys remain inside the intended storage boundary.",
      "Deletion, retention, cleanup of abandoned temporary files, ownership changes, and legal or business holds should be part of the design rather than afterthoughts.",
      "Generated storage names make path handling safer and reviewable.",
      "Private files can be served through controlled download routes, short-lived signed URLs, or storage mechanisms that enforce authorization.",
      "Avoid using untrusted input to construct paths, URLs, object keys, shell commands, or downstream processing arguments."
  ]
};
