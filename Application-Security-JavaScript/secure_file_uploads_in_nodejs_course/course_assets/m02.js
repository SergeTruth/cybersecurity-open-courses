window.COURSE_MODULE = {
  "title": "Upload Architecture and Threat Modeling",
  "graphicAlt": "Lifecycle sequence for Upload Architecture and Threat Modeling in Secure File Uploads in Node.js, tracing creation, validation, use, failure handling, cleanup, and verification while highlighting the component responsible at every transition.",
  "narration": "Upload security improves when the team understands the intended lifecycle before choosing implementation details. Start with basic questions: who can upload, what file types are allowed, why those files are needed, how large they can be, where they are stored, who can access them later, and how long they remain. A resume upload, avatar image, customer invoice, support attachment, and administrative import file do not have the same risk profile or the same security requirements.\n\nMap the path from the client to the Node.js application, parser middleware, temporary storage, validation, scanning, permanent storage, background processing, download routes, and preview features. Then identify the trust boundaries between browsers, APIs, reverse proxies, Node.js processes, file systems, object stores, malware scanners, worker queues, databases, administrators, and users. A file that is safe to receive may still be unsafe to process immediately or unsafe to make available for download before scanning finishes.\n\nSecurity requirements should be defined per use case rather than globally for every file. Public uploads, authenticated user uploads, administrative imports, service-to-service uploads, and internal processing queues may need different authentication, size limits, scanning expectations, retention rules, and access controls. Some files can be rejected immediately. Others may need to be accepted into quarantine, processed asynchronously, and released only after validation. Threat modeling keeps those decisions intentional instead of accidental.",
  "narrationPoints": [
      "Start with basic questions: who can upload, what file types are allowed, why those files are needed, how large they can be, where they are stored, who can access them later, and how long they remain.",
      "Map the path from the client to the Node.js application, parser middleware, temporary storage, validation, scanning, permanent storage, background processing, download routes, and preview features.",
      "Security requirements should be defined per use case rather than globally for every file.",
      "A resume upload, avatar image, customer invoice, support attachment, and administrative import file do not have the same risk profile or the same security requirements.",
      "Public uploads, authenticated user uploads, administrative imports, service-to-service uploads, and internal processing queues may need different authentication, size limits, scanning expectations, retention rules, and access controls.",
      "Threat modeling keeps those decisions intentional instead of accidental."
  ]
};
