window.COURSE_MODULE = {
  "title": "File Type Validation and Filename Safety",
  "graphicAlt": "Left-to-right security workflow for File Type Validation and Filename Safety in Secure File Uploads in Node.js: untrusted input crosses a labeled boundary, a validating component enforces policy, and only a verified result reaches the protected resource.",
  "narration": "File validation is about reducing ambiguity. A filename ending in .jpg does not prove the content is a safe image, and a Content-Type header does not prove the client is telling the truth. Clients control filenames, extensions, and declared MIME types. Use allowlists for the file types required by the business function, then validate identity with multiple signals where appropriate: extension, declared type, file signature, parser behavior, content expectations, and successful processing by a safe library.\n\nUser-supplied filenames should not become storage paths or object keys. Filenames can contain confusing characters, double extensions, reserved names, hidden-file patterns, unexpected Unicode, path separators, excessive length, or values that create display and logging problems. Generate server-side storage names or object keys and preserve the original filename only as metadata when there is a business need. Treat that metadata as untrusted display text whenever it appears in HTML, headers, logs, or administrative screens.\n\nArchive and compound file formats deserve extra care because they can contain many files, paths, metadata, and compressed content that expands dramatically. The defensive point is not to distrust every file equally. It is to match validation depth to risk and use case. If the workflow only needs profile images, do not accept general document formats. If it needs invoices, define allowed formats and limits. If it needs administrative imports, isolate the processing path and validate the content structure, not just the filename.",
  "narrationPoints": [
      "Use allowlists for the file types required by the business function, then validate identity with multiple signals where appropriate: extension, declared type, file signature, parser behavior, content expectations, and successful processing by a safe library.",
      "Filenames can contain confusing characters, double extensions, reserved names, hidden-file patterns, unexpected Unicode, path separators, excessive length, or values that create display and logging problems.",
      "If it needs administrative imports, isolate the processing path and validate the content structure, not just the filename.",
      "Archive and compound file formats deserve extra care because they can contain many files, paths, metadata, and compressed content that expands dramatically.",
      "Generate server-side storage names or object keys and preserve the original filename only as metadata when there is a business need.",
      "User-supplied filenames should not become storage paths or object keys."
  ]
};
