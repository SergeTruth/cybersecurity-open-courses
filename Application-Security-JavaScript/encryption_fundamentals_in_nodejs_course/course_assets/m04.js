window.COURSE_MODULE = {
  "title": "Symmetric Encryption and Authenticated Encryption",
  "graphicAlt": "Security diagram for Encryption Fundamentals in Node.js, Symmetric Encryption and Authenticated Encryption, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Symmetric encryption uses the same key to encrypt and decrypt. It is a common pattern for application data because it is efficient and works well when a service or controlled backend owns the decryption process. The key point is that encryption alone may only protect confidentiality. If the design does not detect tampering, the application may be unable to tell whether ciphertext, metadata, or context was modified before decryption.\n\nAuthenticated encryption modes are preferred for sensitive application data because they protect confidentiality and integrity together when used correctly. They produce ciphertext and an authentication tag. During decryption, the tag must be verified. If verification fails, the application should fail closed and return no plaintext. Ignoring tag failures or returning partial decrypted data defeats a major part of the protection.\n\nAssociated data can bind unencrypted context into the authentication check. For example, a record type, tenant identifier, key version, purpose, or schema version can be authenticated even if it remains visible. This helps prevent encrypted data from being copied into the wrong context without detection. Associated data is not a magic requirement for every design, but when used, it must be reproduced exactly during verification.\n\nAn encrypted record should be treated as a structured object, not just a blob of unreadable text. The implementation needs a clear place for the algorithm version, key identifier, IV or nonce, ciphertext, authentication tag, and optional associated-data policy. A ciphertext should not merely look unreadable. The application should know whether it was protected with the right key, in the right context, and without modification.",
  "narrationPoints": [
    "The key point is that encryption alone may only protect confidentiality.",
    "Authenticated encryption modes are preferred for sensitive application data because they protect confidentiality and integrity together when used correctly.",
    "For example, a record type, tenant identifier, key version, purpose, or schema version can be authenticated even if it remains visible.",
    "The implementation needs a clear place for the algorithm version, key identifier, IV or nonce, ciphertext, authentication tag, and optional associated-data policy.",
    "Symmetric encryption uses the same key to encrypt and decrypt.",
    "The application should know whether it was protected with the right key, in the right context, and without modification."
  ]
};
