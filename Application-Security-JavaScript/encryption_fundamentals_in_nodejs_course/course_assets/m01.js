window.COURSE_MODULE = {
  "title": "Why Encryption Matters in Node.js",
  "graphicAlt": "Security diagram for Encryption Fundamentals in Node.js, Why Encryption Matters in Node.js, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "A Node.js team often adds encryption after a system is already handling sensitive data. The application stores customer records, support notes, tokens, documents, messages, configuration values, and database fields. Someone says, encrypt it, and the team chooses an algorithm. That is a start, but it is not an encryption design. The important questions are what data is protected, where plaintext appears, who controls the keys, and what happens during rotation or an incident.\n\nEncryption is a confidentiality control. It can protect data at rest, data in transit, or data inside a specific application workflow. It is not the same thing as authentication, authorization, access control, hashing, signing, redaction, or data minimization. If the application can decrypt a field for every request, encryption does not automatically stop an overly broad application permission from exposing that field.\n\nWeak encryption designs usually fail around the edges. Keys get hardcoded into source code, copied into environment files, stored beside ciphertext, or logged during troubleshooting. Old examples use unauthenticated modes. IVs or nonces are reused. Authentication tags are ignored. Logs, analytics, backups, support tools, and exports quietly reintroduce plaintext outside the intended boundary.\n\nThe goal of this course is practical defensive use. Good encryption work begins with a threat model, uses reviewed algorithms and authenticated modes, protects keys separately from data, handles IVs and nonces according to the algorithm, and makes behavior testable and reviewable. Encryption is useful only when it is part of a complete design that includes keys, access control, integrity, logging, rotation, and operational recovery.",
  "narrationPoints": [
    "The important questions are what data is protected, where plaintext appears, who controls the keys, and what happens during rotation or an incident.",
    "If the application can decrypt a field for every request, encryption does not automatically stop an overly broad application permission from exposing that field.",
    "Keys get hardcoded into source code, copied into environment files, stored beside ciphertext, or logged during troubleshooting.",
    "Good encryption work begins with a threat model, uses reviewed algorithms and authenticated modes, protects keys separately from data, handles IVs and nonces according to the algorithm, and makes behavior testable and reviewable.",
    "Encryption is useful only when it is part of a complete design that includes keys, access control, integrity, logging, rotation, and operational recovery.",
    "The application stores customer records, support notes, tokens, documents, messages, configuration values, and database fields."
  ]
};
