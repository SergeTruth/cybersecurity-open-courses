window.COURSE_MODULE = {
  "title": "Encryption Concepts and Threat Modeling",
  "graphicAlt": "Security diagram for Encryption Fundamentals in Node.js, Encryption Concepts and Threat Modeling, showing the protected asset, trust boundary, enforcing component, and verification path with arrows from untrusted input to controlled output.",
  "narration": "Encryption turns plaintext into ciphertext using a key, but the design question is broader than whether the data is encrypted. A useful review asks who has the key, where plaintext is allowed to exist, which systems can decrypt, and what threat is actually reduced. Those questions keep encryption from becoming a checkbox that looks strong but leaves the real exposure unchanged.\n\nPlaintext can appear before encryption, after decryption, and in places the feature team did not expect. Logs may capture request bodies. Analytics may store field values. Backups may preserve older formats. Support tools may display decrypted data. Exports, memory dumps, message queues, search indexes, screenshots, and error reports may all create copies. An encryption design should name those paths rather than assuming ciphertext in one database column solves the whole problem.\n\nDifferent encryption layers reduce different risks. TLS protects data in motion between endpoints, but not data after it is stored or logged. Disk or database encryption can protect against some storage-level exposure, but the running application and authorized database workflows may still see plaintext. Application-layer or field-level encryption can reduce exposure from database reads, but it can complicate search, analytics, support, and rotation.\n\nBefore implementation, define data sensitivity, allowed plaintext locations, decrypting systems, key access, retention, deletion, recovery needs, and incident response expectations. Ask what happens if the database is copied, if a backup is exposed, if the app server is compromised, or if a support operator has too much access. Good encryption starts as a design decision, not as a late call to a crypto function.",
  "narrationPoints": [
    "Encryption turns plaintext into ciphertext using a key, but the design question is broader than whether the data is encrypted.",
    "An encryption design should name those paths rather than assuming ciphertext in one database column solves the whole problem.",
    "Disk or database encryption can protect against some storage-level exposure, but the running application and authorized database workflows may still see plaintext.",
    "Good encryption starts as a design decision, not as a late call to a crypto function.",
    "A useful review asks who has the key, where plaintext is allowed to exist, which systems can decrypt, and what threat is actually reduced.",
    "Application-layer or field-level encryption can reduce exposure from database reads, but it can complicate search, analytics, support, and rotation."
  ]
};
