window.COURSE_MODULE = {
  "title": "Authentication, Database Users, and Least Privilege",
  "graphicAlt": "Lifecycle sequence for Authentication, Database Users, and Least Privilege in Secure Database Connections in Node.js, tracing creation, validation, use, failure handling, cleanup, and verification while highlighting the component responsible at every transition.",
  "narration": "Every database connection uses an identity. That identity might be a username and password, certificate, IAM-style role, service account, token, or another approved mechanism. Connection security is about the authority attached to that identity. The application should not connect as the database owner, root user, or broad administrator just because that credential makes development easy. Runtime identity should be designed around the operations the application truly needs.\n\nSeparate application runtime users from migration users, administrative users, reporting users, backup users, read-only users, analytics users, and human operator accounts where practical. Limit privileges by database, schema, collection, table, operation, and environment when the platform supports it. Read-only services should not have write access. Runtime services should not carry schema modification privileges unless there is a clearly reviewed requirement. Migration credentials may require elevated privileges, which means they should be more tightly protected and used only in controlled workflows.\n\nLeast privilege reduces blast radius, but it does not replace safe application code. A limited account can still expose sensitive data if authorization is missing. A read-write account can still damage records if query logic is unsafe. Still, privilege design matters because mistakes happen. Rotate credentials when they are exposed, when staff or vendors change, when a workflow is retired, or when a credential has spread beyond its intended boundary. Authority should be intentional, reviewable, and limited.",
  "narrationPoints": [
      "Runtime identity should be designed around the operations the application truly needs.",
      "Separate application runtime users from migration users, administrative users, reporting users, backup users, read-only users, analytics users, and human operator accounts where practical.",
      "Least privilege reduces blast radius, but it does not replace safe application code.",
      "Migration credentials may require elevated privileges, which means they should be more tightly protected and used only in controlled workflows.",
      "Limit privileges by database, schema, collection, table, operation, and environment when the platform supports it.",
      "The application should not connect as the database owner, root user, or broad administrator just because that credential makes development easy."
  ]
};
