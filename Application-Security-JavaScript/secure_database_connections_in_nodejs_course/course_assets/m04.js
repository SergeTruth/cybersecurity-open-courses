window.COURSE_MODULE = {
  "title": "TLS, Certificate Validation, and Encrypted Transport",
  "graphicAlt": "Two-path comparison for TLS, Certificate Validation, and Encrypted Transport in Secure Database Connections in Node.js: an unsafe flow bypasses the relevant control, while the approved flow passes through enforcement, bounded processing, and an auditable outcome.",
  "narration": "TLS helps protect database traffic from passive observation and some active network attacks, but secure transport is not just a checkbox. There is an important difference between turning on encryption and verifying who the application connected to. A connection can be encrypted and still unsafe if the application disables certificate validation, ignores hostname verification, trusts an arbitrary certificate, or accepts a development shortcut in production.\n\nDatabase drivers and deployment platforms express TLS settings differently, but the security goals are consistent: use encryption when appropriate, validate the server, protect client certificates when used, and avoid silently accepting the wrong endpoint. Managed database services, private networks, containers, self-managed PostgreSQL, MySQL, SQL Server, and MongoDB deployments may all require different certificate and CA handling. The review should focus on the goal and the actual deployment, not memorizing one option name.\n\nTLS settings should be reviewed in code, connection strings, deployment variables, infrastructure configuration, and platform defaults. Development exceptions should be explicit, documented, and prevented from reaching production. Client certificates, private keys, and CA material should be handled as sensitive configuration. TLS protects transport; it does not replace authentication, authorization, least privilege, query safety, or monitoring. It is one layer in a complete connection security design.",
  "narrationPoints": [
      "A connection can be encrypted and still unsafe if the application disables certificate validation, ignores hostname verification, trusts an arbitrary certificate, or accepts a development shortcut in production.",
      "Database drivers and deployment platforms express TLS settings differently, but the security goals are consistent: use encryption when appropriate, validate the server, protect client certificates when used, and avoid silently accepting the wrong endpoint.",
      "TLS protects transport; it does not replace authentication, authorization, least privilege, query safety, or monitoring.",
      "TLS helps protect database traffic from passive observation and some active network attacks, but secure transport is not just a checkbox.",
      "Managed database services, private networks, containers, self-managed PostgreSQL, MySQL, SQL Server, and MongoDB deployments may all require different certificate and CA handling.",
      "TLS settings should be reviewed in code, connection strings, deployment variables, infrastructure configuration, and platform defaults."
  ]
};
