window.COURSE_MODULE = {
  "title": "Code Review, CI/CD, Migrations, and Operational Governance",
  "graphicAlt": "Trust-boundary map for Code Review, CI/CD, Migrations, and Operational Governance in Secure Database Connections in Node.js, with distinct caller, application, platform, and external-service zones connected only through labeled validation and authorization checkpoints.",
  "narration": "Secure database connections should be repeatable in engineering and release workflows. Code review should include connection setup, driver options, ORM configuration, TLS settings, pool settings, timeout behavior, retry behavior, environment variable use, secret access, and logging behavior. A reviewer should be able to answer where the connection configuration comes from, what secrets are used, whether TLS is validated, which database identity runs the application, how pooling behaves, and how failures are detected.\n\nCI/CD deserves careful handling because it often has enough automation power to reach sensitive systems. Protect database credentials and avoid exposing production access to untrusted pull requests, forks, preview builds, or broad jobs. Migration workflows should use reviewed scripts, controlled credentials, approval gates where appropriate, and clear rollback or recovery plans. Seed scripts, admin scripts, reporting scripts, backup scripts, and one-off maintenance scripts can carry powerful credentials and should be reviewed like production code.\n\nSecret scanning, dependency checks, configuration validation, infrastructure review, and deployment policy can all support connection security. Tests should verify missing configuration behavior, startup validation, required TLS settings where applicable, pool lifecycle, safe error responses, and environment separation. Production changes to connection settings should leave evidence: who changed what, when, why, and how it was validated. Governance should name owners for credentials, connection options, certificates, pool settings, migration accounts, and incident response.",
  "narrationPoints": [
      "Code review should include connection setup, driver options, ORM configuration, TLS settings, pool settings, timeout behavior, retry behavior, environment variable use, secret access, and logging behavior.",
      "Protect database credentials and avoid exposing production access to untrusted pull requests, forks, preview builds, or broad jobs.",
      "Secret scanning, dependency checks, configuration validation, infrastructure review, and deployment policy can all support connection security.",
      "A reviewer should be able to answer where the connection configuration comes from, what secrets are used, whether TLS is validated, which database identity runs the application, how pooling behaves, and how failures are detected.",
      "Tests should verify missing configuration behavior, startup validation, required TLS settings where applicable, pool lifecycle, safe error responses, and environment separation.",
      "Secure database connections should be repeatable in engineering and release workflows."
  ]
};
