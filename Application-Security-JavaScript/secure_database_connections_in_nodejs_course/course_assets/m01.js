window.COURSE_MODULE = {
  "title": "Why Database Connection Security Matters",
  "graphicAlt": "Left-to-right security workflow for Why Database Connection Security Matters in Secure Database Connections in Node.js: untrusted input crosses a labeled boundary, a validating component enforces policy, and only a verified result reaches the protected resource.",
  "narration": "A database connection is not just a technical detail hidden in startup code. It is the path by which a Node.js application reaches the most important data in the system. That path carries authority, credentials, network access, transport assumptions, pooling behavior, and operational risk. If the connection is weak, the application may expose credentials, bypass intended network boundaries, weaken encryption, exhaust database capacity, or run with more database power than the feature actually needs.\n\nA typical application often starts with one local database URL. Later it grows into staging, production, migration jobs, CI/CD workflows, containers, serverless functions, background workers, reporting scripts, and admin tools. Each component that connects to the database may need different credentials, different network access, different privileges, and different monitoring. Treating all of them as the same connection is convenient, but it hides important security decisions.\n\nSecure connection design controls who connects, where the connection comes from, how transport is protected, what the connected identity can do, and how the team detects failure. It affects confidentiality, integrity, availability, incident response, and release safety. The goal is to make database connectivity intentional, encrypted where appropriate, authenticated, least-privileged, observable, and recoverable across every environment where Node.js code touches data.",
  "narrationPoints": [
      "If the connection is weak, the application may expose credentials, bypass intended network boundaries, weaken encryption, exhaust database capacity, or run with more database power than the feature actually needs.",
      "Each component that connects to the database may need different credentials, different network access, different privileges, and different monitoring.",
      "Secure connection design controls who connects, where the connection comes from, how transport is protected, what the connected identity can do, and how the team detects failure.",
      "A database connection is not just a technical detail hidden in startup code.",
      "Treating all of them as the same connection is convenient, but it hides important security decisions.",
      "The goal is to make database connectivity intentional, encrypted where appropriate, authenticated, least-privileged, observable, and recoverable across every environment where Node.js code touches data."
  ]
};
