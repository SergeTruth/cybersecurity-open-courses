window.COURSE_MODULE = {
  "title": "Connection Architecture and Trust Boundaries",
  "graphicAlt": "Layered architecture view of Connection Architecture and Trust Boundaries for Secure Database Connections in Node.js, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "Secure connection review starts with an inventory. Identify every component that connects to a database: public APIs, internal services, workers, scheduled jobs, migration tasks, seed scripts, admin tools, reporting jobs, backup processes, test runners, and CI/CD workflows. Then map where each one runs. A containerized API, a local developer process, a serverless function, and a migration job may all use the same database technology while crossing very different trust boundaries.\n\nMap the boundaries between clients, reverse proxies, Node.js services, containers, serverless platforms, database servers, managed database services, secret stores, CI/CD systems, logging platforms, administrators, and support staff. Distinguish runtime connections from migration, maintenance, reporting, backup, analytics, and administrative connections. Also identify the environments in use: local development, test, staging, preview, production, disaster recovery, and analytics. Each environment should have a deliberate connection purpose and a clear owner.\n\nFor each connection path, ask what connects, from where, using which identity, over which network route, with which privileges, and under whose operational control. Also ask who can read the configuration, who can change it, who can deploy it, and who responds when it fails. Security requirements should be tied to the purpose of the connection rather than to the database brand. A production API connection and a migration connection should not automatically share credentials, privileges, or exposure simply because both talk to the same database.",
  "narrationPoints": [
      "A containerized API, a local developer process, a serverless function, and a migration job may all use the same database technology while crossing very different trust boundaries.",
      "Map the boundaries between clients, reverse proxies, Node.js services, containers, serverless platforms, database servers, managed database services, secret stores, CI/CD systems, logging platforms, administrators, and support staff.",
      "A production API connection and a migration connection should not automatically share credentials, privileges, or exposure simply because both talk to the same database.",
      "Security requirements should be tied to the purpose of the connection rather than to the database brand.",
      "For each connection path, ask what connects, from where, using which identity, over which network route, with which privileges, and under whose operational control.",
      "Each environment should have a deliberate connection purpose and a clear owner."
  ]
};
