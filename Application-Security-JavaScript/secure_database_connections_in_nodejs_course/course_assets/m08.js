window.COURSE_MODULE = {
  "title": "Errors, Logging, Monitoring, and Sensitive Diagnostics",
  "graphicAlt": "Layered architecture view of Errors, Logging, Monitoring, and Sensitive Diagnostics for Secure Database Connections in Node.js, placing the protected asset at the center and surrounding it with identity, validation, resource, lifecycle, and monitoring controls.",
  "narration": "Database connection errors are useful to operators, but they can reveal hostnames, ports, usernames, database names, connection strings, TLS details, stack traces, driver options, internal paths, and operational topology. Client responses should not expose connection details, credentials, SQL statements, stack traces, or internal database metadata. External callers usually need a safe error message and a correlation ID, not the connection internals.\n\nLogs should preserve diagnostic context without leaking secrets. Useful fields may include request ID, service name, environment, connection target alias, error category, timeout category, pool state summary, operation type, migration identifier, deployment version, and safe correlation identifiers. Avoid full connection strings, passwords, tokens, private keys, certificates, cookies, authorization headers, full sensitive records, and raw process environment dumps. Use aliases and categories instead of raw URLs and credentials.\n\nMonitoring should detect connection failures, authentication failures, TLS validation failures, pool exhaustion, retry spikes, slow connection establishment, unusual source locations, failed migrations, and sudden changes in database connectivity. Alerts should help teams distinguish credential problems, network problems, certificate problems, capacity problems, and database-side outages. Incident response should include credential rotation, connection-source review, log review, network rule review, certificate review, and validation of restored connection behavior.",
  "narrationPoints": [
      "Database connection errors are useful to operators, but they can reveal hostnames, ports, usernames, database names, connection strings, TLS details, stack traces, driver options, internal paths, and operational topology.",
      "Avoid full connection strings, passwords, tokens, private keys, certificates, cookies, authorization headers, full sensitive records, and raw process environment dumps.",
      "Monitoring should detect connection failures, authentication failures, TLS validation failures, pool exhaustion, retry spikes, slow connection establishment, unusual source locations, failed migrations, and sudden changes in database connectivity.",
      "Incident response should include credential rotation, connection-source review, log review, network rule review, certificate review, and validation of restored connection behavior.",
      "Client responses should not expose connection details, credentials, SQL statements, stack traces, or internal database metadata.",
      "External callers usually need a safe error message and a correlation ID, not the connection internals."
  ]
};
