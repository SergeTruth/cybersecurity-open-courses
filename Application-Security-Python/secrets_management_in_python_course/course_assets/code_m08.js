window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Detection, Logging, Testing, and Incident Response with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Redact secrets recursively before logging",
      "language": "python",
      "blurb": "Nested structures are copied while a shared sensitive-field policy catches common token, password, cookie, credential, and private-key names before logging.",
      "code": "import re\nfrom collections.abc import Mapping\n\nSENSITIVE_TERMS = {\n    \"credential\", \"credentials\", \"passwd\", \"password\", \"pwd\", \"secret\", \"secrets\",\n    \"session\", \"token\", \"tokens\", \"cookie\", \"cookies\", \"private\",\n}\nSENSITIVE_SQUASHED_FIELDS = {\n    \"apikey\", \"apitoken\", \"authheader\", \"authorization\", \"bearertoken\",\n    \"clientsecret\", \"hmacsecret\", \"passwordhash\", \"privatekey\",\n    \"refreshtoken\", \"resettoken\", \"sessioncookie\", \"signingkey\",\n}\n\ndef key_terms(key: object) -> set[str]:\n    if not isinstance(key, str):\n        return set()\n    separated = re.sub(r\"([a-z0-9])([A-Z])\", r\"\\1_\\2\", key)\n    return {part for part in re.split(r\"[^A-Za-z0-9]+\", separated.casefold()) if part}\n\ndef is_sensitive_field(key: object) -> bool:\n    if not isinstance(key, str):\n        return False\n    terms = key_terms(key)\n    squashed = re.sub(r\"[^a-z0-9]+\", \"\", key.casefold())\n    return (\n        bool(terms & SENSITIVE_TERMS)\n        or squashed in SENSITIVE_SQUASHED_FIELDS\n        or (\"key\" in terms and bool(terms & {\"api\", \"encryption\", \"hmac\", \"private\", \"signing\"}))\n    )\n\ndef redact(value):\n    if isinstance(value, Mapping):\n        return {\n            key: \"[REDACTED]\" if is_sensitive_field(key) else redact(item)\n            for key, item in value.items()\n        }\n    if isinstance(value, list):\n        return [redact(item) for item in value]\n    if isinstance(value, tuple):\n        return tuple(redact(item) for item in value)\n    if value is None or isinstance(value, (str, int, float, bool)):\n        return value\n    return f\"[{type(value).__name__}]\"\n"
    },
    {
      "title": "Coordinate response to an exposed secret",
      "language": "python",
      "blurb": "The runbook validates audit-safe identifiers, revokes first, records a non-secret incident event, and only then requests a replacement credential.",
      "code": "from dataclasses import dataclass\nimport unicodedata\n\n@dataclass(frozen=True)\nclass ExposureResult:\n    revoked_version: str\n    replacement_version: str\n\ndef has_control_character(value: str) -> bool:\n    return any(unicodedata.category(character) in {\"Cc\", \"Cf\"} for character in value)\n\ndef safe_component(component: str) -> bool:\n    return (\n        1 <= len(component) <= 40\n        and component not in {\".\", \"..\"}\n        and all(character.isascii() and (character.isalnum() or character in \"-_\") for character in component)\n    )\n\ndef safe_secret_name(secret_name: str) -> str:\n    if not isinstance(secret_name, str) or not 1 <= len(secret_name) <= 120:\n        raise ValueError(\"secret name rejected\")\n    if has_control_character(secret_name):\n        raise ValueError(\"secret name rejected\")\n    parts = secret_name.split(\"/\")\n    if not parts or not all(safe_component(part) for part in parts):\n        raise ValueError(\"secret name rejected\")\n    return \"/\".join(parts)\n\ndef safe_version(version: str) -> str:\n    if not isinstance(version, str) or not 1 <= len(version) <= 80:\n        raise ValueError(\"secret version rejected\")\n    if has_control_character(version) or not all(character.isascii() and (character.isalnum() or character in \"-_.\") for character in version):\n        raise ValueError(\"secret version rejected\")\n    return version\n\ndef respond_to_exposure(provider, audit, secret_name: str, exposed_version: str) -> ExposureResult:\n    secret_name = safe_secret_name(secret_name)\n    exposed_version = safe_version(exposed_version)\n    provider.revoke(secret_name, exposed_version)\n    audit.record(\"secret_revoked\", secret=secret_name, version=exposed_version)\n    replacement = provider.rotate(secret_name)\n    replacement_version = safe_version(replacement.version)\n    return ExposureResult(exposed_version, replacement_version)\n"
    }
  ]
};
