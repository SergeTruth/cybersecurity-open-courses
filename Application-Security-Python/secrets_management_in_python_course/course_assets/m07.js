window.COURSE_MODULE = {
  "title": "Secrets in CI/CD, Containers, and Automation",
  "graphicAlt": "Control-flow diagram for Secrets in CI/CD, Containers, and Automation, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Automation expands the places secrets can appear. CI/CD systems, deployment scripts, container builds, infrastructure-as-code pipelines, notebooks, data workflows, scheduled jobs, and test environments may all need credentials. The risk is that a secret used for one short operation gets copied into logs, images, caches, artifacts, debug output, or long-lived variables with broad access. Automation should retrieve secrets securely and keep them out of durable artifacts whenever possible.\n\nBuild-time and runtime secrets are different. A build may need a temporary credential to fetch a private dependency or sign an artifact. A runtime service may need a database password or API token while serving users. Secrets used during builds should not be baked into container images or compiled outputs. Container images may be stored, pulled, scanned, cached, shared, and reused well beyond the intended runtime environment.\n\nCI/CD secret variables should be scoped carefully. Not every branch, pull request, or job should receive production credentials. Deployment credentials should be separated by environment and purpose. Infrastructure-as-code should reference secrets without embedding secret values directly in repository files or generated plans. Test environments should use test secrets with limited access rather than production credentials copied for convenience.\n\nNotebooks and data workflows deserve the same discipline as services. Exploratory Python code often grows into scheduled automation. A token pasted into a notebook can end up in version history, shared storage, output cells, or exported reports. The safe pattern is to use approved secret retrieval, keep outputs clean, limit access, and treat automation as production-like when it touches real systems or sensitive data.",
  "narrationPoints": [
    "CI/CD systems, deployment scripts, container builds, infrastructure-as-code pipelines, notebooks, data workflows, scheduled jobs, and test environments may all need credentials.",
    "A runtime service may need a database password or API token while serving users.",
    "Not every branch, pull request, or job should receive production credentials.",
    "Test environments should use test secrets with limited access rather than production credentials copied for convenience.",
    "A token pasted into a notebook can end up in version history, shared storage, output cells, or exported reports.",
    "CI/CD secret variables should be scoped carefully."
  ]
};
