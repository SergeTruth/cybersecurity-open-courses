window.COURSE_MODULE = {
  "title": "Configuration, Environment Variables, and Boundaries",
  "graphicAlt": "Layered architecture diagram for Configuration, Environment Variables, and Boundaries, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Separating configuration from code is a good principle because applications need different settings in local development, testing, staging, and production. Secrets should not be compiled into source files or committed alongside business logic. But separation by itself is not enough. A value moved from code into an environment variable can still be exposed if the environment is broadly readable, logged, dumped during crashes, or copied into deployment metadata.\n\nEnvironment variables are common because they are easy for frameworks, containers, and deployment platforms to provide. They can be useful for passing secret references or runtime values into a Python process. Their limitation is that they are not automatically encrypted, access-controlled, rotated, audited, or protected from every form of inspection. Treat them as one delivery mechanism, not a complete secrets management strategy.\n\nConfiguration should distinguish secret values from non-secret settings. A feature flag, public hostname, timeout, or display name may not need secret handling. A token, password, private key, or signing secret does. Configuration validation should check that required secrets are present, values are well formed, and unsafe fallback behavior is not allowed. A Python application should fail safely when a required secret is missing or invalid, rather than silently using a weak default or a development credential.\n\nSecret loading boundaries should be explicit. The application should know when secrets are read, which component can read them, how they are cached, and whether reload is possible after rotation. Per-environment access should prevent a development system from reading production secrets and prevent a staging service from using production credentials. Clear boundaries make deployment safer and make incident response much faster.",
  "narrationPoints": [
    "Separating configuration from code is a good principle because applications need different settings in local development, testing, staging, and production.",
    "Their limitation is that they are not automatically encrypted, access-controlled, rotated, audited, or protected from every form of inspection.",
    "Configuration should distinguish secret values from non-secret settings.",
    "Configuration validation should check that required secrets are present, values are well formed, and unsafe fallback behavior is not allowed.",
    "Per-environment access should prevent a development system from reading production secrets and prevent a staging service from using production credentials.",
    "Secret loading boundaries should be explicit."
  ]
};
