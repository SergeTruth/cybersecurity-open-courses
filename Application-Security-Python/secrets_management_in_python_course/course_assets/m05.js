window.COURSE_MODULE = {
  "title": "Access Control and Least Privilege",
  "graphicAlt": "Decision diagram for Access Control and Least Privilege, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Secrets should be accessed through application identity, not through broad human credentials whenever possible. A Python service, scheduled job, data pipeline, or background worker should have a clear service account, workload identity, or runtime identity that can be granted only the access it needs. This makes secret access reviewable and limits the blast radius if one workload is misconfigured or compromised.\n\nLeast privilege means each application or workload receives only the secrets required for its approved purpose. Avoid broad wildcard access to entire secret namespaces. A reporting job should not read payment processor credentials if it only needs a read-only analytics token. A staging application should not read production database passwords. A tenant-specific service should not receive customer secrets outside its boundary unless the design explicitly requires it.\n\nCI/CD and developer access need special attention. Build systems may need deployment credentials or secret references, but they should not automatically expose every production secret to every branch, job, or contributor. Repository permissions, branch protections at a high level, approval workflows, and environment-specific access help prevent secrets from becoming available in places where code is less trusted or less reviewed.\n\nLeast privilege is maintained through review, not set once and forgotten. Secrets should have owners, purpose, environment, allowed consumers, and review cadence. High-impact secrets may need approval before access is granted. When a service is retired, access should be removed. When responsibilities change, permissions should be updated. The application should access only the secrets it needs for the task it performs, and evidence should support that claim.",
  "narrationPoints": [
    "Secrets should be accessed through application identity, not through broad human credentials whenever possible.",
    "A reporting job should not read payment processor credentials if it only needs a read-only analytics token.",
    "A tenant-specific service should not receive customer secrets outside its boundary unless the design explicitly requires it.",
    "Secrets should have owners, purpose, environment, allowed consumers, and review cadence.",
    "When a service is retired, access should be removed.",
    "When responsibilities change, permissions should be updated."
  ]
};
