window.COURSE_MODULE = {
  "title": "What Secrets Management Means in Python",
  "graphicAlt": "Diagram for What Secrets Management Means in Python, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Secrets are sensitive values that applications use to authenticate, connect, sign, decrypt, or access another system. In Python projects, they may include API keys, database passwords, tokens, private keys, certificates, signing keys, webhook secrets, service credentials, and encryption keys. These values are often short strings, but their impact can be large because they may unlock databases, cloud services, payment systems, source repositories, deployment tools, or customer data.\n\nPython applications handle secrets in web apps, APIs, scripts, data pipelines, automation jobs, serverless functions, notebooks, background workers, and internal tools. A secret may be needed during local development, build, deployment, runtime, or incident response. Each context has a different risk profile. A personal script and a production service should not treat the same credential pattern as equally safe.\n\nIt is useful to distinguish secrets from configuration, identifiers, public settings, and sensitive data. A public service URL or feature flag may be configuration but not secret. A user email address may be sensitive data but not a credential. A database password is a secret because possession can grant access. Good secrets management starts by identifying which values actually provide authority and which values merely describe behavior.\n\nSecrets management is not simply hiding strings. It is the lifecycle of creation, storage, access, distribution, rotation, revocation, logging hygiene, monitoring, and recovery. Exposed secrets can create broad downstream risk because a single leaked credential may be reused by automation, copied into artifacts, cached in logs, or trusted by many systems. The goal is to keep Python applications secure, maintainable, and recoverable when secrets inevitably change.",
  "narrationPoints": [
    "Secrets are sensitive values that applications use to authenticate, connect, sign, decrypt, or access another system.",
    "Python applications handle secrets in web apps, APIs, scripts, data pipelines, automation jobs, serverless functions, notebooks, background workers, and internal tools.",
    "A personal script and a production service should not treat the same credential pattern as equally safe.",
    "A database password is a secret because possession can grant access.",
    "The goal is to keep Python applications secure, maintainable, and recoverable when secrets inevitably change.",
    "Secrets management is not simply hiding strings."
  ]
};
