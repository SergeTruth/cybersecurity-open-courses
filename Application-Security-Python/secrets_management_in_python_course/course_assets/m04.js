window.COURSE_MODULE = {
  "title": "Secret Storage Patterns",
  "graphicAlt": "Sequence diagram for Secret Storage Patterns, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Secret storage should match sensitivity, environment, scale, and operational needs. Managed secret stores at a high level provide centralized storage, access control, audit trails, versioning, and sometimes rotation support. Operating system secret stores can help protect local or workstation secrets. Cloud secret managers and orchestration platforms can provide runtime delivery to applications. Encrypted configuration files may be appropriate in some workflows when keys and access are handled carefully.\n\nLocal development needs its own safe pattern. Developers often need credentials for tests, sandboxes, or integration work, but those values should not live in shared plaintext files or repository history. A good local pattern documents where development secrets come from, how they are scoped, how they are excluded from commits, and how they are replaced. Convenience matters because insecure workarounds appear when the approved path is too painful.\n\nAccess control is the core of storage. A secret store is only helpful if identities and permissions are managed well. The application should retrieve only the secrets it needs. Developers, CI/CD systems, support tools, and runtime workloads should have different access. High-impact secrets may require approval, stronger monitoring, or separate ownership. Audit trails should show who or what accessed important secrets and when.\n\nAvailability also matters. If the secret store is unavailable, can the application start? Should it fail closed? Does it cache secrets? How does caching interact with rotation and revocation? These are design decisions. The safest storage pattern is not only encrypted; it is operationally reliable, reviewable, recoverable, and aligned with how the Python application actually runs.",
  "narrationPoints": [
    "Secret storage should match sensitivity, environment, scale, and operational needs.",
    "Encrypted configuration files may be appropriate in some workflows when keys and access are handled carefully.",
    "A secret store is only helpful if identities and permissions are managed well.",
    "Audit trails should show who or what accessed important secrets and when.",
    "The safest storage pattern is not only encrypted; it is operationally reliable, reviewable, recoverable, and aligned with how the Python application actually runs.",
    "Does it cache secrets?"
  ]
};
