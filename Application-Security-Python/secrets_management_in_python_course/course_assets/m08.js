window.COURSE_MODULE = {
  "title": "Detection, Logging, Testing, and Incident Response",
  "graphicAlt": "Operational lifecycle diagram for Detection, Logging, Testing, and Incident Response, showing Python workload identity, managed secret store, short-lived credential, cache, rotation controller, and audit event; labeled arrows identify secret delivery, use, and revocation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Detection helps catch mistakes before they become long-lived exposures. Secrets scanning at a high level can identify secret-like values in code, configuration, repositories, artifacts, and pipelines. Pre-commit and repository scanning concepts help catch issues early. CI/CD scanning can detect secrets before build output is published. Scanning is not perfect, but it gives teams another chance to stop accidental spread.\n\nSafe logging is a design requirement. Logs should avoid secrets, tokens, credentials, private keys, reset links, webhook secrets, and sensitive secret material. Redaction should be applied carefully and tested. Exception messages and stack traces should not dump environment variables, request headers, connection strings, or full configuration objects. Audit logs for secret access are useful, but they should record metadata about access, not the secret value itself.\n\nTesting should cover missing secrets, invalid secrets, unsafe defaults, rotation behavior, and failure modes. The application should fail safely when a required secret is absent rather than falling back to a weak development value. Tests should also confirm that secret values are not emitted in common logs or error paths. Reviewers should ask how secrets enter the process, who can access them, how they are rotated, and what evidence exists for access.\n\nWhen a secret is exposed, treat it as potentially compromised until assessed. Incident response usually involves identifying scope, preserving evidence, revoking or rotating the secret, checking where it was used, reviewing logs for suspicious access, communicating with affected owners, and preventing recurrence. The response should be calm and practiced. A secret exposure is not only a string cleanup task; it is an access control and incident response event.",
  "narrationPoints": [
    "Detection helps catch mistakes before they become long-lived exposures.",
    "CI/CD scanning can detect secrets before build output is published.",
    "Exception messages and stack traces should not dump environment variables, request headers, connection strings, or full configuration objects.",
    "Tests should also confirm that secret values are not emitted in common logs or error paths.",
    "A secret exposure is not only a string cleanup task; it is an access control and incident response event.",
    "The response should be calm and practiced."
  ]
};
