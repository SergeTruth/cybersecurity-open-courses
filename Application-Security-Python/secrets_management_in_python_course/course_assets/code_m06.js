window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Secret Rotation and Lifecycle Management with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Support a bounded overlap during secret rotation",
      "language": "python",
      "blurb": "Fallback catches only a credential rejection and uses trusted, timezone-aware rotation metadata to enforce the retiring credential's deadline.",
      "code": "from collections.abc import Callable\nfrom dataclasses import dataclass\nfrom datetime import datetime, timedelta, timezone\n\nMAX_SECRET_TEXT_LENGTH = 4096\nMAX_ROTATION_OVERLAP = timedelta(minutes=15)\n\nclass CredentialRejected(Exception):\n    pass\n\ndef valid_secret_text(value: object) -> bool:\n    return (\n        type(value) is str\n        and 1 <= len(value) <= MAX_SECRET_TEXT_LENGTH\n        and \"\\x00\" not in value\n    )\n\n@dataclass(frozen=True)\nclass RotationSecrets:\n    current: str\n    retiring: str | None\n    overlap_ends_at: datetime | None\n\n    def __post_init__(self) -> None:\n        if not valid_secret_text(self.current):\n            raise ValueError(\"current credential rejected\")\n        if (self.retiring is None) != (self.overlap_ends_at is None):\n            raise ValueError(\"rotation overlap metadata rejected\")\n        if self.retiring is not None and not valid_secret_text(self.retiring):\n            raise ValueError(\"retiring credential rejected\")\n        if (\n            self.overlap_ends_at is not None\n            and (\n                type(self.overlap_ends_at) is not datetime\n                or self.overlap_ends_at.tzinfo is None\n                or self.overlap_ends_at.utcoffset() is None\n            )\n        ):\n            raise ValueError(\"rotation overlap metadata rejected\")\n\ndef connect_during_rotation(\n    connect: Callable[[str], object],\n    secrets: RotationSecrets,\n    now: datetime,\n):\n    if not callable(connect) or type(secrets) is not RotationSecrets:\n        raise TypeError(\"rotation dependencies rejected\")\n    if (\n        type(now) is not datetime\n        or now.tzinfo is None\n        or now.utcoffset() is None\n    ):\n        raise ValueError(\"timezone-aware current time is required\")\n    now_utc = now.astimezone(timezone.utc)\n    if secrets.overlap_ends_at is not None:\n        overlap_utc = secrets.overlap_ends_at.astimezone(timezone.utc)\n        if not now_utc <= overlap_utc <= now_utc + MAX_ROTATION_OVERLAP:\n            raise ValueError(\"rotation overlap window rejected\")\n    try:\n        return connect(secrets.current)\n    except CredentialRejected:\n        if secrets.retiring is None:\n            raise\n        return connect(secrets.retiring)\n"
    },
    {
      "title": "Invalidate a cached secret after rotation",
      "language": "python",
      "blurb": "Versioned cache entries validate their keys and immutable bytes, then discard old versions as soon as the provider reports a different secret version.",
      "code": "MAX_SECRET_CACHE_ENTRIES = 256\n\nclass VersionedSecretCache:\n    def __init__(self):\n        self._entries: dict[str, tuple[str, bytes]] = {}\n\n    def _validated_name(self, name: str) -> str:\n        if type(name) is not str or not 1 <= len(name) <= 120:\n            raise ValueError(\"secret name rejected\")\n        parts = name.split(\"/\")\n        if any(part in {\"\", \".\", \"..\"} for part in parts):\n            raise ValueError(\"secret name rejected\")\n        if any(\n            not all(\n                character.isascii()\n                and (character.isalnum() or character in \"-_\")\n                for character in part\n            )\n            for part in parts\n        ):\n            raise ValueError(\"secret name rejected\")\n        return name\n\n    def _validated_version(self, version: str) -> str:\n        if type(version) is not str or not 1 <= len(version) <= 80:\n            raise ValueError(\"secret version rejected\")\n        if not all(\n            character.isascii()\n            and (character.isalnum() or character in \"-_.\")\n            for character in version\n        ):\n            raise ValueError(\"secret version rejected\")\n        return version\n\n    def update(self, name: str, version: str, value: bytes) -> None:\n        name = self._validated_name(name)\n        version = self._validated_version(version)\n        if type(value) is not bytes or not 1 <= len(value) <= 4096:\n            raise ValueError(\"secret value rejected\")\n        if name not in self._entries and len(self._entries) >= MAX_SECRET_CACHE_ENTRIES:\n            raise RuntimeError(\"secret cache capacity exceeded\")\n        previous = self._entries.get(name)\n        if previous and previous[0] != version:\n            self._entries.pop(name)\n        self._entries[name] = (version, bytes(value))\n\n    def get(self, name: str, version: str) -> bytes | None:\n        name = self._validated_name(name)\n        version = self._validated_version(version)\n        entry = self._entries.get(name)\n        return bytes(entry[1]) if entry and entry[0] == version else None\n"
    }
  ]
};
