window.COURSE_MODULE = {
  "title": "Certificates, Tokens, APIs, and Webhooks",
  "graphicAlt": "Boundary map for Certificates, Tokens, APIs, and Webhooks, showing signer, protected private key, message bytes, signature, and verifier public key; labeled arrows identify signing and verification boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Digital signatures appear inside certificates and public key infrastructure at a high level. Certificates bind public keys to identities through a chain of trust. Applications that rely on certificate validation should use standard libraries and platform validation rather than inventing certificate rules. The important principle is that signature verification is part of a larger trust decision about who issued the certificate, which identity it represents, and whether the certificate is valid for the intended use.\n\nSigned tokens are another common pattern. A token may contain claims such as subject, issuer, audience, expiration, scope, role, tenant, or authentication context. The signature helps detect tampering and establish the issuer, but the application must validate context. A signed token from the wrong issuer, wrong audience, expired time window, or wrong intended use should not be trusted merely because its signature is mathematically valid.\n\nWebhook and API request signing concepts follow the same pattern. The receiver verifies that a message came from an expected party and was not modified. Context often includes timestamp windows, request identifiers, selected headers, body bytes, endpoint paths, or nonce concepts at a high level. These details help prevent replay and confusion, but only if the signer and verifier agree on what is covered and how freshness is enforced.\n\nAccepting any valid signature from any key is not a safe trust model. A Python application should define which issuers, keys, algorithms, audiences, time windows, and purposes are accepted for each integration. Context validation matters in addition to cryptographic verification. Signed data can still be stale, malformed, unauthorized, outside business policy, or intended for a different system.",
  "narrationPoints": [
    "Digital signatures appear inside certificates and public key infrastructure at a high level.",
    "Applications that rely on certificate validation should use standard libraries and platform validation rather than inventing certificate rules.",
    "Webhook and API request signing concepts follow the same pattern.",
    "Context often includes timestamp windows, request identifiers, selected headers, body bytes, endpoint paths, or nonce concepts at a high level.",
    "Context validation matters in addition to cryptographic verification.",
    "Signed tokens are another common pattern."
  ]
};
