window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Key Management and Rotation with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Select rotated verification keys by identifier",
      "language": "python",
      "blurb": "A bounded, application-owned key ring supports overlap during rotation without trying every key.",
      "code": "from collections.abc import Mapping\nimport re\nfrom cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PublicKey\n\nKEY_ID = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,63}\")\nMAX_ACTIVE_KEYS = 32\n\ndef select_verification_key(\n    key_id: str,\n    active_keys: Mapping[str, Ed25519PublicKey],\n) -> Ed25519PublicKey:\n    if type(key_id) is not str or KEY_ID.fullmatch(key_id) is None:\n        raise ValueError(\"key identifier rejected\")\n    if not isinstance(active_keys, Mapping) or not 1 <= len(active_keys) <= MAX_ACTIVE_KEYS:\n        raise ValueError(\"active key policy rejected\")\n    try:\n        key = active_keys[key_id]\n    except KeyError:\n        raise ValueError(\"unknown signing key\") from None\n    if not isinstance(key, Ed25519PublicKey):\n        raise TypeError(\"Ed25519 verification key required\")\n    return key\n"
    },
    {
      "title": "Enforce certificate policy before using its public key",
      "language": "python",
      "blurb": "A platform or PKI library should validate the chain first; this code still enforces leaf policy, revocation stance, approved identity, key usage, and algorithm before verifying.",
      "code": "from collections.abc import Sequence\nfrom datetime import datetime, timezone\nfrom cryptography import x509\nfrom cryptography.hazmat.primitives import hashes\nfrom cryptography.hazmat.primitives.asymmetric import ec\nfrom cryptography.x509.oid import ObjectIdentifier\n\nDOCUMENT_SIGNING_EKU = ObjectIdentifier(\"1.3.6.1.4.1.55555.1.1\")\nMAX_DOCUMENT_BYTES = 1_000_000\nMAX_CHAIN_CERTIFICATES = 8\nMAX_APPROVED_LEAVES = 32\n\ndef verify_certificate_signature(\n    validated_chain: Sequence[x509.Certificate],\n    document: bytes,\n    signature: bytes,\n    now: datetime,\n    allowed_leaf_fingerprints: set[bytes],\n    revocation_checked: bool,\n) -> None:\n    if (\n        type(now) is not datetime\n        or now.tzinfo is None\n        or now.utcoffset() is None\n    ):\n        raise ValueError(\"timezone-aware validation time required\")\n    if (\n        type(validated_chain) not in {tuple, list}\n        or not 1 <= len(validated_chain) <= MAX_CHAIN_CERTIFICATES\n        or any(\n            not isinstance(certificate, x509.Certificate)\n            for certificate in validated_chain\n        )\n    ):\n        raise ValueError(\"validated certificate chain required\")\n    if revocation_checked is not True:\n        raise ValueError(\"revocation status must be checked by policy\")\n    if (\n        type(document) is not bytes\n        or not 1 <= len(document) <= MAX_DOCUMENT_BYTES\n        or type(signature) is not bytes\n        or not 8 <= len(signature) <= 144\n    ):\n        raise ValueError(\"document signature input rejected\")\n    if (\n        type(allowed_leaf_fingerprints) is not set\n        or not 1 <= len(allowed_leaf_fingerprints) <= MAX_APPROVED_LEAVES\n        or any(\n            type(fingerprint) is not bytes or len(fingerprint) != 32\n            for fingerprint in allowed_leaf_fingerprints\n        )\n    ):\n        raise ValueError(\"approved certificate policy rejected\")\n\n    leaf = validated_chain[0]\n    now_utc = now.astimezone(timezone.utc)\n    if not (\n        leaf.not_valid_before_utc\n        <= now_utc\n        <= leaf.not_valid_after_utc\n    ):\n        raise ValueError(\"certificate is outside its validity window\")\n    if leaf.fingerprint(hashes.SHA256()) not in allowed_leaf_fingerprints:\n        raise ValueError(\"certificate is not approved for this verifier\")\n    try:\n        key_usage = leaf.extensions.get_extension_for_class(\n            x509.KeyUsage\n        ).value\n        eku = leaf.extensions.get_extension_for_class(\n            x509.ExtendedKeyUsage\n        ).value\n    except x509.ExtensionNotFound as error:\n        raise ValueError(\n            \"certificate is missing signing policy extensions\"\n        ) from error\n    if not key_usage.digital_signature or DOCUMENT_SIGNING_EKU not in eku:\n        raise ValueError(\n            \"certificate is not approved for document signatures\"\n        )\n    public_key = leaf.public_key()\n    if (\n        not isinstance(public_key, ec.EllipticCurvePublicKey)\n        or public_key.key_size < 256\n    ):\n        raise TypeError(\"approved elliptic-curve signing certificate required\")\n    public_key.verify(\n        signature,\n        document,\n        ec.ECDSA(hashes.SHA256()),\n    )\n"
    }
  ]
};
