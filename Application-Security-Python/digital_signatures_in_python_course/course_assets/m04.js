window.COURSE_MODULE = {
  "title": "Signing Workflows and Data Preparation",
  "graphicAlt": "Sequence diagram for Signing Workflows and Data Preparation, showing signer, protected private key, message bytes, signature, and verifier public key; labeled arrows identify signing and verification boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "A signature is over bytes, so the first design question is exactly what data is being signed. Human-readable content, JSON-like structures, files, request bodies, headers, timestamps, and metadata can all be represented in multiple ways. If the signer and verifier do not agree on the exact byte representation, verification may fail. Worse, the application may verify one representation while acting on another.\n\nCanonicalization is the process of making data representation consistent before signing or verifying. It can involve field ordering, whitespace, character encoding, normalization, timestamp format, metadata selection, and message boundaries. Structured data is especially tricky because two documents may appear equivalent to a person but produce different bytes. The signing workflow should remove ambiguity before the signature is created.\n\nMessage boundaries matter. A system should know whether it signs only the body, selected headers, a timestamp, a request path, a method, a file digest, or a complete envelope. Detached signatures keep the signature separate from the signed data. Attached signature concepts bundle signature material with the content. Both can be valid patterns, but the application must understand what is protected and what remains outside the signature.\n\nSigning the wrong representation can create trust confusion. If a webhook signs only part of a request, unsigned fields may still influence behavior. If a token signs claims but the application trusts external parameters too, the signature may not protect the full decision. A good signing workflow documents the protected data, the unprotected context, and the business checks that happen after cryptographic verification succeeds.",
  "narrationPoints": [
    "A signature is over bytes, so the first design question is exactly what data is being signed.",
    "Worse, the application may verify one representation while acting on another.",
    "Structured data is especially tricky because two documents may appear equivalent to a person but produce different bytes.",
    "Attached signature concepts bundle signature material with the content.",
    "Signing the wrong representation can create trust confusion.",
    "A good signing workflow documents the protected data, the unprotected context, and the business checks that happen after cryptographic verification succeeds."
  ]
};
