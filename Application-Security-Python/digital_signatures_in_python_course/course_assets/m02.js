window.COURSE_MODULE = {
  "title": "Public Keys, Private Keys, and Trust",
  "graphicAlt": "Flowchart for Public Keys, Private Keys, and Trust, showing signer, protected private key, message bytes, signature, and verifier public key; labeled arrows identify signing and verification boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Digital signature systems are built around public and private key pairs. The private key creates signatures and must be protected. The public key verifies signatures and may be widely distributed. The security of the system depends on keeping the private key under the control of the intended signer and giving verifiers a trustworthy way to know which public key belongs to which signer.\n\nPublic key distribution is a trust problem, not just a file transfer problem. A verifier needs confidence that the public key it uses actually belongs to the expected signer. Trust anchors, key identifiers, certificates at a high level, and certificate chains at a high level are mechanisms for answering that question in different systems. A self-managed key can be appropriate in a closed environment, while externally issued certificates may be appropriate when broader trust is required.\n\nKey identifiers help applications select the right verification key, especially during rotation. They do not create trust by themselves. An attacker-controlled key with a familiar-looking identifier is not trustworthy just because the identifier matches a pattern. Verifiers need rules for which keys are accepted, where those keys come from, who owns them, and how changes are reviewed.\n\nA valid signature generally proves possession of the corresponding private key for the signed data under the chosen scheme. It does not prove the signer is allowed to perform a business action, that the signed content is safe, or that the message should be accepted forever. Trust decisions must connect key control, expected signer, intended use, freshness, and application policy. Otherwise any valid signature from any key can become a dangerously broad acceptance rule.",
  "narrationPoints": [
    "Digital signature systems are built around public and private key pairs.",
    "The public key verifies signatures and may be widely distributed.",
    "A verifier needs confidence that the public key it uses actually belongs to the expected signer.",
    "An attacker-controlled key with a familiar-looking identifier is not trustworthy just because the identifier matches a pattern.",
    "A valid signature generally proves possession of the corresponding private key for the signed data under the chosen scheme.",
    "Otherwise any valid signature from any key can become a dangerously broad acceptance rule."
  ]
};
