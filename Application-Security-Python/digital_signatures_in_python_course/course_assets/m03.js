window.COURSE_MODULE = {
  "title": "Signature Algorithms and Python Libraries",
  "graphicAlt": "Layered architecture diagram for Signature Algorithms and Python Libraries, showing signer, protected private key, message bytes, signature, and verifier public key; labeled arrows identify signing and verification boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Python developers may encounter RSA, ECDSA, EdDSA, and post-quantum signature concepts at a high level. Algorithm names matter, but they are not enough. Safe use depends on current vetted libraries, platform requirements, organizational policy, parameter choices, key handling, and protocol context. A once-common example may become obsolete or inappropriate as guidance changes, so durable designs rely on maintained libraries and reviewable policy rather than copied snippets.\n\nHigh-level cryptographic APIs are safer than assembling primitives manually. A mature library should guide developers toward appropriate signing and verification operations, handle important format details, and avoid low-level footguns. Developers should not design custom signature schemes, combine primitives by intuition, or adjust parameters without understanding the security impact. Custom cryptography tends to fail quietly before it fails obviously.\n\nSigning and verification code should be separated from ordinary business logic. A route handler, webhook receiver, package verifier, or token parser should call a central verification path rather than reimplementing checks in many places. Centralization makes it easier to enforce algorithm restrictions, key selection, error handling, logging, context validation, and future migration. It also helps reviewers inspect the actual trust boundary.\n\nSafe implementation is intentionally plain. The code should make clear what is signed, how the key is selected, which algorithms are accepted, how errors fail closed, and what context must be checked before the content is trusted. Libraries reduce risk, but they do not remove engineering responsibility. Developers still need to choose the right abstraction and apply it consistently across Python services, scripts, and integrations.",
  "narrationPoints": [
    "Python developers may encounter RSA, ECDSA, EdDSA, and post-quantum signature concepts at a high level.",
    "Safe use depends on current vetted libraries, platform requirements, organizational policy, parameter choices, key handling, and protocol context.",
    "Developers should not design custom signature schemes, combine primitives by intuition, or adjust parameters without understanding the security impact.",
    "Signing and verification code should be separated from ordinary business logic.",
    "Libraries reduce risk, but they do not remove engineering responsibility.",
    "Safe implementation is intentionally plain."
  ]
};
