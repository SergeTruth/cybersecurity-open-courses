window.COURSE_MODULE = {
  "title": "What Secure Template Rendering Means",
  "graphicAlt": "A server-owned template combines trusted markup with untrusted values at labeled HTML text, attribute, URL, JavaScript, JSON, and CSS sinks, each guarded by its context-specific control.",
  "narration": "Template rendering is the process of combining application data with presentation markup. In Python applications, that may happen through Django templates, Jinja templates in Flask, template rendering in FastAPI or Starlette, admin pages, email templates, reports, dashboards, or internal tools. The security question is not whether templates are useful. They are. The question is whether untrusted values remain ordinary data when they are placed into browser-facing or document-facing output.\n\nA template is different from a view, model, request, response, or browser execution context. Views gather data and make decisions. Models represent stored state. Requests bring in data from outside the application. Responses send output to users or systems. Browsers then interpret that output as HTML, attributes, URLs, scripts, styles, or documents. Secure rendering means keeping those responsibilities clear so user-controlled data does not become executable behavior or unsafe markup.\n\nTemplate rendering is security-sensitive whenever untrusted data reaches HTML, JavaScript-adjacent content, URL fields, attributes, CSS, emails, reports, or exported documents. A value that looks harmless in a database can become dangerous when placed into a new context. A support ticket, translated string, uploaded filename, partner field, or admin note can all become browser-facing output later. Safe rendering is about data flow, context, escaping, sanitization, and review.\n\nFramework defaults help, but they do not replace secure design. Autoescaping can reduce risk in common cases, yet developers can bypass it, mark content as safe, render untrusted template strings, or reuse data in a context the original code did not expect. A secure template strategy makes the safe path routine, makes exceptions visible, and treats every rendering path as a trust boundary worth understanding.",
  "narrationPoints": [
    "The question is whether untrusted values remain ordinary data when they are placed into browser-facing or document-facing output.",
    "A template is different from a view, model, request, response, or browser execution context.",
    "A value that looks harmless in a database can become dangerous when placed into a new context.",
    "Framework defaults help, but they do not replace secure design.",
    "Secure rendering means keeping those responsibilities clear so user-controlled data does not become executable behavior or unsafe markup.",
    "A support ticket, translated string, uploaded filename, partner field, or admin note can all become browser-facing output later."
  ]
};
