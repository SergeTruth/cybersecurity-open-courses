window.COURSE_MODULE = {
  "title": "Template Injection and Unsafe Dynamic Rendering",
  "graphicAlt": "Caller text is blocked from the template compiler while a fixed document type selects one installed template; an audit inspects imports, inheritance, and dynamic includes before deployment.",
  "narration": "Template injection is a risk that appears when untrusted input is treated as template code rather than ordinary data. At a high level, server-side template injection involves a server-side template engine interpreting user-influenced text as template logic. Client-side template injection follows a similar concern in browser-side rendering systems. This course avoids attack details, but the defensive lesson is clear: template source should be trusted and controlled.\n\nA safe design separates template authors from application users. Application developers, reviewed content editors, or controlled build processes may provide templates. Ordinary users, partners, imported data, and request parameters should provide values. When these roles are blurred, the rendering engine may expose behavior, data, helper functions, or environment capabilities that were never meant for untrusted control.\n\nSandboxing concepts can reduce risk in specialized systems, but they have limitations and should not be treated as permission to execute arbitrary untrusted template logic. Rendering environments should follow least privilege. Helpers, globals, filters, file access, network access, and sensitive objects should be restricted to what the rendering task actually needs. The safest approach is still prevention: do not turn untrusted strings into executable template logic.\n\nTrying to filter dangerous template syntax after the fact is fragile because template languages are expressive and rendering behavior changes across engines and versions. Defensive code should avoid user-controlled template source, avoid broad helper exposure, and review any dynamic rendering feature as a high-risk design decision. Untrusted template execution is not ordinary string formatting. It is a boundary that requires explicit architecture and controls.",
  "narrationPoints": [
    "At a high level, server-side template injection involves a server-side template engine interpreting user-influenced text as template logic.",
    "Application developers, reviewed content editors, or controlled build processes may provide templates.",
    "The safest approach is still prevention: do not turn untrusted strings into executable template logic.",
    "Defensive code should avoid user-controlled template source, avoid broad helper exposure, and review any dynamic rendering feature as a high-risk design decision.",
    "Template injection is a risk that appears when untrusted input is treated as template code rather than ordinary data.",
    "Helpers, globals, filters, file access, network access, and sensitive objects should be restricted to what the rendering task actually needs."
  ]
};
