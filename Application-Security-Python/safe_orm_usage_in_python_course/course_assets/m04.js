window.COURSE_MODULE = {
  "title": "Dynamic Filters, Sorting, and Search",
  "graphicAlt": "Sequence diagram for Dynamic Filters, Sorting, and Search, showing request DTO, authorization-aware repository, ORM session, tenant filter, database, and minimized response model; labeled arrows identify query and object-authorization boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Dynamic query behavior is common. Users may choose optional filters, search fields, sort fields, sort direction, page size, date ranges, or report columns. Administrators may build saved reports. Internal tools may expose broad search features. These are legitimate requirements, but they should be constrained by explicit rules. The application should not allow request data to choose arbitrary model attributes, SQL fragments, relationship paths, or expressions.\n\nA safe pattern maps user choices to approved internal field names or query functions. For example, a public API might accept a small set of sort options and map each option to a known model field. A report builder might expose predefined filters rather than free-form query syntax. A search feature might select from known searchable fields and apply consistent normalization. This keeps the user-facing feature flexible while keeping the query structure under developer control.\n\nPagination and limits are part of dynamic query safety. Unbounded result sets can create availability and privacy problems. A list endpoint that allows broad filters, nested relationship expansion, or arbitrary page sizes can expose more data than intended even if no injection flaw exists. Safe dynamic queries combine constrained structure, authorization-aware filtering, reasonable limits, and predictable response shapes.\n\nAdministrative queries deserve the same discipline as public APIs. Admin tools often run with more privilege and expose more fields, so unsafe dynamic behavior can have a larger impact. Teams should review admin filters, exports, dashboard builders, and support search tools for direct user control over attributes, relationships, and raw conditions. Dynamic behavior should be powerful because it is well designed, not because it accepts whatever string a caller sends.",
  "narrationPoints": [
    "Users may choose optional filters, search fields, sort fields, sort direction, page size, date ranges, or report columns.",
    "A report builder might expose predefined filters rather than free-form query syntax.",
    "Unbounded result sets can create availability and privacy problems.",
    "Administrative queries deserve the same discipline as public APIs.",
    "Dynamic behavior should be powerful because it is well designed, not because it accepts whatever string a caller sends.",
    "Internal tools may expose broad search features."
  ]
};
