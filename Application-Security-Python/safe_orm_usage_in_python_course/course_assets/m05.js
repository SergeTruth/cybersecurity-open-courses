window.COURSE_MODULE = {
  "title": "Authorization-Aware ORM Access",
  "graphicAlt": "Decision diagram for Authorization-Aware ORM Access, showing request DTO, authorization-aware repository, ORM session, tenant filter, database, and minimized response model; labeled arrows identify query and object-authorization boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Authentication and authorization are different. Authentication identifies the caller. Authorization decides what the caller may access or do. ORM access must include authorization because a query can be technically safe and still return data the caller should not see. Every sensitive data operation should verify the subject, action, resource, and context before returning or changing records.\n\nObject-level authorization is central to ORM safety. A user may be allowed to access a path or endpoint, but not every record behind it. Queries should filter by the current user, role, tenant, organization, project, permission, workflow state, or ownership as appropriate. List endpoints are especially important because overbroad queries can quietly expose many records at once. Detail endpoints, updates, deletes, and exports need the same resource-specific checks.\n\nTenant and organization boundaries should be built into query patterns rather than added by memory. Multi-tenant applications need consistent filters so one tenant cannot read or modify another tenant's records. Support and administrative access should have explicit rules, logging, and review. Hidden buttons, hidden routes, and client-side filtering are not access controls. Server-side ORM queries should return only what the current context is allowed to see.\n\nPreventing overbroad queries is partly architecture and partly testing. Sensitive repository functions should require context, not allow callers to fetch everything and filter later. Tests should confirm denied cases, cross-tenant isolation, role limits, object ownership, and administrative exceptions. When authorization-aware filters are embedded in the data-access layer, developers are less likely to forget them in new routes, jobs, and reports.",
  "narrationPoints": [
    "Authorization decides what the caller may access or do.",
    "Queries should filter by the current user, role, tenant, organization, project, permission, workflow state, or ownership as appropriate.",
    "Tenant and organization boundaries should be built into query patterns rather than added by memory.",
    "Server-side ORM queries should return only what the current context is allowed to see.",
    "Tests should confirm denied cases, cross-tenant isolation, role limits, object ownership, and administrative exceptions.",
    "Object-level authorization is central to ORM safety."
  ]
};
