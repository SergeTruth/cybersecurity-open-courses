window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "Requires Python 3.10+, SQLAlchemy 2.x, PostgreSQL 18, and psycopg 3. These examples apply Raw SQL, Migrations, and Advanced ORM Features with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Allowlist report structure and enforce permission",
      "language": "python",
      "blurb": "This PostgreSQL-specific facade requires an authenticated subject with the order:report permission, derives the tenant from that trusted context, and assumes orders.created_at is timestamptz. Daily buckets are explicitly UTC, and the query bounds records to the half-open interval from the validated start through the trusted current time, for a maximum 366-day window. A five-second transaction-local timeout bounds server execution, while fetching one extra group lets the structured result disclose truncation.",
      "code": "from collections.abc import Mapping\nfrom dataclasses import dataclass\nfrom datetime import datetime, timedelta, timezone\nfrom types import MappingProxyType\n\nfrom sqlalchemy import text\n\n# PostgreSQL schema contract: orders.created_at is timestamp with time zone.\nREPORT_COLUMNS = MappingProxyType(\n    {\"daily\": \"date(created_at AT TIME ZONE 'UTC')\", \"owner\": \"owner_id\"}\n)\nREPORT_PERMISSION = \"order:report\"\nMAX_IDENTIFIER_LENGTH = 64\nMAX_PERMISSION_COUNT = 128\nMAX_REPORT_ROWS = 1000\nMAX_REPORT_LOOKBACK = timedelta(days=366)\n\n@dataclass(frozen=True, slots=True)\nclass ReportResult:\n    rows: tuple[Mapping[str, object], ...]\n    truncated: bool\n\ndef build_order_report(\n    session_factory, current_authenticated_principal, current_time\n):\n    \"\"\"Called once by the trusted composition root, not per request.\"\"\"\n\n    if not all(\n        callable(value)\n        for value in (\n            session_factory, current_authenticated_principal, current_time\n        )\n    ):\n        raise TypeError(\"trusted report dependencies are required\")\n\n    def run_order_report(*, grouping: object, since: object) -> ReportResult:\n        if type(grouping) is not str or not 1 <= len(grouping) <= 16:\n            raise ValueError(\"report grouping is not supported\")\n        try:\n            column = REPORT_COLUMNS[grouping]\n        except KeyError:\n            raise ValueError(\"report grouping is not supported\") from None\n        if (\n            type(since) is not datetime\n            or since.tzinfo is None\n            or since.utcoffset() is None\n        ):\n            raise ValueError(\"since must be a timezone-aware datetime\")\n\n        now = current_time()\n        if (\n            type(now) is not datetime\n            or now.tzinfo is None\n            or now.utcoffset() is None\n        ):\n            raise RuntimeError(\"trusted clock returned an invalid datetime\")\n        since_utc = since.astimezone(timezone.utc)\n        now_utc = now.astimezone(timezone.utc)\n        if since_utc > now_utc or since_utc < now_utc - MAX_REPORT_LOOKBACK:\n            raise ValueError(\"since is outside the permitted reporting window\")\n\n        principal = current_authenticated_principal()\n        tenant_id = getattr(principal, \"tenant_id\", None)\n        subject_id = getattr(principal, \"subject_id\", None)\n        permissions = getattr(principal, \"permissions\", None)\n        if any(\n            type(value) is not str or not 1 <= len(value) <= MAX_IDENTIFIER_LENGTH\n            for value in (tenant_id, subject_id)\n        ):\n            raise PermissionError(\"authenticated principal is invalid\")\n        if (\n            type(permissions) is not frozenset\n            or len(permissions) > MAX_PERMISSION_COUNT\n            or any(\n                type(permission) is not str\n                or not 1 <= len(permission) <= MAX_IDENTIFIER_LENGTH\n                for permission in permissions\n            )\n            or REPORT_PERMISSION not in permissions\n        ):\n            raise PermissionError(\"order report is not permitted\")\n\n        statement = text(\n            f\"SELECT {column} AS bucket, count(*) AS total FROM orders \"\n            f\"WHERE tenant_id = :tenant_id \"\n            f\"AND created_at >= :since AND created_at < :until \"\n            f\"GROUP BY {column} ORDER BY {column} LIMIT {MAX_REPORT_ROWS + 1}\"\n        )\n        with session_factory() as session:\n            # PostgreSQL SET LOCAL lasts only for this transaction.\n            session.execute(text(\"SET LOCAL statement_timeout = '5000ms'\"))\n            fetched = tuple(\n                session.execute(\n                    statement,\n                    {\n                        \"tenant_id\": tenant_id,\n                        \"since\": since_utc,\n                        \"until\": now_utc,\n                    },\n                ).mappings()\n            )\n        return ReportResult(\n            rows=fetched[:MAX_REPORT_ROWS],\n            truncated=len(fetched) > MAX_REPORT_ROWS,\n        )\n\n    return run_order_report\n"
    },
    {
      "title": "Use a transaction for a state transition",
      "language": "python",
      "blurb": "One PostgreSQL conditional update checks tenant, active approval permission, and pending state atomically. RETURNING identifies the updated invoice without relying on DBAPI rowcount, and the authenticated audit record shares the commit.",
      "code": "from dataclasses import dataclass\n\nfrom sqlalchemy import exists, update\nfrom sqlalchemy.exc import MultipleResultsFound\n\nMAX_IDENTIFIER_LENGTH = 64\n\n@dataclass(frozen=True, slots=True)\nclass AuthenticatedPrincipal:\n    \"\"\"Produced by trusted authentication middleware, never request data.\"\"\"\n\n    subject_id: str\n    tenant_id: str\n\n    def __post_init__(self) -> None:\n        for value in (self.subject_id, self.tenant_id):\n            if type(value) is not str or not 1 <= len(value) <= MAX_IDENTIFIER_LENGTH:\n                raise ValueError(\"authenticated identity is invalid\")\n\ndef build_invoice_approver(\n    session_factory,\n    Invoice,\n    ApprovalGrant,\n    Audit,\n    current_authenticated_principal,\n):\n    \"\"\"Called once by the trusted composition root, not per request.\"\"\"\n\n    if not callable(current_authenticated_principal):\n        raise TypeError(\"authenticated-principal provider is required\")\n\n    def approve_invoice(*, invoice_id: object) -> bool:\n        principal = current_authenticated_principal()\n        if type(principal) is not AuthenticatedPrincipal:\n            raise PermissionError(\"authenticated principal is required\")\n        if (\n            type(invoice_id) is not str\n            or not 1 <= len(invoice_id) <= MAX_IDENTIFIER_LENGTH\n        ):\n            raise ValueError(\"invoice_id is invalid\")\n\n        with session_factory.begin() as session:\n            active_grant = exists().where(\n                ApprovalGrant.tenant_id == principal.tenant_id,\n                ApprovalGrant.subject_id == principal.subject_id,\n                ApprovalGrant.permission == \"invoice:approve\",\n                ApprovalGrant.revoked_at.is_(None),\n            )\n            statement = (\n                update(Invoice)\n                .where(\n                    Invoice.id == invoice_id,\n                    Invoice.tenant_id == principal.tenant_id,\n                    Invoice.state == \"pending\",\n                    active_grant,\n                )\n                .values(state=\"approved\")\n                .returning(Invoice.id)\n                .execution_options(synchronize_session=False)\n            )\n            try:\n                updated_id = session.scalars(statement).one_or_none()\n            except MultipleResultsFound:\n                raise RuntimeError(\n                    \"invoice uniqueness invariant was violated\"\n                ) from None\n            if updated_id is None:\n                # Missing, unauthorized, and non-pending invoices share one result.\n                return False\n            if type(updated_id) is not str or updated_id != invoice_id:\n                raise RuntimeError(\"invoice identifier invariant was violated\")\n            session.add(\n                Audit(\n                    tenant_id=principal.tenant_id,\n                    event=\"invoice_approved\",\n                    resource_id=updated_id,\n                    actor_id=principal.subject_id,\n                )\n            )\n        return True\n\n    return approve_invoice\n"
    }
  ]
};
