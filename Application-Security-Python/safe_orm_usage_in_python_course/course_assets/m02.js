window.COURSE_MODULE = {
  "title": "Models, Boundaries, and Data Flow",
  "graphicAlt": "Flowchart for Models, Boundaries, and Data Flow, showing request DTO, authorization-aware repository, ORM session, tenant filter, database, and minimized response model; labeled arrows identify query and object-authorization boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "ORM models are application-facing representations of database tables, records, and relationships. They can make domain concepts visible in code: users, tenants, organizations, projects, invoices, messages, files, roles, and events. Model design should reflect more than column names. It should capture ownership, sensitivity, lifecycle, relationships, and business rules that affect how data is created, changed, queried, and exposed.\n\nData flowing into models can come from trusted and untrusted sources. Request bodies, form fields, API payloads, uploaded files, imported spreadsheets, partner responses, administrative edits, and background jobs may all influence model values or query choices. Stored data can later be reused in queries, reports, exports, or templates. A value should not become trusted merely because it has been saved to a database.\n\nValidation boundaries should be clear. Some rules belong at the request or form layer because they depend on the action being performed. Some rules belong near the model because they protect data consistency. Some rules belong in the service or repository layer because they combine identity, permissions, workflow state, and persistence. Teams should be able to explain which layer enforces which assumption instead of hoping the ORM enforces everything implicitly.\n\nService and repository layer concepts can help keep database access explicit and reviewable. Instead of scattering complex queries across views, jobs, serializers, and admin actions, teams can centralize sensitive operations in named functions that include validation, authorization, tenant filtering, transaction behavior, and logging expectations. The exact architecture can vary, but safe ORM design makes important data flows easy to find and hard to bypass accidentally.",
  "narrationPoints": [
    "ORM models are application-facing representations of database tables, records, and relationships.",
    "Data flowing into models can come from trusted and untrusted sources.",
    "A value should not become trusted merely because it has been saved to a database.",
    "Some rules belong in the service or repository layer because they combine identity, permissions, workflow state, and persistence.",
    "Service and repository layer concepts can help keep database access explicit and reviewable.",
    "Validation boundaries should be clear."
  ]
};
