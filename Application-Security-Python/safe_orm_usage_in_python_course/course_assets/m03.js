window.COURSE_MODULE = {
  "title": "Query Safety and Parameter Binding",
  "graphicAlt": "Layered architecture diagram for Query Safety and Parameter Binding, showing request DTO, authorization-aware repository, ORM session, tenant filter, database, and minimized response model; labeled arrows identify query and object-authorization boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Safe query APIs are one of the main reasons developers use ORMs. Filtering by values, creating records, updating fields, deleting records, joining relationships, and paginating results can often be expressed without hand-built SQL strings. The ORM or database driver can bind values separately from query structure. That separation is what prevents ordinary data values from being interpreted as database command syntax.\n\nParameter binding remains important even when using an ORM. When developers stay within normal value-based filter methods and prepared statement concepts, the data layer can pass values safely to the database driver. When developers build raw fragments, concatenate conditions, or insert user-controlled strings into query methods that expect structure, they may leave the safe path. Safe ORM use depends on knowing which API accepts values and which API accepts query structure.\n\nSELECT, INSERT, UPDATE, and DELETE operations all need safe construction. Bulk operations deserve special care because they can affect many records and may bypass model hooks, validation paths, or per-object checks depending on the ORM and usage pattern. Query composition also needs review. A helper that adds filters, joins, or conditions should preserve parameter binding and authorization assumptions, not quietly replace them with unsafe string assembly.\n\nThe safest standard is simple: use structured ORM APIs for values, use parameters for raw SQL values, and avoid mixing untrusted data into query structure. If a feature genuinely needs dynamic structure, such as selecting a sort column or report field, that decision belongs in an allowlisted mapping, not in raw user input. Safe query construction should be the default behavior in every data-access path.",
  "narrationPoints": [
    "Safe query APIs are one of the main reasons developers use ORMs.",
    "The ORM or database driver can bind values separately from query structure.",
    "When developers stay within normal value-based filter methods and prepared statement concepts, the data layer can pass values safely to the database driver.",
    "Safe ORM use depends on knowing which API accepts values and which API accepts query structure.",
    "A helper that adds filters, joins, or conditions should preserve parameter binding and authorization assumptions, not quietly replace them with unsafe string assembly.",
    "Query composition also needs review."
  ]
};
