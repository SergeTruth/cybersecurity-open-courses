window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "Requires Python 3.10+ and SQLAlchemy 2.x with an appropriate DBAPI driver. These examples apply Mass Assignment, Serialization, and Data Exposure with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Validate and allowlist mutable profile fields",
      "language": "python",
      "blurb": "An upstream request or import decoder must first enforce an aggregate byte limit. The validator rejects more fields than the three-field contract permits before scanning keys, applies exact scalar, normalization, and allowlist policies, and returns a read-only copy so fields cannot be added or replaced after validation. This generic display-name policy rejects every Unicode default-ignorable character; a localized product should add join controls only with script-aware contextual validation.",
      "code": "import unicodedata\nfrom collections.abc import Mapping\nfrom types import MappingProxyType\n\nWRITABLE_PROFILE_FIELDS = frozenset({\"display_name\", \"timezone\", \"locale\"})\nLOCALES = frozenset({\"en-US\", \"en-GB\", \"es-ES\", \"fr-FR\"})\nTIMEZONES = frozenset({\"UTC\", \"America/New_York\", \"Europe/London\", \"Europe/Paris\"})\nMAX_DISPLAY_NAME_INPUT_LENGTH = 80\nMAX_TIMEZONE_INPUT_LENGTH = 64\nMAX_LOCALE_INPUT_LENGTH = 16\n# Unicode 17.0 Default_Ignorable_Code_Point ranges. This policy approves no\n# ZWJ or ZWNJ exception; add one only with contextual script validation.\nDEFAULT_IGNORABLE_RANGES = (\n    (0x00AD, 0x00AD), (0x034F, 0x034F), (0x061C, 0x061C),\n    (0x115F, 0x1160), (0x17B4, 0x17B5), (0x180B, 0x180F),\n    (0x200B, 0x200F), (0x202A, 0x202E), (0x2060, 0x206F),\n    (0x3164, 0x3164), (0xFE00, 0xFE0F), (0xFEFF, 0xFEFF),\n    (0xFFA0, 0xFFA0), (0xFFF0, 0xFFF8), (0x1BCA0, 0x1BCA3),\n    (0x1D173, 0x1D17A), (0xE0000, 0xE0FFF),\n)\n\ndef is_default_ignorable(character: str) -> bool:\n    code_point = ord(character)\n    return any(start <= code_point <= end for start, end in DEFAULT_IGNORABLE_RANGES)\n\ndef has_disallowed_text_character(value: str) -> bool:\n    return any(\n        unicodedata.category(character) in {\"Cc\", \"Cs\", \"Zl\", \"Zp\"}\n        or is_default_ignorable(character)\n        for character in value\n    )\n\ndef profile_changes(payload: dict[str, object]) -> Mapping[str, str]:\n    if type(payload) is not dict:\n        raise TypeError(\"profile update must be an object\")\n    if len(payload) > len(WRITABLE_PROFILE_FIELDS):\n        raise ValueError(\"profile contains too many fields\")\n    if any(type(name) is not str for name in payload):\n        raise TypeError(\"profile field names must be strings\")\n    if any(name not in WRITABLE_PROFILE_FIELDS for name in payload):\n        raise ValueError(\"profile contains fields that are not writable\")\n\n    changes: dict[str, str] = {}\n    for name in (\"display_name\", \"timezone\", \"locale\"):\n        if name not in payload:\n            continue\n        value = payload[name]\n        if type(value) is not str:\n            raise TypeError(f\"{name} must be a string\")\n        changes[name] = value\n\n    if \"display_name\" in changes:\n        value = changes[\"display_name\"]\n        if (\n            not 1 <= len(value) <= MAX_DISPLAY_NAME_INPUT_LENGTH\n            or has_disallowed_text_character(value)\n        ):\n            raise ValueError(\"display name is invalid\")\n        normalized = unicodedata.normalize(\"NFC\", value.strip())\n        if not 1 <= len(normalized) <= MAX_DISPLAY_NAME_INPUT_LENGTH:\n            raise ValueError(\"display name is invalid\")\n        changes[\"display_name\"] = normalized\n\n    if \"timezone\" in changes:\n        value = changes[\"timezone\"]\n        if (\n            not 1 <= len(value) <= MAX_TIMEZONE_INPUT_LENGTH\n            or value not in TIMEZONES\n        ):\n            raise ValueError(\"timezone is not supported\")\n\n    if \"locale\" in changes:\n        value = changes[\"locale\"]\n        if (\n            not 1 <= len(value) <= MAX_LOCALE_INPUT_LENGTH\n            or value not in LOCALES\n        ):\n            raise ValueError(\"locale is not supported\")\n    return MappingProxyType(changes.copy())\n"
    },
    {
      "title": "Serialize an explicit ORM response",
      "language": "python",
      "blurb": "The response projection names public fields, validates and normalizes stored display names with the same control and default-ignorable policy used on input, and does not traverse relationships or expose internal columns automatically. The identifier policy shown here treats public IDs as canonical ASCII opaque strings.",
      "code": "import re\nimport unicodedata\nfrom dataclasses import dataclass\n\nPUBLIC_ID_PATTERN = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,63}\")\nPUBLIC_ACCOUNT_STATES = frozenset({\"active\", \"disabled\"})\nMAX_PUBLIC_DISPLAY_NAME_LENGTH = 80\nDEFAULT_IGNORABLE_RANGES = (\n    (0x00AD, 0x00AD), (0x034F, 0x034F), (0x061C, 0x061C),\n    (0x115F, 0x1160), (0x17B4, 0x17B5), (0x180B, 0x180F),\n    (0x200B, 0x200F), (0x202A, 0x202E), (0x2060, 0x206F),\n    (0x3164, 0x3164), (0xFE00, 0xFE0F), (0xFEFF, 0xFEFF),\n    (0xFFA0, 0xFFA0), (0xFFF0, 0xFFF8), (0x1BCA0, 0x1BCA3),\n    (0x1D173, 0x1D17A), (0xE0000, 0xE0FFF),\n)\n\ndef is_default_ignorable(character: str) -> bool:\n    code_point = ord(character)\n    return any(start <= code_point <= end for start, end in DEFAULT_IGNORABLE_RANGES)\n\ndef normalize_public_display_name(value: object) -> str:\n    if type(value) is not str or not 1 <= len(value) <= MAX_PUBLIC_DISPLAY_NAME_LENGTH:\n        raise ValueError(\"stored public display name is invalid\")\n    if any(\n        unicodedata.category(character) in {\"Cc\", \"Cs\", \"Zl\", \"Zp\"}\n        or is_default_ignorable(character)\n        for character in value\n    ):\n        raise ValueError(\"stored public display name is invalid\")\n    normalized = unicodedata.normalize(\"NFC\", value.strip())\n    if not 1 <= len(normalized) <= MAX_PUBLIC_DISPLAY_NAME_LENGTH:\n        raise ValueError(\"stored public display name is invalid\")\n    return normalized\n\n@dataclass(frozen=True, slots=True)\nclass AccountResponse:\n    id: str\n    display_name: str\n    state: str\n\n    def __post_init__(self) -> None:\n        if type(self.id) is not str or PUBLIC_ID_PATTERN.fullmatch(self.id) is None:\n            raise ValueError(\"stored public account identifier is invalid\")\n        object.__setattr__(\n            self,\n            \"display_name\",\n            normalize_public_display_name(self.display_name),\n        )\n        if type(self.state) is not str or self.state not in PUBLIC_ACCOUNT_STATES:\n            raise ValueError(\"stored public account state is invalid\")\n\n    @classmethod\n    def from_model(cls, account):\n        return cls(\n            id=account.public_id,\n            display_name=account.display_name,\n            state=account.state,\n        )\n"
    }
  ]
};
