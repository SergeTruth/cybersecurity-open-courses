window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Testing, Rollout, Monitoring, and Maintenance with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Regression-test security headers",
      "language": "python",
      "blurb": "The test asserts route-specific values and core CSP directives, catching rollout conflicts and accidental CSP overwrites.",
      "code": "def test_account_headers(client):\n    response = client.get(\"/account\")\n    assert response.headers[\"Strict-Transport-Security\"].startswith(\"max-age=\")\n    assert response.headers[\"X-Content-Type-Options\"] == \"nosniff\"\n    csp = response.headers[\"Content-Security-Policy\"]\n    assert \"default-src 'self'\" in csp\n    assert \"object-src 'none'\" in csp\n    assert \"base-uri 'none'\" in csp\n    assert \"script-src 'self'\" in csp and \"'nonce-\" in csp\n    assert \"frame-ancestors 'none'\" in csp\n    assert response.headers[\"Cache-Control\"] == \"no-store\"\n    assert \"Content-Security-Policy-Report-Only\" not in response.headers\n"
    },
    {
      "title": "Validate CSP reports and map metrics to finite categories",
      "language": "python",
      "blurb": "Malformed, oversized, or invalid reports receive a controlled 400 response, while legacy CSP reports and Reporting API batches map unexpected values to finite metric tags.",
      "code": "import json\n\nKNOWN_CSP_DIRECTIVES = {\n    \"base-uri\", \"connect-src\", \"default-src\", \"font-src\", \"form-action\",\n    \"frame-ancestors\", \"frame-src\", \"img-src\", \"media-src\", \"object-src\",\n    \"script-src\", \"script-src-elem\", \"style-src\", \"style-src-elem\",\n}\nKNOWN_CSP_DISPOSITIONS = {\"enforce\", \"report\"}\nCSP_REPORT_TYPES = {\"application/csp-report\", \"application/reports+json\"}\n\ndef media_type(content_type: str) -> str:\n    return (content_type or \"\").split(\";\", 1)[0].strip().lower()\n\ndef csp_report_bodies(document):\n    if isinstance(document, dict) and isinstance(document.get(\"csp-report\"), dict):\n        return [document[\"csp-report\"]]\n    if not isinstance(document, list):\n        return []\n    reports = []\n    for item in document:\n        if not isinstance(item, dict):\n            continue\n        if item.get(\"type\") not in {\"csp-violation\", \"csp-report\"}:\n            continue\n        body = item.get(\"body\")\n        if isinstance(body, dict):\n            reports.append(body)\n    return reports\n\ndef report_field(report: dict, *names: str) -> str | None:\n    for name in names:\n        value = report.get(name)\n        if isinstance(value, str):\n            return value\n    return None\n\ndef record_csp_report(request, metrics) -> tuple[str, int]:\n    if media_type(request.content_type) not in CSP_REPORT_TYPES:\n        return \"invalid report\", 400\n    if request.content_length is not None and request.content_length > 16_384:\n        return \"invalid report\", 400\n    raw = request.get_data(cache=False, as_text=False)\n    if len(raw) > 16_384:\n        return \"invalid report\", 400\n    try:\n        document = json.loads(raw)\n    except (UnicodeDecodeError, json.JSONDecodeError, TypeError):\n        return \"invalid report\", 400\n    reports = csp_report_bodies(document)\n    if not reports:\n        return \"invalid report\", 400\n    for report in reports:\n        directive = report_field(report, \"violated-directive\", \"effective-directive\", \"effectiveDirective\")\n        disposition = report_field(report, \"disposition\")\n        if not directive or not disposition:\n            return \"invalid report\", 400\n        metrics.increment(\n            \"csp_violation\",\n            tags={\n                \"directive\": directive if directive in KNOWN_CSP_DIRECTIVES else \"other\",\n                \"disposition\": disposition if disposition in KNOWN_CSP_DISPOSITIONS else \"other\",\n            },\n        )\n    return \"\", 204\n"
    }
  ]
};
