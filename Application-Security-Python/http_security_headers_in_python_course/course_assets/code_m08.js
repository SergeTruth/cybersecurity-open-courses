window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Testing, Rollout, Monitoring, and Maintenance with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Regression-test security headers",
      "language": "python",
      "blurb": "The test asserts route-specific values and core CSP directives, catching rollout conflicts and accidental CSP overwrites.",
      "code": "def test_account_headers(client):\n    response = client.get(\"/account\")\n    assert response.headers[\"Strict-Transport-Security\"].startswith(\"max-age=\")\n    assert response.headers[\"X-Content-Type-Options\"] == \"nosniff\"\n    csp = response.headers[\"Content-Security-Policy\"]\n    assert \"default-src 'self'\" in csp\n    assert \"object-src 'none'\" in csp\n    assert \"base-uri 'none'\" in csp\n    assert \"script-src 'self'\" in csp and \"'nonce-\" in csp\n    assert \"frame-ancestors 'none'\" in csp\n    assert response.headers[\"Cache-Control\"] == \"no-store\"\n    assert \"Content-Security-Policy-Report-Only\" not in response.headers\n"
    },
    {
      "title": "Validate CSP reports and map metrics to finite categories",
      "language": "python",
      "blurb": "Malformed, oversized, or invalid reports receive a controlled 400 response, while legacy CSP reports and Reporting API batches map unexpected values to finite metric tags.",
      "code": "import json\n\nKNOWN_CSP_DIRECTIVES = {\n    \"base-uri\", \"connect-src\", \"default-src\", \"font-src\", \"form-action\",\n    \"frame-ancestors\", \"frame-src\", \"img-src\", \"media-src\", \"object-src\",\n    \"script-src\", \"script-src-elem\", \"style-src\", \"style-src-elem\",\n}\nKNOWN_CSP_DISPOSITIONS = {\"enforce\", \"report\"}\nCSP_REPORT_TYPES = {\"application/csp-report\", \"application/reports+json\"}\nMAX_CSP_REPORT_BYTES = 16_384\nMAX_CSP_JSON_DEPTH = 16\n\ndef media_type(content_type: object) -> str:\n    if type(content_type) is not str:\n        return \"\"\n    return content_type.split(\";\", 1)[0].strip().lower()\n\ndef unique_json_object(pairs):\n    result = {}\n    for key, value in pairs:\n        if key in result:\n            raise ValueError(\"duplicate CSP report member\")\n        result[key] = value\n    return result\n\ndef json_depth_is_bounded(raw: bytes) -> bool:\n    depth = 0\n    in_string = False\n    escaped = False\n    for byte in raw:\n        if in_string:\n            if escaped:\n                escaped = False\n            elif byte == 0x5C:\n                escaped = True\n            elif byte == 0x22:\n                in_string = False\n            continue\n        if byte == 0x22:\n            in_string = True\n        elif byte in (0x5B, 0x7B):\n            depth += 1\n            if depth > MAX_CSP_JSON_DEPTH:\n                return False\n        elif byte in (0x5D, 0x7D) and depth > 0:\n            depth -= 1\n    return True\n\ndef csp_report_bodies(document):\n    if isinstance(document, dict) and isinstance(document.get(\"csp-report\"), dict):\n        return [document[\"csp-report\"]]\n    if not isinstance(document, list) or len(document) > 100:\n        return []\n    reports = []\n    for item in document:\n        if not isinstance(item, dict):\n            continue\n        if item.get(\"type\") not in {\"csp-violation\", \"csp-report\"}:\n            continue\n        body = item.get(\"body\")\n        if isinstance(body, dict):\n            reports.append(body)\n    return reports\n\ndef report_field(report: dict, *names: str) -> str | None:\n    for name in names:\n        value = report.get(name)\n        if type(value) is str:\n            return value\n    return None\n\ndef record_csp_report(request, metrics) -> tuple[str, int]:\n    try:\n        content_type = request.content_type\n        content_length = request.content_length\n    except Exception:\n        return \"invalid report\", 400\n    if media_type(content_type) not in CSP_REPORT_TYPES:\n        return \"invalid report\", 400\n    if (\n        content_length is not None\n        and (\n            type(content_length) is not int\n            or not 1 <= content_length <= MAX_CSP_REPORT_BYTES\n        )\n    ):\n        return \"invalid report\", 400\n    try:\n        raw = request.get_data(cache=False, as_text=False)\n    except Exception:\n        return \"invalid report\", 400\n    if (\n        type(raw) is not bytes\n        or not 1 <= len(raw) <= MAX_CSP_REPORT_BYTES\n        or not json_depth_is_bounded(raw)\n    ):\n        return \"invalid report\", 400\n    try:\n        document = json.loads(\n            raw.decode(\"utf-8\"),\n            object_pairs_hook=unique_json_object,\n            parse_constant=lambda value: (_ for _ in ()).throw(\n                ValueError(\"invalid CSP report number\")\n            ),\n        )\n    except (UnicodeDecodeError, json.JSONDecodeError, RecursionError, ValueError):\n        return \"invalid report\", 400\n    reports = csp_report_bodies(document)\n    if not reports:\n        return \"invalid report\", 400\n    for report in reports:\n        directive = report_field(\n            report,\n            \"violated-directive\",\n            \"effective-directive\",\n            \"effectiveDirective\",\n        )\n        disposition = report_field(report, \"disposition\")\n        if not directive or not disposition:\n            return \"invalid report\", 400\n        metrics.increment(\n            \"csp_violation\",\n            tags={\n                \"directive\": (\n                    directive if directive in KNOWN_CSP_DIRECTIVES else \"other\"\n                ),\n                \"disposition\": (\n                    disposition\n                    if disposition in KNOWN_CSP_DISPOSITIONS\n                    else \"other\"\n                ),\n            },\n        )\n    return \"\", 204\n"
    }
  ]
};
