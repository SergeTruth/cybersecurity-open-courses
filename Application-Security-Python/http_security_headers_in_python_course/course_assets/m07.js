window.COURSE_MODULE = {
  "title": "Cookies, Caching, and Sensitive Responses",
  "graphicAlt": "Control-flow diagram for Cookies, Caching, and Sensitive Responses, showing Python response pipeline, reverse proxy, browser, document, iframe, cache, and cookie jar; labeled arrows identify where browser security policies are enforced and the point where unsafe input or behavior is rejected.",
  "narration": "Cookies carry important security behavior through attributes such as Secure, HttpOnly, SameSite, domain, path, and expiration. Authentication cookies and session cookies should be scoped deliberately. Secure aligns cookie transmission with secure transport where supported. HttpOnly helps reduce unnecessary access from client-side scripts. SameSite influences cross-site request behavior. Domain and path scope determine where cookies are eligible to be sent. Expiration affects session lifecycle.\n\nCookie safety needs to align with the rest of the application. A Secure cookie assumes HTTPS is working as intended. A SameSite setting should match the browser flows the application supports. A broad cookie domain can expose cookies to more hosts than necessary. A long-lived session cookie can increase exposure when devices are shared or sessions are not monitored. Headers express policy, but the session design gives that policy meaning.\n\nCache-control matters for sensitive pages and API responses. Browsers, shared caches, proxy caches, and intermediate systems may store responses depending on headers and context. Account pages, administrative pages, personalized API responses, files, exports, and error responses may need stricter cache behavior than public assets. File downloads may also need careful content disposition and content type handling so the client treats them as intended.\n\nSensitive data should not be placed casually in headers, URLs, or referrers. Tokens and secrets in URLs can appear in logs, browser history, analytics, referrer headers, and support screenshots. Session safety, cache safety, and header safety should be designed together. The question is not only whether a response works, but where its sensitive values might be stored, forwarded, cached, or observed after it leaves the Python application.",
  "narrationPoints": [
    "Cookies carry important security behavior through attributes such as Secure, HttpOnly, SameSite, domain, path, and expiration.",
    "Domain and path scope determine where cookies are eligible to be sent.",
    "A broad cookie domain can expose cookies to more hosts than necessary.",
    "Cache-control matters for sensitive pages and API responses.",
    "Sensitive data should not be placed casually in headers, URLs, or referrers.",
    "Expiration affects session lifecycle."
  ]
};
