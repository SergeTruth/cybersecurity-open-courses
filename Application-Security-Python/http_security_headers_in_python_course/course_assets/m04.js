window.COURSE_MODULE = {
  "title": "Content Security Policy Fundamentals",
  "graphicAlt": "Sequence diagram for Content Security Policy Fundamentals, showing Python response pipeline, reverse proxy, browser, document, iframe, cache, and cookie jar; labeled arrows identify where browser security policies are enforced and the point where unsafe input or behavior is rejected.",
  "narration": "Content Security Policy, or CSP, is a browser defense-in-depth control that can restrict where scripts, styles, images, fonts, frames, connections, and other resources may load or execute from under browser rules. It is especially relevant to XSS risk reduction because it can reduce the impact of some unsafe rendering mistakes. CSP works best when the application already separates code from data and has predictable frontend asset behavior.\n\nSource concepts define which locations are allowed for different resource types. Nonce and hash concepts can support specific approved inline resources without allowing every inline script or style. Reducing unsafe inline behavior makes CSP easier to maintain because templates and frontend code do not need broad exceptions. A policy should reflect the real asset model of the application, including templates, bundled frontend assets, APIs, analytics, fonts, images, and partner services.\n\nReport-only rollout is a practical way to learn before enforcement. In report-only mode, browsers can report potential violations without blocking behavior. That helps teams discover unexpected inline code, third-party dependencies, development-only assets, or edge-case pages. Reports can be noisy and may contain sensitive context, so collection and review should be handled carefully. The goal is to improve the policy, not to gather endless telemetry without action.\n\nCSP does not replace context-aware output encoding or safe template rendering. If untrusted data is rendered into a dangerous context, the code should still be fixed. CSP can reduce impact and provide signals, but relying on it alone creates brittle safety. A strong Python application uses CSP to support safe templates, safe APIs, response minimization, and frontend discipline. It is a seatbelt, not permission to drive carelessly.",
  "narrationPoints": [
    "CSP works best when the application already separates code from data and has predictable frontend asset behavior.",
    "Nonce and hash concepts can support specific approved inline resources without allowing every inline script or style.",
    "Report-only rollout is a practical way to learn before enforcement.",
    "Reports can be noisy and may contain sensitive context, so collection and review should be handled carefully.",
    "If untrusted data is rendered into a dangerous context, the code should still be fixed.",
    "A strong Python application uses CSP to support safe templates, safe APIs, response minimization, and frontend discipline."
  ]
};
