window.COURSE_MODULE = {
  "title": "Where Headers Are Set in Python Systems",
  "graphicAlt": "Flowchart for Where Headers Are Set in Python Systems, showing Python response pipeline, reverse proxy, browser, document, iframe, cache, and cookie jar; labeled arrows identify where browser security policies are enforced and the point where unsafe input or behavior is rejected.",
  "narration": "HTTP headers can be set close to application code or farther out in the delivery path. A Django project may use middleware and settings. A Flask application may use response helpers, decorators, or after-request hooks. A FastAPI service may use middleware, route-level response handling, or dependency patterns. Infrastructure may also add or override headers through reverse proxies, load balancers, API gateways, static hosting layers, or content delivery networks.\n\nGlobal headers are useful for consistent defaults, but not every response has the same needs. HTML pages, JSON APIs, file downloads, static assets, redirects, admin pages, and error responses may require different policies. Route-level or response-specific headers can be appropriate when they express a deliberate exception. The risk is fragmentation: if every route invents its own policy, teams can end up with missing, duplicated, conflicting, or environment-specific behavior.\n\nHeader ownership should be explicit. If the application sets one policy and the proxy overwrites it, the final browser-visible result may not match what developers reviewed. If both layers add similar headers, conflicts can make behavior confusing. If only production infrastructure sets the headers, local and staging review may miss important differences. Ownership should define who sets the baseline, who approves exceptions, and how changes are tested.\n\nA practical Python header program starts with an inventory. Which headers are emitted by application middleware? Which are added by proxies or hosting platforms? Which responses differ? Which environments diverge? Once that map exists, teams can define stable defaults, document exceptions, and create regression checks. The key is not whether headers come from code or infrastructure. The key is whether the final response policy is intentional and maintainable.",
  "narrationPoints": [
    "HTTP headers can be set close to application code or farther out in the delivery path.",
    "Infrastructure may also add or override headers through reverse proxies, load balancers, API gateways, static hosting layers, or content delivery networks.",
    "The risk is fragmentation: if every route invents its own policy, teams can end up with missing, duplicated, conflicting, or environment-specific behavior.",
    "If only production infrastructure sets the headers, local and staging review may miss important differences.",
    "Once that map exists, teams can define stable defaults, document exceptions, and create regression checks.",
    "Which headers are emitted by application middleware?"
  ]
};
