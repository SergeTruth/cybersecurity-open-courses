window.COURSE_MODULE = {
  "title": "MIME, Referrer, Permissions, and Isolation Headers",
  "graphicAlt": "Boundary map for MIME, Referrer, Permissions, and Isolation Headers, showing Python response pipeline, reverse proxy, browser, document, iframe, cache, and cookie jar; labeled arrows identify where browser security policies are enforced and the point where unsafe input or behavior is rejected.",
  "narration": "Browsers make decisions about content handling, navigation metadata, and feature access. X-Content-Type-Options concepts are about reducing MIME sniffing risk, where a browser may interpret content differently than intended if content type handling is unclear. Clear content types and defensive headers help keep scripts, styles, downloads, JSON, and other responses in the categories the application expects.\n\nReferrer-Policy helps control how much referrer information browsers send when navigating or loading resources. That can reduce unintended leakage of paths, identifiers, query strings, or workflow context to other origins. The right policy depends on the application, analytics needs, partner flows, and privacy expectations. The stable principle is to avoid sending more navigation context than the receiving party needs.\n\nPermissions-Policy helps limit access to certain browser features and capabilities under defined rules. The exact feature list evolves over time, but the durable idea is smaller browser capability surfaces. If a page does not need a sensitive browser feature, the policy can help keep that capability unavailable. Cross-origin isolation concepts and resource loading boundaries also require careful alignment with frontend assets and third-party dependencies.\n\nThese headers reduce unintended exposure by making browser behavior clearer and narrower. They are most effective when paired with correct content types, response minimization, safe templates, and reviewed frontend dependencies. A policy that blocks needed functionality will be weakened. A policy that allows everything adds little value. Teams should choose controls based on what the application actually serves, loads, and exposes.",
  "narrationPoints": [
    "Browsers make decisions about content handling, navigation metadata, and feature access.",
    "Clear content types and defensive headers help keep scripts, styles, downloads, JSON, and other responses in the categories the application expects.",
    "The stable principle is to avoid sending more navigation context than the receiving party needs.",
    "The exact feature list evolves over time, but the durable idea is smaller browser capability surfaces.",
    "A policy that blocks needed functionality will be weakened.",
    "Teams should choose controls based on what the application actually serves, loads, and exposes."
  ]
};
