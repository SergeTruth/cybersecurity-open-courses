window.COURSE_MODULE = {
  "title": "Connection Architecture and Trust Boundaries",
  "graphicAlt": "Four workloads—web API, background worker, migration job, and analyst notebook—cross separate network and identity boundaries toward one database, each following a different approved route and role.",
  "narration": "Database connections usually cross several trust boundaries before a query reaches storage. Application code runs inside a process, container, serverless function, virtual machine, or managed platform. It loads configuration from files, environment variables, service discovery, or a secret store. It talks through a driver or ORM, over a network path that may include private addressing, proxies, connection brokers, load balancers, service mesh components, or managed database endpoints. Each layer can strengthen the design or quietly expand the attack surface.\n\nA useful architecture review starts by documenting who can connect, from where, with which identity, through which route, and for what purpose. Local development, staging, and production should not collapse into the same access pattern. A notebook used for analysis, a migration job, a web API, and a background queue worker may all connect to the same database family, but they should not automatically share the same credentials, privileges, network path, or logging expectations.\n\nInternal network location is helpful, but it is not a complete security control. Systems inside private networks can still be compromised, misconfigured, overprivileged, or incorrectly routed. A database endpoint that only accepts traffic from an application subnet is safer than one exposed broadly, but the application identity, database account, TLS expectations, and permission model still matter. Treat private networking as one control in a layered design, not as proof that the connection is safe.\n\nTrust-boundary documentation also improves operations. When a credential is exposed, teams need to know which applications use it, which environments are affected, which pools may retain active sessions, and which logs can confirm revocation. When a database service changes endpoints or certificate chains, teams need to know which clients validate those endpoints. Clear connection architecture turns emergency response from guesswork into a rehearsed, bounded process.",
  "narrationPoints": [
    "Application code runs inside a process, container, serverless function, virtual machine, or managed platform.",
    "Local development, staging, and production should not collapse into the same access pattern.",
    "Treat private networking as one control in a layered design, not as proof that the connection is safe.",
    "Clear connection architecture turns emergency response from guesswork into a rehearsed, bounded process.",
    "Internal network location is helpful, but it is not a complete security control.",
    "Systems inside private networks can still be compromised, misconfigured, overprivileged, or incorrectly routed."
  ]
};
