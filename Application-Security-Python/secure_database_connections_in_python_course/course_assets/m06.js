window.COURSE_MODULE = {
  "title": "Connection Pools, Timeouts, and Retry Safety",
  "graphicAlt": "A bounded connection pool shows eight reusable sessions, a two-second acquisition gate, statement deadlines, recycling after rotation, and a rejected overflow queue rather than unlimited connections.",
  "narration": "Connection pools improve performance by reusing established database connections, but they also create lifecycle questions. A pool has a size, a maximum overflow behavior, an idle policy, a connection lifetime, and a way to detect stale or broken connections. If those settings are too loose, a service can exhaust database resources. If they are too tight, a traffic spike can turn into unnecessary failures. Either outcome can become security-relevant when availability and controlled access are part of the system?s risk model.\n\nPools also interact with credential rotation and permission changes. If a password, token, certificate, or database role changes, existing pooled connections may continue to behave according to their previous session state until they are closed or refreshed. Teams should understand how their framework, driver, and deployment platform handle reconnects, pool recycling, health checks, and graceful restarts. Rotation is not complete just because a secret value changed in a store.\n\nTimeouts and retries are not just reliability knobs. A missing connection timeout can cause request threads or workers to hang until capacity is exhausted. Aggressive retry loops can amplify database outages, repeat non-idempotent work, or create sudden bursts after partial recovery. Safer retry behavior uses bounded attempts, backoff, clear error handling, and awareness of whether an operation can be safely repeated. Fail-fast and fail-safe behavior should be designed, not improvised under load.\n\nConnection leaks deserve special attention in scripts, workers, and less frequently reviewed code paths. A web framework may manage request-scoped sessions, while a data job or notebook may leave connections open unintentionally. Secure operations depend on knowing who is connected, why they are connected, and when sessions should end. Pool settings, timeout defaults, cleanup practices, and monitoring should support that visibility instead of hiding resource exhaustion until users are affected.",
  "narrationPoints": [
    "Connection pools improve performance by reusing established database connections, but they also create lifecycle questions.",
    "Teams should understand how their framework, driver, and deployment platform handle reconnects, pool recycling, health checks, and graceful restarts.",
    "A missing connection timeout can cause request threads or workers to hang until capacity is exhausted.",
    "Secure operations depend on knowing who is connected, why they are connected, and when sessions should end.",
    "Aggressive retry loops can amplify database outages, repeat non-idempotent work, or create sudden bursts after partial recovery.",
    "Connection leaks deserve special attention in scripts, workers, and less frequently reviewed code paths."
  ]
};
