window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Testing, Logging, Monitoring, and Incident Response through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Record database connection outcomes without secrets",
      "language": "python",
      "blurb": "A trusted bootstrap function starts one module-owned consumer thread before producers can enqueue events. Database paths expose only the bounded put_nowait() producer; blocking logger and metrics collaborators remain private to the consumer. Each shutdown caller captures the consumer generation it originally observed, so concurrent callers can join the same idempotent transition but can never adopt and stop a replacement generation. Shutdown rejects new events, drains and joins within a validated deadline. A blocked or unexpectedly dead consumer keeps state closed and causes bounded local drop accounting rather than unbounded queue growth or request-path I/O.",
      "code": "from dataclasses import dataclass\nfrom math import isfinite\nfrom queue import Empty, Full, Queue\nfrom threading import Condition, Event, Lock, Thread, current_thread\nfrom time import monotonic\n\nDATABASE_EVENTS = frozenset({\n    \"connected\", \"timeout\", \"certificate_rejected\", \"pool_exhausted\",\n})\nMAX_QUEUED_DATABASE_EVENTS = 1024\nMAX_DROPPED_DATABASE_EVENTS = 2_147_483_647\n_CONSUMER_POLL_SECONDS = 0.1\n\n@dataclass(frozen=True, slots=True)\nclass DatabaseConnectionEvent:\n    endpoint_id: str\n    category: str\n    elapsed_ms: int\n\n@dataclass(slots=True)\nclass _DatabaseEventConsumerState:\n    thread: Thread\n    stop: Event\n    generation: int\n    closing: bool = False\n    shutdown_in_progress: bool = False\n\n_DATABASE_EVENT_QUEUE = Queue(maxsize=MAX_QUEUED_DATABASE_EVENTS)\n_DROP_COUNT_LOCK = Lock()\n_CONSUMER_LOCK = Condition(Lock())\n_dropped_database_events = 0\n_consumer_state: _DatabaseEventConsumerState | None = None\n_consumer_generation = 0\n\ndef _record_database_event_drops(count: int = 1) -> None:\n    global _dropped_database_events\n    if type(count) is not int or count <= 0:\n        raise ValueError(\"telemetry drop count rejected\")\n    with _DROP_COUNT_LOCK:\n        _dropped_database_events = min(\n            _dropped_database_events + count,\n            MAX_DROPPED_DATABASE_EVENTS,\n        )\n\ndef database_event_drop_count() -> int:\n    with _DROP_COUNT_LOCK:\n        return _dropped_database_events\n\ndef _emit_database_event(record, logger, metrics) -> None:\n    try:\n        logger.info(\n            \"database_connection_event\",\n            extra={\n                \"endpoint_id\": record.endpoint_id,\n                \"event\": record.category,\n                \"elapsed_ms\": record.elapsed_ms,\n            },\n        )\n        metrics.increment(\n            \"database_connections\",\n            tags={\"endpoint\": record.endpoint_id, \"event\": record.category},\n        )\n    except Exception:\n        _record_database_event_drops()\n\ndef _consume_database_events(logger, metrics, stop: Event) -> None:\n    while True:\n        try:\n            record = _DATABASE_EVENT_QUEUE.get(timeout=_CONSUMER_POLL_SECONDS)\n        except Empty:\n            if stop.is_set():\n                return\n            continue\n        try:\n            _emit_database_event(record, logger, metrics)\n        finally:\n            _DATABASE_EVENT_QUEUE.task_done()\n\ndef configure_database_event_consumer(logger, metrics) -> None:\n    global _consumer_state, _consumer_generation\n    if not callable(getattr(logger, \"info\", None)):\n        raise TypeError(\"telemetry logger rejected\")\n    if not callable(getattr(metrics, \"increment\", None)):\n        raise TypeError(\"telemetry metrics adapter rejected\")\n    stop = Event()\n    thread = Thread(\n        target=_consume_database_events,\n        args=(logger, metrics, stop),\n        name=\"database-event-consumer\",\n        daemon=True,\n    )\n    with _CONSUMER_LOCK:\n        if _consumer_state is not None:\n            raise RuntimeError(\"database telemetry consumer already configured\")\n        _consumer_generation += 1\n        state = _DatabaseEventConsumerState(\n            thread,\n            stop,\n            _consumer_generation,\n        )\n        # Holding the lifecycle lock prevents producers from observing a state\n        # whose thread has not started.\n        thread.start()\n        _consumer_state = state\n\ndef record_database_event(event: object, elapsed_ms: object) -> None:\n    category = event if type(event) is str and event in DATABASE_EVENTS else \"other\"\n    bounded_elapsed = (\n        min(max(elapsed_ms, 0), 30_000)\n        if type(elapsed_ms) is int\n        else 0\n    )\n    record = DatabaseConnectionEvent(\n        \"orders-primary\",\n        category,\n        bounded_elapsed,\n    )\n    accepted = False\n    with _CONSUMER_LOCK:\n        state = _consumer_state\n        if (\n            type(state) is _DatabaseEventConsumerState\n            and not state.closing\n            and state.thread.is_alive()\n        ):\n            try:\n                _DATABASE_EVENT_QUEUE.put_nowait(record)\n                accepted = True\n            except Full:\n                pass\n    if not accepted:\n        _record_database_event_drops()\n\ndef _discard_queued_database_events() -> None:\n    discarded = 0\n    while True:\n        try:\n            _DATABASE_EVENT_QUEUE.get_nowait()\n        except Empty:\n            break\n        else:\n            discarded += 1\n            _DATABASE_EVENT_QUEUE.task_done()\n    if discarded:\n        _record_database_event_drops(discarded)\n\ndef shutdown_database_event_consumer(\n    *,\n    deadline_seconds: float = 2.0,\n) -> None:\n    global _consumer_state\n    if (\n        type(deadline_seconds) not in (int, float)\n        or not isfinite(deadline_seconds)\n        or not 0.25 <= deadline_seconds <= 10.0\n    ):\n        raise ValueError(\"telemetry shutdown deadline rejected\")\n    deadline = monotonic() + float(deadline_seconds)\n\n    # Capture one generation once. A waiting caller may observe that its\n    # original generation completed, but it must never adopt a replacement.\n    with _CONSUMER_LOCK:\n        target = _consumer_state\n        if target is None:\n            return\n        if type(target) is not _DatabaseEventConsumerState:\n            raise RuntimeError(\"database telemetry state rejected\")\n        if type(target.generation) is not int or target.generation <= 0:\n            raise RuntimeError(\"database telemetry generation rejected\")\n        target_generation = target.generation\n        while True:\n            state = _consumer_state\n            if (\n                state is None\n                or state is not target\n                or state.generation != target_generation\n            ):\n                return\n            if current_thread() is target.thread:\n                raise RuntimeError(\"telemetry consumer cannot stop itself\")\n            target.closing = True\n            target.stop.set()\n            if not target.shutdown_in_progress:\n                target.shutdown_in_progress = True\n                break\n            remaining = deadline - monotonic()\n            if remaining <= 0:\n                raise RuntimeError(\n                    \"database telemetry shutdown ownership timed out\"\n                )\n            _CONSUMER_LOCK.wait(timeout=remaining)\n\n    try:\n        target.thread.join(timeout=max(0.0, deadline - monotonic()))\n        still_alive = target.thread.is_alive()\n    except BaseException:\n        with _CONSUMER_LOCK:\n            state = _consumer_state\n            if (\n                state is target\n                and state.generation == target_generation\n            ):\n                target.shutdown_in_progress = False\n                _CONSUMER_LOCK.notify_all()\n        raise\n\n    with _CONSUMER_LOCK:\n        state = _consumer_state\n        if (\n            state is None\n            or state is not target\n            or state.generation != target_generation\n        ):\n            # The original generation completed. A replacement belongs to a\n            # later lifecycle and must remain untouched.\n            return\n        target.shutdown_in_progress = False\n        if still_alive:\n            _CONSUMER_LOCK.notify_all()\n            raise RuntimeError(\n                \"database telemetry consumer did not stop before its deadline\"\n            )\n        _discard_queued_database_events()\n        _consumer_state = None\n        _CONSUMER_LOCK.notify_all()\n"
    },
    {
      "title": "Report health through the module-owned bounded service",
      "language": "python",
      "blurb": "The web-facing health function calls an authorization-scoped client for the separately deployed database-worker service. The web artifact contains no Psycopg opener, secret adapter, engine, supervisor, or process handle. Expected capacity, deadline, and connection failures become bounded categories; defects still surface.",
      "code": "from dataclasses import dataclass\nfrom time import monotonic\n\nfrom orders_service.database_client import (\n    DatabaseConnectionFailed,\n    DatabaseDeadlineExceeded,\n    DatabaseWorkerCapacityExceeded,\n    check_database_ready,\n)\n\n@dataclass(frozen=True)\nclass DatabaseHealth:\n    healthy: bool\n    category: str\n    elapsed_ms: int\n\ndef build_database_health_checker():\n    def check_database_health() -> DatabaseHealth:\n        started = monotonic()\n        try:\n            healthy = check_database_ready()\n            if type(healthy) is not bool:\n                raise TypeError(\"database health contract failed\")\n            category = \"ready\" if healthy else \"unexpected_result\"\n        except DatabaseWorkerCapacityExceeded:\n            healthy, category = False, \"worker_capacity\"\n        except DatabaseDeadlineExceeded:\n            healthy, category = False, \"client_deadline\"\n        except DatabaseConnectionFailed as error:\n            healthy = False\n            category = (\n                \"query_canceled\"\n                if error.sqlstate == \"57014\"\n                else \"connection_failure\"\n            )\n        elapsed_ms = min(int((monotonic() - started) * 1000), 10_000)\n        return DatabaseHealth(healthy, category, elapsed_ms)\n\n    return check_database_health\n"
    }
  ]
};
