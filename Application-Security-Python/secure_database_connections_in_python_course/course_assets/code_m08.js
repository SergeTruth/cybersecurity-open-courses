window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Testing, Logging, Monitoring, and Incident Response through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Record database connection outcomes without secrets",
      "language": "python",
      "blurb": "Finite values and an application-owned endpoint keep secrets out of telemetry. The database path performs only a bounded put_nowait() into a module-owned queue and increments a local saturated drop counter when capacity is unavailable. A dedicated telemetry worker performs potentially blocking logger and metrics I/O, so sink delay or failure cannot replace or postpone the database outcome.",
      "code": "from dataclasses import dataclass\nfrom queue import Empty, Full, Queue\nfrom threading import Lock\n\nDATABASE_EVENTS = frozenset({\n    \"connected\", \"timeout\", \"certificate_rejected\", \"pool_exhausted\",\n})\nMAX_QUEUED_DATABASE_EVENTS = 1024\nMAX_DROPPED_DATABASE_EVENTS = 2_147_483_647\n\n@dataclass(frozen=True, slots=True)\nclass DatabaseConnectionEvent:\n    endpoint_id: str\n    category: str\n    elapsed_ms: int\n\n_DATABASE_EVENT_QUEUE = Queue(maxsize=MAX_QUEUED_DATABASE_EVENTS)\n_DROP_COUNT_LOCK = Lock()\n_dropped_database_events = 0\n\ndef _record_database_event_drop() -> None:\n    global _dropped_database_events\n    with _DROP_COUNT_LOCK:\n        _dropped_database_events = min(\n            _dropped_database_events + 1,\n            MAX_DROPPED_DATABASE_EVENTS,\n        )\n\ndef database_event_drop_count() -> int:\n    with _DROP_COUNT_LOCK:\n        return _dropped_database_events\n\ndef record_database_event(event: object, elapsed_ms: object) -> None:\n    category = event if type(event) is str and event in DATABASE_EVENTS else \"other\"\n    bounded_elapsed = (\n        min(max(elapsed_ms, 0), 30_000)\n        if type(elapsed_ms) is int\n        else 0\n    )\n    record = DatabaseConnectionEvent(\n        \"orders-primary\",\n        category,\n        bounded_elapsed,\n    )\n    try:\n        _DATABASE_EVENT_QUEUE.put_nowait(record)\n    except Full:\n        _record_database_event_drop()\n\ndef emit_next_database_event(logger, metrics, *, timeout_seconds: float = 0.25) -> bool:\n    # Run only in the dedicated telemetry worker, never in a request or\n    # database-operation thread. Sink I/O may block this worker safely.\n    if type(timeout_seconds) not in (int, float) or not 0 <= timeout_seconds <= 1:\n        raise ValueError(\"telemetry dequeue timeout rejected\")\n    try:\n        record = _DATABASE_EVENT_QUEUE.get(timeout=float(timeout_seconds))\n    except Empty:\n        return False\n    try:\n        logger.info(\n            \"database_connection_event\",\n            extra={\n                \"endpoint_id\": record.endpoint_id,\n                \"event\": record.category,\n                \"elapsed_ms\": record.elapsed_ms,\n            },\n        )\n        metrics.increment(\n            \"database_connections\",\n            tags={\"endpoint\": record.endpoint_id, \"event\": record.category},\n        )\n    except Exception:\n        _record_database_event_drop()\n    finally:\n        _DATABASE_EVENT_QUEUE.task_done()\n    return True\n"
    },
    {
      "title": "Report health through the module-owned bounded service",
      "language": "python",
      "blurb": "The health function calls module 6’s fixed readiness operation instead of receiving an operations object, probe, opener, engine, supervisor, or process handle. It therefore shares the same prestarted workers and authenticated deployment connection budget without exposing mutable execution state. Expected capacity, deadline, and connection failures become bounded categories; defects still surface.",
      "code": "from dataclasses import dataclass\nfrom time import monotonic\n\nfrom orders_service.database_workers import (\n    DatabaseConnectionFailed,\n    DatabaseDeadlineExceeded,\n    DatabaseWorkerCapacityExceeded,\n    check_database_ready,\n)\n\n@dataclass(frozen=True)\nclass DatabaseHealth:\n    healthy: bool\n    category: str\n    elapsed_ms: int\n\ndef build_database_health_checker():\n    def check_database_health() -> DatabaseHealth:\n        started = monotonic()\n        try:\n            healthy = check_database_ready()\n            if type(healthy) is not bool:\n                raise TypeError(\"database health contract failed\")\n            category = \"ready\" if healthy else \"unexpected_result\"\n        except DatabaseWorkerCapacityExceeded:\n            healthy, category = False, \"worker_capacity\"\n        except DatabaseDeadlineExceeded:\n            healthy, category = False, \"client_deadline\"\n        except DatabaseConnectionFailed as error:\n            healthy = False\n            category = (\n                \"query_canceled\"\n                if error.sqlstate == \"57014\"\n                else \"connection_failure\"\n            )\n        elapsed_ms = min(int((monotonic() - started) * 1000), 10_000)\n        return DatabaseHealth(healthy, category, elapsed_ms)\n\n    return check_database_health\n"
    }
  ]
};
