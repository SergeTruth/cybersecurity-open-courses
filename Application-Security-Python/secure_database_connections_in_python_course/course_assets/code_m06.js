window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Connection Pools, Timeouts, and Retry Safety through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Run fixed queries through one bounded process supervisor",
      "language": "python",
      "blurb": "The trusted builder constructs one fixed-capacity supervisor and the exact spawn-tested least-privilege opener from authenticated deployment policy, then captures both privately. Each worker must exit before the deadline and returns at most four KiB of strict JSON. Connection failures retain only an optional validated SQLSTATE; malformed rows and messages become one sanitized operation failure.",
      "code": "from json import dumps, loads\nfrom math import isfinite\nfrom multiprocessing import get_context\nfrom multiprocessing.connection import Connection, wait\nfrom threading import BoundedSemaphore\nfrom time import monotonic\nfrom psycopg import InterfaceError, OperationalError\nimport re\n\nfrom orders_service.database_runtime import (\n    VerifiedRuntimeOpener,\n    build_verified_runtime_opener,\n)\nfrom orders_service.database_transport import TLSDeploymentPolicy\n\nORDER_ID = re.compile(r\"[A-Za-z0-9_-]{1,64}\")\nSQLSTATE = re.compile(r\"[0-9A-Z]{5}\")\nMAX_WORKER_MESSAGE_BYTES = 4096\nMAX_TOTAL_CENTS = 9_000_000_000_000_000\n\nclass DatabaseWorkerCapacityExceeded(TimeoutError):\n    pass\n\nclass DatabaseDeadlineExceeded(TimeoutError):\n    pass\n\nclass DatabaseOperationFailed(RuntimeError):\n    pass\n\nclass DatabaseConnectionFailed(DatabaseOperationFailed):\n    def __init__(self, sqlstate: str | None):\n        if sqlstate is not None and (\n            type(sqlstate) is not str or SQLSTATE.fullmatch(sqlstate) is None\n        ):\n            raise ValueError(\"database SQLSTATE rejected\")\n        super().__init__(\"database connection operation failed\")\n        self.sqlstate = sqlstate\n\ndef _bounded_json_int(value: str) -> int:\n    if len(value) > 16:\n        raise ValueError(\"worker integer is too large\")\n    return int(value)\n\ndef _reject_json_constant(_value: str):\n    raise ValueError(\"non-finite worker value rejected\")\n\ndef _unique_json_object(pairs):\n    result = {}\n    for key, value in pairs:\n        if key in result:\n            raise ValueError(\"duplicate worker field rejected\")\n        result[key] = value\n    return result\n\ndef _encode_message(message: dict[str, object]) -> bytes:\n    payload = dumps(\n        message,\n        ensure_ascii=True,\n        separators=(\",\", \":\"),\n    ).encode(\"utf-8\")\n    if len(payload) > MAX_WORKER_MESSAGE_BYTES:\n        raise ValueError(\"worker message is too large\")\n    return payload\n\ndef _order_result_message(row) -> dict[str, object]:\n    if row is None:\n        return {\"kind\": \"ok\", \"row\": None}\n    if type(row) not in (tuple, list) or len(row) != 3:\n        raise ValueError(\"unexpected order row\")\n    order_id, status, total_cents = row\n    if type(order_id) is not str or ORDER_ID.fullmatch(order_id) is None:\n        raise ValueError(\"unexpected stored order identifier\")\n    if (\n        type(status) is not str\n        or not 1 <= len(status) <= 64\n        or any(ord(character) < 0x20 or ord(character) == 0x7F for character in status)\n    ):\n        raise ValueError(\"unexpected stored order status\")\n    if type(total_cents) is not int or not 0 <= total_cents <= MAX_TOTAL_CENTS:\n        raise ValueError(\"unexpected stored order total\")\n    return {\"kind\": \"ok\", \"row\": [order_id, status, total_cents]}\n\ndef _load_order_worker(\n    open_process_connection: VerifiedRuntimeOpener,\n    order_id: str,\n    sender: Connection,\n) -> None:\n    try:\n        try:\n            with open_process_connection() as connection:\n                with connection.transaction():\n                    connection.execute(\"set local transaction_timeout = '5000ms'\")\n                    connection.execute(\"set local idle_in_transaction_session_timeout = '2500ms'\")\n                    connection.execute(\"set local statement_timeout = '2000ms'\")\n                    connection.execute(\"set local lock_timeout = '500ms'\")\n                    row = connection.execute(\n                        \"select id, status, total_cents from public.orders where id = %s\",\n                        (order_id,),\n                    ).fetchone()\n            message = _order_result_message(row)\n        except (InterfaceError, OperationalError) as error:\n            sqlstate = getattr(error, \"sqlstate\", None)\n            if sqlstate is not None and (\n                type(sqlstate) is not str or SQLSTATE.fullmatch(sqlstate) is None\n            ):\n                sqlstate = None\n            message = {\"kind\": \"database_error\", \"sqlstate\": sqlstate}\n        sender.send_bytes(_encode_message(message))\n    finally:\n        sender.close()\n\ndef _stop_worker(worker) -> None:\n    if worker.is_alive():\n        worker.terminate()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        worker.kill()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        raise DatabaseOperationFailed(\"database worker could not be stopped\")\n\nclass DatabaseWorkerSupervisor:\n    def __init__(self, *, max_workers: int, max_waiting: int):\n        if type(max_workers) is not int or not 1 <= max_workers <= 32:\n            raise ValueError(\"worker capacity rejected\")\n        if type(max_waiting) is not int or not 0 <= max_waiting <= 128:\n            raise ValueError(\"worker queue capacity rejected\")\n        self._worker_slots = BoundedSemaphore(max_workers)\n        self._admission_slots = BoundedSemaphore(max_workers + max_waiting)\n        self._context = get_context(\"spawn\")\n\n    def run(\n        self,\n        target,\n        args: tuple[object, ...],\n        *,\n        deadline_seconds: float,\n    ) -> bytes:\n        if (\n            type(deadline_seconds) not in (int, float)\n            or not isfinite(deadline_seconds)\n            or not 0.1 <= deadline_seconds <= 30.0\n        ):\n            raise ValueError(\"database deadline rejected\")\n        if not self._admission_slots.acquire(blocking=False):\n            raise DatabaseWorkerCapacityExceeded(\"database worker queue is full\")\n        has_worker_slot = False\n        try:\n            if not self._worker_slots.acquire(timeout=0.25):\n                raise DatabaseWorkerCapacityExceeded(\"database worker capacity exceeded\")\n            has_worker_slot = True\n            return self._run_admitted(\n                target,\n                args,\n                deadline_seconds=float(deadline_seconds),\n            )\n        finally:\n            if has_worker_slot:\n                self._worker_slots.release()\n            self._admission_slots.release()\n\n    def _run_admitted(self, target, args, *, deadline_seconds: float) -> bytes:\n        receiver, sender = self._context.Pipe(duplex=False)\n        worker = self._context.Process(\n            target=target,\n            args=(*args, sender),\n            daemon=True,\n        )\n        deadline = monotonic() + deadline_seconds\n        started = False\n        try:\n            worker.start()\n            started = True\n            sender.close()\n            remaining = max(0.0, deadline - monotonic())\n            if not wait([worker.sentinel], timeout=remaining):\n                _stop_worker(worker)\n                raise DatabaseDeadlineExceeded(\"database operation deadline exceeded\")\n            worker.join(timeout=0)\n            if worker.exitcode != 0:\n                raise DatabaseOperationFailed(\"database worker failed\")\n            if not receiver.poll(0):\n                raise DatabaseOperationFailed(\"database worker returned no result\")\n            try:\n                return receiver.recv_bytes(MAX_WORKER_MESSAGE_BYTES)\n            except (EOFError, OSError) as error:\n                raise DatabaseOperationFailed(\"database worker result rejected\") from error\n        finally:\n            if not started:\n                sender.close()\n            receiver.close()\n            if started:\n                _stop_worker(worker)\n            if not worker.is_alive():\n                worker.close()\n\ndef _decode_order_result(payload: bytes):\n    try:\n        message = loads(\n            payload.decode(\"utf-8\"),\n            parse_int=_bounded_json_int,\n            parse_constant=_reject_json_constant,\n            object_pairs_hook=_unique_json_object,\n        )\n    except (UnicodeDecodeError, ValueError) as error:\n        raise DatabaseOperationFailed(\"database worker result rejected\") from error\n    if type(message) is dict and message.get(\"kind\") == \"database_error\":\n        if set(message) != {\"kind\", \"sqlstate\"}:\n            raise DatabaseOperationFailed(\"database worker result rejected\")\n        sqlstate = message[\"sqlstate\"]\n        if sqlstate is not None and (\n            type(sqlstate) is not str or SQLSTATE.fullmatch(sqlstate) is None\n        ):\n            raise DatabaseOperationFailed(\"database worker result rejected\")\n        raise DatabaseConnectionFailed(sqlstate)\n    if type(message) is not dict or set(message) != {\"kind\", \"row\"} or message[\"kind\"] != \"ok\":\n        raise DatabaseOperationFailed(\"database worker result rejected\")\n    row = message[\"row\"]\n    if row is None:\n        return None\n    if type(row) is not list or len(row) != 3:\n        raise DatabaseOperationFailed(\"database worker result rejected\")\n    try:\n        validated = _order_result_message(tuple(row))\n    except ValueError as error:\n        raise DatabaseOperationFailed(\n            \"database worker result rejected\"\n        ) from error\n    return tuple(validated[\"row\"])\n\ndef build_order_loader(deployment_policy: TLSDeploymentPolicy):\n    supervisor = DatabaseWorkerSupervisor(max_workers=8, max_waiting=32)\n    open_process_connection = build_verified_runtime_opener(deployment_policy)\n    if type(open_process_connection) is not VerifiedRuntimeOpener:\n        raise TypeError(\"reviewed runtime database opener required\")\n\n    def load_order(order_id: str):\n        if type(order_id) is not str or ORDER_ID.fullmatch(order_id) is None:\n            raise ValueError(\"order identifier rejected\")\n        payload = supervisor.run(\n            _load_order_worker,\n            (open_process_connection, order_id),\n            deadline_seconds=3.0,\n        )\n        return _decode_order_result(payload)\n\n    return load_order\n"
    },
    {
      "title": "Retry only validated supervisor connection failures",
      "language": "python",
      "blurb": "A concrete adapter reads the preceding supervisor’s validated order tuple and converts only its allowlisted DatabaseConnectionFailed SQLSTATEs into retryable failures. The exception constructor and public retry loop both revalidate the allowlist before sleeping. Capacity, hard-deadline, malformed-result, and non-allowlisted failures propagate immediately.",
      "code": "from secrets import randbelow\nfrom time import sleep\nfrom typing import Protocol\nimport re\n\nfrom orders_service.database_workers import DatabaseConnectionFailed\n\nORDER_ID = re.compile(r\"[A-Za-z0-9_-]{1,64}\")\nSQLSTATE = re.compile(r\"[0-9A-Z]{5}\")\nRETRYABLE_READ_STATES = frozenset({\n    \"08001\", \"08003\", \"08006\", \"08007\",\n    \"57P01\", \"57P02\", \"57P03\",\n})\nRETRY_DELAY_CAPS_MS = (100, 500)\nMAX_TOTAL_CENTS = 9_000_000_000_000_000\n\nclass DatabaseReadContractError(RuntimeError):\n    pass\n\nclass RetryableDatabaseRead(RuntimeError):\n    def __init__(self, sqlstate: str):\n        if (\n            type(sqlstate) is not str\n            or SQLSTATE.fullmatch(sqlstate) is None\n            or sqlstate not in RETRYABLE_READ_STATES\n        ):\n            raise ValueError(\"retryable database SQLSTATE rejected\")\n        super().__init__(\"database read failed\")\n        self.sqlstate = sqlstate\n\nclass BoundedOrderLoader(Protocol):\n    def __call__(self, order_id: str) -> tuple[str, str, int] | None: ...\n\nclass BoundedOrderStatusReader(Protocol):\n    def __call__(self, order_id: str) -> str | None: ...\n\ndef _validate_order_id(order_id: object) -> str:\n    if type(order_id) is not str or ORDER_ID.fullmatch(order_id) is None:\n        raise ValueError(\"order identifier rejected\")\n    return order_id\n\ndef _validate_status(result: object) -> str:\n    if (\n        type(result) is not str\n        or not 1 <= len(result) <= 64\n        or any(ord(character) < 0x20 or ord(character) == 0x7F for character in result)\n    ):\n        raise DatabaseReadContractError(\"database read result rejected\")\n    return result\n\ndef build_supervisor_status_reader(\n    load_order: BoundedOrderLoader,\n) -> BoundedOrderStatusReader:\n    def read_once(order_id: str) -> str | None:\n        expected_order_id = _validate_order_id(order_id)\n        try:\n            row = load_order(expected_order_id)\n        except DatabaseConnectionFailed as error:\n            if error.sqlstate in RETRYABLE_READ_STATES:\n                raise RetryableDatabaseRead(error.sqlstate) from error\n            raise\n        if row is None:\n            return None\n        if type(row) is not tuple or len(row) != 3:\n            raise DatabaseReadContractError(\"database read result rejected\")\n        returned_order_id, status, total_cents = row\n        if (\n            type(returned_order_id) is not str\n            or returned_order_id != expected_order_id\n            or type(total_cents) is not int\n            or not 0 <= total_cents <= MAX_TOTAL_CENTS\n        ):\n            raise DatabaseReadContractError(\"database read result rejected\")\n        return _validate_status(status)\n\n    return read_once\n\ndef build_retrying_order_reader(read_once: BoundedOrderStatusReader):\n    def read_with_bounded_retry(order_id: str) -> str | None:\n        expected_order_id = _validate_order_id(order_id)\n        for attempt in range(3):\n            try:\n                result = read_once(expected_order_id)\n                return None if result is None else _validate_status(result)\n            except RetryableDatabaseRead as error:\n                if (\n                    type(error) is not RetryableDatabaseRead\n                    or type(error.sqlstate) is not str\n                    or SQLSTATE.fullmatch(error.sqlstate) is None\n                    or error.sqlstate not in RETRYABLE_READ_STATES\n                ):\n                    raise DatabaseReadContractError(\n                        \"retry classification rejected\"\n                    ) from error\n                if attempt == 2:\n                    raise\n                cap_ms = RETRY_DELAY_CAPS_MS[attempt]\n                sleep(randbelow(cap_ms + 1) / 1000)\n        raise AssertionError(\"retry loop exhausted unexpectedly\")\n\n    return read_with_bounded_retry\n"
    }
  ]
};
