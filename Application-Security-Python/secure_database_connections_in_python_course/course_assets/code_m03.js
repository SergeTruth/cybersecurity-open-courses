window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Connection Strings, Credentials, and Secret Handling through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Validate and consume one canonical PostgreSQL descriptor",
      "language": "python",
      "blurb": "The production function accepts only one byte-canonical, password-free descriptor. It imports the reviewed Psycopg driver and obtains the runtime credential from an application-owned secret boundary, then binds the approved numeric address, CA, TLS, authentication, and startup-session policy. Callers cannot substitute a driver, secret provider, endpoint, or connection option.",
      "code": "from dataclasses import dataclass\nfrom urllib.parse import parse_qsl, urlsplit\n\nAPPROVED_CA_FILE = \"/etc/orders-service/trust/orders-ca.pem\"\nAPPROVED_HOSTADDR = \"10.20.30.40\"\nAPPROVED_DATABASE_DSN = (\n    \"postgresql://orders_runtime@orders-db.internal.example:5432/orders?\"\n    \"channel_binding=require&gssencmode=disable&hostaddr=10.20.30.40&\"\n    \"require_auth=scram-sha-256&sslcertmode=disable&\"\n    \"ssl_min_protocol_version=TLSv1.2&sslmode=verify-full&\"\n    \"sslrootcert=%2Fetc%2Forders-service%2Ftrust%2Forders-ca.pem\"\n)\nAPPROVED_CONNECTION_QUERY = (\n    (\"channel_binding\", \"require\"),\n    (\"gssencmode\", \"disable\"),\n    (\"hostaddr\", APPROVED_HOSTADDR),\n    (\"require_auth\", \"scram-sha-256\"),\n    (\"sslcertmode\", \"disable\"),\n    (\"ssl_min_protocol_version\", \"TLSv1.2\"),\n    (\"sslmode\", \"verify-full\"),\n    (\"sslrootcert\", APPROVED_CA_FILE),\n)\n\n@dataclass(frozen=True)\nclass _DatabaseTarget:\n    host: str\n    hostaddr: str\n    port: int\n    database: str\n    user: str\n\ndef _parse_database_target(dsn: str) -> _DatabaseTarget:\n    if type(dsn) is not str or len(dsn) > 1024:\n        raise ValueError(\"database descriptor rejected\")\n    if any(ord(character) < 0x20 or ord(character) == 0x7F for character in dsn):\n        raise ValueError(\"database descriptor contains controls\")\n    if dsn != APPROVED_DATABASE_DSN:\n        raise ValueError(\"database descriptor is not canonical\")\n    parsed = urlsplit(dsn)\n    query_pairs = tuple(parse_qsl(\n        parsed.query,\n        keep_blank_values=True,\n        strict_parsing=True,\n    ))\n    if parsed.password is not None or parsed.username != \"orders_runtime\" or parsed.fragment:\n        raise ValueError(\"embedded or unexpected database identity\")\n    if parsed.scheme != \"postgresql\" or parsed.hostname != \"orders-db.internal.example\":\n        raise ValueError(\"database endpoint rejected\")\n    if (\n        parsed.port != 5432\n        or parsed.path != \"/orders\"\n        or query_pairs != APPROVED_CONNECTION_QUERY\n    ):\n        raise ValueError(\"database connection policy rejected\")\n    return _DatabaseTarget(\n        parsed.hostname,\n        APPROVED_HOSTADDR,\n        parsed.port,\n        \"orders\",\n        parsed.username,\n    )\n\ndef connect_validated_database(dsn: str):\n    target = _parse_database_target(dsn)\n    from orders_service.secrets import read_runtime_database_password\n    password = read_runtime_database_password()\n    if type(password) is not bytes or not 16 <= len(password) <= 1024 or b\"\\x00\" in password:\n        raise ValueError(\"database credential rejected\")\n    try:\n        password_text = password.decode(\"utf-8\")\n    except UnicodeDecodeError as error:\n        raise ValueError(\"database credential rejected\") from error\n    import psycopg\n    return psycopg.connect(\n        host=target.host,\n        hostaddr=target.hostaddr,\n        port=target.port,\n        dbname=target.database,\n        user=target.user,\n        password=password_text,\n        connect_timeout=5,\n        tcp_user_timeout=3000,\n        channel_binding=\"require\",\n        gssencmode=\"disable\",\n        require_auth=\"scram-sha-256\",\n        options=(\n            \"-csearch_path= \"\n            \"-csynchronous_commit=on \"\n            \"-cstatement_timeout=2000 \"\n            \"-ctransaction_timeout=5000 \"\n            \"-cidle_in_transaction_session_timeout=2500 \"\n            \"-clock_timeout=500 \"\n            \"-crow_security=on\"\n        ),\n        sslcertmode=\"disable\",\n        ssl_min_protocol_version=\"TLSv1.2\",\n        sslmode=\"verify-full\",\n        sslrootcert=APPROVED_CA_FILE,\n    )\n"
    },
    {
      "title": "Resolve and consume a database password at connection time",
      "language": "python",
      "blurb": "This zero-argument production opener imports the reviewed driver and resolves the fixed runtime credential through the process-owned secret adapter. The password is validated immediately and combined only with application-owned endpoint, TLS, authentication, and session settings; callers receive the connection but cannot inject a driver or provider that observes the secret or ignores policy.",
      "code": "APPROVED_DATABASE_HOST = \"orders-db.internal.example\"\nAPPROVED_DATABASE_HOSTADDR = \"10.20.30.40\"\nRUNTIME_PASSWORD_REFERENCE = \"vault://production/orders/runtime-password\"\nAPPROVED_SECRET_PROVIDER_ID = \"production-vault-v1\"\n\ndef open_database_connection():\n    from orders_service.secrets import read_secret_bytes\n    password = read_secret_bytes(\n        APPROVED_SECRET_PROVIDER_ID,\n        RUNTIME_PASSWORD_REFERENCE,\n    )\n    if type(password) is not bytes or not 16 <= len(password) <= 1024 or b\"\\x00\" in password:\n        raise ValueError(\"database credential rejected\")\n    try:\n        password_text = password.decode(\"utf-8\")\n    except UnicodeDecodeError as error:\n        raise ValueError(\"database credential rejected\") from error\n    import psycopg\n    return psycopg.connect(\n        host=APPROVED_DATABASE_HOST,\n        hostaddr=APPROVED_DATABASE_HOSTADDR,\n        port=5432,\n        dbname=\"orders\",\n        user=\"orders_runtime\",\n        password=password_text,\n        connect_timeout=5,\n        tcp_user_timeout=3000,\n        channel_binding=\"require\",\n        gssencmode=\"disable\",\n        require_auth=\"scram-sha-256\",\n        options=(\n            \"-csearch_path= \"\n            \"-csynchronous_commit=on \"\n            \"-cstatement_timeout=2000 \"\n            \"-ctransaction_timeout=5000 \"\n            \"-cidle_in_transaction_session_timeout=2500 \"\n            \"-clock_timeout=500 \"\n            \"-crow_security=on\"\n        ),\n        sslcertmode=\"disable\",\n        ssl_min_protocol_version=\"TLSv1.2\",\n        sslmode=\"verify-full\",\n        sslrootcert=\"/etc/orders-service/trust/orders-ca.pem\",\n    )\n"
    }
  ]
};
