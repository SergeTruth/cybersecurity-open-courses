window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Incident Response",
  "graphicAlt": "A monitoring panel groups connection outcomes into success, pool timeout, certificate rejection, and unexpected role; DSNs, passwords, SQL values, and raw exceptions are visibly excluded.",
  "narration": "Secure database connectivity should be testable. Tests can confirm that required configuration is present, malformed secrets fail safely, production code does not accept development-only TLS shortcuts, and environment-specific settings do not point to the wrong database. Rotation readiness can be tested through controlled secret changes and restart behavior. Dependency review should include database drivers, ORM versions, and configuration defaults that affect authentication, TLS, pooling, or error behavior.\n\nLogging should help operators understand connection health without exposing secrets. Safe logs can record error categories, connection source, service identity, environment, timing, correlation identifiers, and whether a failure was authentication-related, network-related, certificate-related, or permission-related. Unsafe logs include passwords, tokens, private keys, full sensitive DSNs, certificate material, and large query payloads containing sensitive data. Redaction should be deliberate and tested, not assumed.\n\nMonitoring should make unusual database access visible. Useful signals include failed logins, unexpected client sources, new accounts, privilege errors, disabled TLS where it is expected, unusual connection counts, repeated connection failures, and application accounts performing surprising operations. Database audit logs, application telemetry, secret-store access logs, and platform logs all contribute different pieces of evidence. Monitoring is strongest when teams know which identities and access paths are normal.\n\nIncident response planning turns connection security into recoverable operations. If a credential, token, private key, or connection string is exposed, teams should be able to identify affected systems, revoke or rotate access, recycle pools, redeploy workloads, validate that old access no longer works, review logs for suspicious activity, and prevent recurrence. The response should avoid panic changes that create outages or leave stale sessions behind. Connection security must be observable without leaking the very secrets it protects.",
  "narrationPoints": [
    "Dependency review should include database drivers, ORM versions, and configuration defaults that affect authentication, TLS, pooling, or error behavior.",
    "Unsafe logs include passwords, tokens, private keys, full sensitive DSNs, certificate material, and large query payloads containing sensitive data.",
    "Database audit logs, application telemetry, secret-store access logs, and platform logs all contribute different pieces of evidence.",
    "The response should avoid panic changes that create outages or leave stale sessions behind.",
    "Monitoring is strongest when teams know which identities and access paths are normal.",
    "Rotation readiness can be tested through controlled secret changes and restart behavior."
  ]
};
