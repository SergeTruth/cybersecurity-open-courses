window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Frameworks, ORMs, Migrations, and Background Jobs through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Build an ORM engine through the verified runtime opener",
      "language": "python",
      "blurb": "The trusted builder constructs the complete module 5 runtime opener from authenticated deployment policy and gives it to SQLAlchemy as the creator for every physical pooled connection. The engine retains no password, while each new connection resolves the process-local credential and rechecks the authenticated CA, TLS session, identities, and direct grants. Checkout pre-ping remains disabled; hard client deadlines belong behind the module 6 worker boundary.",
      "code": "from sqlalchemy import create_engine\n\nfrom orders_service.database_runtime import (\n    VerifiedRuntimeOpener,\n    build_verified_runtime_opener,\n)\nfrom orders_service.database_transport import TLSDeploymentPolicy\n\ndef build_runtime_engine(deployment_policy: TLSDeploymentPolicy):\n    open_runtime_connection = build_verified_runtime_opener(deployment_policy)\n    if type(open_runtime_connection) is not VerifiedRuntimeOpener:\n        raise TypeError(\"reviewed runtime database opener required\")\n    return create_engine(\n        \"postgresql+psycopg://\",\n        creator=open_runtime_connection,\n        pool_size=8,\n        max_overflow=4,\n        pool_timeout=2,\n        pool_recycle=900,\n        pool_pre_ping=False,\n    )\n"
    },
    {
      "title": "Guard a migration entry point",
      "language": "python",
      "blurb": "The trusted composition root constructs the migration transport from authenticated deployment policy rather than accepting a caller-supplied factory. The opener resolves the dedicated credential process-locally and verifies the database, authenticated login, effective role, SCRAM identity, and current backend TLS state before the separately approved revision and digest reach the schema-qualified migration function.",
      "code": "from dataclasses import dataclass\nfrom typing import Protocol\nimport re\n\nfrom orders_service.database_transport import (\n    PostgresTransportOpener,\n    TLSDeploymentPolicy,\n    build_postgres_migration_transport_opener,\n)\n\nREVISION = re.compile(r\"[0-9a-f]{12}\")\nARTIFACT_SHA256 = re.compile(r\"[0-9a-f]{64}\")\nEXPECTED_MIGRATION_SYSTEM_USER = \"scram-sha-256:orders_migrator\"\n\nclass DeploymentAuthorizer(Protocol):\n    def require_approved_migration(\n        self,\n        operation: str,\n        environment: str,\n        revision: str,\n        artifact_sha256: str,\n    ) -> None: ...\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedMigrationOpener:\n    open_transport_connection: PostgresTransportOpener\n\n    def __call__(self):\n        connection = self.open_transport_connection()\n        try:\n            row = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            expected = (\n                \"orders\",\n                \"orders_migrator\",\n                \"orders_migrator\",\n                EXPECTED_MIGRATION_SYSTEM_USER,\n                True,\n            )\n            if row is None or tuple(row) != expected:\n                raise PermissionError(\"verified migration TLS identity required\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef build_verified_migration_opener(\n    deployment_policy: TLSDeploymentPolicy,\n) -> VerifiedMigrationOpener:\n    transport = build_postgres_migration_transport_opener(deployment_policy)\n    if type(transport) is not PostgresTransportOpener:\n        raise TypeError(\"reviewed migration transport required\")\n    return VerifiedMigrationOpener(transport)\n\ndef build_migration_runner(\n    authorizer: DeploymentAuthorizer,\n    deployment_policy: TLSDeploymentPolicy,\n):\n    open_migration_connection = build_verified_migration_opener(deployment_policy)\n    if type(open_migration_connection) is not VerifiedMigrationOpener:\n        raise TypeError(\"reviewed migration database opener required\")\n\n    def run_migration(revision: str, artifact_sha256: str) -> None:\n        if type(revision) is not str or REVISION.fullmatch(revision) is None:\n            raise ValueError(\"migration revision rejected\")\n        if (\n            type(artifact_sha256) is not str\n            or ARTIFACT_SHA256.fullmatch(artifact_sha256) is None\n        ):\n            raise ValueError(\"migration artifact digest rejected\")\n        authorizer.require_approved_migration(\n            \"orders-schema-migration\",\n            \"production\",\n            revision,\n            artifact_sha256,\n        )\n        with open_migration_connection() as connection:\n            connection.execute(\n                \"select orders_admin.apply_approved_migration(%s, %s)\",\n                (revision, artifact_sha256),\n            )\n\n    return run_migration\n"
    }
  ]
};
