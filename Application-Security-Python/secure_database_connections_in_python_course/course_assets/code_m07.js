window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Frameworks, ORMs, Migrations, and Background Jobs through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer. ORM connections stay inside the database-worker artifact, and migration processes start only from a guarded deployment executable.",
  "codeExamples": [
    {
      "title": "Keep ORM pooling inside prestarted database workers",
      "language": "python",
      "blurb": "This orders-db-worker-only module owns one single-connection SQLAlchemy pool backed by the verified worker opener. The web artifact omits this module, the Psycopg driver, the secret adapter, and database credentials. Every order query enforces the authenticated tenant and subject scope, and every stored status passes the shared allowlist. Checkout rechecks the authenticated deployment generation before use, a mismatch exits and poisons the worker for replacement, and pool_recycle bounds physical-connection age to five minutes. Shutdown disposes the engine inside the worker before module 6 joins it; the deployment response must also terminate server sessions when emergency revocation requires immediate enforcement.",
      "code": "from threading import Lock\nfrom sqlalchemy import create_engine, event, text\nfrom sqlalchemy.exc import DisconnectionError\nimport re\n\nfrom orders_db_worker.database_runtime import VerifiedRuntimeOpener\nfrom orders_db_worker.order_contracts import validate_order_status\n\n# orders_db_worker.orm_operations: this module is absent from the web artifact.\nORDER_ID = re.compile(r\"[A-Za-z0-9_-]{1,64}\")\nSCOPE_ID = re.compile(r\"[A-Za-z0-9_-]{1,64}\")\n_EXPECTED_SESSION_POLICY = (\n    \"orders_runtime\",\n    \"orders_runtime\",\n    True,\n    True,\n    True,\n    True,\n    True,\n    \"on\",\n    \"origin\",\n    True,\n)\n_ENGINE_LOCK = Lock()\n_worker_engine = None\n_worker_opener = None\n\ndef _set_session_policy(dbapi_connection) -> None:\n    if dbapi_connection.prepare_threshold is not None:\n        raise PermissionError(\"automatic prepared statements must be disabled\")\n    previous_autocommit = dbapi_connection.autocommit\n    dbapi_connection.autocommit = True\n    try:\n        with dbapi_connection.cursor() as cursor:\n            for name, value in (\n                (\"search_path\", \"\"),\n                (\"synchronous_commit\", \"on\"),\n                (\"statement_timeout\", \"2000ms\"),\n                (\"transaction_timeout\", \"5000ms\"),\n                (\"idle_in_transaction_session_timeout\", \"2500ms\"),\n                (\"lock_timeout\", \"500ms\"),\n                (\"row_security\", \"on\"),\n            ):\n                cursor.execute(\n                    \"select pg_catalog.set_config(%s, %s, false)\",\n                    (name, value),\n                )\n            cursor.execute(\n                \"select session_user, current_user, \"\n                \"current_setting('synchronous_commit') = 'on', \"\n                \"current_setting('statement_timeout')::interval = interval '2 seconds', \"\n                \"current_setting('transaction_timeout')::interval = interval '5 seconds', \"\n                \"current_setting('idle_in_transaction_session_timeout')::interval \"\n                \"= interval '2.5 seconds', \"\n                \"current_setting('lock_timeout')::interval \"\n                \"= interval '500 milliseconds', \"\n                \"current_setting('row_security'), \"\n                \"current_setting('session_replication_role'), \"\n                \"orders_security.runtime_authority_approved(session_user)\"\n            )\n            row = cursor.fetchone()\n        if row is None or tuple(row) != _EXPECTED_SESSION_POLICY:\n            raise PermissionError(\"pooled database session policy rejected\")\n    finally:\n        dbapi_connection.autocommit = previous_autocommit\n\ndef _reset_session(dbapi_connection) -> None:\n    dbapi_connection.rollback()\n    previous_autocommit = dbapi_connection.autocommit\n    dbapi_connection.autocommit = True\n    try:\n        with dbapi_connection.cursor() as cursor:\n            cursor.execute(\"DISCARD ALL\")\n    finally:\n        dbapi_connection.autocommit = previous_autocommit\n        dbapi_connection.rollback()\n\ndef _build_worker_engine(opener: VerifiedRuntimeOpener):\n    if type(opener) is not VerifiedRuntimeOpener:\n        raise TypeError(\"reviewed runtime database opener required\")\n    engine = create_engine(\n        \"postgresql+psycopg://\",\n        creator=opener,\n        pool_size=1,\n        max_overflow=0,\n        pool_timeout=0.25,\n        pool_recycle=300,\n        pool_pre_ping=False,\n        pool_reset_on_return=None,\n    )\n\n    @event.listens_for(engine.pool, \"checkout\")\n    def apply_and_verify_policy(dbapi_connection, _record, _proxy) -> None:\n        # A generation change is a deployment fence: it escapes checkout,\n        # closes this worker, and makes the supervisor require replacement.\n        opener.require_current_policy()\n        try:\n            _set_session_policy(dbapi_connection)\n        except Exception as error:\n            raise DisconnectionError(\n                \"pooled database session rejected\"\n            ) from error\n\n    @event.listens_for(engine.pool, \"reset\")\n    def reset_policy(dbapi_connection, _record, reset_state) -> None:\n        if not reset_state.terminate_only:\n            _reset_session(dbapi_connection)\n\n    return engine\n\ndef _engine_for_worker(opener: VerifiedRuntimeOpener):\n    global _worker_engine, _worker_opener\n    with _ENGINE_LOCK:\n        if _worker_engine is None:\n            _worker_engine = _build_worker_engine(opener)\n            _worker_opener = opener\n        elif opener != _worker_opener:\n            raise PermissionError(\"worker database opener changed\")\n        return _worker_engine\n\ndef _require_order_id(order_id: str) -> None:\n    if type(order_id) is not str or ORDER_ID.fullmatch(order_id) is None:\n        raise ValueError(\"order identifier rejected\")\n\ndef _require_scope_id(value: object) -> str:\n    if type(value) is not str or SCOPE_ID.fullmatch(value) is None:\n        raise PermissionError(\"order read scope rejected\")\n    return value\n\ndef execute_health(opener: VerifiedRuntimeOpener) -> bool:\n    engine = _engine_for_worker(opener)\n    with engine.connect() as connection:\n        row = connection.execute(text(\"select 1\")).fetchone()\n    if row is None or tuple(row) != (1,):\n        raise RuntimeError(\"database health result rejected\")\n    return True\n\ndef execute_order(\n    opener: VerifiedRuntimeOpener,\n    tenant_id: str,\n    subject_id: str,\n    order_id: str,\n):\n    _require_order_id(order_id)\n    tenant_id = _require_scope_id(tenant_id)\n    subject_id = _require_scope_id(subject_id)\n    engine = _engine_for_worker(opener)\n    with engine.connect() as connection:\n        row = connection.execute(\n            text(\n                \"select id, status, total_cents from public.orders \"\n                \"where id = :order_id and tenant_id = :tenant_id \"\n                \"and owner_subject_id = :subject_id\"\n            ),\n            {\n                \"order_id\": order_id,\n                \"tenant_id\": tenant_id,\n                \"subject_id\": subject_id,\n            },\n        ).fetchone()\n    return None if row is None else tuple(row)\n\ndef execute_order_status(\n    opener: VerifiedRuntimeOpener,\n    tenant_id: str,\n    subject_id: str,\n    order_id: str,\n) -> str | None:\n    # Only module 6's fixed, authorization-scoped dispatcher imports this.\n    _require_order_id(order_id)\n    tenant_id = _require_scope_id(tenant_id)\n    subject_id = _require_scope_id(subject_id)\n    engine = _engine_for_worker(opener)\n    with engine.connect() as connection:\n        row = connection.execute(\n            text(\n                \"select status from public.orders \"\n                \"where id = :order_id and tenant_id = :tenant_id \"\n                \"and owner_subject_id = :subject_id\"\n            ),\n            {\n                \"order_id\": order_id,\n                \"tenant_id\": tenant_id,\n                \"subject_id\": subject_id,\n            },\n        ).fetchone()\n    if row is None:\n        return None\n    if len(row) != 1:\n        raise RuntimeError(\"stored order status rejected\")\n    try:\n        return validate_order_status(row[0])\n    except ValueError as error:\n        raise RuntimeError(\"stored order status rejected\") from error\n\ndef shutdown_worker_engine(opener: VerifiedRuntimeOpener) -> None:\n    global _worker_engine, _worker_opener\n    if type(opener) is not VerifiedRuntimeOpener:\n        raise TypeError(\"reviewed runtime database opener required\")\n    with _ENGINE_LOCK:\n        if _worker_engine is None:\n            if _worker_opener is not None:\n                raise RuntimeError(\"worker engine state rejected\")\n            return\n        if opener != _worker_opener:\n            raise PermissionError(\"worker database opener changed\")\n        _worker_engine.dispose(close=True)\n        _worker_engine = None\n        _worker_opener = None\n\n# No web-facing alias is defined here. Only the authenticated database-worker\n# dispatcher can call these operation-specific functions.\n"
    },
    {
      "title": "Run migrations only from a prestarted deployment worker",
      "language": "python",
      "blurb": "Before dispatch, the separately privileged deployment service persists an opaque reconciliation identifier bound to the migration and a globally unique worker generation. Its prestarted child intentionally waits without a request deadline while idle; shutdown or channel closure can interrupt that wait. After dispatch, every request, response, approval-binding, and cleanup read uses the operation's absolute deadline. Explicit request or signed-approval denial produces the rejected result, while unexpected pre-acceptance defects produce a distinct sanitized internal-failure result. After acceptance, uncertainty remains outcome-unknown. The worker durably binds approval context before opening the database, and reconciliation remains informational rather than retry authorization.",
      "code": "from dataclasses import dataclass\nfrom multiprocessing import freeze_support, get_context\nfrom multiprocessing.connection import wait\nfrom socket import socket, socketpair, timeout as SocketTimeout\nfrom struct import pack, unpack\nfrom threading import Lock\nfrom math import isfinite\nfrom time import monotonic\nimport re\n\n# orders_deployment.migration_service is packaged separately from the web app.\n# Only its deployment workload identity may resolve migration credentials.\nfrom orders_deployment.approvals import (\n    ApprovedMigration,\n    MigrationApprovalRejected,\n    require_signed_migration_approval,\n)\nfrom orders_deployment.database_transport import (\n    MigrationTransportOpener,\n    build_migration_transport_opener,\n)\nfrom orders_deployment.migration_outcomes import (\n    DurableMigrationOutcome,\n    activate_migration_service_generation,\n    begin_durable_migration_attempt,\n    bind_durable_migration_approval,\n    confirm_migration_worker_terminated,\n    read_current_migration_reconciliation_status,\n)\nfrom orders_deployment.platform_identity import (\n    require_verified_migration_workload,\n)\n\nREVISION = re.compile(r\"[0-9a-f]{12}\")\nARTIFACT_SHA256 = re.compile(r\"[0-9a-f]{64}\")\nAUDIT_IDENTIFIER = re.compile(r\"[A-Za-z0-9][A-Za-z0-9._:-]{7,127}\")\nMIGRATION_GENERATION = re.compile(r\"mg_[A-Za-z0-9_-]{43}\")\nRECONCILIATION_ID = re.compile(r\"mr_[A-Za-z0-9_-]{43}\")\nMIGRATION_CLIENT_DEADLINE_SECONDS = 750.0\nMIGRATION_CLEANUP_RESERVE_SECONDS = 2.0\nMIGRATION_STOP_BUDGET_SECONDS = 2.0\nMIGRATION_SHUTDOWN_CLEANUP_RESERVE_SECONDS = 2.0\nMIGRATION_RECEIPT_TIMEOUT_SECONDS = 2.0\nMIGRATION_GENERATION_TIMEOUT_SECONDS = 5.0\nMIGRATION_CONTROL_SEND_TIMEOUT_SECONDS = 2.0\nMIGRATION_APPROVAL_BIND_TIMEOUT_SECONDS = 5.0\n_READY = b\"ready\"\n_MIGRATION_REJECTED = b\"migration-rejected\"\n_MIGRATION_INTERNAL_FAILURE = b\"migration-internal-failure\"\n_MIGRATION_APPLIED = b\"migration-applied\"\n_MIGRATION_UNKNOWN = b\"migration-outcome-unknown\"\n_MIGRATION_APPROVAL_BOUND = b\"migration-approval-bound\"\n_ACCEPTED_PREFIX = b\"accepted\\n\"\n_MAX_TASK_BYTES = 46 + 1 + 46 + 1 + 12 + 1 + 64\n_MAX_ACCEPTED_BYTES = len(_ACCEPTED_PREFIX) + 128 + 1 + 128\n_MAX_CONTROL_BYTES = max(\n    len(_READY),\n    len(_MIGRATION_REJECTED),\n    len(_MIGRATION_INTERNAL_FAILURE),\n    len(_MIGRATION_APPLIED),\n    len(_MIGRATION_UNKNOWN),\n    len(_MIGRATION_APPROVAL_BOUND),\n    _MAX_ACCEPTED_BYTES,\n)\n_MAX_MIGRATION_FRAME_BYTES = max(_MAX_TASK_BYTES, _MAX_CONTROL_BYTES)\n_EXPECTED_MIGRATION_ROLE_POLICY = (False, False, False, False, False, True, True)\n\ndef _apply_migration_socket_deadline(\n    channel: socket,\n    deadline: float | None,\n) -> None:\n    if deadline is None:\n        channel.settimeout(None)\n        return\n    remaining = deadline - monotonic()\n    if remaining <= 0:\n        raise TimeoutError(\"migration IPC deadline exceeded\")\n    channel.settimeout(remaining)\n\ndef _restore_migration_blocking_socket(channel: socket) -> None:\n    try:\n        channel.settimeout(None)\n    except OSError:\n        pass\n\ndef _send_migration_frame(\n    channel: socket,\n    payload: bytes,\n    *,\n    deadline: float | None,\n) -> None:\n    if (\n        type(payload) is not bytes\n        or not 1 <= len(payload) <= _MAX_MIGRATION_FRAME_BYTES\n    ):\n        raise ValueError(\"migration IPC payload rejected\")\n    pending = memoryview(pack(\"!I\", len(payload)) + payload)\n    try:\n        while pending:\n            _apply_migration_socket_deadline(channel, deadline)\n            try:\n                sent = channel.send(pending)\n            except SocketTimeout as error:\n                raise TimeoutError(\"migration IPC send deadline exceeded\") from error\n            if sent <= 0:\n                raise OSError(\"migration IPC channel closed\")\n            pending = pending[sent:]\n    finally:\n        _restore_migration_blocking_socket(channel)\n\ndef _receive_migration_exact(\n    channel: socket,\n    size: int,\n    *,\n    deadline: float | None,\n) -> bytes:\n    received = bytearray()\n    try:\n        while len(received) < size:\n            _apply_migration_socket_deadline(channel, deadline)\n            try:\n                part = channel.recv(size - len(received))\n            except SocketTimeout as error:\n                raise TimeoutError(\n                    \"migration IPC receive deadline exceeded\"\n                ) from error\n            if not part:\n                raise EOFError(\"migration IPC channel closed\")\n            received.extend(part)\n        return bytes(received)\n    finally:\n        _restore_migration_blocking_socket(channel)\n\ndef _recv_migration_frame(\n    channel: socket,\n    maximum: int,\n    *,\n    deadline: float | None,\n) -> bytes:\n    if type(maximum) is not int or maximum <= 0:\n        raise ValueError(\"migration IPC bound rejected\")\n    header = _receive_migration_exact(channel, 4, deadline=deadline)\n    size = unpack(\"!I\", header)[0]\n    if not 1 <= size <= maximum:\n        raise ValueError(\"migration IPC frame rejected\")\n    return _receive_migration_exact(channel, size, deadline=deadline)\n\nclass MigrationFailed(RuntimeError):\n    pass\n\nclass MigrationWorkerTerminationFailed(RuntimeError):\n    pass\n\nclass MigrationRequestRejected(ValueError):\n    pass\n\nclass MigrationOutcomeUnknown(RuntimeError):\n    __slots__ = (\"_reconciliation_id\",)\n\n    def __init__(self, reconciliation_id: str) -> None:\n        if (\n            type(reconciliation_id) is not str\n            or RECONCILIATION_ID.fullmatch(reconciliation_id) is None\n        ):\n            raise TypeError(\"migration reconciliation identifier rejected\")\n        super().__init__(\"migration outcome requires reconciliation\")\n        self._reconciliation_id = reconciliation_id\n\n    @property\n    def reconciliation_id(self) -> str:\n        return self._reconciliation_id\n\n    def __reduce__(self):\n        # The opaque identifier is safe to persist. The authenticated outcome\n        # service, not Python object identity, validates and resolves it.\n        return type(self), (self._reconciliation_id,)\n\n@dataclass(frozen=True, slots=True)\nclass MigrationReconciliationStatus:\n    reconciliation_id: str\n    generation: str\n    revision: str\n    artifact_sha256: str\n    approval_id: str | None\n    correlation_id: str | None\n    approval_bound: bool\n    outcome: str\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedMigrationOpener:\n    __transport: MigrationTransportOpener\n\n    def __call__(self):\n        connection = self.__transport()\n        try:\n            identity = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            role_policy = connection.execute(\n                \"select rolsuper, rolcreaterole, rolcreatedb, rolreplication, \"\n                \"rolbypassrls, rolcanlogin, rolconnlimit = 1 \"\n                \"from pg_catalog.pg_roles where rolname = session_user\"\n            ).fetchone()\n            memberships = connection.execute(\n                \"select pg_catalog.count(*) from pg_catalog.pg_auth_members as m \"\n                \"join pg_catalog.pg_roles as r on r.oid = m.member \"\n                \"where r.rolname = session_user\"\n            ).fetchone()\n            settings = connection.execute(\n                \"select current_setting('search_path') = '', \"\n                \"current_setting('synchronous_commit') = 'on', \"\n                \"current_setting('statement_timeout')::interval \"\n                \"= interval '10 minutes', \"\n                \"current_setting('transaction_timeout')::interval \"\n                \"= interval '700 seconds', \"\n                \"current_setting('lock_timeout')::interval = interval '5 seconds', \"\n                \"current_setting('row_security') = 'on', \"\n                \"current_setting('session_replication_role') = 'origin'\"\n            ).fetchone()\n            approved = connection.execute(\n                \"select orders_security.migration_authority_approved(session_user)\"\n            ).fetchone()\n            if (\n                identity is None\n                or tuple(identity) != (\n                    \"orders\",\n                    \"orders_migrator\",\n                    \"orders_migrator\",\n                    \"scram-sha-256:orders_migrator\",\n                    True,\n                )\n                or role_policy is None\n                or tuple(role_policy) != _EXPECTED_MIGRATION_ROLE_POLICY\n                or memberships is None\n                or tuple(memberships) != (0,)\n                or settings is None\n                or tuple(settings) != (True,) * 7\n                or approved is None\n                or tuple(approved) != (True,)\n            ):\n                raise PermissionError(\"migration database authority rejected\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef _validated_audit_identifier(value: object) -> str:\n    if type(value) is not str or AUDIT_IDENTIFIER.fullmatch(value) is None:\n        raise PermissionError(\"migration audit identity rejected\")\n    return value\n\ndef _validated_optional_audit_identifier(value: object) -> str | None:\n    if value is None:\n        return None\n    return _validated_audit_identifier(value)\n\ndef _validated_generation(value: object) -> str:\n    if type(value) is not str or MIGRATION_GENERATION.fullmatch(value) is None:\n        raise PermissionError(\"migration service generation rejected\")\n    return value\n\ndef _validated_reconciliation_id(value: object) -> str:\n    if type(value) is not str or RECONCILIATION_ID.fullmatch(value) is None:\n        raise PermissionError(\"migration reconciliation identifier rejected\")\n    return value\n\ndef _accepted_message(approval_id: str, correlation_id: str) -> bytes:\n    payload = _ACCEPTED_PREFIX + approval_id.encode(\"ascii\") + b\"\\n\" + correlation_id.encode(\"ascii\")\n    if len(payload) > _MAX_ACCEPTED_BYTES:\n        raise ValueError(\"migration acceptance message rejected\")\n    return payload\n\ndef _decode_accepted_message(payload: bytes) -> tuple[str, str]:\n    parts = payload.split(b\"\\n\")\n    if len(parts) != 3 or parts[0] != b\"accepted\":\n        raise ValueError(\"migration acceptance message rejected\")\n    try:\n        approval_id = parts[1].decode(\"ascii\")\n        correlation_id = parts[2].decode(\"ascii\")\n    except UnicodeDecodeError as error:\n        raise ValueError(\"migration acceptance message rejected\") from error\n    return (\n        _validated_audit_identifier(approval_id),\n        _validated_audit_identifier(correlation_id),\n    )\n\ndef _migration_worker(\n    opener: VerifiedMigrationOpener,\n    channel: socket,\n) -> None:\n    accepted = False\n    result = _MIGRATION_REJECTED\n    try:\n        _send_migration_frame(\n            channel,\n            _READY,\n            deadline=monotonic() + MIGRATION_CONTROL_SEND_TIMEOUT_SECONDS,\n        )\n        payload = _recv_migration_frame(\n            channel,\n            _MAX_TASK_BYTES,\n            deadline=None,\n        )\n        try:\n            (\n                generation_bytes,\n                reconciliation_bytes,\n                revision_bytes,\n                digest_bytes,\n            ) = payload.split(b\":\", 3)\n            generation = generation_bytes.decode(\"ascii\")\n            reconciliation_id = reconciliation_bytes.decode(\"ascii\")\n            revision = revision_bytes.decode(\"ascii\")\n            artifact_sha256 = digest_bytes.decode(\"ascii\")\n        except (UnicodeDecodeError, ValueError) as error:\n            raise MigrationRequestRejected(\n                \"migration request rejected\"\n            ) from error\n        if (\n            MIGRATION_GENERATION.fullmatch(generation) is None\n            or RECONCILIATION_ID.fullmatch(reconciliation_id) is None\n            or REVISION.fullmatch(revision) is None\n            or ARTIFACT_SHA256.fullmatch(artifact_sha256) is None\n        ):\n            raise MigrationRequestRejected(\"migration request rejected\")\n        approval = require_signed_migration_approval(revision, artifact_sha256)\n        if (\n            type(approval) is not ApprovedMigration\n            or approval.revision != revision\n            or approval.artifact_sha256 != artifact_sha256\n        ):\n            raise PermissionError(\"migration approval rejected\")\n        approval_id = _validated_audit_identifier(approval.approval_id)\n        correlation_id = _validated_audit_identifier(approval.correlation_id)\n        _send_migration_frame(\n            channel,\n            _accepted_message(approval_id, correlation_id),\n            deadline=monotonic() + MIGRATION_CONTROL_SEND_TIMEOUT_SECONDS,\n        )\n        # The child cannot touch the database until the parent confirms that\n        # this exact approval and correlation identity are durably attached to\n        # the attempt receipt.\n        approval_binding = _recv_migration_frame(\n            channel,\n            _MAX_CONTROL_BYTES,\n            deadline=monotonic() + MIGRATION_APPROVAL_BIND_TIMEOUT_SECONDS,\n        )\n        if approval_binding != _MIGRATION_APPROVAL_BOUND:\n            raise PermissionError(\"durable migration approval binding rejected\")\n        accepted = True\n        with opener() as connection:\n            row = connection.execute(\n                \"select reconciliation_id, approval_id, correlation_id, outcome \"\n                \"from orders_admin.apply_approved_migration(\"\n                \"%s, %s, %s, %s, %s, %s)\",\n                (\n                    generation,\n                    reconciliation_id,\n                    revision,\n                    artifact_sha256,\n                    approval_id,\n                    correlation_id,\n                ),\n            ).fetchone()\n            if row is None or tuple(row) != (\n                reconciliation_id,\n                approval_id,\n                correlation_id,\n                \"applied\",\n            ):\n                raise RuntimeError(\"durable migration outcome rejected\")\n        result = _MIGRATION_APPLIED\n    except (MigrationApprovalRejected, MigrationRequestRejected):\n        result = _MIGRATION_UNKNOWN if accepted else _MIGRATION_REJECTED\n    except Exception:\n        # A defect before acceptance is safely not applied, but it is not an\n        # approval denial. After acceptance, preserve the unknown outcome.\n        result = _MIGRATION_UNKNOWN if accepted else _MIGRATION_INTERNAL_FAILURE\n    finally:\n        try:\n            _send_migration_frame(\n                channel,\n                result,\n                deadline=monotonic() + MIGRATION_CONTROL_SEND_TIMEOUT_SECONDS,\n            )\n        finally:\n            channel.close()\n\ndef _poison_migration_service() -> None:\n    global _migration_service_failed\n    _migration_service_failed = True\n\ndef _stop_migration_worker(process, channel: socket, *, deadline: float) -> None:\n    cleanup_error = None\n    try:\n        channel.close()\n    except Exception as error:\n        cleanup_error = error\n\n    try:\n        alive = process.is_alive()\n    except Exception as error:\n        cleanup_error = cleanup_error or error\n        alive = True\n    if alive:\n        try:\n            process.terminate()\n        except Exception as error:\n            cleanup_error = cleanup_error or error\n        try:\n            remaining = max(0.0, deadline - monotonic())\n            process.join(timeout=remaining / 2)\n        except Exception as error:\n            cleanup_error = cleanup_error or error\n\n    try:\n        alive = process.is_alive()\n    except Exception as error:\n        cleanup_error = cleanup_error or error\n        alive = True\n    if alive:\n        try:\n            process.kill()\n        except Exception as error:\n            cleanup_error = cleanup_error or error\n        try:\n            process.join(timeout=max(0.0, deadline - monotonic()))\n        except Exception as error:\n            cleanup_error = cleanup_error or error\n\n    try:\n        alive = process.is_alive()\n    except Exception as error:\n        cleanup_error = cleanup_error or error\n        alive = True\n    if alive:\n        _poison_migration_service()\n        raise MigrationWorkerTerminationFailed(\n            \"migration worker termination could not be confirmed\"\n        ) from cleanup_error\n    try:\n        process.join(timeout=0)\n        process.close()\n    except Exception as error:\n        cleanup_error = cleanup_error or error\n    if cleanup_error is not None:\n        _poison_migration_service()\n        raise MigrationWorkerTerminationFailed(\n            \"migration worker cleanup failed\"\n        ) from cleanup_error\n\ndef _migration_outcome(reconciliation_id: str) -> MigrationOutcomeUnknown:\n    return MigrationOutcomeUnknown(\n        _validated_reconciliation_id(reconciliation_id)\n    )\n\ndef _retire_migration_state(state, *, deadline: float) -> None:\n    remaining = deadline - monotonic()\n    if remaining <= 0 or not state.cleanup_lock.acquire(timeout=remaining):\n        _poison_migration_service()\n        raise MigrationWorkerTerminationFailed(\n            \"migration cleanup ownership deadline exceeded\"\n        )\n    try:\n        if state.retired:\n            return\n        _stop_migration_worker(state.process, state.channel, deadline=deadline)\n        state.retired = True\n    finally:\n        state.cleanup_lock.release()\n\ndef _retire_migration_request(\n    state,\n    *,\n    deadline: float,\n    reconciliation_id: str | None,\n) -> None:\n    try:\n        _retire_migration_state(state, deadline=deadline)\n    except MigrationWorkerTerminationFailed as error:\n        if reconciliation_id is None:\n            # No task was dispatched, so an applied outcome is impossible.\n            raise\n        raise _migration_outcome(reconciliation_id) from error\n\n    if reconciliation_id is None:\n        return\n    remaining = deadline - monotonic()\n    if remaining <= 0:\n        raise _migration_outcome(reconciliation_id)\n    try:\n        confirmed = confirm_migration_worker_terminated(\n            reconciliation_id,\n            state.generation,\n            timeout_seconds=min(\n                MIGRATION_RECEIPT_TIMEOUT_SECONDS,\n                remaining,\n            ),\n        )\n    except Exception as error:\n        raise _migration_outcome(reconciliation_id) from error\n    if type(confirmed) is not bool or not confirmed:\n        raise _migration_outcome(reconciliation_id)\n\n@dataclass(slots=True)\nclass _OneShotMigrationState:\n    process: object\n    channel: socket\n    lock: Lock\n    cleanup_lock: Lock\n    generation: str\n    used: bool = False\n    closing: bool = False\n    retired: bool = False\n\n_MIGRATION_CONFIGURATION_LOCK = Lock()\n_migration_state: _OneShotMigrationState | None = None\n_migration_service_failed = False\n\ndef _receive_control(\n    state: _OneShotMigrationState,\n    *,\n    deadline: float,\n) -> bytes:\n    return _recv_migration_frame(\n        state.channel,\n        _MAX_CONTROL_BYTES,\n        deadline=deadline,\n    )\ndef _confirm_worker_exit(state: _OneShotMigrationState, *, deadline: float) -> None:\n    remaining = deadline - monotonic()\n    if remaining <= 0 or not wait(\n        [state.process.sentinel],\n        timeout=max(0.0, remaining),\n    ):\n        raise TimeoutError(\"migration worker exit deadline exceeded\")\n    state.process.join(timeout=0)\n    if state.process.exitcode != 0:\n        raise RuntimeError(\"migration worker exited unsuccessfully\")\n\ndef run_approved_migration(revision: str, artifact_sha256: str) -> None:\n    deadline = monotonic() + MIGRATION_CLIENT_DEADLINE_SECONDS\n    work_deadline = deadline - MIGRATION_CLEANUP_RESERVE_SECONDS\n    if type(revision) is not str or REVISION.fullmatch(revision) is None:\n        raise ValueError(\"migration revision rejected\")\n    if (\n        type(artifact_sha256) is not str\n        or ARTIFACT_SHA256.fullmatch(artifact_sha256) is None\n    ):\n        raise ValueError(\"migration artifact digest rejected\")\n    with _MIGRATION_CONFIGURATION_LOCK:\n        if _migration_service_failed:\n            raise MigrationFailed(\"migration service requires replacement\")\n        state = _migration_state\n        if type(state) is not _OneShotMigrationState:\n            raise RuntimeError(\"migration worker is not configured\")\n        if state.closing or state.retired:\n            raise MigrationFailed(\"migration service is stopping\")\n        if not state.lock.acquire(blocking=False):\n            raise MigrationFailed(\"migration runner is busy\")\n        if state is not _migration_state or state.closing or state.retired:\n            state.lock.release()\n            raise MigrationFailed(\"migration service is stopping\")\n\n    retire = True\n    reconciliation_id = None\n    approval_id = None\n    correlation_id = None\n    try:\n        if state.used:\n            raise MigrationFailed(\"migration runner already used\")\n        state.used = True\n        try:\n            reconciliation_id = _validated_reconciliation_id(\n                begin_durable_migration_attempt(\n                    revision,\n                    artifact_sha256,\n                    state.generation,\n                    timeout_seconds=MIGRATION_RECEIPT_TIMEOUT_SECONDS,\n                )\n            )\n        except Exception as error:\n            raise MigrationFailed(\n                \"durable migration receipt unavailable\"\n            ) from error\n        try:\n            _send_migration_frame(\n                state.channel,\n                (\n                    state.generation\n                    + \":\"\n                    + reconciliation_id\n                    + \":\"\n                    + revision\n                    + \":\"\n                    + artifact_sha256\n                ).encode(\"ascii\"),\n                deadline=work_deadline,\n            )\n            first = _receive_control(state, deadline=work_deadline)\n        except (EOFError, OSError, RuntimeError, TimeoutError, ValueError) as error:\n            raise _migration_outcome(reconciliation_id) from error\n\n        if first in {_MIGRATION_REJECTED, _MIGRATION_INTERNAL_FAILURE}:\n            try:\n                _confirm_worker_exit(state, deadline=work_deadline)\n            except (OSError, RuntimeError, TimeoutError, ValueError) as error:\n                raise _migration_outcome(reconciliation_id) from error\n            try:\n                _retire_migration_request(\n                    state,\n                    deadline=deadline,\n                    reconciliation_id=reconciliation_id,\n                )\n            finally:\n                retire = False\n            if first == _MIGRATION_REJECTED:\n                raise MigrationFailed(\"migration approval or request rejected\")\n            raise MigrationFailed(\"migration worker internal failure\")\n\n        try:\n            approval_id, correlation_id = _decode_accepted_message(first)\n            remaining = work_deadline - monotonic()\n            if remaining <= 0:\n                raise TimeoutError(\"migration approval binding deadline exceeded\")\n            binding = bind_durable_migration_approval(\n                reconciliation_id,\n                state.generation,\n                revision,\n                artifact_sha256,\n                approval_id,\n                correlation_id,\n                timeout_seconds=min(\n                    MIGRATION_RECEIPT_TIMEOUT_SECONDS,\n                    remaining,\n                ),\n            )\n            if (\n                type(binding) is not DurableMigrationOutcome\n                or binding.reconciliation_id != reconciliation_id\n                or binding.generation != state.generation\n                or binding.revision != revision\n                or binding.artifact_sha256 != artifact_sha256\n                or binding.approval_id != approval_id\n                or binding.correlation_id != correlation_id\n                or binding.approval_bound is not True\n                or binding.outcome != \"pending\"\n            ):\n                raise RuntimeError(\n                    \"durable migration approval binding rejected\"\n                )\n            _send_migration_frame(\n                state.channel,\n                _MIGRATION_APPROVAL_BOUND,\n                deadline=work_deadline,\n            )\n            final = _receive_control(state, deadline=work_deadline)\n            _confirm_worker_exit(state, deadline=work_deadline)\n        except (EOFError, OSError, PermissionError, RuntimeError, TimeoutError, ValueError) as error:\n            raise _migration_outcome(reconciliation_id) from error\n\n        try:\n            _retire_migration_request(\n                state,\n                deadline=deadline,\n                reconciliation_id=reconciliation_id,\n            )\n        finally:\n            retire = False\n        if final == _MIGRATION_APPLIED:\n            return\n        raise _migration_outcome(reconciliation_id)\n    finally:\n        try:\n            if retire:\n                _retire_migration_request(\n                    state,\n                    deadline=deadline,\n                    reconciliation_id=reconciliation_id,\n                )\n        finally:\n            state.lock.release()\n\ndef read_migration_reconciliation_status(\n    error: MigrationOutcomeUnknown,\n) -> MigrationReconciliationStatus:\n    if type(error) is not MigrationOutcomeUnknown:\n        raise TypeError(\"migration reconciliation contract rejected\")\n    reconciliation_id = _validated_reconciliation_id(\n        error.reconciliation_id\n    )\n\n    # This one trusted service operation atomically verifies the opaque receipt\n    # against the authenticated current workflow attempt and reads its durable\n    # generation-fenced outcome. The caller cannot combine stale snapshots.\n    record = read_current_migration_reconciliation_status(\n        reconciliation_id,\n        timeout_seconds=MIGRATION_GENERATION_TIMEOUT_SECONDS,\n    )\n    if type(record) is not DurableMigrationOutcome:\n        raise RuntimeError(\"durable migration outcome rejected\")\n    approval_id = _validated_optional_audit_identifier(record.approval_id)\n    correlation_id = _validated_optional_audit_identifier(\n        record.correlation_id\n    )\n    if (\n        record.reconciliation_id != reconciliation_id\n        or _validated_generation(record.generation) != record.generation\n        or type(record.revision) is not str\n        or REVISION.fullmatch(record.revision) is None\n        or type(record.artifact_sha256) is not str\n        or ARTIFACT_SHA256.fullmatch(record.artifact_sha256) is None\n        or type(record.approval_bound) is not bool\n        or (record.approval_bound and (\n            approval_id is None or correlation_id is None\n        ))\n        or (not record.approval_bound and (\n            approval_id is not None or correlation_id is not None\n        ))\n        or type(record.outcome) is not str\n        or record.outcome not in {\"applied\", \"not_applied\", \"pending\"}\n        or (record.outcome == \"applied\" and not record.approval_bound)\n    ):\n        raise RuntimeError(\"durable migration outcome rejected\")\n\n    # Informational only. A retry must be claimed and executed atomically by\n    # the trusted deployment workflow; callers must not branch on this value\n    # and invoke run_approved_migration() themselves.\n    return MigrationReconciliationStatus(\n        reconciliation_id,\n        record.generation,\n        record.revision,\n        record.artifact_sha256,\n        approval_id,\n        correlation_id,\n        record.approval_bound,\n        record.outcome,\n    )\n\ndef shutdown_migration_worker(*, deadline_seconds: float = 10.0) -> None:\n    global _migration_state\n    if (\n        type(deadline_seconds) not in (int, float)\n        or not isfinite(deadline_seconds)\n        or not 4.0 <= deadline_seconds <= 30.0\n    ):\n        raise ValueError(\"migration shutdown deadline rejected\")\n    deadline = monotonic() + float(deadline_seconds)\n    drain_deadline = deadline - min(\n        MIGRATION_SHUTDOWN_CLEANUP_RESERVE_SECONDS,\n        float(deadline_seconds) / 2,\n    )\n    with _MIGRATION_CONFIGURATION_LOCK:\n        state = _migration_state\n        if state is None:\n            return\n        if type(state) is not _OneShotMigrationState:\n            raise RuntimeError(\"migration worker state rejected\")\n        state.closing = True\n        remaining = drain_deadline - monotonic()\n        acquired = remaining > 0 and state.lock.acquire(timeout=remaining)\n        if not acquired:\n            _retire_migration_state(state, deadline=deadline)\n            remaining = deadline - monotonic()\n            acquired = remaining > 0 and state.lock.acquire(timeout=remaining)\n            if not acquired:\n                _poison_migration_service()\n                raise MigrationWorkerTerminationFailed(\n                    \"migration caller did not drain before shutdown\"\n                )\n        try:\n            _retire_migration_state(state, deadline=deadline)\n            if state is not _migration_state:\n                _poison_migration_service()\n                raise RuntimeError(\"migration worker generation changed\")\n            _migration_state = None\n        finally:\n            state.lock.release()\n\ndef configure_migration_worker() -> None:\n    global _migration_state\n    with _MIGRATION_CONFIGURATION_LOCK:\n        if _migration_service_failed:\n            raise RuntimeError(\"migration service requires replacement\")\n        if _migration_state is not None:\n            raise RuntimeError(\"migration worker already configured\")\n        require_verified_migration_workload()\n        generation = _validated_generation(\n            activate_migration_service_generation(\n                timeout_seconds=MIGRATION_GENERATION_TIMEOUT_SECONDS,\n            )\n        )\n        transport = build_migration_transport_opener()\n        if type(transport) is not MigrationTransportOpener:\n            raise TypeError(\"reviewed migration database transport required\")\n        opener = VerifiedMigrationOpener(transport)\n        context = get_context(\"spawn\")\n        parent, child = socketpair()\n        process = context.Process(\n            target=_migration_worker,\n            args=(opener, child),\n            daemon=True,\n        )\n        started = False\n        try:\n            process.start()\n            started = True\n            child.close()\n            ready = _recv_migration_frame(\n                parent,\n                _MAX_CONTROL_BYTES,\n                deadline=monotonic() + 30.0,\n            )\n            if ready != _READY or not process.is_alive():\n                raise MigrationFailed(\n                    \"migration worker failed to initialize\"\n                )\n            _migration_state = _OneShotMigrationState(\n                process,\n                parent,\n                Lock(),\n                Lock(),\n                generation,\n            )\n        except BaseException:\n            if not started:\n                parent.close()\n                child.close()\n                process.close()\n            else:\n                _stop_migration_worker(\n                    process,\n                    parent,\n                    deadline=monotonic() + MIGRATION_STOP_BUDGET_SECONDS,\n                )\n            raise\n\n\n# orders_deployment/__main__.py: spawned children import this module without\n# recursively configuring another migration service.\ndef migration_service_main() -> None:\n    from orders_deployment.service_transport import serve_migration_operations\n    configure_migration_worker()\n    try:\n        serve_migration_operations(\n            run_migration=run_approved_migration,\n            read_reconciliation=read_migration_reconciliation_status,\n        )\n    finally:\n        shutdown_migration_worker()\n\nif __name__ == \"__main__\":\n    freeze_support()  # Required by some frozen Windows executables.\n    migration_service_main()\n"
    }
  ]
};
