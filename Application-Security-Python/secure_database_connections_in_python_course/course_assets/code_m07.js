window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Frameworks, ORMs, Migrations, and Background Jobs through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Build an ORM engine through the verified runtime opener",
      "language": "python",
      "blurb": "The engine binds its certificate hostname, approved numeric address, minimum TLS version, SCRAM authentication policy, and server-side operation limits. It deliberately disables checkout pre-ping: a ping is another database operation and cannot be given a complete hard deadline by closing a live PGconn from another thread. Operations that require a hard client deadline belong behind a separately killable worker boundary.",
      "code": "from sqlalchemy import URL, create_engine\n\nAPPROVED_DATABASE_HOSTADDR = \"10.20.30.40\"\n\ndef build_runtime_engine(password: str):\n    if type(password) is not str or not 16 <= len(password) <= 1024 or \"\\x00\" in password:\n        raise ValueError(\"database credential rejected\")\n    target = URL.create(\n        \"postgresql+psycopg\",\n        username=\"orders_runtime\",\n        password=password,\n        host=\"orders-db.internal.example\",\n        port=5432,\n        database=\"orders\",\n        query={\n            \"channel_binding\": \"require\",\n            \"gssencmode\": \"disable\",\n            \"hostaddr\": APPROVED_DATABASE_HOSTADDR,\n            \"options\": \"-c statement_timeout=2000 -c transaction_timeout=5000 -c idle_in_transaction_session_timeout=2500 -c lock_timeout=500\",\n            \"require_auth\": \"scram-sha-256\",\n            \"sslcertmode\": \"disable\",\n            \"ssl_min_protocol_version\": \"TLSv1.2\",\n            \"sslmode\": \"verify-full\",\n            \"sslrootcert\": \"/etc/orders-service/trust/orders-ca.pem\",\n            \"tcp_user_timeout\": \"3000\",\n        },\n    )\n    return create_engine(\n        target,\n        connect_args={\"connect_timeout\": 5},\n        pool_size=8,\n        max_overflow=4,\n        pool_timeout=2,\n        pool_recycle=900,\n        pool_pre_ping=False,\n    )\n"
    },
    {
      "title": "Guard a migration entry point",
      "language": "python",
      "blurb": "The trusted composition root captures an authenticated deployment authorizer and migration connection factory. The approved revision and digest are applied only after the database, authenticated login, effective role, and SCRAM identity match; the migration function is schema-qualified.",
      "code": "from typing import Protocol\nimport re\n\nREVISION = re.compile(r\"[0-9a-f]{12}\")\nARTIFACT_SHA256 = re.compile(r\"[0-9a-f]{64}\")\n\nclass DeploymentAuthorizer(Protocol):\n    def require_approved_migration(\n        self,\n        operation: str,\n        environment: str,\n        revision: str,\n        artifact_sha256: str,\n    ) -> None: ...\n\ndef build_migration_runner(authorizer: DeploymentAuthorizer, open_migration_connection):\n    def run_migration(revision: str, artifact_sha256: str) -> None:\n        if type(revision) is not str or REVISION.fullmatch(revision) is None:\n            raise ValueError(\"migration revision rejected\")\n        if (\n            type(artifact_sha256) is not str\n            or ARTIFACT_SHA256.fullmatch(artifact_sha256) is None\n        ):\n            raise ValueError(\"migration artifact digest rejected\")\n        authorizer.require_approved_migration(\n            \"orders-schema-migration\",\n            \"production\",\n            revision,\n            artifact_sha256,\n        )\n        with open_migration_connection() as connection:\n            row = connection.execute(\n                \"select pg_catalog.current_database(), session_user, \"\n                \"current_user, system_user\"\n            ).fetchone()\n            if row is None or tuple(row) != (\n                \"orders\",\n                \"orders_migrator\",\n                \"orders_migrator\",\n                \"scram-sha-256:orders_migrator\",\n            ):\n                raise PermissionError(\"dedicated migration identity required\")\n            connection.execute(\n                \"select orders_admin.apply_approved_migration(%s, %s)\",\n                (revision, artifact_sha256),\n            )\n\n    return run_migration\n"
    }
  ]
};
