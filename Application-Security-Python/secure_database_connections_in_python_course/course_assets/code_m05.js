window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Least Privilege and Environment Separation through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Resolve database identities through trusted deployment policy",
      "language": "python",
      "blurb": "The trusted composition root captures an authenticated workload authorizer and a private immutable role policy. Every runtime, migration, or reporting identity request is authorized before its application-owned credential reference is released.",
      "code": "from enum import Enum\nfrom types import MappingProxyType\nfrom typing import Protocol\n\nclass DatabasePurpose(Enum):\n    RUNTIME = \"runtime\"\n    MIGRATION = \"migration\"\n    REPORTING = \"reporting\"\n\nclass DatabaseIdentityAuthorizer(Protocol):\n    def require_database_identity(self, purpose: str, reference: str) -> None: ...\n\ndef build_role_reference_resolver(authorizer: DatabaseIdentityAuthorizer):\n    role_references = MappingProxyType({\n        DatabasePurpose.RUNTIME: \"vault://production/orders/runtime\",\n        DatabasePurpose.MIGRATION: \"vault://deployment/orders/migration\",\n        DatabasePurpose.REPORTING: \"vault://production/orders/reporting-readonly\",\n    })\n\n    def role_reference(purpose: DatabasePurpose) -> str:\n        if type(purpose) is not DatabasePurpose:\n            raise ValueError(\"database purpose rejected\")\n        reference = role_references[purpose]\n        authorizer.require_database_identity(purpose.value, reference)\n        return reference\n\n    return role_reference\n"
    },
    {
      "title": "Open a runtime session with the exact direct table grants",
      "language": "python",
      "blurb": "The trusted builder opens a fresh runtime connection, verifies the authenticated and effective identities, and compares every direct table grant in the application schema with one exact non-grantable allowlist. It rolls back only its own verification transaction; deployment policy must separately reject ownership, superuser status, memberships, PUBLIC access, and non-table privileges.",
      "code": "EXPECTED_SYSTEM_USER = \"scram-sha-256:orders_runtime\"\nREQUIRED_DIRECT_TABLE_GRANTS = frozenset({\n    (\"public\", \"orders\", \"SELECT\", \"NO\"),\n    (\"public\", \"orders\", \"INSERT\", \"NO\"),\n    (\"public\", \"orders\", \"UPDATE\", \"NO\"),\n})\n\ndef build_verified_runtime_opener(open_connection):\n    def open_verified_runtime_connection():\n        connection = open_connection()\n        try:\n            identity = connection.execute(\n                \"select session_user, current_user, system_user\"\n            ).fetchone()\n            if identity is None or tuple(identity) != (\n                \"orders_runtime\",\n                \"orders_runtime\",\n                EXPECTED_SYSTEM_USER,\n            ):\n                raise PermissionError(\"runtime database identity rejected\")\n\n            rows = connection.execute(\n                \"select table_schema, table_name, privilege_type, is_grantable \"\n                \"from information_schema.role_table_grants \"\n                \"where grantee = session_user and table_schema = 'public'\"\n            ).fetchall()\n            actual = frozenset(tuple(row) for row in rows)\n            if actual != REQUIRED_DIRECT_TABLE_GRANTS:\n                raise PermissionError(\"runtime direct table grants differ from policy\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\n    return open_verified_runtime_connection\n"
    }
  ]
};
