window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Least Privilege and Environment Separation through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer. This worker-only module composes worker-local policies from modules 3 and 4 into the sole raw connection opener, then verifies the current deployment generation, authenticated CA and CRL files, identity, session state, and a versioned catalog-wide read-only authority contract before returning a connection to reviewed worker operations.",
  "codeExamples": [
    {
      "title": "Bind the worker identity to one credential-reference policy",
      "language": "python",
      "blurb": "The database-worker identity contains the fixed database login and the immutable module 3 secret-reference policy, but never credential bytes. Its loader obtains both through platform-authenticated, zero-argument boundaries. The web image omits this module, and its workload identity is denied access to the referenced secret.",
      "code": "from dataclasses import dataclass, field\n\nfrom orders_db_worker.database_secret_policy import (\n    RuntimeDatabaseSecretPolicy,\n    load_runtime_database_secret_policy,\n)\n\n_RUNTIME_IDENTITY_TOKEN = object()\n\n@dataclass(frozen=True, slots=True)\nclass RuntimeDatabaseIdentity:\n    database_user: str\n    secret_policy: RuntimeDatabaseSecretPolicy\n    _construction_token: object = field(repr=False, compare=False)\n\n    def __post_init__(self) -> None:\n        if (\n            self._construction_token is not _RUNTIME_IDENTITY_TOKEN\n            or self.database_user != \"orders_runtime\"\n            or type(self.secret_policy) is not RuntimeDatabaseSecretPolicy\n            or self.secret_policy != load_runtime_database_secret_policy()\n        ):\n            raise PermissionError(\"runtime database identity rejected\")\n\ndef load_runtime_database_identity() -> RuntimeDatabaseIdentity:\n    secret_policy = load_runtime_database_secret_policy()\n    return RuntimeDatabaseIdentity(\n        \"orders_runtime\",\n        secret_policy,\n        _RUNTIME_IDENTITY_TOKEN,\n    )\n"
    },
    {
      "title": "Open through the sole complete runtime authority boundary",
      "language": "python",
      "blurb": "This is the only deployed worker component that calls Psycopg's connection API, resolves credential bytes, or returns a raw connection. It revalidates the authenticated deployment generation, target, CA, CRL, workload identity, and secret-reference policy immediately before connecting; explicitly supplies both trust files to libpq; verifies the database, login identity, effective role, authentication method, TLS, and session settings; and compares a versioned authority snapshot covering every PostgreSQL privilege and ownership class. Each spawned module 6 child builds this complete opener locally before acknowledging readiness.",
      "code": "from dataclasses import dataclass\n\nfrom orders_db_worker.database_identity import (\n    RuntimeDatabaseIdentity,\n    load_runtime_database_identity,\n)\nfrom orders_db_worker.database_policy_bootstrap import (\n    build_worker_local_runtime_transport_policy,\n)\nfrom orders_db_worker.database_transport import RuntimePostgresTransportPolicy\nfrom orders_db_worker.database_secret_policy import (\n    RuntimeDatabaseSecretPolicy,\n    load_runtime_database_secret_policy,\n)\n\nREQUIRED_DIRECT_TABLE_GRANTS = frozenset({\n    (\"public\", \"orders\", \"SELECT\", \"NO\"),\n})\nEXPECTED_RUNTIME_CONNECTION_LIMIT = 8\nRUNTIME_AUTHORITY_POLICY_VERSION = \"orders-runtime-read-v1\"\nEXPECTED_RUNTIME_AUTHORITY = (\n    RUNTIME_AUTHORITY_POLICY_VERSION,\n) + (True,) * 15\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedRuntimeOpener:\n    transport_policy: RuntimePostgresTransportPolicy\n    identity: RuntimeDatabaseIdentity\n\n    def __post_init__(self) -> None:\n        if (\n            type(self.transport_policy) is not RuntimePostgresTransportPolicy\n            or type(self.identity) is not RuntimeDatabaseIdentity\n            or self.identity != load_runtime_database_identity()\n            or self.transport_policy.target.user != self.identity.database_user\n        ):\n            raise PermissionError(\"complete runtime database policy rejected\")\n\n    def require_current_policy(self) -> RuntimeDatabaseIdentity:\n        if (\n            type(self.transport_policy) is not RuntimePostgresTransportPolicy\n            or type(self.identity) is not RuntimeDatabaseIdentity\n        ):\n            raise PermissionError(\"complete runtime database policy changed\")\n        current_identity = load_runtime_database_identity()\n        if self.identity != current_identity:\n            raise PermissionError(\"runtime database identity changed\")\n        secret_policy = current_identity.secret_policy\n        if (\n            type(secret_policy) is not RuntimeDatabaseSecretPolicy\n            or secret_policy != load_runtime_database_secret_policy()\n        ):\n            raise PermissionError(\"runtime database secret policy changed\")\n        self.transport_policy.require_current_policy()\n        return current_identity\n\n    def __call__(self):\n        current_identity = self.require_current_policy()\n        secret_policy = current_identity.secret_policy\n        ca_file, crl_file = self.transport_policy.verified_trust_files()\n        target = self.transport_policy.target\n\n        from orders_db_worker.secrets import read_secret_bytes\n        password = read_secret_bytes(\n            secret_policy.provider_id,\n            secret_policy.reference,\n        )\n        if (\n            type(password) is not bytes\n            or not 16 <= len(password) <= 1024\n            or b\"\\x00\" in password\n        ):\n            raise ValueError(\"database credential rejected\")\n        try:\n            password_text = password.decode(\"utf-8\")\n        except UnicodeDecodeError as error:\n            raise ValueError(\"database credential rejected\") from error\n\n        import psycopg\n        connection = psycopg.connect(\n            host=target.host,\n            hostaddr=target.hostaddr,\n            port=target.port,\n            dbname=target.database,\n            user=current_identity.database_user,\n            password=password_text,\n            prepare_threshold=None,\n            connect_timeout=5,\n            tcp_user_timeout=3000,\n            channel_binding=\"require\",\n            gssencmode=\"disable\",\n            require_auth=\"scram-sha-256\",\n            options=(\n                \"-csearch_path= \"\n                \"-csynchronous_commit=on \"\n                \"-cstatement_timeout=2000 \"\n                \"-ctransaction_timeout=5000 \"\n                \"-cidle_in_transaction_session_timeout=2500 \"\n                \"-clock_timeout=500 \"\n                \"-crow_security=on\"\n            ),\n            sslcertmode=\"disable\",\n            ssl_min_protocol_version=\"TLSv1.2\",\n            sslmode=\"verify-full\",\n            sslrootcert=ca_file,\n            sslcrl=crl_file,\n        )\n        try:\n            session_identity = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            settings = connection.execute(\n                \"select current_setting('search_path') = '', \"\n                \"current_setting('synchronous_commit') = 'on', \"\n                \"current_setting('statement_timeout')::interval = interval '2 seconds', \"\n                \"current_setting('transaction_timeout')::interval = interval '5 seconds', \"\n                \"current_setting('idle_in_transaction_session_timeout')::interval \"\n                \"= interval '2.5 seconds', \"\n                \"current_setting('lock_timeout')::interval = interval '500 milliseconds', \"\n                \"current_setting('row_security') = 'on', \"\n                \"current_setting('session_replication_role') = 'origin'\"\n            ).fetchone()\n            if (\n                session_identity is None\n                or tuple(session_identity) != (\n                    target.database,\n                    current_identity.database_user,\n                    current_identity.database_user,\n                    \"scram-sha-256:orders_runtime\",\n                    True,\n                )\n                or settings is None\n                or tuple(settings) != (True,) * 8\n            ):\n                raise PermissionError(\"runtime database session policy rejected\")\n\n            # orders_security.runtime_authority_snapshot() is installed by a\n            # reviewed admin migration as SECURITY DEFINER with\n            # search_path=pg_catalog, a no-login owner, and PUBLIC execution\n            # revoked. Its versioned contract expands actual and default ACLs\n            # across database, schema, relation, column, routine, sequence,\n            # large-object, tablespace, foreign-data, type, language, and\n            # parameter catalogs; checks ownership; and evaluates PUBLIC.\n            # The only allowed effective authority is CONNECT to the current\n            # database, USAGE on public and orders_security, SELECT on\n            # public.orders, and EXECUTE on this hardened assertion function.\n            authority = connection.execute(\n                \"select snapshot.policy_version, \"\n                \"snapshot.assertion_function_hardened, \"\n                \"snapshot.database_privileges_match, \"\n                \"snapshot.schema_privileges_match, \"\n                \"snapshot.table_privileges_match, \"\n                \"snapshot.column_privileges_match, \"\n                \"snapshot.routine_privileges_match, \"\n                \"snapshot.sequence_privileges_match, \"\n                \"snapshot.large_object_privileges_match, \"\n                \"snapshot.tablespace_privileges_match, \"\n                \"snapshot.foreign_data_privileges_match, \"\n                \"snapshot.type_and_language_privileges_match, \"\n                \"snapshot.parameter_privileges_match, \"\n                \"snapshot.ownership_matches, \"\n                \"snapshot.default_privileges_match, \"\n                \"snapshot.public_privileges_match \"\n                \"from orders_security.runtime_authority_snapshot() \"\n                \"as snapshot\"\n            ).fetchone()\n            role_policy = connection.execute(\n                \"select not rolsuper, not rolcreaterole, not rolcreatedb, \"\n                \"not rolreplication, not rolbypassrls, rolcanlogin, \"\n                \"rolconnlimit = %s, \"\n                \"coalesce(array_length(rolconfig, 1), 0) = 0 \"\n                \"from pg_catalog.pg_roles where rolname = session_user\",\n                (EXPECTED_RUNTIME_CONNECTION_LIMIT,),\n            ).fetchone()\n            memberships = connection.execute(\n                \"select pg_catalog.count(*) from pg_catalog.pg_auth_members as m \"\n                \"join pg_catalog.pg_roles as r on r.oid = m.member \"\n                \"where r.rolname = session_user\"\n            ).fetchone()\n            if (\n                authority is None\n                or tuple(authority) != EXPECTED_RUNTIME_AUTHORITY\n                or role_policy is None\n                or tuple(role_policy) != (True,) * 8\n                or memberships is None\n                or tuple(memberships) != (0,)\n            ):\n                raise PermissionError(\"runtime database authority rejected\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef build_verified_runtime_opener() -> VerifiedRuntimeOpener:\n    # Called inside each spawned database child. No policy object, construction\n    # token, credential, or connection is accepted from the parent process.\n    transport_policy = build_worker_local_runtime_transport_policy()\n    identity = load_runtime_database_identity()\n    opener = VerifiedRuntimeOpener(transport_policy, identity)\n    opener.require_current_policy()\n    return opener\n"
    }
  ]
};
