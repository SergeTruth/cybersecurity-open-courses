window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply TLS, Certificates, and Transport Protection through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Build a runtime-only, spawn-safe PostgreSQL transport opener",
      "language": "python",
      "blurb": "The runtime artifact exposes no purpose selector and contains no migration credential reference. Its zero-argument builder obtains platform identity and the authenticated CA digest from application-owned bootstrap modules. It disables Psycopg automatic preparation for the reviewed DISCARD ALL reset and pins durability, TLS, authentication, and session policy so ambient settings cannot weaken the runtime connection.",
      "code": "from dataclasses import dataclass, field\nfrom hashlib import sha256\nfrom hmac import compare_digest\nfrom pathlib import Path\nfrom pickle import HIGHEST_PROTOCOL, dumps\nimport re\nimport stat\n\nfrom orders_service.database_identity import (\n    RuntimeDatabaseIdentity,\n    load_runtime_database_identity,\n)\nfrom orders_service.deployment_policy import approved_database_ca_sha256\n\nDATABASE_CA_FILE = Path(\"/etc/orders-service/trust/orders-ca.pem\")\nAPPROVED_DATABASE_HOSTADDR = \"10.20.30.40\"\nAPPROVED_SECRET_PROVIDER_ID = \"production-vault-v1\"\nMAX_CA_BYTES = 1_048_576\nMAX_SERIALIZED_OPENER_BYTES = 65_536\n_OPENER_CONSTRUCTION_TOKEN = object()\n\ndef _read_runtime_secret(reference: str) -> bytes:\n    identity = load_runtime_database_identity()\n    if reference != identity.password_reference:\n        raise PermissionError(\"runtime database secret reference rejected\")\n    from orders_service.secrets import read_secret_bytes\n    return read_secret_bytes(APPROVED_SECRET_PROVIDER_ID, reference)\n\ndef _approved_ca_path(expected_sha256: str) -> str:\n    if (\n        type(expected_sha256) is not str\n        or re.fullmatch(r\"[0-9a-f]{64}\", expected_sha256) is None\n    ):\n        raise ValueError(\"approved CA digest rejected\")\n    for directory in DATABASE_CA_FILE.parents:\n        info = directory.lstat()\n        if (\n            not stat.S_ISDIR(info.st_mode)\n            or info.st_uid != 0\n            or info.st_mode & 0o022\n        ):\n            raise PermissionError(\"immutable root-owned CA ancestry required\")\n    ca_info = DATABASE_CA_FILE.lstat()\n    if (\n        not stat.S_ISREG(ca_info.st_mode)\n        or ca_info.st_uid != 0\n        or ca_info.st_mode & 0o022\n        or not 1 <= ca_info.st_size <= MAX_CA_BYTES\n    ):\n        raise PermissionError(\"trusted CA file required\")\n    ca_bytes = DATABASE_CA_FILE.read_bytes()\n    if len(ca_bytes) != ca_info.st_size:\n        raise PermissionError(\"CA file changed during validation\")\n    if not compare_digest(sha256(ca_bytes).hexdigest(), expected_sha256):\n        raise PermissionError(\"CA digest differs from deployment policy\")\n    return str(DATABASE_CA_FILE)\n\n@dataclass(frozen=True, slots=True)\nclass RuntimePostgresTransportOpener:\n    expected_ca_sha256: str\n    identity: RuntimeDatabaseIdentity\n    _construction_token: object = field(repr=False, compare=False)\n\n    def __post_init__(self) -> None:\n        if (\n            self._construction_token is not _OPENER_CONSTRUCTION_TOKEN\n            or type(self.identity) is not RuntimeDatabaseIdentity\n            or self.identity != load_runtime_database_identity()\n        ):\n            raise PermissionError(\"runtime database transport policy rejected\")\n\n    def __call__(self):\n        password = _read_runtime_secret(self.identity.password_reference)\n        if (\n            type(password) is not bytes\n            or not 16 <= len(password) <= 1024\n            or b\"\\x00\" in password\n        ):\n            raise ValueError(\"database credential rejected\")\n        try:\n            password_text = password.decode(\"utf-8\")\n        except UnicodeDecodeError as error:\n            raise ValueError(\"database credential rejected\") from error\n        ca_file = _approved_ca_path(self.expected_ca_sha256)\n        import psycopg\n        return psycopg.connect(\n            host=\"orders-db.internal.example\",\n            hostaddr=APPROVED_DATABASE_HOSTADDR,\n            port=5432,\n            dbname=\"orders\",\n            user=self.identity.database_user,\n            password=password_text,\n            prepare_threshold=None,\n            connect_timeout=5,\n            tcp_user_timeout=3000,\n            channel_binding=\"require\",\n            gssencmode=\"disable\",\n            require_auth=\"scram-sha-256\",\n            options=(\n                \"-csearch_path= \"\n                \"-csynchronous_commit=on \"\n                \"-cstatement_timeout=2000 \"\n                \"-ctransaction_timeout=5000 \"\n                \"-cidle_in_transaction_session_timeout=2500 \"\n                \"-clock_timeout=500 \"\n                \"-crow_security=on\"\n            ),\n            sslcertmode=\"disable\",\n            ssl_min_protocol_version=\"TLSv1.2\",\n            sslmode=\"verify-full\",\n            sslrootcert=ca_file,\n        )\n\ndef build_runtime_transport_opener() -> RuntimePostgresTransportOpener:\n    identity = load_runtime_database_identity()\n    opener = RuntimePostgresTransportOpener(\n        approved_database_ca_sha256(),\n        identity,\n        _OPENER_CONSTRUCTION_TOKEN,\n    )\n    _approved_ca_path(opener.expected_ca_sha256)\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"spawn-safe runtime database policy required\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"runtime database opener policy is too large\")\n    return opener\n"
    },
    {
      "title": "Construct and spawn-test the runtime session opener",
      "language": "python",
      "blurb": "The runtime session builder has no caller-supplied identity, purpose, CA policy, or authorizer. It consumes the fixed runtime transport, verifies the database, login identity, effective role, authentication method, TLS status, and security-critical session settings, then round-trips the complete top-level object through a real spawn child during trusted application bootstrap.",
      "code": "from dataclasses import dataclass\nfrom multiprocessing import get_context\nfrom multiprocessing.connection import Connection, wait\nfrom pickle import HIGHEST_PROTOCOL, dumps\n\nfrom orders_service.database_transport import (\n    RuntimePostgresTransportOpener,\n    build_runtime_transport_opener,\n)\n\nMAX_SERIALIZED_OPENER_BYTES = 65_536\nSPAWN_CHECK_TIMEOUT_SECONDS = 2.0\n_SPAWN_ACK = b\"runtime-database-opener-ready\"\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedRuntimeSessionOpener:\n    open_transport_connection: RuntimePostgresTransportOpener\n\n    def __call__(self):\n        connection = self.open_transport_connection()\n        try:\n            identity = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            settings = connection.execute(\n                \"select current_setting('search_path') = '', \"\n                \"current_setting('synchronous_commit') = 'on', \"\n                \"current_setting('statement_timeout')::interval = interval '2 seconds', \"\n                \"current_setting('transaction_timeout')::interval = interval '5 seconds', \"\n                \"current_setting('idle_in_transaction_session_timeout')::interval \"\n                \"= interval '2.5 seconds', \"\n                \"current_setting('lock_timeout')::interval = interval '500 milliseconds', \"\n                \"current_setting('row_security') = 'on', \"\n                \"current_setting('session_replication_role') = 'origin'\"\n            ).fetchone()\n            expected_identity = (\n                \"orders\",\n                \"orders_runtime\",\n                \"orders_runtime\",\n                \"scram-sha-256:orders_runtime\",\n                True,\n            )\n            if (\n                identity is None\n                or tuple(identity) != expected_identity\n                or settings is None\n                or tuple(settings) != (True, True, True, True, True, True, True, True)\n            ):\n                raise ConnectionError(\"runtime database session policy rejected\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef _confirm_spawn_payload(_opener: object, sender: Connection) -> None:\n    try:\n        sender.send_bytes(_SPAWN_ACK)\n    finally:\n        sender.close()\n\ndef _stop_spawn_check(worker) -> None:\n    if worker.is_alive():\n        worker.terminate()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        worker.kill()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        raise RuntimeError(\"spawn check worker could not be stopped\")\n\ndef require_spawn_round_trip(opener: object) -> None:\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"runtime database opener is not serializable\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"runtime database opener policy is too large\")\n    context = get_context(\"spawn\")\n    receiver, sender = context.Pipe(duplex=False)\n    worker = context.Process(\n        target=_confirm_spawn_payload,\n        args=(opener, sender),\n        daemon=True,\n    )\n    started = False\n    try:\n        worker.start()\n        started = True\n        sender.close()\n        if not wait([worker.sentinel], timeout=SPAWN_CHECK_TIMEOUT_SECONDS):\n            raise TypeError(\"runtime database opener spawn check timed out\")\n        worker.join(timeout=0)\n        if worker.exitcode != 0 or not receiver.poll(0):\n            raise TypeError(\"runtime database opener failed spawn reconstruction\")\n        if receiver.recv_bytes(len(_SPAWN_ACK)) != _SPAWN_ACK:\n            raise TypeError(\"runtime database opener spawn acknowledgment rejected\")\n    finally:\n        if not started:\n            sender.close()\n        receiver.close()\n        if started:\n            _stop_spawn_check(worker)\n        if not worker.is_alive():\n            worker.close()\n\ndef build_verified_runtime_session_opener() -> VerifiedRuntimeSessionOpener:\n    transport = build_runtime_transport_opener()\n    if type(transport) is not RuntimePostgresTransportOpener:\n        raise TypeError(\"reviewed runtime PostgreSQL transport required\")\n    opener = VerifiedRuntimeSessionOpener(transport)\n    require_spawn_round_trip(opener)\n    return opener\n"
    }
  ]
};
