window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply TLS, Certificates, and Transport Protection through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Build a spawn-safe PostgreSQL transport opener",
      "language": "python",
      "blurb": "The trusted composition root returns a top-level, zero-argument, picklable transport policy for one application-approved database identity. Internal builders select the reviewed runtime or migration role and secret reference. The opener serializes only an authenticated CA digest and fixed identifiers; process-local code retrieves the credential after the child starts.",
      "code": "from dataclasses import dataclass\nfrom hashlib import sha256\nfrom hmac import compare_digest\nfrom pathlib import Path\nfrom pickle import HIGHEST_PROTOCOL, dumps\nfrom typing import Protocol\nimport re\nimport stat\n\nDATABASE_CA_FILE = Path(\"/etc/orders-service/trust/orders-ca.pem\")\nAPPROVED_DATABASE_HOSTADDR = \"10.20.30.40\"\nAPPROVED_SECRET_PROVIDER_ID = \"production-vault-v1\"\nDATABASE_PASSWORD_REFERENCE = \"vault://production/orders/runtime-password\"\nMIGRATION_PASSWORD_REFERENCE = \"vault://deployment/orders/migration-password\"\nAPPROVED_DATABASE_IDENTITIES = frozenset({\n    (\"orders_runtime\", DATABASE_PASSWORD_REFERENCE),\n    (\"orders_migrator\", MIGRATION_PASSWORD_REFERENCE),\n})\nMAX_CA_BYTES = 1_048_576\nMAX_SERIALIZED_OPENER_BYTES = 65_536\n\nclass TLSDeploymentPolicy(Protocol):\n    def approved_database_ca_sha256(self) -> str: ...\n\ndef _read_process_secret(provider_id: str, reference: str) -> bytes:\n    if (\n        provider_id != APPROVED_SECRET_PROVIDER_ID\n        or not any(reference == approved[1] for approved in APPROVED_DATABASE_IDENTITIES)\n    ):\n        raise PermissionError(\"database secret reference rejected\")\n    # This application-owned function resolves its provider from trusted,\n    # process-local bootstrap state. No provider object crosses the spawn pipe.\n    from orders_service.secrets import read_secret_bytes\n    return read_secret_bytes(provider_id, reference)\n\ndef _approved_ca_path(expected_sha256: str) -> str:\n    if type(expected_sha256) is not str or re.fullmatch(r\"[0-9a-f]{64}\", expected_sha256) is None:\n        raise ValueError(\"approved CA digest rejected\")\n    for directory in DATABASE_CA_FILE.parents:\n        info = directory.lstat()\n        if not stat.S_ISDIR(info.st_mode) or info.st_uid != 0 or info.st_mode & 0o022:\n            raise PermissionError(\"immutable root-owned CA ancestry required\")\n    ca_info = DATABASE_CA_FILE.lstat()\n    if (\n        not stat.S_ISREG(ca_info.st_mode)\n        or ca_info.st_uid != 0\n        or ca_info.st_mode & 0o022\n        or not 1 <= ca_info.st_size <= MAX_CA_BYTES\n    ):\n        raise PermissionError(\"trusted CA file required\")\n    ca_bytes = DATABASE_CA_FILE.read_bytes()\n    if len(ca_bytes) != ca_info.st_size:\n        raise PermissionError(\"CA file changed during validation\")\n    if not compare_digest(sha256(ca_bytes).hexdigest(), expected_sha256):\n        raise PermissionError(\"CA digest differs from deployment policy\")\n    return str(DATABASE_CA_FILE)\n\n@dataclass(frozen=True, slots=True)\nclass PostgresTransportOpener:\n    expected_ca_sha256: str\n    database_user: str\n    password_reference: str\n\n    def __post_init__(self) -> None:\n        if (self.database_user, self.password_reference) not in APPROVED_DATABASE_IDENTITIES:\n            raise ValueError(\"database identity rejected\")\n\n    def __call__(self):\n        password = _read_process_secret(\n            APPROVED_SECRET_PROVIDER_ID,\n            self.password_reference,\n        )\n        if type(password) is not bytes or not 16 <= len(password) <= 1024 or b\"\\x00\" in password:\n            raise ValueError(\"database credential rejected\")\n        try:\n            password_text = password.decode(\"utf-8\")\n        except UnicodeDecodeError as error:\n            raise ValueError(\"database credential rejected\") from error\n        ca_file = _approved_ca_path(self.expected_ca_sha256)\n        import psycopg\n        return psycopg.connect(\n            host=\"orders-db.internal.example\",\n            hostaddr=APPROVED_DATABASE_HOSTADDR,\n            port=5432,\n            dbname=\"orders\",\n            user=self.database_user,\n            password=password_text,\n            connect_timeout=5,\n            tcp_user_timeout=3000,\n            channel_binding=\"require\",\n            gssencmode=\"disable\",\n            require_auth=\"scram-sha-256\",\n            options=(\n                \"-c statement_timeout=2000 \"\n                \"-c transaction_timeout=5000 \"\n                \"-c idle_in_transaction_session_timeout=2500 \"\n                \"-c lock_timeout=500\"\n            ),\n            sslcertmode=\"disable\",\n            ssl_min_protocol_version=\"TLSv1.2\",\n            sslmode=\"verify-full\",\n            sslrootcert=ca_file,\n        )\n\ndef _build_postgres_transport_opener(\n    deployment_policy: TLSDeploymentPolicy,\n    *,\n    database_user: str,\n    password_reference: str,\n) -> PostgresTransportOpener:\n    opener = PostgresTransportOpener(\n        deployment_policy.approved_database_ca_sha256(),\n        database_user,\n        password_reference,\n    )\n    _approved_ca_path(opener.expected_ca_sha256)\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"spawn-safe database policy required\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"database opener policy is too large\")\n    return opener\n\ndef build_postgres_transport_opener(\n    deployment_policy: TLSDeploymentPolicy,\n) -> PostgresTransportOpener:\n    return _build_postgres_transport_opener(\n        deployment_policy,\n        database_user=\"orders_runtime\",\n        password_reference=DATABASE_PASSWORD_REFERENCE,\n    )\n\ndef build_postgres_migration_transport_opener(\n    deployment_policy: TLSDeploymentPolicy,\n) -> PostgresTransportOpener:\n    return _build_postgres_transport_opener(\n        deployment_policy,\n        database_user=\"orders_migrator\",\n        password_reference=MIGRATION_PASSWORD_REFERENCE,\n    )\n"
    },
    {
      "title": "Construct and spawn-test the complete verified opener",
      "language": "python",
      "blurb": "Called from a safe application bootstrap entry point, this builder constructs the exact reviewed transport itself and wraps it in a top-level frozen session verifier. It bounds the serialized policy and starts a real spawn child that reconstructs the complete object without retrieving a credential or opening the database.",
      "code": "from dataclasses import dataclass\nfrom multiprocessing import get_context\nfrom multiprocessing.connection import Connection, wait\nfrom pickle import HIGHEST_PROTOCOL, dumps\n\nfrom orders_service.database_transport import (\n    PostgresTransportOpener,\n    TLSDeploymentPolicy,\n    build_postgres_transport_opener,\n)\n\nEXPECTED_SYSTEM_USER = \"scram-sha-256:orders_runtime\"\nMAX_SERIALIZED_OPENER_BYTES = 65_536\nSPAWN_CHECK_TIMEOUT_SECONDS = 2.0\n_SPAWN_ACK = b\"database-opener-ready\"\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedDatabaseSessionOpener:\n    open_transport_connection: PostgresTransportOpener\n\n    def __call__(self):\n        connection = self.open_transport_connection()\n        try:\n            row = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            expected = (\n                \"orders\",\n                \"orders_runtime\",\n                \"orders_runtime\",\n                EXPECTED_SYSTEM_USER,\n                True,\n            )\n            if row is None or tuple(row) != expected:\n                raise ConnectionError(\"database session identity rejected\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef _confirm_spawn_payload(\n    _opener: object,\n    sender: Connection,\n) -> None:\n    try:\n        sender.send_bytes(_SPAWN_ACK)\n    finally:\n        sender.close()\n\ndef _stop_spawn_check(worker) -> None:\n    if worker.is_alive():\n        worker.terminate()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        worker.kill()\n        worker.join(timeout=0.25)\n    if worker.is_alive():\n        raise RuntimeError(\"spawn check worker could not be stopped\")\n\ndef require_spawn_round_trip(opener: object) -> None:\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"database opener is not serializable\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"database opener policy is too large\")\n\n    context = get_context(\"spawn\")\n    receiver, sender = context.Pipe(duplex=False)\n    worker = context.Process(\n        target=_confirm_spawn_payload,\n        args=(opener, sender),\n        daemon=True,\n    )\n    started = False\n    try:\n        try:\n            worker.start()\n            started = True\n        except Exception as error:\n            raise TypeError(\"database opener failed spawn bootstrap\") from error\n        sender.close()\n        if not wait([worker.sentinel], timeout=SPAWN_CHECK_TIMEOUT_SECONDS):\n            raise TypeError(\"database opener spawn check timed out\")\n        worker.join(timeout=0)\n        if worker.exitcode != 0 or not receiver.poll(0):\n            raise TypeError(\"database opener failed spawn reconstruction\")\n        try:\n            acknowledgment = receiver.recv_bytes(len(_SPAWN_ACK))\n        except (EOFError, OSError) as error:\n            raise TypeError(\"database opener spawn check failed\") from error\n        if acknowledgment != _SPAWN_ACK:\n            raise TypeError(\"database opener spawn acknowledgment rejected\")\n    finally:\n        if not started:\n            sender.close()\n        receiver.close()\n        if started:\n            _stop_spawn_check(worker)\n        if not worker.is_alive():\n            worker.close()\n\ndef build_verified_database_opener(\n    deployment_policy: TLSDeploymentPolicy,\n) -> VerifiedDatabaseSessionOpener:\n    transport = build_postgres_transport_opener(deployment_policy)\n    if type(transport) is not PostgresTransportOpener:\n        raise TypeError(\"reviewed PostgreSQL transport required\")\n    opener = VerifiedDatabaseSessionOpener(transport)\n    require_spawn_round_trip(opener)\n    return opener\n"
    }
  ]
};
