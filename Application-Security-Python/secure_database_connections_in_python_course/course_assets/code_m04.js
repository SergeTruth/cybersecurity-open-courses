window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply TLS, Certificates, and Transport Protection through concrete, reviewable Python controls. Tested contract: Python 3.11, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer.",
  "codeExamples": [
    {
      "title": "Build a spawn-safe PostgreSQL transport opener",
      "language": "python",
      "blurb": "The trusted composition root returns a top-level, zero-argument, picklable transport policy. It serializes only an authenticated CA digest; a fixed provider identifier and secret reference are resolved through application-owned process-local code after the child starts, so neither a provider object nor credential state enters the spawn payload.",
      "code": "from dataclasses import dataclass\nfrom hashlib import sha256\nfrom hmac import compare_digest\nfrom pathlib import Path\nfrom pickle import HIGHEST_PROTOCOL, dumps\nfrom typing import Protocol\nimport re\nimport stat\n\nDATABASE_CA_FILE = Path(\"/etc/orders-service/trust/orders-ca.pem\")\nAPPROVED_DATABASE_HOSTADDR = \"10.20.30.40\"\nAPPROVED_SECRET_PROVIDER_ID = \"production-vault-v1\"\nDATABASE_PASSWORD_REFERENCE = \"vault://production/orders/runtime-password\"\nMAX_CA_BYTES = 1_048_576\nMAX_SERIALIZED_OPENER_BYTES = 65_536\n\nclass TLSDeploymentPolicy(Protocol):\n    def approved_database_ca_sha256(self) -> str: ...\n\ndef _read_process_secret(provider_id: str, reference: str) -> bytes:\n    if (\n        provider_id != APPROVED_SECRET_PROVIDER_ID\n        or reference != DATABASE_PASSWORD_REFERENCE\n    ):\n        raise PermissionError(\"database secret reference rejected\")\n    # This application-owned function resolves its provider from trusted,\n    # process-local bootstrap state. No provider object crosses the spawn pipe.\n    from orders_service.secrets import read_secret_bytes\n    return read_secret_bytes(provider_id, reference)\n\ndef _approved_ca_path(expected_sha256: str) -> str:\n    if type(expected_sha256) is not str or re.fullmatch(r\"[0-9a-f]{64}\", expected_sha256) is None:\n        raise ValueError(\"approved CA digest rejected\")\n    for directory in DATABASE_CA_FILE.parents:\n        info = directory.lstat()\n        if not stat.S_ISDIR(info.st_mode) or info.st_uid != 0 or info.st_mode & 0o022:\n            raise PermissionError(\"immutable root-owned CA ancestry required\")\n    ca_info = DATABASE_CA_FILE.lstat()\n    if (\n        not stat.S_ISREG(ca_info.st_mode)\n        or ca_info.st_uid != 0\n        or ca_info.st_mode & 0o022\n        or not 1 <= ca_info.st_size <= MAX_CA_BYTES\n    ):\n        raise PermissionError(\"trusted CA file required\")\n    ca_bytes = DATABASE_CA_FILE.read_bytes()\n    if len(ca_bytes) != ca_info.st_size:\n        raise PermissionError(\"CA file changed during validation\")\n    if not compare_digest(sha256(ca_bytes).hexdigest(), expected_sha256):\n        raise PermissionError(\"CA digest differs from deployment policy\")\n    return str(DATABASE_CA_FILE)\n\n@dataclass(frozen=True, slots=True)\nclass PostgresTransportOpener:\n    expected_ca_sha256: str\n\n    def __call__(self):\n        password = _read_process_secret(\n            APPROVED_SECRET_PROVIDER_ID,\n            DATABASE_PASSWORD_REFERENCE,\n        )\n        if type(password) is not bytes or not 16 <= len(password) <= 1024 or b\"\\x00\" in password:\n            raise ValueError(\"database credential rejected\")\n        try:\n            password_text = password.decode(\"utf-8\")\n        except UnicodeDecodeError as error:\n            raise ValueError(\"database credential rejected\") from error\n        ca_file = _approved_ca_path(self.expected_ca_sha256)\n        import psycopg\n        return psycopg.connect(\n            host=\"orders-db.internal.example\",\n            hostaddr=APPROVED_DATABASE_HOSTADDR,\n            port=5432,\n            dbname=\"orders\",\n            user=\"orders_runtime\",\n            password=password_text,\n            connect_timeout=5,\n            tcp_user_timeout=3000,\n            channel_binding=\"require\",\n            gssencmode=\"disable\",\n            require_auth=\"scram-sha-256\",\n            sslcertmode=\"disable\",\n            ssl_min_protocol_version=\"TLSv1.2\",\n            sslmode=\"verify-full\",\n            sslrootcert=ca_file,\n        )\n\ndef build_postgres_transport_opener(\n    deployment_policy: TLSDeploymentPolicy,\n) -> PostgresTransportOpener:\n    opener = PostgresTransportOpener(\n        deployment_policy.approved_database_ca_sha256()\n    )\n    _approved_ca_path(opener.expected_ca_sha256)\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"spawn-safe database policy required\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"database opener policy is too large\")\n    return opener\n"
    },
    {
      "title": "Wrap transport opening in spawn-safe session verification",
      "language": "python",
      "blurb": "A top-level frozen wrapper performs the connection and post-connect identity checks within one callable. The builder serializes this complete wrapper—not only its transport opener—so the same object that passes spawn validation also checks the database, authenticated login, effective role, authentication identity, and TLS evidence before returning a connection.",
      "code": "from dataclasses import dataclass\nfrom pickle import HIGHEST_PROTOCOL, dumps\nfrom typing import Protocol\n\nEXPECTED_SYSTEM_USER = \"scram-sha-256:orders_runtime\"\nMAX_SERIALIZED_OPENER_BYTES = 65_536\n\nclass SpawnSafeTransportOpener(Protocol):\n    def __call__(self): ...\n\n@dataclass(frozen=True, slots=True)\nclass VerifiedDatabaseSessionOpener:\n    open_transport_connection: SpawnSafeTransportOpener\n\n    def __call__(self):\n        connection = self.open_transport_connection()\n        try:\n            row = connection.execute(\n                \"select pg_catalog.current_database(), session_user, current_user, \"\n                \"system_user, ssl from pg_catalog.pg_stat_ssl \"\n                \"where pid = pg_catalog.pg_backend_pid()\"\n            ).fetchone()\n            expected = (\n                \"orders\",\n                \"orders_runtime\",\n                \"orders_runtime\",\n                EXPECTED_SYSTEM_USER,\n                True,\n            )\n            if row is None or tuple(row) != expected:\n                raise ConnectionError(\"database session identity rejected\")\n            connection.rollback()\n            return connection\n        except BaseException:\n            try:\n                connection.close()\n            except Exception:\n                pass\n            raise\n\ndef build_verified_database_opener(\n    open_transport_connection: SpawnSafeTransportOpener,\n) -> VerifiedDatabaseSessionOpener:\n    opener = VerifiedDatabaseSessionOpener(open_transport_connection)\n    try:\n        serialized = dumps(opener, protocol=HIGHEST_PROTOCOL)\n    except Exception as error:\n        raise TypeError(\"complete database opener must be spawn-safe\") from error\n    if len(serialized) > MAX_SERIALIZED_OPENER_BYTES:\n        raise ValueError(\"database opener policy is too large\")\n    return opener\n"
    }
  ]
};
