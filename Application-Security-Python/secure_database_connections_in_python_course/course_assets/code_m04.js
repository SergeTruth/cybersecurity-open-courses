window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply TLS, Certificates, and Transport Protection through concrete, reviewable Python controls. Tested contract: Python 3.11 on a Linux/POSIX database-worker image, Psycopg 3.3.4, psycopg_pool 3.3.1, SQLAlchemy 2.0.51, and PostgreSQL/libpq 17 or newer. These worker-only modules authenticate immutable CA, CRL, endpoint, and deployment-generation policy without importing Psycopg, resolving a secret, returning a raw connection, or serializing policy objects between processes.",
  "codeExamples": [
    {
      "title": "Build an authenticated PostgreSQL transport policy",
      "language": "python",
      "blurb": "The worker-only builder combines the canonical module 3 target with one authenticated deployment record containing a monotonic policy generation plus CA and CRL digests. The record's generation changes whenever endpoint, trust, or credential versions change. On the declared Linux/POSIX worker image, the builder verifies root-owned non-writable ancestry, rejects links and oversized files, authenticates both trust files, and returns only an immutable policy value. Module 5 rechecks the generation and both files before connecting; this module contains neither Psycopg nor the secret adapter.",
      "code": "from dataclasses import dataclass, field\nfrom hashlib import sha256\nfrom hmac import compare_digest\nfrom pathlib import Path\nimport os\nimport re\nimport stat\n\nfrom orders_db_worker.database_target import (\n    DatabaseTarget,\n    load_runtime_database_target,\n)\nfrom orders_db_worker.deployment_policy import (\n    ApprovedRuntimeDatabasePolicy,\n    approved_runtime_database_policy,\n)\n\nDATABASE_CA_FILE = Path(\"/etc/orders-db-worker/trust/orders-ca.pem\")\nDATABASE_CRL_FILE = Path(\"/etc/orders-db-worker/trust/orders.crl\")\nMAX_TRUST_FILE_BYTES = 1_048_576\nDATABASE_POLICY_GENERATION = re.compile(r\"dbp_[A-Za-z0-9_-]{43}\")\n_TRANSPORT_POLICY_TOKEN = object()\n\ndef _current_database_policy() -> ApprovedRuntimeDatabasePolicy:\n    policy = approved_runtime_database_policy()\n    if (\n        type(policy) is not ApprovedRuntimeDatabasePolicy\n        or type(policy.generation) is not str\n        or DATABASE_POLICY_GENERATION.fullmatch(policy.generation) is None\n        or type(policy.ca_sha256) is not str\n        or re.fullmatch(r\"[0-9a-f]{64}\", policy.ca_sha256) is None\n        or type(policy.crl_sha256) is not str\n        or re.fullmatch(r\"[0-9a-f]{64}\", policy.crl_sha256) is None\n    ):\n        raise PermissionError(\"approved runtime database policy rejected\")\n    # The authenticated policy service increments generation whenever the\n    # endpoint, CA, CRL, or credential version changes.\n    return policy\n\ndef _approved_trust_path(path: Path, expected_sha256: str) -> str:\n    if os.name != \"posix\":\n        raise RuntimeError(\"POSIX database-worker filesystem required\")\n    if path not in (DATABASE_CA_FILE, DATABASE_CRL_FILE):\n        raise PermissionError(\"database trust path rejected\")\n    if (\n        type(expected_sha256) is not str\n        or re.fullmatch(r\"[0-9a-f]{64}\", expected_sha256) is None\n    ):\n        raise ValueError(\"approved trust digest rejected\")\n    for directory in path.parents:\n        info = directory.lstat()\n        if (\n            not stat.S_ISDIR(info.st_mode)\n            or info.st_uid != 0\n            or info.st_mode & 0o022\n        ):\n            raise PermissionError(\"immutable root-owned trust ancestry required\")\n    file_info = path.lstat()\n    if (\n        not stat.S_ISREG(file_info.st_mode)\n        or file_info.st_uid != 0\n        or file_info.st_mode & 0o022\n        or not 1 <= file_info.st_size <= MAX_TRUST_FILE_BYTES\n    ):\n        raise PermissionError(\"trusted database file required\")\n    file_bytes = path.read_bytes()\n    if len(file_bytes) != file_info.st_size:\n        raise PermissionError(\"database trust file changed during validation\")\n    if not compare_digest(sha256(file_bytes).hexdigest(), expected_sha256):\n        raise PermissionError(\"database trust digest differs from policy\")\n    return str(path)\n\ndef _approved_ca_path(expected_sha256: str) -> str:\n    return _approved_trust_path(DATABASE_CA_FILE, expected_sha256)\n\ndef _approved_crl_path(expected_sha256: str) -> str:\n    return _approved_trust_path(DATABASE_CRL_FILE, expected_sha256)\n\n@dataclass(frozen=True, slots=True)\nclass RuntimePostgresTransportPolicy:\n    target: DatabaseTarget\n    generation: str\n    expected_ca_sha256: str\n    expected_crl_sha256: str\n    ca_file: str\n    crl_file: str\n    _construction_token: object = field(repr=False, compare=False)\n\n    def __post_init__(self) -> None:\n        approval = _current_database_policy()\n        if (\n            self._construction_token is not _TRANSPORT_POLICY_TOKEN\n            or type(self.target) is not DatabaseTarget\n            or self.target != load_runtime_database_target()\n            or type(self.generation) is not str\n            or DATABASE_POLICY_GENERATION.fullmatch(self.generation) is None\n            or type(self.expected_ca_sha256) is not str\n            or re.fullmatch(r\"[0-9a-f]{64}\", self.expected_ca_sha256) is None\n            or type(self.expected_crl_sha256) is not str\n            or re.fullmatch(r\"[0-9a-f]{64}\", self.expected_crl_sha256) is None\n            or self.generation != approval.generation\n            or not compare_digest(self.expected_ca_sha256, approval.ca_sha256)\n            or not compare_digest(self.expected_crl_sha256, approval.crl_sha256)\n            or self.ca_file != str(DATABASE_CA_FILE)\n            or self.crl_file != str(DATABASE_CRL_FILE)\n        ):\n            raise PermissionError(\"runtime database transport policy rejected\")\n        _approved_ca_path(approval.ca_sha256)\n        _approved_crl_path(approval.crl_sha256)\n\n    def require_current_policy(self) -> ApprovedRuntimeDatabasePolicy:\n        approval = _current_database_policy()\n        if (\n            self._construction_token is not _TRANSPORT_POLICY_TOKEN\n            or type(self.target) is not DatabaseTarget\n            or self.target != load_runtime_database_target()\n            or type(self.generation) is not str\n            or DATABASE_POLICY_GENERATION.fullmatch(self.generation) is None\n            or type(self.expected_ca_sha256) is not str\n            or re.fullmatch(r\"[0-9a-f]{64}\", self.expected_ca_sha256) is None\n            or type(self.expected_crl_sha256) is not str\n            or re.fullmatch(r\"[0-9a-f]{64}\", self.expected_crl_sha256) is None\n            or self.generation != approval.generation\n            or not compare_digest(self.expected_ca_sha256, approval.ca_sha256)\n            or not compare_digest(self.expected_crl_sha256, approval.crl_sha256)\n            or self.ca_file != str(DATABASE_CA_FILE)\n            or self.crl_file != str(DATABASE_CRL_FILE)\n        ):\n            raise PermissionError(\"runtime database policy generation changed\")\n        return approval\n\n    def verified_trust_files(self) -> tuple[str, str]:\n        approval = self.require_current_policy()\n        return (\n            _approved_ca_path(approval.ca_sha256),\n            _approved_crl_path(approval.crl_sha256),\n        )\n\ndef build_runtime_transport_policy() -> RuntimePostgresTransportPolicy:\n    target = load_runtime_database_target()\n    approval = _current_database_policy()\n    ca_file = _approved_ca_path(approval.ca_sha256)\n    crl_file = _approved_crl_path(approval.crl_sha256)\n    return RuntimePostgresTransportPolicy(\n        target,\n        approval.generation,\n        approval.ca_sha256,\n        approval.crl_sha256,\n        ca_file,\n        crl_file,\n        _TRANSPORT_POLICY_TOKEN,\n    )\n"
    },
    {
      "title": "Rebuild the transport policy inside each worker",
      "language": "python",
      "blurb": "Each spawned database child calls this import-only builder after process reconstruction. It reloads authenticated endpoint, generation, CA, and CRL policy from the child's own environment and revalidates every construction invariant. No policy object, construction token, credential, or raw connection crosses the multiprocessing serializer. Module 6 performs this worker-local bootstrap before acknowledging readiness.",
      "code": "from orders_db_worker.database_transport import (\n    RuntimePostgresTransportPolicy,\n    build_runtime_transport_policy,\n)\n\ndef build_worker_local_runtime_transport_policy(\n) -> RuntimePostgresTransportPolicy:\n    # This function runs inside the spawned database child. The child reloads\n    # authenticated deployment state instead of accepting a pickled policy.\n    policy = build_runtime_transport_policy()\n    if type(policy) is not RuntimePostgresTransportPolicy:\n        raise TypeError(\"reviewed runtime PostgreSQL transport policy required\")\n    policy.require_current_policy()\n    return policy\n"
    }
  ]
};
