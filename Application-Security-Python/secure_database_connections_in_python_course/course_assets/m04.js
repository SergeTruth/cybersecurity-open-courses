window.COURSE_MODULE = {
  "title": "TLS, Certificates, and Transport Protection",
  "graphicAlt": "Client and database exchange certificates across an encrypted channel; the client checks the CA chain and expected DNS name before the session reaches the authenticated database endpoint.",
  "narration": "Transport protection matters because database traffic can include credentials, session setup, query text, result data, metadata, and operational signals. Depending on architecture, that traffic may cross hosts, subnets, cloud network boundaries, proxies, or managed service infrastructure. TLS can help protect confidentiality and integrity in transit, but only when it is configured to validate the endpoint rather than merely wrapping bytes in encryption.\n\nServer certificate validation is the part many teams accidentally weaken. The application should verify that the database endpoint presents a certificate chaining to a trusted authority and matching the expected identity for the service. If the client accepts any certificate, disables hostname checks, or treats validation errors as development noise that follows the code into production, encryption may no longer prove that the application is talking to the intended database endpoint.\n\nCertificate authority trust and hostname validation should be explicit enough for review. Some deployments rely on public trust stores, some rely on private authorities, and some managed database services publish their own guidance. The stable principle is that production clients should not silently trust arbitrary endpoints. Client certificates can also be used in some designs to authenticate the application, but their private keys must be protected like other high-value secrets.\n\nDevelopment and production settings should not be casually interchangeable. Local development may use test certificates, local containers, or temporary endpoints, but those exceptions should be visibly scoped and prevented from becoming production defaults. Transport encryption is strongest when paired with endpoint identity validation, least-privilege database accounts, protected private keys, and monitoring that can identify clients using unsafe or unexpected connection modes.",
  "narrationPoints": [
    "Depending on architecture, that traffic may cross hosts, subnets, cloud network boundaries, proxies, or managed service infrastructure.",
    "Server certificate validation is the part many teams accidentally weaken.",
    "The stable principle is that production clients should not silently trust arbitrary endpoints.",
    "Development and production settings should not be casually interchangeable.",
    "Transport protection matters because database traffic can include credentials, session setup, query text, result data, metadata, and operational signals.",
    "Certificate authority trust and hostname validation should be explicit enough for review."
  ]
};
