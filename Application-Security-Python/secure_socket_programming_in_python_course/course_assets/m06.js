window.COURSE_MODULE = {
  "title": "Parsing, Validation, and Resource Limits",
  "graphicAlt": "An incremental parser accumulates bytes until one delimiter, enforcing command grammar, maximum frame size, message count, aggregate bytes, idle time, and reply size.",
  "narration": "Received bytes should be treated as untrusted input. A socket parser should enforce message size limits, field types, numeric ranges, allowed commands, expected state, and business rules before data reaches sensitive logic. The parser should reject invalid messages predictably and avoid revealing unnecessary internal details in error responses. Input validation is not only for web forms. It is fundamental to every network protocol.\n\nUnsafe deserialization and overly flexible parsing are common design hazards at a high level. If a protocol accepts complex objects, expression-like data, or dynamically interpreted content from a peer, the application has to understand the risk. Simpler formats with explicit fields and allowlisted commands are easier to validate, log, and reason about. The protocol should make unsafe behavior difficult to express.\n\nResource limits protect availability. A peer may send oversized messages, send data slowly, open many connections, trigger expensive parsing paths, or create backpressure through slow reads and writes. The server and client should define limits for message size, connection count, memory use, CPU-intensive operations, and time spent waiting. These controls are not only performance tuning. They define how the protocol behaves under stress.\n\nSafe error responses should be consistent and limited. The peer can be told that a message is invalid, unauthorized, too large, or unsupported without receiving stack traces, secrets, internal object names, or detailed parser internals. Logging can preserve safe diagnostic information for operators. A well-designed parser fails closed, preserves resources, and gives legitimate clients enough information to recover without helping untrusted peers map the implementation.",
  "narrationPoints": [
    "The parser should reject invalid messages predictably and avoid revealing unnecessary internal details in error responses.",
    "Simpler formats with explicit fields and allowlisted commands are easier to validate, log, and reason about.",
    "The server and client should define limits for message size, connection count, memory use, CPU-intensive operations, and time spent waiting.",
    "A well-designed parser fails closed, preserves resources, and gives legitimate clients enough information to recover without helping untrusted peers map the implementation.",
    "Unsafe deserialization and overly flexible parsing are common design hazards at a high level.",
    "The protocol should make unsafe behavior difficult to express."
  ]
};
