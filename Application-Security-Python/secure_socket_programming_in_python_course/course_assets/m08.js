window.COURSE_MODULE = {
  "title": "Concurrency, Logging, Testing, and Monitoring",
  "graphicAlt": "A selector loop owns several nonblocking client sockets, each with its own buffer and deadline; finite telemetry counts accepted, timed-out, frame-rejected, TLS-rejected, and closed outcomes.",
  "narration": "Socket programs often handle concurrency through threads, processes, asynchronous runtimes, event loops, or a combination of patterns. Concurrency creates questions about shared state, per-connection isolation, cleanup, cancellation, and race conditions. A parser or connection handler should not let one peer corrupt state used by another. Authentication state, authorization decisions, buffers, counters, and temporary data should be scoped deliberately.\n\nGraceful error handling keeps the service understandable during failure. A connection may close unexpectedly, a peer may send partial data, a TLS handshake may fail, a parser may reject a message, or a timeout may fire. The application should close resources, preserve useful operational evidence, and avoid leaving shared state inconsistent. Error paths deserve the same review as successful message handling.\n\nStructured logging helps operations without leaking secrets. Useful fields can include connection IDs, correlation IDs, service name, safe peer category, protocol version, message type, outcome, timeout status, authentication result, and resource-limit events. Logs should avoid secrets, tokens, credentials, private keys, raw sensitive payloads, and excessive internal detail. The goal is observability, not permanent storage of protocol contents.\n\nTesting and monitoring complete the security story. Tests should cover framing, malformed messages, partial reads, timeouts, connection failures, authorization, resource limits, and TLS validation behavior. Monitoring should watch connection counts, error spikes, unusual peers, repeated failures, slow clients, resource exhaustion, and certificate issues. If socket services expose data or unsafe behavior, incident response should include containment, credential rotation when needed, log review, remediation, and regression tests.",
  "narrationPoints": [
    "Socket programs often handle concurrency through threads, processes, asynchronous runtimes, event loops, or a combination of patterns.",
    "The application should close resources, preserve useful operational evidence, and avoid leaving shared state inconsistent.",
    "Logs should avoid secrets, tokens, credentials, private keys, raw sensitive payloads, and excessive internal detail.",
    "Tests should cover framing, malformed messages, partial reads, timeouts, connection failures, authorization, resource limits, and TLS validation behavior.",
    "A parser or connection handler should not let one peer corrupt state used by another.",
    "Monitoring should watch connection counts, error spikes, unusual peers, repeated failures, slow clients, resource exhaustion, and certificate issues."
  ]
};
