window.COURSE_MODULE = {
  "title": "Secure Client Socket Behavior",
  "graphicAlt": "A socket client resolves one approved hostname into addresses inside a service network, then connects with a deadline and uses sendall for a complete length-prefixed command.",
  "narration": "Socket clients should control where they connect. A configured endpoint, approved service record, or allowlisted destination is usually safer than accepting arbitrary hosts and ports from untrusted input. User-supplied destinations can send a server-side client toward unintended public, private, internal, or sensitive locations. DNS and name resolution behavior may change over time, so destination policy should be expressed at the application level rather than left to convenience.\n\nConnection timeouts, read timeouts, and write timeouts are essential. A client that waits indefinitely can tie up workers, command-line jobs, automation tasks, or background processing capacity. A slow or unavailable peer should not be able to consume resources forever. Timeouts should match the business purpose of the operation and should produce safe, observable failure behavior rather than ambiguous partial success.\n\nRetries and backoff require care. Retrying a connection may help with transient network issues, but repeated attempts can amplify outages, overload a dependency, or duplicate state-changing work if the protocol is not designed for it. A client should know whether an operation is safe to retry, how many attempts are reasonable, and how failure should be reported. Retry behavior is part of the protocol contract, not just a reliability setting.\n\nClient logging should avoid exposing credentials, connection strings, tokens, private addresses where sensitive, raw payloads, and protocol secrets. Logs can still record safe destination identifiers, operation names, connection outcomes, timeouts, retry counts, and correlation IDs. A secure client validates where it connects, how long it waits, what it sends, what it accepts from the peer, and how failures become operational evidence.",
  "narrationPoints": [
    "User-supplied destinations can send a server-side client toward unintended public, private, internal, or sensitive locations.",
    "A client that waits indefinitely can tie up workers, command-line jobs, automation tasks, or background processing capacity.",
    "Retry behavior is part of the protocol contract, not just a reliability setting.",
    "Logs can still record safe destination identifiers, operation names, connection outcomes, timeouts, retry counts, and correlation IDs.",
    "Client logging should avoid exposing credentials, connection strings, tokens, private addresses where sensitive, raw payloads, and protocol secrets.",
    "A slow or unavailable peer should not be able to consume resources forever."
  ]
};
