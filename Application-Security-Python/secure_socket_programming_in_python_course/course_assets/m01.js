window.COURSE_MODULE = {
  "title": "What Secure Socket Programming Means",
  "graphicAlt": "A client and server exchange framed messages across a network boundary; endpoint control, peer identity, parsing, authorization, byte budgets, deadlines, TLS, and graceful close surround the socket channel.",
  "narration": "Secure socket programming means designing low-level network communication so endpoints, messages, credentials, resources, and trust decisions are controlled intentionally. A socket is a network communication endpoint. In Python, sockets may appear in client tools, internal services, custom protocols, IoT integrations, message systems, proxies, daemons, background workers, and legacy integrations. They are flexible, but that flexibility comes with responsibility.\n\nSockets sit below many higher-level patterns. HTTP clients, web frameworks, message queues, database drivers, and service SDKs often hide socket details behind structured APIs and established protocol behavior. Raw socket code exposes developers to lower-level decisions about message framing, parsing, timeouts, retries, connection lifecycle, peer identity, and resource limits. Those choices can become security decisions whether or not the code labels them that way.\n\nA socket connection that works is not automatically safe. A client may connect to the wrong destination, wait forever, trust malformed data, or leak credentials in logs. A server may bind more broadly than intended, accept too many connections, treat all peers as trusted, or parse bytes without clear message boundaries. The absence of immediate errors does not prove the protocol is secure or maintainable.\n\nSecure socket code should be deliberate, constrained, observable, and reviewed. The application should define who can connect, where clients may connect, how messages start and end, what inputs are valid, how peers authenticate, which commands are authorized, how resources are limited, how failures are handled, and what gets logged. Low-level networking is safest when it is boring, explicit, and testable.",
  "narrationPoints": [
    "Secure socket programming means designing low-level network communication so endpoints, messages, credentials, resources, and trust decisions are controlled intentionally.",
    "Raw socket code exposes developers to lower-level decisions about message framing, parsing, timeouts, retries, connection lifecycle, peer identity, and resource limits.",
    "A client may connect to the wrong destination, wait forever, trust malformed data, or leak credentials in logs.",
    "Low-level networking is safest when it is boring, explicit, and testable.",
    "The absence of immediate errors does not prove the protocol is secure or maintainable.",
    "HTTP clients, web frameworks, message queues, database drivers, and service SDKs often hide socket details behind structured APIs and established protocol behavior."
  ]
};
