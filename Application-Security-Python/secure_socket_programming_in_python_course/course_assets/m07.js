window.COURSE_MODULE = {
  "title": "TLS, Certificates, and Socket Security",
  "graphicAlt": "Raw sockets enter verified client and server TLS wrappers; callouts show expected hostname, certificate chain, minimum protocol, handshake deadline, and application authorization after transport identity.",
  "narration": "TLS can be layered over sockets to protect data in transit and validate endpoint identity when configured correctly. In Python, TLS contexts and related library features define how certificates are validated, which trust anchors are used, and whether the client or server presents certificates. The details may vary by library and platform, but the core idea is stable: encryption should be paired with identity validation.\n\nClient-side TLS validation should include server certificate validation and hostname validation for sensitive connections. A client should verify that the certificate chains to a trusted authority and matches the service name it intended to reach. Disabling verification or accepting any certificate may make a connection succeed, but it removes the identity assurance that TLS is supposed to provide. Local development exceptions should not become production defaults.\n\nServers need private key protection and certificate lifecycle management. Private keys should not appear in source code, logs, support bundles, or broadly shared files. Certificates expire and need rotation. A socket service should have an owner for certificate renewal, trust-store changes, and incident response if a key is exposed. TLS failure handling should be safe, observable, and free of sensitive leakage.\n\nTLS does not replace application authentication, authorization, or message validation. Mutual TLS can strengthen service identity by having both sides present certificates, but the application must still decide which actions a peer may perform. A valid certificate does not make every command safe, every resource accessible, or every message well formed. TLS protects the transport boundary; the protocol still needs application controls.",
  "narrationPoints": [
    "TLS can be layered over sockets to protect data in transit and validate endpoint identity when configured correctly.",
    "Client-side TLS validation should include server certificate validation and hostname validation for sensitive connections.",
    "Private keys should not appear in source code, logs, support bundles, or broadly shared files.",
    "A valid certificate does not make every command safe, every resource accessible, or every message well formed.",
    "A socket service should have an owner for certificate renewal, trust-store changes, and incident response if a key is exposed.",
    "TLS failure handling should be safe, observable, and free of sensitive leakage."
  ]
};
