window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply APIs, Background Jobs, and Third-Party Failures with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Map downstream timeouts without echoing details",
      "language": "python",
      "blurb": "Transport, HTTP, size, JSON, and schema failures become one bounded service error; telemetry failure cannot replace that result.",
      "code": "import json\nimport math\nimport re\n\nimport requests\n\nMAX_RESPONSE_BYTES = 16_384\nMAX_NUMBER_CHARACTERS = 32\nCUSTOMER_ID = re.compile(r\"cus_[A-Za-z0-9_-]{8,64}\\Z\")\n\nclass DependencyUnavailable(Exception):\n    pass\n\nclass InvalidDependencyResponse(Exception):\n    pass\n\ndef increment_safely(metrics, name: str) -> bool:\n    try:\n        metrics.increment(name)\n    except Exception:\n        return False\n    return True\n\ndef unique_json_object(pairs: list[tuple[str, object]]) -> dict[str, object]:\n    result: dict[str, object] = {}\n    for key, value in pairs:\n        if key in result:\n            raise ValueError(\"duplicate JSON member\")\n        result[key] = value\n    return result\n\ndef bounded_json_int(text: str) -> int:\n    if len(text) > MAX_NUMBER_CHARACTERS:\n        raise ValueError(\"JSON integer is too long\")\n    return int(text)\n\ndef bounded_json_float(text: str) -> float:\n    if len(text) > MAX_NUMBER_CHARACTERS:\n        raise ValueError(\"JSON number is too long\")\n    value = float(text)\n    if not math.isfinite(value):\n        raise ValueError(\"non-finite JSON number\")\n    return value\n\ndef read_bounded_json(response):\n    body = bytearray()\n    for chunk in response.iter_content(chunk_size=4096):\n        if not isinstance(chunk, bytes) or len(chunk) > MAX_RESPONSE_BYTES - len(body):\n            raise InvalidDependencyResponse\n        body.extend(chunk)\n    try:\n        return json.loads(\n            bytes(body),\n            object_pairs_hook=unique_json_object,\n            parse_int=bounded_json_int,\n            parse_float=bounded_json_float,\n            parse_constant=lambda _value: (_ for _ in ()).throw(\n                ValueError(\"non-standard JSON number\")\n            ),\n        )\n    except (UnicodeDecodeError, ValueError, RecursionError) as error:\n        raise InvalidDependencyResponse from error\n\ndef validated_score(payload) -> float:\n    if not isinstance(payload, dict) or set(payload) != {\"score\"}:\n        raise InvalidDependencyResponse\n    score = payload[\"score\"]\n    if isinstance(score, bool) or not isinstance(score, (int, float)):\n        raise InvalidDependencyResponse\n    if not math.isfinite(score) or not 0 <= score <= 100:\n        raise InvalidDependencyResponse\n    return float(score)\n\ndef fetch_risk_score(session: requests.Session, customer_id: str, metrics) -> float:\n    if not isinstance(customer_id, str) or CUSTOMER_ID.fullmatch(customer_id) is None:\n        raise ValueError(\"customer identifier rejected\")\n    try:\n        with session.get(\n            \"https://risk.example/v1/scores\",\n            params={\"customer\": customer_id},\n            timeout=(2, 4),\n            stream=True,\n        ) as response:\n            response.raise_for_status()\n            score = validated_score(read_bounded_json(response))\n    except requests.RequestException:\n        increment_safely(metrics, \"risk_dependency_unavailable\")\n        raise DependencyUnavailable(\"risk service unavailable\") from None\n    except InvalidDependencyResponse:\n        increment_safely(metrics, \"risk_dependency_invalid_response\")\n        raise DependencyUnavailable(\"risk service unavailable\") from None\n    return score\n"
    },
    {
      "title": "Record a failed background job safely",
      "language": "python",
      "blurb": "Only an explicitly transient failure from idempotent work is retried; durable state is saved before bounded best-effort telemetry records the exception type.",
      "code": "from datetime import datetime, timedelta, timezone\n\nMAX_ATTEMPTS = 3\n\nclass RetryableJobError(Exception):\n    pass\n\ndef record_job_failure_safely(logger, job, error: Exception) -> bool:\n    try:\n        logger.error(\n            \"job_failed\",\n            extra={\n                \"job_id\": str(job.id)[:64],\n                \"error_type\": type(error).__name__[:80],\n            },\n        )\n    except Exception:\n        return False\n    return True\n\ndef fail_job(\n    job,\n    error: Exception,\n    attempts: int,\n    logger,\n    *,\n    operation_is_idempotent: bool,\n) -> None:\n    if isinstance(attempts, bool) or not isinstance(attempts, int) or attempts < 1:\n        raise ValueError(\"attempts must be a positive integer\")\n    if not isinstance(operation_is_idempotent, bool):\n        raise TypeError(\"operation_is_idempotent must be a boolean\")\n\n    retry = (\n        operation_is_idempotent\n        and isinstance(error, RetryableJobError)\n        and attempts < MAX_ATTEMPTS\n    )\n    job.status = \"retry_scheduled\" if retry else \"failed\"\n    job.failure_code = \"transient_processing_error\" if retry else \"processing_error\"\n    job.retry_at = datetime.now(timezone.utc) + timedelta(minutes=5) if retry else None\n    job.save()\n    record_job_failure_safely(logger, job, error)\n"
    }
  ]
};
