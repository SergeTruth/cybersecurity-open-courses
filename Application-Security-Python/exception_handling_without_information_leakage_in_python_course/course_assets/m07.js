window.COURSE_MODULE = {
  "title": "APIs, Background Jobs, and Third-Party Failures",
  "graphicAlt": "Control-flow diagram for APIs, Background Jobs, and Third-Party Failures, showing request handler, exception mapper, safe response, protected diagnostic log, and correlation identifier; labeled arrows identify public and internal error paths and the point where unsafe input or behavior is rejected.",
  "narration": "API error contracts should be consistent and intentionally limited. Clients benefit from stable response shapes, safe error codes, appropriate statuses, and request identifiers. They do not need raw Python exception objects, database driver messages, framework tracebacks, internal hostnames, or third-party response bodies. A safe API boundary translates internal failure into application-level meaning while preserving detailed diagnostics in controlled systems.\n\nBackground jobs and queues need special attention because failure may not have an immediate user response. A worker might process a message, call a database, contact a third-party API, write a file, and retry on failure. Its exceptions can end up in queue metadata, retry dashboards, logs, dead-letter stores, alert payloads, and operational tickets. Those destinations should not receive secrets or unredacted downstream content simply because the failure happened outside a web request.\n\nThird-party and downstream failures should not be passed through blindly. A payment provider, identity service, database driver, object storage API, webhook endpoint, or internal service may return messages meant for developers or administrators. The Python application should translate those details into safe client responses and log only the context needed for investigation. This reduces leakage and keeps dependency-specific behavior from becoming part of the public API contract.\n\nRetryable and non-retryable errors should be separated at a high level. Timeouts, rate limits, temporary network failures, validation errors, authorization failures, and permanent business conflicts require different responses. If exception handling treats them all the same, it may retry unsafe work, hide abuse signals, leak downstream detail, or create confusing client behavior. Good failure design makes dependency problems understandable internally and safe externally.",
  "narrationPoints": [
    "API error contracts should be consistent and intentionally limited.",
    "A safe API boundary translates internal failure into application-level meaning while preserving detailed diagnostics in controlled systems.",
    "Its exceptions can end up in queue metadata, retry dashboards, logs, dead-letter stores, alert payloads, and operational tickets.",
    "A payment provider, identity service, database driver, object storage API, webhook endpoint, or internal service may return messages meant for developers or administrators.",
    "Timeouts, rate limits, temporary network failures, validation errors, authorization failures, and permanent business conflicts require different responses.",
    "Good failure design makes dependency problems understandable internally and safe externally."
  ]
};
