window.COURSE_MODULE = {
  "title": "Error Handling Architecture and Trust Boundaries",
  "graphicAlt": "Flowchart for Error Handling Architecture and Trust Boundaries, showing request handler, exception mapper, safe response, protected diagnostic log, and correlation identifier; labeled arrows identify public and internal error paths and the point where unsafe input or behavior is rejected.",
  "narration": "Error handling should be part of the application architecture, not a scattering of one-off try and except blocks. A Python service normally has several layers that can shape errors: request validation, domain logic, framework middleware, route handlers, database drivers, background workers, queues, logging libraries, tracing tools, alerting pipelines, and user interface code. Each layer can translate detail safely or accidentally pass sensitive information to the wrong place.\n\nTrust boundaries define who should see what. User input crosses into application code. Application code calls dependencies. Framework handlers prepare responses. Logs and traces flow to centralized systems. Developers, administrators, support staff, and external clients have different roles and different need-to-know. A safe architecture routes diagnostic detail toward controlled internal channels while sending users a stable, limited explanation and a reference identifier when useful.\n\nExpected and unexpected errors should be handled differently, but both need discipline. Validation errors can often tell a user what field needs correction without exposing internal schemas. Authentication and authorization failures should avoid revealing extra account or permission detail. Dependency, database, file, queue, and third-party failures should usually be translated into application-level messages. Background job failures need durable records because there may be no user waiting for a response.\n\nCentralizing error behavior makes secure defaults easier. Teams can define standard response shapes, exception classes, log fields, redaction rules, retry boundaries, and alerting signals in one pattern instead of relying on every developer to remember them. That does not eliminate local judgment, but it reduces drift. When failure behavior is designed intentionally, it is easier to review, test, monitor, and improve.",
  "narrationPoints": [
    "Error handling should be part of the application architecture, not a scattering of one-off try and except blocks.",
    "A safe architecture routes diagnostic detail toward controlled internal channels while sending users a stable, limited explanation and a reference identifier when useful.",
    "Authentication and authorization failures should avoid revealing extra account or permission detail.",
    "When failure behavior is designed intentionally, it is easier to review, test, monitor, and improve.",
    "Application code calls dependencies.",
    "Centralizing error behavior makes secure defaults easier."
  ]
};
