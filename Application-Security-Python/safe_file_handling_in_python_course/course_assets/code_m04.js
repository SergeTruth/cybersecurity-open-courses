window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply File Upload Safety with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Identify upload content from bounded bytes",
      "language": "python",
      "blurb": "The validator checks a small file signature and actual stream length instead of accepting the browser's filename or content type.",
      "code": "from io import BufferedReader\n\nSIGNATURES = {\n    b\"%PDF-\": \"application/pdf\",\n    b\"\\x89PNG\\r\\n\\x1a\\n\": \"image/png\",\n}\nMAX_UPLOAD_BYTES = 16_000_000\nREAD_CHUNK_BYTES = 64 * 1024\n\ndef identify_upload(\n    stream: BufferedReader,\n    maximum: int = 8_000_000,\n) -> tuple[str, bytes]:\n    if type(maximum) is not int or not 1 <= maximum <= MAX_UPLOAD_BYTES:\n        raise ValueError(\"upload byte limit rejected\")\n    read = getattr(stream, \"read\", None)\n    if not callable(read):\n        raise TypeError(\"upload stream contract rejected\")\n    body = bytearray()\n    while True:\n        chunk = read(min(READ_CHUNK_BYTES, maximum + 1 - len(body)))\n        if type(chunk) is not bytes:\n            raise ValueError(\"upload stream returned invalid data\")\n        if not chunk:\n            break\n        body.extend(chunk)\n        if len(body) > maximum:\n            raise ValueError(\"upload is too large\")\n    data = bytes(body)\n    if not data:\n        raise ValueError(\"upload is empty\")\n    media_type = next(\n        (\n            kind\n            for magic, kind in SIGNATURES.items()\n            if data.startswith(magic)\n        ),\n        None,\n    )\n    if media_type is None:\n        raise ValueError(\"unsupported file content\")\n    return media_type, data\n"
    },
    {
      "title": "Retain quarantined content until publication commits",
      "language": "python",
      "blurb": "A clean upload is removed from quarantine only after public storage succeeds; failures retain a durable recovery state.",
      "code": "import re\n\nUPLOAD_ID = re.compile(r\"upl_[A-Za-z0-9_-]{8,64}\\Z\")\n\nclass PublicationCommittedError(Exception):\n    pass\n\ndef scan_and_publish(quarantine, public_store, scanner, upload_id: str) -> None:\n    if not isinstance(upload_id, str) or UPLOAD_ID.fullmatch(upload_id) is None:\n        raise ValueError(\"upload identifier rejected\")\n    open_upload = quarantine.open\n    mark_failed = quarantine.mark_failed\n    delete_quarantine = quarantine.delete\n    inspect = scanner.inspect\n    put_public = public_store.put\n    if not all(\n        callable(operation)\n        for operation in (\n            open_upload,\n            mark_failed,\n            delete_quarantine,\n            inspect,\n            put_public,\n        )\n    ):\n        raise TypeError(\"upload publication adapter contract rejected\")\n    handle = open_upload(upload_id)\n    seek = handle.seek\n    close = handle.close\n    if not callable(seek) or not callable(close):\n        raise TypeError(\"quarantine handle contract rejected\")\n    primary_error: BaseException | None = None\n    cleanup_errors: list[BaseException] = []\n    published = False\n    failure_reason = \"scan_rejected\"\n    try:\n        verdict = inspect(handle)\n        if verdict != \"clean\":\n            raise ValueError(\"upload was not accepted\")\n        seek(0)\n        failure_reason = \"publication_failed\"\n        put_public(upload_id, handle)\n        published = True\n        delete_quarantine(upload_id)\n    except BaseException as error:\n        primary_error = error\n        if not published:\n            try:\n                mark_failed(upload_id, reason=failure_reason)\n            except BaseException as cleanup_error:\n                cleanup_errors.append(cleanup_error)\n    finally:\n        try:\n            close()\n        except BaseException as cleanup_error:\n            cleanup_errors.append(cleanup_error)\n    if published and (primary_error is not None or cleanup_errors):\n        causes = [\n            error\n            for error in (primary_error, *cleanup_errors)\n            if error is not None\n        ]\n        raise PublicationCommittedError(\n            \"upload was published, but quarantine cleanup was incomplete\"\n        ) from BaseExceptionGroup(\"post-publication cleanup failed\", causes)\n    if primary_error is not None:\n        if cleanup_errors:\n            raise BaseExceptionGroup(\n                \"upload publication and cleanup failed\",\n                [primary_error, *cleanup_errors],\n            )\n        raise primary_error\n    if cleanup_errors:\n        raise BaseExceptionGroup(\"upload handle cleanup failed\", cleanup_errors)\n"
    }
  ]
};
