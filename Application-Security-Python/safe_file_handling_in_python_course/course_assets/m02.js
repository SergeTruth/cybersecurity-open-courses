window.COURSE_MODULE = {
  "title": "Trust Boundaries, Paths, and File Names",
  "graphicAlt": "Flowchart for Trust Boundaries, Paths, and File Names, showing upload stream, quarantine directory, content validator, private storage, authorization gate, and download response; labeled arrows identify file lifecycle and isolation controls and the point where unsafe input or behavior is rejected.",
  "narration": "Python applications may receive file-related values from many places: user-supplied filenames, uploaded files, command-line arguments, configuration, API requests, archive member names, generated filenames, and database records. Each source has a different trust level. A value that looks like a normal filename may still contain unexpected separators, encodings, reserved names, confusing characters, or context-dependent behavior.\n\nAbsolute and relative paths should be handled deliberately. A safe design usually defines a base directory or allowlisted storage location, then constrains operations to that area. Canonicalization concepts help the application reason about the final resolved location before opening, writing, serving, or deleting a file. The stable principle is not to trust a path string until the application has confirmed where it leads and whether that location is allowed.\n\nGenerated server-side filenames are often safer than storing user-provided names directly as filesystem names. The original filename can be preserved as metadata for display when needed, but storage names should avoid relying on user control. Extensions may be useful for user experience or routing, but they should not be the only control for content trust, parser choice, or authorization decisions.\n\nSymbolic links and mounted directories deserve attention at a high level because they can make a path behave differently than it appears. The application should constrain file access to intended directories and intended operations, and it should avoid letting callers choose arbitrary paths. Safe path design is about reducing ambiguity before a file operation reaches the operating system.",
  "narrationPoints": [
    "A value that looks like a normal filename may still contain unexpected separators, encodings, reserved names, confusing characters, or context-dependent behavior.",
    "A safe design usually defines a base directory or allowlisted storage location, then constrains operations to that area.",
    "The stable principle is not to trust a path string until the application has confirmed where it leads and whether that location is allowed.",
    "The original filename can be preserved as metadata for display when needed, but storage names should avoid relying on user control.",
    "The application should constrain file access to intended directories and intended operations, and it should avoid letting callers choose arbitrary paths.",
    "Each source has a different trust level."
  ]
};
