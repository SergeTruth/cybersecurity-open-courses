window.COURSE_MODULE = {
  "title": "Reading and Writing Files Safely",
  "graphicAlt": "Layered architecture diagram for Reading and Writing Files Safely, showing upload stream, quarantine directory, content validator, private storage, authorization gate, and download response; labeled arrows identify file lifecycle and isolation controls and the point where unsafe input or behavior is rejected.",
  "narration": "Reading and writing files safely starts with intentional operations. Python makes file access easy, so reviewers should look for explicit modes, clear encoding choices, and predictable behavior. Text files and binary files should be handled differently. A script that reads structured text should define encoding expectations. A service that handles images, documents, or archives should treat them as binary until a specific parser is selected.\n\nContext managers help ensure files are closed even when errors occur. They do not make the operation secure by themselves, but they reduce resource leaks and make lifecycle easier to reason about. Error handling should distinguish expected conditions, such as missing files or permission failures, from unexpected faults that require logging and investigation. User-facing errors should not expose sensitive paths or internal details.\n\nWrite behavior should avoid accidental overwrite or append. When a file should be newly created, the code should express that expectation. When updating a file, atomic write concepts can reduce the chance of partial or corrupted output if a process fails halfway through. Large files should be streamed or bounded rather than loaded entirely into memory without limits. File size limits protect both performance and availability.\n\nRace condition concepts matter at a high level when the code checks a file and later acts on it. The state may change between the check and the operation, especially in shared directories, concurrent workers, or multi-user systems. The stable practice is to minimize assumptions between decision and action, use safe APIs where possible, and design file operations so they are predictable, bounded, and recoverable.",
  "narrationPoints": [
    "Reading and writing files safely starts with intentional operations.",
    "A script that reads structured text should define encoding expectations.",
    "Error handling should distinguish expected conditions, such as missing files or permission failures, from unexpected faults that require logging and investigation.",
    "Write behavior should avoid accidental overwrite or append.",
    "Large files should be streamed or bounded rather than loaded entirely into memory without limits.",
    "The state may change between the check and the operation, especially in shared directories, concurrent workers, or multi-user systems."
  ]
};
