window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Reading and Writing Files Safely with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Read a bounded UTF-8 file through a directory descriptor",
      "language": "python",
      "blurb": "The POSIX reader refuses links and checks type and size on the opened descriptor, eliminating the earlier stat-then-open pathname race.",
      "code": "import os\nimport stat\n\nMAX_TEXT_FILE_BYTES = 1_048_576\n\ndef read_text_file(\n    root_fd: int,\n    stored_name: str,\n    maximum_bytes: int = 65_536,\n) -> str:\n    if not hasattr(os, \"O_NOFOLLOW\"):\n        raise NotImplementedError(\"secure file reading requires O_NOFOLLOW\")\n    if (\n        type(root_fd) is not int\n        or root_fd < 0\n        or type(stored_name) is not str\n        or not stored_name\n        or os.path.basename(stored_name) != stored_name\n        or \"\\x00\" in stored_name\n    ):\n        raise ValueError(\"stored name must be one path component\")\n    if (\n        type(maximum_bytes) is not int\n        or not 1 <= maximum_bytes <= MAX_TEXT_FILE_BYTES\n    ):\n        raise ValueError(\"text file byte limit rejected\")\n    descriptor = os.open(\n        stored_name,\n        os.O_RDONLY\n        | os.O_NOFOLLOW\n        | getattr(os, \"O_NONBLOCK\", 0)\n        | getattr(os, \"O_CLOEXEC\", 0),\n        dir_fd=root_fd,\n    )\n    with os.fdopen(descriptor, \"rb\") as stream:\n        info = os.fstat(stream.fileno())\n        if (\n            not stat.S_ISREG(info.st_mode)\n            or not 0 <= info.st_size <= maximum_bytes\n        ):\n            raise ValueError(\"configuration file rejected\")\n        raw = stream.read(maximum_bytes + 1)\n    if len(raw) > maximum_bytes:\n        raise ValueError(\"configuration grew beyond its limit\")\n    return raw.decode(\"utf-8\", errors=\"strict\")\n"
    },
    {
      "title": "Commit a file and its directory entry durably",
      "language": "python",
      "blurb": "The workflow distinguishes a failed write from a replacement whose directory synchronization could not be confirmed.",
      "code": "from enum import Enum\nfrom pathlib import Path\nimport os\nimport tempfile\n\nMAX_REPLACEMENT_BYTES = 10_000_000\n\nclass CommitResult(Enum):\n    NOT_REPLACED = \"not_replaced\"\n    REPLACED_UNCONFIRMED = \"replaced_unconfirmed\"\n    REPLACED_DURABLE = \"replaced_durable\"\n\ndef durable_replace(target: Path, payload: bytes) -> CommitResult:\n    if not isinstance(target, Path) or target.name in {\"\", \".\", \"..\"}:\n        raise ValueError(\"replacement target rejected\")\n    if not isinstance(payload, bytes) or len(payload) > MAX_REPLACEMENT_BYTES:\n        raise ValueError(\"replacement payload rejected\")\n    parent = target.parent.resolve(strict=True)\n    if not parent.is_dir():\n        raise ValueError(\"replacement parent rejected\")\n    destination = parent / target.name\n    descriptor, temporary = tempfile.mkstemp(dir=parent)\n    committed = False\n    try:\n        with os.fdopen(descriptor, \"wb\") as output:\n            output.write(payload)\n            output.flush()\n            os.fsync(output.fileno())\n        os.replace(temporary, destination)\n        committed = True\n        flags = os.O_RDONLY | getattr(os, \"O_DIRECTORY\", 0) | getattr(os, \"O_CLOEXEC\", 0)\n        directory = os.open(parent, flags)\n        try:\n            os.fsync(directory)\n        finally:\n            os.close(directory)\n        return CommitResult.REPLACED_DURABLE\n    except OSError as error:\n        if not committed:\n            try:\n                os.unlink(temporary)\n            except FileNotFoundError:\n                pass\n            except OSError as cleanup_error:\n                raise ExceptionGroup(\n                    \"replacement and cleanup failed\",\n                    [error, cleanup_error],\n                )\n        return (\n            CommitResult.REPLACED_UNCONFIRMED\n            if committed\n            else CommitResult.NOT_REPLACED\n        )\n"
    }
  ]
};
