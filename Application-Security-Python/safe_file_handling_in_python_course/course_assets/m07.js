window.COURSE_MODULE = {
  "title": "Parsing, Deserialization, Archives, and Metadata",
  "graphicAlt": "Control-flow diagram for Parsing, Deserialization, Archives, and Metadata, showing upload stream, quarantine directory, content validator, private storage, authorization gate, and download response; labeled arrows identify file lifecycle and isolation controls and the point where unsafe input or behavior is rejected.",
  "narration": "File contents are untrusted input. JSON, CSV, XML, YAML, images, PDFs, office documents, and custom formats each require parsers with different assumptions and failure modes. A file may be malformed, oversized, misleading, or crafted in ways that stress a parser or workflow. Safe design chooses parsers deliberately, limits resources, validates expected structure, and handles parse failures without exposing sensitive details.\n\nDeserialization deserves special care. Python's pickle format is powerful but should not be used with untrusted data because loading data can influence object creation and dangerous behavior depending on context. YAML and XML parser safety concepts also matter because parser defaults and features can create risk if they process untrusted content without constraints. The safest parser is the one appropriate for the data, configured for the trust boundary, and reviewed for expected behavior.\n\nArchive extraction should be reviewed carefully because archives can contain many files, nested paths, unexpected names, large content, duplicate entries, metadata, and compression behavior that affects extraction. The application should decide which members are allowed, where extraction may occur, how much data may be expanded, and what happens to rejected files. Extraction is not just reading one file; it is creating many filesystem effects.\n\nMetadata can carry security and privacy impact. Image metadata and EXIF concepts may reveal device, time, or location details. Document metadata may expose authors, comments, paths, or revision history. Decompression and resource exhaustion concepts matter when small inputs expand into large workloads. Parsing should be isolated, limited, validated, logged appropriately, and tied to the specific business purpose of the file.",
  "narrationPoints": [
    "JSON, CSV, XML, YAML, images, PDFs, office documents, and custom formats each require parsers with different assumptions and failure modes.",
    "YAML and XML parser safety concepts also matter because parser defaults and features can create risk if they process untrusted content without constraints.",
    "The application should decide which members are allowed, where extraction may occur, how much data may be expanded, and what happens to rejected files.",
    "Document metadata may expose authors, comments, paths, or revision history.",
    "Parsing should be isolated, limited, validated, logged appropriately, and tied to the specific business purpose of the file.",
    "Metadata can carry security and privacy impact."
  ]
};
