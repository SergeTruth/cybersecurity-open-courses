window.COURSE_MODULE = {
  "title": "What SQL Injection Prevention Means",
  "graphicAlt": "Untrusted values stop at a parameter boundary while fixed SQL structure reaches the driver; separate gates control identifiers, authorization predicates, database privileges, errors, logs, and tests.",
  "narration": "SQL injection is a risk created when untrusted data is treated as part of a database command. The defensive concern is not limited to one framework, one route, or one login form. Python applications use databases from Flask views, Django models, FastAPI services, scripts, internal tools, data pipelines, scheduled jobs, APIs, and background workers. Any place that builds a query from data can become a security boundary.\n\nIt helps to separate related ideas. Input validation checks whether data has the expected type, size, format, and business meaning. Query construction decides how the application turns a request into a database command. Authorization decides whether the caller may perform the action on the resource. Database permissions limit what the application account can do. SQL injection prevention focuses first on query construction: keeping data values separate from SQL structure.\n\nThe impact can touch confidentiality, integrity, availability, and business logic. Unsafe queries may expose data the application did not intend to return, alter records incorrectly, break workflow assumptions, create operational instability, or bypass business rules implemented in application code. Even an internal reporting tool can become high impact if it has broad database access or handles sensitive information. Risk follows capability, not just public exposure.\n\nPrevention begins with how queries are built, not only with how inputs look. The safest default is to use parameterized queries, safe ORM patterns, allowlisted dynamic structure, and reviewed data-access helpers consistently across every database path. A team should not have one safe pattern for normal routes and a separate unsafe shortcut for admin screens, reports, migrations, or background jobs. Consistency is what turns a secure coding rule into an engineering habit.",
  "narrationPoints": [
    "SQL injection is a risk created when untrusted data is treated as part of a database command.",
    "SQL injection prevention focuses first on query construction: keeping data values separate from SQL structure.",
    "Even an internal reporting tool can become high impact if it has broad database access or handles sensitive information.",
    "Prevention begins with how queries are built, not only with how inputs look.",
    "The defensive concern is not limited to one framework, one route, or one login form.",
    "Input validation checks whether data has the expected type, size, format, and business meaning."
  ]
};
