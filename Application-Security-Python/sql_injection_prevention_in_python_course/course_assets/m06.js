window.COURSE_MODULE = {
  "title": "Input Validation, Authorization, and Least Privilege",
  "graphicAlt": "An exact account-update schema validates names and timezone before a tenant-and-owner update; the runtime database role holds only the narrow grants needed for that operation.",
  "narration": "Input validation is an important supporting control. It checks type, range, length, format, allowed values, and business rules before data reaches deeper application logic. Validation can reject impossible dates, oversized strings, unsupported choices, invalid identifiers, and inconsistent workflow states. It improves clarity and reduces ambiguity. But validation should support safe query construction, not replace parameterized queries or safe ORM use.\n\nAuthorization must happen before database operations expose or change data. A query can be perfectly parameterized and still return records the caller should not see if the application forgets tenant boundaries or object-level checks. Secure database access combines safe query construction with resource-level permission decisions. The application should know who the caller is, what action is requested, which resource is affected, and whether the context allows it.\n\nLeast privilege reduces damage when something fails. The database account used by an application should have only the permissions needed for its role. A reporting service may need read access but not write access. A background job may need access to a specific schema but not administrative database privileges. Separating accounts by function can limit impact and make unusual behavior easier to notice.\n\nValidation, authorization, and least privilege are not substitutes for safe queries. They are layers that reduce risk around safe query construction. If a developer mixes untrusted data into SQL structure, validation may miss a future context, authorization may occur too late, and a privileged account may amplify the problem. A strong design uses all of these controls together: validate inputs, enforce access, use least privilege, and build queries safely by default.",
  "narrationPoints": [
    "Validation can reject impossible dates, oversized strings, unsupported choices, invalid identifiers, and inconsistent workflow states.",
    "Secure database access combines safe query construction with resource-level permission decisions.",
    "The database account used by an application should have only the permissions needed for its role.",
    "Validation, authorization, and least privilege are not substitutes for safe queries.",
    "A background job may need access to a specific schema but not administrative database privileges.",
    "Separating accounts by function can limit impact and make unusual behavior easier to notice."
  ]
};
