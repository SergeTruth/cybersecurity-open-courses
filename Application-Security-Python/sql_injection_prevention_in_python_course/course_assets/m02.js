window.COURSE_MODULE = {
  "title": "Trust Boundaries and Data Flow",
  "graphicAlt": "Browser input, API validation, authorization, query construction, driver binding, database role, returned columns, and response projection form a data-flow chain with trust decisions at each transition.",
  "narration": "SQL injection prevention starts by identifying where data comes from. Untrusted data can include form fields, path parameters, query parameters, JSON bodies, headers, cookies, uploaded files, imported CSV or spreadsheet data, third-party API responses, administrative fields, and values copied from logs or support tickets. Some sources look internal, but that does not automatically make them safe for use in database operations.\n\nData often flows from user input to application logic to database queries. A route receives a search term, a report tool receives a sort option, an import job reads a spreadsheet value, or an API receives a filter request. Each value should be understood before it influences a query. Is it a data value that belongs in a parameter? Is it a constrained choice that maps to a known column? Is it business context derived from the authenticated subject? Those distinctions matter.\n\nStored data can become risky later when reused in a query. A value saved as a display label today might become a filter, report name, export parameter, or administrative search field months later. Treating stored data as automatically trusted can recreate the same problem in a different part of the system. Teams should track whether data is user-controlled, partner-controlled, operator-provided, or system-derived before letting it influence database behavior.\n\nInternal tools and background jobs need query safety too. They may run with broader database permissions, process large datasets, or bypass normal request-path controls. A data pipeline, maintenance script, customer support console, or analytics job can still mix untrusted values into database commands if developers take shortcuts. Safe query construction should be a property of the data-access layer, not only the public web interface.",
  "narrationPoints": [
    "Some sources look internal, but that does not automatically make them safe for use in database operations.",
    "Data often flows from user input to application logic to database queries.",
    "Teams should track whether data is user-controlled, partner-controlled, operator-provided, or system-derived before letting it influence database behavior.",
    "Safe query construction should be a property of the data-access layer, not only the public web interface.",
    "Treating stored data as automatically trusted can recreate the same problem in a different part of the system.",
    "Stored data can become risky later when reused in a query."
  ]
};
