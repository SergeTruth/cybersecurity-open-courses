window.COURSE_MODULE = {
  "title": "Testing, Review, and Remediation",
  "graphicAlt": "An in-memory regression sends quote, comment, stacked-statement, identifier, and cross-tenant payloads through real driver calls, checking table survival, bounded rows, and unchanged authorization.",
  "narration": "Testing should cover the data-access paths that matter most. Unit tests can exercise repository functions, query builders, validation helpers, and mapping logic for dynamic fields. Integration tests can confirm that ORM paths, raw SQL paths, and transaction behavior work as intended with a real or representative database. Negative tests can confirm that unsupported filters, invalid identifiers, and unauthorized resource access are rejected safely at a high level.\n\nCode review should look for unsafe string-built SQL, raw query escape hatches, dynamic identifiers, custom expressions, stored procedures that build dynamic SQL, and places where authorization filters are missing. Reviewers do not need attack payloads to find risk. They need to understand whether untrusted data can influence SQL structure, whether values are parameterized, whether dynamic choices are allowlisted, and whether the query respects the current caller's permissions.\n\nStatic analysis, dependency review, scanner findings, and driver updates can support the program, but they do not replace engineering judgment. A scanner may flag a string-building pattern that is harmless, or miss a dynamic query hidden behind a helper. Dependency changes can alter APIs or safe defaults. Teams should triage findings based on data flow, database capability, exposure, and business impact, then add regression tests for confirmed fixes.\n\nRemediation should replace unsafe dynamic SQL with parameterized values, allowlisted identifiers, safer ORM patterns, narrower helper functions, and clearer authorization checks. It should also consider database account permissions, error behavior, logging, and tests. The best fix is not only removing one risky line. It is changing the local pattern so future features, reports, filters, and APIs naturally use safe query construction.",
  "narrationPoints": [
    "Unit tests can exercise repository functions, query builders, validation helpers, and mapping logic for dynamic fields.",
    "Reviewers do not need attack payloads to find risk.",
    "A scanner may flag a string-building pattern that is harmless, or miss a dynamic query hidden behind a helper.",
    "Remediation should replace unsafe dynamic SQL with parameterized values, allowlisted identifiers, safer ORM patterns, narrower helper functions, and clearer authorization checks.",
    "Negative tests can confirm that unsupported filters, invalid identifiers, and unauthorized resource access are rejected safely at a high level.",
    "Static analysis, dependency review, scanner findings, and driver updates can support the program, but they do not replace engineering judgment."
  ]
};
