window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Error Handling, Logging, and Data Exposure through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Translate explicit driver errors into opaque failures",
      "language": "python",
      "blurb": "The boundary catches psycopg's imported database exception, rolls back, logs only a stable query identifier and exception class, and returns no SQL text or values to users.",
      "code": "import re\nfrom psycopg import DatabaseError\n\nORDER_ID = re.compile(r\"ord_[A-Za-z0-9_-]{1,60}\")\n\nclass OrderLookupFailed(RuntimeError):\n    pass\n\ndef load_order_safely(connection, logger, order_id: str):\n    if type(order_id) is not str or ORDER_ID.fullmatch(order_id) is None:\n        raise ValueError(\"order identifier rejected\")\n    try:\n        return connection.execute(\n            \"select id, status from orders where id = %s\",\n            (order_id,),\n        ).fetchone()\n    except DatabaseError as error:\n        rollback_failed = False\n        try:\n            connection.rollback()\n        except Exception:\n            rollback_failed = True\n        try:\n            logger.error(\n                \"database_operation_failed\",\n                extra={\n                    \"query_id\": \"load_order_v2\",\n                    \"driver_error\": type(error).__name__,\n                    \"rollback_failed\": rollback_failed,\n                },\n            )\n        except Exception:\n            pass\n        raise OrderLookupFailed(\"order lookup unavailable\") from None\n"
    },
    {
      "title": "Project only fields the caller may receive",
      "language": "python",
      "blurb": "The query names permitted columns instead of selecting an entire row that might later expose credentials, notes, or authorization metadata.",
      "code": "from datetime import datetime\nimport re\nimport unicodedata\n\nIDENTIFIER = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,63}\")\n\ndef valid_display_name(value: object) -> str:\n    if type(value) is not str or not 1 <= len(value) <= 80:\n        raise RuntimeError(\"stored account result rejected\")\n    if any(\n        unicodedata.category(character) in {\"Cc\", \"Cf\", \"Zl\", \"Zp\"}\n        for character in value\n    ):\n        raise RuntimeError(\"stored account result rejected\")\n    return value\n\ndef public_account(\n    connection,\n    tenant_id: str,\n    account_id: str,\n) -> dict[str, object] | None:\n    if type(tenant_id) is not str or IDENTIFIER.fullmatch(tenant_id) is None:\n        raise ValueError(\"tenant identifier rejected\")\n    if type(account_id) is not str or IDENTIFIER.fullmatch(account_id) is None:\n        raise ValueError(\"account identifier rejected\")\n    cursor = connection.execute(\n        \"select public_id, display_name, created_at from accounts \"\n        \"where tenant_id = %s and public_id = %s\",\n        (tenant_id, account_id),\n    )\n    row = cursor.fetchone()\n    if row is None:\n        return None\n    if type(row) not in {tuple, list} or len(row) != 3:\n        raise RuntimeError(\"stored account result rejected\")\n    public_id, display_name, created_at = row\n    if (\n        type(public_id) is not str\n        or public_id != account_id\n        or type(created_at) is not datetime\n        or created_at.tzinfo is None\n        or created_at.utcoffset() is None\n    ):\n        raise RuntimeError(\"stored account result rejected\")\n    return {\n        \"id\": public_id,\n        \"display_name\": valid_display_name(display_name),\n        \"created_at\": created_at.isoformat(),\n    }\n"
    }
  ]
};
