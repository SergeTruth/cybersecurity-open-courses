window.COURSE_MODULE = {
  "title": "Parameterized Queries",
  "graphicAlt": "A parameterized SELECT keeps tenant and numeric values in placeholders, and tuple rows map through cursor column metadata; string-built SQL branches toward an injection warning.",
  "narration": "Parameterized queries are the primary prevention pattern for raw SQL. The core idea is simple: SQL structure is defined by the developer, and data values are passed separately to the database driver. The database driver or prepared statement mechanism then treats those values as data, not as syntax that changes the command. This separation is more reliable than trying to clean or manually rewrite values into a safe string.\n\nPlaceholders and parameter binding vary across database drivers and libraries, but the principle is stable. The application supplies a query shape and a separate collection of values. The driver binds those values through the database interface. Developers should follow the parameter style required by the chosen library rather than inventing string formatting conventions. If a value came from a request, import, partner response, or stored field, it should normally flow through binding rather than concatenation.\n\nParameterized queries apply to SELECT, INSERT, UPDATE, and DELETE operations. They are not only for login checks or search boxes. Any operation that includes external or variable values should use the safe parameter mechanism for those values. This includes identifiers used as lookup values, timestamps, status values, tenant IDs, account IDs, text fields, and numeric filters. The safest standard is that raw SQL without parameters should be unusual and easy to review.\n\nEscaping is not the preferred primary defense because it is context-dependent and easy to get wrong. Different databases, encodings, settings, and query contexts can behave differently. Parameterization delegates value handling to the database driver in the intended way. Developers still need to review SQL structure, dynamic identifiers, and authorization, but parameter binding removes a large class of mistakes where data values are accidentally interpreted as part of a command.",
  "narrationPoints": [
    "The database driver or prepared statement mechanism then treats those values as data, not as syntax that changes the command.",
    "Placeholders and parameter binding vary across database drivers and libraries, but the principle is stable.",
    "The safest standard is that raw SQL without parameters should be unusual and easy to review.",
    "Escaping is not the preferred primary defense because it is context-dependent and easy to get wrong.",
    "Developers should follow the parameter style required by the chosen library rather than inventing string formatting conventions.",
    "Any operation that includes external or variable values should use the safe parameter mechanism for those values."
  ]
};
