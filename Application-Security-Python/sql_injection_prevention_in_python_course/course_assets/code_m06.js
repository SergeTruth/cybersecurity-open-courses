window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Input Validation, Authorization, and Least Privilege through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Combine input validation with tenant authorization",
      "language": "python",
      "blurb": "Validation constrains the identifier grammar, while the SQL predicate independently enforces tenant and subject ownership before an update.",
      "code": "import re\nimport unicodedata\n\nDOCUMENT_ID = re.compile(r\"doc-[0-9a-f]{16}\")\nIDENTIFIER = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,63}\")\n\ndef valid_title(value: object) -> str:\n    if type(value) is not str:\n        raise ValueError(\"document title rejected\")\n    title = value.strip()\n    if (\n        not 1 <= len(title) <= 120\n        or any(\n            unicodedata.category(character) in {\"Cc\", \"Cf\", \"Zl\", \"Zp\"}\n            for character in title\n        )\n    ):\n        raise ValueError(\"document title rejected\")\n    return title\n\ndef rename_document(\n    connection,\n    *,\n    tenant_id: str,\n    user_id: str,\n    document_id: str,\n    title: str,\n) -> bool:\n    if type(tenant_id) is not str or IDENTIFIER.fullmatch(tenant_id) is None:\n        raise ValueError(\"tenant identifier rejected\")\n    if type(user_id) is not str or IDENTIFIER.fullmatch(user_id) is None:\n        raise ValueError(\"user identifier rejected\")\n    if (\n        type(document_id) is not str\n        or DOCUMENT_ID.fullmatch(document_id) is None\n    ):\n        raise ValueError(\"document update rejected\")\n    normalized_title = valid_title(title)\n    cursor = connection.execute(\n        \"update documents set title = %s \"\n        \"where id = %s and tenant_id = %s and owner_id = %s\",\n        (normalized_title, document_id, tenant_id, user_id),\n    )\n    if type(cursor.rowcount) is not int or cursor.rowcount not in {0, 1}:\n        raise RuntimeError(\"document update result rejected\")\n    return cursor.rowcount == 1\n"
    },
    {
      "title": "Reject unknown update fields and validate every value",
      "language": "python",
      "blurb": "An exact operation schema fails on unexpected fields and applies type, range, and format rules before any values reach the parameterized update.",
      "code": "import re\nimport unicodedata\n\nACCOUNT_ID = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,63}\")\nTIMEZONES = {\n    \"UTC\",\n    \"America/New_York\",\n    \"America/Chicago\",\n    \"America/Los_Angeles\",\n}\n\ndef validated_account_update(document: object) -> tuple[str, str]:\n    if not isinstance(document, dict) or set(document) != {\"display_name\", \"timezone\"}:\n        raise ValueError(\"account update shape rejected\")\n    name = document[\"display_name\"]\n    timezone = document[\"timezone\"]\n    if type(name) is not str:\n        raise ValueError(\"display name rejected\")\n    normalized_name = name.strip()\n    if (\n        not 1 <= len(normalized_name) <= 80\n        or any(\n            unicodedata.category(character) in {\"Cc\", \"Cf\", \"Zl\", \"Zp\"}\n            for character in normalized_name\n        )\n    ):\n        raise ValueError(\"display name rejected\")\n    if type(timezone) is not str or timezone not in TIMEZONES:\n        raise ValueError(\"timezone rejected\")\n    return normalized_name, timezone\n\ndef update_account(connection, account_id: str, document: object) -> None:\n    if type(account_id) is not str or ACCOUNT_ID.fullmatch(account_id) is None:\n        raise ValueError(\"account identifier rejected\")\n    name, timezone = validated_account_update(document)\n    cursor = connection.execute(\n        \"update accounts set display_name = %s, timezone = %s where id = %s\",\n        (name, timezone, account_id),\n    )\n    if type(cursor.rowcount) is not int or cursor.rowcount not in {0, 1}:\n        raise RuntimeError(\"account update result rejected\")\n"
    }
  ]
};
