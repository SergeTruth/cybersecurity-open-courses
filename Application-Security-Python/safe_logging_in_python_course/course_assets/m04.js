window.COURSE_MODULE = {
  "title": "Structured Logging and Event Design",
  "graphicAlt": "Sequence diagram for Structured Logging and Event Design, showing application event, field allowlist, recursive redactor, structured log sink, restricted viewer, and retention boundary; labeled arrows identify safe telemetry flow and blocked sensitive fields and the point where unsafe input or behavior is rejected.",
  "narration": "Structured logging records events as consistent fields rather than only free-text sentences. A useful event may include an event name, timestamp, severity, component, environment, request ID, correlation ID, outcome, reason code, and safe contextual metadata. In Python, the exact logging library or observability platform may change, but the design principle remains stable: predictable fields make logs easier to search, alert on, review, protect, and automate.\n\nEvent names should describe what happened at a level that supports investigation. For example, an authentication failure, permission denial, export started, export completed, admin setting changed, background job failed, dependency timeout, or rate limit triggered event each tells a different story. A generic message such as something went wrong provides little value. A structured event can preserve meaning without copying sensitive payloads into the record.\n\nIdentifiers should be chosen carefully. Request IDs and correlation IDs help connect logs across services and user-facing errors without exposing internal stack traces or sensitive details. User, account, or tenant identifiers may be appropriate in some logs, but teams should choose forms that respect privacy and access needs. Sometimes an internal stable identifier is better than an email address. Sometimes even a tenant identifier should be limited to audit or security logs with stricter access controls.\n\nFree text still has a place, especially for human-readable context, but it should not be the only source of meaning. Free-text-only logs are harder to parse, harder to redact reliably, and easier to misinterpret. Structured event design turns logs into operational records rather than a stream of improvised messages. That consistency is what makes monitoring, access control, retention, and review more manageable.",
  "narrationPoints": [
    "Structured logging records events as consistent fields rather than only free-text sentences.",
    "Event names should describe what happened at a level that supports investigation.",
    "Request IDs and correlation IDs help connect logs across services and user-facing errors without exposing internal stack traces or sensitive details.",
    "Sometimes an internal stable identifier is better than an email address.",
    "Free-text-only logs are harder to parse, harder to redact reliably, and easier to misinterpret.",
    "Identifiers should be chosen carefully."
  ]
};
