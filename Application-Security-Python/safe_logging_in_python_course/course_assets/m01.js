window.COURSE_MODULE = {
  "title": "What Safe Logging Means",
  "graphicAlt": "Diagram for What Safe Logging Means, showing application event, field allowlist, recursive redactor, structured log sink, restricted viewer, and retention boundary; labeled arrows identify safe telemetry flow and blocked sensitive fields and the point where unsafe input or behavior is rejected.",
  "narration": "Logging is one of the most useful habits in Python engineering because it helps teams understand what happened, when it happened, and which component was involved. A Flask application, Django site, FastAPI service, command-line tool, scheduled script, background worker, data pipeline, notebook, or internal service may all produce logs for debugging, operations, support, compliance, and incident response. The same usefulness is what makes logging security-sensitive. Logs often preserve details long after a request is gone.\n\nSafe logging means capturing information that supports legitimate operational and security work without collecting unnecessary sensitive data. Application logs, audit logs, traces, metrics, error reports, and security events serve related but different purposes. Application logs explain behavior. Audit logs record important actions. Traces connect work across services. Metrics summarize trends. Error reports capture failures. Security events highlight activity that may matter for investigation. A good design chooses the right record for the right purpose.\n\nUnsafe logs can expose secrets, passwords, API keys, tokens, session identifiers, reset links, private keys, connection strings, personal data, internal file paths, stack traces, customer information, and business rules. That exposure may not be obvious at first. A developer logs a request body for debugging, a framework logs headers during an exception, a worker prints a downstream error, or a notebook stores output. Later, those records move into central logging, tickets, exports, backups, or vendor support channels.\n\nThe goal is balance. Too little logging leaves teams blind during outages and incidents. Too much logging creates privacy, security, cost, and retention problems. Safe logging gives teams enough visibility to operate Python systems confidently while protecting confidentiality, preserving integrity, and keeping logs trustworthy enough to support decisions. It is a design practice, not just a library setting.",
  "narrationPoints": [
    "Logs often preserve details long after a request is gone.",
    "Security events highlight activity that may matter for investigation.",
    "Later, those records move into central logging, tickets, exports, backups, or vendor support channels.",
    "Safe logging gives teams enough visibility to operate Python systems confidently while protecting confidentiality, preserving integrity, and keeping logs trustworthy enough to support decisions.",
    "Audit logs record important actions.",
    "The goal is balance."
  ]
};
