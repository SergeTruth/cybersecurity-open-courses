window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Sensitive Data and Redaction with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Redact sensitive fields at every depth",
      "language": "python",
      "blurb": "The recursive copier normalizes snake_case, camelCase, and punctuation variants, redacts common singular and plural credential/session/token fields at every depth, and leaves the caller's object unchanged.",
      "code": "import math\nimport re\nimport unicodedata\n\nMAX_REDACTION_DEPTH = 32\nMAX_REDACTION_NODES = 10_000\nMAX_MAPPING_ITEMS = 256\nMAX_SEQUENCE_ITEMS = 1_000\nMAX_FIELD_BYTES = 128\nMAX_TEXT_BYTES = 4_096\nMAX_INTEGER_MAGNITUDE = 10**18\nSENSITIVE_FIELD_EXACT = {\n    \"access_token\", \"api_key\", \"authorization\", \"bearer_token\",\n    \"client_secret\", \"connection_string\", \"cookie\", \"credential\", \"credentials\",\n    \"password\", \"passwords\", \"password_hash\", \"private_key\", \"refresh_token\",\n    \"reset_link\", \"secret\", \"secrets\", \"session_cookie\", \"session_id\", \"token\", \"tokens\",\n}\nSENSITIVE_FIELD_TERMS = {\n    \"authorization\", \"bearer\", \"cookie\", \"cookies\", \"credential\", \"credentials\",\n    \"key\", \"keys\", \"password\", \"passwords\", \"secret\", \"secrets\",\n    \"session\", \"sessions\", \"token\", \"tokens\",\n}\n\ndef normalized_field_name(field_name: str) -> str:\n    if not isinstance(field_name, str):\n        raise TypeError(\"log field names must be strings\")\n    text = re.sub(r\"(?<=[a-z0-9])(?=[A-Z])\", \"_\", field_name)\n    return re.sub(r\"[^a-z0-9]+\", \"_\", text.casefold()).strip(\"_\")\n\ndef is_sensitive_field(field_name: str) -> bool:\n    normalized = normalized_field_name(field_name)\n    if normalized in SENSITIVE_FIELD_EXACT:\n        return True\n    return bool(set(normalized.split(\"_\")) & SENSITIVE_FIELD_TERMS)\n\ndef escaped_log_text(value: str) -> str:\n    if len(value.encode(\"utf-8\")) > MAX_TEXT_BYTES:\n        raise ValueError(\"log text exceeds its byte limit\")\n    pieces: list[str] = []\n    for character in value:\n        category = unicodedata.category(character)\n        if category in {\"Cc\", \"Cf\", \"Zl\", \"Zp\"}:\n            codepoint = ord(character)\n            pieces.append(\n                f\"\\\\u{codepoint:04x}\"\n                if codepoint <= 0xFFFF\n                else f\"\\\\U{codepoint:08x}\"\n            )\n        else:\n            pieces.append(character)\n    return \"\".join(pieces)\n\ndef _redact_fields(\n    value: object,\n    field_name: str | None,\n    depth: int,\n    budget: list[int],\n    active: set[int],\n) -> object:\n    budget[0] -= 1\n    if budget[0] < 0 or depth > MAX_REDACTION_DEPTH:\n        raise ValueError(\"log value exceeds the redaction complexity limit\")\n    if field_name is not None and is_sensitive_field(field_name):\n        return \"[REDACTED]\"\n    if isinstance(value, str):\n        return escaped_log_text(value)\n    if value is None or type(value) is bool:\n        return value\n    if type(value) is int:\n        if abs(value) > MAX_INTEGER_MAGNITUDE:\n            raise ValueError(\"log integer exceeds its limit\")\n        return value\n    if type(value) is float:\n        if not math.isfinite(value):\n            raise ValueError(\"non-finite log number rejected\")\n        return value\n    if not isinstance(value, (dict, list, tuple)):\n        return f\"[{type(value).__name__[:80]}]\"\n\n    identity = id(value)\n    if identity in active:\n        raise ValueError(\"cyclic log value rejected\")\n    active.add(identity)\n    try:\n        if isinstance(value, dict):\n            if len(value) > MAX_MAPPING_ITEMS:\n                raise ValueError(\"log mapping has too many fields\")\n            output: dict[str, object] = {}\n            for key, item in value.items():\n                if (\n                    not isinstance(key, str)\n                    or not key\n                    or len(key.encode(\"utf-8\")) > MAX_FIELD_BYTES\n                    or any(unicodedata.category(character) in {\"Cc\", \"Cf\", \"Zl\", \"Zp\"} for character in key)\n                ):\n                    raise ValueError(\"log field name rejected\")\n                output[key] = _redact_fields(\n                    item, key, depth + 1, budget, active\n                )\n            return output\n        if len(value) > MAX_SEQUENCE_ITEMS:\n            raise ValueError(\"log sequence has too many items\")\n        return [\n            _redact_fields(item, None, depth + 1, budget, active)\n            for item in value\n        ]\n    finally:\n        active.remove(identity)\n\ndef redact_fields(value: object, field_name: str | None = None) -> object:\n    if field_name is not None and not isinstance(field_name, str):\n        raise TypeError(\"log field name rejected\")\n    return _redact_fields(\n        value,\n        field_name,\n        0,\n        [MAX_REDACTION_NODES],\n        set(),\n    )\n\n"
    },
    {
      "title": "Allowlist fields for an authentication event",
      "language": "python",
      "blurb": "The event builder accepts a fixed outcome vocabulary and only opaque bounded identifiers; it omits usernames, passwords, tokens, request bodies, and exception strings.",
      "code": "import re\n\nAUTH_OUTCOMES = {\"success\", \"bad_credentials\", \"locked\", \"rate_limited\"}\nOPAQUE_ID = re.compile(r\"[A-Za-z0-9][A-Za-z0-9._-]{0,63}\")\n\ndef require_opaque_id(value: str, field_name: str) -> str:\n    if not isinstance(value, str) or OPAQUE_ID.fullmatch(value) is None:\n        raise ValueError(f\"{field_name} must be an opaque bounded identifier\")\n    return value\n\ndef authentication_event(request_id: str, subject_id: str | None, outcome: str) -> dict[str, object]:\n    if outcome not in AUTH_OUTCOMES:\n        raise ValueError(\"unknown authentication outcome\")\n    return {\n        \"event\": \"authentication_completed\",\n        \"request_id\": require_opaque_id(request_id, \"request_id\"),\n        \"subject_id\": None if subject_id is None else require_opaque_id(subject_id, \"subject_id\"),\n        \"outcome\": outcome,\n    }\n"
    }
  ]
};
