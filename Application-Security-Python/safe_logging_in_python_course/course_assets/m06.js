window.COURSE_MODULE = {
  "title": "Log Injection, Integrity, and Trust",
  "graphicAlt": "Boundary map for Log Injection, Integrity, and Trust, showing application event, field allowlist, recursive redactor, structured log sink, restricted viewer, and retention boundary; labeled arrows identify safe telemetry flow and blocked sensitive fields and the point where unsafe input or behavior is rejected.",
  "narration": "Logs influence investigations, alerts, audits, reports, and sometimes automated decisions. That means logs need integrity. They should reflect what the application observed without allowing untrusted data to reshape the record. If user-controlled text is written directly into free-form log lines, it may confuse interpretation, break parsing, imitate another event, or hide context. The concern is not a payload demonstration. The concern is that logs are records, and records can be distorted.\n\nStructured logging is a strong defensive pattern because untrusted values become fields rather than pieces of the log format itself. Newlines, delimiters, quoting, encoding, and formatting should be handled by the logging system or serializer instead of handcrafted string concatenation. Safe logs make it clear which fields came from users, which came from the application, and which came from trusted infrastructure.\n\nTimestamp and source integrity also matter. Investigators need to know when an event occurred, which service produced it, which environment it came from, and whether clocks are reasonably synchronized. In distributed Python systems, a request may pass through several services and workers. Consistent timestamps, source fields, and correlation IDs help reconstruct what happened without guessing. They also make unusual gaps or impossible sequences easier to spot.\n\nProtecting logs from unauthorized modification or deletion is part of safe logging. Append-only storage, tamper-evident designs, restricted write permissions, controlled administrative access, backups, and separation between producers and log administrators can all help at a high level. Teams should not blindly trust every logged field, especially fields derived from untrusted input, but the logging pipeline itself should be trustworthy enough to support investigation.",
  "narrationPoints": [
    "Logs influence investigations, alerts, audits, reports, and sometimes automated decisions.",
    "The concern is that logs are records, and records can be distorted.",
    "Newlines, delimiters, quoting, encoding, and formatting should be handled by the logging system or serializer instead of handcrafted string concatenation.",
    "In distributed Python systems, a request may pass through several services and workers.",
    "Protecting logs from unauthorized modification or deletion is part of safe logging.",
    "Timestamp and source integrity also matter."
  ]
};
