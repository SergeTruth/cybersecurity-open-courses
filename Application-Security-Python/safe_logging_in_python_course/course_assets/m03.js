window.COURSE_MODULE = {
  "title": "Sensitive Data and Redaction",
  "graphicAlt": "Layered architecture diagram for Sensitive Data and Redaction, showing application event, field allowlist, recursive redactor, structured log sink, restricted viewer, and retention boundary; labeled arrows identify safe telemetry flow and blocked sensitive fields and the point where unsafe input or behavior is rejected.",
  "narration": "Sensitive data in logs should be treated as an exposure risk. Obvious examples include passwords, API keys, bearer tokens, refresh tokens, session identifiers, reset links, private keys, and full connection strings. Less obvious examples include payment data, health data, government identifiers, email addresses, phone numbers, customer-specific record names, tenant identifiers, internal account references, file paths, and business data that can reveal how a system or customer operates.\n\nThe safest pattern is to avoid logging sensitive values in the first place. Redaction and masking are useful controls, but they should not be the only line of defense. A blocklist that looks for known secret names may miss a new token field. A broad object dump may include nested values that nobody expected. An exception string from a library may include a connection URL. Allowlist-oriented logging is usually safer because the code chooses known safe fields instead of copying entire objects.\n\nRedaction removes or replaces sensitive values before they reach logs, traces, alerts, error reports, or tickets. Masking may preserve a small safe portion of a value, such as a suffix used for support correlation, but only when that partial value is truly acceptable. Hashing or tokenizing identifiers can help in some designs, but teams should understand whether the transformed value can still be linked back to individuals, tenants, or accounts.\n\nRedaction needs testing. It is not enough to say the logger redacts secrets if nobody has exercised the failure paths that produce real log events. Tests should include authentication failures, validation errors, exception paths, dependency failures, and representative structured log records. Safe logging is strongest when sensitive values are excluded before logging and redaction catches mistakes that still slip through.",
  "narrationPoints": [
    "Sensitive data in logs should be treated as an exposure risk.",
    "The safest pattern is to avoid logging sensitive values in the first place.",
    "A blocklist that looks for known secret names may miss a new token field.",
    "An exception string from a library may include a connection URL.",
    "Redaction removes or replaces sensitive values before they reach logs, traces, alerts, error reports, or tickets.",
    "Safe logging is strongest when sensitive values are excluded before logging and redaction catches mistakes that still slip through."
  ]
};
