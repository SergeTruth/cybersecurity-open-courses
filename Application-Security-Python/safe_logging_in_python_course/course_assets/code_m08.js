window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Testing, Monitoring, and Incident Response with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Test a self-contained recursive log redactor",
      "language": "python",
      "blurb": "The listing defines the same recursive field policy used earlier, then verifies nested password, token, session, plural credential/secret, and camelCase secret variants never reach captured logs.",
      "code": "import re\n\nSENSITIVE_FIELD_EXACT = {\n    \"access_token\", \"api_key\", \"authorization\", \"bearer_token\",\n    \"client_secret\", \"connection_string\", \"cookie\", \"credential\", \"credentials\",\n    \"password\", \"passwords\", \"password_hash\", \"private_key\", \"refresh_token\",\n    \"reset_link\", \"secret\", \"secrets\", \"session_cookie\", \"session_id\", \"token\", \"tokens\",\n}\nSENSITIVE_FIELD_TERMS = {\n    \"authorization\", \"bearer\", \"cookie\", \"cookies\", \"credential\", \"credentials\",\n    \"key\", \"keys\", \"password\", \"passwords\", \"secret\", \"secrets\",\n    \"session\", \"sessions\", \"token\", \"tokens\",\n}\n\ndef normalized_field_name(field_name: object) -> str:\n    text = str(field_name)\n    text = re.sub(r\"(?<=[a-z0-9])(?=[A-Z])\", \"_\", text)\n    return re.sub(r\"[^a-z0-9]+\", \"_\", text.casefold()).strip(\"_\")\n\ndef is_sensitive_field(field_name: object) -> bool:\n    normalized = normalized_field_name(field_name)\n    if normalized in SENSITIVE_FIELD_EXACT:\n        return True\n    return bool(set(normalized.split(\"_\")) & SENSITIVE_FIELD_TERMS)\n\ndef redact_fields(value: object, field_name: object | None = None) -> object:\n    if field_name is not None and is_sensitive_field(field_name):\n        return \"[REDACTED]\"\n    if isinstance(value, dict):\n        return {str(key): redact_fields(item, key) for key, item in value.items()}\n    if isinstance(value, list):\n        return [redact_fields(item) for item in value]\n    if isinstance(value, tuple):\n        return tuple(redact_fields(item) for item in value)\n    return value\n\ndef test_nested_credentials_are_redacted(caplog, application_logger):\n    secrets = {\n        \"profile\": {\n            \"password\": \"test-only-password-9f3a\",\n            \"password_hash\": \"hash-should-not-log\",\n        },\n        \"tokens\": [{\"refresh_token\": \"r-123\"}, {\"apiToken\": \"api-456\"}],\n        \"session_cookie\": \"session-789\",\n        \"credentials\": \"credentials-should-not-log\",\n        \"secrets\": {\"apiKeys\": \"api-keys-should-not-log\"},\n    }\n    application_logger.info(\n        \"profile_update\",\n        extra={\"payload\": redact_fields(secrets)},\n    )\n    combined = \"\\n\".join(record.getMessage() + repr(record.__dict__) for record in caplog.records)\n    assert \"test-only-password-9f3a\" not in combined\n    assert \"hash-should-not-log\" not in combined\n    assert \"r-123\" not in combined\n    assert \"api-456\" not in combined\n    assert \"session-789\" not in combined\n    assert \"credentials-should-not-log\" not in combined\n    assert \"api-keys-should-not-log\" not in combined\n"
    },
    {
      "title": "Alert on bounded security-event rates",
      "language": "python",
      "blurb": "The monitor aggregates a finite event name and a validated opaque tenant identifier without using usernames, messages, request payloads, or raw external identifiers as metric dimensions.",
      "code": "import re\n\nOPAQUE_ID = re.compile(r\"[A-Za-z0-9][A-Za-z0-9._-]{0,63}\")\n\ndef require_opaque_id(value: object, field_name: str) -> str:\n    if not isinstance(value, str) or OPAQUE_ID.fullmatch(value) is None:\n        raise ValueError(f\"{field_name} must be an opaque bounded identifier\")\n    return value\n\ndef observe_authorization_denial(metrics, tenant_id: str, policy_reason: str) -> None:\n    reasons = {\"missing_scope\", \"wrong_tenant\", \"object_denied\", \"account_disabled\"}\n    metrics.increment(\n        \"authorization_denied\",\n        tags={\n            \"tenant_id\": require_opaque_id(tenant_id, \"tenant_id\"),\n            \"reason\": policy_reason if policy_reason in reasons else \"other\",\n        },\n    )\n"
    }
  ]
};
