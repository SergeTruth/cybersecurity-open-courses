window.COURSE_MODULE = {
  "title": "Log Levels, Exceptions, and Error Detail",
  "graphicAlt": "Decision diagram for Log Levels, Exceptions, and Error Detail, showing application event, field allowlist, recursive redactor, structured log sink, restricted viewer, and retention boundary; labeled arrows identify safe telemetry flow and blocked sensitive fields and the point where unsafe input or behavior is rejected.",
  "narration": "Log levels help separate routine information from warnings, errors, and urgent failures. Debug logs are often useful during development but risky and expensive in production if they include broad internal detail. Info logs can describe normal important events. Warnings may indicate recoverable problems or unusual conditions. Errors and critical events should draw attention to failures that need action. The point is not the label alone, but the operational behavior attached to that label.\n\nException logging is one of the easiest places to leak information. Stack traces and tracebacks may reveal file paths, module names, dependency versions, code structure, function names, query fragments, object representations, or sensitive values embedded in exception messages. Raw exception strings from databases, HTTP clients, payment providers, identity systems, or storage services may contain details that should not be copied directly into user-facing responses or broad logs.\n\nProduction logs should preserve diagnostic value without unnecessary disclosure. Logging the exception type, safe error category, component, correlation ID, request ID, dependency name at an appropriate level, and sanitized context may be enough to begin investigation. The full traceback may belong in a restricted error reporting system, or it may need redaction before collection. User-facing errors should point to a support reference rather than include internal details.\n\nDevelopment and production settings should be clearly separated. Developers may need verbose logs while working locally, but that does not mean debug-level records, raw bodies, or unredacted exception context belong in production. The safe pattern is to make production defaults conservative and to require deliberate, time-bounded changes for increased verbosity. Temporary debugging should have an owner, a reason, an expiration, and a cleanup path.",
  "narrationPoints": [
    "Log levels help separate routine information from warnings, errors, and urgent failures.",
    "Errors and critical events should draw attention to failures that need action.",
    "Exception logging is one of the easiest places to leak information.",
    "User-facing errors should point to a support reference rather than include internal details.",
    "Developers may need verbose logs while working locally, but that does not mean debug-level records, raw bodies, or unredacted exception context belong in production.",
    "Info logs can describe normal important events."
  ]
};
