window.COURSE_MODULE = {
  "title": "Response Validation and Safe Parsing",
  "graphicAlt": "Decision diagram for Response Validation and Safe Parsing, showing Python API client, credential provider, destination policy, TLS channel, response validator, retry budget, and remote service; labeled arrows identify outbound request and response trust boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Remote responses are untrusted input. A status code, header set, content type, body, or error message may be missing, stale, malformed, oversized, misleading, or inconsistent with the expected contract. HTTPS protects transport to the endpoint, but it does not prove that a response is semantically correct, authorized for the current user, safe to store, or appropriate to act on. The client must validate what it receives before using it.\n\nResponse validation should check both structure and meaning. JSON parsing tells you whether bytes can be decoded, but schema validation concepts help confirm expected fields, types, optional fields, unknown fields, and nested shapes. The client should decide how strict to be when fields are missing or extra fields appear. For high-impact workflows, it should also check identifiers, tenant context, status transitions, timestamps, authorization context, and business conditions before updating local state.\n\nError responses need their own handling. A remote denial, timeout, rate limit, malformed response, partial result, or service outage should not be interpreted as success. At the same time, raw remote error messages may contain sensitive details or confusing text that should not be shown directly to users. Safe clients separate internal diagnostic information from user-facing messages, preserve enough context for troubleshooting, and avoid logging sensitive response bodies by default.\n\nNon-JSON formats deserve caution. XML, YAML, CSV, compressed files, archives, and binary responses can introduce parsing complexity, resource consumption, and downstream processing risk. A secure client sets size limits, validates content type, rejects unexpected formats, and uses safe parsers appropriate to the data. The theme is consistent: do not let a remote service silently redefine what your application accepts, stores, displays, or acts upon.",
  "narrationPoints": [
    "A status code, header set, content type, body, or error message may be missing, stale, malformed, oversized, misleading, or inconsistent with the expected contract.",
    "The client should decide how strict to be when fields are missing or extra fields appear.",
    "A remote denial, timeout, rate limit, malformed response, partial result, or service outage should not be interpreted as success.",
    "XML, YAML, CSV, compressed files, archives, and binary responses can introduce parsing complexity, resource consumption, and downstream processing risk.",
    "The theme is consistent: do not let a remote service silently redefine what your application accepts, stores, displays, or acts upon.",
    "Non-JSON formats deserve caution."
  ]
};
