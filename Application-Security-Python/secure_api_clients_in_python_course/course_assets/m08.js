window.COURSE_MODULE = {
  "title": "SDKs, Dependencies, Testing, Logging, and Monitoring",
  "graphicAlt": "Operational lifecycle diagram for SDKs, Dependencies, Testing, Logging, and Monitoring, showing Python API client, credential provider, destination policy, TLS channel, response validator, retry budget, and remote service; labeled arrows identify outbound request and response trust boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "SDKs can improve consistency, but they do not eliminate responsibility. Official and third-party SDKs may handle authentication, signing, pagination, retries, serialization, and endpoint construction, yet the application still owns credential scope, business authorization, data minimization, response handling, version pinning, and monitoring. Dependency review should consider package source, maintenance, transitive dependencies, configuration defaults, deprecation plans, and how the SDK exposes security-sensitive options.\n\nTesting should cover more than the happy path. API client tests should exercise authentication failure, authorization failure, malformed responses, unexpected content types, oversized responses, timeouts, connection failures, rate limits, retries, pagination boundaries, duplicate records, redirects, unavailable services, and contract changes. Mocked and staged API testing can both help, as long as tests do not teach the code to trust unrealistic responses or ignore important failure modes.\n\nLogging must be useful without becoming a data leak. Outbound requests and responses may contain tokens, credentials, sensitive headers, full URLs with query parameters, private payloads, regulated data, and response bodies that should not be retained. Safe logging uses redaction, event names, correlation identifiers, timing, destination identity, status categories, and high-impact audit events. The point is to support troubleshooting and accountability while limiting exposure in long-lived log stores.\n\nMonitoring and incident response complete the lifecycle. Watch for unusual destinations, authentication failures, authorization errors, retry storms, latency changes, quota exhaustion, error spikes, unexpected redirects, and changes in response shape. If credentials, requests, or responses are exposed, teams need a path to revoke or rotate credentials, assess data exposure, review logs, notify owners, and improve controls. Secure API clients are maintained systems, not one-time snippets.",
  "narrationPoints": [
    "SDKs can improve consistency, but they do not eliminate responsibility.",
    "Testing should cover more than the happy path.",
    "Logging must be useful without becoming a data leak.",
    "The point is to support troubleshooting and accountability while limiting exposure in long-lived log stores.",
    "Watch for unusual destinations, authentication failures, authorization errors, retry storms, latency changes, quota exhaustion, error spikes, unexpected redirects, and changes in response shape.",
    "Dependency review should consider package source, maintenance, transitive dependencies, configuration defaults, deprecation plans, and how the SDK exposes security-sensitive options."
  ]
};
