window.COURSE_MODULE = {
  "title": "API Client Architecture and Trust Boundaries",
  "graphicAlt": "Flowchart for API Client Architecture and Trust Boundaries, showing Python API client, credential provider, destination policy, TLS channel, response validator, retry budget, and remote service; labeled arrows identify outbound request and response trust boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Every API client should have an explicit trust model. Internal APIs, external APIs, partner APIs, cloud APIs, identity APIs, webhook callbacks, service-to-service calls, gateways, and proxies all move data across boundaries. The boundary may be between applications, business units, networks, tenants, vendors, regions, or privilege levels. A secure design names those boundaries so the client can apply the right controls at the point where data, identity, credentials, destination, or assumptions change.\n\nConfigured endpoints are different from user-supplied destinations. A client that always calls a known service from configuration has a narrower risk profile than a client that accepts a URL, host, tenant endpoint, webhook target, or callback address from input. Environment-specific endpoints also need care. Development, staging, production, and disaster recovery may use different destinations, but each environment should still define what is allowed and what data may flow there.\n\nInternal APIs should not be trusted only because they are internal. Internal services can be misconfigured, overexposed, compromised, called from unexpected paths, or given broader access than their callers should have. A service-to-service request still needs authentication, authorization, request validation, response validation, and monitoring appropriate to the impact of the operation. Network location can support a control model, but it should not be the whole control model.\n\nGood architecture also defines ownership and failure modes. Someone should own the integration, the credentials, the data contract, the allowed destinations, the retry policy, the logging expectations, and the incident response path. The design should explain what happens when the remote service is slow, unavailable, returns malformed data, changes a contract, denies access, or produces partial results. Those choices affect security because they shape what the application does under stress.",
  "narrationPoints": [
    "Every API client should have an explicit trust model.",
    "The boundary may be between applications, business units, networks, tenants, vendors, regions, or privilege levels.",
    "Internal services can be misconfigured, overexposed, compromised, called from unexpected paths, or given broader access than their callers should have.",
    "Network location can support a control model, but it should not be the whole control model.",
    "Someone should own the integration, the credentials, the data contract, the allowed destinations, the retry policy, the logging expectations, and the incident response path.",
    "Environment-specific endpoints also need care."
  ]
};
