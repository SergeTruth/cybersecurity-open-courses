window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Response Validation and Safe Parsing with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Bound and strictly parse a JSON response",
      "language": "python",
      "blurb": "The client streams a size-limited body, requires JSON, and rejects non-finite numbers before schema validation.",
      "code": "import json\n\nclass APIResponseError(Exception):\n    pass\n\ndef response_status(response):\n    try:\n        return getattr(response, \"status_code\", None)\n    except Exception:\n        raise APIResponseError(\"API returned invalid status\") from None\n\ndef header_getter(response):\n    try:\n        headers = getattr(response, \"headers\", None)\n        getter = getattr(headers, \"get\", None)\n    except Exception:\n        raise ValueError(\"response headers rejected\") from None\n    if not callable(getter):\n        raise ValueError(\"response headers rejected\")\n    return getter\n\ndef unique_object(pairs):\n    value = {}\n    for key, item in pairs:\n        if key in value:\n            raise ValueError(\"duplicate JSON field rejected\")\n        value[key] = item\n    return value\n\ndef read_json_response(response, maximum: int = 1_000_000):\n    if type(maximum) is not int or not 1 <= maximum <= 10_000_000:\n        raise ValueError(\"response size limit rejected\")\n    try:\n        response.raise_for_status()\n    except Exception:\n        status = response_status(response)\n        if type(status) is int and 100 <= status <= 599:\n            raise APIResponseError(f\"API returned HTTP {status}\") from None\n        raise APIResponseError(\"API request failed\") from None\n    get_header = header_getter(response)\n    try:\n        content_type = get_header(\"Content-Type\", \"\")\n    except Exception:\n        raise ValueError(\"response headers rejected\") from None\n    if not isinstance(content_type, str):\n        raise ValueError(\"response content type rejected\")\n    media_type = content_type.split(\";\", 1)[0].lower()\n    if media_type != \"application/json\":\n        raise ValueError(\"unexpected response media type\")\n\n    chunks = bytearray()\n    try:\n        iterator = iter(response.iter_content(64 * 1024))\n    except Exception:\n        raise APIResponseError(\"API response body could not be read\") from None\n    while True:\n        try:\n            chunk = next(iterator)\n        except StopIteration:\n            break\n        except Exception:\n            raise APIResponseError(\"API response body could not be read\") from None\n        if not isinstance(chunk, (bytes, bytearray)):\n            raise ValueError(\"response body chunk rejected\")\n        if len(chunk) > maximum - len(chunks):\n            raise ValueError(\"response body is too large\")\n        chunks.extend(chunk)\n\n    try:\n        text = chunks.decode(\"utf-8\")\n    except UnicodeDecodeError:\n        raise ValueError(\"response body is not valid UTF-8\") from None\n    try:\n        return json.loads(\n            text,\n            object_pairs_hook=unique_object,\n            parse_constant=lambda token: (_ for _ in ()).throw(\n                ValueError(f\"non-finite JSON number rejected: {token}\")\n            ),\n        )\n    except json.JSONDecodeError:\n        raise ValueError(\"response body is not valid JSON\") from None\n"
    },
    {
      "title": "Validate an API response schema and value ranges",
      "language": "python",
      "blurb": "Exact fields, identifier syntax, amount bounds, and a tightly constrained HTTPS receipt URL are checked before use.",
      "code": "import re\nfrom urllib.parse import urlsplit\n\nINVOICE_ID = re.compile(r\"inv_[A-Za-z0-9]{1,60}\\Z\")\nRECEIPT_PATH = re.compile(r\"/receipts/[A-Za-z0-9_-]{1,80}\\Z\")\n\ndef has_control_or_space(value: str) -> bool:\n    return any(ch.isspace() or ord(ch) < 32 or ord(ch) == 127 for ch in value)\n\ndef validate_invoice(value: object) -> dict[str, object]:\n    if not isinstance(value, dict) or set(value) != {\"id\", \"amount_cents\", \"receipt_url\"}:\n        raise ValueError(\"invoice response schema mismatch\")\n    identifier = value[\"id\"]\n    amount = value[\"amount_cents\"]\n    receipt_url = value[\"receipt_url\"]\n    if not isinstance(identifier, str) or not INVOICE_ID.fullmatch(identifier):\n        raise ValueError(\"invoice identifier rejected\")\n    if type(amount) is not int or not 0 <= amount <= 100_000_000:\n        raise ValueError(\"invoice amount rejected\")\n    if not isinstance(receipt_url, str) or has_control_or_space(receipt_url):\n        raise ValueError(\"receipt URL rejected\")\n    receipt = urlsplit(receipt_url)\n    try:\n        port = receipt.port\n    except ValueError:\n        raise ValueError(\"receipt URL port rejected\") from None\n    if (\n        receipt.scheme != \"https\"\n        or receipt.hostname != \"receipts.example\"\n        or port not in {None, 443}\n        or receipt.username is not None\n        or receipt.password is not None\n        or not RECEIPT_PATH.fullmatch(receipt.path)\n        or receipt.query\n        or receipt.fragment\n    ):\n        raise ValueError(\"receipt URL destination rejected\")\n    return {\n        \"id\": identifier,\n        \"amount_cents\": amount,\n        \"receipt_url\": f\"https://receipts.example{receipt.path}\",\n    }\n"
    }
  ]
};
