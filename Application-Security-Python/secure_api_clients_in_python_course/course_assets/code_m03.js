window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Authentication, Tokens, and Credential Handling with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Build a bearer-authenticated client",
      "language": "python",
      "blurb": "The credential is validated, kept out of global session headers, and attached only inside approved API calls to the fixed inventory origin.",
      "code": "import re\nimport requests\n\nACCESS_TOKEN = re.compile(r\"[A-Za-z0-9._~+/=-]{20,2048}\\Z\")\nPRODUCT_ID = re.compile(r\"SKU_[A-Za-z0-9_-]{1,60}\\Z\")\nAPI_ORIGIN = \"https://inventory.example\"\n\nclass InventoryAPIError(Exception):\n    pass\n\nclass InventoryAPIClient:\n    def __init__(self, access_token: str):\n        if not isinstance(access_token, str) or not ACCESS_TOKEN.fullmatch(access_token):\n            raise ValueError(\"access token format rejected\")\n        self._access_token = access_token\n        self._session = requests.Session()\n        self._session.trust_env = False\n        self._session.headers.update({\n            \"Accept\": \"application/json\",\n            \"User-Agent\": \"inventory-client/3\",\n        })\n\n    def get_product(self, product_id: str):\n        if not isinstance(product_id, str) or not PRODUCT_ID.fullmatch(product_id):\n            raise ValueError(\"product identifier rejected\")\n        try:\n            return self._session.get(\n                f\"{API_ORIGIN}/v2/products\",\n                params={\"id\": product_id},\n                headers={\"Authorization\": f\"Bearer {self._access_token}\"},\n                timeout=(3, 8),\n                allow_redirects=False,\n            )\n        except Exception:\n            raise InventoryAPIError(\"inventory API request could not be completed\") from None\n\n    def close(self) -> None:\n        self._session.close()\n"
    },
    {
      "title": "Refresh credentials under a lock",
      "language": "python",
      "blurb": "Only one thread refreshes an expiring token, and the credential provider—not a remote response—sets the resulting scope.",
      "code": "import re\nfrom dataclasses import dataclass\nfrom datetime import datetime, timedelta, timezone\nfrom threading import Lock\n\nACCESS_TOKEN = re.compile(r\"[A-Za-z0-9._~+/=-]{20,2048}\\Z\")\nMAX_TOKEN_LIFETIME = timedelta(hours=1)\n\nclass CredentialProviderError(Exception):\n    pass\n\n@dataclass(frozen=True)\nclass CachedToken:\n    value: str\n    expires_at: datetime\n\ndef validated_token(provider_token, now: datetime) -> CachedToken:\n    if (\n        type(now) is not datetime\n        or now.tzinfo is None\n        or now.utcoffset() is None\n    ):\n        raise ValueError(\"token validation time rejected\")\n    try:\n        value = getattr(provider_token, \"value\", None)\n        expires_at = getattr(provider_token, \"expires_at\", None)\n    except Exception:\n        raise CredentialProviderError(\n            \"credential provider returned an unreadable token\"\n        ) from None\n    now_utc = now.astimezone(timezone.utc)\n    if not isinstance(value, str) or not ACCESS_TOKEN.fullmatch(value):\n        raise ValueError(\"issued access token rejected\")\n    if (\n        type(expires_at) is not datetime\n        or expires_at.tzinfo is None\n        or expires_at.utcoffset() is None\n    ):\n        raise ValueError(\"issued token expiration rejected\")\n    expires_utc = expires_at.astimezone(timezone.utc)\n    if not (\n        now_utc + timedelta(seconds=30)\n        < expires_utc\n        <= now_utc + MAX_TOKEN_LIFETIME\n    ):\n        raise ValueError(\"issued token expiration rejected\")\n    return CachedToken(value=value, expires_at=expires_utc)\n\nclass TokenCache:\n    def __init__(self, provider):\n        self._provider = provider\n        self._lock = Lock()\n        self._cached = None\n\n    def current(self):\n        with self._lock:\n            now = datetime.now(timezone.utc)\n            if (\n                self._cached is None\n                or self._cached.expires_at <= now + timedelta(seconds=30)\n            ):\n                try:\n                    issued = self._provider.issue(scope=\"inventory:read\")\n                except Exception:\n                    raise CredentialProviderError(\n                        \"credential provider could not issue a token\"\n                    ) from None\n                self._cached = validated_token(issued, now)\n            return self._cached.value\n"
    }
  ]
};
