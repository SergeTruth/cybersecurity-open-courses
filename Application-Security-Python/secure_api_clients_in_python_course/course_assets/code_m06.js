window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Pagination, Rate Limits, Retries, and Idempotency with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Bound cursor pagination",
      "language": "python",
      "blurb": "The iterator limits pages and records, rejects repeated cursors, and validates every page before yielding items.",
      "code": "import re\n\nCURSOR_VALUE = re.compile(r\"[A-Za-z0-9._~:-]{1,256}\\Z\")\n\nclass PaginationAPIError(Exception):\n    pass\n\ndef read_page(fetch_page, cursor):\n    try:\n        page = fetch_page(cursor=cursor, limit=100)\n        items = getattr(page, \"items\", None)\n        next_cursor = getattr(page, \"next_cursor\", None)\n    except Exception:\n        raise PaginationAPIError(\"pagination page could not be read\") from None\n    return items, next_cursor\n\ndef iter_accounts(fetch_page, maximum_pages: int = 20, maximum_records: int = 2_000):\n    if type(maximum_pages) is not int or not 1 <= maximum_pages <= 100:\n        raise ValueError(\"pagination page budget rejected\")\n    if type(maximum_records) is not int or not 1 <= maximum_records <= 10_000:\n        raise ValueError(\"pagination record budget rejected\")\n\n    cursor = None\n    seen = set()\n    emitted = 0\n    for _ in range(maximum_pages):\n        items, next_cursor = read_page(fetch_page, cursor)\n        if not isinstance(items, list):\n            raise ValueError(\"pagination items shape rejected\")\n        if next_cursor is not None:\n            if not isinstance(next_cursor, str) or not CURSOR_VALUE.fullmatch(next_cursor):\n                raise ValueError(\"pagination cursor rejected\")\n            if next_cursor in seen:\n                raise ValueError(\"pagination cursor repeated\")\n            seen.add(next_cursor)\n\n        for account in items:\n            emitted += 1\n            if emitted > maximum_records:\n                raise ValueError(\"pagination record budget exceeded\")\n            yield account\n        if next_cursor is None:\n            return\n        cursor = next_cursor\n    raise ValueError(\"pagination page budget exceeded\")\n"
    },
    {
      "title": "Retry only bounded transient failures",
      "language": "python",
      "blurb": "Each abandoned transient response is closed before a bounded delay; only the final non-retried response remains caller-owned.",
      "code": "from time import sleep\n\nAPI_ORIGIN = \"https://api.example\"\nENDPOINTS = {\"inventory\": \"/v1/inventory\", \"health\": \"/health\"}\n\nclass APIAvailabilityError(Exception):\n    pass\n\ndef close_response(response, primary_error: BaseException | None = None) -> None:\n    close = getattr(response, \"close\", None)\n    try:\n        if not callable(close):\n            raise TypeError(\"API response close capability rejected\")\n        close()\n    except BaseException as cleanup_error:\n        if primary_error is not None:\n            raise BaseExceptionGroup(\n                \"API operation and response cleanup failed\",\n                [primary_error, cleanup_error],\n            )\n        raise APIAvailabilityError(\"API response cleanup failed\") from cleanup_error\n    if primary_error is not None:\n        raise primary_error\n\ndef response_status(response) -> int:\n    try:\n        status = getattr(response, \"status_code\", None)\n    except Exception:\n        raise RuntimeError(\"API returned invalid status\") from None\n    if type(status) is not int or not 100 <= status <= 599:\n        raise RuntimeError(\"API returned invalid status\")\n    return status\n\ndef retry_after_seconds(response, fallback: int) -> int:\n    try:\n        headers = getattr(response, \"headers\", None)\n        getter = getattr(headers, \"get\", None)\n        if not callable(getter):\n            return fallback\n        header = getter(\"Retry-After\", \"\")\n    except Exception:\n        return fallback\n    if isinstance(header, str) and len(header) <= 3 and header.isdecimal():\n        return min(int(header), 5)\n    return fallback\n\ndef get_with_retry(session, operation: str):\n    if not isinstance(operation, str) or operation not in ENDPOINTS:\n        raise ValueError(\"API operation is not approved\")\n    url = API_ORIGIN + ENDPOINTS[operation]\n    transient = {429, 502, 503, 504}\n    for attempt in range(3):\n        try:\n            response = session.get(url, timeout=(3, 8), allow_redirects=False)\n        except Exception:\n            raise APIAvailabilityError(\"API request could not be completed\") from None\n        try:\n            status = response_status(response)\n        except BaseException as error:\n            close_response(response, error)\n            raise AssertionError(\"unreachable\")\n        if status not in transient:\n            return response\n        delay = retry_after_seconds(response, 2 ** attempt)\n        close_response(response)\n        if attempt < 2:\n            sleep(delay)\n    raise RuntimeError(\"API retry budget exhausted\")\n"
    }
  ]
};
