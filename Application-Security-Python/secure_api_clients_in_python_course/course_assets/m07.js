window.COURSE_MODULE = {
  "title": "TLS, Redirects, Proxies, and Destination Control",
  "graphicAlt": "Control-flow diagram for TLS, Redirects, Proxies, and Destination Control, showing Python API client, credential provider, destination policy, TLS channel, response validator, retry budget, and remote service; labeled arrows identify outbound request and response trust boundaries and the point where unsafe input or behavior is rejected.",
  "narration": "Destination control protects both data and credentials. A secure client should know which schemes, hosts, ports, and paths are allowed for a given integration. HTTPS should be the normal expectation for sensitive API communication, and TLS certificate validation and hostname validation should remain enabled. Disabling verification may make a connection work temporarily, but it weakens the client's ability to confirm that it is communicating with the intended trusted endpoint.\n\nName resolution and address behavior matter at a high level. A configured host can resolve differently across environments, networks, or time. Some applications need to consider private address ranges, loopback addresses, metadata services, internal-only destinations, and public endpoints when accepting destination-related input. The defensive principle is simple: do not allow untrusted input to decide where sensitive requests or credentials are sent without validation against an approved destination policy.\n\nRedirects should be handled deliberately. A redirect can move a request from one host, scheme, or trust boundary to another. Clients need to decide whether redirects are allowed, which methods can follow them, whether authorization headers or other sensitive headers should be preserved, and what destination changes are acceptable. Blindly following redirects can leak metadata or credentials and can make audit trails harder to understand.\n\nProxies are also part of the trust model. Python clients and underlying libraries may use explicit proxy settings, environment-driven proxy settings, service mesh behavior, or platform egress controls. Those mechanisms can be useful for inspection, routing, and policy enforcement, but they should be expected and documented. A secure client avoids surprising proxy behavior for sensitive calls and logs enough destination and error context to support operations without exposing secrets.",
  "narrationPoints": [
    "A secure client should know which schemes, hosts, ports, and paths are allowed for a given integration.",
    "Name resolution and address behavior matter at a high level.",
    "Some applications need to consider private address ranges, loopback addresses, metadata services, internal-only destinations, and public endpoints when accepting destination-related input.",
    "Proxies are also part of the trust model.",
    "A secure client avoids surprising proxy behavior for sensitive calls and logs enough destination and error context to support operations without exposing secrets.",
    "Redirects should be handled deliberately."
  ]
};
