window.COURSE_MODULE = {
  "title": "Hostname Validation and Service Identity",
  "graphicAlt": "Sequence diagram for Hostname Validation and Service Identity, showing Python client, TLS endpoint, certificate chain, hostname, and trust store; labeled arrows identify accepted and rejected validation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Hostname validation checks that the certificate matches the service the client intended to reach. If the Python application connects to a configured service name, the certificate should identify that service name according to the applicable validation rules. This is different from simply proving that the certificate chains to some trusted authority. A trusted authority can issue many certificates, and the client needs the one for the expected endpoint.\n\nSubject alternative names are central in modern certificate identity. DNS names, service names, wildcard certificates, and IP address certificates each have specific operational considerations. Wildcards can simplify management in some designs, but they broaden the set of names covered by one key and certificate. IP address certificates and internal service names require careful planning so clients validate the identity they actually expect instead of relying on network location alone.\n\nProxies, gateways, and load balancers can change where certificate identity is presented. A client may connect to a gateway name while the gateway routes to a backend service. A service mesh may terminate and re-establish TLS internally. A database proxy may present its own certificate. These architectures can be safe, but the expected service identity should be documented. The certificate name should match the boundary the client is intentionally trusting.\n\nCertificate name mismatches should be treated as meaningful failures. Local development may use test names or local-only certificates, but those exceptions should not normalize insecure production behavior. Hostname validation matters even when transport is encrypted because it tells the client whether the encrypted connection is with the intended service. Service identity should be based on expected names and trust anchors, not only on private network placement.",
  "narrationPoints": [
    "Hostname validation checks that the certificate matches the service the client intended to reach.",
    "Subject alternative names are central in modern certificate identity.",
    "Proxies, gateways, and load balancers can change where certificate identity is presented.",
    "A service mesh may terminate and re-establish TLS internally.",
    "Certificate name mismatches should be treated as meaningful failures.",
    "Service identity should be based on expected names and trust anchors, not only on private network placement."
  ]
};
