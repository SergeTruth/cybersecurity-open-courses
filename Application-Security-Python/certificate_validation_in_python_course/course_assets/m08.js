window.COURSE_MODULE = {
  "title": "Failure Handling, Logging, Testing, and Monitoring",
  "graphicAlt": "Operational lifecycle diagram for Failure Handling, Logging, Testing, and Monitoring, showing Python client, TLS endpoint, certificate chain, hostname, and trust store; labeled arrows identify accepted and rejected validation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Certificate validation failures should fail safely or follow a documented secure error path. A hostname mismatch, expired certificate, untrusted issuer, or purpose mismatch is not just an inconvenience. It means the client cannot confirm the trust decision it expected. User-facing errors should be limited and clear, while internal logs can capture safe diagnostic detail for operators without exposing secrets or sensitive certificate material.\n\nLogging should make validation observable without becoming a leak. Useful fields include service name, environment, safe error category, validation outcome, certificate expiration warning, dependency name, and correlation ID. Logs should avoid private keys, tokens, credentials, full sensitive payloads, and unnecessary certificate details. Structured logging helps teams monitor trends and investigate incidents without dumping raw internals to broad audiences.\n\nTesting should cover expected trust behavior. Tests can verify hostname mismatch handling, expired or untrusted certificate behavior at a high level, development versus production settings, and safe errors when validation fails. Dependency and library review should confirm that clients, drivers, runtimes, and container images use maintained TLS behavior and that validation exceptions do not creep into shared production code paths.\n\nMonitoring and incident response complete the lifecycle. Teams should monitor certificate expiration, handshake failures, validation errors, renewal failures, and configuration drift. If a private key is exposed, a certificate is misissued, or unsafe validation settings are discovered, incident response should identify affected services, rotate or revoke material as appropriate, review logs, fix configuration, and add regression tests. Certificate validation must remain observable without weakening the trust decision.",
  "narrationPoints": [
    "Certificate validation failures should fail safely or follow a documented secure error path.",
    "User-facing errors should be limited and clear, while internal logs can capture safe diagnostic detail for operators without exposing secrets or sensitive certificate material.",
    "Useful fields include service name, environment, safe error category, validation outcome, certificate expiration warning, dependency name, and correlation ID.",
    "Structured logging helps teams monitor trends and investigate incidents without dumping raw internals to broad audiences.",
    "Certificate validation must remain observable without weakening the trust decision.",
    "Monitoring and incident response complete the lifecycle."
  ]
};
