window.COURSE_MODULE = {
  "title": "Certificate Validation in Python Clients",
  "graphicAlt": "Decision diagram for Certificate Validation in Python Clients, showing Python client, TLS endpoint, certificate chain, hostname, and trust store; labeled arrows identify accepted and rejected validation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Python client code uses certificate validation in many places: HTTP clients, database drivers, message broker clients, service SDKs, webhook callers, and internal integration libraries. Each library has its own defaults and configuration options, but the durable principle is the same. Sensitive client connections should use validated TLS defaults, approved trust stores, explicit destinations, safe timeouts, and controlled error handling.\n\nHTTPS URLs are a common starting point, but the scheme alone is not enough. The client should verify certificates and hostnames, use appropriate certificate bundles or platform trust stores, and avoid shortcuts that disable verification. Proxy and redirect behavior should also be reviewed because requests can cross boundaries after the initial destination is chosen. If sensitive headers or tokens move with a request, the trust boundary matters.\n\nDatabase driver and message broker TLS validation deserves the same attention as HTTP. Application teams sometimes treat non-HTTP protocols as internal plumbing and assume private network access is sufficient. In reality, these connections may carry credentials, personal data, operational commands, and business records. The driver should be configured so endpoint identity and certificate trust are appropriate for the environment and data sensitivity.\n\nExceptions to normal validation should be rare, documented, and reviewed. A local-only development setting, a private root certificate, or a staging certificate chain may be reasonable in context, but the context should be explicit. Trust any certificate behavior should not become a shared helper or production fallback. Python clients should fail safely when validation fails and provide operational detail through controlled logs rather than weakening trust decisions silently.",
  "narrationPoints": [
    "Python client code uses certificate validation in many places: HTTP clients, database drivers, message broker clients, service SDKs, webhook callers, and internal integration libraries.",
    "HTTPS URLs are a common starting point, but the scheme alone is not enough.",
    "If sensitive headers or tokens move with a request, the trust boundary matters.",
    "Application teams sometimes treat non-HTTP protocols as internal plumbing and assume private network access is sufficient.",
    "Exceptions to normal validation should be rare, documented, and reviewed.",
    "Python clients should fail safely when validation fails and provide operational detail through controlled logs rather than weakening trust decisions silently."
  ]
};
