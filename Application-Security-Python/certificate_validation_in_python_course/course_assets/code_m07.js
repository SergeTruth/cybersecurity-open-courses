window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Mutual TLS and Client Certificate Validation with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Open an approved mutual-TLS connection",
      "language": "python",
      "blurb": "Application bootstrap captures the partner endpoint, authenticated server CA, and read-only client-identity paths; callers provide only the noninteractive key-password provider and receive a completed connection, not a mutable SSLContext.",
      "code": "from collections.abc import Callable\nfrom hashlib import sha256\nfrom hmac import compare_digest\nfrom pathlib import Path\nimport re\nimport socket\nimport ssl\nfrom typing import Final\n\nPasswordProvider = Callable[[], str | bytes]\nMAX_CA_BYTES = 1_000_000\nPARTNER_HOST: Final = \"partner.example.test\"\nPARTNER_ADDRESS: Final = \"192.0.2.40\"\nPARTNER_PORT: Final = 443\nPARTNER_CA_FILE: Final = Path(\"/run/secrets/partner-root.pem\")\nPARTNER_CA_SHA256: Final = (\n    \"bcf9b43c5b9315fca05cdd7be5e06dab7c3165ac05b7709a6d471f9963e60f38\"\n)\nCLIENT_CERTIFICATE: Final = \"/run/secrets/client-certificate.pem\"\nCLIENT_PRIVATE_KEY: Final = \"/run/secrets/client-private-key.pem\"\n\ndef _mtls_connection_factory(\n    host: str, address: str, port: int, ca_file: Path, ca_sha256: str,\n    certificate: str, private_key: str\n) -> Callable[[PasswordProvider], ssl.SSLSocket]:\n    if re.fullmatch(r\"[0-9a-fA-F]{64}\", ca_sha256) is None:\n        raise RuntimeError(\"trusted partner CA policy is invalid\")\n    expected = ca_sha256.lower()\n\n    def connect(password_provider: PasswordProvider) -> ssl.SSLSocket:\n        if not callable(password_provider):\n            raise TypeError(\"private-key password provider required\")\n        with ca_file.open(\"rb\") as source:\n            ca_bytes = source.read(MAX_CA_BYTES + 1)\n        if not ca_bytes or len(ca_bytes) > MAX_CA_BYTES:\n            raise ValueError(\"partner CA is empty or too large\")\n        if not compare_digest(sha256(ca_bytes).hexdigest(), expected):\n            raise PermissionError(\"partner CA is not approved\")\n        try:\n            ca_pem = ca_bytes.decode(\"ascii\")\n        except UnicodeDecodeError as error:\n            raise ValueError(\"partner CA must be PEM encoded\") from error\n        context = ssl.create_default_context(cadata=ca_pem)\n        context.minimum_version = ssl.TLSVersion.TLSv1_2\n        context.load_cert_chain(\n            certfile=certificate, keyfile=private_key, password=password_provider\n        )\n        raw = socket.create_connection((address, port), timeout=5)\n        try:\n            return context.wrap_socket(raw, server_hostname=host)\n        except BaseException:\n            raw.close()\n            raise\n\n    return connect\n\nopen_partner_mtls = _mtls_connection_factory(\n    PARTNER_HOST, PARTNER_ADDRESS, PARTNER_PORT, PARTNER_CA_FILE,\n    PARTNER_CA_SHA256, CLIENT_CERTIFICATE, CLIENT_PRIVATE_KEY\n)\ndel _mtls_connection_factory\n"
    },
    {
      "title": "Authorize an mTLS workload identity",
      "language": "python",
      "blurb": "Trusted server bootstrap captures one client-verifying TLS context and an immutable role-policy snapshot; its acceptor performs the handshake and authorization before request handling receives the completed channel and permissions.",
      "code": "from collections.abc import Callable\nimport socket\nimport ssl\nfrom types import MappingProxyType\nfrom cryptography import x509\nfrom cryptography.x509.oid import ExtensionOID\n\ndef build_client_connection_authorizer(\n    approved_context: ssl.SSLContext, workload_roles: dict[str, set[str]]\n) -> Callable[[socket.socket], tuple[ssl.SSLSocket, frozenset[str]]]:\n    if (\n        approved_context.protocol != ssl.PROTOCOL_TLS_SERVER\n        or approved_context.verify_mode != ssl.CERT_REQUIRED\n    ):\n        raise ValueError(\"client-verifying server context required\")\n    policy_data = {}\n    for identity, permissions in workload_roles.items():\n        if (\n            not isinstance(identity, str)\n            or not identity\n            or not permissions\n            or any(not isinstance(value, str) or not value for value in permissions)\n        ):\n            raise ValueError(\"workload role policy is invalid\")\n        policy_data[identity] = frozenset(permissions)\n    policy = MappingProxyType(policy_data)\n    context = approved_context\n\n    def accept_and_authorize(\n        raw: socket.socket,\n    ) -> tuple[ssl.SSLSocket, frozenset[str]]:\n        if not isinstance(raw, socket.socket) or isinstance(raw, ssl.SSLSocket):\n            raise TypeError(\"unwrapped accepted socket required\")\n        try:\n            tls = context.wrap_socket(raw, server_side=True)\n        except BaseException:\n            raw.close()\n            raise\n        try:\n            certificate_bytes = tls.getpeercert(binary_form=True)\n            if not certificate_bytes:\n                raise PermissionError(\"verified client certificate required\")\n            certificate = x509.load_der_x509_certificate(certificate_bytes)\n            identities = certificate.extensions.get_extension_for_oid(\n                ExtensionOID.SUBJECT_ALTERNATIVE_NAME\n            ).value.get_values_for_type(x509.UniformResourceIdentifier)\n            if len(identities) != 1:\n                raise PermissionError(\"client certificate identity rejected\")\n            try:\n                permissions = policy[identities[0]]\n            except KeyError as error:\n                raise PermissionError(\"client certificate identity rejected\") from error\n            return tls, permissions\n        except BaseException:\n            tls.close()\n            raise\n\n    return accept_and_authorize\n"
    }
  ]
};
