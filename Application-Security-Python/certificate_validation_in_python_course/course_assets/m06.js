window.COURSE_MODULE = {
  "title": "Private PKI, Self-Signed Certificates, and Development Environments",
  "graphicAlt": "Boundary map for Private PKI, Self-Signed Certificates, and Development Environments, showing Python client, TLS endpoint, certificate chain, hostname, and trust store; labeled arrows identify accepted and rejected validation paths and the point where unsafe input or behavior is rejected.",
  "narration": "Publicly trusted certificates work well for many internet-facing services, but internal systems often use private certificate authorities, privately issued certificates, or self-signed development certificates. These patterns are not automatically wrong. They become risky when trust is improvised, undocumented, copied across environments, or handled by disabling validation instead of configuring approved trust anchors.\n\nPrivate PKI requires ownership. Someone must decide which private roots are trusted, where trust stores are configured, who can issue certificates, how names are assigned, how private keys are protected, how certificates are rotated, and how incidents are handled. Adding a private root to a Python client or runtime should be an intentional environment decision, not a convenience copied from one developer machine to production.\n\nSelf-signed and development certificates should be scoped to their purpose. Local development may need a certificate that is not publicly trusted. Test environments may use private roots. Production should have a clear trust model and avoid inheriting local exceptions. Development convenience should not train teams to ignore certificate warnings or to treat validation failure as a routine obstacle to bypass.\n\nCertificate pinning can narrow trust in some designs by restricting acceptance to a specific certificate or key identity, but it carries operational tradeoffs. Rotation, emergency replacement, platform migration, or certificate renewal can break clients if pins are not managed carefully. Pinning should be used with a lifecycle plan, not as a quick substitute for understanding trust stores and hostname validation.",
  "narrationPoints": [
    "Publicly trusted certificates work well for many internet-facing services, but internal systems often use private certificate authorities, privately issued certificates, or self-signed development certificates.",
    "Local development may need a certificate that is not publicly trusted.",
    "Development convenience should not train teams to ignore certificate warnings or to treat validation failure as a routine obstacle to bypass.",
    "Certificate pinning can narrow trust in some designs by restricting acceptance to a specific certificate or key identity, but it carries operational tradeoffs.",
    "Pinning should be used with a lifecycle plan, not as a quick substitute for understanding trust stores and hostname validation.",
    "Test environments may use private roots."
  ]
};
