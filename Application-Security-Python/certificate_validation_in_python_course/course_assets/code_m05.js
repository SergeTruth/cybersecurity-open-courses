window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Certificate Validation in Python Clients with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Use one SSL context in Requests",
      "language": "python",
      "blurb": "For Requests 2.32.3 or newer, callers select an application-owned service identifier; a private session validates the approved HTTPS authority at every redirect hop and rejects plaintext, credentials, custom CA, client-certificate, and proxy paths.",
      "code": "import ssl\nfrom types import MappingProxyType\nfrom urllib.parse import urljoin, urlsplit\nimport requests\nfrom requests.adapters import BaseAdapter, HTTPAdapter\n\nMINIMUM_REQUESTS_RELEASE = (2, 32, 3)\n\nclass RejectPlaintextAdapter(BaseAdapter):\n    def send(self, request, **kwargs):\n        raise ValueError(\"plaintext HTTP is disabled\")\n\n    def close(self):\n        pass\n\nclass TLSAdapter(HTTPAdapter):\n    def __init__(self, context: ssl.SSLContext):\n        if not context.check_hostname or context.verify_mode != ssl.CERT_REQUIRED:\n            raise ValueError(\"validating SSL context required\")\n        self._context = context\n        super().__init__()\n\n    def init_poolmanager(self, *args, **kwargs):\n        kwargs[\"ssl_context\"] = self._context\n        return super().init_poolmanager(*args, **kwargs)\n\n    def build_connection_pool_key_attributes(self, request, verify, cert=None):\n        if verify is not True or cert is not None:\n            raise ValueError(\"per-request TLS overrides are disabled\")\n        host, pool = super().build_connection_pool_key_attributes(request, verify, cert)\n        pool.update(ssl_context=self._context, cert_reqs=\"CERT_REQUIRED\")\n        pool.pop(\"ca_certs\", None)\n        pool.pop(\"ca_cert_dir\", None)\n        return host, pool\n\n    def cert_verify(self, connection, url, verify, cert):\n        if verify is not True or cert is not None:\n            raise ValueError(\"per-request TLS overrides are disabled\")\n\n    def proxy_manager_for(self, proxy, **proxy_kwargs):\n        raise ValueError(\"proxies require a separately reviewed TLS adapter\")\n\ndef _require_supported_requests() -> None:\n    try:\n        release = tuple(\n            int(part) for part in requests.__version__.split(\".\")[:3]\n        )\n    except (AttributeError, ValueError) as error:\n        raise RuntimeError(\"a maintained Requests release is required\") from error\n    if len(release) != 3 or release < MINIMUM_REQUESTS_RELEASE:\n        raise RuntimeError(\"Requests 2.32.3 or newer is required\")\n\ndef _require_approved_destination(\n    url: str, authorities: frozenset[tuple[str, int]]\n) -> None:\n    target = urlsplit(url)\n    try:\n        port = target.port or 443\n    except ValueError as error:\n        raise ValueError(\"HTTPS destination rejected\") from error\n    if (\n        target.scheme.lower() != \"https\"\n        or target.hostname is None\n        or target.username is not None\n        or target.password is not None\n        or (target.hostname.lower(), port) not in authorities\n    ):\n        raise ValueError(\"HTTPS destination rejected\")\n\ndef _verified_get_factory(services):\n    approved = MappingProxyType({\n        service: (url, frozenset(authorities))\n        for service, (url, authorities) in services.items()\n    })\n\n    def verified_get(service_id: str) -> requests.Response:\n        if not isinstance(service_id, str):\n            raise ValueError(\"approved service identifier required\")\n        try:\n            url, authorities = approved[service_id]\n        except KeyError as error:\n            raise ValueError(\"approved service identifier required\") from error\n        _require_supported_requests()\n        context = ssl.create_default_context()\n        context.minimum_version = ssl.TLSVersion.TLSv1_2\n        with requests.Session() as session:\n            session.trust_env = False\n            session.mount(\"http://\", RejectPlaintextAdapter())\n            session.mount(\"https://\", TLSAdapter(context))\n            for _ in range(4):\n                _require_approved_destination(url, authorities)\n                response = session.get(\n                    url, allow_redirects=False, timeout=(3, 10),\n                    verify=True, stream=False\n                )\n                try:\n                    is_redirect = response.is_redirect\n                    if type(is_redirect) is not bool:\n                        raise ValueError(\"redirect response evidence rejected\")\n                    if not is_redirect:\n                        return response\n                    next_url = urljoin(url, response.headers[\"Location\"])\n                    _require_approved_destination(next_url, authorities)\n                except BaseException as error:\n                    try:\n                        response.close()\n                    except BaseException as cleanup_error:\n                        raise BaseExceptionGroup(\n                            \"redirect validation and response cleanup failed\",\n                            [error, cleanup_error],\n                        )\n                    raise\n                try:\n                    response.close()\n                except BaseException as cleanup_error:\n                    raise RuntimeError(\n                        \"redirect response cleanup failed\"\n                    ) from cleanup_error\n                url = next_url\n        raise RuntimeError(\"redirect limit exceeded\")\n\n    return verified_get\n\nverified_get = _verified_get_factory({\n    \"partner-api\": (\n        \"https://api.example.com/v1/status\",\n        {\n            (\"api.example.com\", 443),\n            (\"api-backup.example.com\", 443),\n        },\n    ),\n})\ndel _verified_get_factory\n"
    },
    {
      "title": "Require full validation in a database driver",
      "language": "python",
      "blurb": "Application-owned values pin the database hostname, network address, port, and CA path; password authentication explicitly disables ambient libpq client certificates.",
      "code": "from typing import Final\nimport psycopg\n\nINVENTORY_HOST: Final = \"db.example.test\"\nINVENTORY_HOST_ADDRESS: Final = \"192.0.2.10\"\nINVENTORY_PORT: Final = 5432\nINVENTORY_ROOT_CA: Final = \"/run/secrets/inventory-root.pem\"\n\ndef connect_to_inventory(database: str, user: str, password: str):\n    if (\n        not isinstance(database, str)\n        or not database\n        or not isinstance(user, str)\n        or not user\n        or not isinstance(password, str)\n    ):\n        raise ValueError(\"database credentials are invalid\")\n    return psycopg.connect(\n        dbname=database,\n        user=user,\n        password=password,\n        host=INVENTORY_HOST,\n        hostaddr=INVENTORY_HOST_ADDRESS,\n        port=INVENTORY_PORT,\n        sslmode=\"verify-full\",\n        sslrootcert=INVENTORY_ROOT_CA,\n        sslcertmode=\"disable\",\n        gssencmode=\"disable\",\n        connect_timeout=5,\n    )\n"
    }
  ]
};
