window.COURSE_MODULE = {
  "title": "Certificates, Chains, and Trust Stores",
  "graphicAlt": "Flowchart for Certificates, Chains, and Trust Stores, showing Python client, TLS endpoint, certificate chain, hostname, and trust store; labeled arrows identify accepted and rejected validation paths and the point where unsafe input or behavior is rejected.",
  "narration": "A certificate is a statement that binds a name or identity to a public key under a trust model. The matching private key is held by the service or subject that owns that identity. When a Python client connects to a remote service, the service presents a certificate. The client then needs to decide whether the certificate is valid for the service being contacted and for the purpose of the connection.\n\nCertificate authorities make that decision scalable. A root certificate represents a trust anchor. Intermediate certificates connect end-entity certificates back to that root. The certificate chain is the sequence that lets a client determine whether an endpoint certificate is linked to a trusted authority. The trust store is the set of roots or trust anchors the client accepts during validation. If the chain does not lead to a trusted anchor, the client should not treat the endpoint as trusted by default.\n\nNames and purposes matter. Certificates may include subject names and subject alternative names that identify the service or identity at a high level. Certificates may also include intended purposes or key usage concepts that describe what the certificate is meant to support. Expiration dates define the window in which the certificate is expected to be valid. Trust depends on the chain, the name, the purpose, and timing, not simply on the existence of a certificate file.\n\nPrivate key exposure can undermine the identity represented by a certificate. If an attacker or unauthorized party obtains the private key, they may be able to impersonate or sign for that identity depending on the system. Private keys should not appear in repositories, logs, support bundles, broadly shared file systems, container images, or casual documentation. Certificate validation relies on the assumption that the private key remains private.",
  "narrationPoints": [
    "A certificate is a statement that binds a name or identity to a public key under a trust model.",
    "The client then needs to decide whether the certificate is valid for the service being contacted and for the purpose of the connection.",
    "If the chain does not lead to a trusted anchor, the client should not treat the endpoint as trusted by default.",
    "Expiration dates define the window in which the certificate is expected to be valid.",
    "Certificate validation relies on the assumption that the private key remains private.",
    "Names and purposes matter."
  ]
};
