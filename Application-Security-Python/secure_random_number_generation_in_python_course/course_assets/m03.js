window.COURSE_MODULE = {
  "title": "Python Randomness APIs and Safe Selection",
  "graphicAlt": "Python API cards route secrets, SystemRandom, os.urandom, and randbelow to approved uses, while random.seed and time-based values point to non-security simulation tasks.",
  "narration": "Python provides different randomness interfaces for different purposes. The general random module is useful for simulations, sampling, games, shuffling, non-security choices, and repeatable tests. It is not intended for passwords, tokens, keys, reset links, session identifiers, or other secrets. Using it in security-sensitive code creates risk because its design goals are not the same as cryptographic unpredictability.\n\nThe secrets module is a high-level interface intended for security-sensitive random values. It helps developers generate tokens, choose from character sets, and work with randomness suitable for secrets. System randomness concepts, including os.urandom and SystemRandom at a high level, rely on operating system facilities designed for security use. The exact implementation details can change, but the selection principle remains stable: choose the API based on the purpose of the value.\n\nSecurity-sensitive generation should be centralized where practical. If every route or utility function chooses its own randomness method, mistakes become hard to find. A shared helper can express the approved pattern for reset tokens, invitation links, API keys, and internal identifiers. Centralization also makes it easier to adjust length, encoding, storage, expiration, and logging behavior when requirements change.\n\nCode review should treat randomness API choice as a security decision. Reviewers should ask whether the value grants access, proves identity, protects cryptography, or controls a sensitive workflow. If yes, non-security random APIs, predictable seeds, counters, timestamps, and custom string mixing should be treated as concerns. Safe selection is not about memorizing every function; it is about matching the API to the risk.",
  "narrationPoints": [
    "The general random module is useful for simulations, sampling, games, shuffling, non-security choices, and repeatable tests.",
    "The secrets module is a high-level interface intended for security-sensitive random values.",
    "Centralization also makes it easier to adjust length, encoding, storage, expiration, and logging behavior when requirements change.",
    "Reviewers should ask whether the value grants access, proves identity, protects cryptography, or controls a sensitive workflow.",
    "Safe selection is not about memorizing every function; it is about matching the API to the risk.",
    "Using it in security-sensitive code creates risk because its design goals are not the same as cryptographic unpredictability."
  ]
};
