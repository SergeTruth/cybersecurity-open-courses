window.COURSE_MODULE = {
  "title": "Bias, Encoding, and Length Decisions",
  "graphicAlt": "A modulo operation creates uneven buckets from random byte values, while rejection sampling through randbelow distributes selections evenly across a seven-value range.",
  "narration": "Random value length should be selected intentionally. Longer values can increase guessing resistance and reduce collision risk, but they may affect storage, URLs, human handling, and support workflows. Shorter values may be easier for users to type, but they usually need additional controls such as expiration, throttling, attempt limits, monitoring, and narrow purpose. Copying an example length without understanding the use case is a common mistake.\n\nEncoding changes how random bytes are represented. Hex, base64-style encodings, URL-safe encodings, and human-friendly character sets all have tradeoffs. Some encodings are compact. Some are easier to paste into URLs. Some avoid ambiguous characters. Encoding is not the source of security by itself. The underlying unpredictability and the number of possible values matter more than whether the output looks complicated.\n\nModulo bias at a high level occurs when a method maps random values into a range in a way that makes some outputs more likely than others. This can happen when developers take a random number and reduce it with a simple remainder operation without considering the distribution. The safer approach is to use library functions designed to choose uniformly from a range or character set, especially for security-sensitive values.\n\nPredictable prefixes, truncation mistakes, and fixed patterns can reduce effective strength or leak information about purpose, environment, tenant, or generation time. Prefixes may be useful for operations, but they should not be counted as secret entropy. If a token includes a readable label, the random part still needs enough unpredictability for the risk. Design the format deliberately, then test lifecycle behavior rather than trying to test randomness quality directly.",
  "narrationPoints": [
    "Copying an example length without understanding the use case is a common mistake.",
    "The underlying unpredictability and the number of possible values matter more than whether the output looks complicated.",
    "The safer approach is to use library functions designed to choose uniformly from a range or character set, especially for security-sensitive values.",
    "Design the format deliberately, then test lifecycle behavior rather than trying to test randomness quality directly.",
    "Prefixes may be useful for operations, but they should not be counted as secret entropy.",
    "Hex, base64-style encodings, URL-safe encodings, and human-friendly character sets all have tradeoffs."
  ]
};
