window.COURSE_MODULE = {
  "title": "Review, Logging, Monitoring, and Incident Response",
  "graphicAlt": "A token-issuance dashboard records purpose, entropy class, outcome, and collision retry count; raw tokens, digests, nonces, salts, and keys are absent from every event.",
  "narration": "Code review should identify every place the application generates security-sensitive values. Reviewers should ask what the value is used for, whether it grants access, which API generates it, how much unpredictability is needed, how it is encoded, where it is stored, how long it lives, and whether it is logged. Use of non-secure random APIs for tokens, keys, session identifiers, or reset links should be treated as a high-priority concern.\n\nStatic analysis at a high level can help find suspicious calls, but it does not replace design review. A scanner may find use of a non-security random function, but it may not understand whether a particular identifier is security-sensitive. Unit tests can verify behavior, length, encoding, expiration, uniqueness expectations in ordinary operation, and failure handling. They should not attempt to prove the actual quality of cryptographic randomness through fragile statistical tests.\n\nLogs should avoid generated secrets, reset links, session identifiers, API keys, cryptographic keys, and full bearer tokens. Logging the first few characters of a secret may still create risk depending on the token format and operational context. Redaction should be deliberate and tested. Monitoring should focus on suspicious token use, repeated failed attempts, unexpected reset activity, duplicate generation signals, and use after revocation.\n\nWhen predictable or exposed random values are discovered, the fix is often more than changing code. Existing tokens may need revocation, keys may need rotation, sessions may need invalidation, reset links may need expiration, and affected systems may need review. Incident response should assess scope, preserve evidence, communicate with owners, and prevent recurrence. Randomness-related bugs often require both remediation in code and cleanup of values already issued.",
  "narrationPoints": [
    "Use of non-secure random APIs for tokens, keys, session identifiers, or reset links should be treated as a high-priority concern.",
    "Unit tests can verify behavior, length, encoding, expiration, uniqueness expectations in ordinary operation, and failure handling.",
    "Logs should avoid generated secrets, reset links, session identifiers, API keys, cryptographic keys, and full bearer tokens.",
    "Randomness-related bugs often require both remediation in code and cleanup of values already issued.",
    "Static analysis at a high level can help find suspicious calls, but it does not replace design review.",
    "Incident response should assess scope, preserve evidence, communicate with owners, and prevent recurrence."
  ]
};
