window.COURSE_MODULE = {
  "title": "Local Development and .env File Safety",
  "graphicAlt": "Decision diagram for Local Development and .env File Safety, showing developer shell, Python process, deployment platform, child process, and secret store; labeled arrows identify configuration flow and exposure paths and the point where unsafe input or behavior is rejected.",
  "narration": "Local development needs convenience, but convenience should not normalize unsafe secret handling. Many Python projects use .env files or similar local configuration files to make development predictable. That pattern can be reasonable when values are clearly local, access is limited, and real secrets are kept out of source control. It becomes risky when developers copy production credentials, share files through chat, or commit active secrets to repositories.\n\nSample environment files should contain example names, placeholders, documentation, and safe dummy values rather than active credentials. They help developers understand what variables exist without exposing real access. Repository ignore patterns can reduce accidental commits, but they are not enough by themselves. Teams should also use secrets scanning, review habits, onboarding guidance, and clear procedures for requesting local development credentials.\n\nDevelopment secrets should not grant production access. A local database password, test API token, or sandbox key should have limited scope, limited data, and clear ownership. Developer machines and shared workstations are different trust environments from production systems. If a laptop backup, screen share, shell history, notebook, or local log captures a development value, the blast radius should be small.\n\nEnvironment drift is another risk. Local settings can hide bugs when they differ too much from staging and production, while copied production settings can create excessive risk. The better pattern is documented, safe, reproducible local configuration that resembles production behavior without reusing production secrets. Local convenience should support secure engineering habits, not train teams to treat secrets as disposable strings.",
  "narrationPoints": [
    "Local development needs convenience, but convenience should not normalize unsafe secret handling.",
    "Sample environment files should contain example names, placeholders, documentation, and safe dummy values rather than active credentials.",
    "A local database password, test API token, or sandbox key should have limited scope, limited data, and clear ownership.",
    "If a laptop backup, screen share, shell history, notebook, or local log captures a development value, the blast radius should be small.",
    "Local convenience should support secure engineering habits, not train teams to treat secrets as disposable strings.",
    "Environment drift is another risk."
  ]
};
