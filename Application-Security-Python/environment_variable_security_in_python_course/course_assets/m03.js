window.COURSE_MODULE = {
  "title": "Loading and Validating Environment Variables",
  "graphicAlt": "Layered architecture diagram for Loading and Validating Environment Variables, showing developer shell, Python process, deployment platform, child process, and secret store; labeled arrows identify configuration flow and exposure paths and the point where unsafe input or behavior is rejected.",
  "narration": "Environment variables should usually be read and validated during application startup, before the application begins accepting work. Startup validation lets the service fail early when a required value is missing, malformed, out of range, or inconsistent with the deployment environment. This is much safer than discovering the problem halfway through a request, background job, or security-sensitive operation.\n\nValidation should include required variables, optional variables, default values, type conversion, boolean parsing, numeric limits, allowed values, and relationships between settings. A boolean should not become true merely because a string is nonempty. A timeout should not accept a negative value. A deployment environment should be one of the approved names. A security-critical setting should not quietly fall back to a development behavior.\n\nConfiguration schema concepts help teams make validation consistent. A schema can describe each variable, its purpose, type, sensitivity, allowed values, default behavior, owner, and whether it can be logged. Even a simple internal convention is better than scattered reads from the environment throughout the codebase. Centralized loading makes review easier and reduces accidental drift between modules.\n\nFail-fast behavior is especially important for authentication, authorization, TLS validation, logging, encryption, token validation, and debug behavior. If a required key or endpoint is missing, the application should not silently disable protection or switch to a broad permissive mode. Safe defaults should preserve security expectations. Missing or invalid configuration should produce controlled operational errors, not weakened runtime behavior.",
  "narrationPoints": [
    "Environment variables should usually be read and validated during application startup, before the application begins accepting work.",
    "A boolean should not become true merely because a string is nonempty.",
    "A security-critical setting should not quietly fall back to a development behavior.",
    "A schema can describe each variable, its purpose, type, sensitivity, allowed values, default behavior, owner, and whether it can be logged.",
    "Fail-fast behavior is especially important for authentication, authorization, TLS validation, logging, encryption, token validation, and debug behavior.",
    "Safe defaults should preserve security expectations."
  ]
};
