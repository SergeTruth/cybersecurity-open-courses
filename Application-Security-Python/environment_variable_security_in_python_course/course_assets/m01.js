window.COURSE_MODULE = {
  "title": "What Environment Variable Security Means",
  "graphicAlt": "Diagram for What Environment Variable Security Means, showing developer shell, Python process, deployment platform, child process, and secret store; labeled arrows identify configuration flow and exposure paths and the point where unsafe input or behavior is rejected.",
  "narration": "Environment variables are runtime configuration values available to a Python process. They are common in Flask, Django, FastAPI, command-line tools, scripts, background jobs, data pipelines, notebooks, containers, serverless functions, and CI/CD jobs. They can select an environment, point to a service, enable a feature, set a timeout, or provide a credential. Because they shape application behavior at runtime, they should be treated as part of the application trust boundary.\n\nNot every environment variable has the same sensitivity. Some values are ordinary configuration, such as a service name, log level, region, or feature flag. Some are public identifiers. Others are secrets, such as API keys, database passwords, signing keys, tokens, private key references, and sensitive connection strings. A secure design distinguishes those categories instead of treating every value as harmless text.\n\nEnvironment variables are convenient because they keep configuration outside source code and support different deployments from the same application artifact. Convenience does not make them automatically safe. Depending on the platform, environment values can appear in process environments, debug pages, crash reports, logs, CI output, container metadata, notebooks, support bundles, child processes, or screenshots. The exposure path changes, but the principle remains: sensitive values need protection.\n\nEnvironment variable security includes loading, validation, exposure control, logging hygiene, child-process inheritance, rotation, ownership, and lifecycle management. The goal is not to avoid environment variables entirely. The goal is to use them deliberately, minimize secrets, validate critical values, avoid unsafe defaults, and make configuration predictable, reviewable, and recoverable across development, deployment, and operations.",
  "narrationPoints": [
    "Environment variables are runtime configuration values available to a Python process.",
    "Not every environment variable has the same sensitivity.",
    "Others are secrets, such as API keys, database passwords, signing keys, tokens, private key references, and sensitive connection strings.",
    "The exposure path changes, but the principle remains: sensitive values need protection.",
    "The goal is not to avoid environment variables entirely.",
    "Convenience does not make them automatically safe."
  ]
};
