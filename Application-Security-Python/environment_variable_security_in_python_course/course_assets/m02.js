window.COURSE_MODULE = {
  "title": "Configuration, Secrets, and Trust Boundaries",
  "graphicAlt": "Flowchart for Configuration, Secrets, and Trust Boundaries, showing developer shell, Python process, deployment platform, child process, and secret store; labeled arrows identify configuration flow and exposure paths and the point where unsafe input or behavior is rejected.",
  "narration": "A Python application should know which environment variables are required, optional, sensitive, and dangerous to trust blindly. Non-secret configuration might identify the deployment environment, choose a region, set a timeout, or enable a controlled feature. Sensitive configuration may include credentials, tokens, signing material, database access details, or private key references. Mixing these categories makes it harder to apply the right access controls, logging rules, and rotation procedures.\n\nTrust boundaries depend on who controls the value. Operator-controlled variables may be set by deployment tooling, secret stores, infrastructure configuration, or CI/CD systems. User-controlled values may flow indirectly through job parameters, notebooks, generated configuration, or stored records. A value that arrives in an environment variable is not automatically trustworthy. Teams should understand the source, precedence, and approval path for each important setting.\n\nConfiguration sources and precedence can become security-relevant. A local file, process environment, deployment platform, secret store, command-line option, or framework default may all compete to define a value. If precedence is unclear, a development setting can override a production expectation, or a missing secret can silently fall back to an unsafe default. Configuration behavior should be visible enough that reviewers can reason about it.\n\nSecret values should be minimized and separated from ordinary configuration. A secret manager or managed platform secret feature may be appropriate for sensitive values, while environment variables may carry references or short-lived injected values depending on the architecture. The stable principle is to reduce how many places secrets live, limit who can set or read them, and make it easy to rotate them without guessing which systems depend on them.",
  "narrationPoints": [
    "A Python application should know which environment variables are required, optional, sensitive, and dangerous to trust blindly.",
    "Mixing these categories makes it harder to apply the right access controls, logging rules, and rotation procedures.",
    "Operator-controlled variables may be set by deployment tooling, secret stores, infrastructure configuration, or CI/CD systems.",
    "Teams should understand the source, precedence, and approval path for each important setting.",
    "If precedence is unclear, a development setting can override a production expectation, or a missing secret can silently fall back to an unsafe default.",
    "Configuration sources and precedence can become security-relevant."
  ]
};
