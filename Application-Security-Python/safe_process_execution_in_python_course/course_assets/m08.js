window.COURSE_MODULE = {
  "title": "Output Handling, Logging, Testing, and Monitoring",
  "graphicAlt": "Operational lifecycle diagram for Output Handling, Logging, Testing, and Monitoring, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "Standard output and standard error are data streams that may contain useful results, diagnostic details, sensitive information, or unexpectedly large content. Applications should decide whether output is captured, streamed, limited, stored, displayed, or discarded. A child process may include internal paths, input fragments, tokens, configuration details, or downstream error messages in output. Safe handling treats output as potentially sensitive until it is classified.\n\nExit codes and errors should be translated into safe application behavior. A nonzero exit may mean invalid input, dependency failure, timeout, resource exhaustion, or an internal problem. Users and clients usually need a safe message and possibly a correlation ID, not a raw diagnostic dump. Operators need structured logs that identify the approved operation, outcome, component, duration, and safe reason code. High-impact execution may also require audit events.\n\nTesting should cover allowed and rejected paths. Tests can verify that only approved operations run, untrusted values do not control command structure, invalid paths are rejected, timeouts work, output is limited, secrets are not logged, authorization happens before execution, and error paths fail safely. Code review should ask what input can influence the executable, arguments, environment, working directory, files, privileges, and output handling.\n\nMonitoring should watch for unusual process activity without exposing secrets. Useful signals include repeated rejected attempts, unexpected failures, long-running jobs, timeouts, unusual volumes of output, new operation types, high concurrency, and execution from unexpected components. If unsafe execution behavior or leaked output is discovered, teams should contain the workflow, rotate exposed secrets if needed, review logs, fix the design, and add regression tests.",
  "narrationPoints": [
    "Standard output and standard error are data streams that may contain useful results, diagnostic details, sensitive information, or unexpectedly large content.",
    "A child process may include internal paths, input fragments, tokens, configuration details, or downstream error messages in output.",
    "Exit codes and errors should be translated into safe application behavior.",
    "Operators need structured logs that identify the approved operation, outcome, component, duration, and safe reason code.",
    "Monitoring should watch for unusual process activity without exposing secrets.",
    "Testing should cover allowed and rejected paths."
  ]
};
