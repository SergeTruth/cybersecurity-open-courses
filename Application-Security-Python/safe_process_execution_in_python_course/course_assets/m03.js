window.COURSE_MODULE = {
  "title": "Avoiding Shell Interpretation",
  "graphicAlt": "Layered architecture diagram for Avoiding Shell Interpretation, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "Shell interpretation is a major source of process execution risk because a shell treats text as instructions, not just as data. Shell features such as pipelines, expansion, redirection, pattern matching, variables, quoting, and control syntax are useful for interactive administration, but they can become dangerous when application data is mixed into command text. The safest default is to avoid shell-mediated execution unless the feature is truly required and carefully constrained.\n\nDirect process execution separates the executable from its arguments. In that model, the application identifies the program to run and passes each argument as a structured value. This makes it harder for an ordinary input value to become command structure. It also makes review clearer because the code shows which program runs and which values are data. String-built command lines do the opposite: they blur the line between command syntax and application input.\n\nMany shell conveniences have safer alternatives in Python or platform APIs. Instead of relying on shell pipelines, the application may connect library calls or process streams deliberately. Instead of relying on globbing, it may enumerate files with path APIs and apply allowlists. Instead of using variable expansion or redirection, it may pass explicit values and file handles. The exact implementation can vary, but the principle is stable: use structured APIs when data might be untrusted.\n\nWhen shell behavior is unavoidable, the execution path should receive extra scrutiny. The operation should be narrowly scoped, inputs should be mapped to approved choices, the shell environment should be minimized, and sensitive values should not be placed into command text. Reviewers should ask why the shell is needed, what input can influence it, what privileges it has, and how failure is handled. Shell convenience should never be accepted as a substitute for safe design.",
  "narrationPoints": [
    "Shell interpretation is a major source of process execution risk because a shell treats text as instructions, not just as data.",
    "Direct process execution separates the executable from its arguments.",
    "Many shell conveniences have safer alternatives in Python or platform APIs.",
    "Instead of using variable expansion or redirection, it may pass explicit values and file handles.",
    "When shell behavior is unavoidable, the execution path should receive extra scrutiny.",
    "Shell convenience should never be accepted as a substitute for safe design."
  ]
};
