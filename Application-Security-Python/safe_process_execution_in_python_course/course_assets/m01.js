window.COURSE_MODULE = {
  "title": "What Safe Process Execution Means",
  "graphicAlt": "Diagram for What Safe Process Execution Means, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "Safe process execution means launching external programs from Python only when there is a clear reason, and doing it in a way that is constrained, reviewable, and recoverable. Python applications often start child processes from web applications, APIs, command-line tools, automation scripts, data pipelines, background workers, scheduled jobs, build tools, and internal administrative utilities. The pattern is powerful, but it reaches outside normal Python control and asks the operating system to run something else.\n\nProcess execution is different from calling a Python library, using a system API, or scheduling a background job inside an application framework. A library call usually stays inside the process and follows the runtime permissions of the application. A child process may interpret arguments differently, inherit environment variables, read and write files, use network access, produce output, or continue running after the parent has moved on. Those differences create a security boundary that deserves design attention.\n\nThe risks affect confidentiality, integrity, availability, and auditability. A child process may receive secrets it does not need, read files outside the intended workspace, write output to an unsafe location, run too long, consume too many resources, or expose internal details through logs and errors. If untrusted input influences command structure, file paths, environment variables, or options, the application can become an unsafe interface to operating system behavior.\n\nThe safest design is often to avoid external process execution entirely when a trusted library or platform feature can perform the work directly. When a process is genuinely needed, the design should be explicit: choose the executable from trusted configuration, pass arguments as structured values, avoid shell interpretation, validate inputs for the approved operation, control the environment, limit privileges, set timeouts, handle output carefully, and log enough to investigate without exposing sensitive data.",
  "narrationPoints": [
    "The pattern is powerful, but it reaches outside normal Python control and asks the operating system to run something else.",
    "Process execution is different from calling a Python library, using a system API, or scheduling a background job inside an application framework.",
    "A library call usually stays inside the process and follows the runtime permissions of the application.",
    "The risks affect confidentiality, integrity, availability, and auditability.",
    "If untrusted input influences command structure, file paths, environment variables, or options, the application can become an unsafe interface to operating system behavior.",
    "The safest design is often to avoid external process execution entirely when a trusted library or platform feature can perform the work directly."
  ]
};
