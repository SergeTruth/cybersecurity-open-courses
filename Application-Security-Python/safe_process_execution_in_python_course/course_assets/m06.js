window.COURSE_MODULE = {
  "title": "Environment Variables, Secrets, and Credentials",
  "graphicAlt": "Boundary map for Environment Variables, Secrets, and Credentials, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "Child processes often inherit environment variables from the parent process unless the application deliberately controls that behavior. This can expose credentials, tokens, connection strings, proxy settings, path variables, debug flags, cloud configuration, or feature flags to code that does not need them. Even when the child process is trusted, minimizing inherited environment reduces blast radius and makes behavior easier to review.\n\nA safer pattern is explicit environment construction. The application decides which variables the child process needs for the approved task and passes only those values. If a secret is required, the design should consider whether it can be provided through a safer mechanism than a broad inherited environment or a command argument. Secrets should not appear in logs, user-facing errors, process listings, shell history, crash output, or monitoring records.\n\nArguments are not always a safe place for secrets. Depending on the platform and tooling, command arguments may be visible in diagnostics, process inspection, crash reports, job metadata, audit logs, or operational dashboards. Environment variables can have similar exposure risks. The stable principle is to provide the least secret material necessary, through the narrowest appropriate channel, for the shortest practical time.\n\nPath-related environment values also matter. Variables that influence executable lookup, library loading, configuration discovery, proxy behavior, or temporary locations can change what a child process runs or reaches. The application should avoid letting untrusted input shape those variables. Safe process execution includes controlling the environment as part of the command contract, not treating it as invisible background state.",
  "narrationPoints": [
    "Child processes often inherit environment variables from the parent process unless the application deliberately controls that behavior.",
    "The application decides which variables the child process needs for the approved task and passes only those values.",
    "Depending on the platform and tooling, command arguments may be visible in diagnostics, process inspection, crash reports, job metadata, audit logs, or operational dashboards.",
    "Variables that influence executable lookup, library loading, configuration discovery, proxy behavior, or temporary locations can change what a child process runs or reaches.",
    "A safer pattern is explicit environment construction.",
    "Path-related environment values also matter."
  ]
};
