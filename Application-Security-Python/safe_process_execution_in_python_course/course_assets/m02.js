window.COURSE_MODULE = {
  "title": "Risk Model and Trust Boundaries",
  "graphicAlt": "Flowchart for Risk Model and Trust Boundaries, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "A safe design starts by identifying who or what can influence each part of a process execution request. User-controlled input may appear as form fields, API parameters, file names, uploaded files, task identifiers, database-stored parameters, configuration values, queue messages, scheduled job inputs, or third-party data. Some values look internal because they came from a database or queue, but they may have originated from a user or external system earlier in the workflow.\n\nDifferent pieces of an execution request have different sensitivity. The executable name or path decides what program runs. Arguments decide what the program does. The working directory can change which files are visible by relative reference. Environment variables can change behavior or expose secrets. Input files and output locations can affect confidentiality and integrity. Timeouts and resource limits affect availability. Logs and error handling affect auditability.\n\nUntrusted data should not control executable names, shell syntax, command structure, privileged options, or sensitive target locations. The application should map a user intent to a small set of internal operations that are already approved. For example, a user may choose a report format or processing mode, while the code maps that choice to known safe behavior. The user should not be writing the command line through an application field.\n\nTrust-boundary documentation makes review and incident response easier. A reviewer should be able to see which inputs are trusted, which are validated, which are constrained by allowlists, which files may be touched, which account runs the process, which environment variables are passed, and which outputs are captured. Without that map, process execution tends to grow through convenience until nobody knows which parts are user-influenced.",
  "narrationPoints": [
    "A safe design starts by identifying who or what can influence each part of a process execution request.",
    "The working directory can change which files are visible by relative reference.",
    "Input files and output locations can affect confidentiality and integrity.",
    "The user should not be writing the command line through an application field.",
    "Without that map, process execution tends to grow through convenience until nobody knows which parts are user-influenced.",
    "Logs and error handling affect auditability."
  ]
};
