window.COURSE_MODULE = {
  "title": "Least Privilege, Isolation, and Resource Controls",
  "graphicAlt": "Control-flow diagram for Least Privilege, Isolation, and Resource Controls, showing validated operation, fixed executable, argument vector, minimal environment, bounded child process, and captured result; labeled arrows identify process launch and termination controls and the point where unsafe input or behavior is rejected.",
  "narration": "Least privilege means the child process runs with only the permissions needed for its approved purpose. It should not run as an administrator or root-equivalent account simply because that is convenient. A background converter, report generator, build helper, or automation task usually does not need broad filesystem access, broad network access, user management permissions, or access to unrelated secrets. The execution identity should match the operation, not the most powerful account available.\n\nIsolation can reduce damage if a process behaves unexpectedly. At a high level, isolation may involve service accounts, workload identity, containers, sandboxing, read-only filesystems, restricted volumes, limited network access, or separate worker environments. These controls vary by platform, but the goal is durable: limit what the child process can see, change, contact, and persist. Isolation should be reviewed along with the application code that starts the process.\n\nResource controls protect availability. A child process that runs forever, produces unlimited output, opens too many files, consumes too much CPU or memory, or starts too many concurrent jobs can affect the parent application and shared infrastructure. Timeouts, output size limits, concurrency controls, queue limits, cancellation behavior, and process termination rules should be part of the design. Reliability settings are security-relevant when they prevent denial-of-service conditions.\n\nTermination and cleanup need clear behavior. If a process times out, the application should know whether child work stopped, whether temporary files remain, whether partial output is safe to discard, and whether the operation can be retried. If the process is connected to a business transaction, the application should avoid treating ambiguous failure as success. Safe execution controls not only what starts, but also how it stops.",
  "narrationPoints": [
    "Least privilege means the child process runs with only the permissions needed for its approved purpose.",
    "Isolation can reduce damage if a process behaves unexpectedly.",
    "Isolation should be reviewed along with the application code that starts the process.",
    "Reliability settings are security-relevant when they prevent denial-of-service conditions.",
    "Safe execution controls not only what starts, but also how it stops.",
    "Termination and cleanup need clear behavior."
  ]
};
