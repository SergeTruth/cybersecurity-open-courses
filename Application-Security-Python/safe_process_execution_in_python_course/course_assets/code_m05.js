window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply File Paths, Working Directories, and Temporary Files with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Inspect a document with bounded child output",
      "language": "python",
      "blurb": "The child receives bounded input, and its standard output is streamed under a hard byte ceiling with cleanup that preserves kill, wait, and close failures.",
      "code": "import subprocess\nfrom tempfile import TemporaryFile\nfrom threading import Thread\n\nMAX_INSPECTION_INPUT_BYTES = 5_000_000\nMAX_INSPECTION_OUTPUT_BYTES = 10_000_000\n\ndef kill_child(process: subprocess.Popen) -> OSError | None:\n    try:\n        process.kill()\n    except ProcessLookupError:\n        return None\n    except OSError as error:\n        return error\n    return None\n\ndef inspect_document(content: bytes, maximum_output: int = 1_000_000) -> bytes:\n    if type(content) is not bytes or len(content) > MAX_INSPECTION_INPUT_BYTES:\n        raise ValueError(\"document exceeds inspection limit\")\n    if (\n        type(maximum_output) is not int\n        or not 0 <= maximum_output <= MAX_INSPECTION_OUTPUT_BYTES\n    ):\n        raise ValueError(\"inspector output limit rejected\")\n\n    with TemporaryFile() as input_file:\n        input_file.write(content)\n        input_file.seek(0)\n        process = subprocess.Popen(\n            [\"/usr/local/bin/document-inspector\", \"--input\", \"-\", \"--output\", \"json\"],\n            stdin=input_file,\n            stdout=subprocess.PIPE,\n            stderr=subprocess.DEVNULL,\n        )\n        output = bytearray()\n        reader_failures: list[Exception] = []\n        reader_cleanup_failures: list[Exception] = []\n\n        def read_output() -> None:\n            try:\n                if process.stdout is None:\n                    raise RuntimeError(\"inspector output pipe was not opened\")\n                while chunk := process.stdout.read(64 * 1024):\n                    if len(output) + len(chunk) > maximum_output:\n                        reader_failures.append(\n                            ValueError(\"inspector output exceeded its limit\")\n                        )\n                        kill_error = kill_child(process)\n                        if kill_error is not None:\n                            reader_cleanup_failures.append(kill_error)\n                        return\n                    output.extend(chunk)\n            except Exception as error:\n                reader_failures.append(\n                    RuntimeError(\"inspector output reader failed\")\n                )\n                reader_failures[-1].__cause__ = error\n                kill_error = kill_child(process)\n                if kill_error is not None:\n                    reader_cleanup_failures.append(kill_error)\n\n        reader = Thread(target=read_output, daemon=False)\n        reader.start()\n        primary_error: BaseException | None = None\n        result: bytes | None = None\n        try:\n            try:\n                return_code = process.wait(timeout=15)\n            except subprocess.TimeoutExpired:\n                primary_error = TimeoutError(\"document inspection exceeded its deadline\")\n            else:\n                reader.join(timeout=2)\n                if reader.is_alive():\n                    primary_error = RuntimeError(\n                        \"inspector output reader did not finish\"\n                    )\n                elif reader_failures:\n                    primary_error = reader_failures[0]\n                elif return_code != 0:\n                    primary_error = RuntimeError(\"document inspection failed\")\n                else:\n                    result = bytes(output)\n        except BaseException as error:\n            primary_error = error\n\n        cleanup_errors: list[Exception] = list(reader_cleanup_failures)\n        try:\n            running = process.poll() is None\n        except Exception as error:\n            cleanup_errors.append(error)\n            running = True\n        if running:\n            kill_error = kill_child(process)\n            if kill_error is not None:\n                cleanup_errors.append(kill_error)\n        try:\n            process.wait(timeout=2)\n        except Exception as error:\n            cleanup_errors.append(error)\n        reader.join(timeout=2)\n        if reader.is_alive():\n            cleanup_errors.append(\n                RuntimeError(\"inspector output reader remained active\")\n            )\n        if process.stdout is not None:\n            try:\n                process.stdout.close()\n            except Exception as error:\n                cleanup_errors.append(error)\n\n        if primary_error is not None and cleanup_errors:\n            raise BaseExceptionGroup(\n                \"document inspection and cleanup failed\",\n                [primary_error, *cleanup_errors],\n            )\n        if primary_error is not None:\n            raise primary_error\n        if cleanup_errors:\n            raise ExceptionGroup(\"document inspection cleanup failed\", cleanup_errors)\n        if result is None:\n            raise RuntimeError(\"document inspection produced no result\")\n        return result\n"
    },
    {
      "title": "Create and confine a quiet child workspace",
      "language": "python",
      "blurb": "The converter receives bounded input, emits no captured diagnostic stream, and the parent bounds the generated output file before reading it.",
      "code": "import os\nimport stat\nimport subprocess\nfrom pathlib import Path\nfrom tempfile import TemporaryDirectory\n\nMAX_THUMBNAIL_INPUT_BYTES = 10_000_000\nMAX_THUMBNAIL_OUTPUT_BYTES = 5_000_000\n\ndef read_thumbnail(workspace_fd: int, maximum_output: int) -> bytes:\n    if not hasattr(os, \"O_NOFOLLOW\"):\n        raise NotImplementedError(\"secure thumbnail opening requires O_NOFOLLOW\")\n    descriptor = os.open(\n        \"output.png\",\n        os.O_RDONLY\n        | os.O_NOFOLLOW\n        | getattr(os, \"O_CLOEXEC\", 0)\n        | getattr(os, \"O_NONBLOCK\", 0),\n        dir_fd=workspace_fd,\n    )\n    with os.fdopen(descriptor, \"rb\") as output:\n        info = os.fstat(output.fileno())\n        if (\n            not stat.S_ISREG(info.st_mode)\n            or not 0 <= info.st_size <= maximum_output\n        ):\n            raise ValueError(\"thumbnail output rejected\")\n        data = output.read(maximum_output + 1)\n    if len(data) > maximum_output:\n        raise ValueError(\"thumbnail grew beyond its limit\")\n    return data\n\ndef convert_thumbnail(image: bytes, maximum_output: int = 2_000_000) -> bytes:\n    if type(image) is not bytes or len(image) > MAX_THUMBNAIL_INPUT_BYTES:\n        raise ValueError(\"image input is too large\")\n    if (\n        type(maximum_output) is not int\n        or not 0 <= maximum_output <= MAX_THUMBNAIL_OUTPUT_BYTES\n    ):\n        raise ValueError(\"thumbnail output limit rejected\")\n    if os.name != \"posix\" or not hasattr(os, \"O_NOFOLLOW\"):\n        raise NotImplementedError(\"secure thumbnail conversion requires POSIX no-follow\")\n    with TemporaryDirectory(prefix=\"thumbnail-\") as directory:\n        workspace = Path(directory)\n        workspace_fd = os.open(\n            workspace,\n            os.O_RDONLY\n            | getattr(os, \"O_DIRECTORY\", 0)\n            | os.O_NOFOLLOW\n            | getattr(os, \"O_CLOEXEC\", 0),\n        )\n        try:\n            (workspace / \"input.png\").write_bytes(image)\n            subprocess.run(\n                [\"/usr/bin/convert\", \"input.png\", \"-resize\", \"256x256>\", \"output.png\"],\n                cwd=workspace,\n                env={\"PATH\": \"/usr/bin:/bin\", \"LANG\": \"C\"},\n                stdout=subprocess.DEVNULL,\n                stderr=subprocess.DEVNULL,\n                timeout=20,\n                check=True,\n            )\n            return read_thumbnail(workspace_fd, maximum_output)\n        finally:\n            os.close(workspace_fd)\n"
    }
  ]
};
