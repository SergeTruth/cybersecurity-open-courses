window.COURSE_MODULE = {
  "title": "Naming, Race Conditions, and Path Safety",
  "graphicAlt": "Exclusive random creation succeeds through mkstemp or open with O_EXCL and O_NOFOLLOW; a planted file, symlink, and check-then-open pathname lose the race and are rejected.",
  "narration": "Predictable temporary names create avoidable risk. If a name is based on a timestamp, process ID, user name, original upload name, or simple counter, another process may collide with it, infer sensitive context, or interfere with the workflow in shared environments. Generated unpredictable names from vetted APIs reduce that risk because the application is not inventing names through ad hoc string construction.\n\nAtomic creation is important because temporary files often exist in directories that other processes can observe or influence. The safe pattern is to create the file in a way that confirms the application owns a new file rather than checking whether a name is available and then opening it later. A time-of-check and time-of-use concern arises when the filesystem state can change between validation and use. The lesson is conceptual: avoid designs that assume paths remain unchanged after a separate check.\n\nPath safety matters whenever temporary paths include user-influenced values. The application should avoid direct user control over temporary locations. If a workflow needs to relate a temporary artifact to a user-facing name, separate the display name from the storage name. Build paths from trusted base directories, use path-aware APIs, apply canonicalization and containment checks where appropriate, and handle invalid paths with safe errors rather than fallback behavior that broadens access.\n\nSymbolic links and hard links require awareness because a path may not always refer to an ordinary file in the way a developer expects. The specific behavior depends on operating system and API choices, but the stable principle is to control who can create entries in temporary directories and to use APIs that create files safely. File creation should be controlled by the application, not negotiated through names supplied by untrusted input.",
  "narrationPoints": [
    "Generated unpredictable names from vetted APIs reduce that risk because the application is not inventing names through ad hoc string construction.",
    "A time-of-check and time-of-use concern arises when the filesystem state can change between validation and use.",
    "The application should avoid direct user control over temporary locations.",
    "File creation should be controlled by the application, not negotiated through names supplied by untrusted input.",
    "The lesson is conceptual: avoid designs that assume paths remain unchanged after a separate check.",
    "Atomic creation is important because temporary files often exist in directories that other processes can observe or influence."
  ]
};
