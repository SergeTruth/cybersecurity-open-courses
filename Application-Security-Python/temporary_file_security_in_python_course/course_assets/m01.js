window.COURSE_MODULE = {
  "title": "What Temporary File Security Means",
  "graphicAlt": "A short-lived file moves through secure creation, descriptor-based use, restrictive permissions, bounded content processing, cleanup, and optional durable publication inside a trusted temporary workspace.",
  "narration": "Temporary file security means treating short-lived files and directories as real storage with real security impact. Python applications create temporary artifacts during uploads, report generation, document conversion, archive handling, image processing, tests, data pipelines, background jobs, command-line tools, and subprocess workflows. These files may exist for seconds or minutes, but while they exist they can contain sensitive data, control application behavior, or affect later processing.\n\nTemporary files are different from caches, logs, uploads, working directories, and persistent storage, but the boundaries often overlap. A temporary upload may later become a permanent attachment. A report may be staged before download. A converted image may be passed through several workers. A temporary directory may hold archive contents. Because temporary files sit between steps, teams sometimes treat them as harmless scratch space. That habit is where risk begins.\n\nA temporary file can contain user content, personal data, generated documents, intermediate outputs, private business records, decrypted data, credentials, or operational details. It can also affect integrity if another process can modify it, availability if cleanup fails and disk fills, and auditability if nobody can explain where a sensitive artifact came from or why it remained. Short lifetime reduces exposure only when creation, access, and deletion are actually controlled.\n\nSecure temporary storage should be designed rather than improvised. The application should choose safe APIs, avoid manual predictable names, use controlled directories, apply appropriate permissions, minimize sensitive data written to disk, handle cleanup after success and failure, and test file workflows that involve uploads, archives, subprocesses, and background jobs. Temporary does not mean unimportant. It means the lifecycle should be deliberately short and well managed.",
  "narrationPoints": [
    "Temporary file security means treating short-lived files and directories as real storage with real security impact.",
    "Temporary files are different from caches, logs, uploads, working directories, and persistent storage, but the boundaries often overlap.",
    "Short lifetime reduces exposure only when creation, access, and deletion are actually controlled.",
    "Secure temporary storage should be designed rather than improvised.",
    "A temporary file can contain user content, personal data, generated documents, intermediate outputs, private business records, decrypted data, credentials, or operational details.",
    "A temporary upload may later become a permanent attachment."
  ]
};
