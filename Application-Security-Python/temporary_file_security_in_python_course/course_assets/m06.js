window.COURSE_MODULE = {
  "title": "Sensitive Data, Secrets, and Privacy",
  "graphicAlt": "Sensitive plaintext enters an unnamed temporary descriptor, is processed without a reusable path, and is overwritten before close; logs retain only purpose, outcome, and size class.",
  "narration": "Temporary files should be treated as sensitive whenever their contents or metadata are sensitive. Uploads, generated documents, exports, reports, converted media, decrypted intermediate data, personal data, regulated records, credentials, certificates, and private keys can all appear in temporary workflows. The fact that the file is temporary does not reduce the impact if the wrong user, process, log bundle, backup, or support artifact receives it.\n\nSensitive-data minimization is the strongest control. If a workflow can process data in memory safely and reliably, it may not need to write sensitive content to disk. If disk staging is required, write the smallest necessary data, store it in an approved location, restrict permissions, avoid unnecessary copies, and remove it as soon as the workflow no longer needs it. Avoid writing secrets to temporary files unless there is a carefully justified design and a clear cleanup path.\n\nEncryption at rest can help in some architectures, but it is not a reason to ignore lifecycle, permissions, or exposure paths. A temporary file can still be exposed through logs, crash reports, support bundles, debug output, backups, snapshots, or copied artifacts. Filenames and directory names can also reveal sensitive context. Avoid putting secrets, personal identifiers, tenant names, or private business terms into temporary filenames.\n\nPrivacy-aware temporary storage asks not only who can read the file, but also who can infer meaning from its existence, name, location, metadata, and lifecycle. A report staged for one customer should not reveal itself to another. A processing failure should not preserve private data indefinitely. Temporary storage should support the business workflow without becoming an unmanaged side channel for sensitive content.",
  "narrationPoints": [
    "Temporary files should be treated as sensitive whenever their contents or metadata are sensitive.",
    "Avoid writing secrets to temporary files unless there is a carefully justified design and a clear cleanup path.",
    "Avoid putting secrets, personal identifiers, tenant names, or private business terms into temporary filenames.",
    "Temporary storage should support the business workflow without becoming an unmanaged side channel for sensitive content.",
    "A temporary file can still be exposed through logs, crash reports, support bundles, debug output, backups, snapshots, or copied artifacts.",
    "A report staged for one customer should not reveal itself to another."
  ]
};
