window.COURSE_MODULE = {
  "title": "Permissions, Locations, and Lifecycle Management",
  "graphicAlt": "A trusted temporary root is checked for directory type, owner, and mode 0700; every created object receives mode 0600 and cleanup runs on both success and exception branches.",
  "narration": "Temporary directory selection is part of the security design. A per-application temporary directory can be easier to reason about than a broad shared system location. In some systems, per-user, per-tenant, or per-job isolation is appropriate. The right choice depends on deployment, sensitivity, runtime identity, and workflow. What matters is that the location is intentional and that only appropriate principals can read, write, modify, or delete the artifacts inside it.\n\nPermissions and ownership influence whether temporary files are confidential and whether their integrity can be trusted. Directory permissions determine who can create or replace entries. File permissions determine who can read or modify content. Runtime identity determines which process owns the files. Umask and platform defaults may affect new file modes. Teams do not need to memorize every platform rule to apply the principle: verify that permissions match the sensitivity and workflow.\n\nContainers and serverless platforms can make temporary storage feel isolated, but assumptions should still be reviewed. Containers may have mounted volumes, shared nodes, writable layers, or sidecar processes. Serverless runtimes may reuse execution environments or retain temporary data longer than a single function call in some designs. Short-lived infrastructure is useful, but it is not a substitute for sensitive-data minimization, controlled paths, and cleanup.\n\nLifecycle management means temporary files exist only as long as needed. Cleanup should happen after success, failure, cancellation, timeout, and exception paths. Deletion failures should be observable enough for operations to respond before disk usage or sensitive artifact accumulation becomes a problem. Retention should be deliberate when temporary outputs must remain for later processing. The absence of cleanup is a design decision, even when nobody intended it.",
  "narrationPoints": [
    "A per-application temporary directory can be easier to reason about than a broad shared system location.",
    "Permissions and ownership influence whether temporary files are confidential and whether their integrity can be trusted.",
    "Containers and serverless platforms can make temporary storage feel isolated, but assumptions should still be reviewed.",
    "The absence of cleanup is a design decision, even when nobody intended it.",
    "Short-lived infrastructure is useful, but it is not a substitute for sensitive-data minimization, controlled paths, and cleanup.",
    "Serverless runtimes may reuse execution environments or retain temporary data longer than a single function call in some designs."
  ]
};
