window.COURSE_MODULE = {
  "title": "Python Temporary File APIs and Safe Defaults",
  "graphicAlt": "TemporaryFile and TemporaryDirectory create private operating-system-managed work areas; already-open descriptors and context managers replace predictable names and manual cleanup.",
  "narration": "Python provides vetted tempfile abstractions because temporary file creation is more subtle than choosing a convenient name. At a high level, the tempfile module can create temporary files, temporary directories, named files, and automatically managed objects with generated unpredictable names. These APIs exist to avoid common mistakes around collisions, permissions, cleanup, and shared directories. They should usually be preferred over handcrafted filenames in a temporary location.\n\nContext-managed cleanup is one of the most useful patterns. A context manager can create a temporary file or directory for a bounded block of work and remove it when the block ends, including many failure cases. That does not remove the need for design judgment. The application still needs to decide where temporary storage should live, what permissions are appropriate, how much data may be written, whether the data is sensitive, and what happens if cleanup fails.\n\nChoosing the right abstraction matters. A temporary file may fit when a single stream of data needs to be passed to a library or tool. A temporary directory may fit when a workflow needs multiple related files, extracted archive contents, or separate input and output paths. Text and binary modes should match the data being handled. Automatically managed unnamed files may be useful for some workflows, while named files may be required when another component needs a path.\n\nSafe defaults still require careful use. A temporary file API can generate a safer name, but it cannot decide whether a user is authorized to process the data, whether sensitive content should be written to disk, whether a subprocess should receive the path, whether logs should include metadata, or how long a report should remain available. Vetted APIs reduce dangerous mechanics, while application design controls trust, sensitivity, and lifecycle.",
  "narrationPoints": [
    "Python provides vetted tempfile abstractions because temporary file creation is more subtle than choosing a convenient name.",
    "Context-managed cleanup is one of the most useful patterns.",
    "A temporary file may fit when a single stream of data needs to be passed to a library or tool.",
    "Vetted APIs reduce dangerous mechanics, while application design controls trust, sensitivity, and lifecycle.",
    "A temporary directory may fit when a workflow needs multiple related files, extracted archive contents, or separate input and output paths.",
    "Text and binary modes should match the data being handled."
  ]
};
