window.COURSE_MODULE = {
  "title": "Course Summary and Key Takeaways",
  "graphicAlt": "A lifecycle ring connects vetted temporary APIs, exclusive creation, trusted directories, descriptor identity, restrictive modes, data minimization, exception cleanup, durable commit, private telemetry, and incident recovery.",
  "narration": "Temporary file security in Python means treating short-lived storage as real storage with confidentiality, integrity, availability, and auditability risks. Temporary files appear in uploads, reports, archives, image processing, subprocess workflows, tests, data pipelines, background jobs, containers, serverless runtimes, and local tools. They may live briefly, but while they exist they can contain sensitive data or influence important processing.\n\nStrong designs use vetted temporary file APIs, generated unpredictable names, controlled directories, safe permissions, path-aware handling, containment checks where appropriate, least-privilege processing, and reliable cleanup. They avoid direct user control over temporary paths, separate display names from storage names, minimize sensitive data written to disk, and avoid secrets or private context in filenames, logs, crash reports, and support bundles.\n\nTemporary file safety is a workflow property. A secure creation call is not enough if later steps pass the file to a subprocess unsafely, extract archives into the wrong location, leave partial outputs after failure, or log sensitive paths and contents. The full pipeline should be reviewed from input through processing, output, cleanup, monitoring, and incident response.\n\nThe goal is predictable, authorized temporary storage that supports processing needs without leaking data, weakening file boundaries, or leaving sensitive artifacts behind. When teams design temporary storage deliberately, they reduce subtle risks and make Python applications easier to operate, review, and recover after failures.",
  "narrationPoints": [
    "Temporary file security in Python means treating short-lived storage as real storage with confidentiality, integrity, availability, and auditability risks.",
    "Strong designs use vetted temporary file APIs, generated unpredictable names, controlled directories, safe permissions, path-aware handling, containment checks where appropriate, least-privilege processing, and reliable cleanup.",
    "The full pipeline should be reviewed from input through processing, output, cleanup, monitoring, and incident response.",
    "The goal is predictable, authorized temporary storage that supports processing needs without leaking data, weakening file boundaries, or leaving sensitive artifacts behind.",
    "Temporary files appear in uploads, reports, archives, image processing, subprocess workflows, tests, data pipelines, background jobs, containers, serverless runtimes, and local tools.",
    "Temporary file safety is a workflow property."
  ]
};
