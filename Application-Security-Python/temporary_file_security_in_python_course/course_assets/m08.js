window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Incident Response",
  "graphicAlt": "Tests plant colliding names and links, force failures, inspect actual permissions, and verify cleanup; an orphan remover opens no-follow, checks owner, type, and age, then unlinks one entry.",
  "narration": "Testing should exercise temporary file behavior directly. Unit tests can verify that vetted APIs are used, names are not manually predictable, paths remain inside approved directories, and cleanup happens after normal completion. Integration tests can cover uploads, archive processing, subprocess handoffs, report generation, and background jobs. Failure tests are especially important because exceptions, timeouts, and cancellation are where temporary artifacts are often left behind.\n\nCode review should ask practical questions. Who can influence the temporary file name or directory? What data is written? Who can read it? What permissions are applied? Does the workflow handle symbolic links or path resolution safely? Does cleanup run after failure? Are subprocesses given only necessary paths? Are logs safe? These questions keep review focused on the security boundary rather than treating temporary files as incidental implementation detail.\n\nLogging and monitoring should support operations without leaking sensitive data. Logs may record a safe event name, job identifier, storage area, file category, cleanup status, error category, and correlation ID. They should avoid private file contents, secrets, tokens, full sensitive paths, and unnecessary personal data. Monitoring can watch for disk usage growth, failed cleanup, repeated processing failures, unexpected temporary directory changes, or unusual access patterns.\n\nIf temporary files expose sensitive data, incident response should assess what was exposed, where it was stored, who could access it, and whether it was copied into logs, backups, support bundles, or downstream systems. Rotate or revoke exposed secrets when needed, delete exposed files where feasible, preserve necessary evidence, fix the storage design, and add regression tests. Temporary file safety improves when incidents lead to better defaults and cleaner workflows.",
  "narrationPoints": [
    "Integration tests can cover uploads, archive processing, subprocess handoffs, report generation, and background jobs.",
    "These questions keep review focused on the security boundary rather than treating temporary files as incidental implementation detail.",
    "Logs may record a safe event name, job identifier, storage area, file category, cleanup status, error category, and correlation ID.",
    "Temporary file safety improves when incidents lead to better defaults and cleaner workflows.",
    "Failure tests are especially important because exceptions, timeouts, and cancellation are where temporary artifacts are often left behind.",
    "Monitoring can watch for disk usage growth, failed cleanup, repeated processing failures, unexpected temporary directory changes, or unusual access patterns."
  ]
};
