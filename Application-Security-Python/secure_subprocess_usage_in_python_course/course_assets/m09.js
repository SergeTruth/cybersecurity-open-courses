window.COURSE_MODULE = {
  "title": "Course Summary and Key Takeaways",
  "graphicAlt": "The subprocess checklist follows fixed executable provenance, structured arguments, controlled input descriptors, minimal environment, separate identity, resource limits, process-group cleanup, private errors, tests, and monitoring.",
  "narration": "Secure subprocess usage in Python means launching external programs only when necessary and doing so with explicit trust boundaries. The application should know which executable will run, which arguments are data, whether a shell is involved, which directory is used, which files are accessible, which environment values are passed, which identity is used, how long the process may run, and how output will be handled.\n\nStrong designs avoid shell interpretation, separate executables from structured arguments, and map user choices to allowlisted internal operations. They validate inputs, constrain paths and working directories, minimize inherited environment state, protect secrets, control standard input and output, apply least privilege, set timeouts, and translate errors into safe user-facing behavior. Each control reduces ambiguity around what the child process can do.\n\nSubprocess security connects to file handling, secrets management, authorization, safe logging, deployment design, monitoring, and incident response. A subprocess call is not just a utility line. It can reach across application boundaries into the operating system and surrounding infrastructure. That is why review should include successful execution, rejected inputs, failure modes, output handling, and cleanup behavior.\n\nThe practical goal is predictable, reviewable, least-privilege process execution that supports business needs without turning Python applications into unsafe command interfaces. Prefer libraries when they fit. When subprocess is necessary, keep the code explicit, narrow, observable, and tested. That discipline makes automation safer and easier to operate over time.",
  "narrationPoints": [
    "Secure subprocess usage in Python means launching external programs only when necessary and doing so with explicit trust boundaries.",
    "Strong designs avoid shell interpretation, separate executables from structured arguments, and map user choices to allowlisted internal operations.",
    "Subprocess security connects to file handling, secrets management, authorization, safe logging, deployment design, monitoring, and incident response.",
    "The practical goal is predictable, reviewable, least-privilege process execution that supports business needs without turning Python applications into unsafe command interfaces.",
    "Each control reduces ambiguity around what the child process can do.",
    "A subprocess call is not just a utility line."
  ]
};
