window.COURSE_MODULE = {
  "title": "Paths, Working Directories, and File Handling",
  "graphicAlt": "A trusted directory descriptor opens and validates the actual input file before its descriptor reaches a fixed scanner; a separately verified executable is non-link, correctly owned, and not writable.",
  "narration": "Executable path selection should be deliberate. If lookup behavior is ambiguous, the application may run a different program than intended because of path configuration, deployment layout, or inherited environment. A safer pattern is to choose executables from trusted configuration, known installation locations, or tightly controlled allowlists. The goal is to remove guesswork about which program actually starts.\n\nWorking directories shape how relative paths are resolved. If a subprocess inherits a surprising current directory, the same argument can read or write a different file than reviewers expect. Sensitive workflows should set the working directory intentionally or avoid relying on relative paths. Input and output locations should be built from trusted base directories using path-aware APIs, with containment checks when local filesystem access is involved.\n\nTemporary files often connect Python code to child processes. They may contain uploads, reports, transformed data, extracted content, or intermediate results. Predictable names, shared temporary directories, weak permissions, incomplete cleanup, and concurrent reuse can create exposure or confusion. Safer designs use platform helpers that create unpredictable names with appropriate permissions and clean up after both success and failure.\n\nFile permissions should match the child process purpose. A conversion task should not need access to source code, deployment secrets, unrelated customer files, or broad host directories. A report generator should write only to approved output locations. Subprocess security and file handling security are tightly connected because arguments often point to files, and child processes may trust those paths without understanding the application policy.",
  "narrationPoints": [
    "A safer pattern is to choose executables from trusted configuration, known installation locations, or tightly controlled allowlists.",
    "Sensitive workflows should set the working directory intentionally or avoid relying on relative paths.",
    "Predictable names, shared temporary directories, weak permissions, incomplete cleanup, and concurrent reuse can create exposure or confusion.",
    "A conversion task should not need access to source code, deployment secrets, unrelated customer files, or broad host directories.",
    "Safer designs use platform helpers that create unpredictable names with appropriate permissions and clean up after both success and failure.",
    "The goal is to remove guesswork about which program actually starts."
  ]
};
