window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Timeouts, Resource Controls, and Error Handling through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Enforce output and wall-clock limits for approved tools",
      "language": "python",
      "blurb": "An operation allowlist chooses the fixed child command; nonblocking selector reads share one monotonic deadline with process completion, and every post-launch failure attempts to kill, reap, and close the POSIX process group.",
      "code": "import math\nimport os\nimport selectors\nimport signal\nimport subprocess\nfrom time import monotonic\n\nAPPROVED_OUTPUT_COMMANDS = {\n    \"identify_version\": (\"/usr/bin/identify\", \"-version\"),\n    \"pdf_renderer_version\": (\"/usr/bin/pdftotext\", \"-v\"),\n}\nMAX_OUTPUT_BYTES = 10_000_000\nMAX_WALL_SECONDS = 30.0\n\nclass ChildExecutionError(RuntimeError):\n    pass\n\nclass ChildOutputLimitExceeded(ValueError):\n    pass\n\ndef bounded_output(\n    operation: str,\n    maximum: int = 1_000_000,\n    seconds: float = 3.0,\n) -> bytes:\n    command = APPROVED_OUTPUT_COMMANDS.get(operation) if type(operation) is str else None\n    if command is None:\n        raise ValueError(\"subprocess operation rejected\")\n    if os.name != \"posix\":\n        raise NotImplementedError(\"this process-group example requires POSIX\")\n    if type(maximum) is not int or not 0 <= maximum <= MAX_OUTPUT_BYTES:\n        raise ValueError(\"POSIX process limits required\")\n    if (\n        type(seconds) not in {int, float}\n        or not math.isfinite(seconds)\n        or not 0 < seconds <= MAX_WALL_SECONDS\n    ):\n        raise ValueError(\"POSIX process limits required\")\n    try:\n        process = subprocess.Popen(\n            command,\n            stdout=subprocess.PIPE,\n            stderr=subprocess.DEVNULL,\n            start_new_session=True,\n            env={\"PATH\": \"/usr/bin:/bin\", \"LANG\": \"C\"},\n        )\n    except Exception:\n        raise ChildExecutionError(\"approved child could not be started\") from None\n\n    selector = None\n    stdout = process.stdout\n    primary_error: BaseException | None = None\n    result: bytes | None = None\n    try:\n        if stdout is None:\n            raise ChildExecutionError(\"child output pipe unavailable\")\n        descriptor = stdout.fileno()\n        os.set_blocking(descriptor, False)\n        selector = selectors.DefaultSelector()\n        selector.register(descriptor, selectors.EVENT_READ)\n        deadline = monotonic() + seconds\n        output = bytearray()\n        while selector.get_map():\n            remaining = deadline - monotonic()\n            if remaining <= 0:\n                raise TimeoutError(\"child deadline exceeded\")\n            for key, _ in selector.select(min(remaining, 0.1)):\n                try:\n                    chunk = os.read(key.fd, 65_536)\n                except BlockingIOError:\n                    continue\n                if not chunk:\n                    selector.unregister(key.fd)\n                elif len(output) + len(chunk) > maximum:\n                    raise ChildOutputLimitExceeded(\n                        \"child output limit exceeded\"\n                    )\n                else:\n                    output.extend(chunk)\n        try:\n            return_code = process.wait(\n                timeout=max(0.001, deadline - monotonic())\n            )\n        except subprocess.TimeoutExpired:\n            raise TimeoutError(\"child deadline exceeded\") from None\n        if return_code != 0:\n            raise ChildExecutionError(\"child failed\")\n        result = bytes(output)\n    except (ChildExecutionError, TimeoutError, ChildOutputLimitExceeded) as error:\n        primary_error = error\n    except BaseException as error:\n        wrapped = ChildExecutionError(\"child output handling failed\")\n        wrapped.__cause__ = error\n        primary_error = wrapped\n\n    cleanup_errors: list[Exception] = []\n    if selector is not None:\n        try:\n            selector.close()\n        except Exception as error:\n            cleanup_errors.append(error)\n    try:\n        running = process.poll() is None\n    except Exception as error:\n        cleanup_errors.append(error)\n        running = True\n    if running:\n        try:\n            os.killpg(process.pid, getattr(signal, \"SIGKILL\", signal.SIGTERM))\n        except ProcessLookupError:\n            pass\n        except Exception as error:\n            cleanup_errors.append(error)\n    try:\n        process.wait(timeout=1)\n    except Exception as error:\n        cleanup_errors.append(error)\n    if stdout is not None:\n        try:\n            stdout.close()\n        except Exception as error:\n            cleanup_errors.append(error)\n\n    if primary_error is not None and cleanup_errors:\n        raise BaseExceptionGroup(\n            \"child output handling and cleanup failed\",\n            [primary_error, *cleanup_errors],\n        )\n    if primary_error is not None:\n        raise primary_error\n    if cleanup_errors:\n        raise ExceptionGroup(\"child output cleanup failed\", cleanup_errors)\n    if result is None:\n        raise ChildExecutionError(\"child produced no output result\")\n    return result\n"
    },
    {
      "title": "Apply operating-system limits through a fixed launcher",
      "language": "python",
      "blurb": "A Linux prlimit launcher caps CPU time, address space, output files, and descriptors before the fixed renderer begins work, without a thread-unsafe preexec hook.",
      "code": "import subprocess\n\ndef render_untrusted_document(source) -> subprocess.Popen[bytes]:\n    return subprocess.Popen(\n        [\n            \"/usr/bin/prlimit\",\n            \"--cpu=2:2\",\n            \"--as=268435456:268435456\",\n            \"--fsize=8388608:8388608\",\n            \"--nofile=32:32\",\n            \"--\",\n            \"/usr/bin/pdftotext\",\n            \"-\",\n            \"-\",\n        ],\n        stdin=source,\n        stdout=subprocess.PIPE,\n        stderr=subprocess.DEVNULL,\n        shell=False,\n        start_new_session=True,\n        close_fds=True,\n        env={\"PATH\": \"/usr/bin:/bin\", \"LANG\": \"C\"},\n    )\n"
    }
  ]
};
