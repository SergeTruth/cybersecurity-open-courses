window.COURSE_MODULE = {
  "title": "Environment Variables, Secrets, and Inherited State",
  "graphicAlt": "A minimal child environment contains fixed PATH, locale, and mode values while parent secrets, proxy settings, preload variables, and debugging state remain outside the process boundary.",
  "narration": "Child processes may inherit environment variables from the parent Python process unless the application controls the environment explicitly. That inherited state can include API keys, tokens, passwords, connection strings, private key paths, proxy settings, debug flags, cloud configuration, feature flags, executable search paths, and library paths. Even when the child program is trusted, it rarely needs everything the parent process knows.\n\nA safer pattern is to construct an explicit environment for the approved task. Pass only the variables needed for that operation, and avoid letting untrusted input change behavior-affecting variables. This makes subprocess behavior more predictable and reduces blast radius if the child process fails, logs its environment, spawns another tool, or writes diagnostic output. Environment minimization is both a security control and a review aid.\n\nSecrets should not be placed in command arguments unless there is a carefully justified reason. Depending on the operating system and tooling, arguments may appear in process listings, monitoring systems, job metadata, crash output, logs, diagnostics, or support bundles. Standard input can also be risky when it is captured or logged. The stable principle is to provide the least sensitive material necessary through the narrowest appropriate channel.\n\nInherited user permissions matter as much as inherited variables. A subprocess normally runs with the parent process identity or a configured execution identity. If that identity has broad filesystem, network, or secret-store access, the child process may have it too. Secure subprocess usage treats identity, permissions, environment, and configuration as part of the command contract instead of invisible background state.",
  "narrationPoints": [
    "Even when the child program is trusted, it rarely needs everything the parent process knows.",
    "Pass only the variables needed for that operation, and avoid letting untrusted input change behavior-affecting variables.",
    "The stable principle is to provide the least sensitive material necessary through the narrowest appropriate channel.",
    "A subprocess normally runs with the parent process identity or a configured execution identity.",
    "Secrets should not be placed in command arguments unless there is a carefully justified reason.",
    "Child processes may inherit environment variables from the parent Python process unless the application controls the environment explicitly."
  ]
};
