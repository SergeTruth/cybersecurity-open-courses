window.COURSE_MODULE = {
  "title": "What Secure Subprocess Usage Means",
  "graphicAlt": "Python crosses into an external program through explicit executable, arguments, standard streams, environment, files, operating-system identity, resource limits, lifecycle control, and sanitized results.",
  "narration": "Secure subprocess usage means using Python to launch external programs in a controlled, explicit, and reviewable way. The subprocess module is powerful because it lets Python code start child processes, pass inputs, capture outputs, wait for completion, and react to return codes. That power appears in web applications, APIs, command-line tools, automation scripts, background workers, scheduled tasks, build systems, data pipelines, and internal administrative utilities.\n\nSubprocess usage is different from calling a Python library or using a platform API directly. A library call usually stays inside the Python process and follows familiar application control flow. A child process runs as a separate program with its own parsing rules, input streams, output streams, environment, working directory, permissions, and lifecycle. It may read files, write files, use network access, produce diagnostic output, or continue longer than expected.\n\nThat separation creates trust boundaries between Python code, the operating system, files, environment variables, user-controlled inputs, and the child process. A seemingly simple helper can affect confidentiality if it receives secrets it does not need, integrity if it writes to unintended locations, availability if it runs without limits, and auditability if output and errors are logged carelessly. Subprocess code deserves the same attention as database access or file handling.\n\nThe safest design is often to avoid subprocess entirely when a well-supported Python library can perform the task safely. When an external program is genuinely needed, the code should make the boundary obvious: choose the executable deliberately, pass arguments as structured values, avoid shell interpretation, validate inputs, control paths and environment variables, apply least privilege, use timeouts, handle output safely, and test failure behavior.",
  "narrationPoints": [
    "Secure subprocess usage means using Python to launch external programs in a controlled, explicit, and reviewable way.",
    "Subprocess usage is different from calling a Python library or using a platform API directly.",
    "Subprocess code deserves the same attention as database access or file handling.",
    "The safest design is often to avoid subprocess entirely when a well-supported Python library can perform the task safely.",
    "A library call usually stays inside the Python process and follows familiar application control flow.",
    "A child process runs as a separate program with its own parsing rules, input streams, output streams, environment, working directory, permissions, and lifecycle."
  ]
};
