window.COURSE_MODULE = {
  "title": "Avoiding Shell Interpretation",
  "graphicAlt": "A fixed image utility receives separate format, option terminator, and filename arguments; shell expansion, command chaining, redirection, and caller-selected executable paths are blocked.",
  "narration": "Shell interpretation is one of the most important subprocess boundaries. A shell treats text as instructions with features such as parsing, expansion, redirection, chaining, variables, and pattern matching. Those features are useful for interactive administration, but they are risky when application data can influence the command text. The safer default is direct execution with the shell disabled, especially when any value may come from a user or external system.\n\nDirect execution separates the executable from its arguments. The application chooses a program from trusted configuration or an approved allowlist, then passes each argument as a structured value. This makes it much harder for ordinary data to become command structure. It also makes code review more straightforward because reviewers can see which parts are the command and which parts are data.\n\nString-built command lines are fragile because they mix command syntax, options, file paths, and untrusted values into one piece of text. Escaping and quoting rules vary across shells and platforms, and the safest answer is usually to avoid needing those rules. When developers reach for shell conveniences such as pipelines, globbing, variable expansion, or redirection, they should ask whether Python APIs or explicit process handling can provide the same result more safely.\n\nThere are cases where shell features are genuinely required, but they should be treated as exceptions that need justification and review. The operation should be narrow, inputs should be mapped to approved choices, secrets should not be placed into command text, and the runtime should use least privilege. Avoiding shell interpretation does not solve every subprocess risk, but it removes a large class of unintended parsing behavior from the design.",
  "narrationPoints": [
    "A shell treats text as instructions with features such as parsing, expansion, redirection, chaining, variables, and pattern matching.",
    "The application chooses a program from trusted configuration or an approved allowlist, then passes each argument as a structured value.",
    "Escaping and quoting rules vary across shells and platforms, and the safest answer is usually to avoid needing those rules.",
    "There are cases where shell features are genuinely required, but they should be treated as exceptions that need justification and review.",
    "String-built command lines are fragile because they mix command syntax, options, file paths, and untrusted values into one piece of text.",
    "Shell interpretation is one of the most important subprocess boundaries."
  ]
};
