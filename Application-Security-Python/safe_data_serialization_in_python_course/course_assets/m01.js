window.COURSE_MODULE = {
  "title": "What Data Serialization Means",
  "graphicAlt": "Diagram for What Data Serialization Means, showing untrusted bytes, bounded parser, schema validator, typed data, and rejected object-construction path; labeled arrows identify parse, validation, and trust transitions and the point where unsafe input or behavior is rejected.",
  "narration": "Serialization is the process of converting application data structures into a format that can be stored, transmitted, logged, cached, queued, or exchanged with another system. Deserialization is the reverse process: reading serialized data and turning it back into structures the application can use. Python applications do this constantly in APIs, files, queues, caches, configuration, databases, background jobs, ML pipelines, and service integrations.\n\nA useful distinction is the difference between plain data formats and object serialization. Plain data formats, such as JSON or CSV, generally describe values: strings, numbers, arrays, rows, fields, and records. Object serialization may try to preserve richer application objects, including class names, object graphs, or reconstruction behavior. That convenience can be valuable in controlled internal contexts, but it becomes risky when the data crosses trust boundaries.\n\nSerialized data often crosses a boundary because it moves between users, services, files, storage systems, queues, caches, partners, and processes. Each boundary can change who controls the data, how fresh it is, what assumptions apply, and what the receiving code will do with it. The safest design starts by asking where the data came from, who owns it, and what authority it should have after deserialization.\n\nA successful parse is not the same as safe data. Parsed data may still contain unexpected fields, wrong types, excessive size, stale values, sensitive content, or meanings the application should not accept. Safe serialization work combines format selection, safe parser configuration, validation, type control, authorization where relevant, resource limits, privacy-aware logging, and reviewable error handling.",
  "narrationPoints": [
    "Deserialization is the reverse process: reading serialized data and turning it back into structures the application can use.",
    "A useful distinction is the difference between plain data formats and object serialization.",
    "Object serialization may try to preserve richer application objects, including class names, object graphs, or reconstruction behavior.",
    "Each boundary can change who controls the data, how fresh it is, what assumptions apply, and what the receiving code will do with it.",
    "A successful parse is not the same as safe data.",
    "Safe serialization work combines format selection, safe parser configuration, validation, type control, authorization where relevant, resource limits, privacy-aware logging, and reviewable error handling."
  ]
};
