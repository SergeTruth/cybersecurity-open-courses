window.COURSE_MODULE = {
  "title": "Webhook Architecture and Trust Boundaries",
  "graphicAlt": "Provider, Internet-facing endpoint, verification service, replay store, tenant data, durable queue, worker, and dead-letter state form distinct trust and durability boundaries joined by labeled event identifiers.",
  "narration": "A webhook architecture has several parts: the provider that emits events, the consumer endpoint that receives them, any public callback URL, internal handlers, gateways or reverse proxies, queues, background jobs, databases, and downstream actions. Each part has a different responsibility. The provider asserts that an event occurred. The receiving endpoint validates and acknowledges the request. Internal processing decides what the event means for local state and business rules.\n\nTrust boundaries appear wherever identity, data, credentials, network path, or authority changes. A request crosses from the provider's environment into your public endpoint, through a proxy or gateway, into Python application code, and possibly into a queue, database, worker, or downstream API call. Headers, payloads, signatures, secrets, timestamps, and event identifiers all cross these boundaries. A secure design identifies where each value becomes trusted, what proof is required, and what happens when proof is missing.\n\nEnvironment separation is another important boundary. Test and production events should not be interchangeable. A staging webhook endpoint should not update production records, and a production event should not be accepted by a test handler simply because the shape looks familiar. Endpoint names, credentials, signing secrets, event types, allowed providers, and data classification should be clear per environment. When teams blur those boundaries, webhook failures can become data integrity or compliance problems.\n\nWebhook security decisions should be centralized and reviewable. Ownership should be explicit: who owns the integration, the endpoint, the signing secret, the allowed event list, the processing job, the data mapping, and the incident path. Central validation middleware, helper functions, or framework-specific utilities can help make behavior consistent. Without that structure, each webhook route may interpret signatures, timestamps, errors, retries, and logs differently, which makes the whole system harder to reason about.",
  "narrationPoints": [
    "Internal processing decides what the event means for local state and business rules.",
    "Trust boundaries appear wherever identity, data, credentials, network path, or authority changes.",
    "Endpoint names, credentials, signing secrets, event types, allowed providers, and data classification should be clear per environment.",
    "Central validation middleware, helper functions, or framework-specific utilities can help make behavior consistent.",
    "Headers, payloads, signatures, secrets, timestamps, and event identifiers all cross these boundaries.",
    "A secure design identifies where each value becomes trusted, what proof is required, and what happens when proof is missing."
  ]
};
