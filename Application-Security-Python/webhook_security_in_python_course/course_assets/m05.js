window.COURSE_MODULE = {
  "title": "Replay Resistance, Freshness, and Idempotency",
  "graphicAlt": "A current event identifier enters an atomic replay store, then a database transaction inserts the unique event and applies one pending-to-settled state change; duplicate delivery returns the prior result.",
  "narration": "Replay risk exists when a previously valid event is reused outside its intended time or context. A signature may still match an old payload, especially if the signing secret has not changed. Freshness controls such as timestamps, bounded acceptance windows, event identifiers, request identifiers, or nonce concepts help the consumer decide whether an event is plausible now. These controls reduce the chance that old events can be accepted as new business signals.\n\nFreshness and idempotency are related but separate. Freshness asks whether an event should still be accepted based on time or context. Idempotency asks whether processing the same intended event more than once creates the same outcome without unwanted duplicate side effects. A handler can reject stale events and still need idempotency for legitimate duplicate deliveries. Conversely, deduplication alone does not prove that a very old event should still be honored.\n\nDuplicate delivery is normal in webhook systems. Providers often retry when they do not receive a timely success response, and networks can fail after the provider has sent the event but before it receives the acknowledgement. Exactly-once delivery is rarely a practical assumption. Python handlers should use event identifiers, idempotency keys, deduplication records, state checks, or transaction-aware processing to avoid duplicate charges, repeated approvals, duplicate tickets, repeated messages, or conflicting state transitions.\n\nTimeout ambiguity is the hard case. If a worker times out while processing an event, the application may not know whether the downstream action completed. A secure design defines reconciliation behavior, durable records, and safe retry rules. Handlers should be able to resume or ignore work based on recorded state rather than guesses. The goal is to make repeated delivery safe while still ensuring legitimate events are not silently lost.",
  "narrationPoints": [
    "Replay risk exists when a previously valid event is reused outside its intended time or context.",
    "Conversely, deduplication alone does not prove that a very old event should still be honored.",
    "Duplicate delivery is normal in webhook systems.",
    "Handlers should be able to resume or ignore work based on recorded state rather than guesses.",
    "A signature may still match an old payload, especially if the signing secret has not changed.",
    "The goal is to make repeated delivery safe while still ensuring legitimate events are not silently lost."
  ]
};
