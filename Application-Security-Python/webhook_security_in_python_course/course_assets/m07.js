window.COURSE_MODULE = {
  "title": "Processing Patterns, Queues, and Reliability",
  "graphicAlt": "The endpoint returns 202 only after a persistent queue confirms publication; workers claim due rows with skip-locked, increment bounded attempts, and move exhausted jobs to dead-letter state.",
  "narration": "Webhook receivers often need to acknowledge quickly. Providers may expect a timely response and retry when the consumer is slow. A common pattern is to validate the request, record the event, and place work on a queue for background processing. This separates the public HTTP acknowledgement path from slower business work. The handler can remain small and predictable while workers perform retries, database updates, notifications, or downstream API calls with better operational controls.\n\nQueueing does not remove security obligations. Validation should happen before enqueueing enough information to trust the event, or the queued record should clearly preserve an untrusted state until validation completes. Workers need the same tenant mapping, authorization checks, idempotency records, and payload validation expectations as synchronous code. A queue can improve reliability, but it can also spread unsafe assumptions if the event is treated as trusted merely because it reached internal infrastructure.\n\nTransaction boundaries matter. If the handler writes a deduplication record, updates a database, and triggers a downstream action, the order and atomicity of those steps affect correctness. Inbox and outbox pattern concepts can help preserve durable state about received events and outgoing actions. Dead-letter queues can help operators inspect events that cannot be processed after repeated attempts. These patterns are not just reliability tools; they reduce the chance of hidden data loss or repeated side effects.\n\nOrdering, partial failure, and backpressure should be explicit. Some providers do not guarantee event order, and even when order is intended, retries and parallel workers can rearrange processing. A secure system should avoid assuming that every event arrives once, in order, and with all dependencies available. Backpressure controls, worker limits, retry bounds, and monitoring help the application remain stable when event volume spikes or downstream services fail.",
  "narrationPoints": [
    "Providers may expect a timely response and retry when the consumer is slow.",
    "Workers need the same tenant mapping, authorization checks, idempotency records, and payload validation expectations as synchronous code.",
    "Inbox and outbox pattern concepts can help preserve durable state about received events and outgoing actions.",
    "A secure system should avoid assuming that every event arrives once, in order, and with all dependencies available.",
    "Dead-letter queues can help operators inspect events that cannot be processed after repeated attempts.",
    "A common pattern is to validate the request, record the event, and place work on a queue for background processing."
  ]
};
