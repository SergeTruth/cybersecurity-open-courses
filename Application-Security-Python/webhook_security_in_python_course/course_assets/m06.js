window.COURSE_MODULE = {
  "title": "Payload Validation and Business Logic Safety",
  "graphicAlt": "Strict JSON validation fixes version, event type, event identifier, and nested payment shape before stored integration ownership maps the event to one tenant and pending payment.",
  "narration": "Webhook payloads should be treated as untrusted input until they are validated. Signature verification may confirm source and integrity, but it does not make every field meaningful, authorized, current, or safe. The handler should check content type, size limits, expected event type, required fields, optional fields, unknown fields, data types, identifiers, timestamps, and nested structure before the payload influences state. JSON schema concepts can help make these expectations explicit.\n\nEvent-type allowlists are especially useful. A single webhook endpoint may receive many possible events, but the application should process only the event types it intentionally supports. Unknown or unsupported events should be logged at an appropriate level and acknowledged or rejected according to the integration design, without falling through into generic processing. When event contracts change, schema validation and tests should make the change visible rather than quietly accepting new meaning.\n\nBusiness logic safety requires authoritative local checks. Do not trust client-controlled prices, roles, permissions, account status, approval decisions, tenant identifiers, or entitlement changes simply because they appear in a verified payload. Use the event as a signal, then look up authoritative local or provider state when the action is important. Confirm that the event maps to the expected tenant, organization, account, resource, and workflow state before changing local records.\n\nAuthorization-aware processing keeps provider identity separate from business authority. A provider may be trusted to send an event, but the event still needs to be allowed for the specific local context. For example, an event about one account should not affect another account because an identifier was ambiguous. A subscription event should not grant access until the local system has confirmed the relationship, status, and expected transition. Verified data still needs validation, authorization, and business-rule checks.",
  "narrationPoints": [
    "Signature verification may confirm source and integrity, but it does not make every field meaningful, authorized, current, or safe.",
    "A single webhook endpoint may receive many possible events, but the application should process only the event types it intentionally supports.",
    "Use the event as a signal, then look up authoritative local or provider state when the action is important.",
    "For example, an event about one account should not affect another account because an identifier was ambiguous.",
    "Confirm that the event maps to the expected tenant, organization, account, resource, and workflow state before changing local records.",
    "A subscription event should not grant access until the local system has confirmed the relationship, status, and expected transition."
  ]
};
