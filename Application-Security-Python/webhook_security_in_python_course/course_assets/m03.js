window.COURSE_MODULE = {
  "title": "Endpoint Exposure, Authentication, and Access Control",
  "graphicAlt": "The endpoint accepts only POST application/json with declared and actual byte limits, while a key identifier selects provider-held verification material and source IP remains supplementary evidence.",
  "narration": "Webhook endpoints are often reachable from the public internet because providers need to call them. That exposure is not automatically unsafe, but it must be intentional. Endpoint URL secrecy can reduce casual discovery, yet it is not a strong security boundary by itself. URLs can leak through logs, configuration, support tickets, screenshots, deployment output, referrers, or vendor consoles. Reaching the URL should never be enough to trigger sensitive state changes.\n\nSource authentication can include provider-specific signatures, shared secrets, mutual TLS concepts, gateway controls, or other identity checks. IP allowlisting can support defense in depth, but it has limitations because provider address ranges can change, proxies can alter the observed source, and network-based checks rarely express business authorization. The durable principle is to authenticate the provider identity using controls that match the integration, then apply additional checks before local action.\n\nAccess control continues after the request is authenticated. A valid event from a provider does not automatically mean it applies to the tenant, account, organization, user, project, subscription, or resource claimed in the payload. The handler should map provider identifiers to local records using trusted configuration or authoritative lookup. It should check whether the event type is allowed for that integration and whether the resulting action is permitted for the mapped context.\n\nRate limiting and throttling also belong in endpoint design. A webhook receiver should tolerate bursts, reject clearly invalid traffic cheaply, and avoid letting expensive processing happen before basic validation. Gateways, reverse proxies, and application middleware can help limit load, but the Python handler still needs safe failure behavior. When an endpoint receives unexpected volume, invalid signatures, or unknown event types, it should fail closed, preserve useful telemetry, and avoid leaking secrets in responses or logs.",
  "narrationPoints": [
    "Webhook endpoints are often reachable from the public internet because providers need to call them.",
    "Source authentication can include provider-specific signatures, shared secrets, mutual TLS concepts, gateway controls, or other identity checks.",
    "The handler should map provider identifiers to local records using trusted configuration or authoritative lookup.",
    "Gateways, reverse proxies, and application middleware can help limit load, but the Python handler still needs safe failure behavior.",
    "Endpoint URL secrecy can reduce casual discovery, yet it is not a strong security boundary by itself.",
    "URLs can leak through logs, configuration, support tickets, screenshots, deployment output, referrers, or vendor consoles."
  ]
};
