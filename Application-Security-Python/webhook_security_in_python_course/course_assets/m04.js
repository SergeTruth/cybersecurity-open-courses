window.COURSE_MODULE = {
  "title": "Signature Verification and Shared Secrets",
  "graphicAlt": "Exact timestamp and body bytes enter HMAC-SHA256 or Ed25519 verification; key identifier, freshness window, signature length, encoding, and constant-time comparison surround the authenticated envelope.",
  "narration": "Webhook signatures are used to check integrity and authenticity at a high level. A provider signs some representation of the request, often with a shared secret and an HMAC, or sometimes with an asymmetric key scheme. The consumer recomputes or verifies the signature and compares it with the value received in a header. If the verification fails, the handler should not trust the payload, parse it for business decisions, or perform state-changing work.\n\nThe exact signed representation matters. Many signature schemes sign the raw request body, a timestamp plus body, selected headers, or a canonicalized form of the request. If the Python framework parses JSON, normalizes whitespace, changes encoding, or reads the body before verification in a way that changes the bytes being checked, valid events may fail or invalid assumptions may creep in. Handlers should follow the provider's signing contract and verify the exact data the scheme expects.\n\nShared signing secrets are credentials. They should be stored in an approved secret store or secure configuration path, separated by environment, scoped to the specific integration, and rotated when needed. They should not appear in code, notebooks, traces, error messages, logs, or support bundles. Rotation often requires a transition period where old and new secrets are both accepted safely, with clear telemetry showing which secret version verified an event.\n\nVerification code should fail safely. It should select the expected signature header deliberately, reject missing or malformed signature metadata, use timing-safe comparison concepts where appropriate, and avoid returning detailed validation internals to the caller. Signature verification proves that the request matches the signing scheme and secret or key. It does not prove that the event is fresh, authorized for the local tenant, structurally valid, or safe to process. Those checks come next.",
  "narrationPoints": [
    "The consumer recomputes or verifies the signature and compares it with the value received in a header.",
    "Handlers should follow the provider's signing contract and verify the exact data the scheme expects.",
    "Rotation often requires a transition period where old and new secrets are both accepted safely, with clear telemetry showing which secret version verified an event.",
    "Signature verification proves that the request matches the signing scheme and secret or key.",
    "Webhook signatures are used to check integrity and authenticity at a high level.",
    "Many signature schemes sign the raw request body, a timestamp plus body, selected headers, or a canonicalized form of the request."
  ]
};
