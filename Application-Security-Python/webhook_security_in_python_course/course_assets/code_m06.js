window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Payload Validation and Business Logic Safety through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Validate a strictly versioned webhook payload",
      "language": "python",
      "blurb": "The parser rejects Boolean versions, invalid event identifiers, duplicate or unknown fields, non-standard numbers, unsupported event types, and malformed nested payment data before dispatch.",
      "code": "import json\nimport re\n\nEVENT_ID = re.compile(r\"evt_[A-Za-z0-9_-]{16,80}\")\nPAYMENT_ID = re.compile(r\"pay_[A-Za-z0-9]{16,40}\")\nMAX_WEBHOOK_BYTES = 256_000\nMAX_JSON_DEPTH = 16\n\ndef preflight_json_depth(body: bytes) -> None:\n    depth = 0\n    in_string = False\n    escaped = False\n    for byte in body:\n        if in_string:\n            if escaped:\n                escaped = False\n            elif byte == 0x5C:\n                escaped = True\n            elif byte == 0x22:\n                in_string = False\n            continue\n        if byte == 0x22:\n            in_string = True\n        elif byte in (0x5B, 0x7B):\n            depth += 1\n            if depth > MAX_JSON_DEPTH:\n                raise ValueError(\"webhook JSON depth rejected\")\n        elif byte in (0x5D, 0x7D) and depth > 0:\n            depth -= 1\n\ndef bounded_json_int(text: str) -> int:\n    if len(text) > 4:\n        raise ValueError(\"webhook integer rejected\")\n    return int(text)\n\ndef parse_payment_event(body: bytes) -> dict[str, object]:\n    if type(body) is not bytes or not 1 <= len(body) <= MAX_WEBHOOK_BYTES:\n        raise ValueError(\"webhook body rejected\")\n    preflight_json_depth(body)\n\n    def unique_object(pairs):\n        result = {}\n        for key, value in pairs:\n            if key in result:\n                raise ValueError(\"duplicate webhook member\")\n            result[key] = value\n        return result\n\n    try:\n        document = json.loads(\n            body.decode(\"utf-8\"),\n            object_pairs_hook=unique_object,\n            parse_int=bounded_json_int,\n            parse_float=lambda value: (_ for _ in ()).throw(\n                ValueError(\"webhook float rejected\")\n            ),\n            parse_constant=lambda value: (_ for _ in ()).throw(\n                ValueError(\"webhook number rejected\")\n            ),\n        )\n    except (UnicodeDecodeError, json.JSONDecodeError, RecursionError):\n        raise ValueError(\"webhook JSON rejected\") from None\n    if (\n        not isinstance(document, dict)\n        or set(document) != {\"version\", \"event_id\", \"type\", \"data\"}\n    ):\n        raise ValueError(\"webhook envelope rejected\")\n    version = document[\"version\"]\n    event_id = document[\"event_id\"]\n    event_type = document[\"type\"]\n    if (\n        type(version) is not int\n        or version != 1\n        or type(event_type) is not str\n        or event_type not in {\"payment.settled\", \"payment.failed\"}\n    ):\n        raise ValueError(\"webhook event type rejected\")\n    if type(event_id) is not str or EVENT_ID.fullmatch(event_id) is None:\n        raise ValueError(\"webhook event identifier rejected\")\n    data = document[\"data\"]\n    if not isinstance(data, dict) or set(data) != {\"payment_id\"}:\n        raise ValueError(\"webhook data shape rejected\")\n    payment_id = data[\"payment_id\"]\n    if type(payment_id) is not str or PAYMENT_ID.fullmatch(payment_id) is None:\n        raise ValueError(\"payment identifier rejected\")\n    return {\n        \"version\": 1,\n        \"event_id\": event_id,\n        \"type\": event_type,\n        \"data\": {\"payment_id\": payment_id},\n    }\n"
    },
    {
      "title": "Authorize webhook business effects independently",
      "language": "python",
      "blurb": "A valid vendor signature does not choose the tenant or transition; stored integration ownership and current object state decide whether the event may act.",
      "code": "import re\n\nINTEGRATION_ID = re.compile(r\"int_[A-Za-z0-9_-]{8,64}\\Z\")\nPAYMENT_ID = re.compile(r\"pay_[A-Za-z0-9_-]{8,64}\\Z\")\nTENANT_ID = re.compile(r\"ten_[A-Za-z0-9_-]{8,64}\\Z\")\n\ndef authorize_payment_event(repository, integration_id: str, event: dict[str, object]) -> object:\n    if not isinstance(integration_id, str) or INTEGRATION_ID.fullmatch(integration_id) is None:\n        raise ValueError(\"integration identifier rejected\")\n    if not isinstance(event, dict) or set(event) != {\"data\"}:\n        raise ValueError(\"payment event schema rejected\")\n    data = event[\"data\"]\n    if not isinstance(data, dict) or set(data) != {\"payment_id\"}:\n        raise ValueError(\"payment event data rejected\")\n    payment_id = data[\"payment_id\"]\n    if not isinstance(payment_id, str) or PAYMENT_ID.fullmatch(payment_id) is None:\n        raise ValueError(\"payment identifier rejected\")\n    load_integration = repository.integration\n    load_payment = repository.payment_for_tenant\n    if not callable(load_integration) or not callable(load_payment):\n        raise TypeError(\"payment repository contract rejected\")\n    integration = load_integration(integration_id)\n    tenant_id = getattr(integration, \"tenant_id\", None)\n    if (\n        integration is None\n        or getattr(integration, \"enabled\", None) is not True\n        or getattr(integration, \"vendor\", None) != \"payment_partner\"\n        or not isinstance(tenant_id, str)\n        or TENANT_ID.fullmatch(tenant_id) is None\n    ):\n        raise PermissionError(\"webhook integration rejected\")\n    payment = load_payment(tenant_id, payment_id)\n    if payment is None or getattr(payment, \"status\", None) != \"pending\":\n        raise PermissionError(\"webhook business target rejected\")\n    return payment\n"
    }
  ]
};
