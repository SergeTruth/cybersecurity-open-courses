window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Incident Response",
  "graphicAlt": "A test matrix alters body, signature, timestamp, event identifier, tenant, schema, queue confirmation, and retry outcome; logs retain opaque identifiers and finite stages without payloads or signatures.",
  "narration": "Webhook tests should cover more than a valid happy path. Useful tests include accepted valid signatures, rejected invalid signatures, missing signature headers, stale timestamps, duplicate event identifiers, unsupported event types, malformed payloads, oversized payloads, wrong content types, tenant-boundary failures, authorization failures, retry scenarios, and idempotent reprocessing. These tests give teams confidence that the handler fails safely when integration assumptions are wrong.\n\nLogging should support operations without exposing sensitive material. Webhook logs may include event names, provider identifiers, local correlation identifiers, validation outcome, processing status, latency, and safe error categories. They should avoid shared secrets, tokens, signatures, full sensitive payloads, unnecessary personal data, raw authorization headers, and anything that would make replay or impersonation easier. Redaction should be part of the logging design, not an afterthought after an incident.\n\nMonitoring should make abnormal webhook behavior visible. Track failed validations, unknown event types, invalid signatures, stale events, duplicate rates, processing delays, queue depth, retry storms, downstream failures, unusually high event volume, and sudden drops in expected events. Good monitoring helps distinguish provider outages, integration bugs, credential problems, malicious traffic, and local processing failures. Without telemetry, webhook systems can silently lose events or repeat actions for a long time.\n\nIncident response should include exposed secrets, unsafe processing, data leakage, and incorrect state changes. Teams need a plan to rotate or revoke signing secrets, coordinate with providers, pause or throttle processing, replay or reconcile events safely, review logs, assess data impact, and add regression tests. After an incident, the durable improvement is not only a fixed handler. It is clearer ownership, better validation, safer logging, stronger monitoring, and documented recovery steps.",
  "narrationPoints": [
    "Webhook tests should cover more than a valid happy path.",
    "Redaction should be part of the logging design, not an afterthought after an incident.",
    "Good monitoring helps distinguish provider outages, integration bugs, credential problems, malicious traffic, and local processing failures.",
    "Incident response should include exposed secrets, unsafe processing, data leakage, and incorrect state changes.",
    "Without telemetry, webhook systems can silently lose events or repeat actions for a long time.",
    "Webhook logs may include event names, provider identifiers, local correlation identifiers, validation outcome, processing status, latency, and safe error categories."
  ]
};
