window.COURSE_MODULE = {
  "title": "What Webhook Security Means",
  "graphicAlt": "A provider sends a timestamped, signed HTTP callback through transport limits, key selection, signature verification, replay reservation, schema checks, tenant authorization, idempotent transaction, durable queue, and private telemetry.",
  "narration": "Webhook security is the practice of safely receiving and processing event-driven HTTP callbacks between systems. A provider sends an event to a consumer when something happens, and the consumer decides what to do with that event. In Python, webhook handlers may live in Flask, Django, FastAPI, serverless functions, background workers, queues, automation scripts, payment integrations, identity workflows, CI/CD systems, or SaaS integrations. The implementation may look like a small route, but it often sits on top of important business behavior.\n\nA webhook is different from a normal API request pattern. With polling, your application asks another system for updates. With an incoming webhook, another system calls your application. With an outgoing webhook, your application calls a subscriber when an event occurs. Message queues and event streams can provide more durable event delivery models, but webhooks remain common because HTTP callbacks are easy to integrate. Their simplicity is exactly why security design matters.\n\nWebhooks are security-sensitive because they can trigger behavior automatically. A single event might create an account, mark an invoice as paid, disable access, update a ticket, start a build, change a subscription, or notify a downstream service. The endpoint receiving the event is often public, and a request reaching that endpoint is not trustworthy by itself. The handler must establish source, integrity, freshness, expected event type, payload shape, authorization context, and business meaning before it changes state.\n\nSecure webhook design covers more than the first validation check. It includes source validation, signature verification, shared-secret protection, payload validation, idempotency, replay resistance, retry-safe processing, careful logging, monitoring, and incident response. The best designs centralize these decisions so every handler does not invent its own rules. A webhook endpoint should be treated as a small integration system, not as a casual route that happens to receive JSON.",
  "narrationPoints": [
    "Webhook security is the practice of safely receiving and processing event-driven HTTP callbacks between systems.",
    "With an outgoing webhook, your application calls a subscriber when an event occurs.",
    "The endpoint receiving the event is often public, and a request reaching that endpoint is not trustworthy by itself.",
    "The best designs centralize these decisions so every handler does not invent its own rules.",
    "The implementation may look like a small route, but it often sits on top of important business behavior.",
    "A provider sends an event to a consumer when something happens, and the consumer decides what to do with that event."
  ]
};
