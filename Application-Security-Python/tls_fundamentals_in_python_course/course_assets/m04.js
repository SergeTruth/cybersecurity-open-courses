window.COURSE_MODULE = {
  "title": "Certificate and Hostname Validation",
  "graphicAlt": "The client sends the intended hostname, validates chain and validity dates, and rejects an unrelated certificate; no fallback arrow leads to an unverified context after failure.",
  "narration": "Certificate validation asks whether the certificate presented by a service chains to a trusted authority under the expected rules. Hostname validation asks whether the certificate matches the service name the client intended to reach. Both checks are needed for sensitive connections. Encryption alone does not answer the identity question. A client can encrypt traffic to the wrong endpoint if identity validation is weakened or disabled.\n\nValidation also includes expiration, issuer trust, and name mismatch handling. If a certificate is expired, issued by an untrusted authority, or presented for a different name, the client should treat that as a meaningful failure. Production code should not turn those failures into silent warnings or insecure fallbacks. A certificate error is often an operational problem, but it is also a security signal that deserves investigation.\n\nDevelopment and production behavior should be clearly separated. Local test certificates, private development roots, and temporary endpoints may be acceptable in controlled development workflows, but those exceptions should not become general-purpose helpers used in production. The code and deployment process should make it hard to accidentally disable verification for real users, real credentials, or real data.\n\nCertificate pinning can be useful in some narrow designs, but it has operational tradeoffs. Pinning can make some unexpected trust changes visible, yet it can also break services during normal certificate rotation if it is not managed carefully. The durable principle is to validate the endpoint identity through a trust model that the team understands, monitors, and can operate. TLS validation failures should not be bypassed casually for convenience.",
  "narrationPoints": [
    "A client can encrypt traffic to the wrong endpoint if identity validation is weakened or disabled.",
    "A certificate error is often an operational problem, but it is also a security signal that deserves investigation.",
    "The code and deployment process should make it hard to accidentally disable verification for real users, real credentials, or real data.",
    "Certificate pinning can be useful in some narrow designs, but it has operational tradeoffs.",
    "Hostname validation asks whether the certificate matches the service name the client intended to reach.",
    "Certificate validation asks whether the certificate presented by a service chains to a trusted authority under the expected rules."
  ]
};
