window.COURSE_MODULE = {
  "title": "Certificates, Keys, and PKI Concepts",
  "graphicAlt": "An endpoint certificate links service DNS names to a public key through intermediate and root certificates, while the matching private key sits in a restrictive server-only file.",
  "narration": "Certificates support identity validation by binding a name or identity to a public key under a trust model. The matching private key must remain protected by the service or subject that owns the identity. When a Python client connects to a service, it can examine the certificate presented by that service and decide whether the identity and trust chain match what the client expected.\n\nCertificate authorities and certificate chains make that trust decision scalable. A certificate chain connects an endpoint certificate through one or more intermediate certificates to a trusted root certificate. Trust stores contain roots or authorities that the client accepts for validation. Public certificate authorities, private certificate authorities, and self-signed certificates can all appear in real systems, but each has different operational and trust implications.\n\nNames matter. Certificates contain identity information such as subject names and subject alternative names at a high level. The client must compare the intended service name with the names in the certificate according to expected validation rules. Expiration also matters because certificates are issued for a limited period. A certificate that was valid yesterday may fail tomorrow if renewal is not planned and monitored.\n\nPrivate key protection is central. If a private key is exposed, the trust in the associated service identity may be undermined depending on the system and exposure. Keys should not be committed to source code, copied into broad logs, stored in shared folders, or handled casually in support bundles. Certificate management is not only about getting a certificate issued. It is about protecting the private key, documenting ownership, and managing lifecycle safely.",
  "narrationPoints": [
    "The matching private key must remain protected by the service or subject that owns the identity.",
    "A certificate chain connects an endpoint certificate through one or more intermediate certificates to a trusted root certificate.",
    "Certificates contain identity information such as subject names and subject alternative names at a high level.",
    "Keys should not be committed to source code, copied into broad logs, stored in shared folders, or handled casually in support bundles.",
    "A certificate that was valid yesterday may fail tomorrow if renewal is not planned and monitored.",
    "Certificates support identity validation by binding a name or identity to a public key under a trust model."
  ]
};
