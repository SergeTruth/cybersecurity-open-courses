window.COURSE_MODULE = {
  "title": "Testing, Monitoring, and TLS Lifecycle Management",
  "graphicAlt": "A lifecycle dashboard tracks days to expiry, renewal status, handshake failures, hostname rejection, and context generation; a lock swaps verified contexts for new connections during rotation.",
  "narration": "TLS security has to be maintained over time. Tests should confirm that production-like clients validate certificates, reject unexpected names, handle expired or untrusted certificates safely at a high level, and do not inherit development-only verification bypasses. Server deployment tests should confirm that HTTPS assumptions, proxy behavior, secure cookies, and backend trust boundaries match the intended architecture.\n\nDependency and library review matters because TLS behavior is affected by HTTP clients, database drivers, message clients, runtimes, operating system trust stores, container images, and platform defaults. Guidance on protocol versions and cipher choices changes over time, so durable engineering practice focuses on using maintained libraries, current platform defaults, clear configuration ownership, and timely updates rather than hardcoded folklore.\n\nLogging and monitoring should make TLS failures visible without leaking sensitive material. Logs can include safe error categories, service names, certificate expiration warnings, connection error trends, and correlation identifiers. They should avoid private keys, tokens, credentials, full sensitive payloads, or unnecessary certificate material. Monitoring should watch certificate expiration, renewal failures, handshake errors, configuration drift, and unusual connection failures.\n\nIncident response planning should include private key exposure, misissued certificates, unexpected trust-store changes, and failed renewals. Teams should know who owns the certificate, where the private key lives, which services use it, how to rotate or revoke it, and how to validate recovery. TLS lifecycle management is not a one-time setup task. It is an ongoing operational responsibility that follows services, domains, dependencies, and infrastructure changes.",
  "narrationPoints": [
    "Server deployment tests should confirm that HTTPS assumptions, proxy behavior, secure cookies, and backend trust boundaries match the intended architecture.",
    "Dependency and library review matters because TLS behavior is affected by HTTP clients, database drivers, message clients, runtimes, operating system trust stores, container images, and platform defaults.",
    "Logs can include safe error categories, service names, certificate expiration warnings, connection error trends, and correlation identifiers.",
    "Incident response planning should include private key exposure, misissued certificates, unexpected trust-store changes, and failed renewals.",
    "Monitoring should watch certificate expiration, renewal failures, handshake errors, configuration drift, and unusual connection failures.",
    "Logging and monitoring should make TLS failures visible without leaking sensitive material."
  ]
};
