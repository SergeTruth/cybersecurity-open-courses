window.COURSE_MODULE = {
  "title": "TLS for Python Servers and Web Applications",
  "graphicAlt": "A server SSL context loads certificate and key, requires a modern minimum version, advertises HTTP, disables compression, and completes each accepted handshake within four seconds.",
  "narration": "Many Python web applications run behind application servers, reverse proxies, load balancers, or managed platforms that handle TLS termination. TLS termination is the point where protected traffic is decrypted and handed to another layer or service. Some architectures terminate at the edge and use a separate trusted path internally. Others re-encrypt traffic to backends. Teams should know which model they use and who owns each boundary.\n\nTLS deployment affects application behavior. Secure cookies should align with HTTPS behavior so session data is not exposed over weaker transport. HSTS concepts can help browsers prefer secure connections for a site when deployed correctly. Trusted proxy headers should be handled carefully because the Python application may rely on forwarded scheme, host, or client information. The application should not trust spoofable headers unless the proxy boundary is controlled.\n\nDevelopment certificates and production certificates should not be confused. Local development may use test certificates or local endpoints. Production certificates require ownership, renewal, deployment coordination, private key protection, and monitoring. Admin panels and internal services deserve particular attention because teams sometimes assume limited network exposure is enough. Sensitive server endpoints should still have clear transport, authentication, authorization, and monitoring expectations.\n\nTerminating TLS does not remove the need for authentication, authorization, secure session management, safe input handling, or response security. TLS protects the communication path to a point in the architecture. Application policy decides who can do what after the request arrives. Server-side TLS configuration should have clear ownership between application, infrastructure, and operations teams so renewal, incidents, and configuration changes do not fall through the cracks.",
  "narrationPoints": [
    "TLS termination is the point where protected traffic is decrypted and handed to another layer or service.",
    "Secure cookies should align with HTTPS behavior so session data is not exposed over weaker transport.",
    "Admin panels and internal services deserve particular attention because teams sometimes assume limited network exposure is enough.",
    "Terminating TLS does not remove the need for authentication, authorization, secure session management, safe input handling, or response security.",
    "HSTS concepts can help browsers prefer secure connections for a site when deployed correctly.",
    "Many Python web applications run behind application servers, reverse proxies, load balancers, or managed platforms that handle TLS termination."
  ]
};
