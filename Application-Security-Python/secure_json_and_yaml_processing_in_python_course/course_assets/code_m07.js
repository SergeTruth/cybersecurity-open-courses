window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Resource Limits and Parser Robustness through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Enforce strict JSON depth and node budgets",
      "language": "python",
      "blurb": "Byte-bounded decoding first scans structural nesting outside strings, then rejects duplicate object members, NaN, infinity, exponent overflow, oversized numbers, aggregate nodes, and excessive depth.",
      "code": "import json\nimport math\n\nclass JSONTreeError(ValueError):\n    pass\n\ndef duplicate_rejecting_object(pairs: list[tuple[str, object]]) -> dict[str, object]:\n    result: dict[str, object] = {}\n    for key, value in pairs:\n        if key in result:\n            raise JSONTreeError(\"duplicate JSON member rejected\")\n        result[key] = value\n    return result\n\ndef bounded_json_int(text: str) -> int:\n    digits = text[1:] if text.startswith(\"-\") else text\n    if len(digits) > 18:\n        raise JSONTreeError(\"JSON integer rejected\")\n    value = int(text)\n    if abs(value) > 1_000_000_000_000_000:\n        raise JSONTreeError(\"JSON integer rejected\")\n    return value\n\ndef bounded_json_float(text: str) -> float:\n    if len(text) > 64:\n        raise JSONTreeError(\"JSON float rejected\")\n    value = float(text)\n    if not math.isfinite(value) or abs(value) > 1_000_000_000_000:\n        raise JSONTreeError(\"JSON float rejected\")\n    return value\n\ndef reject_nonstandard_number(value: str):\n    raise JSONTreeError(\"non-standard JSON number rejected\")\n\ndef preflight_json_depth(raw: bytes, max_depth: int) -> None:\n    depth = 0\n    in_string = False\n    escaped = False\n    for byte in raw:\n        if in_string:\n            if escaped:\n                escaped = False\n            elif byte == 0x5C:\n                escaped = True\n            elif byte == 0x22:\n                in_string = False\n            continue\n        if byte == 0x22:\n            in_string = True\n        elif byte in (0x5B, 0x7B):\n            depth += 1\n            if depth > max_depth:\n                raise JSONTreeError(\"JSON complexity limit exceeded\")\n        elif byte in (0x5D, 0x7D) and depth > 0:\n            depth -= 1\n\ndef bounded_json_tree(raw: bytes, max_nodes: int = 2000, max_depth: int = 20) -> object:\n    if type(raw) is not bytes or len(raw) > 64_000:\n        raise JSONTreeError(\"JSON byte limit exceeded\")\n    preflight_json_depth(raw, max_depth)\n    try:\n        document = json.loads(\n            raw.decode(\"utf-8\"),\n            object_pairs_hook=duplicate_rejecting_object,\n            parse_int=bounded_json_int,\n            parse_float=bounded_json_float,\n            parse_constant=reject_nonstandard_number,\n        )\n    except JSONTreeError:\n        raise\n    except (UnicodeDecodeError, json.JSONDecodeError, RecursionError, ValueError):\n        raise JSONTreeError(\"JSON parse rejected\") from None\n    pending = [(document, 0)]\n    nodes = 0\n    while pending:\n        value, depth = pending.pop()\n        nodes += 1\n        if nodes > max_nodes or depth > max_depth:\n            raise JSONTreeError(\"JSON complexity limit exceeded\")\n        if isinstance(value, dict):\n            pending.extend((item, depth + 1) for item in value.values())\n        elif isinstance(value, list):\n            pending.extend((item, depth + 1) for item in value)\n        elif isinstance(value, float) and not math.isfinite(value):\n            raise JSONTreeError(\"JSON float rejected\")\n    return document\n"
    },
    {
      "title": "Read a YAML stream through size and document limits",
      "language": "python",
      "blurb": "The stream is capped before token scanning rejects aliases and duplicate-key rejecting safe parsing, only one document is accepted, and the loaded value must be a mapping with a finite key count.",
      "code": "import yaml\nfrom yaml.tokens import AliasToken\n\nclass YAMLBoundaryError(ValueError):\n    pass\n\nclass UniqueKeySafeLoader(yaml.SafeLoader):\n    pass\n\ndef construct_unique_mapping(loader, node, deep=False):\n    loader.flatten_mapping(node)\n    result = {}\n    for key_node, value_node in node.value:\n        key = loader.construct_object(key_node, deep=deep)\n        try:\n            duplicate = key in result\n        except TypeError:\n            raise YAMLBoundaryError(\"YAML mapping key rejected\") from None\n        if duplicate:\n            raise YAMLBoundaryError(\"duplicate YAML key rejected\")\n        result[key] = loader.construct_object(value_node, deep=deep)\n    return result\n\nUniqueKeySafeLoader.add_constructor(\n    yaml.resolver.BaseResolver.DEFAULT_MAPPING_TAG,\n    construct_unique_mapping,\n)\n\n\ndef read_one_yaml_document(stream) -> dict[str, object]:\n    try:\n        raw = stream.read(32_769)\n    except Exception:\n        raise YAMLBoundaryError(\"YAML stream read rejected\") from None\n    if type(raw) is not bytes or not raw or len(raw) > 32_768:\n        raise YAMLBoundaryError(\"YAML stream size rejected\")\n    try:\n        text = raw.decode(\"utf-8\")\n        token_count = 0\n        for token in yaml.scan(text, Loader=yaml.SafeLoader):\n            token_count += 1\n            if isinstance(token, AliasToken):\n                raise YAMLBoundaryError(\"YAML aliases rejected\")\n            if token_count > 4000:\n                raise YAMLBoundaryError(\"YAML complexity budget exceeded\")\n        documents = list(yaml.load_all(text, Loader=UniqueKeySafeLoader))\n    except YAMLBoundaryError:\n        raise\n    except (UnicodeDecodeError, yaml.YAMLError, ValueError):\n        raise YAMLBoundaryError(\"YAML stream parse rejected\") from None\n    if len(documents) != 1 or not isinstance(documents[0], dict):\n        raise YAMLBoundaryError(\"one YAML mapping required\")\n    if len(documents[0]) > 100 or any(not isinstance(key, str) for key in documents[0]):\n        raise YAMLBoundaryError(\"YAML mapping budget rejected\")\n    return documents[0]\n"
    }
  ]
};
