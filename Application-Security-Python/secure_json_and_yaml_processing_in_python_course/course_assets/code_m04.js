window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Safe YAML Parsing Principles through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Apply an exact schema after safe YAML loading",
      "language": "python",
      "blurb": "Safe loading prevents arbitrary object construction, while alias rejection, duplicate-key rejection, depth budgets, and explicit shape/value checks keep unknown configuration fields within policy.",
      "code": "import yaml\nfrom yaml.tokens import (\n    AliasToken,\n    BlockEndToken,\n    BlockMappingStartToken,\n    BlockSequenceStartToken,\n    FlowMappingEndToken,\n    FlowMappingStartToken,\n    FlowSequenceEndToken,\n    FlowSequenceStartToken,\n)\n\nclass YAMLBoundaryError(ValueError):\n    pass\n\nclass UniqueKeySafeLoader(yaml.SafeLoader):\n    pass\n\ndef construct_unique_mapping(loader, node, deep=False):\n    loader.flatten_mapping(node)\n    result = {}\n    for key_node, value_node in node.value:\n        key = loader.construct_object(key_node, deep=deep)\n        try:\n            duplicate = key in result\n        except TypeError:\n            raise YAMLBoundaryError(\"YAML mapping key rejected\") from None\n        if duplicate:\n            raise YAMLBoundaryError(\"duplicate YAML key rejected\")\n        result[key] = loader.construct_object(value_node, deep=deep)\n    return result\n\nUniqueKeySafeLoader.add_constructor(\n    yaml.resolver.BaseResolver.DEFAULT_MAPPING_TAG,\n    construct_unique_mapping,\n)\n\ndef preflight_yaml_structure(text: str, *, max_tokens: int = 4000, max_depth: int = 40) -> None:\n    starts = (BlockMappingStartToken, BlockSequenceStartToken, FlowMappingStartToken, FlowSequenceStartToken)\n    ends = (BlockEndToken, FlowMappingEndToken, FlowSequenceEndToken)\n    token_count = 0\n    depth = 0\n    for token in yaml.scan(text, Loader=yaml.SafeLoader):\n        token_count += 1\n        if isinstance(token, AliasToken):\n            raise YAMLBoundaryError(\"YAML aliases rejected\")\n        if isinstance(token, starts):\n            depth += 1\n            if depth > max_depth:\n                raise YAMLBoundaryError(\"YAML complexity budget exceeded\")\n        elif isinstance(token, ends) and depth > 0:\n            depth -= 1\n        if token_count > max_tokens:\n            raise YAMLBoundaryError(\"YAML complexity budget exceeded\")\n\n\ndef parse_service_yaml(raw: bytes) -> dict[str, object]:\n    if type(raw) is not bytes or not 1 <= len(raw) <= 16_384:\n        raise YAMLBoundaryError(\"YAML byte limit rejected\")\n    try:\n        text = raw.decode(\"utf-8\")\n        preflight_yaml_structure(text)\n        document = yaml.load(text, Loader=UniqueKeySafeLoader)\n    except YAMLBoundaryError:\n        raise\n    except (UnicodeDecodeError, yaml.YAMLError, RecursionError, ValueError):\n        raise YAMLBoundaryError(\"service YAML parse rejected\") from None\n    if not isinstance(document, dict) or set(document) != {\"replicas\", \"region\", \"features\"}:\n        raise YAMLBoundaryError(\"service YAML shape rejected\")\n    if type(document[\"replicas\"]) is not int or not 1 <= document[\"replicas\"] <= 20:\n        raise YAMLBoundaryError(\"replica count rejected\")\n    region = document[\"region\"]\n    if not isinstance(region, str) or region not in {\"us-east\", \"us-west\"}:\n        raise YAMLBoundaryError(\"service region rejected\")\n    features = document[\"features\"]\n    allowed_features = {\"audit\", \"metrics\"}\n    if (\n        not isinstance(features, list)\n        or len(features) > 10\n        or any(not isinstance(item, str) or item not in allowed_features for item in features)\n    ):\n        raise YAMLBoundaryError(\"service feature list rejected\")\n    return document\n"
    },
    {
      "title": "Reject YAML aliases before construction",
      "language": "python",
      "blurb": "A token pass rejects aliases and enforces token and depth budgets before duplicate-key rejecting safe loading, reducing amplification and ambiguous mapping behavior.",
      "code": "import yaml\nfrom yaml.tokens import (\n    AliasToken,\n    BlockEndToken,\n    BlockMappingStartToken,\n    BlockSequenceStartToken,\n    FlowMappingEndToken,\n    FlowMappingStartToken,\n    FlowSequenceEndToken,\n    FlowSequenceStartToken,\n)\n\nclass YAMLBoundaryError(ValueError):\n    pass\n\nclass UniqueKeySafeLoader(yaml.SafeLoader):\n    pass\n\ndef construct_unique_mapping(loader, node, deep=False):\n    loader.flatten_mapping(node)\n    result = {}\n    for key_node, value_node in node.value:\n        key = loader.construct_object(key_node, deep=deep)\n        try:\n            duplicate = key in result\n        except TypeError:\n            raise YAMLBoundaryError(\"YAML mapping key rejected\") from None\n        if duplicate:\n            raise YAMLBoundaryError(\"duplicate YAML key rejected\")\n        result[key] = loader.construct_object(value_node, deep=deep)\n    return result\n\nUniqueKeySafeLoader.add_constructor(\n    yaml.resolver.BaseResolver.DEFAULT_MAPPING_TAG,\n    construct_unique_mapping,\n)\n\ndef preflight_yaml_structure(text: str, *, max_tokens: int = 4000, max_depth: int = 40) -> None:\n    starts = (BlockMappingStartToken, BlockSequenceStartToken, FlowMappingStartToken, FlowSequenceStartToken)\n    ends = (BlockEndToken, FlowMappingEndToken, FlowSequenceEndToken)\n    token_count = 0\n    depth = 0\n    for token in yaml.scan(text, Loader=yaml.SafeLoader):\n        token_count += 1\n        if isinstance(token, AliasToken):\n            raise YAMLBoundaryError(\"YAML aliases rejected\")\n        if isinstance(token, starts):\n            depth += 1\n            if depth > max_depth:\n                raise YAMLBoundaryError(\"YAML complexity budget exceeded\")\n        elif isinstance(token, ends) and depth > 0:\n            depth -= 1\n        if token_count > max_tokens:\n            raise YAMLBoundaryError(\"YAML complexity budget exceeded\")\n\n\ndef load_yaml_with_budgets(raw: bytes) -> object:\n    if type(raw) is not bytes or len(raw) > 32_768:\n        raise YAMLBoundaryError(\"YAML document too large\")\n    try:\n        text = raw.decode(\"utf-8\")\n        preflight_yaml_structure(text)\n        return yaml.load(text, Loader=UniqueKeySafeLoader)\n    except YAMLBoundaryError:\n        raise\n    except (UnicodeDecodeError, yaml.YAMLError, RecursionError, ValueError):\n        raise YAMLBoundaryError(\"YAML parse rejected\") from None\n"
    }
  ]
};
