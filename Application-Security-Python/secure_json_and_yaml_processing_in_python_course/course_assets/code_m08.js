window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Output, Logging, Storage, and Review through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Serialize only an approved public projection",
      "language": "python",
      "blurb": "Output construction validates and selects public fields and a version explicitly, including an opaque ID, bounded text, and a timezone-aware timestamp normalized to UTC.",
      "code": "import json\nimport re\nimport unicodedata\nfrom datetime import datetime, timezone\n\nPUBLIC_ID = re.compile(r\"usr_[A-Za-z0-9_-]{1,40}\")\n\ndef has_control_character(value: str) -> bool:\n    for character in value:\n        category = unicodedata.category(character)\n        if category[0] == \"C\" or category in {\"Zl\", \"Zp\"}:\n            return True\n    return False\n\ndef bounded_public_text(value: object, *, maximum: int, message: str) -> str:\n    if not isinstance(value, str) or not 1 <= len(value) <= maximum or has_control_character(value):\n        raise ValueError(message)\n    return value\n\ndef bounded_joined_at(value: object) -> str:\n    text = bounded_public_text(value, maximum=40, message=\"joined timestamp rejected\")\n    candidate = text[:-1] + \"+00:00\" if text.endswith(\"Z\") else text\n    try:\n        parsed = datetime.fromisoformat(candidate)\n    except ValueError:\n        raise ValueError(\"joined timestamp rejected\") from None\n    if parsed.tzinfo is None or parsed.utcoffset() is None:\n        raise ValueError(\"joined timestamp rejected\")\n    return parsed.astimezone(timezone.utc).isoformat().replace(\"+00:00\", \"Z\")\n\ndef public_user_json(user) -> bytes:\n    try:\n        public_id = user.public_id\n        display_name = user.display_name\n        joined_at = user.created_at.isoformat()\n    except Exception:\n        raise ValueError(\"public user projection rejected\") from None\n    if not isinstance(public_id, str) or PUBLIC_ID.fullmatch(public_id) is None:\n        raise ValueError(\"public user identifier rejected\")\n    document = {\n        \"version\": 1,\n        \"id\": public_id,\n        \"display_name\": bounded_public_text(display_name, maximum=80, message=\"display name rejected\"),\n        \"joined_at\": bounded_joined_at(joined_at),\n    }\n    return json.dumps(\n        document,\n        ensure_ascii=False,\n        allow_nan=False,\n        separators=(\",\", \":\"),\n        sort_keys=True,\n    ).encode(\"utf-8\")\n"
    },
    {
      "title": "Log parser failures without copying input",
      "language": "python",
      "blurb": "The event contains a stable parser, source, and error category while omitting raw documents, field values, stack-derived messages, and file paths.",
      "code": "PARSER_ERRORS = {\"invalid_utf8\", \"invalid_syntax\", \"schema_rejected\", \"resource_limit\"}\n\ndef record_parse_failure(logger, metrics, parser: str, source: str, category: str) -> None:\n    parsers = {\"json\", \"yaml\"}\n    sources = {\"api\", \"configuration\", \"queue\"}\n    safe_parser = parser if isinstance(parser, str) and parser in parsers else \"other\"\n    safe_source = source if isinstance(source, str) and source in sources else \"other\"\n    safe_category = category if isinstance(category, str) and category in PARSER_ERRORS else \"other\"\n    logger.warning(\n        \"structured_document_rejected\",\n        extra={\"parser\": safe_parser, \"source\": safe_source, \"category\": safe_category},\n    )\n    metrics.increment(\"document_rejections\", tags={\"parser\": safe_parser, \"category\": safe_category})\n"
    }
  ]
};
