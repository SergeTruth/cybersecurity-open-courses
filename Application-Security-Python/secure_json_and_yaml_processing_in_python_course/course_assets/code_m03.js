window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Safe JSON Parsing Principles through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Reject duplicate keys and non-standard JSON numbers",
      "language": "python",
      "blurb": "Strict decoder hooks reject duplicate object members, oversized integers, floats, NaN, infinity, and non-opaque job identifiers with bounded errors before an exact schema is returned.",
      "code": "import json\nimport re\n\nOPAQUE_JOB_ID = re.compile(r\"[A-Za-z0-9][A-Za-z0-9_-]{0,39}\")\n\nclass JobJSONError(ValueError):\n    pass\n\ndef unique_object(pairs: list[tuple[str, object]]) -> dict[str, object]:\n    result: dict[str, object] = {}\n    for key, value in pairs:\n        if key in result:\n            raise JobJSONError(\"duplicate JSON member rejected\")\n        result[key] = value\n    return result\n\ndef bounded_json_int(text: str) -> int:\n    digits = text[1:] if text.startswith(\"-\") else text\n    if len(digits) > 18:\n        raise JobJSONError(\"JSON integer rejected\")\n    value = int(text)\n    if abs(value) > 1_000_000_000_000_000:\n        raise JobJSONError(\"JSON integer rejected\")\n    return value\n\ndef reject_json_float(text: str):\n    raise JobJSONError(\"JSON float rejected\")\n\ndef reject_nonstandard_number(value: str):\n    raise JobJSONError(\"non-standard JSON number rejected\")\n\ndef decode_job_json(raw: bytes) -> object:\n    try:\n        return json.loads(\n            raw.decode(\"utf-8\"),\n            object_pairs_hook=unique_object,\n            parse_int=bounded_json_int,\n            parse_float=reject_json_float,\n            parse_constant=reject_nonstandard_number,\n        )\n    except JobJSONError:\n        raise\n    except (UnicodeDecodeError, json.JSONDecodeError, ValueError):\n        raise JobJSONError(\"job JSON parse rejected\") from None\n\ndef parse_job(raw: bytes) -> dict[str, object]:\n    if type(raw) is not bytes or not 1 <= len(raw) <= 16_384:\n        raise JobJSONError(\"JSON byte limit rejected\")\n    document = decode_job_json(raw)\n    if not isinstance(document, dict) or set(document) != {\"job_id\", \"attempt\"}:\n        raise JobJSONError(\"job JSON shape rejected\")\n    job_id = document[\"job_id\"]\n    if not isinstance(job_id, str) or OPAQUE_JOB_ID.fullmatch(job_id) is None:\n        raise JobJSONError(\"job identifier rejected\")\n    if type(document[\"attempt\"]) is not int or not 1 <= document[\"attempt\"] <= 5:\n        raise JobJSONError(\"job attempt rejected\")\n    return document\n"
    },
    {
      "title": "Decode a bounded JSON array into typed values",
      "language": "python",
      "blurb": "The parser requires UTF-8, a top-level list, a finite item count, and exact scalar types instead of filtering unknown input into an empty object.",
      "code": "import json\n\nclass MeasurementJSONError(ValueError):\n    pass\n\ndef reject_object(pairs: list[tuple[str, object]]) -> dict[str, object]:\n    seen = set()\n    for key, _ in pairs:\n        if key in seen:\n            raise MeasurementJSONError(\"duplicate JSON member rejected\")\n        seen.add(key)\n    return dict(pairs)\n\ndef bounded_json_int(text: str) -> int:\n    digits = text[1:] if text.startswith(\"-\") else text\n    if len(digits) > 8:\n        raise MeasurementJSONError(\"measurement integer rejected\")\n    value = int(text)\n    if not -1000 <= value <= 1000:\n        raise MeasurementJSONError(\"measurement value rejected\")\n    return value\n\ndef reject_json_float(text: str):\n    raise MeasurementJSONError(\"measurement float rejected\")\n\ndef reject_nonstandard_number(value: str):\n    raise MeasurementJSONError(\"non-standard JSON number rejected\")\n\ndef parse_measurements(raw: bytes) -> tuple[int, ...]:\n    if type(raw) is not bytes or not 1 <= len(raw) <= 8192:\n        raise MeasurementJSONError(\"measurement document too large\")\n    try:\n        values = json.loads(\n            raw.decode(\"utf-8\"),\n            object_pairs_hook=reject_object,\n            parse_int=bounded_json_int,\n            parse_float=reject_json_float,\n            parse_constant=reject_nonstandard_number,\n        )\n    except MeasurementJSONError:\n        raise\n    except (UnicodeDecodeError, json.JSONDecodeError, ValueError):\n        raise MeasurementJSONError(\"measurement JSON parse rejected\") from None\n    if not isinstance(values, list) or not 1 <= len(values) <= 100:\n        raise MeasurementJSONError(\"measurement array rejected\")\n    if any(type(value) is not int for value in values):\n        raise MeasurementJSONError(\"measurement value rejected\")\n    return tuple(values)\n"
    }
  ]
};
