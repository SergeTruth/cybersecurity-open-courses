window.COURSE_MODULE = {
  "title": "Browser Rendering and Trust Boundaries",
  "graphicAlt": "User input, stored profile data, third-party API values, server templates, JSON responses, frontend components, browser parsers, and DOM APIs are arranged across server and browser trust boundaries.",
  "narration": "Browsers interpret content according to context. HTML body text, HTML attributes, URLs, JavaScript strings, JSON data, and CSS each have different rules. The same string can be harmless in one context and unsafe in another because the browser is not simply displaying characters; it is parsing a document with rules for markup, navigation, scripting, styling, and data exchange. Secure rendering begins by knowing which context receives the data.\n\nUntrusted data can come from many places: user profiles, comments, support tickets, uploaded files, search terms, third-party APIs, logs, markdown, rich text editors, administrative fields, and system integrations. Internal does not automatically mean trusted. A support note entered by an employee, a value imported from a partner, or a log message shown in an admin portal can still cross into a browser context and affect users if it is rendered unsafely.\n\nServer-side rendering and client-side rendering move the trust boundary in different ways. A Django or Jinja template may combine data with HTML on the server. A FastAPI or Django API may return JSON that a frontend uses to update the page. A hybrid app may do both. In every case, data crosses from Python backend to browser frontend, and the team needs to understand how the final browser-visible output is created.\n\nDevelopers should track where data came from, where it is stored, how it is transformed, and where it is rendered. A value that is validated for length during input may later be used in an attribute, a URL, a script-adjacent context, a rich-text area, or a client-side component. XSS prevention becomes much easier when trust boundaries are visible and each rendering path has an intentional safety rule.",
  "narrationPoints": [
    "HTML body text, HTML attributes, URLs, JavaScript strings, JSON data, and CSS each have different rules.",
    "Untrusted data can come from many places: user profiles, comments, support tickets, uploaded files, search terms, third-party APIs, logs, markdown, rich text editors, administrative fields, and system integrations.",
    "A FastAPI or Django API may return JSON that a frontend uses to update the page.",
    "XSS prevention becomes much easier when trust boundaries are visible and each rendering path has an intentional safety rule.",
    "A Django or Jinja template may combine data with HTML on the server.",
    "Server-side rendering and client-side rendering move the trust boundary in different ways."
  ]
};
