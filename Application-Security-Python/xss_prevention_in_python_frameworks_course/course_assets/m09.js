window.COURSE_MODULE = {
  "title": "Course Summary and Key Takeaways",
  "graphicAlt": "The XSS defense map links trusted templates, contextual encoding, URL validation, safe JSON embedding, rich-text sanitization, safe DOM sinks, nonce-based CSP, tests, review, telemetry, and remediation.",
  "narration": "XSS prevention in Python frameworks depends on understanding browser contexts and keeping untrusted data out of executable or unsafe rendering paths. Django, Flask, FastAPI, Jinja templates, APIs, admin portals, internal tools, and hybrid frontends all create browser-facing output in different ways. The framework can help, but the team still needs to understand where data crosses from backend storage or API responses into the browser.\n\nStrong prevention uses context-aware output encoding, safe template defaults, careful rich-content handling, response minimization, secure frontend integration, CSP defense in depth, testing, review, and remediation. Input validation is useful, but it does not replace output safety. Sanitization is useful for rich content, but only when backed by a clear policy and maintained over time. CSP is useful defense in depth, but it does not make unsafe rendering patterns acceptable.\n\nFramework protections are valuable but not automatic guarantees. Autoescaping can be bypassed. Helpers can mark content as safe. APIs can return data that frontend code inserts unsafely. Admin screens can render imported or user-controlled values. Logs and error pages can become browser output. A practical review treats all of these as rendering paths that deserve the right control for the right context.\n\nThe goal is to make unsafe rendering difficult, safe rendering routine, and browser-facing output predictable, reviewable, and maintainable. When teams track trust boundaries, encode for context, use vetted sanitization, minimize responses, test risky paths, and monitor browser-facing failures, XSS prevention becomes a normal part of application engineering rather than a last-minute cleanup task.",
  "narrationPoints": [
    "Django, Flask, FastAPI, Jinja templates, APIs, admin portals, internal tools, and hybrid frontends all create browser-facing output in different ways.",
    "CSP is useful defense in depth, but it does not make unsafe rendering patterns acceptable.",
    "A practical review treats all of these as rendering paths that deserve the right control for the right context.",
    "The goal is to make unsafe rendering difficult, safe rendering routine, and browser-facing output predictable, reviewable, and maintainable.",
    "Sanitization is useful for rich content, but only when backed by a clear policy and maintained over time.",
    "XSS prevention in Python frameworks depends on understanding browser contexts and keeping untrusted data out of executable or unsafe rendering paths."
  ]
};
