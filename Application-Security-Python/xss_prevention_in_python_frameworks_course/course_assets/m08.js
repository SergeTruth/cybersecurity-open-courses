window.COURSE_MODULE = {
  "title": "Testing, Review, Logging, and Remediation",
  "graphicAlt": "Regression payloads exercise text, attribute, URL, JSON, rich-content, and frontend paths; CSP reports map unexpected directive strings to other before finite metrics.",
  "narration": "Code review for XSS prevention should follow data into browser contexts. Reviewers can ask where untrusted data enters, where it is stored, which templates or API responses expose it, and how the frontend renders it. Useful review focuses include unsafe rendering shortcuts, disabled escaping, raw HTML insertion, unreviewed template filters, rich-content handling, URL construction, error pages, admin screens, and client-side DOM updates.\n\nTests should verify escaping and sanitization behavior for the fields and rendering paths that matter most. Unit and integration tests can check that known risky fields remain text, that rich-content policies preserve allowed formatting while removing unsafe content, and that templates continue using safe defaults. Regression tests are especially useful after template refactors, frontend rewrites, sanitization library updates, or changes in how API data is rendered.\n\nLogging should support investigation without collecting unnecessary browser-side secrets, credentials, tokens, session data, or sensitive personal information. Client-side errors and CSP reports can help identify rendering problems, but they should be handled as potentially sensitive operational data. Monitoring unusual client-side errors or policy reports can reveal broken rendering paths, misconfigured defenses, or unexpected frontend behavior.\n\nRemediation should address the root rendering path rather than only the visible symptom. That may mean restoring autoescaping, replacing unsafe raw insertion, adding a content policy, re-sanitizing stored content, narrowing API responses, updating tests, or reviewing related templates that use the same helper. XSS prevention requires continuous review because templates, APIs, frontend code, and content policies change over time.",
  "narrationPoints": [
    "Code review for XSS prevention should follow data into browser contexts.",
    "Tests should verify escaping and sanitization behavior for the fields and rendering paths that matter most.",
    "Logging should support investigation without collecting unnecessary browser-side secrets, credentials, tokens, session data, or sensitive personal information.",
    "XSS prevention requires continuous review because templates, APIs, frontend code, and content policies change over time.",
    "Monitoring unusual client-side errors or policy reports can reveal broken rendering paths, misconfigured defenses, or unexpected frontend behavior.",
    "Remediation should address the root rendering path rather than only the visible symptom."
  ]
};
