window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply TLS, Certificates, and Transport Validation through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Use a verified TLS context in an HTTPS connection",
      "language": "python",
      "blurb": "The configured context is passed to the actual transport, which connects only to the expected hostname and enforces a minimum TLS version.",
      "code": "import http.client\nimport ssl\n\ndef open_api_connection(ca_file: str) -> http.client.HTTPSConnection:\n    context = ssl.create_default_context(cafile=ca_file)\n    context.minimum_version = ssl.TLSVersion.TLSv1_2\n    context.check_hostname = True\n    context.verify_mode = ssl.CERT_REQUIRED\n    return http.client.HTTPSConnection(\n        \"api.example.com\",\n        port=443,\n        timeout=5,\n        context=context,\n    )\n"
    },
    {
      "title": "Apply deliberate TLS policy to direct and proxy connections",
      "language": "python",
      "blurb": "The adapter supplies verified TLS contexts for destination and approved HTTPS-proxy TLS, while the wrapper owns the method, destination, headers, timeout, verification, proxy mode, and final prepared request for named operations only.",
      "code": "import ssl\nimport requests\nimport unicodedata\nfrom urllib.parse import urlsplit\nfrom requests.adapters import HTTPAdapter\n\nTLS_OPERATIONS = {\"health_check\": \"/v1/health\", \"status_check\": \"/v1/status\"}\nAPPROVED_HOST = \"api.example.com\"\nAPPROVED_HTTPS_PROXY = \"https://proxy.example:8443\"\nAPPROVED_HEADERS = {\"accept\": \"application/json\", \"user-agent\": \"orders-service/1\"}\nOWNED_SEND_OPTIONS = {\n    \"allow_redirects\": False,\n    \"timeout\": (2, 5),\n    \"proxies\": {},\n    \"stream\": False,\n    \"verify\": True,\n    \"cert\": None,\n}\n\nclass TLSPolicyError(RuntimeError):\n    pass\n\ndef has_disallowed_url_character(value: str) -> bool:\n    for character in value:\n        category = unicodedata.category(character)\n        if character.isspace() or category[0] == \"Z\" or category in {\"Cc\", \"Cf\"}:\n            return True\n    return False\n\nclass TLSPolicySession(requests.Session):\n    def __init__(self, proxy_map: dict[str, str] | None = None):\n        super().__init__()\n        self._owned_send_options = dict(OWNED_SEND_OPTIONS)\n        self._owned_send_options[\"proxies\"] = dict(proxy_map or {})\n\n    def _validate_method(self, method: object) -> None:\n        if not isinstance(method, str) or method.upper() != \"GET\":\n            raise ValueError(\"approved TLS client method required\")\n\n    def _validate_raw_url(self, url: object) -> None:\n        if not isinstance(url, str) or not 1 <= len(url) <= 2048 or has_disallowed_url_character(url):\n            raise ValueError(\"approved TLS client URL required\")\n\n    def _validate_prepared(self, prepared) -> None:\n        self._validate_method(prepared.method)\n        self._validate_raw_url(prepared.url)\n        parts = urlsplit(prepared.url)\n        if (\n            parts.scheme != \"https\"\n            or parts.netloc != APPROVED_HOST\n            or parts.path not in set(TLS_OPERATIONS.values())\n            or parts.query\n            or parts.fragment\n            or prepared.body not in (None, b\"\", \"\")\n        ):\n            raise ValueError(\"approved TLS client request required\")\n        lowered_headers = {}\n        for name, value in prepared.headers.items():\n            if not isinstance(name, str) or not isinstance(value, str):\n                raise ValueError(\"approved TLS client headers required\")\n            lower_name = name.lower()\n            if lower_name in lowered_headers:\n                raise ValueError(\"approved TLS client headers required\")\n            lowered_headers[lower_name] = value\n        if lowered_headers != APPROVED_HEADERS:\n            raise ValueError(\"approved TLS client headers required\")\n\n    def _owned_send_kwargs(self, kwargs: dict[str, object]) -> dict[str, object]:\n        unknown = set(kwargs) - set(self._owned_send_options)\n        if unknown:\n            raise ValueError(\"TLS client transport options are owned by the client\")\n        for name, expected in self._owned_send_options.items():\n            if kwargs.get(name) != expected:\n                raise ValueError(\"TLS client transport options are owned by the client\")\n        return dict(self._owned_send_options)\n\n    def request(self, method, url, **kwargs):\n        self._validate_method(method)\n        self._validate_raw_url(url)\n        if kwargs:\n            raise ValueError(\"TLS client request options are owned by the client\")\n        return super().request(method, url, **dict(self._owned_send_options))\n\n    def get(self, url, **kwargs):\n        if kwargs:\n            raise ValueError(\"TLS client request options are owned by the client\")\n        return self.request(\"GET\", url)\n\n    def prepare_request(self, request):\n        self._validate_method(getattr(request, \"method\", None))\n        self._validate_raw_url(getattr(request, \"url\", None))\n        prepared = super().prepare_request(request)\n        self._validate_prepared(prepared)\n        return prepared\n\n    def send(self, request, **kwargs):\n        self._validate_prepared(request)\n        return super().send(request, **self._owned_send_kwargs(kwargs))\n\nclass TLSContextAdapter(HTTPAdapter):\n    def __init__(self, destination_context: ssl.SSLContext, proxy_context: ssl.SSLContext):\n        self.destination_context = destination_context\n        self.proxy_context = proxy_context\n        super().__init__()\n\n    def init_poolmanager(self, connections, maxsize, block=False, **kwargs):\n        kwargs[\"ssl_context\"] = self.destination_context\n        return super().init_poolmanager(connections, maxsize, block=block, **kwargs)\n\n    def proxy_manager_for(self, proxy, **proxy_kwargs):\n        if proxy != APPROVED_HTTPS_PROXY:\n            raise ValueError(\"approved HTTPS proxy required\")\n        proxy_kwargs[\"ssl_context\"] = self.destination_context\n        proxy_kwargs[\"proxy_ssl_context\"] = self.proxy_context\n        return super().proxy_manager_for(proxy, **proxy_kwargs)\n\ndef tls_policy_session(destination_ca: str, proxy_ca: str, *, through_approved_proxy: bool = False) -> TLSPolicySession:\n    if type(through_approved_proxy) is not bool:\n        raise ValueError(\"proxy mode rejected\")\n    destination_context = ssl.create_default_context(cafile=destination_ca)\n    destination_context.minimum_version = ssl.TLSVersion.TLSv1_2\n    proxy_context = ssl.create_default_context(cafile=proxy_ca)\n    proxy_context.minimum_version = ssl.TLSVersion.TLSv1_2\n    proxy_map = {\"https\": APPROVED_HTTPS_PROXY} if through_approved_proxy else {}\n    session = TLSPolicySession(proxy_map)\n    session.trust_env = False\n    session.proxies.clear()\n    session.adapters.pop(\"http://\", None)\n    session.headers.clear()\n    session.headers.update({\"Accept\": \"application/json\", \"User-Agent\": \"orders-service/1\"})\n    session.mount(\"https://\", TLSContextAdapter(destination_context, proxy_context))\n    return session\n\nclass TLSAPIClient:\n    def __init__(self, session: TLSPolicySession):\n        self._session = session\n\n    def get(self, operation: str):\n        if not isinstance(operation, str) or operation not in TLS_OPERATIONS:\n            raise ValueError(\"approved TLS client operation required\")\n        try:\n            return self._session.request(\"GET\", \"https://api.example.com\" + TLS_OPERATIONS[operation])\n        except ValueError:\n            raise\n        except Exception:\n            raise TLSPolicyError(\"TLS API request failed\") from None\n\ndef verified_client(destination_ca: str, proxy_ca: str, *, through_approved_proxy: bool = False) -> TLSAPIClient:\n    return TLSAPIClient(tls_policy_session(destination_ca, proxy_ca, through_approved_proxy=through_approved_proxy))\n"
    }
  ]
};
