window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Redirects, Proxies, and Cross-Boundary Behavior through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Revalidate every redirect authority",
      "language": "python",
      "blurb": "Each redirect hop rejects whitespace, Unicode separators, controls, credentials, alternate ports, fragments, and unapproved hosts before another request is sent.",
      "code": "import unicodedata\nfrom urllib.parse import urljoin, urlsplit\n\nAPPROVED_API_HOSTS = {\"api.example.com\", \"api-backup.example.com\"}\n\ndef has_disallowed_url_character(value: str) -> bool:\n    for character in value:\n        category = unicodedata.category(character)\n        if character.isspace() or category[0] == \"Z\" or category in {\"Cc\", \"Cf\"}:\n            return True\n    return False\n\ndef approved_redirect_target(url: object):\n    if not isinstance(url, str) or not 1 <= len(url) <= 2048 or has_disallowed_url_character(url):\n        raise ValueError(\"redirect destination rejected\")\n    target = urlsplit(url)\n    try:\n        port = target.port\n    except ValueError:\n        raise ValueError(\"invalid redirect authority\") from None\n    if (\n        target.scheme != \"https\" or target.hostname not in APPROVED_API_HOSTS\n        or target.username is not None or target.password is not None\n        or port not in {None, 443} or target.fragment\n    ):\n        raise ValueError(\"redirect destination rejected\")\n    return target\n\ndef close_after_redirect(response, primary_error: BaseException | None = None) -> None:\n    close = getattr(response, \"close\", None)\n    if not callable(close):\n        cleanup_error = TypeError(\"redirect response close capability rejected\")\n    else:\n        try:\n            close()\n        except BaseException as error:\n            cleanup_error = error\n        else:\n            cleanup_error = None\n    if cleanup_error is not None:\n        if primary_error is not None:\n            raise BaseExceptionGroup(\n                \"redirect handling and response cleanup failed\",\n                [primary_error, cleanup_error],\n            )\n        raise RuntimeError(\"redirect response cleanup failed\") from cleanup_error\n    if primary_error is not None:\n        raise primary_error\n\ndef get_approved_redirects(session, url: str):\n    current = url\n    for _ in range(4):\n        approved_redirect_target(current)\n        try:\n            response = session.get(current, allow_redirects=False, timeout=(3, 8))\n        except Exception:\n            raise RuntimeError(\"redirect request failed\") from None\n        try:\n            try:\n                is_redirect = response.is_redirect\n            except Exception:\n                raise ValueError(\"redirect response rejected\") from None\n            if type(is_redirect) is not bool:\n                raise ValueError(\"redirect response rejected\")\n            if not is_redirect:\n                return response\n            try:\n                location = response.headers[\"Location\"]\n            except Exception:\n                raise ValueError(\"redirect location rejected\") from None\n            if not isinstance(location, str) or not 1 <= len(location) <= 2048 or has_disallowed_url_character(location):\n                raise ValueError(\"redirect location rejected\")\n            try:\n                next_url = urljoin(current, location)\n            except Exception:\n                raise ValueError(\"redirect location rejected\") from None\n            approved_redirect_target(next_url)\n        except BaseException as error:\n            close_after_redirect(response, error)\n            raise AssertionError(\"unreachable\")\n        close_after_redirect(response)\n        current = next_url\n    raise RuntimeError(\"redirect limit exceeded\")\n"
    },
    {
      "title": "Disable ambient proxy inheritance for a direct API client",
      "language": "python",
      "blurb": "The session ignores process proxy variables, owns its headers and transport options, blocks caller-supplied request settings, and validates the final prepared request for one of the named direct API operations.",
      "code": "import requests\nimport unicodedata\nfrom urllib.parse import urlsplit\n\nAPPROVED_DIRECT_PATHS = {\"/v1/health\", \"/v1/status\"}\nAPPROVED_HEADERS = {\"accept\": \"application/json\", \"user-agent\": \"orders-service/1\"}\nAPPROVED_METHODS = {\"GET\"}\nOWNED_SEND_OPTIONS = {\n    \"allow_redirects\": False,\n    \"timeout\": (2, 5),\n    \"proxies\": {},\n    \"stream\": False,\n    \"verify\": True,\n    \"cert\": None,\n}\n\nclass DirectAPIError(RuntimeError):\n    pass\n\ndef has_disallowed_url_character(value: str) -> bool:\n    for character in value:\n        category = unicodedata.category(character)\n        if character.isspace() or category[0] == \"Z\" or category in {\"Cc\", \"Cf\"}:\n            return True\n    return False\n\ndef options_match_owned(name: str, value: object) -> bool:\n    expected = OWNED_SEND_OPTIONS[name]\n    return value == expected\n\nclass DirectAPISession(requests.Session):\n    def _validate_raw_url(self, url: object) -> None:\n        if not isinstance(url, str) or not 1 <= len(url) <= 2048 or has_disallowed_url_character(url):\n            raise ValueError(\"approved API URL required\")\n\n    def _validate_method(self, method: object) -> None:\n        if not isinstance(method, str) or method.upper() not in APPROVED_METHODS:\n            raise ValueError(\"approved API method required\")\n\n    def _validate_prepared(self, prepared) -> None:\n        self._validate_method(prepared.method)\n        self._validate_raw_url(prepared.url)\n        parts = urlsplit(prepared.url)\n        if (\n            parts.scheme != \"https\"\n            or parts.netloc != \"api.example.com\"\n            or parts.path not in APPROVED_DIRECT_PATHS\n            or parts.query\n            or parts.fragment\n        ):\n            raise ValueError(\"approved API URL required\")\n        if prepared.body not in (None, b\"\", \"\"):\n            raise ValueError(\"approved API request body required\")\n        lowered_headers = {}\n        for name, value in prepared.headers.items():\n            if not isinstance(name, str) or not isinstance(value, str):\n                raise ValueError(\"approved API headers required\")\n            lower_name = name.lower()\n            if lower_name in lowered_headers:\n                raise ValueError(\"approved API headers required\")\n            lowered_headers[lower_name] = value\n        if lowered_headers != APPROVED_HEADERS:\n            raise ValueError(\"approved API headers required\")\n\n    def _owned_send_kwargs(self, kwargs: dict[str, object]) -> dict[str, object]:\n        unknown = set(kwargs) - set(OWNED_SEND_OPTIONS)\n        if unknown:\n            raise ValueError(\"direct API transport options are owned by the client\")\n        for name, value in kwargs.items():\n            if not options_match_owned(name, value):\n                raise ValueError(\"direct API transport options are owned by the client\")\n        return dict(OWNED_SEND_OPTIONS)\n\n    def request(self, method, url, **kwargs):\n        self._validate_method(method)\n        self._validate_raw_url(url)\n        if kwargs:\n            raise ValueError(\"direct API request options are owned by the client\")\n        return super().request(method, url, **dict(OWNED_SEND_OPTIONS))\n\n    def get(self, url, **kwargs):\n        if kwargs:\n            raise ValueError(\"direct API request options are owned by the client\")\n        return self.request(\"GET\", url)\n\n    def prepare_request(self, request):\n        self._validate_method(getattr(request, \"method\", None))\n        self._validate_raw_url(getattr(request, \"url\", None))\n        prepared = super().prepare_request(request)\n        self._validate_prepared(prepared)\n        return prepared\n\n    def send(self, request, **kwargs):\n        self._validate_prepared(request)\n        return super().send(request, **self._owned_send_kwargs(kwargs))\n\ndef direct_api_session() -> DirectAPISession:\n    session = DirectAPISession()\n    session.trust_env = False\n    session.proxies.clear()\n    session.adapters.pop(\"http://\", None)\n    session.headers.clear()\n    session.headers.update({\"Accept\": \"application/json\", \"User-Agent\": \"orders-service/1\"})\n    return session\n\ndef fetch_direct(session: DirectAPISession, path: str):\n    approved_paths = {\"health\": \"/v1/health\", \"status\": \"/v1/status\"}\n    if not isinstance(session, DirectAPISession):\n        raise ValueError(\"direct API session required\")\n    if not isinstance(path, str) or path not in approved_paths:\n        raise ValueError(\"API operation rejected\")\n    try:\n        return session.request(\"GET\", \"https://api.example.com\" + approved_paths[path])\n    except ValueError:\n        raise\n    except Exception:\n        raise DirectAPIError(\"direct API request failed\") from None\n"
    }
  ]
};
