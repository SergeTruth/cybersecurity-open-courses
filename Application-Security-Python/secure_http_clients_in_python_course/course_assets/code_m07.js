window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Response Validation and Safe Parsing through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Stream a JSON response under a real byte ceiling",
      "language": "python",
      "blurb": "The client enforces media type, streamed byte limits, strict JSON decoding, and bounded JSON structure before returning remote data.",
      "code": "import json\n\nclass HTTPResponseError(ValueError):\n    pass\n\ndef unique_json_object(pairs: list[tuple[str, object]]) -> dict[str, object]:\n    result: dict[str, object] = {}\n    for key, value in pairs:\n        if key in result:\n            raise HTTPResponseError(\"duplicate JSON member\")\n        result[key] = value\n    return result\n\ndef response_content_type(response) -> str:\n    try:\n        content_type = response.headers.get(\"Content-Type\", \"\")\n    except Exception:\n        raise HTTPResponseError(\"response headers rejected\") from None\n    if not isinstance(content_type, str):\n        raise HTTPResponseError(\"response headers rejected\")\n    return content_type.partition(\";\")[0].lower()\n\ndef read_response_body(response, maximum: int) -> bytes:\n    if type(maximum) is not int or not 1 <= maximum <= 1_048_576:\n        raise ValueError(\"response byte limit rejected\")\n    body = bytearray()\n    try:\n        for chunk in response.iter_content(8192):\n            if not isinstance(chunk, (bytes, bytearray, memoryview)):\n                raise HTTPResponseError(\"response chunk rejected\")\n            if len(body) + len(chunk) > maximum:\n                raise HTTPResponseError(\"HTTP response exceeded its byte limit\")\n            body.extend(chunk)\n    except HTTPResponseError:\n        raise\n    except Exception:\n        raise HTTPResponseError(\"response body rejected\") from None\n    return bytes(body)\n\ndef validate_json_structure(document: object) -> object:\n    stack = [(document, 0)]\n    nodes = 0\n    while stack:\n        value, depth = stack.pop()\n        nodes += 1\n        if nodes > 10_000 or depth > 64:\n            raise HTTPResponseError(\"JSON response structure rejected\")\n        if isinstance(value, dict):\n            if len(value) > 256:\n                raise HTTPResponseError(\"JSON response structure rejected\")\n            for key, child in value.items():\n                if not isinstance(key, str) or len(key) > 128:\n                    raise HTTPResponseError(\"JSON response structure rejected\")\n                stack.append((child, depth + 1))\n        elif isinstance(value, list):\n            if len(value) > 1024:\n                raise HTTPResponseError(\"JSON response structure rejected\")\n            for child in value:\n                stack.append((child, depth + 1))\n        elif isinstance(value, str):\n            if len(value) > 8192:\n                raise HTTPResponseError(\"JSON response structure rejected\")\n        elif value is None or isinstance(value, (bool, int, float)):\n            continue\n        else:\n            raise HTTPResponseError(\"JSON response structure rejected\")\n    return document\n\ndef decode_json_body(body: bytes) -> object:\n    try:\n        text = body.decode(\"utf-8\")\n        document = json.loads(\n            text,\n            object_pairs_hook=unique_json_object,\n            parse_constant=lambda value: (_ for _ in ()).throw(HTTPResponseError(\"invalid JSON number rejected\")),\n        )\n    except HTTPResponseError:\n        raise\n    except (UnicodeDecodeError, json.JSONDecodeError):\n        raise HTTPResponseError(\"JSON response rejected\") from None\n    return validate_json_structure(document)\n\ndef bounded_json_response(response, maximum: int = 65_536) -> object:\n    if response_content_type(response) != \"application/json\":\n        raise HTTPResponseError(\"JSON media type required\")\n    return decode_json_body(read_response_body(response, maximum))\n"
    },
    {
      "title": "Validate a strictly versioned partner response schema",
      "language": "python",
      "blurb": "An exact nested schema requires an actual integer version, rejects Boolean-as-integer confusion and unknown fields, and bounds every partner-controlled identifier and value.",
      "code": "import re\n\nPARTNER_ORDER_ID = re.compile(r\"[A-Za-z0-9_-]{1,40}\")\n\ndef parse_partner_order(document: object) -> dict[str, object]:\n    if not isinstance(document, dict) or set(document) != {\"version\", \"order\"}:\n        raise ValueError(\"partner envelope rejected\")\n    version = document[\"version\"]\n    if type(version) is not int or version != 1 or not isinstance(document[\"order\"], dict):\n        raise ValueError(\"partner version rejected\")\n    order = document[\"order\"]\n    if set(order) != {\"id\", \"total_cents\", \"currency\"}:\n        raise ValueError(\"partner order shape rejected\")\n    order_id = order[\"id\"]\n    total_cents = order[\"total_cents\"]\n    currency = order[\"currency\"]\n    if not isinstance(order_id, str) or PARTNER_ORDER_ID.fullmatch(order_id) is None:\n        raise ValueError(\"partner order identifier rejected\")\n    if type(total_cents) is not int or not 0 <= total_cents <= 10_000_000:\n        raise ValueError(\"partner total rejected\")\n    if not isinstance(currency, str) or currency not in {\"USD\", \"CAD\"}:\n        raise ValueError(\"partner currency rejected\")\n    return {\"id\": order_id, \"total_cents\": total_cents, \"currency\": currency}\n"
    }
  ]
};
