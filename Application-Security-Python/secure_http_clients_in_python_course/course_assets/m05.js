window.COURSE_MODULE = {
  "title": "Authentication, Tokens, and Secret Handling",
  "graphicAlt": "A token travels from a vault directly into an Authorization header; nearby URL, log, exception, configuration, and metric boxes are shown without the bearer value.",
  "narration": "HTTP clients often authenticate with API keys, bearer tokens, signed tokens, OAuth-style client credentials, service accounts, workload identities, mutual TLS, or other service-specific mechanisms. These credentials determine what the Python application can do in the remote system. They should be treated as high-value secrets with controlled storage, limited access, safe injection into runtime configuration, rotation plans, and clear ownership.\n\nLeast privilege applies to API credentials just as it applies to database accounts. A reporting integration should not have broad administrative permissions if it only reads approved summaries. A notification worker should not share the credential used by deployment automation. Development, staging, and production should use separate credentials with separate scopes. If one credential is exposed, the blast radius should be understandable and limited.\n\nSecrets should be kept out of URLs, logs, traces, exception messages, debug output, process environments when unnecessary, support bundles, and broad telemetry. Authentication headers, signed values, cookies, reset links, callback secrets, and sensitive query parameters all deserve redaction. A logging statement that helps during development can become a long-lived exposure once it reaches centralized logging or a ticketing system.\n\nAuthenticating to a remote service does not prove that every response is safe or authorized for every use. The remote service may return partial data, an error, stale content, an unexpected schema, or a result that the current user should not see. Python code still needs local authorization, response validation, and safe handling of denied or ambiguous outcomes. Credentials establish access; they do not remove application responsibility.",
  "narrationPoints": [
    "HTTP clients often authenticate with API keys, bearer tokens, signed tokens, OAuth-style client credentials, service accounts, workload identities, mutual TLS, or other service-specific mechanisms.",
    "A reporting integration should not have broad administrative permissions if it only reads approved summaries.",
    "Authentication headers, signed values, cookies, reset links, callback secrets, and sensitive query parameters all deserve redaction.",
    "Python code still needs local authorization, response validation, and safe handling of denied or ambiguous outcomes.",
    "Authenticating to a remote service does not prove that every response is safe or authorized for every use.",
    "Least privilege applies to API credentials just as it applies to database accounts."
  ]
};
