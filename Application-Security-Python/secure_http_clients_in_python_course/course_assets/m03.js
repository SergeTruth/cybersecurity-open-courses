window.COURSE_MODULE = {
  "title": "Safe Request Construction",
  "graphicAlt": "A URL builder combines a fixed HTTPS authority and fixed path with one encoded identifier; credentials, alternate ports, fragments, caller paths, and a second query marker are crossed out.",
  "narration": "Safe request construction starts with knowing the approved purpose of the call. The method, URL, query parameters, headers, body, content type, and credentials should all match a specific API contract. A request that sends every available field for convenience may leak more data than necessary. A request that accepts user-controlled headers or body fields without validation may change how a remote system interprets the operation.\n\nURLs and headers should be built with structured APIs and controlled values instead of unsafe string assembly. Query parameters should be encoded as parameters, not pasted into text. Header names and values should come from known configuration or carefully validated inputs. Request bodies should have explicit shapes, size expectations, and content types. JSON, form data, and file uploads each need clear handling rules based on the remote service contract.\n\nCredentials should usually not be placed in URLs because URLs are copied widely. They may appear in logs, monitoring tools, reverse proxies, referrers, browser history in some workflows, support artifacts, exception messages, or screenshots. Secrets belong in controlled authentication mechanisms appropriate for the API, and logs should redact tokens, keys, signed values, session identifiers, and sensitive headers. The request should carry only the credentials and data needed for the approved purpose.\n\nValidation is still important for outbound data. Before sending request fields, the application should verify type, shape, length, format, allowed values, and business intent. A remote API is not a substitute for local authorization or data minimization. If a user is not allowed to trigger a transfer, send a file, look up a resource, or call a partner operation, the Python client should never construct the outbound request in the first place.",
  "narrationPoints": [
    "A request that sends every available field for convenience may leak more data than necessary.",
    "URLs and headers should be built with structured APIs and controlled values instead of unsafe string assembly.",
    "The request should carry only the credentials and data needed for the approved purpose.",
    "Before sending request fields, the application should verify type, shape, length, format, allowed values, and business intent.",
    "JSON, form data, and file uploads each need clear handling rules based on the remote service contract.",
    "The method, URL, query parameters, headers, body, content type, and credentials should all match a specific API contract."
  ]
};
