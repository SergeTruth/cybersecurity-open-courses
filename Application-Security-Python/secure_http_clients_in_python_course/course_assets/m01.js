window.COURSE_MODULE = {
  "title": "What Secure HTTP Client Usage Means",
  "graphicAlt": "An outbound HTTP request crosses destination, TLS, credential, redirect, response, and logging checkpoints before a validated object reaches application code; rejected paths branch to controlled failures.",
  "narration": "Secure HTTP client usage means treating outbound requests from Python as security-sensitive application behavior. A Python service may call partner APIs, internal services, identity providers, storage endpoints, webhook targets, billing systems, metadata services, or monitoring endpoints. Those calls can carry credentials, user data, business instructions, and assumptions about remote trust. When a client sends a request, it is not just moving bytes. It is making a decision about where to connect, what to reveal, what to accept, and how to react.\n\nAn HTTP client is different from a browser client, an API server, a proxy, or a gateway. A browser enforces browser security rules and runs in a user context. A Python HTTP client runs on the server side or inside automation, often with internal network reachability and service credentials. An API server receives and validates inbound requests. A proxy or gateway may route, inspect, or apply policy. The Python client still needs its own safe request construction, response validation, and error handling.\n\nOutbound requests can affect confidentiality, integrity, availability, and business workflows. A client might expose tokens in logs, send data to the wrong destination, trust an unexpected response, retry a state-changing request, follow a redirect across a boundary, or wait indefinitely for a slow dependency. A successful HTTP response only proves that something responded. It does not prove that the destination was approved, the endpoint identity was verified, or the response is safe to use.\n\nSecure HTTP client behavior should be designed as part of application architecture, not scattered as one-off calls. Teams should define approved destinations, transport requirements, authentication patterns, timeout and retry behavior, redirect rules, parsing expectations, logging rules, and monitoring signals. The goal is predictable outbound communication where credentials, data, and trust decisions are visible enough to review and safe enough to operate.",
  "narrationPoints": [
    "Secure HTTP client usage means treating outbound requests from Python as security-sensitive application behavior.",
    "An HTTP client is different from a browser client, an API server, a proxy, or a gateway.",
    "Outbound requests can affect confidentiality, integrity, availability, and business workflows.",
    "Secure HTTP client behavior should be designed as part of application architecture, not scattered as one-off calls.",
    "The Python client still needs its own safe request construction, response validation, and error handling.",
    "A browser enforces browser security rules and runs in a user context."
  ]
};
