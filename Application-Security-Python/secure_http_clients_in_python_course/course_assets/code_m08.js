window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Resilience, Logging, Testing, and Monitoring through concrete, reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Retry only bounded idempotent HTTP operations",
      "language": "python",
      "blurb": "The wrapper exposes only named GET operations, owns retry and transport policy, rejects caller-supplied request settings, and keeps ambient proxy routing and non-HTTPS requests outside the client.",
      "code": "import requests\nimport unicodedata\nfrom requests.adapters import HTTPAdapter\nfrom urllib.parse import urlsplit\nfrom urllib3.util.retry import Retry\n\nREAD_OPERATIONS = {\"health_check\": \"/v1/health\", \"catalog_status\": \"/v1/catalog/status\"}\nAPPROVED_HEADERS = {\"accept\": \"application/json\", \"user-agent\": \"orders-service/1\"}\nOWNED_SEND_OPTIONS = {\n    \"allow_redirects\": False,\n    \"timeout\": (2, 5),\n    \"proxies\": {},\n    \"stream\": False,\n    \"verify\": True,\n    \"cert\": None,\n}\n\nclass ResilientReadError(RuntimeError):\n    pass\n\ndef has_disallowed_url_character(value: str) -> bool:\n    for character in value:\n        category = unicodedata.category(character)\n        if character.isspace() or category[0] == \"Z\" or category in {\"Cc\", \"Cf\"}:\n            return True\n    return False\n\nclass BoundedReadSession(requests.Session):\n    def _validate_method(self, method: object) -> None:\n        if not isinstance(method, str) or method.upper() != \"GET\":\n            raise ValueError(\"approved read method required\")\n\n    def _validate_raw_url(self, url: object) -> None:\n        if not isinstance(url, str) or not 1 <= len(url) <= 2048 or has_disallowed_url_character(url):\n            raise ValueError(\"approved read URL required\")\n\n    def _validate_prepared(self, prepared) -> None:\n        self._validate_method(prepared.method)\n        self._validate_raw_url(prepared.url)\n        parts = urlsplit(prepared.url)\n        if (\n            parts.scheme != \"https\"\n            or parts.netloc != \"api.example.com\"\n            or parts.path not in set(READ_OPERATIONS.values())\n            or parts.query\n            or parts.fragment\n            or prepared.body not in (None, b\"\", \"\")\n        ):\n            raise ValueError(\"approved read request required\")\n        lowered_headers = {}\n        for name, value in prepared.headers.items():\n            if not isinstance(name, str) or not isinstance(value, str):\n                raise ValueError(\"approved read headers required\")\n            lower_name = name.lower()\n            if lower_name in lowered_headers:\n                raise ValueError(\"approved read headers required\")\n            lowered_headers[lower_name] = value\n        if lowered_headers != APPROVED_HEADERS:\n            raise ValueError(\"approved read headers required\")\n\n    def _owned_send_kwargs(self, kwargs: dict[str, object]) -> dict[str, object]:\n        unknown = set(kwargs) - set(OWNED_SEND_OPTIONS)\n        if unknown:\n            raise ValueError(\"resilient read transport options are owned by the client\")\n        for name, value in kwargs.items():\n            if value != OWNED_SEND_OPTIONS[name]:\n                raise ValueError(\"resilient read transport options are owned by the client\")\n        return dict(OWNED_SEND_OPTIONS)\n\n    def request(self, method, url, **kwargs):\n        self._validate_method(method)\n        self._validate_raw_url(url)\n        if kwargs:\n            raise ValueError(\"resilient read request options are owned by the client\")\n        return super().request(method, url, **dict(OWNED_SEND_OPTIONS))\n\n    def get(self, url, **kwargs):\n        if kwargs:\n            raise ValueError(\"resilient read request options are owned by the client\")\n        return self.request(\"GET\", url)\n\n    def prepare_request(self, request):\n        self._validate_method(getattr(request, \"method\", None))\n        self._validate_raw_url(getattr(request, \"url\", None))\n        prepared = super().prepare_request(request)\n        self._validate_prepared(prepared)\n        return prepared\n\n    def send(self, request, **kwargs):\n        self._validate_prepared(request)\n        return super().send(request, **self._owned_send_kwargs(kwargs))\n\ndef bounded_retry_session() -> BoundedReadSession:\n    retry = Retry(\n        total=3,\n        connect=2,\n        read=2,\n        status=2,\n        backoff_factor=0.2,\n        status_forcelist={429, 502, 503, 504},\n        allowed_methods={\"GET\"},\n        respect_retry_after_header=True,\n    )\n    session = BoundedReadSession()\n    session.trust_env = False\n    session.proxies.clear()\n    session.adapters.pop(\"http://\", None)\n    session.headers.clear()\n    session.headers.update({\"Accept\": \"application/json\", \"User-Agent\": \"orders-service/1\"})\n    session.mount(\"https://\", HTTPAdapter(max_retries=retry, pool_connections=4, pool_maxsize=8))\n    return session\n\nclass ResilientReadClient:\n    def __init__(self) -> None:\n        self._session = bounded_retry_session()\n\n    def get(self, operation: str):\n        if not isinstance(operation, str) or operation not in READ_OPERATIONS:\n            raise ValueError(\"approved read operation required\")\n        try:\n            return self._session.request(\"GET\", \"https://api.example.com\" + READ_OPERATIONS[operation])\n        except ValueError:\n            raise\n        except Exception:\n            raise ResilientReadError(\"resilient read request failed\") from None\n\ndef resilient_read_client() -> ResilientReadClient:\n    return ResilientReadClient()\n"
    },
    {
      "title": "Record bounded outbound-request telemetry",
      "language": "python",
      "blurb": "Metrics use an operation identifier and finite outcome category; URLs, query strings, credentials, response bodies, and raw exception messages never become dimensions.",
      "code": "HTTP_OUTCOMES = {\"success\", \"timeout\", \"tls_rejected\", \"invalid_response\", \"unavailable\"}\n\ndef record_http_result(logger, metrics, operation: str, outcome: str, elapsed_ms: int) -> None:\n    operations = {\"account_lookup\", \"health_check\", \"token_exchange\"}\n    safe_operation = operation if isinstance(operation, str) and operation in operations else \"other\"\n    safe_outcome = outcome if isinstance(outcome, str) and outcome in HTTP_OUTCOMES else \"other\"\n    if type(elapsed_ms) is not int:\n        raise ValueError(\"elapsed time rejected\")\n    duration = min(max(elapsed_ms, 0), 60_000)\n    logger.info(\n        \"outbound_http_result\",\n        extra={\"operation\": safe_operation, \"outcome\": safe_outcome, \"elapsed_ms\": duration},\n    )\n    metrics.increment(\"outbound_http_requests\", tags={\"operation\": safe_operation, \"outcome\": safe_outcome})\n"
    }
  ]
};
