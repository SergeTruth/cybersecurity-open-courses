window.COURSE_MODULE = {
  "title": "Redirects, Proxies, and Cross-Boundary Behavior",
  "graphicAlt": "Three redirect hops each pass through the same authority gate for scheme, hostname, credentials, and port; an environment proxy route is disabled and a cross-host Location is rejected.",
  "narration": "Redirects can change where a request goes after the client has already started an operation. Some redirects are expected, such as a service moving from one endpoint to another within the same trust boundary. Others may cross to a different host, scheme, port, or network zone. A secure HTTP client should define whether redirects are allowed, which destinations are acceptable, and how many redirects are reasonable for the integration.\n\nSensitive headers and credentials need special handling across redirects. A token intended for one service should not be sent to a different destination merely because a response pointed there. Depending on the client and configuration, headers may be preserved, stripped, or changed. Teams should understand the behavior of their HTTP client and avoid assuming that automatic redirect handling is safe for every integration.\n\nProxy configuration can also be security-relevant. Corporate proxies, service meshes, API gateways, environment-driven proxy variables, and deployment routing can affect where requests travel, who can observe metadata, which certificates are used, and how failures appear. A Python application should know whether proxy behavior is part of the intended architecture or accidental inherited environment state. Proxy settings can be helpful, but they should be visible enough for review.\n\nConnection reuse, cookies, and cross-environment behavior deserve attention in non-browser clients. Connection pooling can improve performance, but credentials, destinations, and TLS expectations should remain clear. Cookies may appear in some HTTP client workflows even though the client is not a browser. A staging configuration should not accidentally send production tokens through a development proxy. Cross-boundary behavior should be reviewed whenever requests move between environments, services, or trust zones.",
  "narrationPoints": [
    "Redirects can change where a request goes after the client has already started an operation.",
    "Depending on the client and configuration, headers may be preserved, stripped, or changed.",
    "A Python application should know whether proxy behavior is part of the intended architecture or accidental inherited environment state.",
    "Cookies may appear in some HTTP client workflows even though the client is not a browser.",
    "Connection pooling can improve performance, but credentials, destinations, and TLS expectations should remain clear.",
    "Cross-boundary behavior should be reviewed whenever requests move between environments, services, or trust zones."
  ]
};
