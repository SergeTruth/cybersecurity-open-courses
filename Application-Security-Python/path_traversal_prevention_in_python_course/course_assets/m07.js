window.COURSE_MODULE = {
  "title": "Framework and Deployment Patterns",
  "graphicAlt": "Control-flow diagram for Framework and Deployment Patterns, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "Python frameworks provide file-handling patterns, but those patterns still need deliberate use. Flask applications may use helpers for sending files or serving uploaded content. Django separates static assets, media files, and storage backends. FastAPI and Starlette offer response classes and integration points for file responses. These tools can reduce custom code, but they do not automatically authorize resources, choose safe storage names, or enforce tenant boundaries.\n\nReverse proxies and web servers also influence file safety. Some deployments serve static assets directly, while application code controls private downloads. Some use internal redirects or storage service signed URLs. Each pattern has a different trust boundary. The application must ensure that public assets are meant to be public and that private files require authorization before any web server, proxy, or storage service can serve them.\n\nDeployment layout matters. Containers may have read-only filesystems, mounted volumes, writable temporary directories, or shared storage. Serverless runtimes may provide temporary storage with lifecycle limits. Cloud object stores may use keys instead of filesystem paths, but key construction can still be influenced by unsafe input if the design is careless. Environment-specific configuration should be reviewed so development shortcuts do not become production behavior.\n\nThe durable recommendation is to use framework helpers and storage services as part of a clear storage architecture, not as shortcuts around it. Keep storage roots configured, not user-supplied. Keep public and private content separate. Keep runtime credentials least-privileged. Keep direct file reads out of routes unless they are carefully constrained. A framework helper is safest when the surrounding design is also safe.",
  "narrationPoints": [
    "Python frameworks provide file-handling patterns, but those patterns still need deliberate use.",
    "FastAPI and Starlette offer response classes and integration points for file responses.",
    "Some use internal redirects or storage service signed URLs.",
    "The durable recommendation is to use framework helpers and storage services as part of a clear storage architecture, not as shortcuts around it.",
    "Each pattern has a different trust boundary.",
    "Keep runtime credentials least-privileged."
  ]
};
