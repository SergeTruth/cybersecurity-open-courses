window.COURSE_MODULE = {
  "title": "Safe Path Construction and Canonicalization",
  "graphicAlt": "Layered architecture diagram for Safe Path Construction and Canonicalization, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "Safe path construction starts from a trusted base directory or storage root chosen by the application, not from a complete path supplied by a user. The application should combine trusted configuration with controlled identifiers or approved names using path-aware APIs. String concatenation is fragile because path values have structure. Separators, relative segments, absolute forms, drive or root indicators, encoding differences, and platform behavior can all change how a final path is interpreted.\n\nCanonicalization is the process of resolving a path into a consistent form so it can be compared or checked more reliably. At a high level, teams often normalize a path, resolve relative meaning, and compare the result to an approved base. This can help identify whether the final path remains inside the intended storage location. Canonicalization is useful, but it is not a complete design by itself. It must be paired with clear storage policy, allowlists, and authorization.\n\nA base-directory containment check asks a simple question: after the path is resolved according to the platform and storage model, is the final location still inside the approved directory? That check should happen before sensitive reads or writes, and the code should handle missing files, invalid names, and permission errors safely. A safe failure should not expose internal paths or automatically fall back to a broader location.\n\nPath APIs are preferable to ad hoc text manipulation because they represent paths as paths. They still require judgment. Developers must understand whether links are followed, when resolution touches the filesystem, how errors are handled, and whether a later operation can target something different from what was checked. Safe path construction is a pattern: trusted base, controlled input, path-aware joining, resolved containment, authorization, and careful use.",
  "narrationPoints": [
    "The application should combine trusted configuration with controlled identifiers or approved names using path-aware APIs.",
    "Separators, relative segments, absolute forms, drive or root indicators, encoding differences, and platform behavior can all change how a final path is interpreted.",
    "Canonicalization is the process of resolving a path into a consistent form so it can be compared or checked more reliably.",
    "Canonicalization is useful, but it is not a complete design by itself.",
    "A safe failure should not expose internal paths or automatically fall back to a broader location.",
    "Safe path construction is a pattern: trusted base, controlled input, path-aware joining, resolved containment, authorization, and careful use."
  ]
};
