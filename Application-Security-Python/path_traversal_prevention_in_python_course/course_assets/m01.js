window.COURSE_MODULE = {
  "title": "What Path Traversal Prevention Means",
  "graphicAlt": "Diagram for What Path Traversal Prevention Means, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "Path traversal prevention is about controlling how a Python application decides which files and directories it may access. The risk appears when untrusted input influences a file operation in a way the application did not intend. That can happen in web applications, APIs, command-line tools, upload systems, download endpoints, report generators, static file handlers, background workers, data pipelines, and maintenance scripts. Any code path that reads, writes, copies, deletes, extracts, or serves files has a filesystem security boundary.\n\nFile access is not the same as authorization, storage policy, or operating system permissions, although all of them interact. A user may be allowed to download one report but not another. A process may have operating system permission to read a directory that the application should never expose. A framework helper may serve files safely when used as intended, while direct file reads can bypass the policy the team thought was in place. Secure design needs all of these layers to agree.\n\nThe damage from unsafe file access can be serious. Depending on the application and deployment, unintended file access may expose configuration data, source code, logs, uploaded user files, generated reports, database backups, internal artifacts, credentials, or application secrets. It may also allow writes to locations that change application behavior or corrupt data. The exact impact depends on the environment, which is why prevention should not rely on guessing which files might be sensitive.\n\nThe strongest prevention starts with design: do not let users directly control filesystem paths. Let users select objects, documents, reports, or display names, while the application controls storage names and locations. Safe file access should be explicit, constrained, reviewable, and tested. The goal is predictable file behavior where every access is authorized, contained inside approved locations, and understandable during review or incident response.",
  "narrationPoints": [
    "Path traversal prevention is about controlling how a Python application decides which files and directories it may access.",
    "Any code path that reads, writes, copies, deletes, extracts, or serves files has a filesystem security boundary.",
    "A process may have operating system permission to read a directory that the application should never expose.",
    "The damage from unsafe file access can be serious.",
    "Let users select objects, documents, reports, or display names, while the application controls storage names and locations.",
    "The goal is predictable file behavior where every access is authorized, contained inside approved locations, and understandable during review or incident response."
  ]
};
