window.COURSE_CODE_MODULE = {
  "title": "Code Examples",
  "codeIntro": "These examples apply Framework and Deployment Patterns with reviewable Python controls.",
  "codeExamples": [
    {
      "title": "Serve only repository-selected files in Flask",
      "language": "python",
      "blurb": "The route treats the URL segment as an object identifier and gives send_file a path retrieved from an authorization-aware service.",
      "code": "from flask import abort, send_file\nimport re\nfrom uuid import UUID\n\nSUBJECT_ID = re.compile(r\"sub_[A-Za-z0-9_-]{8,64}\\Z\")\nSTORAGE_KEY = re.compile(r\"[a-f0-9]{32}\\Z\")\nDOWNLOAD_NAME = re.compile(r\"[A-Za-z0-9][A-Za-z0-9 ._-]{0,119}\\Z\")\nMEDIA_TYPES = {\"application/pdf\", \"image/jpeg\", \"image/png\", \"text/plain\"}\n\ndef download_file(file_id: str, current_user, files):\n    try:\n        object_id = str(UUID(file_id))\n    except (AttributeError, TypeError, ValueError):\n        abort(404)\n    subject_id = getattr(current_user, \"id\", None)\n    if not isinstance(subject_id, str) or SUBJECT_ID.fullmatch(subject_id) is None:\n        abort(404)\n    find_for_subject = files.find_for_subject\n    open_file = files.open\n    if not callable(find_for_subject) or not callable(open_file):\n        raise TypeError(\"file-service contract rejected\")\n    record = find_for_subject(file_id=object_id, subject_id=subject_id)\n    if record is None:\n        abort(404)\n    storage_key = getattr(record, \"storage_key\", None)\n    download_name = getattr(record, \"safe_download_name\", None)\n    media_type = getattr(record, \"media_type\", None)\n    if (\n        not isinstance(storage_key, str)\n        or STORAGE_KEY.fullmatch(storage_key) is None\n        or not isinstance(download_name, str)\n        or DOWNLOAD_NAME.fullmatch(download_name) is None\n        or media_type not in MEDIA_TYPES\n    ):\n        raise RuntimeError(\"authorized file metadata rejected\")\n    stream = open_file(storage_key)\n    return send_file(\n        stream,\n        as_attachment=True,\n        download_name=download_name,\n        mimetype=media_type,\n    )\n"
    },
    {
      "title": "Delete through an application storage interface",
      "language": "python",
      "blurb": "The route passes opaque identifiers to storage policy instead of translating a user path into a recursive filesystem operation.",
      "code": "import re\nfrom uuid import UUID\n\nSUBJECT_ID = re.compile(r\"sub_[A-Za-z0-9_-]{8,64}\\Z\")\nSTORAGE_KEY = re.compile(r\"[a-f0-9]{32}\\Z\")\n\ndef delete_user_document(document_id: str, subject, repository, storage) -> None:\n    try:\n        object_id = str(UUID(document_id))\n    except (AttributeError, TypeError, ValueError):\n        raise ValueError(\"document identifier rejected\") from None\n    subject_id = getattr(subject, \"id\", None)\n    if not isinstance(subject_id, str) or SUBJECT_ID.fullmatch(subject_id) is None:\n        raise ValueError(\"subject identifier rejected\")\n    lock_document = repository.lock_authorized_document\n    mark_deleting = repository.mark_deleting\n    mark_deleted = repository.mark_deleted\n    delete_object = storage.delete_object\n    if not all(\n        callable(operation)\n        for operation in (lock_document, mark_deleting, mark_deleted, delete_object)\n    ):\n        raise TypeError(\"document-deletion adapter contract rejected\")\n    document = lock_document(\n        document_id=object_id,\n        subject_id=subject_id,\n        permission=\"delete\",\n    )\n    if document is None:\n        raise FileNotFoundError(\"document not found\")\n    trusted_id = getattr(document, \"id\", None)\n    storage_key = getattr(document, \"storage_key\", None)\n    if (\n        trusted_id != object_id\n        or not isinstance(storage_key, str)\n        or STORAGE_KEY.fullmatch(storage_key) is None\n    ):\n        raise RuntimeError(\"authorized document metadata rejected\")\n    mark_deleting(trusted_id)\n    delete_object(storage_key)\n    mark_deleted(trusted_id)\n"
    }
  ]
};
