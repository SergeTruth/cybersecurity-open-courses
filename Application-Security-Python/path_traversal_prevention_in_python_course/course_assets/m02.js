window.COURSE_MODULE = {
  "title": "Filesystem Trust Boundaries and Data Flow",
  "graphicAlt": "Flowchart for Filesystem Trust Boundaries and Data Flow, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "A file-related value should be treated according to where it came from. Trusted application directories are usually chosen by configuration, deployment, or storage policy. Untrusted values may arrive as filenames, path parameters, query parameters, upload metadata, archive entries, database-stored references, generated report names, webhook data, queue messages, or user-provided labels. Some values look harmless because they came from a database, but that database may have stored user input earlier.\n\nPython applications often move file-related data through several layers before a file operation occurs. A route receives a request, a service object looks up a record, a storage adapter builds a location, and a framework response serves content. A background job may read the same reference later. Reviewers need to follow the data from origin to use. The important question is not whether a variable is named filename or path. The question is whether its contents can be influenced outside the trusted boundary.\n\nOperating system path behavior adds complexity. Platforms differ in separators, absolute path formats, relative path handling, case sensitivity, reserved names, links, mounted volumes, and normalization rules. Containers, serverless runtimes, and cloud platforms add their own layout assumptions. A design that relies on ad hoc string checks can break when deployment changes. A durable design uses path-aware APIs, clear base directories, and containment checks that match the environment.\n\nUnderstanding trust boundaries helps teams decide where controls belong. Before reading, writing, copying, deleting, extracting, or serving a file, the application should know what subject is acting, what resource is being requested, where the storage record came from, which directory is approved, and which operation is allowed. File workflows should be designed like security-sensitive data flows, not like simple text manipulation.",
  "narrationPoints": [
    "A file-related value should be treated according to where it came from.",
    "Python applications often move file-related data through several layers before a file operation occurs.",
    "Reviewers need to follow the data from origin to use.",
    "Platforms differ in separators, absolute path formats, relative path handling, case sensitivity, reserved names, links, mounted volumes, and normalization rules.",
    "A durable design uses path-aware APIs, clear base directories, and containment checks that match the environment.",
    "Operating system path behavior adds complexity."
  ]
};
