window.COURSE_MODULE = {
  "title": "Testing, Logging, Monitoring, and Remediation",
  "graphicAlt": "Operational lifecycle diagram for Testing, Logging, Monitoring, and Remediation, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "Path traversal prevention should be tested at the level of the design. Unit tests can cover path construction, identifier validation, base-directory containment, missing files, invalid names, and storage adapter behavior. Integration tests can exercise upload and download endpoints, object-level authorization, tenant boundaries, generated reports, and background file workflows. Negative tests should confirm that unsafe path-shaped inputs are rejected or treated as ordinary data without demonstrating offensive techniques.\n\nCode review should follow file-related data from entry to operation. Reviewers should ask where the filename, ID, archive entry, storage key, or temporary location came from. They should check whether the application controls the base directory, whether containment is enforced, whether authorization happens before access, whether framework helpers are used correctly, and whether errors avoid leaking sensitive paths or file contents. Review should include jobs and scripts, not only web routes.\n\nLogging and monitoring should support investigation without creating new leaks. Logs may record safe event names, file IDs, storage areas, tenant context, authorization outcomes, error categories, and correlation IDs. They should avoid secrets, tokens, credentials, private file contents, full sensitive paths, and unnecessary customer data. Monitoring can watch for unusual file access errors, repeated denied downloads, unexpected archive failures, or access attempts outside normal workflow patterns.\n\nIf files are exposed, remediation should be concrete. Identify which files, users, tenants, logs, and storage locations were affected. Revoke or rotate exposed secrets when needed. Disable unsafe routes or workflows while fixes are prepared. Redesign storage references, add containment checks, strengthen authorization, clean up logs, and add regression tests. Prevention must be maintained as file workflows, frameworks, storage services, and deployment layouts evolve.",
  "narrationPoints": [
    "Path traversal prevention should be tested at the level of the design.",
    "Negative tests should confirm that unsafe path-shaped inputs are rejected or treated as ordinary data without demonstrating offensive techniques.",
    "Review should include jobs and scripts, not only web routes.",
    "Monitoring can watch for unusual file access errors, repeated denied downloads, unexpected archive failures, or access attempts outside normal workflow patterns.",
    "Disable unsafe routes or workflows while fixes are prepared.",
    "Revoke or rotate exposed secrets when needed."
  ]
};
