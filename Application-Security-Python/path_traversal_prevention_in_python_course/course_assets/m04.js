window.COURSE_MODULE = {
  "title": "Allowlisted Identifiers and Storage Abstraction",
  "graphicAlt": "Sequence diagram for Allowlisted Identifiers and Storage Abstraction, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "The best file workflows often avoid user-controlled paths altogether. Instead of accepting a path, the application accepts an opaque file ID, report ID, attachment ID, or storage record identifier. The application then looks up a trusted record that contains the approved storage location, owner, tenant, lifecycle state, content type, and authorization context. The user chooses the resource, not the server path.\n\nAllowlisted identifiers reduce ambiguity. A safe identifier has a known format and maps to a record the application controls. Generated storage names reduce the chance that a display name can influence directories or backend behavior. Users may still choose friendly display names, but those names should be metadata, not storage instructions. Separating display names from storage names also makes it easier to rename files without moving them or weakening controls.\n\nStorage abstraction helps when the backing store changes. A file may live on a local disk, mounted volume, object storage service, document store, or generated report repository. The application should expose a consistent storage policy rather than leak filesystem layout into routes and templates. A well-designed storage layer can enforce allowed locations, safe naming, metadata validation, and consistent access checks across frameworks and jobs.\n\nAllowlists should be used where names, extensions, content categories, or storage areas are constrained. A broad blocklist of dangerous-looking names is brittle because file systems and storage services have many edge cases. A narrow allowlist for expected identifiers, generated names, approved extensions, and known storage records is easier to review. The practical goal is to make unsafe paths impossible to express through the public interface.",
  "narrationPoints": [
    "The best file workflows often avoid user-controlled paths altogether.",
    "The user chooses the resource, not the server path.",
    "Users may still choose friendly display names, but those names should be metadata, not storage instructions.",
    "The application should expose a consistent storage policy rather than leak filesystem layout into routes and templates.",
    "A broad blocklist of dangerous-looking names is brittle because file systems and storage services have many edge cases.",
    "Allowlisted identifiers reduce ambiguity."
  ]
};
