window.COURSE_MODULE = {
  "title": "Course Summary and Key Takeaways",
  "graphicAlt": "Summary diagram for Course Summary and Key Takeaways, showing untrusted path input, identifier map, trusted storage root, open file descriptor, and denied escape path; labeled arrows identify filesystem containment and authorization checks and the point where unsafe input or behavior is rejected.",
  "narration": "Path traversal prevention in Python protects file operations from untrusted path influence. The central design principle is simple: users should not directly control where the application reads, writes, serves, deletes, extracts, or copies files. Python code should treat filenames, path parameters, upload metadata, archive entries, database-stored references, and generated file names according to their source and trust level.\n\nStrong designs use trusted base directories, path-aware APIs, canonicalization, base-directory containment checks, opaque identifiers, allowlisted storage records, generated storage names, and clear separation between display names and storage names. They also enforce authorization before file access, protect tenant boundaries, use framework and storage helpers deliberately, and avoid leaking internal paths through responses, logs, or errors.\n\nFile safety depends on environment details as well as code. Operating system behavior, symbolic links, archive formats, temporary files, mounted volumes, containers, serverless runtimes, object storage keys, proxies, and web servers can all affect outcomes. A design that is safe in one deployment can become fragile if assumptions are undocumented. File workflows should be explicit enough to review and test as the platform changes.\n\nThe goal is predictable, authorized, auditable, and constrained file access. Good prevention reduces the chance of exposing private files, secrets, logs, source code, customer data, or internal artifacts. It also makes incidents easier to investigate and remediate. Treat file access as a security boundary, not a convenience string operation, and the application becomes safer and easier to maintain.",
  "narrationPoints": [
    "Path traversal prevention in Python protects file operations from untrusted path influence.",
    "Python code should treat filenames, path parameters, upload metadata, archive entries, database-stored references, and generated file names according to their source and trust level.",
    "File safety depends on environment details as well as code.",
    "File workflows should be explicit enough to review and test as the platform changes.",
    "The goal is predictable, authorized, auditable, and constrained file access.",
    "Treat file access as a security boundary, not a convenience string operation, and the application becomes safer and easier to maintain."
  ]
};
