window.COURSE_MODULE = {
  "title": "Conditional Probability Tables and Factorization",
  "graphicAlt": "Small Bayesian network connected to local conditional probability tables and a factored joint distribution.",
  "narration": "For discrete variables, Bayesian networks often use conditional probability tables, or CPTs. A discrete variable has a set of named states, such as true or false, high or low, working or failed. A root node has no parents, so it uses a prior probability distribution. For example, Weather might have probabilities for sunny, cloudy, and rainy before considering any parent variable.\n\nA child node has probabilities conditioned on its parents. Suppose a simple network has Weather and Sprinkler as parents of Wet Ground. The probability of Wet Ground depends on whether the weather is rainy and whether the sprinkler is on. The CPT for Wet Ground has a row for each relevant combination of parent states. Each row should form a valid probability distribution. In a true-or-false row, the probability of true plus the probability of false should sum to one.\n\nThe graph lets the joint probability distribution be factored into local terms. One compact way to write the idea is: the probability of all variables together equals the product of each variable's probability given its parents. In words, instead of building one enormous table that lists every possible state combination, the Bayesian network breaks the model into smaller local relationships. That is one reason Bayesian networks are useful for practical modeling.\n\nFactorization is not magic compression. It depends on the conditional independence assumptions in the graph. If the graph leaves out an important dependency, the factorized model can be misleading. Also, not every Bayesian network uses simple CPTs. Continuous or hybrid networks may use Gaussian distributions, regression-style conditional models, or other local probability distributions. The core idea remains the same: local distributions, organized by the graph, define the larger probabilistic model.\n\nIn practice, CPT design is often a modeling bottleneck. Too many parents can create a table with many rows, and each row needs probability values that mean something. Modelers sometimes simplify states, combine variables, or use structured conditional distributions to keep the model manageable. The goal is not to make the largest possible table. The goal is to represent the relationships at the level of detail needed for the reasoning question.",
  "narrationPoints": [
    "Root nodes use prior probability distributions.",
    "Child nodes use probabilities conditioned on parents.",
    "CPT rows must form valid probability distributions.",
    "The joint distribution factors into local terms.",
    "Continuous networks may use distributions instead of tables."
  ]
};
