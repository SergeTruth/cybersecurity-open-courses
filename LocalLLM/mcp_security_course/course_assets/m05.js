window.COURSE_MODULE = {
  "title": "Tool and Server Risk Management",
  "graphicAlt": "Blank course placeholder for module 5.",
  "narration": "MCP tools should be treated like software integrations with permissions, owners, and risk ratings. A tool inventory should record what each tool does, who owns it, what server exposes it, what permissions it requires, what downstream systems it reaches, what data it can access, and whether it is read-only or write-capable. A server inventory should capture provenance, installation source, maintainer, dependencies, version, approval status, and whether the server is trusted for the current environment.\n\nTrusted and untrusted servers should not receive the same treatment. A locally reviewed internal server that exposes narrow read-only queries is different from an unknown server installed from an unreviewed source. Remote servers also introduce transport, authentication, tenant isolation, and service availability considerations. Local servers may introduce file access, command execution, process access, or workstation data exposure. Both models can be useful, but both need governance.\n\nTool descriptions are security-relevant metadata. They may guide the model's decision about when and how to call a tool. Descriptions should be reviewed for accuracy, scope, unsafe language, hidden assumptions, and mismatch between advertised behavior and actual permissions. Allowlisting can limit which tools or operations are available in a given context. Version pinning and dependency review can reduce supply-chain drift. Disabling unused tools reduces the attack surface.\n\nOperational controls should match tool risk. Low-risk read-only lookup tools may need standard logging and authorization. Write-capable tools may need argument validation, approval gates, rate limits, transaction previews, dry-run modes, and rollback planning. Tools that touch local files, cloud resources, email, code repositories, tickets, or production systems deserve stronger review. The security habit is to ask what authority the tool carries, not just what friendly name appears in the client.",
  "narrationPoints": [
    "MCP tools should be treated like software integrations with permissions, owners, and risk ratings.",
    "Trusted and untrusted servers should not receive the same treatment.",
    "Tool descriptions are security-relevant metadata.",
    "Operational controls should match tool risk."
  ]
};
