window.COURSE_MODULE = {
  "title": "Identity, Authorization, and Consent",
  "graphicAlt": "Blank course placeholder for module 4.",
  "narration": "Identity is one of the most important design choices in an MCP system. A user may ask for an action, the AI host may decide how to assist, an MCP server may expose the tool, and a downstream API may enforce permissions. Those layers should not be collapsed into one vague identity. User authorization is different from application authorization. A service account is different from a delegated user token. A model-generated request is different from verified user intent.\n\nDelegated access lets an application act on behalf of a user within defined limits. OAuth-style concepts such as scopes, audience, token lifetime, and consent are useful mental models even when the exact implementation varies. Scope should describe what an integration can do. Audience should constrain where a token can be used. Consent should make the user or administrator aware of meaningful access. Token lifetime should limit exposure if credentials are mishandled.\n\nCareless token passthrough can blur trust boundaries. If a powerful user token is passed through multiple components, tools, or servers without clear policy, components may gain access they do not need and users may not understand what is being authorized. Safer designs separate user intent from model-generated action requests, issue narrowly scoped credentials where possible, keep secrets out of prompts and logs, and enforce authorization at the application or service boundary.\n\nMCP tools should not gain permission merely because a model asks for them. The model can propose an action, but policy must decide whether the action is allowed. High-impact actions should require explicit consent or approval. Permission checks should consider the user, the tool, the resource, the requested operation, the data sensitivity, and the current context. Consent is not a decorative prompt; it is a control that helps connect user intent to authorized action.",
  "narrationPoints": [
    "Identity is one of the most important design choices in an MCP system.",
    "Delegated access lets an application act on behalf of a user within defined limits.",
    "Careless token passthrough can blur trust boundaries.",
    "MCP tools should not gain permission merely because a model asks for them."
  ]
};
