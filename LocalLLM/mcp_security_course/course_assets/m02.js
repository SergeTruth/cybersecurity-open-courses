window.COURSE_MODULE = {
  "title": "MCP Architecture and Trust Boundaries",
  "graphicAlt": "Blank course placeholder for module 2.",
  "narration": "Security work starts with mapping the MCP environment. The diagram should show the AI host application, MCP client, MCP servers, tools, resources, prompt templates, user identity, service identity, downstream APIs, local files, cloud services, secrets, and tokens. It should distinguish local servers from remote servers because local execution and remote service access create different risk profiles. A local server may touch files or processes on the user's machine, while a remote server may rely on network trust, tenant isolation, and external service controls.\n\nA useful architecture diagram shows data flow, authority flow, and action flow. Data flow answers what information enters the model context, tool arguments, logs, resources, and downstream services. Authority flow answers which identity or token is used when a tool acts. Action flow answers what can be read, written, sent, created, deleted, approved, or changed. These flows do not always line up. A user may ask a question, the model may propose an action, and a tool may execute under a service account with broader permissions than the user expected.\n\nTrust boundaries appear between user input, model output, tool metadata, tool execution, external content, and downstream systems. User input is not automatically safe. Model output is not automatically authorized. Tool descriptions are not neutral; they influence how the model thinks about tool use. External resources may contain stale, misleading, or hostile instructions. Downstream systems may enforce their own policies, but the MCP application still decides what to request and what context to expose.\n\nArchitecture visibility prevents vague risk discussions. Instead of saying an agent can use tools, the team can say which host connects to which servers, which tools are available, which resources are exposed, which credentials are used, and where approvals happen. That clarity supports least privilege, monitoring, incident response, and governance. Without it, teams end up trusting the model, the tool, the server, and the user interface as one blended boundary, which is exactly what MCP security needs to avoid.",
  "narrationPoints": [
    "Security work starts with mapping the MCP environment.",
    "A useful architecture diagram shows data flow, authority flow, and action flow.",
    "Trust boundaries appear between user input, model output, tool metadata, tool execution, external content, and downstream systems.",
    "Architecture visibility prevents vague risk discussions."
  ]
};
