window.COURSE_MODULE = {
  "title": "Threat Modeling MCP Systems",
  "graphicAlt": "Blank course placeholder for module 3.",
  "narration": "Threat modeling an MCP system means separating the instruction path, the data path, and the action path. The instruction path includes user prompts, system instructions, tool descriptions, prompt templates, and external content that may influence model behavior. The data path includes resources, files, retrieval results, tool responses, logs, and downstream records. The action path includes tool calls that can read, write, send, modify, approve, or trigger activity in other systems.\n\nPrompt injection is a high-level risk where untrusted instructions influence model behavior in a way the system did not intend. Indirect prompt injection is especially relevant because the model may read external content from documents, tickets, web pages, emails, repositories, or resources exposed through MCP. The training point is not to memorize payloads. The stable lesson is that external content should be treated as data with provenance and policy, not as trusted instructions that can override application rules.\n\nTool metadata can also create risk. A malicious or misleading tool description may shape when the model chooses a tool or what arguments it sends. Overbroad permissions can turn a narrow task into broad access. Confused deputy risk appears when a component with legitimate authority is tricked into using that authority for an unintended purpose. Supply-chain risk appears when a server, dependency, package, or installation path is trusted without enough review.\n\nA practical MCP threat model asks three blunt questions: what can the agent read, what can it call, and what can it change? Then it tests the answers against misuse scenarios such as data leakage, unauthorized actions, stale or untrusted resources, malicious server registration, over-trusting model output, and approval bypass. The resulting controls should live outside the model: authorization checks, allowlists, validation, approvals, isolation, logging, and incident response.",
  "narrationPoints": [
    "Threat modeling an MCP system means separating the instruction path, the data path, and the action path.",
    "Prompt injection is a high-level risk where untrusted instructions influence model behavior in a way the system did not intend.",
    "Tool metadata can also create risk.",
    "A practical MCP threat model asks three blunt questions: what can the agent read, what can it call, and what can it change?"
  ]
};
