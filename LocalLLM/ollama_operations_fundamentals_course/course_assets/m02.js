window.COURSE_MODULE = {
  "title": "Installation, Service Lifecycle, and Local Runtime",
  "graphicAlt": "Preview bullet summary visual for installation, service lifecycle, and local runtime.",
  "narration": "An Ollama installation should be treated as a managed service, not just a one-time tool download. Teams should know how it was installed, how the service starts, how it stops, which user account it runs under, and where its runtime state lives. Those details matter when troubleshooting a failed startup, a missing model, a permission problem, or a configuration mismatch between machines.\n\nService lifecycle practices are especially important when a local tool becomes part of shared work. A developer workstation, a lab server, and a production-like host may all need different settings. A workstation may favor flexibility. A shared server may need predictable startup, controlled network exposure, documented storage, and support expectations. A production-like host may require tighter change control and monitoring.\n\nEnvironment variables and service configuration should be documented because small differences can change behavior. Storage locations, bind addresses, model cache behavior, runtime directories, and service users can all affect operations. If one machine behaves differently from another, the answer is often in configuration rather than in the model itself.\n\nLogs and service status checks are the first place to look when the runtime does not behave as expected. Clear lifecycle practices make upgrades, restarts, troubleshooting, and support easier. They also help new team members understand the environment without depending on tribal knowledge.\n\nIt is also helpful to separate development assumptions from shared-service assumptions. A developer may restart a local service freely, change settings for a test, or remove models to save space. A shared host needs a more predictable process so active users, dependent applications, and support staff understand what is changing and when.",
  "narrationPoints": [
    "An Ollama installation should be treated as a managed service, not just a one-time tool download.",
    "Service lifecycle practices are especially important when a local tool becomes part of shared work.",
    "Environment variables and service configuration should be documented because small differences can change behavior.",
    "Logs and service status checks are the first place to look when the runtime does not behave as expected.",
    "It is also helpful to separate development assumptions from shared-service assumptions."
  ]
};
