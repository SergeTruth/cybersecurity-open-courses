window.COURSE_MODULE = {
  "title": "Embeddings, Indexes, and Vector Stores",
  "graphicAlt": "Draft visual summary for Embeddings, Indexes, and Vector Stores",
  "narration": "Embeddings and indexes are sensitive infrastructure in a private RAG system. An embedding is a mathematical representation used to support similarity search, but it is derived from source content. When the source material is sensitive, the embedding, nearby metadata, and index records deserve careful treatment as part of the data architecture.\n\nVector stores may hold embeddings, chunk text, document IDs, tenant labels, permission tags, source references, and lifecycle metadata. They are not just performance caches. They are systems that influence which evidence reaches the model and which users can discover source-derived material.\n\nCollection and namespace design should follow data sensitivity, ownership, tenant boundaries, and query patterns. Mixing unrelated tenants or sensitivity levels into one broad index can make permission enforcement and lifecycle management harder. Separate collections are not always required, but the design should make access rules clear and testable.\n\nMetadata filters should be deliberate. Tenant identifiers, classification labels, lifecycle status, source type, effective dates, and permission fields often become retrieval controls. If those fields are incomplete or inconsistent, the retrieval layer may select content that is stale, irrelevant, or inappropriate for the workflow.\n\nIndex refresh and deletion behavior matter. Documents change, permissions move, projects close, and old guidance expires. The architecture should define how indexes are rebuilt, updated, backed up, restored, and purged. Administrative access to indexes should be protected because index operators may influence retrieval behavior and source-derived data availability.\n\nTeams should document what each index contains, who owns it, how it is refreshed, and what operational signals show that it is healthy. That record helps reviewers treat the vector store as a managed data system rather than a hidden implementation detail.",
  "narrationPoints": [
    "Embeddings and indexes are sensitive infrastructure in a private RAG system.",
    "Vector stores may hold embeddings, chunk text, document IDs, tenant labels, permission tags, source references, and lifecycle metadata.",
    "Collection and namespace design should follow data sensitivity, ownership, tenant boundaries, and query patterns.",
    "Metadata filters should be deliberate.",
    "Index refresh and deletion behavior matter."
  ]
};
